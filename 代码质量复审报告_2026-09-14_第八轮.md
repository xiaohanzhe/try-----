# 第八轮 · 行为缺陷治理 实施与验证报告

日期：2026-09-14　项目：`ralsei_pet`（Deltarune Ralsei 桌面宠物，Windows + PyQt5）
范围：用户逐条反馈的行为缺陷（对话框 / 坠落 / 甩飞 / 特殊动画）

---

## 0. 结论

本轮把用户反馈里**属于"动画与运动"的部分全部闭环**（10 条），并留下两条可长期执行的闸门
（动画来源白名单、G2 回归基线）。剩余 4 条属于"对话内容 / 窗口摆放 / 聊天框重做"，
已明确划到下一批（#15）单独做，本报告清单里逐条标注。

回归结论：**G2 = 11 套件 / 253 PASS / 0 FAIL，全部 IDENTICAL**（无一行为漂移）。

提交记录（main → origin/main）：

| 提交 | 内容 |
| --- | --- |
| `024b6aa` | #10 对话框 ▼ 闪烁不再撑高抖动 + 滚动位置不被弹回顶部 |
| `cc9471d` | #11 坠落/摔倒误判（按"建楼要求"：窗口→桌面才坠落；楼层身份用稳定标识） |
| `654da19` | #12 斜抛（方向由松手速度定）+ 空中可二次抓住（线速度缓冲减速）+ 卡动画自检 |
| 本轮 | #13 特殊动画治理（只 AI 触发 / 播完不打断不移动 / 待机 3 分钟 / 鞠躬锚点） |

---

## 1. 用户反馈逐条状态

| # | 用户原话（节选） | 状态 | 落点 |
| --- | --- | --- | --- |
| 1 | 对话框死对话内容要全面 AI 接管 | **未做** | #15a（需剥离规则引擎，独立排期） |
| 2 | 对话框 ▼ 闪烁导致文字上下抖 | 已修 | `024b6aa` |
| 3 | 对话框没法下拉 | 已修 | `024b6aa` |
| 4 | 坠落偶尔无触发也掉到屏幕底 | 已修 | `cc9471d` |
| 5 | 摔倒别"看到窗口就摔"，看"建楼要求"文档 | 已修 | `cc9471d` |
| 6 | 甩飞是斜抛，轨迹由初速度方向和大小决定 | 已修 | `654da19` |
| 7 | 甩飞偶尔卡在动画里不继续 | 已修 | `654da19` |
| 8 | 特殊动画只交给 AI 判断，别抽风式突然播 | 已修 | 本轮 #13a |
| 9 | 特殊行动（躲猫猫等）也同上 | 已修 | 本轮 #13a（躲猫猫/施法本就由 AI 发起；环境触发已收编） |
| 10 | 播放就播完，不打断、不边播边移动 | 已修 | 本轮 #13b |
| 11 | 待机动画需原地不动 ≥3 分钟才播 | 已修 | 本轮 #13c |
| 12 | 坠落中能二次抓住，且有对线速度的减速 | 已修 | `654da19` |
| 13 | bilibili 类网站打开后放在不碍眼处；开了 PPT 类工作软件就不开 | **未做** | #15c |
| 14 | 鞠躬动画像镜头在移动 | 已修 | 本轮 #13d |
| 15 | 聊天框尽量像原作，但**别直接在现代码改** | **未做** | #15b（另起原型，不动 `dialogue_ui.py`） |
| 16 | 原作有多人同时对话，按原作对话框模样预留 | **未做** | #15b |

---

## 2. #13a 特殊动画"只交给 AI"——为什么光改两处不够

用户的要求是**来源级**的：特殊动画（除 idle/walk/run）只能由 AI 决定是否播放。
因此先做了一次全量侦察，而不是只改已知的两处。

工具：`code-quality-audit/第八轮/scan_anim_triggers.py`（AST 只读，随仓库保留）。
它列出 `main.py` 中所有 `play_animation_once` / `change_animation` 调用点**及其归属函数**
——报告当时共 **94 个调用点 / 43 个函数**。

按"谁会触发它"分类后，除已改的 `update_animation_by_emotion`（空实现）与 `on_mouse_hover`
（空实现）之外，**还有 2 个环境自动触发点在自行播放特殊动画**：

| 触发点 | 触发条件 | 原行为 |
| --- | --- | --- |
| `react_to_desktop_element` | `update_movement` 每 5 秒 `check_nearby_desktop_elements`，凑近桌面图标 | 直接 `play_animation_once(look_up/act/surprised/wave)` —— 站着突然抬头、比划 |
| `_react_to_video` | 陪看视频时每 5 秒 10% 概率 | 直接 `play_animation_once(laugh/dance/surprised…)` |

这正是用户说的"抽风似的突然一下"。修法不是删掉反应，而是**把规则系统从"演员"降级为"眼睛"**：

- `ai_driver` 新增环境事件通道 `note_event(text, emotion_hint)` → 队列 `_pending_events`（上限 6 条），
  `_build_prompt` 会把它渲染成 `- 刚刚：…` 交给模型，**看过一次就消费掉**；
  系统提示词同步增加一段"要不要回应由你决定，不回应完全没问题"。
- `main.py` 新增 `_note_desktop_observation()`：把"我凑近了桌面上的「报告.pptx」（看起来很脆、金属的），
  心里有点想抬头看看"上报给 AI。`reaction` 里的动作名不再被执行，只作为**心痒提示**。
- AI 未启用时事件只是静静攒着，**没有任何可见行为**（符合第六轮定下的"AI 关闭必须沉默"）。

同时留下**长期闸门**（`verify_round8_anim.py` H1.1）：断言所有 `play_animation_once` 调用点的
归属函数都在白名单内（用户显式交互 / 物理状态机 / AI 决策）。将来谁再加一个"看窗口就挥手"
的自动触发，这条断言会立刻失败，强迫过一遍评审。

> 仍未处理：这两个触发点**附带的文案**仍由规则引擎生成（`react_to_desktop_element` 的
> 易碎/重量/温度模板、`_react_to_video` 的内置台词表），属缺陷 1（对话全 AI 接管）的范围，
> 代码里已用 `TODO(#15 …)` 标注，本轮不动。

---

## 3. #13b 播完不打断、不边播边移动

- 分类常量 `_NON_SPECIAL_ANIM_GROUPS`；`_is_special_anim(name)` 判定"是不是特殊动画"
  （idle/walk/run/jump/fall/splat/land/hatless_throw/slide/roll/spell/item 之外的都是）。
- `change_animation` 增加关键拦截：**特殊动画播放期间，另一个特殊动画一律拒绝，`force=True` 也拒绝**
  （重复请求同一个动画名按"重播"处理，不算打断）。`play_animation_once` 用 `_play_once_arming`
  只放行自己那一次。
- `update_movement` 增加移动锁：`_special_anim_locked()` 为真时 `current_speed=0` 并 return。
  位置放在**拖拽保护/鼠标跟随之前**——因为那些分支都会 `self.move(...)` 后 return，
  不拦住就会出现"一边摆手一边平移"。**只暂停本 tick 的移动，不清掉跟随意图**，播完照常继续跟。
- 非特殊动画（走路）仍可打断特殊动画，避免"卡在动作里出不来"。

---

## 4. #13c 待机动画需原地静止 ≥3 分钟

原实现是**死分支**：

```python
if self.idle_timer >= 180.0: new_animation = "idle"
else:                        new_animation = "idle"     # 两分支同值
```

所以那个"3 分钟判断"从未生效，一停下就走 idle 的 5 帧循环——正是用户说的"停止就播"。

现在：常量 `IDLE_LOOP_MIN_SECONDS = 180.0`；`update_animation` 里
`self._idle_loop_active = self.idle_timer >= IDLE_LOOP_MIN_SECONDS`；
帧推进（`_need_advance`）只在 `_idle_loop_active` 时允许，静止分支把帧**钉在第 0 帧**。
行为级验证：静止 10 秒 → 一直停在第 0 帧；静止 200 秒 → 帧正常推进。

---

## 5. #13d 鞠躬"像镜头在动"——根因是素材锚点

不是动画本身抖动，而是**各姿势素材的画布尺寸不一致**：`idle` 画布 69×47，但角色 alpha
包围盒只有 (1,6,27,40)，右侧留了 41px 透明。切换动画时按画布左上角贴图 → 角色水平位移
（缩放 2 倍后 40px）→ 观感"镜头在动"。

修法：新增 `_anim_anchor_offset(animation)`（按 alpha 包围盒中心算偏移，动画级缓存）与
`_compose_anchored_sprite(...)`（把已缩放精灵放进透明画布并按包围盒中心落位），
两处渲染分支改用它。实测 `idle` 偏移 `(-20, +2.5)`，其余动作 `(0, 0)`；
修好后**所有动画的角色屏幕位置差 ≤1px**（修前 idle↔其它差 40px）。

---

## 6. 验证与回归

### 6.1 本轮套件

`code-quality-audit/第八轮/verify_round8_anim.py`：**28/28 PASS**（证据 `_evidence/round8_anim.txt`）

| 组 | 覆盖 | 结果 |
| --- | --- | --- |
| D0–D3 | 锚点：素材尺寸、偏移值、合成后包围盒中心=画布中心、跨动画位置一致、渲染分支接入 | 8/8 |
| E1 | 待机 3 分钟：常量、静止 10s 停第 0 帧、静止 200s 帧推进、死分支已除 | 4/4 |
| F1 | 只交给 AI：悬停随机已删、`update_animation_by_emotion` 函数体只剩裸 `return`、情绪不再驱动动画 | 5/5 |
| G1 | 播完不打断：分类、起播、拒绝打断、force 也拒、非特殊放行、播放期不移动、移动锁顺序 | 7/7 |
| H1 | 来源闸门：白名单无越权、两个环境触发点已收编、`note_event` 通道可用 | 4/4 |

### 6.2 G2 全量回归

```
解释器 = C:\Python311\python.exe
suite            exit   PASS   FAIL   比对
round5_smoke     0      22     0      IDENTICAL
round5_verify    0      16     0      IDENTICAL
round6_verify    0      39     0      IDENTICAL
round7_launch    0      5      0      IDENTICAL
s1_anim_miss     0      17     0      IDENTICAL
s2_anim_json     0      40     0      IDENTICAL
s3_alias_legacy  0      19     0      IDENTICAL
round8_dialogue  0      20     0      IDENTICAL
round8_floor     0      16     0      IDENTICAL
round8_fling     0      31     0      IDENTICAL
round8_anim      0      28     0      IDENTICAL      ← 本轮新增
合计：PASS=253 FAIL=0  套件=11
```

### 6.3 顺带修掉的**工具链缺陷**（本轮踩到并已修）

1. **回归入口会用错解释器**：套件若用托管 venv（Python 3.13）跑，看不到 3.11 的用户
   site-packages → `round5_smoke` 报 `No module named 'bs4'` **假 FAIL**，且一批套件输出漂移。
   现在 `run_all.py` 按 `REGRESS_PYTHON` → `C:\Python311\python.exe` 自动探测（探针
   `import PyQt5, bs4`），并在表头打印所用解释器。
2. **Qt 环境噪声污染基线**：`round8_dialogue` 的旧基线录进了 112 行
   `QFontDatabase: Cannot find font directory…`，而同一份代码之后不再打印 → 出现**假 DIFF**。
   现在归一化阶段会过滤这类 Qt 插件噪声行（QFontDatabase / This plugin does not support / qt.qpa 等）。

---

## 7. 验证脚本自身的教训（已写成断言规范）

本轮 3 条断言曾误判，**全部是测试写错**，产品代码是对的。教训已固化进脚本注释：

1. **别用 `"字面量" in 源码` 做源码级断言**——注释和文档字符串会误命中：
   `# if self.idle_timer >= 180.0:`（反面例子注释）与 docstring 里提到的
   `random.random() < 0.1` 都被算成"代码还在"。现在统一走 `code_only()`
   （`tokenize` 剥掉 COMMENT/STRING 再扫）。
2. **顺序断言必须限定在目标函数体内**：`MAIN_SRC.index("…")` 取到的是**其它函数**里更早出现的
   同一个字符串 → 顺序判反。现在用 `func_src('update_movement')` 再比。
3. 行为级断言优先：`update_animation_by_emotion` 直接用 AST 断言"函数体只剩一个裸 `return`"，
   比字符串扫描硬得多。

---

## 8. 待办（#14 / #15）

- **#14**：把本轮新契约（动画来源白名单、移动锁顺序、待机门限）沉淀进 G2 断言集，随下次改动一起过。
- **#15（独立排期，用户已知）**：
  - (a) **对话全 AI 接管**：剥离 `dialogue_system.py` 规则引擎、`dialogue_ui.py` 的 `cmds[]`
    与文件命令回复、`main.py` 硬编码 `add_dialogue`（含 `react_to_desktop_element`、
    `_react_to_video`、`check_entertainment_needs`）、`autonomous_agent` 的 `ACTION_DIALOGUES`、
    `emotion_system` 死代码；重定义"AI 关闭"策略（现状：保持沉默）。
  - (b) **原作风格聊天框原型**：**单独写，不碰 `dialogue_ui.py`**；按原作样式预留**多人同时对话**。
  - (c) **完整"建楼"楼层系统**（`floor_manager` 未用 API）+ **bilibili 窗口摆放**
    （视频友好、不碍眼；检测到 PPT 类工作软件则不开）。

---

## 9. 风险与边界

- 环境事件通道目前是"单次消费"：AI 未启用时事件会攒在队列里（上限 6 条，自动丢最旧），
  不会堆积成内存问题，也不会产生可见行为。
- `react_to_desktop_element` / `_react_to_video` 的**文案仍是规则生成的**——在 #15a 之前，
  它们仍会在 AI 关闭时说话。这是已知的、刻意的中间状态（动画已先行收口）。
- 真机限制：窗口是透明非置顶 FramelessWindow，鼠标会穿透 → 甩飞/抛物线只能做
  **离屏物理断言**，无法真机复现；#13d 的位置一致性由离屏像素断言保证。
