# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 运行时产物 / 日志
*.log
# 轮转日志（如 ralsei_pet.log.2026-09-12）：*.log 不匹配，需单独一条
*.log.*
*.err.log
debug.log
output.log
run*.log
smoke_test*.log
startup_test.log
tmp_diag.log
diag*.log
animation_history.json
coordinates.json
*.png.txt

# 配置备份与损坏副本
config.json.backup.*
config.json.corrupt.*
*.bak
*.backup*
*.orig

# 诊断截图 / 帧产物
diag_frames/
diag_frames2/
screenshot*.png

# 系统
desktop.ini
Thumbs.db
.DS_Store

# G2 回归基线的临时产物（baseline.json 要入库，_out/ 是每次运行的原始输出与 diff）
code-quality-audit/regress/_out/

# 各轮次的临时侦察/调试日志（只留可复算的脚本 + 报告，不留散落日志）
code-quality-audit/*/_recon/
# 自检脚本的临时沙箱（临时记忆文件等，绝不该入库）
code-quality-audit/*/_evidence/tmp*/
code-quality-audit/*/_evidence/_tmp*/
