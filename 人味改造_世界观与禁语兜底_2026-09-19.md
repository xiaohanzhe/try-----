# 人味改造 · 补 Deltarune 世界观 + 对话链路禁语兜底

> 轮次：2026-09-19（承接「人味改造」线第四轮）
> 用户口径：「一定要保证三角符文本地的世界观也存在，也就是他知道他该知道的游戏内容这类的」
> 结论：**两处产品缺口已补，G2 全量回归 PASS=1092 / FAIL=0 / 24 套件 / 全 IDENTICAL。**

---

## 1. 这轮修了什么（两个缺口）

### 缺口一：Ralsei 有"人设"却没有"世界观"

改前 persona 只说了**他是谁**（黑暗世界的王子、被带到电脑上陪主人、Kris/Susie 在别的世界），
一句都没说**他知道什么**。后果：问到"你是从哪来的""黑暗世界是什么样""你认识 Susie 吗"，
他只能靠模型的通用知识瞎编 —— 而 4B 模型对 Deltarune 的"记忆"高概率是错的
（把 Fanlore、把 UNDERTALE 设定、把同人当正史）。

**做法：把世界观写进 persona 单一真源**（`ralsei_pet/assets/ralsei_persona.md`，新增
「我知道的世界」节，28 行）。因为它是 system 前缀的**唯一来源**，写这里 = 每次对话都带着。

**关键：内容不取我的记忆，取原作语料本身。** 用
`code-quality-audit/人味改造-2026-09-18/analyze_ralsei_worldview.py` 从
`_evidence/ralsei_lora_corpus.jsonl`（853 条 Ralsei 原台词，ch1–ch4）统计他**自己真的提到过**
哪些实体 —— 只写他真说过的，不写我"觉得他该知道"的。

| 实体 | Ralsei 台词命中 | 实体 | Ralsei 台词命中 |
|---|---|---|---|
| Susie | 120 | prophecy 预言 | 6 |
| Kris | 116 | Lightner 光之民 | 6 |
| Lancer | 9 | Castle Town 城堡镇 | 4 |
| Queen | 9 | hero 英雄 | 5 |
| Mr. Tenna（电视先生） | 6 | Fountain 喷泉 | 15 |
| Knight 骑士 | 3 | Darkner 暗之民 | 7 |
| King 黑桃国王 | 2 | Roaring 咆哮 | 1 |

写进 persona 的世界观覆盖：世界有两面（光明世界 / 黑暗喷泉长出的黑暗世界）·
光之民与暗之民 · 两位光之英雄的预言（且**删减版**，真结尾他藏着）· Kris / Susie / Lancer /
黑桃国王 / Queen / Tenna · 走过的地标（黑桃城堡、城堡镇、电脑世界、电视世界、教堂底下的
黑暗避难所）· 骑士与咆哮 · **他瞒过同伴**（ch4 的核心动机，也是他性格的来源）。

#### 世界观节的写法约束（防"设定倾倒"反效果）

写进去 ≠ 好。设定写多了角色会变成维基百科播报机。所以节内白纸黑字加了三道使用约束：

1. **主人不问就不主动讲**；问到了再像聊过去一样慢慢说，一次别倒太多；
2. **别把话题硬拐到那边去**（主人聊今天吃什么，不要拐到黑暗世界）；
3. 用词**不许出现元游戏词**（玩家 / 存档 / 读档 / 通关 / 第四面墙）——
   一旦出现，他就从"角色"退化成"旁白"，世界观反而更假。

**Ch3–4 的设定名（电视世界 / 黑暗避难所等）先经 WebSearch 核实再写入**，不凭印象。

### 缺口二：persona 明令不说的两类话，对话链路一句都不查

**§4.2 的产品缺口。** persona 第 57~63 行白纸黑字写着"出戏的话、客服腔的话我不说"，
事件链路（S7）也早已有 `_OOC_PATTERNS` / `_BANNED_PATTERNS` 两道代码闸 ——
但**用户天天用的对话链路 `_clean_ai_reply` 一个字都不查**。

证据：3B 时代实测连着回「早安，有什么我可以帮忙的吗？」（见
`_evidence/probe_vividness.txt`）。提示词**不是保证**。

**做法：在 `_clean_ai_reply` 里补 0c 步**，命中判无效 → 走既有"换说法重采样"路径
（不是直接沉默，保留重试机会）。判据**不另抄一张表**，直接从 `modules.event_speech`
导入 `looks_out_of_character` / `looks_like_assistant_speak` —— **同源，杜绝两处漂移**。

#### 顺序锁（H4 那类坑的预防）

0c 步**必须排在 0b（剥 markdown）之后**：否则 `**有什么可以帮你的吗**` 里的星号先被剥掉才匹配，
反而更容易漏（实际是"先剥格式再匹配更干净"）。已由 **C22** 断言锁定顺序语义，**H4 反向控制**
断言 `main.py` 里不存在第二份 `_BANNED_PATTERNS` / `_OOC_PATTERNS`。

---

## 2. 回归结果（G2 全量 · 逐字节）

```
解释器 = C:\Python311\python.exe
合计：PASS=1092 FAIL=0  套件=24          全 IDENTICAL
```

关键套件：

| 套件 | PASS | 比对 | 本轮的变 |
|---|---|---|---|
| `persona_chat` | **88**（← 75） | IDENTICAL | +13 条锁（A13–A17, C20–C23, H1–H4），基线已按"有意变更"重建 |
| `s7_event_speech` | 135 | IDENTICAL | 未动（禁语判据是**被导出方**，行为不变） |
| `s8_stream` | 69 | IDENTICAL | 未动 |
| 其余 21 套件 | — | IDENTICAL | 未动 |

原始输出：`_evidence/G2_回归_24套件_1092PASS_2026-09-19.txt`

### 新增的 13 条锁（都在 `persona_chat`）

- **A13–A17（世界观契约）**：有「我知道的世界」节 · 七个核心设定锚齐备
  （黑暗喷泉/光之民/暗之民/两位光之英雄/Kris/Susie/Lancer）· 带使用约束
  （"主人不问"/"别把话题硬拐"）· 有"瞒过同伴" · **负控制**：无元游戏词。
- **C20–C23（禁语兜底行为级）**：客服腔判无效 · 出戏判无效 · 包裹加粗的客服腔同样拦得住
  （顺序锁）· **负控制**：正常台词原样放行（正/负成对 —— 否则"一律判无效"也能让前三条变绿）。
- **H1–H4（接线）**：`_clean_ai_reply` 真的调用了两个闸 · 判据从 `event_speech` 导入
  （"写对了"≠"产品用上了"）· **反向控制**：main.py 无第二份表。

---

## 3. 改动清单

| 文件 | 改动 |
|---|---|
| `ralsei_pet/assets/ralsei_persona.md` | +28 行「我知道的世界」节（世界观 + 使用约束） |
| `ralsei_pet/src/main.py` | import 补两个判据 + `_clean_ai_reply` 新增 0c 步 + docstring 补 0a/0c |
| `code-quality-audit/人味改造-2026-09-18/verify_persona_chat.py` | +13 条锁（A13–A17 / C20–C23 / H1–H4）+ 分组说明 |
| `code-quality-audit/人味改造-2026-09-18/analyze_ralsei_worldview.py` | **新建**，corpus 统计取证脚本 |
| `code-quality-audit/regress/run_all.py` | 更新 `persona_chat` 的 desc（补世界观 + 禁语闸表述） |
| `code-quality-audit/regress/baseline.json` | persona_chat 基线重建（75 → 88 PASS） |

**未改代码**：`modules/event_speech.py` —— 禁语判据本轮是被**导出方**，本身行为不变
（`s7_event_speech` 保持 135 PASS / IDENTICAL 即证）。

---

## 4. 证据（`_evidence/`）

| 文件 | 说明 |
|---|---|
| `ralsei_worldview_2026-09-19.txt` | corpus 世界观实体统计（本报告表格的可复算来源） |
| `G2_回归_24套件_1092PASS_2026-09-19.txt` | G2 全量逐字节回归原始输出 |
| `ralsei_lora_corpus.jsonl` | 853 条 Ralsei 原台词语料（统计输入） |

编码校验：本轮新增/修改的 5 个文件 **RC=0，无 BOM，零损坏**。
（`_evidence/` 全目录 CRC 有 4 个历史告警，全部是 857/925 PASS 时代的旧文件，非本轮产生。）

---

## 5. 搁置项（用户明确指示）

- **Ollama 真机世界观 A/B 行为探针**（A≠B 硬闸）：用户「现在还没办法实测」→ **暂缓**。
  即：世界观节在真机上"他实际会不会说、会不会退化成设定倾倒"，本轮**没有真机验证**，
  只做到了 **契约级（persona 内容）+ 单元级（禁语判据行为）** 的锁。
  真机验证待用户可实测时进行。
- 日志滚动 bug（`E:\RalseiMemory\logs\ralsei_pet.log` 在 exFAT 上 `os.rename` 撞已存在目标
  → `WinError 1`，日志永不滚动）：**独立基础设施缺陷**，与本次改动无因果，本轮未修，
  已记录待后续轮次。
