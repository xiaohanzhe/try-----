# Ralsei 桌宠 · 深度复审报告（第二轮 · 2026-09-12）

- 审查对象：`ralsei_pet/`（`src/main.py` 8500+ 行 + `modules/` 17 个模块，约 2.3 万行）
- 审查方法：全项目编译 + pyflakes 静态检查 + 自建 AST 扫描（属性/重复/资源/槽保护）+ 4 路并行模块审查 + **逐条独立复现** + 90s/60s 运行时冒烟
- 本轮结论：**确认并修复 27 处缺陷（其中 5 处属"功能从未真正生效"、3 处属"启动即崩"、4 处属"用户直接可见的行为错误"）**，另有 20 余项已定位但未改动的问题列在第四节。

---

## 一、已修复缺陷（全部含复现证据）

### A. "功能写好了却从未生效"类（最隐蔽，用户以为有、其实没有）

| # | 位置 | 问题 | 证据 | 修复 |
|---|---|---|---|---|
| 1 | `main.py` update_mouse_drag | `win32api` 在本函数作用域内**从未导入**（只在新写的 2868/7597 行各自 import 过），调用必然 NameError，又被 `except Exception` 静默吞掉 | pyflakes：`undefined name 'win32api'`；需求第 6 条"拖动鼠标"完全无效 | 补 import + `QCursor.setPos` 兜底 |
| 2 | `main.py` mouse_drag_timer | 定时器是 `setSingleShot(True)` 且 `start(drag_duration*1000)`＝**只回调一次**，回调时 `elapsed >= drag_duration` 立即走"结束"分支 | 光标从头到尾一步都没移动过（即便修好 #1 也一样） | 改周期定时器 16ms 驱动，`stop_mouse_drag` 显式停止 |
| 3 | `main.py` / `sprite_loader` | `start_fall` 用 `if "fall_mad" in sprites` 判断是否播放"生气摔倒"，但该动画键**从未在 sprite_loader 配置过** → 判断恒假 | 素材 `spr_cutscene_24e_ralsei_splat_mad.png` 本来就在；需求"'我'的行为导致摔倒要用生气那个"从未生效 | 新增 `fall_mad`/`splat_mad` 映射；`fall_from_window` 分支接入 |
| 4 | `main.py` 顶部 | `search_summarizer` 整段被注释掉（"因为缺少 bs4"），联网摘要模块成为**不可达死代码** | 需求第 5 条"能上网"缺一环 | 改可选导入 + 条件初始化；`requirements.txt` 补 `beautifulsoup4` |
| 5 | `main.py` 渲染路径 | 每帧用**当前帧**精灵尺寸 `setGeometry`，而同动画各帧尺寸并不一致（`run_right` 5 种、`act` 10 种） | 窗口每帧 resize + 按中心回算位置 → 形象逐帧抖动、位置微移 | 改用"本动画所有帧的最大尺寸"作固定容器（label 已 AlignCenter） |

### B. 启动/运行崩溃类

| # | 位置 | 问题 | 复现 | 修复 |
|---|---|---|---|---|
| 6 | 全局 | **没有安装 `sys.excepthook`**，PyQt5 在定时器/信号槽里遇到未捕获异常会调 `qFatal()` | 实测：默认 excepthook 下进程以 `0xC0000409` 退出；自定义 hook 下存活。本项目有 10+ 个高频定时器（30ms/100ms） | 新增 `install_crash_guard()`，在 QApplication 之前安装，异常写日志+c`rash.log` 且不再终止进程 |
| 7 | `config_manager` | 配置节被写成标量时（`"api":"oops"`），`_merge_configs` 用标量覆盖字典节 → `.get()` 抛 AttributeError，而 except **不含** AttributeError | 实测 `{"api":"oops"}`/`{"api":5}` 均抛异常逃逸 → 桌宠双击后完全不出现 | merge 层拒绝非 dict 覆盖字典节；api 节类型兜底；except 扩展 TypeError/AttributeError |
| 8 | `social_growth_system` | `evolution_path` 成员判断要求可哈希，写成数组即 TypeError，except 不含 TypeError | 实测 `["default"]` → `TypeError: unhashable type` → 构造即失败 | 类型校验 + except 扩展 |
| 9 | `desktop_interaction` | `SHFileOperation` **失败不抛异常**，只返回 `(errcode, aborted)`，代码不校验就宣布"已移至回收站" | 实测不存在路径返回 `(124, False)` | 校验 errcode，非 0 则返回 False 并保留文件 |

### C. 用户直接可见的行为错误

| # | 位置 | 问题 | 复现 | 修复 |
|---|---|---|---|---|
| 10 | `energy_hunger.eat` | 结束进食只挂在"饥饿档位跨越"事件上；饥饿度已 >80 时喂食 → 档位永不变 → `is_eating` 永真、饥饿度钉死 100，饥饿系统彻底失效 | 30 tick 后仍 `hunger=100.0 / is_eating=True` | 收尾改独立判定 + `eat()` 预拦截（"已经吃得饱饱的啦"） |
| 11 | `emotion_system` | excited/expectant/grateful/caring/hopeful/energetic 全部由 happy 线性推导，又被用来反哺 happy ⇒ `happy += c·happy` 指数自增 | `add_emotion('happy',30)` → 12 个 tick（2 分钟）锁死 100 且永不回落（动画恒 dance、表情恒 happy_extremely） | 切断 6 处闭环反哺（只保留"惊讶→开心"这类非派生来源） |
| 12 | `emotion_system` | `_update_complex_emotions` 每 tick 全量覆写 21 个复合情绪，事件写入的复合情绪 ≤10 秒被清零 | shy+45 → 0；disappointed 35 → 0；grateful 25 → 6.9 | 取"推导值"与"残余（按 0.2/秒消退）"的较大者 |
| 13 | `emotion_system` | `relieved` 既不参与推导也不衰减，永久驻留并霸占主导情绪（而它没有 animation/face 映射 → 永久 idle） | 33 分钟后仍 25.0 | 同上（未参与推导的情绪按速率衰减） |
| 14 | `energy_hunger`/`emotion_system` | 时间增量`current-last`未做下限钳位，系统时间回拨（NTP/夏令时/手改表）即反向跳变 | 回拨 1 小时：hunger 70→100、energy 80→100、sad 50→100 | `max(0.0, ...)` |
| 15 | `dialogue_ui.show_dialogue` | 无条件调用 `stop_typing()`，而 `stop_typing` 的语义是"立刻补完整句"；main.py 有 150+ 处 `add_dialogue()+show_dialogue()` | 逐字打字机**100% 失效**、台词瞬间全出、打字音效从不播放 | 移除该调用（hide/新消息路径仍会正确打断） |
| 16 | `dialogue_ui` 渲染 | 先 `html.escape` 整串再按字符切片显示 | 打字过程中闪出 `&am`/`&lt`/`&quot` 残片 | 保留原文逐字显示，渲染那一刻才转义 |
| 17 | `dialogue_ui` | 光标闪烁（530ms）每次都 `sb.setValue(maximum())` | 用户上翻历史每 0.5 秒被弹回底部 | 仅在内容增长时自动跟随 |
| 18 | `dialogue_system` | `"喜欢"` 分支排在 `"不喜欢"` 之前 | 实测 "我不喜欢狗" → "我也喜欢！这真的很有趣呢！"（语义完全相反） | 否定分支前置 |
| 19 | `dialogue_ui` | 游戏进行中说"退出游戏"被闲聊指令 `("游戏",…)` 截获 | 石头剪刀布中无法用最自然的说法退出 | 游戏进行中放行退出词给游戏状态机 |
| 20 | `desktop_interaction` | 用 `GetDetailsOf(item, 15)` 判"是否文件夹"，而索引 15 是"年"列（类型列是 2） | 实测 74 个桌面项：真实文件夹 26 个，判据命中 **0** → 文件夹全被当成文件 | 改用 `item.IsFolder` + `os.path.isdir` 双保险 |
| 21 | `floor_manager` | 用 dict **内容相等**定位"当前楼层"下标；floor 每秒整体重建，一旦不等则 index=-1，`range(0,len)` 变成"从最顶层开始找落点" | 宠物从窗口边掉下会被判成落到更高的窗口（凭空"上吸"） | 改用稳定标识（hwnd/'desktop'）+ 高度退化定位 |
| 22 | `weather_system` | "shower" 在 snow 分支之前命中 | "Snow Showers" → rainy（下雪说成下雨） | 先判雪再判雨 |
| 23 | `desktop_interaction` | 网络检测在主线程阻塞（每 10 秒 1 秒）；窗口平滑移动用 `time.sleep` 合计冻结约 3 秒 | 断网时周期性卡顿；移动 B 站窗口时界面 3 秒无响应 | 超时降到 0.3s + 缓存 30s；循环内 `processEvents()` |

### D. 其他

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 24 | `config_manager` | 备份清理只在"新建备份"时触发，而该路径极少走到 → 程序目录积压 **106 个** `config.json.backup.*`，承诺的"最多保留 10 个"从未兑现 | 启动时主动剪枝一次 |
| 25 | `requirements.txt` | 缺 `beautifulsoup4`（联网摘要依赖） | 补上并注明缺失时自动降级 |
| 26 | `main.py` | 拖动定时器停止后未 `stop()`（改周期后必须显式停） | 已补 |
| 27 | `main.py` | `_README` 中承诺的"固定容器尺寸"实现缺失 | 见 #5 |

---

## 二、五维度结论

### 1. 隐藏 bug
本轮最高价值的发现都属于这一类：**代码写得像是对的、日志也看不出问题，但功能从未生效**（#1~#4、#10~#15、#20、#21）。
共同特征是"**静默降级 + 判据恒真/恒假**"：`if X in sprites` 而 X 从未配置、`GetDetailsOf(item,15)` 而列索引错、`except Exception: log.debug` 把 NameError 吞掉。
→ 建议后续任何"存在性判断"都要有"两个分支都可达"的验证。

### 2. 边界条件
- **时间**：系统时间回拨/夏令时（#14）——所有基于 `now - last` 的增量都要钳位。
- **数值**：饥饿/精力满值时的状态机收敛（#10）。
- **数据文件**：JSON 被手改/截断/类型错（#7、#8）——启动期任何 `in`/`.get()`/`+` 都要防类型。
- **空/None**：文档 1600 行的 `.lower()` 之类已在前几轮修过，本轮补充了配置节与楼层对象。
- **多显示器/小屏**：仍用 `QApplication.desktop().availableGeometry()`（单屏近似），未改（见第四节）。

### 3. 重复编码
- 已量化：`main.py` 中 12 个 `check_*`（IT 助手提醒）**函数体结构完全相同**，只有文案与表情不同，合计约 230 行模板代码；`_recover_from_sadness`/`_recover_from_fear` 也同构。
- `sprite_loader` 的 `_fetch_results`、`config_manager` 的 `_ensure_nested` 等此前已收敛；本轮未新增重复。
- `main.py:11` 重复 `import time`、`desktop_interaction` 内 5 处重复 `import shutil`（无害，建议清理）。
- 未改动（避免大范围重构引入风险），建议按数据驱动方式合并，见第四节。

### 4. 未来拓展会踩的坑
1. **`main.py` 上帝类（8500 行）**：窗口/动画状态机/物理/施法/游戏/托盘/模块编排全在一个类里，任何新功能都往里加。
2. **模块靠 `self.parent.xxx` 反向伸手**：main 改个属性名会静默波及所有模块。
3. **两套 `platform_height` 尺度**（`z_order*5` vs 可见窗口序号*5）被 main.py 混用。
4. **三套配置文件并存**（config.json / customization_config.json / memory.json / growth_data.json）无统一版本与迁移机制。
5. **素材路径/动画名散落在代码与 sprite_loader 两处**，加动画要记得同步（#3 就是这么踩的）。
6. **死代码与未接入模块**：`pet_interaction.py` 整模块、多个 `*_DEPRECATED`、`update_floor`、`send_api_request`、`entertainment_system` 全部游戏方法（导致等级/成就链路实际不运行，等级恒为 1）。

### 5. 异常处理完整性
- 本轮新增了**进程级兜底**（#6），这是此前最大的缺口：之前任何槽异常 = 桌宠无声消失。
- `except Exception` 里继续使用异常细节（`%s`）已成惯例，静默 `pass` 仅剩 8 处且均有注释说明原因。
- 仍需注意："捕获后返回 False/None，但调用方不检查返回值"（本轮修掉了 #9、#24 两例）。

---

## 三、验证证据（可复现）

`code-quality-audit/复审_2026-09-12/` 下保留了本轮全部可重跑脚本：

| 脚本 | 作用 |
|---|---|
| `verify_fixes.py` | 17 项断言：异常兜底、`fall_mad` 接入、鼠标拖动定时器 |
| `verify_dialogue.py` | 打字机生效 / 无 HTML 实体残片 / 滚动条不抢焦点 |
| `repro_emotion.py`、`repro_emotion2.py` | 喂食卡死、happy 自激、时钟回拨、复合情绪覆盖、relieved 驻留 |
| `verify_config.py` | 5 种畸形 config.json 均安全降级 |
| `verify_desktop.py` | Shell 列索引、SHFileOperation 返回码、ctypes restype |
| `scan_resources.py`/`scan_dup.py`/`scan_attrs.py` | 表情/动画/事件覆盖度、重复函数体、未定义属性 |
| `smoke_run.py` | 60~90 秒真实运行冒烟（本轮结果：**0 未处理异常**，动画集合中出现 `fall_mad`） |

其他命令：
```powershell
python -m compileall -q ralsei_pet                 # 全项目编译：通过
python -m pyflakes ralsei_pet\modules ralsei_pet\src\main.py   # 无 undefined name
```

---

## 四、已确认但本轮未改动的问题（建议排期）

**高价值（用户可感知）**
1. **25 个已触发事件在 `react_to_event` 无分支** → 系统 CPU/内存/电量/磁盘/断网告警、备份结果、回收站、打开 Deltarune 视频等，Ralsei 全无反应。
2. **成就系统**：30 个成就里 17 个永不可解锁（`level_10/20`、`game_master`…），`game_master` 的判定还是"玩过任意游戏 4 次"；等级/成就链路实际不运行（`entertainment_system` 无调用点，等级恒为 1）。
3. **记忆系统**：`update()` 先删后提取，导致"升级/成就"这类只发生一次的重要记忆 10 秒内被清掉，长期记忆恒为空；`remember_user_behavior` 双重包装后 `KeyError`；`get_associated_memories` 依赖不存在的 `id` 字段恒返回空。
4. **对话"系统状态"被意图快路径短路**：问"内存占用多少/电池电量"只会得到一句空话，`get_system_resources`/`clean_temp_files` 等真实分支永不执行。
5. **`dialogue_history[-1]` 恒为 ralsei**，"上一句是用户提问"分支永不成立。

**中低**
6. 图标真实位置探测：ctypes 未声明 `restype=c_void_p`（64 位地址被截断），且 `OpenProcess(0x38)` 实测返回 0 → 桌面图标交互坐标实际走随机回退。
7. `EnumWindows` 回调内裸调 `GetClassName/GetWindowRect`，窗口中途销毁会中断整轮枚举（楼层表静默变短）。
8. 隐私文件类型表包含 `.txt/.md/.py` 等几乎所有常见类型 → `get_file_content_preview` 实际永远返回"这是隐私文件"。
9. 隐私豁免机制死代码（`mark_app_as_opened/closed` 无调用点）。
10. `customization_config.json` 非原子写、损坏后静默回退且不备份/不修复。
11. `api_client` 解析了 `max_retries/retry_delay` 但 `_post_json` 无重试。
12. `sprite_loader` 缺图时占位图被当作有效帧（`if frame:` 对 QPixmap 恒真），素材目录缺失时 Ralsei 变成灰问号且只有 debug 日志。
13. 素材自动分组帧号重复不去重（`dance2` 与 `dance_2` 同记第 2 帧），顺序依赖 `os.listdir`。
14. `ppt_control` 对已关闭的 Presentation 重复 `Close()` → 操作成功却返回失败。
15. 12 个 `check_*` 模板方法与若干死代码（建议数据驱动 + 清理，见第二节）。

---

## 五、修改文件清单

| 文件 | 主要变更 |
|---|---|
| `ralsei_pet/src/main.py` | 全局异常兜底；鼠标拖动两处修复；摔倒动画分支；动画固定容器尺寸；搜索模块可选导入与初始化 |
| `ralsei_pet/modules/sprite_loader.py` | 新增 `fall_mad`/`splat_mad` 动画映射 |
| `ralsei_pet/modules/energy_hunger.py` | 进食/休息独立收尾；`eat()`/`rest()` 预校验；时间增量钳位 |
| `ralsei_pet/modules/emotion_system.py` | 切断 happy 正反馈；复合情绪残余保留与衰减；时间钳位 |
| `ralsei_pet/modules/dialogue_ui.py` | 打字机不再被打断；HTML 转义时机；滚动条跟随策略；游戏退出词放行 |
| `ralsei_pet/modules/dialogue_system.py` | 否定意图优先于肯定意图 |
| `ralsei_pet/modules/desktop_interaction.py` | 桌面项类型判据；删除返回码校验；网络检测与窗口动画不再长时间阻塞 |
| `ralsei_pet/modules/floor_manager.py` | 楼层下标改用稳定标识定位 |
| `ralsei_pet/modules/config_manager.py` | 畸形配置安全降级；启动时剪枝历史备份 |
| `ralsei_pet/modules/social_growth_system.py` | `evolution_path` 类型安全；异常捕获扩展 |
| `ralsei_pet/modules/weather_system.py` | 雪/雨判定顺序 |
| `ralsei_pet/requirements.txt` | 补 `beautifulsoup4` |
