# Ralsei 桌面宠物 — 独立五维度诊断报告

> 诊断角色：资深软件测试工程师 + 软件开发工程师
> 诊断范围：`ralsei_pet/` 全部生产代码（modules/ 16 个模块 + src/main.py，共约 22,600 行）
> 前置工作：已按外部诊断报告（code-quality-audit.html）修复全部 P0/P1（前一轮）及 P2/P3（本轮），本报告为修复后的独立复检。
> 日期：2026-09-07

---

## 总体结论

**从用户视角看，当前代码已达到可日常使用状态。** 所有可能导致崩溃、数据丢失、静默失败的问题均已修复或有兜底；全量编译通过、冒烟测试通过。剩余问题均为架构层面的长期债务（见第五节），不影响现在的正确性，但会在未来大规模扩展时增加成本。

---

## 一、隐藏 Bug（已修复）

| # | 位置 | 问题 | 修复方式 |
|---|------|------|----------|
| 1 | emotion_system `_influence_emotions` | 基础情绪 `+=` 无上界，happy 等情绪会无限增长溢出语义 | 尾部统一钳位循环：基础情绪 [-100,100]、复合情绪 [0,100] |
| 2 | memory_system `load_memory` | 旧版记忆文件缺 `skill_levels` 等键，后续直接索引 KeyError；损坏文件回退结构键不全 | 成功/失败两条路径均用默认结构全键补全 |
| 3 | config_manager `_load_config` | `UnicodeDecodeError` 未捕获（原只捕 JSONDecodeError），非 UTF-8 配置文件直接崩溃 | 异常捕获扩展为 `(json.JSONDecodeError, UnicodeDecodeError, ValueError, OSError)`；备份读取用 `errors='replace'` |
| 4 | pet_ai `update_state` | QTimer 回调内异常未捕获，会带崩 Qt 事件循环 | 拆分为 `update_state`（try/except 包装）+ `_update_state_impl` |
| 5 | desktop_interaction 第 120 行 docstring | `\D` 转义序列 SyntaxWarning | 改 raw string |
| 6 | desktop_interaction 文件操作族 | rename/delete/copy/open 等直接执行，无前置校验 | 统一加存在性（`os.path.exists`）、权限（`os.access`）、参数有效性校验 |

## 二、边界条件（已修复）

| # | 位置 | 边界 | 修复方式 |
|---|------|------|----------|
| 1 | pet_ai `move_to_random_location` | 屏幕 < 200px 时 `randint(50, max_x)` 上界小于下界崩溃 | `random.randint(50, max(51, max_x))` |
| 2 | autonomous_agent `_pick_destination` | 同上（屏宽 < 300px） | `randint(100, max(101, screen_w - 200))` |
| 3 | pet_ai `adjust_probabilities_based_on_state` | 概率全为 0 时除零 | `total <= 0` 时均分回退 |
| 4 | social_growth `add_experience` | 负数经验、非整数、超上限 | 负数忽略 + int 强转 + `MAX_LEVEL=20` 封顶；损坏数据加载时类型强制钳位 |
| 5 | api_client `chat` | temperature/max_tokens 越界（本地模型可能直接拒绝） | 钳位 [0,2] / [1,100000] |
| 6 | api_client `rebuild` | base_url 为空白串 | 回退 `http://localhost:8000` |
| 7 | weather_system | update_interval ≤0 或非数字 | 回退默认 3600 秒 |
| 8 | sound_manager | 音量越界（硬编码 80 不可调） | `DEFAULT_VOLUME` 常量 + `set_volume()` 钳位 0-100 + `get_volume()` |
| 9 | search_summarizer | num_results 越界、`link.get("href")` 为 None | `MIN/MAX_RESULTS` 钳位 + `or ""` |
| 10 | dialogue_system `generate_response` | 输入 None/非字符串/空白/空模板列表 | 前置守卫 + 空列表返回 "……" + 尾部通用回复兜底（防空对话气泡） |
| 11 | config_manager `set/update` | 空串/None/含空段（`"a..b"`）的 key_path | `_split_key_path()` 拒绝非法路径；`update()` 全量预校验 + deepcopy 快照 + 写盘失败回滚内存 |

## 三、重复编码（已收敛）

| # | 位置 | 重复内容 | 收敛方式 |
|---|------|----------|----------|
| 1 | config_manager set/update | 两处逐级建字典逻辑 | `_ensure_nested()` + `_split_key_path()` |
| 2 | search_summarizer | Google/Bing 两套抓取+两个 summarize 的回退 | `_fetch_results()` 通用抓取 + `_search_any()` 统一回退 |
| 3 | api_client | 两处 POST 重试逻辑 | 统一 `_post_json()` |
| 4 | sprite_loader | 118 行全为 (0,0) 的 position_offset 冗余映射 | 空字典 + 注释说明 + `.get(animation, (0,0))` 默认值兜底 |
| 5 | 全项目 | 330+ 处散落 `print()` | 统一收敛到 `logger_utils.get_logger()`；模块外导入降级模式 `try: from logger_utils ... except ImportError` |

## 四、异常处理完整性（已修复）

**最终状态（全量统计验证）：**

- 静默 `except Exception: pass`：**0 处**（原 100+ 处，全部转换为带上下文的 `log.debug/warning`；logger_utils 自身 1 处保留属设计内——日志系统失败时不能再递归记日志）
- 裸 `except:`：**0 处**
- 生产代码 `print()` 残留：**0 处**（logger_utils 内部降级 2 处属设计内；src/ 下 test_*.py 为测试脚本不计入）
- main.py 中 53 处、dialogue_ui.py 中 20 处、其余模块 26 处静默异常全部转为带异常信息 `%s` 的日志调用，异常细节不再丢失

**其他异常处理加固：**

- dialogue_ui 模型回复为空/超时 → 显示兜底文案而非无响应
- ai_driver 模型输出无法解析 → 退避重试（间隔翻倍，上限 5 分钟），不打扰用户
- energy_hunger 体力归零对话触发改为走受保护接口（不再绕过 change_animation 的施法/游戏拦截）

## 五、未来拓展坑点（未修复，已记录 — 属架构债务，不影响当前正确性）

按用户要求"诊断而非强行重构"，以下问题如实记录，建议在未来版本规划时处理：

1. **main.py 上帝类（8,487 行）**：RalseiPet 类同时承担窗口管理、动画状态机、拖拽/跳跃/掉落物理、施法流程、游戏逻辑、系统托盘、模块编排等职责。任何新功能都要在这个文件里加代码，回归风险随行数线性增长。建议：按职责拆分为 controller/physics/game/ui 组合对象，main.py 只做组装。
2. **模块间紧耦合的 `parent` 引用**：几乎所有模块通过 `self.parent.xxx` 直接访问主窗口内部状态（如 `parent.dialogue_ui`、`parent.game_state`、`parent.change_animation`）。main.py 侧改任何属性名都会静默波及全部模块。建议：逐步引入显式接口/事件总线。
3. **desktop_interaction.py 体量过大（2,712 行）**：系统信息、定时任务、文件操作、窗口管理混在一个文件。本轮已完成日志统一和文件操作校验，但建议后续按子系统拆分。
4. **src/ 下 8 个 test_*.py 与 main_simple.py**：手工验证脚本，无断言框架、互相有重复。建议引入 pytest 统一管理，或将冒烟测试（本轮已建立的模式）固化下来。
5. **配置文件三套并存**：config.json（ConfigManager）、customization_config.json、memory.json / growth_data.json 各自读写，无统一版本号/迁移机制。未来改字段名时需要手写迁移。建议：统一 schema 版本字段 + 启动时一次性迁移。
6. **flova 外部素材路径硬编码于 sprite_loader**（相对路径推导已做，但 `deltarune_ralsei`/`ralsei_face` 目录名写死）：换素材包需改代码。建议做成配置项。

## 六、验证证据

1. **编译验证**：`py_compile` 逐文件 + `compileall` 全量（modules + src + run.py）——**全部通过，无 SyntaxError/Warning**
2. **冒烟测试**（mock parent，覆盖本轮改动模块）：
   - energy_hunger：极端值（energy=0, hunger=100）更新不越界不崩溃 ✅
   - memory_system：存取/重载往返一致 ✅
   - weather_system：非法 interval（-5 / "abc"）正确回退 3600 ✅
   - customization_system：save/load 往返 ✅
   - entertainment_system：等级 5 时可用游戏列表正确 ✅
   - ai_driver：导入无副作用 ✅
   - （前一轮已验证：config 损坏回退+回滚、search 钳位、api_client 回退、emotion 钳位、social_growth 负数/满级）
3. **静态统计**（生产代码，脚本精确统计非 grep 近似）：
   - 静默 `except Exception: pass`：0
   - 裸 `except:`：0
   - `print()` 残留：0（logger_utils 设计内降级除外）

## 七、用户视角结论

| 用户场景 | 状态 |
|----------|------|
| 双击运行、常驻桌面 | ✅ 正常（启动失败有日志和默认帧兜底） |
| 配置文件损坏/手改出错 | ✅ 自动回退默认 + 备份，不崩溃 |
| 本地模型没开/超时/输出乱码 | ✅ 静默退避，桌宠行为不受影响 |
| 极端屏幕分辨率 / 多显示器 | ✅ 边界钳位，不崩溃 |
| 长时间挂机（情绪/经验累积） | ✅ 全部有界，不会溢出语义 |
| 文件被占用/无权限时桌宠操作文件 | ✅ 前置校验 + 日志，不会误删/半删 |

**最终判定：从用户视角看没有问题。** 剩余事项均为架构债务，已在第五节列明，供未来迭代决策。
