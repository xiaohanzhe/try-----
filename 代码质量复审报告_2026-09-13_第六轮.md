# 第六轮 · 行为缺陷修复报告

- 日期：2026-09-13
- 触发：用户逐条反馈行为问题（瞬移 / 自主对话过频 / 动画过快 / 甩飞判定过广 / 起身应只播一次 / 常识合理性）
- 范围：纯行为层修复，**不动架构**（H4 上帝类拆分、H5 动画配置化仍按原计划单独排期）
- 提交：`4ef922e`（已推送 origin/main，`origin/main == HEAD`，ahead=0 / behind=0）
- 证据：`code-quality-audit/第六轮/_evidence/`（脚本、备份、日志、验证输出）

---

## 1. 瞬移（多显示器 + dt 突增）

**问题**：自主移动、跳跃、躲藏、避让回收站等逻辑统一用
`QApplication.desktop().availableGeometry()` 取边界。该 API 只返回**主屏**且原点固定为 `(0,0)`，
在副屏（x 为负、或超出主屏宽）会被强行夹回主屏坐标 → 表现为"瞬移"。
另一个诱因是 `dt` 未设上限：主线程卡顿后单帧 `dt` 变大，位移按 `dt` 二次放大，同样表现为瞬移。

**修复**：
- 新增 `_current_screen_rect()`：用 `screenGeometry(self)` 取"它当前所在屏幕"的矩形，
  失败时回落 `availableGeometry()`。
- 新增/统一 `_virtual_screen_rect()`（win32 `SM_XVIRTUALSCREEN` / `SM_CXVIRTUALSCREEN`，
  覆盖整个虚拟桌面）与 `_clamp_pos_to_desktop()`、`_desktop_floor_y()`。
- 所有自主移动路径（`generate_new_move_target`、`start_dragging_play` 目标点、
  `_move_away_from_recycle_bin`、`_resolve_target_screen_anchor`、`_hide_*`、
  `_hide_jump_back_to_desktop`、`handle_jump` 落点）改为上述助手。
- `update_movement` / `handle_gravity_fall` / `update_bounce` 中的 `dt` 统一钳到 **0.1s**。

**验证**：`x=2500` 不再被夹回、`x=-1500` 不再被夹回（副屏/负坐标合法保留）、
越界 `-1920` 正确夹回；落地判据统一走 `_desktop_floor_y()`。

---

## 2. 甩飞判定错位（本轮最关键的根因修复）

**问题**：甩飞（fling）逻辑原本写在 `mouseMoveEvent` 的**"无按键"分支**里。
后果有两个：
1. 只有"松手之后再动一次鼠标"才会触发判定 → 拖到一半停住再松手，**永远不触发**；
2. 松手后随手碰一下鼠标，会拿**陈旧的拖拽采样**算出速度，把 Ralsei "自己甩出去"。

**修复**：
- 整段甩飞/弹跳判定从 `mouseMoveEvent` **迁入 `mouseReleaseEvent`**，在松手那一刻做唯一判定。
- 速度改为**真实像素/秒**：由 `_drag_samples`（时间戳采样）取最近 `0.12s` 窗口计算，
  且末次采样距今 `>0.25s` 视为"停住再松手" = 轻放（`release_speed = 0`）。
- 阈值：`_FLING_SPEED = 900 px/s`、`_BOUNCE_SPEED = 250 px/s`（原按每帧像素差判定，阈值形同虚设）。
- 拖拽跟手改为 **1:1**（去掉弹性 20% 与指针劫持），符合"人不会那么做"的常识要求。

---

## 3. 抛物线轨迹（偏俯视 2D 手感）

**修复**：
- 甩飞后进入飞行相：`_fall_vx` / `_fall_vy` + 重力 + 空气阻力；
  水平空气阻力改为**按时间衰减** `fall_velocity_x *= max(0.0, 1.0 - 0.7 * elapsed)`（原按帧衰减，
  帧率变化时手感漂移）。
- 起抛初速做非线性映射与钳制：`vx ∈ [-1600, 1600]`、`vy ∈ [-1400, 600]`，
  且 `vy > -350` 时强制补一个向上分量，保证"甩出去先起再落"，不会擦地滑行。
- 落地判据：下落中且越过**起抛高度**或**桌面地面**，落地精确吸附。

**同时修掉一个隐藏 bug（验证脚本 D5/D6 发现）**：
原实现落地时 `fall_duration = max(fall_duration, 1.0)`，而相位切换又用
`fall_duration >= 1.0` 判断 → 高空弧线会**在空中**就进入 splat / dazed 相。
现改用 `_fall_phase_start` + `_fall_flight_time` 做相位计时，`_fall_landed` 标记真实落地。
相位序列：splat 1s → dazed 1s → recovering。

---

## 4. 自主对话：全权交给 AI

**修复**：
- **删除全部内置系统对话触发**，共 20 个方法：
  17 个 office 检查方法 + `check_interesting_files`、`check_weather_response`、
  `check_excel_table_needs`、`handle_dragging_play`（`main.py` 净删 475 行）；
  `dialogue_system.py` 再删 `should_initiate_conversation` / `initiate_conversation` 共 103 行。
- 唯一入口 `start_autonomous_speech(reason, user_requested=False)`；
  节流 `AUTONOMOUS_SPEECH_MIN_INTERVAL = 600.0`（**10 分钟最多 1 次自主触发**）。
- AI 未启用时**直接保持沉默**，不再回落到内置台词。
- 移除天气定时播报（`weather_timer = None`）；AI 回复只取首句、去引号，空句不发。
- `handle_file_commands` 中引用已删方法的命令改为**事实性回复**，走
  `desktop_interaction.check_ppt_files()` / `identify_browser_windows()`。

---

## 5. 动画播放速率

`config.json` / `config_manager.py`：`fps 30 → 6`，`frame_delay 32 → 166`。

---

## 6. 常识合理性（"人会不会那么做"）

- `start_dragging_play` 原为"假装拖动/移动桌面文件" → 改为**走过去看一眼图标**（不再伪造文件操作）。
- 拖拽跟手 1:1，取消弹性与指针劫持。
- 长按反应加守卫：`is_falling` 为真时不触发按压判定（避免"正往下掉还在原地被抚摸"）。

---

## 7. 站起身只播一次

落地起身改用 `play_animation_once("land", restore_to="idle")`，
播完回 `idle` 而不是回到"躺姿"（原实现会回到上一个动画，即躺姿）。

---

## 验证汇总

| 套件 | 结果 |
| --- | --- |
| 第六轮 `verify_round6_fixes.py`（A–F 共 39 项） | 38 PASS / 1 改写（改写项即上面 D5/D6 的空中 splat，已修） |
| 第五轮 smoke（22 模块导入） | PASS |
| 第五轮 verify（16 项） | PASS |
| H5-S1 verify（17 项） | PASS |
| **合计** | **72 PASS / 0 FAIL** |
| 离屏 `runtime_exercise.py`（13 步交互） | 0 未捕获异常 |
| `py_compile` / pyflakes | 无 F821 未定义名 |

### 遗留（未修，非本轮范围）

- `change_animation` 的"从长到短"名称回退策略，与 `update_animation` 内嵌回退块
  只取前两段的策略**不一致**。两者都只在未命中时生效，当前无实际暴露，登记待 H5-S2 一并收敛。
- pyflakes 少量既有告警（未使用变量、无占位符 f-string、`pet_ai` 未用导入）为本轮之前既有，
  不属 F821 类错误，未在行为修复轮混入改动。
