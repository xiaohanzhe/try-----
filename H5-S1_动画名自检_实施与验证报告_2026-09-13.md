# H5 S1 实施与验证报告：动画名未命中自检

> 日期：2026-09-13　| 计划来源：`架构改造排期方案_H4-H5_2026-09-13.md` §4.2 S1
> 范围：**只加观测，不改行为**。生产代码改动 2 个文件、共 5 处插桩。

---

## 一、一句话结论

H5 S1 已落地：**任何"被请求但不存在"的动画名，现在都会立刻变成一条带定位提示的 WARNING，并在退出时汇总成清单**——而不再像以前那样静默退化成 `idle` 或静默拒绝切换。

改动前后回退行为**逐例等价**，已用 501 个样本与独立复刻的参照算法比对验证。

---

## 二、为什么必须先做这一步（而不是先搬配置）

S1 要解决的是**可观测性**问题，不是配置位置问题：

1. mapping 里"少一个动画名"是完全静默的——`change_animation` 会回退到 `idle`（或直接 `return False`），
   `play_animation_once` 直接 `return False`，调用方全都忽略返回值。
2. 更关键的是 **f-string 拼接**：`walk_{dir}`、`run_{dir}`、`walk_tea_{dir}`、`{anim_type}_{new_dir}`
   共 12 处拼接点、9 种模板，方向/类型取值在运行时才确定。**静态扫描永远看不到拼出来的名字**，
   所以"当前 0 缺失"是碰巧，不是被保证。
3. 因此顺序必须是：先把未命中变成可见清单 → 再决定哪些名字要进 JSON、哪些标 legacy。

---

## 三、改动清单

### `ralsei_pet/modules/sprite_loader.py`

| 位置 | 改动 |
|---|---|
| 模块级 | 新增 `_DIRECTION_TOKENS` / `_ANIM_TYPE_TOKENS` 词表（仅用于诊断日志） |
| `__init__` | 新增 `self.animation_misses`（账本）与 `self._miss_reported`（同名只告警一次，避免每帧刷屏） |
| 新增 `diagnose_dynamic_name()` | 对拼接名做**结构性校验**：缺方向词 / 多个方向词 / 混入帧号 / 空名。只提示，不参与任何决策 |
| 新增 `note_animation_miss()` | 记账 + 首次告警；返回 `resolved`，便于调用方一行完成"记账 + 继续原逻辑" |
| 新增 `get_animation_miss_report()` | 返回 `[(请求名, 次数, [来源], [回退目标])]`，按次数降序——给后续回归脚本做断言用 |
| 新增 `log_animation_miss_summary()` | 退出时输出汇总；无未命中时只留一行 INFO（便于断言"零未命中"） |
| `get_sprite()` | 未命中时记一笔再 `return None`（返回行为不变） |

### `ralsei_pet/src/main.py`

| 位置 | 改动 |
|---|---|
| `change_animation()` 第 1 步 | "从长到短回退 → idle → 拒绝"三个出口**原样保留**，仅在出口处记账（含回退目标） |
| `update_animation()` 内嵌回退块 | 同上记账 |
| `play_animation_once()` | 原本静默 `return False` 的分支加一笔账 |
| `_tick_spell_flow()` casting 分支 | `frames` 为空时记账——这正是此前 P0（`current_time` NameError）导致"施法永不结束"的同一后果路径 |
| `cleanup_on_exit()` | 新增第 2.55 步输出汇总，整段包在 `try/except` 里（观测代码绝不能影响退出流程） |

**未改动**：任何回退规则、任何优先级/冷却/拦截逻辑、任何返回值语义。

---

## 四、验证

脚本：`code-quality-audit/架构改造-H4H5/verify_s1_animation_miss.py`
原始输出：`_evidence/verify_s1.txt`

| 组 | 内容 | 结果 |
|---|---|---|
| A（7 项） | 自检本身：加载阶段不误记账；`diagnose_dynamic_name` 对非法参数报警、对 `walk_up_blush` 等合法缺失**不误报**；`get_sprite` 未命中返回 `None` 且入账 | ✅ |
| B（5 项） | **回退行为等价性**：对 501 个样本（mapping 全部 109 键 + 380 个自动扫描组名 + 12 个人为扰动名）用独立复刻的参照算法比对 `change_animation` 的最终落点 | ✅ **501/501 一致** |
| C（2 项） | 全动画缺失时的"拒绝切换"分支：返回值与状态不变，且该拒绝同样入账 | ✅ |
| D（3 项） | 汇总数 = 账本数；重复请求只累加次数不增种类；report 降序且字段完整 | ✅ |

**合计 17/17 通过。**

### 回归（第五轮基线，改动后重跑）

| 脚本 | 结果 |
|---|---|
| `smoke_import_round5.py` | ✅ 22/22 模块导入成功，`_tick_spell_flow` 裸 `current_time` 残留 0 |
| `verify_round5_fixes.py` | ✅ **16/16 通过**（对话真实动作 6 项、施法回调/阶段复位 4 项、成就去重 3 项…） |
| `pyflakes`（改动后） | ✅ **无 `undefined name`、无新增项**；剩余均为第五轮已记录的装饰性问题 |
| 编译产物洁净 | ✅ 被跟踪的 `main.cpython-311.pyc` 未被改写（`git status` 无该文件） |

---

## 五、附带发现（S1 顺带量出来的，不在本次改动范围）

### 5.1 启动加载膨胀：同一批贴图被加载了两套命名空间

脚本 `analyze_autoscan_cost.py`，输出 `_evidence/autoscan_cost.txt`：

| 指标 | 实测 |
|---|---|
| 运行时 `sprites` 组数 | **489** |
| ├ mapping 的"语义名" | **109 组 / 377 帧对象** ← main.py 真正请求的名字 |
| └ 自动扫描的"文件名前缀" | **380 组 / 1084 帧对象** |
| mapping 引用帧文件（去重） | 363 |
| 自动扫描引用帧文件（去重） | 1084（与 mapping 交集仅 360） |
| 仅自动扫描引用、mapping 未引用 | **724 个文件** |
| 被任何组都不引用的磁盘文件 | 19 |

其中 **82 个自动扫描组的帧文件已被 mapping 100% 覆盖** → 纯重复加载
（例：`spr_cutscene_20_ralsei_walk_down_butler` 与语义名 `walk_down_butler` 指向同一批文件）。

**这条量化同时回答了 H5 的一个关键设计问题**：`scan_and_group_assets` 不该"接管配置"——
它建的 380 个组里，代码一个都不会按名字请求；全量入组只会让命名空间从 109 膨胀到 489。
它应该降级为"补漏报告器"（§4.1 的判断得到数据支持）。

### 5.2 两处回退策略不一致（已记账，未修）

- `change_animation`：**从长到短**逐级回退（`walk_up_blush_x` → `walk_up_blush` → `walk_up`）
- `update_animation` 内嵌块：**只取前两段**（`walk_up_blush_x` → `walk_up`）

同一个"名字不存在"的问题，两个策略。已在两处分别记账并注明来源，便于日后统一时对照。
本次**不改**——S1 的边界是"只观测"。

---

## 六、真实未命中清单怎么取

验证脚本里的 13 条未命中是**人为构造的样本**，不代表真实运行结果。
真实清单需要**在你的机器上正常跑一轮**（正常玩、拖拽、施法、躲猫猫、对话），然后：

- 过程中：首次未命中即打 `[anim-miss] ...` WARNING（每个名字只打一次，不会刷屏）
- 退出时：`cleanup_on_exit` 输出一段汇总，形如
  `[anim-miss] 本轮共 N 个动画名未命中，合计 M 次：` + 逐条明细

这份清单就是 S2（抽 JSON）**必须覆盖的名字集合**，也是 S3（标 legacy）的判定输入。

---

## 七、下一步

- **S2**：从现有 `animation_mapping` 生成 `assets/animations.json` + 加载器；
  断言"JSON 还原的 dict 与原硬编码 dict 深度相等"；加载失败回落内置表。
- **S3**：别名（`happy`/`sad`/`fall_mad`…）显式化成字段；把 5.1 里那 82 个纯重复组按"不建组"处理。
- **待你确认**：是否要顺手把 5.1 的加载膨胀也纳入 Batch A（属独立小改动，与 S1/S2 正交）。

---

*证据：`code-quality-audit/架构改造-H4H5/_evidence/`（`verify_s1.txt`、`autoscan_cost.txt`、`autoscan_run.txt`、`static_s1.txt`）*
