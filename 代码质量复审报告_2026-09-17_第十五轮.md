# 代码质量复审报告 — 第十五轮（2026-09-17）

**主题**：「建楼」遗留的**死代码清干净** ＋ 顺手逮到一个**被错判的坠落起因**
**用户指令**：*"你自己检测完后就继续按照原计划进行吧"*（即兑现第十四轮 §八 两条待办，并把同类死代码一并处置）
**提交**：`1b279d3`（18 文件 / +1073 −283；已推送，git 自报 `21605d4..1b279d3 main -> main`）
**G2**：**19 套件 / PASS=728 / FAIL=0 / 全 IDENTICAL**

---

## 一、结论（先说结果）

上一轮报告末尾留了两条待办：① 清掉 `check_nearby_windows` 尾部的旧裸矩形兜底段；
② 处置 `is_on_floor_edge` / `find_support_below` 两个零调用函数。本轮把范围扩成
**"把建楼遗留的死代码清干净"**，过程中又逮到一个真 bug。

| # | 对象 | 性质 | 处置 |
|---|---|---|---|
| 1 | `main.py::check_nearby_windows` 尾部裸矩形启发式（154 行） | **口径错误的活代码**（第十三轮换成"可见区域"后，它是旧口径：会跳到被挡住的部分；站在窗口上时把目标直接声明成桌面 = 穿透） | **删除**。楼层系统成为跳跃的唯一入口 |
| 2 | `main.py::update_floor`（67 行） | **零调用漂移副本**（第五轮 F3 就点名；与 `check_window_movement` 逐行重复且已不一致），H4/H5 排期方案 G1 的**优先清理项** | **删除**。它的行为在活路径上都已覆盖 |
| 3 | `floor_manager.py::find_support_below`（13 行） | `get_drop_destination` 的**重复实现**，零调用 | **删除** |
| 4 | `floor_manager.py::is_on_floor_edge`（9 行） | 无任何消费者 | **删除** |
| 5 | `floor_manager.py::is_floor_valid` | 同样是**零调用死代码**（第五轮死引用清单里就有它） | **接线**（见 §三）—— 它正好是解决第 6 条所需的判据 |
| 6 | `check_window_movement` 的坠落起因 | **真 bug**：宠物**自己走出楼板**也会被判成"用户行为"→ 播生气动画 ≥5s | **修复**：用 `is_floor_valid` 区分"自己走出去"（常规动画）与"用户关窗抽走楼板"（生气动画） |

**一句话**：本轮把"没改也没人调用的那些"一次清掉（净删 201 行 + 27 行），
并把一个早就写好、同样没人用的判据接上，修掉了与建楼要求**正好相反**的一处观感。

**代码净变化**：`main.py` 9373 → **9172** 行（+40 / −241）｜`floor_manager.py` −27 行

---

## 二、交付物

| 文件 | 动作 | 说明 |
|---|---|---|
| `ralsei_pet/src/main.py` | 改 | 删 `update_floor`（67 行）+ `check_nearby_windows` 尾部裸矩形段（154 行）；`check_window_movement` 接 `is_floor_valid` 分流坠落起因；注释与文档串同步 |
| `ralsei_pet/modules/floor_manager.py` | 改 | 删 `find_support_below` / `is_on_floor_edge`（含段头注释，共 27 行） |
| `code-quality-audit/第十五轮/verify_round15_cleanup.py` | **新增（30 项）** | 自检套件（A 死代码确实清了 / B 跳跃单一入口 / C 坠落起因分流 / D 权威路径未误删） |
| `code-quality-audit/第十五轮/_probe_smoke.py` | **新增** | 真机启动路径冒烟（**真的 `RalseiPet()`**，非 stub） |
| `code-quality-audit/第十五轮/_evidence/` | 新增 | 证据 8 份（改前 G2 / 预期 DIFF 的 G2 / 改后 G2 / 套件输出 / 删除手术日志 / 真机冒烟 / 两份基线 diff） |
| `code-quality-audit/第八轮/verify_round8_floor.py` | 改 | `FakeFloorManager` 补 `is_floor_valid`（**桩必须跟着真实方法面走** —— 本项目的第 3 次） |
| `code-quality-audit/第十三轮/verify_round13_build.py` | 改 | C6 改为在**唯一权威** `floor_visible_contains` 上断言；新增 C6b 锁"零调用重复实现不该长回来"（71 → 72 项） |
| `code-quality-audit/第十四轮/verify_round14_move.py` | 改 | G5b 原来断言的是**死代码** `update_floor` 里的字符串（断言死代码等于没测）→ 改为锁"它不该再长回来" |
| `code-quality-audit/regress/run_all.py` | 改 | 挂入 `round15_cleanup`；加入隔离清单 `HERMETIC_IDS` |
| `code-quality-audit/regress/baseline.json` | 改 | 基线重建（19 套件） |

---

## 三、为什么"删"也是修复（四堆死代码的来历）

这是本项目最贵的坑的**另一面**：前几轮反复踩"**改了却没人调用**"（第八轮 z 序、
第十三轮三处可见区域判定、第十四轮跳转规划），本轮清的是"**没改也没人调用**"的那一堆。

| 对象 | 什么时候写下的 | 为什么没人用 | 为什么留着有害 |
|---|---|---|---|
| `check_nearby_windows` 尾部裸矩形段 | 建楼之前（初版启发式） | 第十四轮加了楼层判定后**早返回**，它成了不可达代码 | 它是**第十三轮口径错误的活样本**：谁把早返回挪掉，bug 立刻复活。第十四轮的 A2 之所以用"故意不给 `desktop_interaction` 的 stub"来锁，就是因为它还在 |
| `update_floor` | 建楼之前 | 第五轮 F3 已确认"全项目无调用点" | 与 `check_window_movement` **逐行重复且已漂移**（多一段"桌面被新窗口覆盖"、少一段楼层身份判定）。留着就是"以后改错地方"的邀请函 |
| `find_support_below` | 第十三轮 | 它的语义被 `get_drop_destination` 完全覆盖 | 同一件事两份实现，改一份忘一份 |
| `is_on_floor_edge` | 建楼之前（英文注释，风格与全文件不一致） | 从未被接线 | 见下 —— 它想做的事，本轮由 `is_floor_valid` 更准确地完成了 |

**`is_floor_valid` 为什么是"接线"而不是"删"**：它回答的是"**这块楼板引用的窗口还在不在**"，
而这正是本轮第 6 条要区分的那个问题。它自己则是从第五轮起就躺在死引用清单里的零调用方法
—— 写了四年、没人用过一次，本轮终于用上了。

---

## 四、被错判的坠落起因（真 bug，对应建楼要求第 36 行）

要求原文：

> *"摔到桌面上的动画用的还是咱之前那个，但**如果是我的行为导致他摔到桌面上的那就用生气的那个**，
> 这两个动画持续至少 5s。"*

`check_window_movement` 原来的判定是：

```
if old_floor 是窗口 and new_floor 不是窗口:
    start_falling(reason='floor_removed')      # 一律当成"用户行为"→ 生气动画
```

但"脚下不再是窗口楼板"其实有**两种成因**：

| 成因 | 是不是"我的行为" | 该用哪组动画 | 原实现 |
|---|---|---|---|
| 宠物**自己走到了楼板边缘之外**（窗口还开着） | **不是** | 常规坠落（要求："就直直掉下去，落到下面第一块能接住的楼板"） | ❌ 误判为生气 |
| 用户**把窗口关了 / 楼板被抽走** | **是** | 生气的那组，≥5s | ✅ |

用户越是用鼠标把宠物赶到窗口边缘、它越会"生气"——观感与要求**正好相反**。

**修法**（判据早就有，只是没人用）：

```python
if self.floor_manager.is_floor_valid(old_floor):
    self.start_falling()                      # 窗口还在 → 自己走出去 → 常规动画
else:
    self.start_falling(reason='floor_removed')  # 窗口没了 → 用户抽走楼板 → 生气动画
```

**为什么用 `is_floor_valid` 而不是"离边缘多近"**：楼板检查是 1 秒节拍，宠物一秒能走出几百像素，
用"距离边缘 ≤N px"判会因为节拍太粗而漏判。"**窗口还在不在**"与节拍无关，是稳的。

**顺带确认没有回退**：`round8_floor` 的 W6b（关窗 → 必须带 `reason='floor_removed'`）本轮仍然 PASS ——
"关窗就生气"这条要求没被改动。

---

## 五、设计取舍

| 取舍 | 选择 | 理由 |
|---|---|---|
| 楼层不可用时怎么办？ | **什么都不做**（`return`），不回落旧启发式 | `floors` 为空 ⟺ 一个可见窗口都没有 ⟹ 本来也没有可跳的楼板。旧启发式在那种情况下同样无对象可枚举 → 删掉**不改变行为**，只是不再留一条错口径的路 |
| `start_jump` 的裸窗口回落分支要不要一起删？ | **保留** | 它还有**两个活的调用方**：`climb_to_top_window()`（`main.py:3610`）与 `command_manager.py:78`。删了会真坏功能（套件 A5b 专门锁这条） |
| 本轮要不要顺手恢复 `update_floor` 独有的"桌面被新窗口覆盖 → `found_window` 情绪"？ | **不恢复** | 那行代码在 `update_floor` 里，而 `update_floor` **从来没被调用过** → 该情绪**当前本就不可达**。本轮的定位是清理与修复，不夹带新行为。留作 §六 待办 |
| 删 `find_support_below` 会不会丢语义？ | 不会 | 它对"被遮处接不住"的判定，本轮改成在**唯一权威** `floor_visible_contains` 上断言（第十三轮 C6），覆盖面更直接 |

---

## 六、验证

### 6.1 自检套件（30 项，全过）

| 分组 | 内容 |
|---|---|
| A 死代码确实清了 | `update_floor` / `find_support_below` / `is_on_floor_edge` 从类与源码双双消失；`check_nearby_windows` **函数体内**已无 `desktop_interaction` / `jump_desktop_edge` / `edge_threshold`；但**不许误删**：唯一入口 `check_window_movement` 与 `start_jump` 的裸窗口分支都还在 |
| B 跳跃只剩一条入口 | 楼层可用 → 走 `_nearest_floor_jump`（行为级）；**楼层为空 → 直接返回**（stub 故意不提供 `desktop_interaction`，一旦偷回老路立刻 `AttributeError`）；有可执行跳跃时确实起跳 |
| C 坠落起因分流 | 自己走出边缘 → `start_falling()`（不带起因）；关窗 → `start_falling(reason='floor_removed')`；`is_floor_valid` **确实被调用**（不是又一处"写了没人用"）；`is_floor_valid` 三条语义（在/不在/桌面）；贴边进入规划未被这轮清理动到 |
| D 权威路径未误删 | `get_drop_destination` / `adjacent_lower_floor` / `nearest_visible_point` / `floor_visible_contains` / `visible_subrects` / 第十四轮六个规划方法都在；`get_drop_destination` 行为仍按可见区域 |

### 6.2 G2 全量

```
改前：18 套件 / PASS=697 / FAIL=0 / 全 IDENTICAL
改后：19 套件 / PASS=728 / FAIL=0 / 全 IDENTICAL（exit=0）
```

两处**有意变更**（已逐行核对 diff，见 `_evidence/round15_diff_round13.txt` / `round15_diff_round14.txt`）：

| 套件 | 变化 | 内容 |
|---|---|---|
| `round13_build` | 71 → **72** | C6 改为在 `floor_visible_contains` 上断言；新增 C6b 锁"零调用重复实现不该长回来" |
| `round14_move` | 41 → **41**（文案变） | G5b 不再断言死代码 `update_floor` 里的字符串，改为锁"它不该再长回来" |

**其余 17 个套件全部 IDENTICAL** —— 包括 `round8_floor`（给它补了 `is_floor_valid` 桩，
但**输出一字未变**，说明补桩没有改变任何既有断言的结论）。

### 6.3 真机启动路径冒烟（离屏，非 stub）

G2 各套件都是"只装需要的表面"的桩驱动 —— 能证明**被测那条链**没坏，证明不了"整只宠物还能起来"。
本轮删了 movement 主循环每 1 秒会走到的分支，所以额外起了一次**真的 `RalseiPet()`**
（`RALSEI_MEMORY_DIR` 指向临时目录，不碰用户 E 盘）：

```
[ OK ] RalseiPet() 实例化成功
[ OK ] floor_manager.update_floors()          -> floors=0 underlying_windows=0
[ OK ] floor_manager.is_floor_valid(desktop)  -> True
[ OK ] check_window_movement() ×3（1 秒节拍那条链）  -> no exception
[ OK ] check_nearby_windows(pos)（跳跃决策）   -> no exception
[ OK ] start_falling() → handle_gravity_fall() 直到落地 -> frames=44 landed=desktop z=0
[ OK ] _nearest_floor_jump()（跳跃规划，不改状态）
```

证据 `_evidence/round15_smoke.txt`。

### 6.4 证据编码

本轮 `_evidence/` 8 份 + 套件/探针 共 **10 个文本文件**经
`tools/check_evidence_encoding.py --strict`：**确定性损坏 0 / 告警 0**（全部合法 UTF-8、无 BOM）。
搬运一律 `shutil.copyfile`（逐字节），中间产物用 Python 自写 UTF-8 —— 不用 PowerShell 捕获原生 stdout。

### 6.5 删除手术怎么做的（可复核）

154 行 + 67 行是**行级手术**（脚本按行号定位 + 断言确认落点 + 删完 `py_compile` 校验），
不是手工重排版 —— 避免"删错位置却看起来正常"。手术日志 `_evidence/round15_surgery_log.txt`：

```
main.py: 删 check_nearby_windows 尾部裸矩形段 行 2138..2291 (154 行)
main.py: 删 update_floor 行 2818..2884 (67 行)
floor_manager.py: 删 is_on_floor_edge + find_support_below 行 633..659 (27 行)
```

脚本里带两条硬断言：① 那句 `get_all_visible_windows()` 在文件里共有 10 处，
必须确认取到的是 `check_nearby_windows` 那一处（且上溯最近的 `def` 就是它）；
② `floor_manager` 的删除必须落到文件末尾（那两个方法确实是最后的内容）。

---

## 七、遗留 / 待办

1. **真机看效果**（只能用户确认）：① 走到楼板边缘掉下去时**不该再生气**；
   ② 关窗掉下来仍然生气 ≥5s；③ 上下跳 / 落地动作与上一轮一致（本轮没动跳跃几何）。
2. **`climb_to_top_window()` 仍是裸窗口路径**（走 `start_jump(top_window, "bottom")` 不经过楼层）。
   本轮**故意没动**它：它挑的是**最前面那个窗口**（按定义不会被别的窗口盖住），落点不存在"看不见的地板"问题。
   但既然"楼层是唯一真源"，建议下一轮把它也收编（或与用户确认这条命令的语义还要不要保留）。
3. **`update_floor` 独有的 `found_window` 情绪现已彻底消失**（它从未可达）。若用户希望"桌面被新窗口盖住"
   时有情绪反应，需要在 `check_window_movement` 的"桌面 → 窗口"分支里新增一行 `react_to_event`。
4. **程序目录里的本地备份文件**：`ralsei_pet/src/main.py.bak`、`main.py.backup_before_fix`、
   `main.py.backup_before_deadcode_cleanup`（都在 `.gitignore` 覆盖下、未纳入版本库）。
   与"程序目录历史开发日志"同类，**未擅自删**，等用户点头。
5. **H4/H5 架构改造**（用户排期"单独做"）仍不在本轮范围。本轮又清掉两块绊脚石：
   `update_floor`（排期方案 §G1 点名的优先清理项）与 `check_nearby_windows` 的不可达尾部。
   `main.py` 已从 8794（H4/H5 基线）→ 9172 行（中间几轮加的功能）→ **本轮净减 201 行**。
6. **历史证据编码 70 个文件**（第五/六/八/九轮 + `架构改造-H4H5/`）：第十三轮报告 §十第 5 条
   仍在等用户点头（BOM 可机械剥；29 个真乱码不该猜着改）。
