# 人味第四轮报告（2026-09-19）

用户口径：「OK，但感觉现在 4B 说出来的话的性格不像 ralsei 一样，你给训练一下，**其他的可以删一删了**」，
随后补：「可以从系统提示词精细化，载体层状态管理和 **LoRA 微调试试**」，
LoRA 的语料路线指定为「**走原作台词语料**」。

本轮四件事：**① 删旧模型 ② 建原作语料 ③ 算清 LoRA 到底可不可行 ④ 把 persona 从「示范句」改成「规则 + 按场合绑定碎片」并做 5 轮 A/B**。

---

## 〇、先说一个必须自己揭穿的错误：上一轮那份 A/B 是假的

上一轮我把「口癖版 persona」和「旧 persona」对比，结论是"输出几乎一致"。
**这个结论是无效的** —— 探针把旧 persona 取自 `git show HEAD:`，而 persona 的改动**已经先提交了**，
于是 `HEAD` == 磁盘，A 组和 B 组是同一条 system prompt。探针静静退化成 A/A。

铁证就在那份证据的头部：**「旧 2136 字 / 新 2136 字」** —— 长度一模一样，我第一眼没看出来。

已修：`load_old_persona()` 改成取「最近一次改动该文件的提交的**父版本**」，
并加了**硬闸**：新旧 md5 相同直接 `SystemExit`，以后不可能再静默发生。
`--old-rev <rev>` 可以指定对比版本，用来隔离"这一次改了哪一项"。

> **教训：凡 A/B，先断言 A ≠ B。** 否则结论必然是"没有差别"，而且非常像真的。

---

## 一、旧模型清理（用户：「其他的可以删一删了」）

执行前已说明影响面。删 `ralsei:v2` / `ralsei:latest` / `qwen2.5:3b` / `qwen2.5:7b-instruct`，
留 `ralsei:v3`（线上）+ `qwen3:4b-instruct-2507-q4_K_M`（底座）。四个 `ollama rm` 全 `exit=0`。

**实际释放约 6.6 GB，不是标称的 10.4 GB。** 标称相加会多算：`ralsei:v2` / `ralsei:latest`
与底座 `qwen2.5:3b` **共用同一个 blob**，删三个只掉一份。按 blobs 目录（`C:\Users\23002\.ollama\models\blobs`）
反推：清理后 2.33 GB，清理前 ≈ 8.9 GB。

删完实测 `/api/chat`（`ralsei:v3`）能出话 —— 不只验 `/api/tags`。
证据：`_evidence/ollama_cleanup_2026-09-19.md`。

---

## 二、原作语料：换了源，因为第一份根本不能用

用户指定"走原作台词语料"。最先拿到的是 hushbugger 的 Deltarune 文本转储（28,768 条 `* ` 台词行）。
**它不能用**：里面 `* ` 行**没有说话人字段**，Susie / Lancer / Ralsei / 旁白全部混排：

```
* Lancer!!!              ← Susie
* What is this time!?    ← Lancer
```

按它建语料 = 把别人的台词标注成 Ralsei —— **这是给训练数据投毒**，比我自己编台词更糟
（至少我编的东西标签是对的）。三条归属策略我都算过：按段落名只剩 116 行且全是战斗教学；
按"段落在讲 Ralsei"覆盖 52% 但那是**场景**不是**说话人**。证据 `_evidence/corpus_build/feasibility.txt`。

**改用 `Deltarunefan/Deltarune-Complete-Transcript-Cleaned`**（HuggingFace，逐行标注 `speaker`）：
第 1~4 章 11,599 行，其中 **Ralsei 825 行**。

建成的 SFT 语料：**853 对**（训练 732 / 验证 121，按场景切分防泄漏），输出侧 31,671 字符，
**95% 带前文**（输入侧只取剧本里真实存在的前文，没有一处是我写的）。

构建过程中修掉一个自己的设计缺陷：第一版遇 Ralsei 台词就清空上下文，
导致 **49% 的对没有前文**（等于教模型"对着空气说话"）—— 而 Ralsei 有 **42% 的台词是连续串的第 2 句之后**。
改成滚动窗口后降到 5% 无前文。

产物：`ralsei_lora_corpus.py`、`_evidence/ralsei_lora_corpus.jsonl`、`_evidence/ralsei_lora_corpus_stats.txt`
（含来源文件 sha256 前 16 位）。**原始游戏文本不入库**（第三方素材，且可随时按脚本重下）。

---

## 三、LoRA：算完账，本机不可行

用户说"试试"，我试的结果是**不该试**，而且这次有数字（上一轮我说"不可行"是没查工具链就下结论，那次是错的）。

| 项 | 实测 |
|---|---|
| CPU / 内存 | 18 逻辑核；内存总 31.6 GB，**可用仅 7.6 GB** |
| GPU | **无**（`torch.cuda.is_available()` False） |
| 训练栈 | **齐**（torch/transformers/peft/datasets/accelerate/bitsandbytes/safetensors），只缺 trl、sentencepiece |
| 底座 | 本机**没有** HF safetensors；`Qwen/Qwen3-4B-Instruct-2507` 需**下载 ≈8 GB** |
| Ollama 的 GGUF | **不能直接训** |
| 转换链 | `llama-quantize` / `convert_hf_to_gguf.py` **均未安装** |

一个 epoch ≈ 8.1×10⁴ token × 20 GFLOP/token ≈ 1.6 TFLOP。
按 25 / 50 / 100 GFLOP/s 折算：**单 epoch 18 / 9 / 4.5 小时**，三 epoch **13~54 小时**。
这还只是训练，不含下载、合并适配器、装 llama.cpp 转 GGUF、量化、重做回归。
内存还有一道硬墙：可用 7.6 GB < bf16 权重 8 GB，只能走 QLoRA。

**外加三处错位**：① 语料 100% 英文，桌宠说中文；② 输入是 Kris/Susie 的剧情台词，
不是主人在聊天框打的话；③ 853 对偏小，3 epoch 容易过拟合、还可能在中文上倒退。

**判断：不开训。** 但这份语料没浪费 —— 它换了用法，见下节。
详见 `code-quality-audit/人味改造-2026-09-18/LoRA可行性评估_2026-09-19.md`。

---

## 四、persona 改造：从「示范句」到「三条规则 + 按场合绑定碎片」（5 轮 A/B）

### 起点：一个实测到的硬毛病 —— 整句复读

高频输入下，4B **逐字吐出 persona 里的示范句**，每次一模一样：

| 输入 | 新旧 persona 都稳定输出 |
|---|---|
| 今天上班好累啊，被领导骂了一顿 | 「被骂了啊……是因为什么事呢？我听着都替你委屈。先坐下歇会儿吧，别的都往后放放。」 |
| 我好喜欢你呀 | 「诶、诶？！你突然说这个干嘛啦……我、我其实也挺喜欢主人的就是了。」 |

这就是用户说的"不像 Ralsei"里最扎人的一部分：一个每天陪聊的桌宠，重复同样三句话。

### 5 轮试错（每轮 6 分钟，同一模型/参数/输入，只换 persona）

| 轮 | 改法 | 新版 特征率 | 新版 生产标记 | 发生了什么 |
|---|---|---|---|---|
| R1 | 加「我的口癖」节 | 94% | 0 | 结巴稳定加进来；同时发现上一轮 A/B 是假的 |
| R2 | 3 条示范句 → 原作短碎片 | 100% | 0 | **复读消失，但回复变短变空** |
| R3 | +「要有实在内容」 | 100% | 1 | 内容回来了，但「杯子打碎了」被接成「诶？真的吗……我也没做什么事呢」——**情绪接错** |
| R4 | + 三条接法规则、碎片按场合绑定 | 100% | 3 | 情绪接对；但「（停顿一下）」频出 |
| **R5** | **修掉我自己埋的雷** | **100%（34/34）** | **1** | 两个维度都赢，复读彻底消失 |

三处值得单独记的：

1. **R2 的"尺子会说谎"**：特征率 100% 的那一版，内容质量反而最差 ——
   计数指标全绿，人读原文才发现它变成打太极。（这正是 MEMORY 里"先读原文再信计数"那条。）
2. **R3 的根因**：碎片只教了"声音"，丢掉了示范句承载的**行为锚**（先接住主人的那件事）。
   补回来的是**三条规则**，不是把示范句放回去 —— 放回去复读就回来了。
3. **R4 的雷是我自己埋的**：我把一组碎片标成「**停顿**、卡住」，模型就把"停顿"写成字面动作「（停顿一下）」。
   **提示词里给分组起的标签，本身就是可能被输出的词。**

### 关于计数公差（这一节比上面那些数字都重要）

R2~R5 的「旧版」**是同一个文件**（md5 `f492ca05`），但它 4 次独立运行的生产标记是 **1 / 2 / 2 / 3**，
特征率是 **94% / 97% / 97% / 97%**。也就是说 **±2 条在生产标记这个量纲上纯属采样噪声**。

所以：
- **站得住**：「复读消失」（定性观察，同一输入从逐字相同变成每次不同）；特征率 100%（对照组 4 次从未到 100%）。
- **站不住，已撤回**：「生产标记从 3 条降到 1 条说明括号动作改善了」——
  准确表述只能是「**没有证据表明变坏**」。

**先量出自己那把尺子的公差，再拿它下结论。** 本轮被"计数"骗了两次。

---

## 五、回归

`persona_chat` 的 A12 断言原为"主人：/ 我： 各 3 组"（few-shot 成对示范）。
实测推翻了那个设计，所以改成「三条接法规则齐备」，并**加了一条反向控制 A12b**：
断言成对示范**确实已经不在了** —— 否则新断言可能在测一份还带着旧结构的文件。
70 → **71 项**。

- G2：**24 套件 / PASS=1070 / FAIL=0 / 全 IDENTICAL**
  证据 `_evidence/g2_after_persona_碎片规则版_2026-09-19.txt`
- 5 轮 A/B 完整对照（含噪声分析）：`_evidence/ab_5rounds_compare.txt`

---

## 六、残留问题（没做完的，如实列）

1. **括号动作没根除**：最终版仍有 1/34 条「（停顿了一下）」。注意 R5 已经把标签里的"停顿"删干净，
   它**照样出现** → 这是**模型层面的习惯**，不是提示词里的词引出来的。
   **真正的解法是生产代码里加闸，不是继续磨提示词。**
2. **生产的两道闸抓不到全角括号动作旁白** —— 而 persona 明令禁止它，说明尺子有洞。
   我在探针里加了一个 `_PAREN_ACTION` 正则（只抓动作词，不搞"见括号就判退"——
   Ralsei 原作 22/825 条真的用括号讲心里话，一刀切会把他的特征一起杀掉），
   **但它目前只在探针里，没进生产代码。**
3. 仍会偶发「（心里想着：…）」这类括号内心独白，且会顺手编一点原作没有的设定。

## 七、下一步

1. **括号动作闸进生产代码**（用探针里那个已经验过误伤面的正则）—— 这是零模型成本、用户看得见的兜底。
2. 探针 `_PAREN_ACTION` 的误伤面还没在**真实对话流**上量过（只在本轮 34 条样本上用过），
   进生产前应先在真机跑一段时间看误伤率。
3. LoRA：**在换机器（有独显）或换更小底座之前不建议重启**。若坚持要试，
   先解决内存（31.6 GB 里 24 GB 被占用）与中文语料两件事。
