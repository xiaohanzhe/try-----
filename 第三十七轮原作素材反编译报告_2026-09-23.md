# 第三十七轮报告：反编译原作素材，落地 87 张场景背景

日期：2026-09-23　｜　仓库根：`try - 副本`　｜　主轴：场景系统线（原作素材）

用户口径（第 37 轮，逐字）：
> 「额，我不会导出，要不还是你自己反编译出来吧」
> 「我下载好了那个你需要的工具了」
> 「请继续执行任务」

承第 36 轮口径：「**开始按照原版的路线**和 `…\DELTARUNE_183049\DELTARUNE` 文件内的素材开始排序，
**场景系统完全遵循原作的逻辑**，**把桌面当成一开始的默认场景就好**」。

---

## 0. 一句话结论

把 DELTARUNE 五章的 `data.win` 全部反编译、按"场景 → 原作房间 → 背景素材"的链路，
为 **87 个作品内场景全部落地了背景图**（12 张原样提取 + 72 张平铺合成，去重后 44 份内容）；
G2 回归 **PASS=1502 / FAIL=0 / 32 套件全 IDENTICAL**；
同时把第 36 轮留下的**恒真判据**升级成真判据，并修掉 3 个此前没被发现的问题（见 §6）。

---

## 1. 工具链

| 项 | 值 |
|---|---|
| 反编译器 | **UTMT CLI v0.9.2.0**（`UndertaleModCli.exe`，用户侧下载） |
| API 参考 | 同目录 `UndertaleModLib.xml`（723 KB，所有脚本 API 都拿它核实，不靠猜） |
| 源文件 | `C:\Users\23002\Desktop\项目文件夹\niko的秘密\DELTARUNE_183049\DELTARUNE\chapter{1..5}_windows\data.win` |
| 导出目录（仓库外） | `E:\Download\_tmp\dr_out\chapter{1..5}_windows\` |
| 工作副本目录 | `E:\Download\_tmp\drw\chapter{1..5}_windows\` |

用到的两条命令：
```
UndertaleModCli.exe dump <data.win> -t --sprites -o <outdir> -v      # 导出纹理 + 精灵
UndertaleModCli.exe load <copy.win> -s <dump_rooms.csx>             # 导出房间结构（不带 -o ⇒ 只读）
```

★ **两条 CLI 怪癖**（都真踩过）：
1. 必须关掉 stdin（`stdin=subprocess.DEVNULL`），否则挂住；
2. 零参数运行会抛 `IOException`，`--help` 在无 stdin 时挂 90 秒。

★ **原始 `data.win` 全程未被改动**：每章导出前后都对原文件做 sha256，五章全部
`orig sha unchanged: True`；房间结构分析一律在**副本**上跑。

---

## 2. 原作机制（GML 实证，不靠文档猜）

* `data.win` 头是 `FORM … GEN8`（GameMaker Studio 2）。
* **`room.Backgrounds` 是空的** —— 背景挂在 **`room.Layers`** 里，`layer.BackgroundData.Sprite`
  才是 `UndertaleSprite`。按文档直觉去读 `Backgrounds` 会得到"所有房间都没背景"的错误结论。
* `LayerType` 枚举：`Path / Background / Instances / Assets / Tiles / Effect / Path2`。
* **多数原作房间本来就没有背景精灵**：ch1 的 147 个房间里只有 **40 个层**带 `bg_sprite`
  （ch2 55 / ch3 40 / ch4 42 / ch5 32）；其余房间用瓷砖条（原作自己就是 `draw_background_tiled`）
  或干脆代码绘制。⇒ 不能把"房间没有 bg_sprite"当成导出失败。
* 房间索引 = `_original_rooms.json` 的 `id`（已验证：ch1 的 20 个房间全对上）。
* ⚠️ 早期（第 33 轮）解析出的 `ROOM/SPRT/OBJT.json` **不可信**（指针基址错），本轮一律以 UTMT 导出为准。

---

## 3. 五章导出结果

| 章 | data.win 大小 | dump 耗时 | 房间数 | 精灵数 | 纹理数 | 原文件未改 |
|---|---|---|---|---|---|---|
| chapter1_windows | 14,658,588 B | 48.0 s | 147 | 2,941 | 21 | ✅ |
| chapter2_windows | — | — | — | 9,435 | — | ✅ |
| chapter3_windows | — | — | — | 14,623 | — | ✅ |
| chapter4_windows | — | — | — | 20,280 | — | ✅ |
| chapter5_windows | 169,526,940 B | 488.6 s | 252 | 23,709 | 91 | ✅ |

合计：**精灵 70,988**、**纹理 261**、房间表 **1,251** 间、`orig_untouched` 五章全 true。

---

## 4. 背景落地口径与结果

### 4.1 命名约定

```
ralsei_pet/assets/scenes/bg/<scene_id 的点换成下划线>.png
例：ch1.card_castle.card_castle_throne  →  ch1_card_castle_card_castle_throne.png
```
场景 JSON 里的 `bg` 字段是相对**场景 JSON 所在目录**的相对路径，`resolve_asset_path()` 是唯一翻译点。

### 4.2 选素材优先级（自上而下，命中即停）

1. **`room.bg_layer`** —— 场景对应房间真的带背景层，且该精灵名在素材池里有 PNG
2. `room.asset_layer` —— 房间资产层引用的精灵
3. `ROOM_OVERRIDE` —— 人工指定的少量例外
4. `AREA_ASSET` —— **区域代表素材表**（按各章实测素材名逐区域策划）
5. `chapter_tiles` —— 章节级瓷砖兜底（**优先 `*_tiles`**）
6. `chapter_fallback`

### 4.3 两种画布口径

* **真背景 → 原样提取**：`shutil.copyfile`，一个像素都不动（保真优先）。
* **平铺素材 → 合成画布**：`alpha_composite` 铺满；画布尺寸
  `宽 1280 / 高 1280 × clamp(rh/rw, 0.5, 0.9)`（防止极端宽高比把图拉成一条）。

### 4.4 结果

```
耗时 8.7s
how 分布: {'room.bg_layer': 9, 'area_table': 78}
OK=84  EXISTS=3  MISS=0  ERR=0   (copy=12  tile=72)
自检：   总场景 87 | 新生成 84 | 已存在跳过 3 | 缺失 0 | 出错 0
内容去重后 44 份
```
逐场景来源分章：ch1 3 真背景 / 16 区域素材；ch2 3 / 18；ch3 0 / 8；ch4 2 / 17；ch5 1 / 19。

### 4.5 三个把口径救回来的关键决定

* **`chapter*10000+local` 的房间 ID**：同一 id 在不同章是不同房间 ⇒ 房间必须与章号联合，否则跨章串味。
* **素材池不能只扫 `bg_` 前缀**：原作还有 `spr_bg_torbathroom`、`spr_mansion_ferris_wheel_bg`
  这类不在 `bg_` 前缀下的背景 ⇒ 池改为 `bg_*` ∪「房间背景层引用名」，`room.bg_layer` 命中 **6 → 9**。
* **章节兜底不能取"面积最大的图"**：会选中 Boss 竞技场
  （`bg_dw_gerson_twisted_base` 960×1200 当了 ch4 城镇、`bg_dw_castle_cafe` 当了整个 ch5）
  ⇒ 改为**优先 `*_tiles`**。

### 4.6 性能坑（差点让进程被 SIGTERM）

`load_assets()` 原本对每个候选名做一次 glob —— ch5 的 `Sprites/` 有 **23,709 项**，
复杂度 O(名字数 × 目录项数)，直接导致**进程在 import 期被杀、零输出**。
修法：`os.listdir` **只列一次**建索引 ⇒ 8.7 秒跑完 87 个场景。

---

## 5. 验证与回归

### 5.1 第 36 轮那个恒真判据已升级（本轮必修项）

原 E3：
```python
check('E3 bg 占位文件当前尚不存在（等待用户导出，属预期）', True, ...)   # ← 恒真
```
**恒真判据比不写还危险**：它看着像在守，其实什么都没守。素材到位后必须换成真判据：

```python
def _bg_health_pairs(pairs):        # -> (missing, not_png)，正负控制共用同一条内核
    for sid, fp in pairs:
        if not isfile(fp): missing.append(sid); continue
        if open(fp,'rb').read(8) != b'\x89PNG\r\n\x1a\n': bad.append(sid)

E3  = 扫描 87 个场景 → missing 与 not_png 都必须为空，且 len(scan) > 0
E3b = 负控制：喂一个不存在的路径 → 必须被抓到
E3c = 负控制：喂 _index.json（存在但非 PNG）→ 必须被抓到
```
升背后该套件 **PASS 50 → 52**（+2 就是两个负控制），整体仍全绿。
基线用**合并模式**重建（`--only scene_route_original --update`），`baseline.json` 只动该一条。

### 5.2 G2 全量

```
合计：PASS=1502 FAIL=0  套件=32          （全部 IDENTICAL，无 DIFF、无 BASELINE）
```
1502 = 第 36 轮的 1500 + 本轮新增的 E3b/E3c 两条。

---

## 6. 本轮发现并修掉的三个问题

### 6.1 沙箱写入通道失效（环境事故，非产品缺陷）

**症状**：G2 里宠物日志栈报
`PermissionError: [Errno 13] 'E:\RalseiMemory\logs\ralsei_pet.log'`，
把 `round17_build_fall` / `round18_climb` 误判成 DIFF。

**逐字节定性**（读的是 diff 全文，不靠猜）：整份 diff 只多了一行环境提示 + 一个空行，
PASS 计数（18 / 53）与 FAIL 计数（0 / 0）一字未变，没有任何断言变化。

**探针矩阵**（Python 实跑，非 PowerShell）：

| 位置 | 新建 | 覆写既有文件 |
|---|---|---|
| `%TEMP%` | OK | — |
| `E:\Download\_tmp` | OK | 本会话新建的 OK ／ 早前建的 **FAIL** |
| `E:\RalseiMemory\logs` | OK | **FAIL** |

文件属性：`mode=0o100666`、`readonly=False` ⇒ **不是 ACL、不是只读位**；
`os.rename` 也报 `WinError 5 拒绝访问` ⇒ **是沙箱策略**。

**处置**：不去改基线（改基线 = 把环境问题固化成预期值，下次环境正常反而假 DIFF），
而是在 `run_all.py` 的 `normalize()` 里加一条**环境噪声归一化**：

```python
_ENV_NOISE = re.compile(r'^\[日志\]\s*文件日志初始化失败')
```
复跑：两套立刻回到 **IDENTICAL** —— 反证定性成立。
★ 判据方向：**先怀疑核验手段（沙箱），再怀疑被测物（产品）**。

### 6.2 3 个套件其实一直**没有基线**（半道闸没关上）

排查 DIFF 时发现 `baseline.json` 只有 **29** 条，而套件有 **32** 套
⇒ `round34_store_config` / `round34b_move_beat` / `round34c_privacy_wire`
一直显示 `BASELINE`（只跑内部断言，**不做字节级漂移比对**）。这三套都是第 34 轮续三新增、
当时基线没落。已用合并模式登记 ⇒ **29 → 32，32 套件全覆盖**。

### 6.3 一个"遗留项"其实不成立：ch1 的 `rooms_map.json` 不用重跑

第 36/37 轮记录的遗留：「ch1 `rooms_map.json` 的 `bg_sprite` 全被解析成类型名 `UndertaleSprite`」。

**实测复核（只读）**：
```
rooms=147   bg_sprite_layers=40   bad(UndertaleSprite)=0
sample names = ['bg_alphysalley','bg_alphysclass','bg_cardcastle_outside','bg_cc_elevator',
                'bg_cc_throneroom','bg_chess_statue_small','bg_darkcastle_front',
                'bg_darkstone_1','bg_darkstone_2','bg_darktown','bg_darkwest','bg_diner', …]
```
40 个背景层**全是真实精灵名**。再对上第 37 轮批处理日志：
```
CHAPTER chapter1_windows   [dump] rc=0 48.0s   [rooms] rc=0 2.6s
rooms_map.json 191665 bytes   rooms in map: 147   orig sha unchanged: True
```
⇒ **`batch_all.py` 那一轮已经用修正版 csx（内含 `ResolveName` 三层回退）把 ch1 重导出过了**。

**旁证**（「推翻依据本身也是证据」）：第 37 轮的映射结果里 ch1 有 **3 个**场景命中 `room.bg_layer`
（`ch1.kris_room.kris_s_room` / `ch1.castle_town.castle_town` / `ch1.card_castle.card_castle_throne`）
—— 若 `bg_sprite` 还是类型名，素材池里绝不可能有叫这个名字的 PNG，命中数会是 0。

### 6.4 顺带：记忆文件压缩（系统强制）

`MEMORY.md` 12270 → **11114 字符**（余量 886）；详版 236598 → **263117 B**。
压缩途中发现并修掉一个**真缺陷：悬空指针** ——
速查本与详版第 2555 行都在引用 `§19`，而**详版从来没有 §19 这一节**（§14/§15/§17/§18/§20 之间是断的）。
按"先补详版再压速查"补写 §19（第 30 轮：产品链路首字实测 + 口径纠正，`aaa89f8`/`8b4d50c`），
复验 40 个指针 **missing=[]**。
★ 教训：**压缩前必须核验"指针指向的章节真的存在"**，否则压缩 = 静默丢信息。

---

## 7. 产物清单

**仓库内**
```
ralsei_pet/assets/scenes/bg/*.png                                  87 张（本轮新增）
code-quality-audit/第37轮-原作素材反编译/
    build_scene_bg.py  make_preview.py  patch_e3.py
    _tools/batch_all.py  _tools/dump_rooms.csx          ← 反编译管线（可复现）
    _tools/build_bg6.py  _tools/rerun_ch1_rooms.py      ← 背景生成 / ch1 复核
    场景背景预览.html                                               按章分组缩略图 + 来源色标
    _evidence/scene_bg_mapping_final.json                           逐场景来源 + how_counts + area_asset
    _evidence/背景自检_final.txt                                     逐场景 [OK] + 尺寸 + how
    _evidence/收尾核验_第37轮.txt                                     本轮收尾的六项证据全文
第三十七轮原作素材反编译报告_2026-09-23.md                            本文件
```

**仓库外临时件（`E:\Download\_tmp\`）**：`build_bg3~6.py`、`batch_all.py`、`dump_rooms.csx`、
`rerun_ch1_rooms.py`、`dr_out/`、`drw/`、`batch_all.log`（用后即删）。

---

## 8. 遗留与待裁定

| # | 事项 | 级别 |
|---|---|---|
| 1 | **78/87 是"区域代表素材"近似**，需人眼过一遍预览页确认语义正确性 | 待用户裁定 |
| 2 | 平铺画布口径（宽 1280 / 高 `1280×clamp(rh/rw,0.5,0.9)`）是否接受 | 待用户裁定 |
| 3 | `bg` 字段**尚无渲染层消费**（P0 只写状态）⇒ 改口径代价极低，但"切场景看得到背景"还没实现 | 下一步 |
| 4 | `progress`（房间级推进，P2）—— 现路由只按 区域/章节/心情/关键词匹配 | 待排期 |
| 5 | 是否要 BGM（`sound_manager` 只有 3 音效、无 BGM 能力）／原作版权尺度 | 待用户裁定 |

---

## 附：可复现命令

```bat
rem 0) 管线脚本已归档进仓库（见 _tools/），仓库外只剩数据
rem 1) 反编译五章（原始 data.win 全程只读，导出前后 sha256 比对）
python code-quality-audit\第37轮-原作素材反编译\_tools\batch_all.py

rem 2) 生成 87 张场景背景（幂等：已存在的跳过）
python code-quality-audit\第37轮-原作素材反编译\_tools\build_bg6.py

rem 3) ch1 遗留项复核（只读，不覆盖任何既有文件）
python code-quality-audit\第37轮-原作素材反编译\_tools\rerun_ch1_rooms.py

rem 4) G2 全量回归（cwd 必须是仓库根）
python code-quality-audit\regress\run_all.py
```
