# 代码质量复审报告（用户视角）

- 项目：`ralsei_pet` 桌面宠物（Ralsei）
- 复审日期：2026-09-12
- 复审范围：`ralsei_pet/` 全部模块 + `src/main.py`（7398 行）+ 资源链路（1106 帧动画 / 50 张表情）
- 方法：静态审查（AST 全量扫描）+ 运行时探针 + 资源存在性校验 + 全项目编译校验
- 本次结论：**发现并修复 3 个用户可见/可扩展性缺陷；另有 4 类问题给出分级建议**

---

## 一、结论摘要

| 级别 | 问题 | 状态 |
|---|---|---|
| P1 | 游戏失败时头像渲染成灰色「?」占位图 | ✅ 已修复 |
| P2 | 配置备份文件无上限，无限堆积（实测 106 个） | ✅ 已修复 |
| P2 | 表情解析兜底会静默生成不存在的素材名 | ✅ 已加固 |
| P2 | 25 个被触发的事件被 `react_to_event` 静默忽略 | ⏳ 待决策 |
| P3 | `main.py` 中 12 个 `check_*` 为复制粘贴模板 | ⏳ 建议重构 |
| P3 | 死代码：整模块 `pet_interaction.py`、8 个 `_DEPRECATED`、2 个 API 方法 | ⏳ 建议清理 |
| P3 | 潜在陷阱：死代码内仍残留缺陷，误调用即出错 | ⏳ 建议清理 |

**验证结果**：全项目 `compileall` 通过（0 语法错误）；209 处表情调用点解析后 **全部命中真实素材**（修复前 1 处失败）。

---

## 二、已修复问题（含证据）

### P1 ✅ 游戏失败时头像变成灰色「?」

**证据链**
- `entertainment_system.py:343` 在「游戏未获胜」分支传入了表情键 `"encouraging"`。
- `dialogue_ui._FACE_MAP` 中没有该键 → `_resolve_face_name()` 走兜底 `"face_" + face_type` → 得到 `face_encouraging`。
- `ralsei_face/` 中不存在 `face_encouraging.png`（实际只有 50 张 `face_*.png`）。
- `sprite_loader.get_face()` 对缺失素材返回 `create_placeholder_image((30,30))` —— **灰色 30×30 的问号方块**。

**用户视角表现**：每次玩小游戏失败，Ralsei 的对话框贴图会变成一个灰色问号，后续其它消息才会恢复正常。

**修复**（两层，既治标也治本）
1. `dialogue_ui._FACE_MAP` 补上 `"encouraging": "face_normal_smile_little"`（安抚性微笑，贴合"下次加油"语义）。
2. `sprite_loader` 新增 `has_face(name)`；`dialogue_ui.set_face()` 解析后校验，缺图则回退 `face_normal`。
   → 今后任何新增/拼错的表情键都**只会退回 normal 头像，不会再出现灰问号**。

**验证**：`_resolve_face_name("encouraging") == "face_normal_smile_little"`；`has_face("face_encouraging") == False`；全量审计 209 处调用点坏键 **1 → 0**。

---

### P2 ✅ 配置备份无限堆积

**证据**：程序目录下已积累 `config.json.backup.*` **106 个**（时间跨度 2025-12-23 ~ 2026-09-12）。`config_manager._backup_config()` 只写入、从不清理。

**用户视角表现**：随着版本演进/重启，程序目录被垃圾备份文件持续污染，且不会被回收。

**修复**：`_backup_config()` 写入后调用新增的 `_prune_old_backups()`，**只保留最近 `MAX_CONFIG_BACKUPS = 10` 个**；按文件名时间戳排序，单文件删除失败仅记 debug 日志、不影响主流程。

**验证**：15 → 10（保留最新 10 个）；端到端 12 个历史 + 1 次真实备份 → 最终 10 个。

> 注：现存 106 个历史备份**未代为删除**（涉及你的桌面目录，遵循"只读扫描、不擅自删除"原则）。程序下次启动会自动收敛到 10 个；如需立即清理，请手动删除 `ralsei_pet/config.json.backup.*`。

---

### P2 ✅ 表情解析兜底静默失灵（加固）

原 `_resolve_face_name()` 兜底为 `"face_" + face_type`，对**任何未知键**都会生成一个大概率不存在的素材名，并由 `get_face()` 静默返回灰问号——这是一类"不会报错、只会变丑"的隐性缺陷。已在 `set_face()` 增加存在性校验并回退 `face_normal`（见 P1 修复 2）。

---

## 三、待处理问题（附证据，建议后续处理）

### P2 ⏳ 25 个被触发的事件被静默忽略

`pet_ai.trigger_event()` 只处理 5 个事件（`game_start` / `creative_activity_start` / `level_up` / `evolution` / `pet_interaction`），其余全部转给 `emotion_system.react_to_event()`；而后者仅覆盖 10 个。

**AST 全量比对结果**：全项目共触发 37 种事件，其中 **27 种在 `react_to_event` 中无分支**；扣除 `pet_ai` 已处理的 2 个，**仍有 25 种事件是彻底的空操作**：

```
data_backup_successful / data_backup_failed / data_restore_successful / data_restore_failed
found_interesting_file / found_window
saw_deltarune_game / saw_deltarune_video / saw_food / saw_gun_game
saw_interesting_video / saw_recycle_bin / saw_sad_content / saw_self_related
scheduled_task_executed
system_critical_battery / system_high_cpu / system_high_memory
system_low_battery / system_low_disk / system_no_network / system_optimized
temp_files_cleaned / temp_files_clean_failed
user_dragged_forcefully
```

**用户视角影响**：系统监控告警（CPU/内存/电量/磁盘/网络）、数据备份结果、桌面清理、游戏/视频/美食识别等事件发生时，Ralsei **完全没有情绪/表情反应**；调用方精心计算的 `event_data`（如 CPU 百分比）被直接丢弃。

**建议**：为上述事件补齐情绪分支（或至少统一记录「未处理事件」告警，便于发现遗漏）。

---

### P3 ⏳ `main.py` 12 个 `check_*` 是复制粘贴模板

`main.py:3349–3581` 的 12 个方法结构完全相同，仅消息列表与表情键不同：

```
check_task_management / check_time_tracking / check_meeting_reminders / check_file_organization
check_quick_notes / check_email_management / check_schedule_planning / check_project_management
check_work_efficiency / check_document_collaboration / check_meeting_recording / check_work_life_balance
```

每个约 17 行、结构为「`import random` → 可选按 `9<=hour<18` 分支 → `random.choice(列表)` → `add_dialogue(..., face) → show_dialogue()`」，合计约 **230 行重复**；`import random` 在函数内重复 12 次。

**扩展踩坑**：新增一个"XX 管理"功能就必须再复制一份 17 行模板；改一处交互逻辑要改 12 处。

**建议**：抽成数据驱动，例如
```python
_CHECK_TOPICS = {
    "task_management": {"work": [...], "off": [...], "face": "helpful"},
    ...
}
def _emit_help(self, topic): ...
```

---

### P3 ⏳ 死代码（含潜在陷阱）

| 位置 | 说明 |
|---|---|
| `modules/pet_interaction.py`（整模块） | `PetInteractionTracker` / `BodyRegionMapper` / `GestureTracker` **全项目从未被导入**，全部不可达 |
| `emotion_system.get_face_for_emotion_DEPRECATED` | 无任何调用；**内部含 `'expectant' → 'face_expectant'`（素材不存在）**，一旦被误用即出现灰问号 |
| `customization_system` 6 个 + `dialogue_system` 1 个 `*_DEPRECATED` | 均无调用 |
| `main.py:5940 send_api_request` / `main.py:6411 _async_api_request` | 无任何调用；其中 `send_api_request` 内 `if 'agent_id' in self.api_config` 恒为真（默认配置总带空 `agent_id`），会把 URL 拼成 `.../v1/agents//chat/completions` —— 属**被埋起来的地雷** |

**建议**：确认无用途后整体删除，或至少加上显式 `DeprecationWarning` 与注释说明；`send_api_request` 的 `agent_id` 判空应改为 `if self.api_config.get('agent_id')`。

---

## 四、已验证「无问题 / 历史已修复」项

| 项 | 结论 |
|---|---|
| 删除文件走回收站 | ✅ 已改用 `shellcon.FO_DELETE` + `FOF_ALLOWUNDO`；失败时返回 False，**不再静默降级为永久删除** |
| `clean_temp_files` / `cut_file` | ✅ 已修复 |
| `memory_system` 保存/加载 | ✅ `__init__` 正确初始化 `long_term_memory`；写盘为「临时文件 + 原子替换」，加载含全键补全。运行时探针通过 |
| 日志中 `保存记忆失败: ... has no attribute 'long_term_memory'` | ⚠️ 来自**旧构建/半初始化对象**，当前代码**不可复现**（已用探针验证正常路径成功） |
| `floor_manager.get_current_floor` / `get_drop_destination` | ✅ 无匹配时兜底返回 `desktop_floor`，不会返回 `None` |
| 精灵动画映射（`animation_mapping`） | ✅ 引用素材全部存在 |
| `config_manager.set()` 拒绝非法 key | ✅ 防御正确；启动日志中的告警来自自检用例，非正常流程 |
| 全项目编译 | ✅ `compileall` 通过，0 语法错误 |

---

## 五、附录：可复现的检查命令

已在项目根目录保留 3 个可复用脚本：

```bash
# 1) 全量表情键审计（调用点 + _FACE_MAP + face_map → 素材存在性）
C:/Python311/python.exe _audit_faces_full.py

# 2) 事件覆盖度审计（被触发事件 vs react_to_event 已处理事件）
C:/Python311/python.exe _audit_events.py

# 3) 修复回归验证（表情解析 / has_face / 备份清理）
C:/Python311/python.exe _verify_fixes.py

# 4) 全项目语法编译
C:/Python311/python.exe -m compileall -q -f ralsei_pet/modules ralsei_pet/src
```

---

## 六、修改文件清单

| 文件 | 变更 |
|---|---|
| `ralsei_pet/modules/dialogue_ui.py` | `_FACE_MAP` 新增 `"encouraging"`；`set_face()` 增加缺图回退 `face_normal` |
| `ralsei_pet/modules/sprite_loader.py` | 新增 `has_face()` 及表情名缓存 `_face_names` |
| `ralsei_pet/modules/config_manager.py` | 新增 `MAX_CONFIG_BACKUPS` + `_prune_old_backups()`，`_backup_config()` 写入后自动清理 |
