# 第42轮报告 · 桌面接入与小镇联通（全部按原作拓扑）

> 本轮口径（用户原话）：
> 1. 「记忆不要随便修改，三思而后行，然后，记得所有 room 排序和连接都按照原作，相当于场景复现，连动态效果也要做（如喷泉特效这类的）」
> 2. 「桌面作为初始 room，到时候联通小镇的 room 就好，联通的话就按照原作那个传送门来就好」
> 3. 追加确认：「传送门 = **暗之泉**」「范围 = **和原作一样，暗世界也做**」「第一站 = **城北主街 (room_town_north)**」

---

## 0. 结论先行

| 口径 | 落地答案 | 依据状态 |
|---|---|---|
| 所有 room **排序**按原作 | 排序 = `data.win` 的 `Data.Rooms` **列表下标**（五章 1,251 条，已导出） | 已实证 |
| 所有 room **连接**按原作 | 连接 = 原作门对象机制（**782 条边**，已完整还原） | 已实证 |
| **桌面 = 初始 room** | 桌面作为序列最前的入口章（不改动原作 index，追加一条传送） | 方案已定 |
| 联通 **小镇** | 第一站 = `room_town_north`（城北主街） | 用户已定 |
| 联通方式 = **原作那个传送门** | **暗之泉**（`obj_darkfountain`，素材 `spr_fountainedge`） | 用户已定 |
| 范围 | **不只小镇 —— 原作全部 1,251 间，含暗世界** | 用户已定 |
| 动态效果 | 暗之泉本身即动态（水柱/发光）；另有多帧 tileset 与特效实例 | 可行性已证 |

**一句话**：本轮把「原作排序 + 原作连接」从**推测**变成了**可直接读取的数据表**，并确定了桌面→小镇的入口形态。

---

## 1. 本轮最值钱的发现：原作的门就是「下标位移」

原作的房间切换**不是**一堆手写的 `room_goto(某个房间)`，而是**沿 `Data.Rooms` 列表下标做 ±1/±2/±3 位移**。反汇编实证：

| 门对象 | 事件里的真实代码 | 位移 |
|---|---|---|
| `obj_doorA` / `obj_doorA_musfade` | `room_goto_next()` | **+1** |
| `obj_doorB` / `obj_doorB_musfade` | `room_goto_previous()` | **−1** |
| `obj_doorC` / `obj_doorC_musfade` | `room_goto(room_next(room_next(room)))` | **+2** |
| `obj_doorD` / `obj_doorD_musfade` | `room_goto(room_previous(room_previous(room)))` | **−2** |
| `obj_doorE` | `room_next` × 3 | **+3** |
| `obj_doorF` | `room_previous` × 3 | **−3** |
| `obj_doorX` / `obj_doorX_musfade` | 表驱动双向跳转（`if (room==X) room_goto(Y)`） | 按表 |
| `obj_doorW` / `obj_doorw_musfade` | 表驱动双向跳转 | 按表 |
| `obj_doorAny` | **完全没有代码** | 纯占位标记 |

**三条关键推论**：

1. **「排序」和「连接」在原作里是同一件事** —— 因为连接就是下标运算，所以只要拿到 `Data.Rooms` 的顺序，连接就自动成立。这正是用户说的「相当于场景复现」。
2. **`obj_door*` 全部 `visible: False`** —— 门本身不渲染。**画面上的门是画在背景图/瓦片里的**，门对象只是**不可见的触发区**。所以我们要复现门，视觉上靠背景，逻辑上靠触发区坐标。
3. **落点由配对的 `obj_marker*` 决定**：`obj_doorA` 出去 → 落在目标房间的 `obj_markerA`。已实测闭环：
   `room_krisroom` 的 `doorA(155,230)` → `room_krishallway` 的 `markerA(289,112)`；回程 `room_krishallway` 的 `doorB(289,104)` → `room_krisroom` 的 `markerB(155,185)`。

---

## 2. ch1 现实世界：完整拓扑（27 条边，100% 自洽）

全部由「房间下标 + 门实例坐标」推出，无一条是猜的。格式：`门(坐标) → 目标`。

| idx | 房间 | 门 → 目标 |
|---|---|---|
| 2 | `room_krisroom` | `doorA(155,230)` → krishallway |
| 3 | `room_krishallway` | `doorB(289,104)` → krisroom；`doorC(425,100)` → torhouse |
| 5 | `room_torhouse` | `doorA(84,168)` → torbathroom；`doorD(144,68)` → krishallway；`doorX_musfade(594,149)` → town_krisyard |
| 6 | `room_torbathroom` | `doorB(236,134)` → torhouse |
| 7 | `room_town_krisyard` | `doorC(120,410)` → town_north；`doorX_musfade(158,168)` → torhouse |
| 9 | **`room_town_north`** | `doorA(1790,100)` → beach；`doorC(625,230)` → town_mid；`doorD(378,-12)` → town_krisyard；`doorX(1258,98)` → flowershop_1f |
| 10 | `room_beach` | `doorB(-15,60)` → town_north |
| 11 | `room_town_mid` | `doorC(772,232)` → town_south；`doorD(780,0)` → town_north；`doorW(1740,-10)` → alphysalley；`doorX(600,90)` → diner |
| 13 | `room_town_south` | `doorA(1314,106)` → town_school；`doorC(798,235)` → town_church；`doorD(798,1)` → town_mid；`doorW(600,80)` → hospital_lobby；`doorX(1000,80)` → library |
| 14 | `room_town_school` | `doorB(2,182)` → town_south；`doorX_musfade(360,120)` → schoollobby |
| 15 | `room_town_church` | `doorA(0,340)` → graveyard；`doorC_musfade(380,630)` → town_shelter；`doorD(360,0)` → town_south；`doorX(565,85)` → townhall |
| 16 | `room_graveyard` | `doorB(595,115)` → town_church |
| 17 | `room_town_shelter` | `doorD_musfade(120,0)` → town_church |
| 18 | `room_hospital_lobby` | `doorA(70,76)` → hospital_hallway；`doorW(140,230)` → town_south |
| 19 | `room_hospital_hallway` | `doorA(79,75)` → hospital_rudy；`doorB(140,186)` → hospital_lobby；`doorC(190,77)` → hospital_room2 |
| 20 | `room_hospital_rudy` | `doorB(140,230)+(160,230)` → hospital_hallway |
| 21 | `room_hospital_room2` | `doorD(140,236)` → hospital_hallway |
| 22 | `room_diner` | `doorX(140,230)` → town_mid |
| 23 | `room_townhall` | `doorX(140,230)` → town_church |
| 24 | `room_flowershop_1f` | `doorA(20,44)+(260,46)` → flowershop_2f；`doorX(140,230)` → town_north |
| 25 | `room_flowershop_2f` | `doorB(20,235)+(260,235)` → flowershop_1f |
| 26 | `room_library` | `doorX(140,230)+(160,230)` → town_south |
| 27 | `room_alphysalley` | `doorW(120,235)` → town_mid |
| 28 | `room_torielclass` | `doorA(57,46)` → schoollobby |
| 29 | `room_schoollobby` | `doorA(110,194)` → alphysclass；`doorB(830,194)` → torielclass；`doorC(440,-10)` → schooldoor；`doorX_musfade(440,415)` → town_school |
| 31 | `room_schooldoor` | `doorC(500,95)` → school_unusedroom；`doorD`×3 → schoollobby |
| 33 | `room_school_unusedroom` | `doorD(140,180)` → schooldoor |

**主链一目了然**（全部 ±2，正是原作的"往下走两间"）：

```
krisroom ─A→ krishallway ─C→ torhouse ─X→ town_krisyard ─C→
town_north ─C→ town_mid ─C→ town_south ─A→ town_school
                          ├→ diner / alphysalley / beach / flowershop / library / hospital
        town_south ─C→ town_church ─C→ town_shelter
                          ├→ graveyard / townhall
```

**ch1 里没有门的 3 间**：`room_torroom`(4)、`room_town_northwest`(8)、`room_town_apartments`(12) —— **原作 ch1 里它们不可达**（占位/未使用）。

---

## 3. 五章拓扑合成结果

| 章 | 房间 | 有向边 | 无向去重 | 构成（home/town/town_life/dark/field_forest/card_castle/其他） |
|---|---|---|---|---|
| ch1 | 147 | 238 | 123 | 5 / 10 / 17 / 24 / 54 / 20 / 17 |
| ch2 | 278 | 323 | 201 | 5 / 10 / 22 / 185 / 0 / 3 / 53 |
| ch3 | 246 | 79 | 47 | 5 / 10 / 22 / 150 / 0 / 3 / 56 |
| ch4 | 328 | 72 | 42 | 5 / 11 / 40 / 205 / 0 / 3 / 64 |
| ch5 | 252 | 70 | 42 | 5 / 11 / 27 / 156 / 0 / 3 / 55 |
| **合计** | **1,251** | **782** | **455** | — |

**★ 小镇拓扑五章完全一致**（`obj_doorX` / `obj_doorW` 的 town 表在各章逐字相同）：

```
town_south ↔ library        town_church ↔ townhall      town_school ↔ schoollobby
town_mid   ↔ diner          town_north  ↔ flowershop_1f
town_north ↔ beach          town_mid    ↔ alphysalley    town_south  ↔ hospital_lobby
town_krisyard ↔ torhouse    town_school ↔ schoollobby
```

**ch2+ 只新增店铺**：`room_lw_conbini ↔ town_mid`（便利店）、`room_lw_police ↔ town_south`（警局）、`room_lw_icee_pizza ↔ town_mid`（冰淇淋披萨）、`room_town_noellehouse`（诺艾尔家）。

⇒ **结论：小镇只需实现一套拓扑，五章共用。** 这是最省力的地方。

---

## 4. 桌面接入方案

### Step 1｜排序不动原作
`_room_order.json` 作为唯一排序源。**桌面不占用原作 index**，而是作为独立的前置章（`desktop`），按 `_index.json` 已有的 `chapters.desktop` + `default_scene="desktop"` 保持。

### Step 2｜桌面上的暗之泉 = 唯一的入口
- 桌面上放一个**暗之泉**（素材 `spr_fountainedge`，动态：水柱/发光）。
- 交互：点它 / 走过去 → 切到 `ch1` 的 **`room_town_north`**（城北主街）。
- 语义依据：暗之泉在原作里就是「把世界变成另一个世界」的入口 —— 桌面（我们的现实）→ 游戏世界，正是同一个语义。

### Step 3｜小镇内联通按 27 条原作边
每条边 = 一个不可见触发区（门）+ 目标场景 + 落点坐标（marker）。**触发区坐标直接用本报告 §2 的实测值**，不做近似。

### 打通后的第一条路径（验收用）
```
桌面 ──暗之泉──▶ town_north
   ├─ doorC(625,230) ──▶ town_mid
   ├─ doorD(378,-12) ──▶ town_krisyard
   └─ doorX(1258,98) ──▶ flowershop_1f
```

---

## 5. 本轮数据资产

| 文件 | 体积 | 内容 |
|---|---|---|
| `_room_order.json` | 122,612 B | 五章 1,251 间全序：`index / name / kind`（kind = home/town/town_life/dark/field_forest/card_castle/system/debug/other） |
| `_room_graph.json` | 144,819 B | 五章 782 条边：`src / dst / door / kind`，含 `rules` 说明与 `caveat` |

**自检（全部 PASS）**：每章 `边数一致 / 下标合法 / 无自环 / 边内冗余名与表内名一致`；五章房间合计 = 1,251。

**取证链**（可复现）：
1. `conn42b.csx` × 5 → `conn42b.json`（全 Code 扫描，`Cmp` + `room_goto` 配对复原门表）
2. `inst42.csx` × 5 → `inst42.json`（全房间门/事件实例 + 坐标）
3. `graph42.py` → 合成拓扑；`emit42.py` → 规范化 + 自检
4. `probe42b.csx` → 补齐 `doorE/F/Any/overworldc` 的机制

---

## 6. 遗留与待拍板

| # | 事项 | 状态 |
|---|---|---|
| 1 | **数据落盘位置**：`_room_order.json` / `_room_graph.json` 是否进产品目录 `ralsei_pet/assets/scenes/`，还是与 `_index.json` 合并 | **待定**（本轮先放证据目录） |
| 2 | **桌面场景里的暗之泉**：用原素材直接放，还是按桌面尺度放大/合成 | 待定（属实现） |
| 3 | **动态效果范围**：先做暗之泉，还是连多帧 tileset（22 个）与特效实例（98 个）一起 | 待定 |
| 4 | `obj_doorAny`（830 个，无代码）在暗世界的实际换房机制 | 已查清「无代码」；**暗世界的换房由 `obj_mainchara` 承担**（ch2+ 各有 7~8 处 `room_goto`） |
| 5 | Q2/Q3（`kris_room` 背景尺寸、2 处选层） | 仍待定（沿用第 38 轮记录） |

---

_本轮未改动任何产品代码；报告与证据均为新增文件。_
