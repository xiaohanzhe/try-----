# 第 46 轮报告 · 伙伴交互系统框架（P0~P2 骨架）

> 轮次：第 46 轮（承第 45 轮"场景自主寻路"）
> 产物：3 个新模块 + 1 套新回归锁 + 1 份鉴别力体检 + 设计文档更新
> 状态：**P0~P2 完成；P3（接线）/ P4（灌人设）按用户口径待命**

---

## 0. 一句话结论

用户要的"多宠物互相互动"框架已经**搭好并锁死**：三个零依赖模块
（`companion` / `companion_dialog` / `companion_roster`）覆盖
**实体 + 对话协议 + 名册调度 + 毛毛虫跟随 + 可交互物协议**；
新增回归锁 **216 项断言全绿**，鉴别力体检 **17 项破坏全部报红**；
G2 全量 **42 套件 / PASS=2099 / FAIL=0 / 全 IDENTICAL** ——
即**对现有产品的行为影响为 0**（用户说"现在还用不上，先搭框架"，这正是要的效果）。

---

## 1. 交付物

| 类型 | 路径 | 规模 |
|---|---|---|
| L1 实体层 | `ralsei_pet/modules/companion.py` | `Companion` / `InteractBus` / `Interactable` / `ReadableInteractable` |
| L2 对话层 | `ralsei_pet/modules/companion_dialog.py` | `Message` / `format_line` / `check_reply` / `DialogueLog` / `build_speaker_header` |
| L3 调度层 | `ralsei_pet/modules/companion_roster.py` | `Roster` / `ChatScheduler` / `step_follow` / `select_mode` / `CompanionStage` |
| 回归锁 | `code-quality-audit/第46轮-伙伴交互框架/verify_companion46.py` | **216 PASS / 0 FAIL**，已进 G2（id `companion_round46`） |
| 鉴别力体检 | `code-quality-audit/第46轮-伙伴交互框架/disc_companion46.py` | **17/17 破坏报红**，还原后逐字节一致 |
| 取证 | `code-quality-audit/第46轮-伙伴交互框架/_evidence/伙伴框架骨架46.md` | 接口↔原作对照表 + 6 个真问题 + 3 条判据教训 |
| 设计文档 | `伙伴交互系统_框架设计_2026-09-25.md`（更新） | 差异说明 + 实装修正 + 状态 |

---

## 2. 用户 8 条口径 → 逐条落点

| # | 用户原话（节选） | 落点 | 状态 |
|---|---|---|---|
| 1 | 「嵌入一下和伙伴的交互系统，方便以后多宠物互相互动」 | `Roster`（2 个队友位）+ `CompanionStage` 门面 | ✅ 框架就绪 |
| 2 | 「做到和真人间对话一样就好」 | `Message(text, from_id, to_id, expr, typer)` 四元组 + 打字机速度 + 表情位 | ✅ |
| 3 | 「我会准备另外两个 7B 的设定」 | `MAX_COMPANIONS = 2`（照抄 `scr_setparty(arg0, arg1)`，**数量正好对上**） | ✅ 留位待灌 |
| 4 | 「一般不会有多个 AI 同时跑；多个一起跑看不出端倪也行」 | `ChatScheduler` **单线程轮转**：`busy` 时 `tick()` 恒 `None`。断言 = 200×5 次 tick **零并发** | ✅ |
| 5 | 「能自主互相聊天，但不会导致 AI 之间分不清谁和谁说话」 | **发言人标识协议**：`from_id` 必填且必可解析（缺失 ⇒ 拒 + 不进历史）、上下文每行带前缀、`to_id` 点名可见、自称校验、代他人发言检测 | ✅ |
| 6 | 「跟随按原作，跟 kris；也可自主行动，需要统一行动时才跟随」 | 毛毛虫式**滞后采样**（25 帧缓冲 / 滞后 12、24 帧 / 暗世界 ×2）+ 三态 `FREE`（默认）/ `FOLLOW` / `RALLY` | ✅ |
| 7 | 「背景音乐就用原作的吧」 | 设计文档 §7 已定 `BgmManager` 接口（play/stop/fade/query） | ⏸ **P3 接线时实现** |
| 8 | 「原作里该有的可交互的东西也要有，效果和原作一样」 | `Interactable` 协议：`interact()` → `on_interact()` + `global.interact` 锁 + `onebuffer = 5` 防抖（**三个状态值/数值全部照抄**） | ✅ 协议就绪 |

> 另：#9「房间表要用全部 1014 个」已在第 46 轮上半场查证（1014/1014 全量注册，见上一份取证）；
> #10「路线做节点、按节点数值差决定方向」= 待办 **#48**，尚未开工。

---

## 3. 三条设计律（本轮的"不可回退"部分）

1. **找不到就是不找到** —— 身份解析失败返回 `None`，绝不回落到"可能是他"。
   断言：`I4`（`resolve('sans') is None`）。
2. **锁必须能释放** —— 交互对象抛异常 / 对话实例凭空消失 / 生成失败，
   都必须把 `global.interact` 那把锁解开。一次异常不解锁 = **所有可交互物一起失灵**。
   断言：`D9`/`D9b`/`D12b`。
3. **数值照抄，出处可查** —— `12 / 24 / 25 / 2 / 5 / 30` 每个都能指到原作脚本；
   **本项目自创的数值（RALLY 倍率 4、远距阈值 160）单独标注**，不与照抄值混放。
   断言：`B1~B9`。

---

## 4. 施工中抓到的 6 个真问题

| # | 问题 | 严重度 |
|---|---|---|
| 1 | `sample_at(lag)` **差一帧**（`trace[-lag]` 实际只滞后 `lag-1`）⇒ "滞后 12 帧"是假话 | 中 |
| 2 | `InteractBus.blocked_count` **恒为 0**（重复的锁判定绕过了计数）⇒ 死字段 | 低（但"看着在统计其实没统计"很危险） |
| 3 | `format_line` 不接 `alias_map` ⇒ 用别名说话被打成 `[未知说话人]` ⇒ **功能实际不可用** | **高** |
| 4 | `NotImplementedError` 被 `except Exception` 吞掉 ⇒ "没实现交互"静默无效 | 中 |
| 5 | 局部 import 藏在 `select_mode` / `apply_modes` 里 ⇒ 依赖清单静态看不全 | 低（可审计性） |
| 6 | 测试用例复用了被改过 `mode` 的实例 ⇒ 1 条断言**假报红** | 低（假报红会掩盖真问题） |

**判据侧另 3 条教训**（详见取证 §三）：
判据报红先看自己的代码（A4）、文本判据会咬到文档字符串（A5）、
鉴别力体检里"破坏手法失效"与"判据恒真"必须分开（破坏 14）。

---

## 5. 顺手发现的**第 45 轮遗留基线漂移**（真问题）

`round5_smoke` 的基线还停在 **44 个模块**（= 加 `scene_walk.py` **之前**）。
⇒ **第 45 轮"G2 全 IDENTICAL"这个结论，在 `round5_smoke` 这一条上不成立**。
本轮一并修正（44 → 48），用 `--only round5_smoke --update`（**合并模式**）重固，
其余 41 套件基线未动。

---

## 6. 未做（刻意）与下一步

**本轮刻意不碰**：`main.py` / `scene_controller` / 任何 UI / BGM 实现 / 寻路接入。
理由 = 用户口径「这个交互系统先设计成这样，我现在还不用，所以你先按照这个搭框架吧」。
证据 = G2 42 套件全部 IDENTICAL + `scene_p0` 的 A6 白名单**不需要改**。

**下一步（等用户点头/给人设）**：

| 阶段 | 内容 | 前置 |
|---|---|---|
| **P3** | 接线：`main.py` 挂 `CompanionStage`；`scene_controller.switch()` 联动 BGM 淡出；交互锁接到现有对话框状态 | 用户说"可以用" |
| **P4** | 用户提供的 2 个 7B 人设灌入名册 + 人味审计 | 用户给人设 |
| **#48** | 房间间路由改成"节点 + 数值差"驱动（用户：「做节点，根据节点所设定的数字之差来决定向哪边走就好」） | 独立任务 |
| **#49** | 接入原作 BGM 与可交互物到场景系统 | P3 同批 |
| **#50** | ch3 room 加门控（数据保留、暂不投入使用） | 独立任务 |

**仍待用户裁定**（沿用前轮）：
伙伴的动作空间（只跟随+说话，还是也能自由走动）；
自主对话的"话题从哪来"（已留可插拔点 `topic_source`，默认策略未定）。
