# H5 S2 / S3 动画表外部化 —— 实施与验证报告

- 日期：2026-09-13
- 范围：`架构改造排期方案_H4-H5_2026-09-13.md` 中 H5 的 **S2（外部化）** 与 **S3（别名/历史组显式化）**
- 前置：H5 S1（动画名未命中自检，见 `H5-S1_动画名自检_实施与验证报告_2026-09-13.md`）、G2 一键回归基线
- 提交：`77b01a1`（G2 基线）→ `b099e4c`（S2）→ `94e4902`（S3）→ `2dc9916`（S2 验证补契约）

---

## 0. 结论先行

| 项 | 结果 |
|---|---|
| S2 动画表外部化 | **完成**。配置落到 `ralsei_pet/assets/animations.json`，与源码硬编码表**逐项等价**（109 组 / 377 帧，键集合、键顺序、每组帧列表、总帧数全等） |
| S3 别名/历史组显式化 | **完成**。4 组别名改用 `alias_of`，28 组标 `legacy:true`；**运行语义不变**（未改 `sprite_loader.py` / `main.py`） |
| 可回滚性 | 任何 JSON 异常（语法 / 版本 / 空 / 类型 / 别名悬空）**一律回落内置表**，5 例全覆盖 |
| 回归兜底 | G2 一条命令跑 6 套件，**153 PASS / 0 FAIL**，连续裸跑全部 `IDENTICAL` |
| 未做的事（有意） | 自动扫描**未降级**为"只报告"。理由是它会改变用户可见的容器尺寸（见 §5） |

---

## 1. S2：配置外部化

### 1.1 产物

- `ralsei_pet/assets/animations.json` —— 21,809 字节，`schema_version: 1`，109 组 / 377 帧
- `code-quality-audit/架构改造-H4H5/extract_animations_json.py` —— 单向导出器（AST 解析源码字面量）

格式：

```json
{
  "schema_version": 1,
  "meta": { "generated_by": "...", "source": "...", "note": "..." },
  "groups": {
    "idle":    { "frames": ["spr_ralsei_idle_0.png", "..."], "comment": ["..."] },
    "happy":   { "frames": [], "alias_of": "laugh", "comment": ["..."] },
    "button_press": { "frames": ["..."], "legacy": true },
    "some_group":   { "frames": ["..."], "offset": [0, -10], "loop": false }
  }
}
```

### 1.2 加载策略（JSON 优先 + 内置表兜底）

- 路径解析：环境变量 `RALSEI_ANIMATIONS_JSON` 优先，否则取 `ralsei_pet/assets/animations.json`
- 校验（`sprite_loader._validate_animation_config`）：
  `schema_version` 白名单 → 组名合法（非空、无首尾空白）→ `frames` 为字符串列表 → `loop` 布尔 →
  `offset` 为 `[int, int]` → `alias_of` 字符串 → `legacy` 布尔 → `comment` 为 `str` 或 `list[str]`
- **未知字段只 warning 不拒绝**（这是"配置里拼错键会静默失效"的根源，见 §4）
- 任何一步抛异常 → 记 ERROR 日志 → 回落内置表
- 新增只读接口：`animation_config_source`（`'json'`/`'builtin'`）、`legacy_animations`、`position_offset`、
  `get_animation_config_report()`、`get_unconfigured_asset_report()`

### 1.3 等价性证明（防"迁移时偷偷改坏了"）

静态侧（`verify_s2_animations_json.py` B 段）——与源码字面量比对：

- B1 键集合相同：`builtin=109 / json=109`
- B2 键顺序相同（导出保持源码顺序）
- B3 逐组帧列表逐项相等：不一致组 = 空
- B4 总帧数相同：`377`

运行侧（C 段）——JSON 来源 vs 强制内置来源两个 loader 实跑 `load_sprites()`：

- C7 `sprites` 键集合相同：`489 / 489`
- C8 每组帧数逐个相同：不同组 = 空
- C9 `frame_container_size` 相同：`(222, 110) / (222, 110)`
- C10 加载到的总帧数相同：`1461`

导出器幂等（A4）：同一输入连续两次导出逐字节一致（21,319 字节）。

---

## 2. S3：别名与历史组显式化

### 2.1 数据层的两处显式化

**别名（4 组）** —— 原来靠"把目标组的帧文件名再抄一遍"来表达复用，现在写 `alias_of`，自身 `frames` 清空：

| 组 | `alias_of` |
|---|---|
| `happy` | `laugh` |
| `neutral` | `idle` |
| `sad` | `cry_start` |
| `splat_mad` | `fall_mad` |

**历史组（28 组）** —— 保留键、只打 `legacy:true`，不删：

`button_press`、`cake_eat`、`cotton_candy_left/right/surprise`、`cry_reverse`、
`darkchurch_sit_happy/sad`、`defeat`、`fall_back_expressions/wince`、`glasses_1/2`、
`nuzzle1`、`shocked_subtle_left/right`、`stool`、`susie_throw_ready`、
`tea_pour/put_down/reach/sip/smile/surprised`、`teacup_land`、`throw_ball`、`yarn_1/2`

### 2.2 一条"不能误伤"的界线

有 **16 个组静态零引用、但会被动态拼接命中**（`walk_*` / `run_*` / `walk_tea_*` 变体）。
它们**不标 legacy** —— 标了就等于给活着的动画发"历史遗留"标签，会误导后续按 `legacy` 做清理的人。
判定规则由 `apply_s3_alias_legacy.py` 的 `DYNAMIC_PATTERNS` 显式持有。

### 2.3 工具

- `apply_s3_alias_legacy.py` —— 规则单一来源 `analyze()`；对 f-string 模板做**数据驱动**匹配
  （仅限"名字像名字"、插值 ≤2 处、且展开后确实命中已存在组）；`--apply` 回写，**幂等**。
- `verify_s3_alias_legacy.py` —— 19 项断言，含**独立字面量重扫**复核 legacy 判定，
  以及"别名/legacy 不改变解析后语义"的等价断言。

---

## 3. G2：一键回归基线（本轮的"安全网"）

H4 拆分的前提是"改完能一句话判断有没有改坏"，所以先把基线立起来：

- `code-quality-audit/regress/run_all.py` —— 依次跑全部套件，对输出做归一化
  （抹掉时间戳 / 内存地址 / 耗时 / 绝对路径），比对 **SHA-256 + PASS/FAIL 计数 + 退出码**，
  给出 `IDENTICAL` / `DIFF` / `BASELINE` / `SKIP`
- `_seed_runner.py` —— 固定随机种子 `20260913` + `PYTHONHASHSEED=0`。
  起因：`verify_round5_fixes` 的 V2b 用 `random.choice` 挑承接语，不固定就必然 `DIFF`
- 套件与结果：

| 套件 | PASS | 说明 |
|---|---|---|
| `round5_smoke` | 22 | modules 全量导入冒烟 |
| `round5_verify` | 16 | 第五轮缺陷修复断言 |
| `round6_verify` | 39 | 第六轮行为修复断言 |
| `s1_anim_miss` | 17 | H5-S1 动画名未命中自检 + 501 样本等价性 |
| `s2_anim_json` | 40 | H5-S2 深度等价 + 回落可用 |
| `s3_alias_legacy` | 19 | H5-S3 语义仍等价 |
| **合计** | **153** | **FAIL=0**，连续裸跑全部 `IDENTICAL` |

复现命令：

```
C:\Python311\python.exe code-quality-audit\regress\run_all.py            # 判定
C:\Python311\python.exe code-quality-audit\regress\run_all.py --list     # 列出套件
C:\Python311\python.exe code-quality-audit\regress\run_all.py --only s2_anim_json --verbose
C:\Python311\python.exe code-quality-audit\regress\run_all.py --update   # 重建基线
```

---

## 4. 本轮新发现（都写进了断言）

### 4.1 `comment` 的实际形状没人说过

导出器把源码里的多行注释收成**非空字符串数组**，加载器则接受 `str` **或** `list[str]`。
两边都能跑，但契约从未被明说 —— 已用 **A7** 锁住"随包发布的那一份统一是数组"。

### 4.2 未知字段会被静默忽略

加载器对未知键只 warning 不拒绝，于是 `legcay: true`（少个 a）这类拼写错误会**无声失效**：
JSON 里写了、程序当没看见。已用 **A6** 上锁：入库文件不得出现未知键。
A6/A7 的白名单直接引用 `sprite_loader._GROUP_KNOWN_KEYS`，不复制一份以免两边漂移。

### 4.3 别名会传播"缺失帧"

`idle` 成为 `neutral` 的别名目标后，往 `idle` 注入一个不存在的帧文件会被**两个组**同时报告。
原先 E 段把期望值写死成 `total=1`，S3 落地后立刻变红。
这暴露的是**验证脚本自身的脆弱**，不是加载器的问题 —— 已改为从文件现算期望值。

---

## 5. 一条明确的"不能碰"边界

**自动扫描目前不能降级为"只报告"。**

- 现状：`load_sprites` 把自动扫描结果并入 `sprites`，共 **489 组 = 配置表 109 + 自动扫描 380**（1084 帧对象）
- 实测：停用自动并入后，`frame_container_size` 由 `(222, 110)` 变为 `(136, 71)` —— **用户可见的尺寸变化**
- 根因：`core_prefixes` 含 `spr_cutscene_27_ralsei`，于是自动扫进来的
  `spr_cutscene_27_ralsei_huh / _look / _mu`（帧高 110）被当成 core 参与包围盒计算
- 因此本轮**只新增**只读的 `get_unconfigured_asset_report()`（返回 380 个"配置表里没有"的前缀名），
  **不改变加载行为**。验证脚本的 G3 是**冻结卡口**：它期望"停用后尺寸**必须不同**"，
  一旦有人悄悄降级就会报警。

> 是否真的降级，需单独排期 + 单独验收（不是本轮的范围）。

---

## 6. 遗留与下一步

1. **真实未命中清单尚未取得**：S1 的账本要在**真机跑一轮**后从日志/退出汇总里读，
   那份清单是"S2 配置必须覆盖的名字集合"。目前 S2/S3 只证明了"与现状等价"，没有证明"覆盖完整"。
2. **两处回退逻辑不一致**（已知未修）：`change_animation` 的长→短回退 vs
   `update_animation` 内嵌块只取前两段。
3. **H4 拆分**现在有 G2 兜底可以开工；施工方式仍按方案里的"转发壳 + Wave 顺序
   （独立子系统 → 鼠标交互 → 动画/移动/物理）"。

---

## 7. 证据与复现

| 类别 | 位置 |
|---|---|
| 配置数据 | `ralsei_pet/assets/animations.json` |
| 加载实现 | `ralsei_pet/modules/sprite_loader.py` |
| 导出器 / 写入器 | `code-quality-audit/架构改造-H4H5/extract_animations_json.py`、`apply_s3_alias_legacy.py` |
| 验证脚本 | `.../verify_s2_animations_json.py`（40 项）、`.../verify_s3_alias_legacy.py`（19 项） |
| 回归基线 | `code-quality-audit/regress/run_all.py`、`_seed_runner.py`、`baseline.json`、`README.md` |

验证环境：Windows + Python 3.11（`C:\Python311\python.exe`，已装 PyQt5 / pywin32）。
