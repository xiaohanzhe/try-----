# 第 44 轮报告 —— 对话框改成原作风格（复刻 `scr_darkbox()`）

- 轮次：第 44 轮（承接第 43 轮"画面怎么动"，本轮解决"对话框长什么样"）
- 用户口径（本轮唯一新指令，硬约束）：
  > 「对了，对话框改成和原作风格一样的，最好就是原作的对话框」
- 交付：`ralsei_pet/modules/dr_textbox.py`（新）+ `ralsei_pet/assets/ui/textbox/`（新，11 项）
  + `ralsei_pet/modules/dialogue_ui.py`（改 8 处）
- 回归：G2 **32 套件 / PASS=1596 / FAIL=0 / 全 IDENTICAL**（新增 `box_round44`，见 §5）
- 证据：`code-quality-audit/第44轮-原作对话框复刻/{_tools, _evidence}/`

---

## 0. 结论先行

1. **原作对话框不是"一张图"，而是一个 9-slice 配方**——`scr_darkbox(x1,y1,x2,y2)`
   用 3 张精灵拼出来：上下共用 `spr_textbox_top`、左右共用 `spr_textbox_left`（靠
   **负 scale 翻转**复用）、四角用 `spr_textbox_topleft` 的 **8 帧动画**。
2. **边框带厚 32px**（16px 精灵 × 2 倍缩放）；`textbox_width = arg2 - arg0 - 63`
   （63 = 2×32 − 1，留出两端各一个角块并互相咬合 1px）。
3. **角帧号 = `cur_jewel / 10`** ⇒ 每 10 帧换一帧；引擎 30fps ⇒ **333ms/帧**、8 帧一轮 2.67s。
4. **框的可见结构**（逐像素实测）：外圈 **8px 完全透明** → **4px 纯白线** → 2px 过渡 →
   **18px 纯黑**；黑底由 `scr_dbox` 的 `draw_rectangle` 各内缩 **14px** 提供，
   与边框图案的黑段**严丝合缝对齐**。
5. **两套世界两套画法**：现实世界（`global.darkzone == 0`）**没有素材**，是
   `spr_pxwhite` 拉两个矩形（白 289×76 + 黑 283×70，即 3px 白边）；暗世界才走 `scr_darkbox`。
   ⇒ 本轮复刻的是**暗世界**那套（桌面端定位为"暗世界"，与第 42 轮口径一致）。
6. **配套参数也一并还原**：打字机 `rate = 1`（≈33ms/字）、字体 `fnt_main`
   （8bitoperator JVE，像素字）、`draw_text_shadow`（+1,+1 黑影）、打字音按
   `scr_textsound()` **跳空格与标点**。
7. **本轮抓到并修掉 1 个真实缺陷**：`_TEXT_SOUND_SKIP` 漏了原作静音表里的 `?`
   （英文问号会发声）——由新回归锁 A8 反查发现，见 §4。
8. **一处原作分支此前被漏读**：四角的动画块整体包在 `if (global.flag[8] == 0)`
   里，`else` 分支把帧号写死 0（**静止**）。产品用
   `DrTextboxFrame.set_animation_enabled()` 表达同一语义，并**接到 hideEvent/showEvent**
   （框不可见时停表，省掉 33ms×N 的空转）。

---

## 1. 取证：原作对话框配方全解

工具链（第 43 轮已升级）：`UndertaleModCli.exe dump <data.win> -o <outdir> -c <code名>`
**不需要 CSX** 就能直接落 `.gml`；`--sprites` 导出全部精灵 PNG（ch1 共 **2,941** 张）。

### 1.1 `scr_darkbox()` 全文（`_evidence/gml/gml_GlobalScript_scr_darkbox.gml`）

```gml
function scr_darkbox(arg0, arg1, arg2, arg3)
{
    cur_jewel += 1;
    textbox_width  = arg2 - arg0 - 63;
    if (textbox_width  < 0) { textbox_width  = 0; }
    textbox_height = arg3 - arg1 - 63;
    if (textbox_height < 0) { textbox_height = 0; }
    if (textbox_width > 0)
    {
        draw_sprite_stretched(spr_textbox_top, 0, arg0 + 32, arg1, textbox_width, 32);
        draw_sprite_ext(spr_textbox_top, 0, arg0 + 32, arg3 + 1, textbox_width, -2, 0, c_white, 1);
    }
    if (textbox_height > 0)
    {
        draw_sprite_ext(spr_textbox_left, 0, arg2 + 1, arg1 + 32, -2, textbox_height, 0, c_white, 1);
        draw_sprite_ext(spr_textbox_left, 0, arg0, arg1 + 32, 2, textbox_height, 0, c_white, 1);
    }
    if (global.flag[8] == 0)   // ★ 动画分支
    {
        draw_sprite_ext(spr_textbox_topleft, cur_jewel / 10, arg0, arg1, 2, 2, 0, c_white, 1);
        draw_sprite_ext(spr_textbox_topleft, cur_jewel / 10, arg2 + 1, arg1, -2, 2, 0, c_white, 1);
        draw_sprite_ext(spr_textbox_topleft, cur_jewel / 10, arg0, arg3 + 1, 2, -2, 0, c_white, 1);
        draw_sprite_ext(spr_textbox_topleft, cur_jewel / 10, arg2 + 1, arg3 + 1, -2, -2, 0, c_white, 1);
    }
    else                        // ★ 静止分支：帧号写死 0
    { /* 同上四行，第 2 参数换成 0 */ }
}
```

### 1.2 `scr_dbox()` 的两套世界

```gml
if (global.darkzone == 0) {                      // 现实世界：无素材
    var off = side * 155;
    draw_sprite_ext(spr_pxwhite, 0, xxx + 16, yyy + 5 + off, 289, 76, 0, c_white, 1);   // 白
    draw_sprite_ext(spr_pxwhite, 0, xxx + 19, yyy + 8 + off, 283, 70, 0, c_black, 1);   // 黑 ⇒ 3px 白边
}
if (global.darkzone == 1) {                      // 暗世界：scr_darkbox
    if (side == 0) {
        draw_set_color(c_black);
        draw_rectangle(xxx + 38, yyy + 16, xxx + 602, yyy + 154, false);
        scr_darkbox((xxx + 32) - 8, (yyy + 10) - 8, xxx + 608 + 8, yyy + 160 + 8);
    } else { /* 同形，y 换成 326 / 464 / 320 / 470 */ }
}
```

**黑底内缩 14 的推导**（本轮 A6 判据就是从这两行反推的）：
框矩形 = `(xxx+24, yyy+2) .. (xxx+616, yyy+168)`；黑矩形 = `(xxx+38, yyy+16) .. (xxx+602, yyy+154)`
⇒ 四条边各差 **14**。

### 1.3 角图案逐像素实测（决定性）

`dump --sprites` 出来的 PNG 直接量：

| 素材 | 尺寸 | 剖面 |
|---|---|---|
| `spr_textbox_topleft_0..7.png` | 16×16 ×8 帧 | 外 4px 透明 → 2px 纯白 → 1px 过渡(157,162,196) → 9px 纯黑 |
| `spr_textbox_top_0.png` | **1×16** | 同上（1px 宽的竖剖面，靠 `stretched` 横铺） |
| `spr_textbox_left_0.png` | **16×1** | 同上（横剖面，靠 `stretched` 竖铺） |

×2 缩放后 ⇒ **白线在框矩形内缩 8px 处、线宽 4px**；黑底从 14px 处开始，与图案黑段对齐。

**素材真实签名**：8 帧里其实只有 **5 种**不同图案（0≡4、1≡7、2≡3≡5≡6）——
这是原作素材的事实，回归锁 E3/E3b 按此锁定，不至于误报。

### 1.4 配套参数（`scr_texttype` / `scr_textsetup` / `obj_dialoguer_Other_10`）

```gml
// typer = 5（scr_writetext 默认）
case 5: scr_textsetup(scr_84_get_font("main"), c_white, x, y, 33, 0, 1, snd_text, 8, 18, 0);
//                                                          ↑charline  ↑shake ↑rate ↑音  ↑hspace ↑vspace
```

- `rate = 1` ⇒ 1 字符/帧；30fps ⇒ **33 ms/字**
- 字体 `fnt_main` = **8bitoperator JVE**，em=12（位图点阵）
- 文字内缩：`obj_dialoguer_Other_10` 里 `xx = 19*f + view_x`、`yy = 20*f + view_y`，
  writer 建在 `xx + 10*f`；框左边界 `xxx + 32 - 8` ⇒ 相对占位差 **32 + 2 = 34**
- `draw_text_shadow(x,y,s)`：先 `c_black` 在 `(x+1, y+1)` 画一遍，再正常色画一遍
- `scr_textsound()`：`getchar` 为 `& " " ^ ! . ? , : / \ | *` 时 `play = 0`（静音）

---

## 2. 实现：`modules/dr_textbox.py`

逐行对应 §1.1，**不引入任何"大概像"的近似**：

```python
BAND = 32                  # 边框带厚度 = 2 帧缩放 × 16px 精灵
STRETCH_GAP = 63           # 原作减数：2 * 32 - 1
CORNER_SIZE = 32           # 角块实绘尺寸 = 16px 精灵 × 2
BLACK_INSET = 14           # 黑底相对框矩形各内缩
CORNER_FRAMES = 8          # spr_textbox_topleft 共 8 帧
JEWEL_TICKS_PER_FRAME = 10 # 帧号 = cur_jewel / 10
ENGINE_FPS = 30            # data.win GeneralInfo.GMS2FPS
TYPE_INTERVAL_MS = round(1000.0 / ENGINE_FPS)   # 33
CONTENT_INSET = BAND + 2   # 文字相对框左上内缩（原作实测 34）
```

- `jewel_frame(tick)` = `(tick // 10) % 8`
- `textbox_metrics(x1,y1,x2,y2)` = 边长 − 63，**负值钳 0**（对应原作两处 `if ... < 0`）
- `darkbox_blits(...)`：**纯几何**，返回 9 条 blit 指令（黑底 + 4 带 + 4 角），
  绘制与测试共用同一份几何（负 scale 的翻转在生成期就等价化成**目标矩形 + 预翻转图**：
  右下角从 `(x2+1, y2+1)` 向左上铺、右带从 `x2+1` 向左铺、下带从 `y2+1` 向上铺）
- `TextboxSprites`：一次加载 + 预翻转四条带/四种角朝向（`_corner/_corner_h/_corner_v/_corner_hv`）
- `paint_dark_box(painter, ...)`：只用到 `fillRect` / `drawPixmap` 两个原语（便于测试注入假画笔）
- `DrTextboxFrame(QWidget)`：自绘框 + 33ms 定时器；`set_animation_enabled()` 等价
  `global.flag[8]` 门控；`hideEvent/showEvent` 自动停表/恢复

### 2.1 与 `dialogue_ui.py` 的接线（8 处）

| # | 改动 |
|---|---|
| 1 | `BOX_INSET = _drbox.CONTENT_INSET`（单一真源，不写第二份 34） |
| 2 | `BASE_WIDTH = 620` / `MIN_BOX_HEIGHT = 220`（容纳 32px 边框带） |
| 3 | `_build_ui`：`QFrame#drFrame` + 样式表 → `self._frame = _drbox.DrTextboxFrame(self)` |
| 4 | `inner.setContentsMargins(34,34,34,34)`（避开边框带） |
| 5 | 头像框：圆角+底色 → **方角 2px 白边、无底色**（原作"表情框"口径） |
| 6 | 字体：`_ralsei_font()` 优先项目根像素字体 `普通字体.ttf`（FZXS12）+ `NoAntialias` |
| 7 | 正文：`QGraphicsDropShadowEffect(blurRadius=0, offset(1,1), 纯黑)` ≡ `draw_text_shadow` |
| 8 | 打字音：`_type_next_char` 按 `_should_play_text_sound(ch)` 决定是否播 |

> ⚠️ `_load_ralsei_font()` 在改造前就存在但**从不被调用**（死代码）。本轮把它接进
> `_ralsei_font()`，并由 `_ralsei_font()` 接进头像名/正文/光标/输入区 —— 死代码转活。

### 2.2 离屏渲染核对（`_tools/preview_ui44.py` → `_evidence/ui44_*.png`）

- `ui44_full.png`（620×341）：黑芯 / 白边 / 四角宝石 / 像素字 / 方角头像框 —— **肉眼确认与原作一致**
- `ui44_sizes.png`（4 种尺寸）：9-slice 在各长宽比下都正确（角块固定 32、带随长度拉伸）
- `ui44_corner.png`（8 帧 ×4 放大）：8 帧差异可见

`_tool/an44.py` 逐像素体检确认底部边框完整：`y=328` 过渡色 → `y=330` 纯白 →
`y=334` 全透明（正是"外圈 8px 透明"的外缘）。

---

## 3. 本轮改动清单（可逐条 diff）

**新增**
- `ralsei_pet/modules/dr_textbox.py`（复刻模块）
- `ralsei_pet/assets/ui/textbox/`：8 张角帧 + `spr_textbox_top_0.png` + `spr_textbox_left_0.png` + `README.txt`
- `code-quality-audit/第44轮-原作对话框复刻/verify_box_round44.py`（回归锁，49 条）
- `code-quality-audit/第44轮-原作对话框复刻/{_tools/5, _evidence/}`（取证与渲染产物）

**修改**
- `ralsei_pet/modules/dialogue_ui.py`（8 处接线 + 补 `?`）
- `code-quality-audit/第八轮/verify_round8_dialogue.py`（T4.5 判据升级，见 §4.2）
- `code-quality-audit/regress/run_all.py`（登记 `box_round44` + HERMETIC_IDS）
- `code-quality-audit/regress/baseline.json`（合并模式更新 3 个套件）

---

## 4. 本轮抓到的两个问题

### 4.1 真实缺陷：`_TEXT_SOUND_SKIP` 漏了 `?`

原作 `scr_textsound()` 逐个 `if` 列出 **12** 个静音字符：
`&` · 空格 · `^` · `!` · `.` · **`?`** · `,` · `:` · `/` · `\` · `|` · `*`

改造初版的表是 `" &^!.,:/\|*"` —— **只有 11 个，缺 `?`**（英文问号会播打字音）。

发现方式：新回归锁 **A8** 不写死字符表，而是**从 `scr_textsound.gml` 里
`re.findall(r'getchar == "([^"]*)"')` 把原作表抠出来**，再断言"产品表必须全覆盖"。
第一次跑就报 `缺=['?']`。已修，A8 转绿。

> 这条正是"套件必须从原文反推、不能自证"的价值：如果 A8 只是
> `assert _TEXT_SOUND_SKIP == ' &^!.,:/\|*'`，它会一路绿到天荒地老。

### 4.2 判据前提被合法改掉：round8 T4.5

`round8_dialogue` 的 **T4.5「覆盖层未被圆角切掉」** 读的是
`ui._frame.styleSheet()` 里的 `border-radius`。本轮把框换成**原作方角**自绘框，
`border-radius` 在 `dialogue_ui.py` 里已 **0 次出现** ⇒ `radius = 0` ⇒ 报红
（`最远角距圆心=42.4 <= radius=0`）。

这不是产品回归，是**判据的前提对象消失**。处置：**升级判据而非删除**——
改为"▼ 的 4 个角都必须落在框的**黑底可见区**内（各边内缩 `dr_textbox.BLACK_INSET`）"，
语义仍是"不被框的边缘装饰压住/切掉"。

鉴别力体检（改文件 → 必须报红）：把 `_position_cursor_overlay` 的
`margin_r/b = BOX_INSET - 18` 改成 `- 34`（即 0）⇒ T4.5 精确报红
（`越界角=[(539,306), (526,319), (539,319)]`）；还原 ⇒ 20/20 PASS。

同时清掉了因此变成未使用的 `import re`，并在套件头部补记升级原因。

---

## 5. 回归锁：`box_round44`（49 条）

放 `code-quality-audit/第44轮-原作对话框复刻/`，`ROOT = HERE/../..`，
**每条成功都用 `print('[PASS] %s')` 字面量**（`run_all.py:425` 按 `[PASS]`/`[OK]` 计数）。

| 段 | 内容 |
|---|---|
| **A**（11 条） | **从原作 GML 反推**：63 / 32 带宽 / `cur_jewel÷10` / 负宽高钳 0 / top·left·topleft 调用数 / 黑底 14（由框矩形与黑矩形反推）/ 文字内缩 34 / **静音表被产品全覆盖（含 `?`）** / 30fps→33ms |
| **B**（9 条） | 九个常量逐条字面量 |
| **C**（4 条） | `jewel_frame` 边界与周期 80、`textbox_metrics` 正常值与钳制 |
| **D**（8 条） | `darkbox_blits` 几何**逐条对应原作 draw 调用**（含极扁框负控制：只剩黑底+四角） |
| **E**（7 条） | 素材结构：16×16×8 / 图案签名 5 种 / 剖条 1×16 与 16×1 / 剖面"外透明→白线→黑芯" |
| **F**（8 条） | 产品接线：无 `border-radius`/`#drFrame`、`_frame` 真是 `DrTextboxFrame`、隐藏即停表、打字音正负控制、**AST 证明 `_should_play_text_sound` 真被 `_type_next_char` 调用**、▼ 落在黑底可见区 |
| **G**（2 条） | 恒真判据自查（无 `check(..., True)`）+ 断言总数 ≥40 |

**鉴别力体检（已做，落档）**：`BAND 32→31` + 静音表删 `?` ⇒
**10 条精确报红**（A2 / A7 / A8 / B×2 / D2–D5 / F6），其余 39 条不受影响；还原 ⇒ 49/49 PASS。

---

## 6. 遗留 / 待拍板

1. **仓库根 `textbox/` 是死桩**：`textbox_base.png` 其实是 **base64 文本**
   （`iVBORw0KGgo...`，按 PNG 头解析会得到 `1398106181×1432830273` 的荒谬尺寸），
   `textbox_base.svg` 只是 400×120 圆角示意，**全仓零引用**。
   ⇒ 建议清理，**等用户点头再动**。
2. **`global.flag[8]` 的真实语义未考证**（只知道它一非 0 就关掉角饰动画）。
   产品只做了等价开关，没有去猜它由什么触发。
3. **现实世界（darkzone=0）的对话框未做**：它是 `spr_pxwhite` 两矩形（3px 白边），
   产品当前只做暗世界那套。若后续要支持"现实世界"场景，需补这一支。
4. **`fnt_main`（8bitoperator JVE）本体未取**：产品用项目根 `普通字体.ttf`（FZXS12）
   替代 —— 是**像素中文字体**，观感契合，但不是原作字体（原作字体不支持中文）。
5. 承第 42/43 轮遗留：相机/过渡/深度是否落成产品代码；`_room_order.json` /
   `_room_graph.json` 是否进产品目录；暗之泉在桌面场景的尺度/合成方式。

---

## 7. 取证方法与判据（可复现）

### 7.1 工具（`_tools/`，5 个）

| 文件 | 作用 |
|---|---|
| `box44.csx` | UTMT 探针：侦测 textbox 相关 Sprite/Font/Object/Code/String（**先过 3 个已知真值锚点**才采信输出） |
| `dump44.py` | 包装 `UndertaleModCli dump`：`codeloop`（**`-c` 一次只吃一个名字**）/ `sprites` |
| `preview_ui44.py` | 离屏渲染 DialogueUI + 多尺寸框 + 8 帧角放大对照 |
| `geom44.py` | `DialogueUI` 与 `DrTextboxFrame` 的尺寸/位置/像素扫描诊断 |
| `an44.py` | 对已落盘截图做非透明包围盒 + 中轴逐行色带体检 |

### 7.2 判据

- **锚点前置**：`box44.csx` 必须 `spr_pixel_white 存在` ∧ `字体总数>0` ∧ `obj_writer 存在`
  三条全 True，才允许采信后面的命中列表（三个都命中，故可用）。
- **配方校验**：先出 `box44_preview.png` 肉眼确认，再写产品模块。
- **效果校验**：离屏渲染 + 逐像素包围盒，不靠"看起来像"。

### 7.3 本轮工具侧踩坑（会产出假结论的那种）

1. `UndertaleModCli.exe` **裸跑**抛 `System.IO.IOException: 句柄无效`
   （`ConsolePal.GetBufferInfo`）——沙箱无 tty。⇒ 只走 `load -s <csx>` 或 `dump` 子命令，
   且 `stdin=subprocess.DEVNULL`。
2. `dump -c` 是**单值**参数：一次传多个名字会被当成"叫这个名字的 code 不存在"。
   ⇒ 逐个调用（本轮 14 次全部 `[ok]`）。
3. **含反引号文本被 bash 命令替换**：README 里那句
   ``用 `UndertaleModCli dump <data.win> ... --sprites` 导出`` 整段被吞掉。
   ⇒ 改用 heredoc `cat > file <<'TXTEOF'`（单引号 = 内容原样）。
4. `Write` 工具报 `ModifyBackup failed … sandbox.backup.modify_backup`（备份服务抖动），
   而 Python 直写三处探针（`%TEMP%` / `E:\Download\_tmp` / 仓库根）**全部正常**
   ⇒ 不是 ACL、不是磁盘。重试/换名即过。
5. `E:\Download\_tmp\drw` 一度抛 `OSError: [WinError 1] 函数不正确`，
   相邻目录正常 ⇒ **瞬时抖动，先重试再下结论**。
