# 人味改造 · 生产侧补闸：括号动作旁白（2026-09-19）

> 起因：第四轮的 A/B 探针把「句中括号动作」单列成一个标记后，实测它在**真实输出里反复出现**
> （本轮量化：574 条真实中文回复里 **18 条**），而生产的两道老闸**全放过** ——
> 探针发现了洞，但探针不是兜底。本轮把闸补进生产。

---

## 1. 洞在哪（先量化，再动手）

`event_speech.looks_like_narration` 的判据是**首字符**：

| 输入 | 老闸 |
|---|---|
| `（歪着头）` | 拦得住（句首括号） |
| `嗯…（轻轻敲了下键盘）我也是这么想的。` | **放过**（括号在句中） |
| `诶……不说话也行啊。我在时。*轻轻敲了敲键盘* 要不要先喝水？` | **放过**（同上，星号版） |

`_clean_ai_reply`（对话链路）则**完全没有**旁白相关处理。

---

## 2. 规则怎么定的（关键是"别一刀切禁括号"）

先量了 Ralsei **原作台词的括号用法**（`_evidence/paren_usage_2026-09-19.txt`，853 条带说话人标注的语料）：

| 项 | 数量 |
|---|---|
| 含括号/星号 | **35（4.1%）** |
| **以括号开头** | **26（3.0%）** |
| 其中"括号内开头就是动作词" | **0** |

结论很清楚：**括号是他正常的表达手段**（原作里 26 条以括号开头，用来对 Kris 交代事情、讲心里话）。
所以规则只能抓下面这一小类：

> **括号内以动作/发声动词开头**（允许前面挂一个"轻轻/慢慢/小声"这类修饰语）。

于是：
* `（轻轻敲了下键盘）`、`（小声）`、`*轻轻敲了敲键盘*` → **命中**
* `（其实我有点怕）`、`（我想想该怎么说）`、`（抱歉，我不是故意的）` → **不命中**（开头是"其实/我/抱歉"）

**处理方式选的是"只删那一段"，不是"整句作废"**：
动作旁白通常只是句子的装饰，整句作废要重采样、白等一两秒还未必更好；切掉那一段，剩下的就是一句正常台词。
删干净（整句只有一个括号动作）才走判退。

---

## 3. 命中率 / 误伤率（`_evidence/paren_gate_2026-09-19.txt`，可复算）

| 维度 | 样本 | 结果 |
|---|---|---|
| **正例**：真实中文模型输出 | 574 条（7 份探针归档） | **命中 18 条** |
| **负控制**：中文"括号讲心里话" | 7 条（手写） | 误伤 **0** |
| **负控制**：原作英文台词 | 853 条 | 误伤 **0**（含 26 条括号开头的，全部不动） |
| **正控制**：构造的该删样本 | 7 条 | 未达预期 **0** |

**隔离量化（回答"闸到底删了什么"）**：

| 项 | 数 |
|---|---|
| 本闸把**整句删空**（真·代价） | **0 条** |
| 本闸**删短但保留台词** | 18 条 |
| 本闸放行、由**其它闸**判退 | 8 条（客服腔「有什么我可以帮忙的吗」×6、出戏闸 ×2） |

即：**它只删装饰，不吞台词。**

删减后的规范化自检（无首尾空白 / 无三连换行 / 无连续空格）：违例 **0**。

---

## 4. 改了哪些文件

| 文件 | 改动 |
|---|---|
| `ralsei_pet/modules/event_speech.py` | 新增 `_ACTION_PAREN_RE` / `strip_action_parentheticals()`；`guard_reaction()` 接入（返回**删减后**的文本） |
| `ralsei_pet/src/main.py` | 导入该函数；`_clean_ai_reply()` 新增 **0a 步**（在剥 markdown **之前**） |
| `code-quality-audit/regress/run_all.py` | 三条套件 desc 同步（避免 desc 变成错误文档） |
| `code-quality-audit/人味改造-2026-09-18/verify_s7_event_speech.py` | 新增 **A22 / A22b / A22c / A22d**（131 → 135） |
| 同上 `verify_persona_chat.py` | 新增 **C18 / C18b / C18c / C19**（71 → 75） |
| 同上 `verify_s8_stream.py` | 新增 **C10d**（68 → 69） |
| 新增工具 | `analyze_paren_usage.py`、`analyze_paren_gate.py`、`run_g2_paren.py` |

### 踩到的一个真坑（已修，并写进注释）

**0a 步必须在剥 markdown 之前。**
星号版旁白 `*轻轻敲了敲键盘*` 若先过 `_md_strip_re`，那对星号会被吃掉，只剩
`轻轻敲了敲键盘` 这段**裸旁白** —— 既认不出是动作旁白，又更像一句真台词，**比不处理更糟**。
（这个坑是断言逼出来的：C18b 第一版直接 FAIL，提示里就是裸文本。）

---

## 5. 有意的取舍（写进代码注释，也写在这里）

**流式层不删括号动作。** 句中删掉一段文本天然**不前缀安全**（`（轻轻敲` 已显示、`)` 到齐那一段才消失）；
若改写成"删掉未闭合括号之后的内容"来保前缀安全，会误伤他用括号讲心里话的正常写法。

所以该删除**只留在收尾层**。代价：极少数回复（本批 18/574）的括号动作会在**收尾那一刻**被摘掉，屏幕上闪一下；
收益是正式台词里不再出现舞台指示。
`verify_s8_stream.py` 新增 **C10d** 把这条分工**锁住**：将来谁要把删除挪进流式层，必须同步更新 C10 的边界清单。

---

## 6. 回归

| 项 | 值 |
|---|---|
| G2（改前基线） | 24 套件 / **PASS=1070** / FAIL=0 / 全 IDENTICAL |
| G2（改后） | 24 套件 / **PASS=1079** / FAIL=0 / **全 IDENTICAL、无 DIFF** |
| 变化 | +9 = A22 组 4 + C18 组 4 + C10d 1 |
| 基线 | 三条受影响套件用 `--only ... --update` **有意重建**（新增断言，不是回归） |

证据：`_evidence/g2_after_括号动作闸_2026-09-19.txt`（7761 B，无 BOM、无 U+FFFD）。

---

## 7. 真机

`_evidence/live_check_2026-09-19.md`：16:17:48 起、加载 112 组动画、无我方改动引起的 Traceback、
结束复核残留 0。

**顺带发现一个与本轮无关的真缺陷**（未修，待排期）：
`E:\RalseiMemory\logs\ralsei_pet.log` 的滚动在 exFAT 上失败 —— `RotatingFileHandler.doRollover()`
的 `os.rename(source, dest)` 撞到已存在的同名文件 → `OSError [WinError 1]`，
每次触发都往 stderr 打一份 Traceback，日志永远不滚动、只会一直变大。

---

## 8. 仍未做

1. **"闪现"的真人验证**：18/574 的括号动作会在收尾时消失，这个视觉代价只能主人真人对答时才看得到。
   若确认碍眼，把它提升到流式层（届时改 C10d 与 C10 的边界清单）。
2. 对话链路仍**没有**"句首括号整句叙述"的作废（只有事件链路有）。这是**有意的**：
   原作 3% 的台词以括号开头，加到对话链路会把特征一起砍掉。
3. 日志滚动 bug（§7）。
