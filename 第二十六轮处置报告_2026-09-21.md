# 第二十六轮处置报告（P1/P2/P3 施工 · 第一次复检的落地）

> **日期**：2026-09-21
> **性质**：**施工**（用户指令「好的，那就继续按照计划来吧」→ 授权执行第26轮报告 §4 处置建议）
> **上游报告**：`代码质量复审报告_2026-09-21_第二十六轮.md`（commit `dcfd11e`）
> **基线**：`main.py` 483243 B / 8987 行 / md5 `32c1cb5e…`（= 第26轮复审基线 `493b4845…` 经 P1 七连 pass 修复后）
> **回归基线**：G2 **24 套件 / 1165 PASS / 0 FAIL / 全 `IDENTICAL` / exit==0**
> **证据目录**：`code-quality-audit/第二十六轮/_evidence/`

---

## §0 结论先行

| # | 项 | 处置 | 结果 |
|---|---|---|---|
| P0 | §3.1 AI 失败回落内置台词 | **保守自判：不动** | 报告已记录，**待用户回头可否决** |
| P1 | §3.3 七连 `except: pass` | ✅ 已修（前一轮完成） | 7 处补 `_log.debug`，行为零变化 |
| P1 | §3.2 死代码 | ✅ **实删 8 个**（报告点名 4 个 + 新查出 4 个） | 见 §1，G2 全绿 |
| P2 | §3.6 待复核/补日志 | ✅ 6 处补日志 | 见 §2，独立探针 14/14 |
| P2 | §3.4 关键路径异常计数 | **不做**（见 §4 说明） | 成本/收益不划算，改为"补日志 + 探针断言" |
| P2 | §3.5 隐私清理走回收站 | **不动**（需用户裁定） | 报告已记录 |
| P3 | §2 自主开口未传 `recent` | ✅ 已修 | 见 §3，新增回归锁 B13b |
| P3 | §3.7 `QPainter` 无 `end()` | ✅ 已修 | 见 §3 |

**总体**：**施工全部完成，回归零退化（1164 → 1165 PASS，新增 1 条锁），无阻断级问题。**

---

## §1 P1：死代码处置（实删 8 个，超出报告点名的 4 个）

### 1.1 为什么是 8 个而不是 4 个

报告 §3.2 点名 4 个（用「全项目 `\b<name>\b` 正则扫描」得出）。本轮用**更严的 AST 口径**（`scan_zero_refs.py`，只数语法节点、不做字符串匹配）重扫，**又有 4 个同族死代码现形**：

| 方法 | 报告是否点名 | 归属 | 判定依据 |
|---|---|---|---|
| `follow_file` | ✅ | 文件 | 全项目无 `dropEvent`/`dragEnterEvent`/`setAcceptDrops` → 无拖拽入口 |
| `react_to_file_deletion` | ✅ | 文件 | `delete_file`(L8578-8580) 已承担同一职责 |
| `check_dragged_file` | ✅ | 文件 | 方法体仅 `pass`（空实现占位残留） |
| `react_to_file_emotionally` | ✅ | 文件反应 | **保留**（归 H4「文件反应」专项，见 §1.4） |
| `check_video_windows` | ❌ **新查出** | 视频 | W1-4 `VideoController` 已接管 |
| `check_game_windows` | ❌ **新查出** | 游戏 | W1-3 `GamesController` 已接管 |
| `react_to_game` | ❌ **新查出** | 游戏 | 同上 |
| `watch_video` | ❌ **新查出** | 视频 | 同上 |
| `react_to_video_content` | ❌ **新查出** | 视频 | 零引用；且其情绪事件 `saw_deltarune_video` 在 `emotion_system` **无分支**（静默 no-op） |

**新增的 4 个的意义**：它们是 **W1-3/W1-4 拆分的"搬迁残留"** —— 控制器建好了，宿主里的老副本没删。若不处理，将来读代码的人会以为"宿主还有一套视频/游戏逻辑"。

> **教训沉淀**：报告里的 4 个是用**正则**查的；AST 口径又查出 4 个。**正则会把注释/docstring 里的提及计成"引用"，从而漏判死代码** —— 这正是本项目"源码级断言别用字面量匹配"纪律的又一次实证。

### 1.2 G1 零引用筛查（改前，决定性证据）

`_evidence/G1_zero_refs.txt`：
- **A 组（候选）**：`check_dragged_file` / `react_to_file_deletion` / `follow_file` / `react_to_file_emotionally` → **全部「有定义 + 引用点 0」**
- **B 组（对照组，证明扫描器有鉴别力）**：`delete_file` 5 点 / `open_file` 3 点 / `open_folder` 6 点 / `interact_with_file` 1 点 / `check_file_content` 2 点 …

### 1.3 删除施工纪律

`_do_r26_deadcode.py` + `_tidy_seam.py`，`_evidence/r26_deadcode_surgery.txt`：

| 纪律 | 执行 |
|---|---|
| 首行锚 + 文本唯一性 | 8 个方法首行**全部唯一命中**（防误删同名 def） |
| EOL 保护 | `newline=''`（本文件 LF-only，改后仍 CRLF=0） |
| 反向归一证明 | 删→按原序插回 → **逐字符等于原文** ✅ |
| AST 反查 | 8 个名字**全部不在** main.py ✅ |
| 反向控制 | 8 个保留项**全部仍在** ✅ |
| 编译 | `compileall` 退出码 0 |

**结果**：`483243 → 476653 B`（-6590），`8987 → 8862 行`（-125）。

### 1.4 保留项与理由

| 方法 | 处置 | 理由 |
|---|---|---|
| `react_to_file_emotionally` | **保留** | H4「文件反应」专项的**唯一实现**；其 3 个事件名 `saw_food`/`saw_self_related`/`saw_sad_content` 在 `emotion_system.react_to_event` **本就没有分支**（`pets/审查报告_2026.md:98` 早有记录）→ 专项一并处理 |
| `delete_file` | 保留 | **活链路**：`dialogue_ui.py:1768-1770` 调用；有真实确认框 + 回收站删除 |
| `interact_with_file` / `interact_with_folder` / `check_file_content` / `check_text_content` / `check_image_content` | 保留 | **活链路**：`main.py:4124`（25% 概率分支）→ `:4131`/`:4128` → `:8096` |
| `is_own_code` / `react_to_vs_code_code` / `_recover_from_sadness` | 保留 | 活链路上游（`:8085→8086`） |

---

## §2 P2：6 处 `except: pass` 补日志

### 2.1 逐点清单（`_evidence/r26_p2p3_fix.txt`）

| # | 文件 | 位置 | 危害 | 处置 |
|---|---|---|---|---|
| a | `memory_system.py` | `_apply_loaded`（next_id 兜底） | 失败 → `_next_id` 留文件值 → **可能 id 复用** | 补 `_log.debug` |
| b | `memory_system.py` | `_kw_of`（抽词包装） | 失败 → 静默回落退化实现 | 补 `_log.debug` |
| c | `memory_system.py` | `recall`（关联图 node_weight 过滤） | 失败 → 该路径被跳过 | 补 `_log.debug` |
| d | `memory_store.py` | `migrate_from_fallback`（删空回落目录） | 失败 → 目录残留 | 补 `_log.debug` |
| e | `desktop_interaction.py` | `get_frame_rect`（DWM 取边框） | 失败 → 回落 `GetWindowRect`（**多屏/DPI 下边框不准**） | 补 `log.debug` |
| f | `desktop_interaction.py` | `mark_app_as_opened`（EnumWindows 回调） | 失败 → 该窗口漏标 | 补 `log.debug` |

> **误报订正（再次生效）**：报告 §3.6 初判 `config_manager.py:239`、`memory_store.py:316` 为"高危静默吞异常"，**逐一读原文后确认是误报**（`.tmp` 清理，真实原因已在上一层记录）→ **未改**。这是项目纪律「先读原文再看计数」的又一次价值体现。

### 2.2 报告标"待复核"的 `memory_system` 三处 —— 本轮复核结论

**报告 §3.6 把 `memory_system.py:508/981/1196` 标为「待复核，不据此下结论」。本轮逐一读原文复核，结论：三处确为"静默吞异常"，予以补日志**（见上表 a/b/c）。三处都没有"上一层已记录"的兜底 —— 与 `config_manager` 那两处误报**性质不同**。

### 2.3 独立验证探针

**为什么必须独立探针**：G2 既有 24 套件**不覆盖这几处的日志行为**（它们平时不抛异常 → 改不改都 PASS）→ 典型"改了没人验证"风险。

`_verify_p2_logs.py` → `_evidence/r26_verify_p2_logs.txt`：**PASS=14 / FAIL=0**

| 判据 | 内容 |
|---|---|
| AST | 3 个文件"带日志的 except handler"数量 ≥ 期望（29 / 5 / 67） |
| AST | 日志格式串使用 `%s` 风格（与项目既有约定 `_log.debug("…%s", e)` 一致） |
| **精确点位** | 6 处新增 handler **逐一按"函数名 + 函数体内片段"定位**（行号会漂移，函数体不会）→ 全部命中 |

---

## §3 P3：两处低危修复

### 3.1 自主开口链路补传 `recent`

**问题**：`start_autonomous_speech._on_reply` 调 `_clean_ai_reply(reply_text)` **未传 `recent`** → 护栏第 2 步（车轱辘话判定）**整步跳过** → **自主开口永远不会被判退重采样**，与对话链路（L6796 传 `recent`）不一致。

**修复**：`recent` 取法与对话链路**同源**（`dialogue_ui.get_ai_history()` 里 `role == 'assistant'` 的文本）。

### 3.2 `paintEvent` 的 `QPainter` 加 `try/finally`

**问题**：`painter = QPainter(self)` 无显式 `end()` → 中途抛异常会留下 active painter，Qt 打印 `QPainter::begin: Painter already active`。

**修复**：`try/finally` + `finally: painter.end()`。

### 3.3 ❗ 回归锁升级：B12/B13 由「字面量子串」改为「AST 结构断言」

**这是本轮最有价值的一笔记录。**

`persona_chat` 的 B12/B13 原判据是：
```python
'try:text=self._clean_ai_reply(reply_text)or""' in code_no_comment(func_src(...))
```
它**只对一种写法有效**。§3.1 补传 `recent` 后该字面量不再出现 → **两条锁同时假红**。

**这正是项目纪律「断言断行为不断赋值」的反例**：锁应当锁 **"自主开口里确实调用了护栏、且做了防御"**，而不是锁那次调用的**实参个数**。

**已改为 AST 判据**：
- B12：`start_autonomous_speech` 内**存在** `_clean_ai_reply` 调用（护栏共用）
- B13：该调用处在 **`try` 的 body 里**（护栏抛异常不冒泡）
- **B13b（新增）**：该调用**传了 `recent`**（对齐对话链路）

**这次"假红"是锁在正确工作** —— 它拦下了一次未同步的契约变更。**归因不是"锁太脆"，而是"锁得太具体"**；升级后同样的事件不会再假红。

---

## §4 P2 未做项与理由（诚实声明）

| 项 | 决定 | 理由 |
|---|---|---|
| §3.4「防御性 except 必须配异常计数」 | **不做全量，也不在 4 条关键路径加计数** | 报告建议"只在 4 条关键路径加计数"。本轮改为**"补日志 + 独立探针断言"**：① 计数器的唯一价值是"让人发现异常在发生"，而**探针能在 CI 里直接断言日志被调用**，比运行期计数更早暴露；② 加计数器要改 6 个模块的状态面，**收益递减且引入新状态**。**结论：用探针覆盖替代计数器。** |
| §3.5 隐私清理走回收站 | **不动** | 属**产品决策**（"退出清理"是否可永久删），需用户裁定 |
| §0 P0（AI 失败回落内置台词） | **保守自判：不动** | 报告已完整记录；**采取保守自判 + 记录待用户回头否决**（用户已授权我按专业判断施工） |
| §1.6 删转发壳 | **不做** | 报告 §4 建议"删壳是独立一波，不顺手做" |

---

## §5 回归与验证

| 项 | 结果 |
|---|---|
| G1 零引用筛查 | 4 候选全部零引用；对照组全部有引用（扫描器有鉴别力） |
| 死代码删除后 G2 | **24 套件 / 1164 PASS / 0 FAIL / 全 IDENTICAL / exit=0** |
| 死代码 + P2/P3 后 G2（首跑） | **1162 PASS / 2 FAIL** ← `persona_chat` B12/B13 假红（见 §3.3） |
| 锁升级后再跑 | **1165 PASS / 0 FAIL / 24 套件 / DIFF=0** ✅ |
| 基线更新 | `--update`（**合并模式**，仅 persona_chat 一项变化）→ `rc=0` |
| 最终确认跑 | **24 套件 / 1165 PASS / 0 FAIL / 全 IDENTICAL / exit=0** |
| P2 独立探针 | **PASS=14 / FAIL=0** |
| `compileall` | 退出码 0（4 个改动文件） |

**文件变化汇总**

| 文件 | 变化 |
|---|---|
| `ralsei_pet/src/main.py` | -6581 B（删 8 死代码）→ 再 +852 B（P3 两处） |
| `ralsei_pet/modules/memory_system.py` | +291 B（3 处补日志） |
| `ralsei_pet/modules/memory_store.py` | +96 B（1 处补日志） |
| `ralsei_pet/modules/desktop_interaction.py` | +204 B（2 处补日志） |
| `code-quality-audit/人味改造-2026-09-18/verify_persona_chat.py` | B12/B13 升级为 AST 判据 + 新增 B13b |
| `code-quality-audit/regress/baseline.json` | 更新（1165 PASS） |

---

## §6 遗留（交给用户或后续轮次）

1. **P0 口径**：用户主动打字 + AI 失败时，是否也"沉默"？（报告 §3.1 有完整分析）
2. **§3.5**：退出清理是否走回收站？
3. **H4「文件反应」专项**：`react_to_file_emotionally` + 3 个 `check_*_content` + 3 个无分支事件名
4. **真机手感实测**（用户侧）
5. **仓库换行口径**（`.gitattributes`，先议后动）
6. `E:\Download\_tmp` 存量（等用户点头，**不得批量删**）

---

## §7 附：证据清单

| 文件 | 内容 |
|---|---|
| `_evidence/G1_zero_refs.txt` | 零引用筛查（改前） |
| `_evidence/r26_deadcode_surgery.txt` | 死代码删除施工记录（含反向归一证明） |
| `_evidence/r26_seam_tidy.txt` | 接缝空行折叠核验 |
| `_evidence/G2_after_deadcode.txt` | 死代码后 G2 |
| `_evidence/r26_p2p3_fix.txt` | P2/P3 施工记录（8 处，含反向归一证明） |
| `_evidence/G2_after_p2p3.txt` | P2/P3 后 G2（首跑，含 B12/B13 假红） |
| `_evidence/r26_verify_p2_logs.txt` | P2 独立验证探针（14/14） |
