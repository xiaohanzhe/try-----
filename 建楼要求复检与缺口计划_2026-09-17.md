# 「建楼」要求复检与缺口计划（2026-09-17 · 第十六轮）

**用户指令**：*"复检一下到底实没实现那些要求，没有的话那就把它也加到计划里就好"*

**本轮定位**：**只复检、只落计划，不改任何产品代码**。
上一轮（第十五轮）已经把「建楼」遗留的死代码清干净，第十三/十四轮报告也各做过一次"逐条对表"，
但那两次的结论都是**在阅读代码的基础上**下的判断。这次改成**行为级复检**：把每条要求写成可执行的断言，
跑一遍看它到底成不成立。

**复检结论（先说结果）**：

- 13 条功能要求 + 3 条铁律里，**7 条已实现**（① ② ③ ④ ⑥ ⑦ ⑫），**5 条部分实现**（⑤ ⑧ ⑨ ⑩ ⑪，缺口都在下落路径上），**2 条动画要求（⑬⑭）名义实现了、实际不成立**。
- **第十三轮报告那句"13 条要求……没有漏项"需要更正**：漏的不是"接线的位置"，而是**一条分支**——"脚下窗口被关掉、但下面还有另一块窗口楼板"这条路上，**根本不会触发坠落**。
- 复检共确认 **6 个缺口 / 偏差**（G1–G6），下文逐条给了证据、影响面与修法建议，并排成 4 个批次。

**复现方式**（只读，不依赖真窗口）：

```powershell
# 必须用系统 Python（PyQt5/win32 在那里）
C:\Python311\python.exe "code-quality-audit\第十六轮\verify_build_req_audit.py"
# 本轮结果：PASS=40  FAIL=0，其中 13 条是"缺口断言"（断言的是当前缺陷）
```

证据：`code-quality-audit/第十六轮/_evidence/round16_audit_output.txt`

---

## 一、13 条要求逐条对账

判定口径：`已实现` = 行为级断言通过且语义与要求一致；
`部分` = 主路径成立、存在明确分支缺失；
`偏差` = 有意偏离字面要求（多数是为既有体验留的，需保留但要登记）。

| # | 要求原文（摘） | 状态 | 落点 / 证据 |
|---|---|---|---|
| ① | 每打开一个新窗口，就在对应位置盖一层新楼板，**大小位置必须和窗口一模一样** | 已实现 | `FloorManager._generate_floors` 每窗一层，`floor['rect'] == window['rect']`（A1） |
| ② | **哪个窗口在最前面，哪个就是最高楼层** | 已实现 | `platform_height = (n − i) × 5`，桌面恒 0（A2） |
| ③ | 后面的窗口被**完全盖住** → 暂时"不存在" | 已实现＋偏差 | 可见面积 < `MIN_FLOOR_VISIBLE_AREA`(1600px²) 即不成楼层，且**不再遮挡更低的窗口**（A3 / A3b / A3c）；阈值本身是偏差，见 G6 |
| ④ | 眼里只有**正站着的这块楼板**和**比它更高、没被完全挡住的悬崖边** | 已实现 | `floor_visible_contains` 只认可见区域（B1）；跳跃候选来自 `get_floors_above` |
| ⑤ | 只能**往旁边跳**（同一层其他位置），或**垂直**上/下跳 | 部分 | 侧向=走路、跳跃候选显式排除当前楼板（B4）；但**走路没有"层高闸门"** → 可直接走进更高层，见 G3 |
| ⑥ | 向上跳**只能跳到没被挡住的那部分边缘** | 已实现 | `_floor_entry_plan` 落点过 `nearest_visible_point`；被推远 >200px 直接放弃（B2 / B2b / B2c） |
| ⑦ | 向下跳**必须先跳到相邻下一层，不能穿透** | 已实现 | `adjacent_lower_floor` 显式取相邻下层（B3 / B3b / B3c / B3d） |
| ⑧ | 走到楼板边缘、脚下没东西 → **直直掉到下面第一块能接住的楼板** | 部分 | 落到桌面那条路成立（C1 / C3）；**下面还有窗口楼板时不掉**，见 G2 |
| ⑨ | 挪动窗口 = 挪楼板，宠物**必须跟着一起移动** | 已实现＋偏差 | 平移 40px 跟随、>30px 摔倒、10px 不摔（D1 / D1b / D2）；>400px 不跟随属偏差，见 G4 |
| ⑩ | 关闭窗口 = 抽走楼板，**必须立刻往下掉** | 部分 | 下面只有桌面时成立（C2）；下面还有窗口时不掉，见 G2；"立刻"= 1 秒节拍，见二 §6 |
| ⑪ | 新窗口盖住旧窗口 → 旧板失效、**宠物现在站在新楼板上** | 已实现 | `current_floor` 重判为新窗口且不摔（C4）—— 正是 G2 修法的**例外**，必须保留 |
| ⑫ | 做出"**一层压一层**"的真实堆叠感 | 已实现 | `_apply_pet_z_order` → `SetWindowPos(pet, insertAfter=所站楼板)`（A4 / A4b） |
| ⑬ | （第36行）用户行为导致摔到桌面 → **生气动画，至少 5s** | **不成立** | 选曲对了（E2），但 5s 是死参数、落地即被普通 splat 顶掉，见 G1 |
| ⑭ | （第37行）移动楼板 → 重心不稳摔倒，**至少 3s** | 部分 | 整段摔倒序列确为 3s 且 `fall_mad` 被选中（E1 / E1b）；但**生气素材只播约 1s**，见 G2-伴生项 |

**三条铁律**：不透明原则 = 已实现（可见区域 + 不置顶 + z 序插位）；当前楼层原则 = 主路径已实现（单真源 `current_floor`），但被 G3 局部破坏；重力原则 = 已实现（`get_drop_destination` 逐层不跳层），被 G2 的调用点缺失局部破坏。

---

## 二、缺口清单（G1–G6）

### G1 ⑬「生气动画至少 5s」实际不成立 —— **必修**

**证据**（三条互相独立，都指向同一结论）：

1. `max_fall_duration` 在**全项目**只被 `handle_fall` 读取（AST 统计 `self.max_fall_duration` 的 Load 点，结果恰好是 `{'handle_fall'}`）；
2. 但 `start_falling()` 里写了 `self.is_falling = False`，而 `update_movement` 的分支顺序是
   `is_jumping → is_gravity_falling → is_falling`，所以关窗坠落走的是 `handle_gravity_fall`，
   **`handle_fall` 根本不会跑** → 那句 `max_fall_duration = 5.0` 是死参数；
3. 落地后 `handle_gravity_fall` 走 `trigger_splat()`，它 `change_animation("splat")` —— **普通** splat，
   于是生气素材在落地那一瞬就被换掉。生气了不到 1 秒。

**顺带一个"素材躺在库里没人用"的实证**：`splat_mad`
（`spr_cutscene_24e_ralsei_splat_mad.png`）已配在 `assets/animations.json`，
也已登记进两处"特殊动画"白名单（`main.py:6396` / `7179`），
但**全项目没有任何一处 `change_animation("splat_mad")` / `play_animation_once("splat_mad")`**。

**影响**：要求第 36 行（用户明确写下的原话）没有兑现；⑭ 的"生气摔 ≥3s"也一样只播约 1s。

**修法建议**：把"起因 → 选曲 + 最短停留"做成**坠落路径的共用结算**，而不是留在 `start_fall` 那条旧路上：

- `trigger_splat(reason=...)` 按 `reason` 选 `splat_mad` / `splat`；
- `_fall_phase` 的 `splat` 阶段最短停留按 `reason` 取：`floor_removed → 5.0`、`window_move → 3.0`、其他 → 1.0；
- 顺带把 `start_falling` 里那句 `max_fall_duration` 删掉（或改成把值传下去），消除死参数。

---

### G2 ⑧/⑩「下方还有窗口楼板时不会坠落」—— **必修，它正是要求原文第 30 行的例子**

**要求原文**（"建楼"要求 第 30 行）：

> 3. 如果我把窗口B（记事本）关掉，3楼消失，宠物如果原来在上面，就会**掉到2楼（浏览器）上**。

**实际行为**：`check_window_movement` 里触发坠落的那段，门是

```python
if old_floor.get('type') == 'window' and new_floor.get('type') != 'window':
```

也就是说**只有"下面变成桌面"才会掉**。如果记事本关掉之后下方还有浏览器，
`get_current_floor` 立刻把 `current_floor` 换成浏览器 → `new_floor` 仍是 window → 整段被跳过：
**不坠落、不播动画、不生气，宠物"瞬移"到了浏览器那一层**。

**三条证据**：

| 编号 | 内容 |
|---|---|
| G2 | 合成楼层表（浏览器 ph=5、记事本 ph=10），宠物站记事本上 → 把记事本从楼层表移除 → `check_window_movement` **没有**任何 `start_falling` / `start_fall`，`current_floor` 直接变成浏览器 |
| G2b | 因为门是 `new_floor.type != 'window'`，第十五轮刚接活的那个"起因分流"（`is_floor_valid`）**根本没被问到**（调用计数 0） |
| G2c | 但**落点几何是好的**：真 `FloorManager` 上"记事本已关、浏览器还在"时，`get_drop_destination` 会正确给出浏览器。**缺的是调用点，不是算法** |

**修法建议**（最小改动，且天然保留要求 ⑪ 的例外）：

把"是否坠落"从"`new_floor` 是不是窗口"改成**层高比较**：

```
new_floor.platform_height < old_floor.platform_height   → 往下跌 → 坠落
new_floor.platform_height >= old_floor.platform_height  → 没往下跌 → 不摔
reason = 'floor_removed' if not is_floor_valid(old_floor) else None
```

这样一次覆盖四种情形，且都不需要新概念：

| 情形 | 判定 | 结果 |
|---|---|---|
| 走出窗口边缘，下面是桌面 | 5 → 0 | 坠落（常规动画）—— 与现状一致（C1） |
| 关掉窗口，下面是桌面 | 5 → 0 | 坠落（`floor_removed` → 生气）—— 与现状一致（C2） |
| **关掉窗口，下面还有窗口** | 10 → 5 | **坠落（`floor_removed` → 生气）← 本缺口** |
| 新窗口盖住旧窗口（要求 ⑪） | 5 → 10（更高） | **不摔**，直接站新板 —— 与现状一致（C4） |

---

### G3 ⑤/④「走路可以直接走进更高楼层并被提升」—— **需用户拍板语义，再修**

**实际行为**：`check_window_movement` 每拍 `new_floor = get_current_floor(pos)` 后**直接赋值**，
没有任何"层高差必须靠跳"的闸门；而 `generate_new_move_target`（随机走路目标）
里**完全不出现 `window` / `floor`** —— 走路对窗口毫无感知。

所以：宠物站在桌面（1 楼）水平走进某个窗口的可见区域时，会被**直接提升**到那一层，
不跳跃、不坠落，只是一个瞬时的层级跳变（并连带把 z 序抬上去）。

**为什么这条要用户拍板**：几何上"楼板悬在头顶、从下面走过去"是可能的，
要求的字面（⑤ 只能旁跳/垂直跳 + ④ 只看得到当前层与更高的悬崖边）与"走路自由漫游"之间需要一个裁断：

- **方案 A（按字面收紧）**：层高变高时不提升，保持原楼层，改由 `check_nearby_windows` 用**跳跃**进入。
  代价：走路会"撞到看不见的墙"，需要给一个贴边起跳的交代。
- **方案 B（承认现状并补交代）**：允许走进更高层，但补一个"爬上去"的过渡动画/动作，避免瞬跳观感。
- **方案 C（维持现状）**：不改，只登记。

---

### G4 ⑨「位移 >400px 不跟随、改失足掉落」—— **有意偏差，建议保留并登记**

`_follow_floor_move` 两级阈值：`>30px` 跟随并摔倒；`>400px` 视为"楼板被瞬间搬走"（最大化/还原/被系统挪走），
**不跟随、改为失足掉落**。要求原文写的是"必须跟着楼板一起移动"。

这是为"最大化/还原"这类瞬移场景留的体验补偿，**建议保留**；本轮只把它从"隐性行为"提升为**登记在案的已知偏差**。
（若要改：把阈值改成"跟随但幅度限位"，需要真机看观感。）

---

### G5 `climb_to_top_window()` 仍是裸窗口路径 —— **收编项，第十五轮已登记**

`start_jump(top_window, "bottom")` 直接把裸窗口当目标，不经楼层 → 落点可能落在被遮挡的部分，
也不受"逐层"约束。它挑的是**最前面那个窗口**（按定义不会被别的窗口盖住），所以**目前不会出问题**，
但"楼层是唯一真源"这条契约下它是个例外口子。

**修法**：改走 `_nearest_floor_jump` / `_start_floor_jump`；或与用户确认这条语音命令是否还要保留。

---

### G6 ①③「可见面积阈值」与字面不一致 —— **登记，不建议改**

`MIN_FLOOR_VISIBLE_AREA = 40×40 = 1600px²`：只被遮住一部分、可见面积不足 1600px² 的窗口
**直接不成楼层**；而要求 ③ 的字面是"被**完全**盖住"才不存在。

保留理由：那是"宠物脚下能站稳的一小块"，比它更细的缝既站不住、用户也看不见宠物站在哪。
**本轮不改**，只把它登记为**已知口径**（`floor_manager.py:43–45` 已有注释说明取值理由）。

### 另：⑩ 的"立刻"= 最长 1 秒
`check_window_movement` 挂在 `floor_check_interval` 的 1 秒节拍上（第十五轮改成不依赖 `is_moving`）。
所以"立刻开始往下掉"实际最长有 1s 延迟。若要求更"立刻"，需要把节拍降到 0.3s 左右 —— 代价是窗口枚举频率翻三倍（性能）。
**建议维持现状**，登记为已知。

---

## 三、与第十三/十四轮对账结论的差异（为什么要再做一次复检）

| 轮次 | 当时的结论 | 本轮复检的意见 |
|---|---|---|
| 第十三轮 §三 | "13 条要求里 4 条是本轮新做的，5 条已由前几轮满足，4 条是动画要求（已满足）。**没有漏项**" | 前半句成立；**"4 条动画要求已满足"不成立**（G1：5s 是死参数、生气素材落地即被顶掉），漏的还有 G2 那条分支 |
| 第十四轮 §三 | 6 条标"本轮闭环"，其余"满足" | 那 6 条在**它自己覆盖的分支上**确实闭环；但覆盖范围没包含"下方还有窗口"（G2）与"走路跨层"（G3） |
| 第十五轮 §七 | 已把 `climb_to_top_window`（=G5）列入待办 | 一致，本轮沿用 |

**方法论差异**：前两轮的对表是在**阅读代码**上做的（"这个判据改对了 → 满足"），
本轮改成**行为级**（"把这条要求写成断言，跑出来成不成立"）。
G2 与 G1 都是"读代码看不出来、跑一下才现形"的：一个是**调用点缺失**，
一个是**参数写在了不会被读到的那条路上**。

---

## 四、计划（分 4 个批次）

> 原则：**先修"行为不对"，再收编"路径不统一"，最后登记"有意偏差"。**
> 每批次做完照旧：自检套件 + G2 全绿 + 真机肉眼确认 + commit & push。

### 批次 A：把"下落路径"一次修对（G2 + G1，两者同源，必须一起做）

1. `check_window_movement` 的坠落判据由"`new_floor` 是不是窗口"改成**层高比较**（第五节 §G2 的四种情形表）；
2. 坠落起因仍由 `is_floor_valid(old_floor)` 分流（第十五轮已接活的逻辑，这次终于会被真的问到）；
3. `trigger_splat(reason=)` 支持 `splat_mad`，并把 `_fall_phase.splat` 的最短停留按起因参数化
   （`floor_removed → 5.0`、`window_move → 3.0`、其他 → 1.0）；删掉 `start_falling` 里的死参数；
4. 把 `splat_mad` 从"躺在 animations.json 里没人播"变成真在播（白名单里本来就有它）；
5. **验收**：本轮的 `G1 / G1b / G1c / G2 / G2b` 五条缺口断言**必须全部翻转**（改成断言"修好后的行为"）；
   G2c 保持成立；G2 涉及的要求原文例子要能真机复现。

### 批次 B：层高闸门（G3，先要用户拍板 A/B/C 三选一）

- 若选 A：`check_window_movement` 增加"新楼层更高时不提升"的闸门，并把进入交给跳跃系统；
  需要在 `check_nearby_windows` 里确认"贴边就能起跳"的手感不受影响。
- 若选 B：保留提升，但补一个向上的过渡动作（可用现有的 jump 起跳帧或 `look_up`）。
- 验收：真机观察"从桌面走向窗口"的观感；走路目标不出现"撞墙"。

### 批次 C：收编旁路（G5）

- `climb_to_top_window` 改走楼层系统；或与用户确认该命令去留。
- 顺带清掉 `start_jump` 里因此变得只剩一个调用点的裸窗口回落分支（**先确认没有别的调用者**再删）。

### 批次 D：登记类（G4 / G6 / 1 秒节拍）

- 不改代码，只把三条"有意偏差"写进 `floor_manager.py` / `main.py` 的注释与项目记忆，
  免得下一轮又有人把它们当 bug 重新"修"一遍。

---

## 五、本轮交付物

| 文件 | 说明 |
|---|---|
| `code-quality-audit/第十六轮/verify_build_req_audit.py` | **新增**：只读复检套件（40 项断言，A–F 六组；其中 13 条是"缺口断言"） |
| `code-quality-audit/第十六轮/_evidence/round16_audit_output.txt` | 复检原始输出（PASS=40 / FAIL=0） |
| `code-quality-audit/第十六轮/_evidence/round16_g2.txt` | 本轮 G2 回归结果（本轮未改产品代码 → 与基线逐字节一致） |
| 本文件 | 复检结论 + 缺口清单 + 4 批次计划 |

**说明**：复检套件**不入 G2**（与第十三轮的 `probe_*` 同规格）。
原因：它的 F 组断言的是"当前缺陷"，一旦有人修好缺口，套件会按设计变红
—— 那是提醒人来更新它，不该把"断言缺陷"变成回归基线的一部分。
修完批次 A 之后，F 组应改写成"断言修好后的行为"，届时再挂进 G2。
