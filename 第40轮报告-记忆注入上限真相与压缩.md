# 第 40 轮报告 — 记忆注入上限真相 + 速查本首次「按真判据」压缩

**日期**：2026-09-24 02:2x　｜　**提交**：`774f7df`（主）+ `1117597`（推送日志附录）
**性质**：记忆维护，**未改任何产品代码**；G2 未跑（无需，`src/` 零改动）

---

## 一、结论先行

| 项 | 结果 |
|---|---|
| 注入上限真相 | **10,000 字符**（JS `content.length`，UTF-16 码元），超限**砍尾** |
| 第 38 轮判据 | ❌ **错的** —— `200 行 / 25,000 字节` 管的是另一个目录，与本项目无关 |
| 压缩前 | **14,159 字符 ⇒ 超 41.6%**，注入在 §10 断掉 ⇒ **§11「待用户裁定」整章被砍** |
| 压缩后 | **9,904 字符 / 16,220 B / 73 行**（余量 96），**零令牌丢失** |
| 用户级 `~/.workbuddy/MEMORY.md` | 3,214 / 4,000 ⇒ 余量 786，**不动** |

---

## 二、上限真相（代码实证，非估算）

```
MemoryCollector.getWorkingMemoryContent(memoryDir)      // 管 <repo>/.workbuddy/memory/MEMORY.md
  var MAX_MEMORY_CHARS = 1e4;
  const content = fs.readFileSync(memoryFilePath, 'utf8').trim();
  if (content.length <= MAX_MEMORY_CHARS) return `<working_memory_content> ... </working_memory_content>`;
  return `... ${content.substring(0, MAX_MEMORY_CHARS)}  ... (memory truncated — MEMORY.md is too large) ...`;
const MAX_USER_MEMORY_CHARS = 4e3;                      // 用户级 ~/.workbuddy/MEMORY.md
```

**怎么找到的**（第 38 轮没找到是因为**找错了地方**）：

- 文案只在 **`resources/app.asar`**（317,114,821 B 的**打包态**文件）里，**不在** `app.asar.unpacked/` 下；
- `ripgrep` 之类会把大二进制当二进制跳过 ⇒ 必须 Python 读整个 blob + `blob.find(needle.encode('utf-8'))`，命中后切 ±3 KB 窗口 `decode('utf-8','replace')`；
- 决定性命中文案：`'working_memory_content'` ×4、`'memory truncated'` ×3、`'MEMORY.md is too large'` ×1。

> ⚠️ 第 38 轮读到的 `buildAgentMemoryPrompt`（`eh=200, ef=25e3`）管的是
> **`.codebuddy/agent-memory/<agent>/MEMORY.md`** —— **另一套机制，不可混用**。

---

## 三、压缩（14,159 → 9,904）

- **有效手段是"整块下沉"，不是"改字"**：前 12 版逐字改写每版只省 300~600 字符（中文密度高）；
  真正见效的是 ① §10 历轮索引 → 紧凑指针；② §7 与 §11 重复的「P1 前置」细节合并；
  ③ §3② 建楼细则改指 §4.9；④ 与详版逐条对照后删掉"详版已完整覆盖"的条目。共 17 个中间态。
- **判据**：`js_len(t) = len(t.strip().encode('utf-16-le')) // 2`（对齐 JS `content.length`；非 BMP 字符在 JS 里算 2）。
- **不许丢令牌**：判据名 / 数字 / 路径 / 代码片段 / commit hash 要么留在速查本，要么**先明确下沉到详版** + 留指针。

---

## 四、复检六类判据（全 PASS，脚本 `_evidence/recheck41.py`）

| # | 判据 | 结果 |
|---|---|---|
| 1 | 体积 `js_len ≤ 10,000` | **9,904**（余量 96）✅ |
| 2 | 编码 | 无 BOM、`fffd` = 0、**纯 LF**、结尾有换行 ✅ |
| 3 | 结构自检 | 12 个 `## N.` 标题（0–11）齐全、无"标题粘行" ✅ |
| 4 | 逐令牌回验 | 父版本 266 个候选令牌 ⇒「现版 ∪ 详版」**真缺 = 0** ✅ |
| 5 | 工作区 | `git status --porcelain` 为空 ✅ |
| 6 | 提交/推送 | `774f7df` → `1117597`，`ls-remote` == `origin/main` == 本地，**双向一致 = True** ✅ |

**逐令牌回验的两个报红，裁决后均不成立**（证据：`_evidence/令牌回验.txt`）：

- `216` —— **有意删除**：父版本第 5 行「24,216 字节」是**已被推翻的错误判据**，留着就是留错；
- `§11.6` —— **误报（判据太窄）**：父版本写 `§11.6`（指它自己 §11 第 6 条），
  详版里对应内容写作 `### 11.6 persona 的 markdown 禁令又赚了一次`（**不带 `§`**）。
  ⇒ 又一次「**分类器太窄**和**闸有洞**长得一样」。

**★ 本轮第二次"判据自己说谎"**：提交之后再跑回验，`git show HEAD:` 已是新版 ⇒ 检查退化成
**自己比自己**，报 "missing = 0" 是**恒真判据**。修法：显式切父提交
`git cat-file blob 7b9d10a:...`。已记入详版 §41.9 并落证据。

---

## 五、回修的过期结论

- **用户级 skill `agent-memory-compaction`（5 处）**：上限判据从 `200 行 / 25,000 字节`
  改为 **`js_len ≤ 10000`**、给出 `js_len` 定义、两套机制并列陈述、补挂复现脚本名。
- **详版 §39.11**：加**推翻横幅**；`200 行 / 25,000 字节` 整段划线；**那笔账重算** ——
  第 38 轮 11,446 → 12,252 字符 **按真判据仍超限** ⇒ 「那次压缩是纯损失」「早就压到安全区」
  **均不成立**（方向对，但远远不够）。
- **详版新增 §41（41.1–41.10）** 与当日日志 §十五。

---

## 六、顺带发现（**本轮只记录、不动**）

详版 §8.5 / §8.6 / 「## 13. 第二十四~二十六轮索引」**被重复插入两份**
（L1452–L1866 与 L1674 / L1707 / L1867 起）⇒ 待后续轮次清理。

---

## 七、待用户拍板（**本轮一律没动**）

1. **Q1（最大）**：856 个场景画面 —— A 保持留空 / **B 按原作瓦片还原**（689 房有瓦片，最忠实、量大）/ C 沿用区域代表素材近似。
2. **Q2** `ch1.kris_room` 的 640×480 放大件是否换回原样（原作 320×240）。
3. **Q3** 2 处选层（`torielclass` 日语变体 `bg_lang_ja_torielclass` / `dw_castle_restaurant` 柜台层）是否定向覆盖。
4. **Q4** 78 个近似锚点是否随 Q1 一起升级；**路由重建**（现 26 条 → 1,014 场景可达面）；BGM；版权尺度；`.gitattributes`。
5. ★★ **P1 硬前置（躲不开）**：扩 `dump_rooms.csx` 导出**背景层几何** + 重跑五章 UTMT。
