# Ralsei 桌宠 · 系统架构地图（面向场景系统扩展）

> 产出日期：2026-09-22
> 目的：为「场景系统」做**接入前置**。本文只描述**现状**，不做设计取舍（设计见
> `场景系统扩展接口设计方案_2026-09-22.md`）。
> 方法：只读勘察 + 关键行号**逐条亲眼核验**（非推断）。
> 基线：G2 `PASS=1169 FAIL=0 / 24 套件 / 全 IDENTICAL`（修复前状态，代码是绿的）。

---

## 0. 一句话总览

```
┌─ 单进程 PyQt5 应用（Python 3.11）────────────────────────────────┐
│                                                                  │
│  RalseiPet(QMainWindow)  ← 上帝类宿主，8876 行，持有全部状态        │
│   ├─ 转发壳 __getattr__  → 5 个控制器（方法在此，状态仍在宿主）      │
│   ├─ sprite_label(QLabel) ← 唯一视觉出口（setPixmap）              │
│   ├─ paintEvent          ← 目前只 fillRect 全透明（无任何绘制）      │
│   └─ 26 个子系统（见 §1.3）                                        │
│                                                                  │
│  持久层  data_store → E:\RalseiMemory\（vault，读优先）             │
│                    → %LOCALAPPDATA%\RalseiPet\（staging，兜底）    │
│  AI 层   api_client → Ollama ralsei:v3（qwen3:4b）                 │
└──────────────────────────────────────────────────────────────────┘
```

**★ 三条对场景系统最关键的现状：**

1. **没有任何"场景/背景"渲染层。** `paintEvent`（`main.py:4969`）唯一动作是
   `fillRect(self.rect(), 全透明)`；像素全来自 `sprite_label.setPixmap()`。
2. **"桌面"已是一等公民的"地面"**：`floor_manager.desktop_floor`（`floor_manager.py:131`，
   `type='desktop'`、`platform_height=0`）——"桌面也算一个场景"可天然映射到此。
3. **`customization_system.py` 里躺着一整套已定义、从未接线的场景数据结构**
   （`appearance.background` / `environment.home_location` + 两张清单 + 4 处空注释）。
   **这是本项目最重要的"预留接口"，场景系统应当**先**考虑复用它，而不是另起炉灶。**

---

## 1. 模块清单与职责

### 1.1 `src/`（有效代码只有 `main.py`）

| 文件 | 行数 | 职责 |
|---|---|---|
| `src/main.py` | **8876** | 上帝类 `RalseiPet` 宿主：窗口/渲染/物理/动画/AI 链路/事件台词全在此 |
| `src/main_simple.py` | 64 | 极简 demo，非生产入口 |
| `src/test_*.py`（9 个） | 12–194 | 早期自检脚本，非生产代码 |

### 1.2 `modules/`（38 个模块，按职责分组）

**交互与桌面**
| 文件 | 行数 | 职责 |
|---|---|---|
| `desktop_interaction.py` | **2973** | Win32 桌面交互全集：窗口枚举/图标枚举/Office COM/拖文件/系统信息 |
| `floor_manager.py` | **632** | **「建楼」核心**：可见窗口→楼层表（含桌面层）、可见区域裁剪、落地/跳跃目的地 |
| `pet_interaction.py` | 364 | 身体区域映射 + 手势追踪（纯逻辑，返回 `PetEvent`） |
| `autonomous_agent.py` | 712 | Ralsei 作为「第二个用户」逛桌面（IDLE→WALKING→ACTING） |

**对话与 AI**
| 文件 | 行数 | 职责 |
|---|---|---|
| `dialogue_ui.py` | **1810** | Deltarune 风格对话框（输入栏/打字机/流式/AI 历史/对话注意力） |
| `dialogue_system.py` | 1501 | 内置规则对话引擎（AI 不可用时的回退） |
| `api_client.py` | 460 | 本地 AI 接入（`LocalAIBase`/`Stub`/`HTTPLocalAI` + `create_client` 工厂） |
| `ai_driver.py` | **673** | AI 行动驱动（行为大脑）；`note_event` 收集环境观察 |
| `event_speech.py` | **513** | 事件台词档位表 + `build_prompt`/`first_sentence`/`RecentLinePicker` |
| `conversation_focus.py` | 499 | 对话注意力/话题锚（`brief()` 拼进 system） |
| `pet_ai.py` | 766 | 宠物 AI 状态机（`react_to_event`） |
| `command_manager.py` | 199 | AI 命令 → 方法映射表（move/jump/animation/say…） |
| `search_summarizer.py` | 144 | 联网搜索摘要 |

**记忆与知识**
| 文件 | 行数 | 职责 |
|---|---|---|
| `memory_system.py` | **1709** | 拟人记忆：片段/关键记忆/遗忘/召回（`recall_text` 是 AI 链路出口） |
| `memory_graph.py` | 704 | 分层关联图 + 多跳受控检索（PPR 路径打分） |
| `worldview_recall.py` | 287 | 世界观按需召回（`@@` 块 + 图边） |
| `memory_store.py` | 342 | 记忆存放位置层（E 盘 vault / 本地 staging） |
| `text_segmenter.py` | 214 | jieba 分词接入层（缺失静默回落） |

**状态与成长**
| 文件 | 行数 | 职责 |
|---|---|---|
| `emotion_system.py` | 1461 | 情绪累计/衰减/心情档位 |
| `social_growth_system.py` | 746 | 社交成长（等级/成就） |
| `relationship.py` | 346 | 关系演进（隐藏信任度四档） |
| `energy_hunger.py` | 255 | 精力/饥饿度 |
| `entertainment_system.py` | 570 | 娱乐需求（无聊→找乐子） |
| `weather_system.py` | 316 | 天气（`get_current_weather`） |
| **`customization_system.py`** | **467** | **自定义系统：已定义 `background`/`home_location` 但未接线（见 §6）** |

**控制器（W1 拆分产物）**
| 文件 | 行数 | 职责 |
|---|---|---|
| `hide_controller.py` | 526 | 躲猫猫（W1-2） |
| `video_controller.py` | 521 | 陪看视频（W1-4） |
| `spell_controller.py` | 517 | 施法流程（W1-1） |
| `games_controller.py` | 481 | 石头剪刀布+猜数字（W1-3） |
| `file_sheet_controller.py` | 468 | 文件/表格操作（W1-6） |

**基础设施**
`data_store.py`(302) / `config_manager.py`(578) / `sprite_loader.py`(**946**) /
`logger_utils.py`(181) / `lazy_log.py`(55) / `sound_manager.py`(162)

> **注意**：`core/`、`ui/`、`utils/`、`systems/`、`config/` 五个目录**全部为空**（历史遗留）。

### 1.3 `main.py` 内部结构（关键行号，均已核验）

| 行号区间 | 内容 |
|---|---|
| L42–L240 | 模块级常量 + 物理派生函数（`FALL_MAD_REASONS` / `_climb_*` / `_NON_SPECIAL_ANIM_GROUPS`） |
| L242–L384 | `PerformanceMonitor` + `_make_relationship()` |
| L386–L526 | **`__init__` + `__getattr__` 转发壳 + `_CONTROLLER_ATTRS`(L512)** |
| L528–L604 | 系统托盘 |
| L605–L659 | `init_ui` / `load_resources` |
| **L660–L807** | **`init_systems`（26 个子系统创建顺序）+ 状态预声明区（L770–L806）** |
| L810–L920 | `init_animation`（动画定时器 + 优先级表） |
| L921–L961 | `init_timers`（AI/stats/weather） |
| L962–L1028 | `init_placeholder_system`（桌面元素透明占位符） |
| **L1031–L1224** | **`init_movement`（物理参数 + 全部运动状态）** |
| L1225–L1330 | `update_environment` / `update_mood` |
| L1376–L2137 | 移动随机化 / `update_movement`（主运动循环） |
| L2138–L2303 | 桌面元素检测 / 鼠标拖拽 |
| L2304–L2562 | 楼层跳跃/攀爬计划（`_floors_for_jump` / `_start_climb_transition`） |
| L2872–L3535 | 坠落处理（`handle_gravity_fall` / `check_window_movement`） |
| L3996–L4250 | **`react_to_desktop_element`（对桌面物件反应总入口）** |
| L4315–L4675 | 娱乐 / 自主开口（`start_autonomous_speech`） |
| **L4712–L4965** | **事件台词（`_event_ai_ready` / `speak_event`）** |
| **L4966–L5037** | **`paintEvent`(L4969，只填透明) / `get_ralsei_body_part`(L4980)** |
| **L5038–L5721** | **鼠标事件：Press(5038) / Move(5195) / Release(5399) / DoubleClick(5609) / Hover** |
| L5722–L5994 | 配置对话框 / 互动入口 |
| L5995–L6190 | `init_api_client` / `handle_api_response` |
| L6191–L6338 | **`change_animation`（动画切换总闸，L6208）** |
| **L6618–L6839** | **`chat_with_ai`（AI 对话主链路）** |
| **L6840–L7031** | **`_build_ai_context`(L6840) / `_build_persona_prompt`(L6982)** |
| L7078–L7422 | 回复清洗 + 精灵锚点 |
| **L7423–L8000** | **`update_animation`（帧推进 + 渲染主循环，`setPixmap` 出口 L7964）** |
| L8090–L8482 | 文件/文件夹交互 + 窗口操作 |
| **L8483–L8546** | **多屏坐标核心（`_virtual_screen_rect` L8483 / `_desktop_floor_y` L8538）** |
| L8547–L8692 | `cleanup_on_exit` |

---

## 2. 窗口与渲染层（★ 场景系统最关键）

### 2.1 窗口与视觉出口

```python
# main.py:605 init_ui()
self.setWindowFlags(Qt.FramelessWindowHint | Qt.Tool)
self.setAttribute(Qt.WA_TranslucentBackground)
# main.py:623
self.sprite_label = QLabel(self)          # ← 唯一视觉层
```

层级：
```
RalseiPet(QMainWindow, 透明无边框, 尺寸 = 精灵容器)
 └── QLabel(self.sprite_label)   ← setPixmap 显示角色（main.py:7964）
```

- 窗口尺寸**随精灵帧容器动态变化**（用「本动画所有帧的最大尺寸」做固定容器，避免逐帧抖动），
  变化时**保持中心不变**回算 `setGeometry`。
- 缩放因子 `scale_factor` 默认 **2.0**（像素风放大）。
- 有精灵缓存 `self._sprite_cache`（上限 100）。

### 2.2 `paintEvent` 是空的（★ 重要事实）

```python
# main.py:4969 —— 唯一的主窗口绘制入口
def paintEvent(self, event):
    painter = QPainter(self)
    try:
        painter.setRenderHint(QPainter.Antialiasing)
        painter.fillRect(self.rect(), QBrush(QColor(0, 0, 0, 0)))   # 只填全透明
    finally:
        painter.end()
```

**没有任何背景/前景分层的概念。** 全项目 `z_order` 只出现在**窗口层级**语境
（`floor_manager` 的窗口 z 序 / `_apply_pet_z_order`），**不是渲染分层**。

> ⚠️ 局限：本窗口尺寸**只包住角色**，所以往 `paintEvent` 里画背景会被裁到角色大小。

### 2.3 多屏坐标（场景系统必须遵守）

```python
# main.py:8483 —— 多屏虚拟桌面矩形（支持负坐标副屏）
def _virtual_screen_rect(self):
    vx = win32api.GetSystemMetrics(76)   # SM_XVIRTUALSCREEN
    ...
# main.py:8505 _current_screen_rect()  → 宠物当前所在屏
# main.py:8520 _clamp_pos_to_desktop() → 夹紧到虚拟桌面
# main.py:8538 _desktop_floor_y()      → "地面" y 坐标
```

> ❗ 铁律（项目既有）：**禁用 `QApplication.desktop().availableGeometry()`**（只返主屏 →「瞬移」）。

---

## 3. 动画系统

### 3.1 `animations.json` schema

顶层仅 3 键：`schema_version`(=1) / `meta` / `groups`（**112 个动画组**）。

两种真实形态：
```json
"idle":       { "frames": ["spr_ralsei_idle_0.png", "..."], "comment": ["基础动作"] }
"splat_mad":  { "frames": [], "comment": ["..."], "alias_of": "fall_mad" }
```

已知字段（`sprite_loader.py:31 _GROUP_KNOWN_KEYS`）：
`frames` / `loop` / `offset` / `alias_of` / `legacy` / `comment`。

> ⚠️ **`loop` / `offset` 字段在 schema 里预留，但 112 个组无一使用**；
> **帧率是全局的**（`config.json` 的 `animation.fps = 6`），不是每组一个。
> `loop` 语义目前由代码里的 `_need_advance` 白名单**隐式**决定。

### 3.2 加载机制

```python
# sprite_loader.py:28
ANIMATIONS_JSON_SUBPATH = ('ralsei_pet', 'assets', 'animations.json')
_G ROUP_KNOWN_KEYS = ('frames', 'loop', 'offset', 'alias_of', 'legacy', 'comment')
# :45 素材目录（注意在仓库上一级！）
self.sprite_dir = os.path.join(_root, "deltarune_ralsei")   # 1112 张 PNG
self.face_dir   = os.path.join(_root, "ralsei_face")
```

- **JSON 优先，读不到/校验不过 → 回落代码内置表** `_builtin_animation_mapping`。
- 缺失动画会被 `note_animation_miss` 记账。

### 3.3 动画切换总闸 `change_animation`（L6208）—— 四道拦截

1. **名字不存在** → 按 `_` 前缀从长到短回退（`walk_up_blush` → `walk_up` → `idle`）。
2. **优先级闸**（L6300）：`new_priority < cur_pri` 直接拒绝（优先级表 L870–L916）。
3. **`spell` 阶段硬拦截**（L6250）：walking 只许 spell/walk；casting 只许 spell。
4. **躲猫猫阶段硬拦截**（L6261）+ **特殊动画"播完为止"**（L6282，`_is_special_anim`）。

### 3.4 ★ 特殊动画的触发来源闸门（场景系统必须遵守）

"特殊动画"定义在 `_NON_SPECIAL_ANIM_GROUPS`（L234–L240）；
`_is_special_anim`（L6191）判 `group = name.split('_')[0]` 是否不在白名单。

**触发来源只有两个（用户第八轮口径，写死在 `update_animation_by_emotion` L4493–L4512 注释）：**

> 1) 本地 AI 决策（`ai_driver` → `play_animation_once`）；
> 2) 用户的显式交互（右键菜单、抚摸/戳一戳等点击事件）。

`update_animation_by_emotion()` **已被改成空实现 `return`（L4512）** ——
情绪→动画自动映射、10% 随机 `force=True` 两条老路径**被彻底删除**。

`ai_driver` 侧闸门（`ai_driver.py:102–L115`）：
```python
ANIMATION_ACTIONS = set(ANIMATION_METADATA.keys())   # 只有元数据表里的动作能被 AI 选
```

> **⇒ 场景物件若要有"特殊动画反应"，按现有架构必须经 `ai_driver.note_event()` 上报，
> 由模型决定播不播** —— 否则就是用户明令禁止的"抽风式突然一下"。

---

## 4. 状态管理

### 4.1 状态分布（三处）

**① 运动/物理状态**（`init_movement` L1031–L1223，全在宿主实例字典）：
- 位置 `target_pos` / `speed` / `current_speed_x/y` / `current_direction`
- 物理 `mass=30.0` / `gravity=500.0` / `friction=0.95` / `bounce_coefficient=0.2`
- 跳跃 `is_jumping` / `jump_height` / `stamina` / `max_jumps`
- 坠落 `is_falling` / `is_recovery` / `_fall_phase` / `_fall_reason` / `_fall_vx/_vy`
- **楼层 `current_floor`(L1175) / `current_window`(L1156) / `window_level`(L1157) /
  `spatial_pos={x,y,z}`(L1171) / `current_platform_z`(L1172)**
- 环境 `time_of_day` / `weather` / `temperature` / `humidity`（L1183–L1187）
- 情绪 `self.emotions`（8 维）+ `self.mood`（L1190–L1200）
- 动作标志 12 个（`is_spellcasting` / `is_rolling` / `is_sleeping_walk` …，L1134–L1146）

**② 子系统内部状态**：`energy_hunger.energy/hunger`、`relationship._trust`（**不可见**，只暴露档位）、
`memory_system`、`emotion_system`、`customization_system.customization_data`。

**③ 游戏状态**：`self.game_state`（`__init__` L433，12 字段）+ `guess_number_game` + `rock_paper_scissors_options`。

### 4.2 ★ 统一状态读写入口 = **宿主持有 + 控制器借用**

- **宿主 `RalseiPet` 是唯一状态持有者**；控制器**只搬方法不搬状态**（经 `self.p` 读写）。
- ❗**铁律**（`main.py:770–L806` 有完整血泪注释）：控制器会用到的状态字段
  **必须在 `init_systems` 里"预声明"**，否则控制器 `__setattr__` 的判据
  （"宿主**已拥有**该名字"）不满足 → **状态劈成两份**。
  - 实例：W1-1 未预声明 `_spell_seen_frame` / `_spell_cast_start_time`；
    W1-2 未预声明 `_hide_search_started_at` / `_hide_search_checked` / `_hide_moving_cb` /
    `_hide_moving_cb_stage` → 后者导致"到达回调永不触发 → 躲猫猫卡在走路态"。

> **⇒ 场景状态（当前场景 id、场景内坐标等）必须声明在宿主，不能声明在 SceneController。**

### 4.3 持久化：`data_store` 是唯一入口

```python
# data_store.py:49
VAULT_SUBDIR   = 'RalseiMemory'   # 最终存储：E 盘（读永远优先）
STAGING_DIRNAME= 'RalseiPet'      # 本地中转站：%LOCALAPPDATA%\RalseiPet
ENV_DATA_DIR   = 'RALSEI_DATA_DIR'
```

API：`artifact_path` / `artifact` / `ensure_artifact`（**只复制不搬走**）/ `app_file` /
`data_root` / `migrate_from_staging`。

用法实例：`customization_system.py:17` `data_store.app_file('customization_config.json')`。

---

## 5. AI 对话链路

### 5.1 完整调用链

```
用户回车
 └─ dialogue_ui.send_message()                      [dialogue_ui.py:1219]
      └─ parent.chat_with_ai(user_input, on_reply, on_delta)   [dialogue_ui.py:1337]
           └─ RalseiPet.chat_with_ai()             [main.py:6618]
                ├─ 主线程准备 system（见 §5.2）
                ├─ threading.Thread(_worker).start()  [main.py:6838]
                │    ├─ cli.chat_stream(...) 或 cli.chat(...)
                │    ├─ _clean_ai_reply()  （护栏清洗）
                │    ├─ 判退 → 重采样一次(temperature+0.1)
                │    └─ self._api_result.emit(...)    （跨线程信号）
                └─ _on_api_result → callback(reply)   [main.py:7306]
                     └─ dialogue_ui._on_ai_reply() → add_dialogue + 打字机
```
流式分片走另一条信号：`_api_delta`(L394) → `_on_api_delta`(L7318) → `dialogue_ui.stream_delta`。

### 5.2 ★ prompt 拼装顺序（`main.py:6666–L6780`）

**用户消息 = 纯原话（不拼任何东西）**；一切挂在 **system 尾部**：

```python
# main.py:6670
user_msg = text                                   # ← 用户消息 = 纯原话
system = self._build_persona_prompt()             # ① persona.md 全文（骨架）
_ctx = "" if lean else self._build_ai_context()
if _ctx: system = system + "\n\n" + _ctx          # ② 【此刻】状态块
_focus = self.dialogue_ui.get_focus_brief()
if _focus and not lean: system += "\n\n" + _focus # ③ 话题锚
_recall = self.memory_system.recall_text(text, limit=3)
if _recall and not lean: system += "\n\n" + _recall   # ④ 【零星的回忆】
_wv = worldview_recall.recall_text(text, limit=2)
if _wv: system += "\n\n" + _wv                    # ⑤ 【你想起了那边的事】
_rel_brief = ...
if _rel_brief: system += "\n\n" + _rel_brief      # ⑥ 关系档位（数值不可见）
```

**`lean=True`（事件台词专用）：只发 persona，跳过 ②–⑥ 全部** ——
目的是保住 Ollama 的 **KV 前缀缓存**（首字 2.5s → 0.6s）。

### 5.3 ★ `_build_ai_context()` 的七项「此刻」（`main.py:6840–L6937`）

| 分片 | 行号 | 产出示例 |
|---|---|---|
| 时段 | L6848 | `现在是深夜`（7 档） |
| 天气 | L6866 | `天气晴` |
| 心情 | L6872 | `心情开心` |
| 精力/饥饿 | L6878 | `你累得快撑不住了` |
| **★ 载体状态** | **L6895–L6907** | **`你正从高处往下掉` / `你正在走动` / `你站在一个打开的窗口上` / `你站在桌面上`** |
| 冷落时长 | L6908 | `你有很久没跟对方说话了` |
| 记忆偏好 | L6920 | `对方的名字是X` |

```python
# main.py:6897 —— ★ 场景描述注入的现成位置
try:
    if getattr(self, 'is_falling', False):
        parts.append("你正从高处往下掉")
    elif getattr(self, 'is_moving', False):
        parts.append("你正在走动")
    elif getattr(self, 'current_window', None) or getattr(self, 'window_level', 0):
        parts.append("你站在一个打开的窗口上")
    else:
        parts.append("你站在桌面上")
except Exception as e:
    _log.debug("AI 上下文：载体状态分片获取失败（已忽略）: %s", e)
```

最终格式（L6937）：
```python
return "【此刻】" + "，".join(parts) + "。（这些是你现在的状态，可以自然地带一点出来，但别逐条念，也别硬把它转成话题。）"
```

> ★ **载体状态那一项就是"场景描述注入"的现成位置** —— 它已经在说"你在窗口上/在桌面上"。
> 加一条 `elif self.current_scene:` 即可。

### 5.4 persona / worldview 的读取

- **`ralsei_persona.md`**：`_build_persona_prompt()`（L6982）读取，**带缓存 + 兜底**。
  > 为什么 App 读文件：**Ollama 用请求里的 system 整体替换 Modelfile 的 SYSTEM**，
  > 写在模型里的设定在真实应用里一次都不生效 ⇒ 读文件是**本机唯一可行的"训练"杠杆**。
- **`ralsei_worldview.md`**：**不常驻**，按需召回（`@@` 块 + 图边，`recall_text`，`MAX_BLOCKS=2`）。

---

## 6. ★★ 已存在但未接线的「场景/房间」代码（最重要发现）

**`modules/customization_system.py`（467 行）里躺着一整套场景数据结构，全部是死代码。**

| 位置 | 内容 | 现状 |
|---|---|---|
| `:27` | `appearance.background = 'default'` | 定义了，**无任何消费者** |
| `:28` | `appearance.weather_effects = False` | 定义了，未接线 |
| `:29` | `appearance.lighting_effects = 'default'` | 定义了，未接线 |
| `:30` | `appearance.particle_effects = False` | 定义了，未接线 |
| `:71` | `environment.home_location = 'bedroom'`（注释列 `bedroom/living_room/office/kitchen`） | 定义了，**无任何消费者** |
| `:308` | `get_available_backgrounds()` → **10 种**：`default/forest/castle/city/night_sky/beach/space/garden/library/cafe` | **零调用** |
| `:312` | `get_available_lighting_effects()` → **10 种** | 零调用 |
| `:336` | `get_available_environment_locations()` → **10 种**：`bedroom/living_room/office/kitchen/garden/park/library/cafe/beach/forest` | **零调用** |
| `:400–402` | `background = appearance['background']` 后紧跟 `# 这里可以添加实际的背景变化逻辑` | **官方预留的接入坑位** |
| `:404–414` | 光照 / 粒子 / 天气 三处**同样只有空注释** | 官方预留坑位 |
| `:378` | `apply_appearance_changes()` —— 已实现「调整动画速度」部分 | 可作为新 setter 的模板 |
| `:368/:373` | `update_environment()` / `update_relationship()` —— **已带 `save_config()`** | 新场景 setter 可照抄 |

**结论：**
1. 这两张清单（10 背景 + 10 位置）**是通用名，不是 Deltarune 名** ——
   场景系统要做「把原作世界搬上来」，需要**用 Deltarune 命名替换/扩展**它们。
2. `apply_appearance_changes()` 的四处空注释 = **本项目官方预留的"场景渲染接入点"**。
3. **没有任何视觉实现**：8876 行 `main.py` 里 `background` 只出现在 `fillRect(全透明)`
   和 `customization_system` 的设置项里；**没有任何一张背景图被加载**。
4. ⚠️ **素材缺口**：`deltarune_ralsei/` 1112 张 PNG **全是 Ralsei 角色精灵**
   （唯一例外 `spr_cutscene_27_queen_hand_surprise_ralsei.png` 是女王的手，属剧情素材）。
   **场景系统必须先解决背景/物件素材来源。**

---

## 7. 控制器分层（★ 场景系统必须复用的模式）

### 7.1 宿主转发壳

```python
# main.py:512
_CONTROLLER_ATTRS = ('games', 'video', 'spell', 'hide_seek', 'file_sheet')

# main.py:514
def __getattr__(self, name):
    for attr in RalseiPet._CONTROLLER_ATTRS:
        ctrl = self.__dict__.get(attr)          # ← 必须 __dict__.get，不能 self.attr（递归）
        if ctrl is None: continue
        impl = getattr(type(ctrl), name, None)  # ← 只看类上定义的名字（不触发回落链）
        if impl is not None: return getattr(ctrl, name)
    raise AttributeError(name)                  # ← 必须抛原始 AttributeError
```

### 7.2 控制器侧反向 `__getattr__`

`games_controller.py:97` 起：回落给宿主读状态（先在宿主实例字典找，再在宿主类型 MRO 找）。
**双向转发必须有一侧是显式白名单**，否则成环（真机踩过 `RecursionError`）。

### 7.3 ★ 新增一个控制器要动的 4 处（场景系统的施工清单）

| # | 动作 | 位置 |
|---|---|---|
| 1 | 新建 `modules/scene_controller.py`（`self.p = pet` + 控制器侧 `__getattr__` 回落） | 新建（照抄 `games_controller.py`） |
| 2 | `_CONTROLLER_ATTRS` 加 `'scene'` | `main.py:512` |
| 3 | `init_systems` 里创建 `self.scene = SceneController(self)` | `main.py:761` 之后（见 §8.2 顺序） |
| 4 | **预声明所有场景状态字段** | `main.py:806` 之后（照抄 `_spell_*`/`_hide_*` 声明区） |

> ❗**控制器不许 import 任何项目内模块**（避免初始化环）；
> 宿主能力全部通过 `self.p` 动态取用。

---

## 8. 初始化顺序

### 8.1 `__main__` 块（L8832）

```
check_single_instance()      # 单实例互斥
install_crash_guard()        # 全局异常兜底（必须早于 QApplication）
text_segmenter.install()     # jieba
data_store.describe() + migrate_from_staging()   # 数据回迁
app = QApplication(sys.argv)
window = RalseiPet(); window.show(); app.exec_()
```

### 8.2 `init_systems()`（L660–L807）完整顺序

| # | 行号 | 创建物 |
|---|---|---|
| 1 | L662 | `desktop_interaction` |
| 2 | L663 | `dialogue_system` |
| 3 | L664 | `dialogue_ui` |
| 4 | L665 | `weather_system` |
| 5 | L666 | `pet_ai` |
| 6 | L672 | `search_summarizer`（失败置 None） |
| 7 | L678 | `energy_hunger` |
| 8 | L679 | `emotion_system` |
| 9 | L680 | `memory_system` |
| 10 | L685 | `relationship` |
| 11 | **L686** | **`customization_system`** |
| 12 | L687 | `social_growth` |
| 13 | L688 | `entertainment_system` |
| 14 | L692 | `sound_manager` |
| 15 | **L698** | **`floor_manager`** ← 建楼/地面 |
| 16 | L702 | `autonomous_agent` + `.start()` |
| 17 | L727 | `api_client` |
| 18 | L728 | `command_manager` |
| 19 | L733 | `ai_driver` |
| 20 | L743 | `games` |
| 21 | L746 | `video` |
| 22 | L751 | `spell` |
| 23 | L755 | `hide_seek` |
| 24 | L761 | `file_sheet` |
| 25 | L764 | `init_placeholder_system()` |
| 26 | L770–L806 | **状态预声明区**（`_spell_*` 11 个 / `_hide_*` 10 个） |

> ★ **`SceneController` 应插在第 24 项之后、第 25 项之前** —— 此时
> `customization_system` / `floor_manager` / `sprite_loader` 都已就绪。

### 8.3 `__init__` 内的调用序列（L396）

```
ConfigManager() → api_config = get_api_config()
init_ui()          # 透明窗口 + sprite_label
load_resources()   # SpriteLoader().load_sprites()
init_systems()     # §8.2
init_animation()   # 动画定时器 + 优先级表
init_movement()    # 物理参数 + 全部运动状态
init_timers()      # AI/stats/weather 定时器
_api_result.connect / _api_delta.connect
_event_line_picker = RecentLinePicker()
game_state / guess_number_game
_setup_tray()
```

---

## 9. 事件与台词

### 9.1 `event_speech.py` 档位表

```python
TIER_INSTANT = "instant"   # 立刻说罐头
TIER_AI      = "ai"        # 先问 AI，超时回落罐头
EVENT_MAX_CHARS = 24

# :47 只有登记为 TIER_AI 才走模型；未登记一律 TIER_INSTANT（安全默认）
EVENT_TIERS = { "poke_body": TIER_AI, "pinch_ear": TIER_AI, ...,
                "fling": TIER_INSTANT, "splat_poked": TIER_INSTANT }

# :85 事件 → 给模型看的"刚刚发生了什么"
EVENT_DIRECTIVES = { "poke_body": "（主人用手指戳了戳你的身体。）", ... }
```

关键函数：`tier_of`(L125) / `pet_kind`(L130) / `build_prompt`(L135) / `first_sentence`(L143) /
`RecentLinePicker`（罐头去重窗口）。

### 9.2 `speak_event`（`main.py:4802`）

```python
def speak_event(self, kind, pool=None, face="happy", instant=False):
    """* `pool=[...]`：保留内置台词，AI 不可用/超时/判退时说。
       * `pool=None`：不给内置台词 —— 说不了就**不说话**。返回值同样是 ""。
    """
```

`_event_ai_ready`（L4765）**6 道闸**：总开关 / 档位是 AI / 用户正在输入 / `_ai_inflight` /
`_streaming` / `_event_speaking` + `EVENT_SPEAK_MIN_INTERVAL` 频率闸。

### 9.3 ★ `ai_driver.note_event` = 场景物件与 AI 的唯一合法上报通道

```python
# ai_driver.py:285
def note_event(self, text, emotion_hint=""):
    """规则系统上报一条环境观察，供下一次决策参考。

    第八轮约定：非 AI 的自动触发（凑近桌面文件、陪看视频等）**不再自行播放
    特殊动画**，只把"我看到了/我正在做什么"写进队列；要不要据此说句话或做个
    小动作，由模型自己决定。AI 未启用时事件只是静静攒着（上限 6 条）。
    """
    self._pending_events.append((t, str(emotion_hint or "").strip()))  # deque(maxlen=6)
```

消费点：`_fire` / `_build_prompt`（`ai_driver.py:605–L606` `popleft()`）。

---

## 10. 资源与配置

### 10.1 素材路径（★ 像素素材在仓库上一级，不在 `assets/` 内）

| 用途 | 真实路径 | 内容 |
|---|---|---|
| **角色精灵** | `../deltarune_ralsei/` | **1112 张 PNG，全是 Ralsei** |
| 头像 | `../ralsei_face/` | `face_*.png` |
| 文本框 | `../textbox/` | `textbox_base.png` / `.svg` |
| 声音 | `ralsei_pet/assets/sounds/` | 3 个文件 |
| **空目录** | `ralsei_pet/assets/sprites/`、`assets/faces/` | **都空** |

### 10.2 `assets/` 完整树

```
ralsei_pet/assets/
├── animations.json          22,870 B  (112 组, schema_version 1)
├── ralsei.modelfile          3,026 B
├── ralsei_persona.md         9,748 B  (138 行)
├── ralsei_worldview.md      11,830 B  (11 个 @@ 块)
├── faces/                     (空)
├── sounds/                    (3 个音频)
└── sprites/                   (空)
```

### 10.3 配置文件

| 文件 | 位置 | 内容 |
|---|---|---|
| `config.json` | `data_store.app_file` | `api`（enabled/model/stream/event_speech/options）/ `animation.fps=6` / `movement.speed=5.0` / `ui` / `behavior` |
| `customization_config.json` | `data_store.app_file` | **`appearance.background` / `environment.home_location`（未接线）** |
| `coordinates.json` | `ralsei_pet/`（gitignore） | 坐标缓存 |
| `animation_history.json` | `ralsei_pet/`（gitignore） | 动画历史 |
| `memory.json` | `data_store` | 记忆 |

---

## 11. ★ 场景系统接入点候选清单（按优先级）

| 优先级 | 接入点 | `文件:行号` | 理由 |
|---|---|---|---|
| **P0** | 复用 `customization_system` 的 `background`/`home_location` 作场景数据源 | `customization_system.py:27,71,308,336,400` | **本项目官方预留的接入坑位** |
| **P0** | `self.current_scene` 加入宿主预声明区 | `main.py:806` 后 | 避免状态劈两份（§4.2 铁律） |
| **P0** | `get_available_backgrounds/locations` 换成 Deltarune 命名 | `customization_system.py:308,336` | 现清单是通用名，非原作名 |
| **P1** | 新建 `SceneController` | 新建 + `main.py:512` + `main.py:761` 后 | 复用控制器转发壳（§7） |
| **P1** | 场景描述注入 AI「载体状态」分片 | **`main.py:6902`** | **现成位置**，已在说"你在窗口上/桌面上" |
| **P2** | 场景物件命中接入鼠标分发 | `main.py:5068` 之前 | 现在先判 `sprite_label.geometry().contains()` |
| **P2** | 场景物件复用 `desktop_elements` schema | `desktop_interaction.py:187`（含 `rect`/`weight`/`material`） | 现成数据模型 |
| **P2** | 物件查询复用 `get_element_at_pos` | `main.py:1022` | 已有"某坐标上是什么" |
| **P2** | 物件反应挂 `react_to_desktop_element` | `main.py:3996` | **已有"对物件做反应"总入口**（含防抖） |
| **P2** | 场景物件上报 `ai_driver.note_event` | `ai_driver.py:285` | **唯一合法通道**（§9.3） |
| **P3** | 背景渲染：新增独立背景窗口（推荐）或扩展 `paintEvent` | 新建 / `main.py:4969` | `paintEvent` 只填透明；但本窗口只包住角色 |
| **P3** | 新增"进入场景"右键菜单项 | `main.py:5679 show_interaction_menu` | 已有的菜单入口 |
| **P4** | 场景物件作为新 floor 注入 `floor_manager` | `floor_manager.py:195 _generate_floors()` | 目前只从 `underlying_windows` 生成 |
| **P4** | 场景内"新摔倒起因"登记 | `main.py:72–L76` `FALL_MAD_REASONS` | 现有派生体系的登记处 |

### 11.1 物理系统与"地面"的关系

| 接入点 | `文件:行号` | 说明 |
|---|---|---|
| **★ `desktop_floor`** | `floor_manager.py:131` | **"地面"= 桌面层**（`type='desktop'`、`platform_height=0`），rect 由虚拟屏 API 算（L147–L151） |
| **★ `_desktop_floor_y()`** | `main.py:8538` | **"地面"的 y 坐标唯一真源** |
| `get_current_floor(pos)` | `floor_manager.py:429` | 脚下应该是哪块楼板 |
| `get_drop_destination(pos,cur)` | `floor_manager.py:496` | 向下坠落的第一块接得住的地板 |
| `get_jump_destinations(cur,pos)` | `floor_manager.py:513` | 可跳目的地 |
| `update_floors()` | `floor_manager.py:140` | 楼层表重建（每 2s 由 `check_window_movement` L3254 调） |
| `handle_gravity_fall` | `main.py:3536` | 重力掉落主循环 |

> **场景若只是"背景 + 可交互物件"，地面判定不用改**（继续用 `desktop_floor`）。
> 但若要让宠物**站在场景物件上**（如城堡镇的箱子），需给 `floor_manager` 开**场景楼层注入点**。

---

## 12. 相关报告与既有口径

- 物理/建楼/多屏等**勿回退契约**：`.workbuddy/memory/MEMORY.md` §4 + 详版 §4.1–§4.12
- 控制器转发铁律：详版 §8.2–§8.6（W1 搬运铁律 8 条 + A/B 结构性盲区）
- 人味线（persona / 护栏 / 事件台词）：详版 §6 / §11 / §15
- 本报告为**只读勘察**，未修改任何产品代码；G2 基线 1169 PASS 未被触碰。
