# 第 45 轮报告 —— 场景自主寻路（AI 路线图）

- 用户原话：「**你需要一个路线图，也就是假设，ralsei 和我说话，语境里表达了我们该去教堂看看，那 ralsei 怎么自主地按路线去教堂这类的路线问题，也记得注意**」
- 一句话回答：**把「去哪」和「怎么去」拆开**——
  `语境 → 目的地（语义）` 与 `当前位置 → 目的地（图搜索）` 是**两个独立问题**，
  中间用「场景 id」当**唯一接口**。前者吃 AI 的理解力，后者吃原作拓扑。
- 交付：`modules/scene_pathfind.py`（新，纯标准库零 Qt）+ `scene_controller` 接线
  + 回归锁 `pathfind_round45` + 本文档（路线图）
- ⚠️ 本轮同时**实锤一个原作拓扑缺口**（§4），是"力争完美"该补的那一块。

---

## §1 先把问题拆对（这是本轮最重要的判断）

用户举的例子是「聊天里说到该去教堂看看」。这句话里其实藏着**三个不同的问题**，
它们**不能用同一套机制解决** —— 混在一起做正是过去 7 轮"只探不做"的根源：

| # | 问题 | 输入 | 输出 | 该谁负责 | 本轮做不做 |
|---|---|---|---|---|---|
| ① | **意图**：他是不是想去某处？ | 对话文本 | `{"go": "教堂"}` 或 None | **AI / 关键词规则**（语义） | P0 只留接口 |
| ② | **定位**：目的地到底是哪个场景？ | `"教堂"` | `ch1.hometown.town_church` | **命名匹配 + 别名表**（数据） | ★ 本轮做 |
| ③ | **寻路**：从当前位置怎么走过去？ | 起点 id + 终点 id | `[A→B→C→D]` + 逐门 | **图搜索**（原作拓扑） | ★ 本轮做 |

**为什么必须拆开**：② 和 ③ 都**不依赖 AI**、**可离线回归**、**确定性**。
只有 ① 需要模型。把 ① 也塞进本轮 = 又要联网、又要真机、又要判"AI 理解对不对"，
那正是"验不了就永远做不完"的坑。**所以本轮把 ②③ 做到能验收，① 只留接口。**

---

## §2 数据流（路线图总览）

```
   用户说话「我们该去教堂看看」
        │
        │  ← ① 意图识别（P1，本轮只留接口 _extract_goal）
        ▼
   目标词 "教堂"
        │
        │  ← ② 语义定位 resolve_target()   ★ 本轮
        │     场景名 / 区域名 / 别名表 / 原房名（英文别名）
        ▼
   scene_id = ch1.hometown.town_church     （歧义时不猜 → 返回候选列表）
        │
        │  ← ③ 图搜索 plan_route()          ★ 本轮
        │     原作边（782 条）BFS 最短路径
        ▼
   [ch1.home.krisroom,  ch1.home.krishallway, ch1.home.torhouse,
    ch1.hometown.krisyard, ch1.hometown.north, ... , ch1.hometown.town_church]
        │
        │  ← ④ 逐门执行（P1：控制器按步 follow_route；本轮出计划不执行）
        ▼
   Ralsei 一步步走过去，每步带一句"为什么走这个门"
```

**关键纪律（沿用 scene_routing 的三条设计律）**：
1. **算不出 → 返回 `None`，不伪装**。没有路径 ≠ 挑一条近的；匹配不到场景 ≠ 随便挑一个教堂。
2. **坏数据跳过、不作废整张图**。一条边坏了不该让整个小镇不可达。
3. **未知字段放行**（渐进增强）。

---

## §3 ★ ③ 寻路：为什么是 BFS，以及"门"怎么当标签

### 3.1 边从哪来（原作拓扑，不是我们发明的）
- 源：`_room_graph.json`（第 42 轮反汇编取证，**782 条边**：ch1 238 / ch2 323 / ch3 79 / ch4 72 / ch5 70）。
- 边的形状：`{src: int, dst: int, door: str, kind: "delta"|"table"}`，
  `src/dst` 是 `Data.Rooms` **下标**（不是场景 id）。
- 换算：`(chapter, room_index)` → `scene_id`，靠 `_index.json` 的
  **`original_room_id`** 字段（★ 见 §3.3 的坑）。

### 3.2 为什么用 BFS 而不是 Dijkstra
原作门是**等权**的（走一个门就是走一个门，没有"这扇门更费劲"），
所以**最短路径 = 最少门数 = BFS**。用 Dijkstra 只会引入一个假的"代价"概念。
（若将来要给边加权重 —— 比如"需要钥匙的门"—— 再升级，接口不变。）

**为什么不是"直线距离"**：第 44 轮已证伪过——落点坐标是**房间自己坐标系里的
局部坐标**（10~300 的小数值），跨房比距离**在原理上不成立**（见
`routes44/路由重建说明.txt` 第二节）。所以**只能走图**，不能走几何。

### 3.3 ★ 两种 id 的换算坑（本轮实测）
`_room_graph.json` 用 `(chapter, room_index)`，产品用 `scene_id`，桥是
`_index.json` 里的 `original_room_id`。两个细节：

- **分片里的场景**（`_zone.<ch>.<area>.json`）**直接带** `original_room_id`；
- **独立文件场景**（`<scene_id>.json`）**文件里没有这个字段**，
  它只登记在 `_index.json` 的对应行上。

⇒ 换算必须**按 `scene_id` 在索引里反查**，不能在场景文件里找。踩过就写进回归锁。

### 3.4 五章可达性实测（本轮新发现，★ 见 §4）

数据源 = 第42轮 `_room_graph.json`（782 边），判据 = 从各章 `room_krisroom` 出发的 BFS 可达集。
**逐条实测值见 `_evidence/五章可达性实测45.txt`**（由 `_tools_reach45.py` 生成，可重跑）：

| 章 | `room_krisroom` 下标 | `krisroom` 出度 | 从 `krisroom` **可达房间数** | 备注 |
|---|---|---|---|---|
| ch1 | 2 | 1 | **28** | `krisroom → … → town_church` **7 跳** ✅ |
| ch2 | 18 | 1 | **18** | 同样 7 跳 ✅ |
| ch3 | 15 | 1 | **18** | 同样 7 跳 ✅ |
| ch4 | 15 | 1 | **4** | ❌ **断在 `torhouse`**（§4.1） |
| ch5 | 13 | **0** | **1** | ❌ 断在 `krisroom` 本身（§4.2） |

四章共有的那条 7 跳链路（**这是"去教堂"的标准答案**）：
```
krisroom → krishallway → torhouse → town_krisyard → town_north → town_mid → town_south → town_church
```
`doorA`   `doorC`      `doorX`      `doorC`        `doorC`     `doorC`      `doorC`(推测)
> ⚠️ 最后一跳 `town_south → town_church` 在 ch1~ch3 是 `doorC`(+2)；
> ch4/ch5 的 `town_church` 有多个独立门（`doorX` 表驱动），细节见 §4.2。

---

## §4 ★★ 本轮实锤的原作拓扑缺口（"力争完美"要补的）

### 4.1 ch4 缺一条回程边（确凿）
对照 ch5 与 ch4 的 `room_torhouse` 全部关联边：

| 章 | `town_krisyard → torhouse` | `torhouse → town_krisyard` |
|---|---|---|
| ch1 | ✅ `doorD` | ✅ `doorB`（走别的门型） |
| ch2 | ✅ | ✅ |
| ch3 | ✅ | ✅ |
| **ch4** | ✅ `obj_doorX_musfade` | ❌ **缺失** |
| **ch5** | ✅ `obj_doorX_musfade` | ✅ `obj_doorX_musfade` |

⇒ **ch4 的 `torhouse` 是死胡同**，导致从卧室出发**走不出家门**（可达面只有 4 间）。
**这不是我们的 bug，是第 42 轮取证时漏采了一条门实例**（`doorX` 是表驱动双向，
第 42 轮只采到单侧）。修法 = 回 UTMT 补采 ch4 的 `room_torhouse` 门实例
（属后续轮次，需重跑反汇编）。

### 4.2 ch5 从 `krisroom` 完全走不出去
ch5 的 `room_krisroom`（idx 13）**出边数 = 0**。而 ch5 反向可达 25 间——
说明 ch5 的门拓扑**是通的，只是起点那扇门没采到**。同一类缺口，同一类修法。

### 4.3 结论：寻路器**如实报告"走不到"**，不绕过
这是**故意**的：`plan_route()` 在 ch4/ch5 返回 `None`（+ 一条可读的原因），
而不是"假装到了"或者"就近凑一个"。理由与 scene_routing 设计律 1 同源 ——
**静默降级是本项目头号敌人**。缺口列进待办，用证据推着补，不用代码绕。

---

## §5 ② 语义定位：把"教堂"翻成 scene_id

### 5.1 匹配层级（从具体到笼统，命中即返回）
| 层 | 匹配对象 | 例：「教堂」命中 |
|---|---|---|
| L1 | 场景 `name` 尾部原样段 | `家乡·town_church` → 尾部 `town_church` |
| L2 | 场景 `name` 中文段（区域名+尾段） | `家乡·town_church` 含"教堂"？→ **不含**（是 `church`） |
| L3 | **别名表**（`_aliases.json`，中文→英文关键词） | `教堂 → church` ✅ |
| L4 | `name_raw`（原作房名） | 含 `church` ✅ |

**★ 关键发现**：产品场景名是「区域中文名·尾段原样」= `家乡·town_church`
——**中文只到区域名，"教堂"这个词本身是英文**（口径见记忆 §7「命名 = 不译」）。
所以**纯中文匹配会全军覆没**，别名表是**必需品**，不是锦上添花。

### 5.2 歧义处理（★ 不猜）
「教堂」在五章命中 **166 个场景**（ch4 的暗之圣域有 100+ 个 `church_*`）。
⇒ `resolve_target()` 必须返回**候选列表 + 每个候选的章/区域**，
由调用方（或 AI）决定去哪个；**只有唯一命中时才直接给 `scene_id`**。
这正是"不伪造"在②层的体现。

### 5.3 默认候选排序
1. 当前章优先（"我们去教堂"通常指**这一章**的教堂）；
2. 名字**更短更通用**的优先（`town_church` > `dw_church_bookshelfpuzzle3`）；
3. 场景**可游玩**（在索引里登记）优先。

---

## §6 ① 意图识别：本轮只留接口（为什么）

`_extract_goal(text)` 的**形状**已定，但**实现留 P1**：
- 输入：用户这句话 + 最近历史；
- 输出：`"教堂"` 这类目标词，或 `None`。
- 为什么不在本轮做：它**必须联网/真机**才能验（要判"AI 有没有正确理解"），
  而且**很容易做成关键词表**——那又会退化成"可逐字搬走的固定例句"（记忆 §5 头号失真源）。
- **正确做法（P1 计划）**：让 AI 用**现有对话链路**自然说出目标
  （把 `destinations_text()` + 别名表注入 system 尾部，AI 想走时**在回复里带一个
  结构化标记**，如 `[[go:教堂]]`），再解析这个标记。
  ⇒ 复用既有的**人味改造线**（护栏会剥掉乱七八糟的括号），不新造一套。

**接口先行**：`scene_pathfind` 只暴露 `resolve_target()` / `plan_route()` /
`plan_from_text()`（= ①+②+③ 串起来的门面），① 用**可替换的规则**占位，
P1 换成 AI 后**调用方一行不改**。

---

## §7 交付物

| 文件 | 说明 |
|---|---|
| `ralsei_pet/modules/scene_pathfind.py` | ★ 新：寻路 + 语义定位（纯标准库零 Qt） |
| `ralsei_pet/modules/scene_controller.py` | 接线：`plan_route_to()` / `plan_route_text()`（只读） |
| `ralsei_pet/assets/scenes/_aliases.json` | ★ 新：中文↔英文关键词别名表（改它就能加目的地，不碰 Python） |
| `code-quality-audit/第45轮-场景自主寻路/verify_pathfind45.py` | ★ 回归锁（35 项，含正/负控制） |
| `code-quality-audit/第45轮-场景自主寻路/_tools_reach45.py` | 五章可达性/缺口取证工具（产物落 `_evidence/`） |
| `code-quality-audit/第45轮-场景自主寻路/_evidence/五章可达性实测45.txt` | ★ 可达性 + ch4/ch5 缺口实证 |
| `code-quality-audit/regress/run_all.py` | 接线：`pathfind_round45` 进 `SUITES` + `HERMETIC_IDS` |
| `code-quality-audit/场景系统-P0/verify_scene_p0.py` | 白名单扩 `scene_pathfind`（A6 初始化环闸门） |
| 本文件 | 路线图（设计文档） |

---

## §8 回归与验收

### 8.1 寻路回归锁（`verify_pathfind45.py`，35 项）

| 段 | 判据 | 关键内容 |
|---|---|---|
| A 数据 | A1–A5 | 别名表 schema=1 / ★**零命中词条 = 0**（64 条全拿真实索引验过）/ 原作 **782 边**五章分章数（238·323·79·72·70）/ 邻接表守恒 / 1,014 场景索引 |
| B ③ 寻路 | B1–B7 | ch1 `krisroom(2)→town_church(15)` **恰 7 跳** / 逐边接得上·无环 / 起终点相同 → `[]`（区别于 `None`）/ ★负控制：无回边 → `None`（**不就近凑**）/ 坏输入不抛 / 确定性 / ★**如实报告 ch4/ch5 走不到** |
| C ② 定位 | C1–C7 | 『教堂』+ch1 → `ch1.hometown.town_church` 唯一 / 负控制：不存在 → `ok=False` / ★**消歧顺序先章后精度**：『医院』+ch1 **绝不跨到 ch5** / 无章多解 → `ambiguous`（不猜）/ 分级匹配把候选压到 ≤10（未分级时全域 166）/ 别名表**不可省**（不给它就不命中） |
| D 门面 | D1–D9 | 用户原场景全链路 7 跳 8 房 / `path` 首尾与 7 条 `steps` / `describe_plan` 中文计划 / 负控制：不可用→空串 · 目标不存在 · 跨章（第三圣域唯一→ch4）· 非字符串 · 当前场景未知 / ★失败**不留半个计划** |
| E 接线 AST | E1–E7 | `scene_pathfind` 零 Qt · 本地依赖 ⊆ {`scene_system`} / 控制器真调 5 个函数 / 4 个只读方法在位 / ★**零行为变化**（4 方法体内无 `switch`·`QTimer`）/ `main.py` 预声明 4 字段 / ★`switch()` 里 `_scene_chapter_id` **恰赋值 1 次** / 幂等守卫 |

### 8.2 鉴别力体检（逐项破坏 → 精确报红 → 还原）

| 破坏 | 报红判据 | 条数 |
|---|---|---|
| 消歧顺序回退（先精度） | C1 / C4 / C6 / D1 / D2 / D3 | 6 |
| 走不到返回 `[]`（伪造） | **B4** | 1 |
| 分级匹配失效 | C1 / D6 | 2 |
| `switch` 不投影章 | **E6** | 1 |
| 还原 | — | 0（35/35 PASS，文件字节一致） |

### 8.3 全量 G2

| 项 | 值 |
|---|---|
| 套件数 | **41**（新增 `pathfind_round45`） |
| PASS / FAIL | **1878 / 0** |
| 比对 | 全 **IDENTICAL** |
| 新增基线 | `pathfind_round45`（35 项） |

**本轮触发的两处既有套件变更（都是"新增合法零依赖模块"的预期后果，非缺陷）**：
- `round5_smoke` DIFF：模块数 43 → **44**（多 `scene_pathfind`）。该套件的说明本来就写着
  「模块数随新增模块而变，**勿把具体数字写进描述**」⇒ 属有意变更，已 `--update`。
- `scene_p0` A6 FAIL：控制器 import 白名单是**显式**的（新增一条必须手加一行，这个"麻烦"是刻意的）。
  已把 `scene_pathfind` 加入 `_CTL_ALLOWED` 并写明理由 ⇒ 90/90 PASS，已 `--update`。
  ★ 这条断言正是它的设计目的：**逼每一次放宽都成为有意识的决定**（第 29、44、45 轮都被它逮到）。

### 8.4 端到端实跑（用户原场景）

```
resolve 教堂 = ch1.hometown.town_church
plan ok = True | hops = 7
【我打算这么走过去】（目的地：ch1.hometown.town_church，共 7 步）
1. ch1.kris_room.kris_s_room → ch1.home.krishallway：走 obj_doorA 出去
2. ch1.home.krishallway     → ch1.home.torhouse   ：走 obj_doorC 出去
3. ch1.home.torhouse        → ch1.hometown.town_krisyard：走 obj_doorX_musfade 出去（表驱动门）
4. ch1.hometown.town_krisyard → ch1.hometown.town_north：走 obj_doorC 出去
5. ch1.hometown.town_north  → ch1.hometown.town_mid：走 obj_doorC 出去
6. ch1.hometown.town_mid    → ch1.hometown.town_south：走 obj_doorC 出去
7. ch1.hometown.town_south  → ch1.hometown.town_church：走 obj_doorC 出去
负控制 ok = False | err = 没有场景匹配 '不存在的地方xyz'
```

---

## §9 待办（推给后续轮次，附证据）

1. ★★ **补采 ch4 `torhouse` / ch5 `krisroom` 的门实例**（重跑 UTMT 反汇编）
   ⇒ 修掉 §4 两处缺口，ch4/ch5 才能整章寻路。
2. ★ **P1：① 意图识别接 AI**（`[[go:目标]]` 标记 + 解析），复用对话链路 + 护栏。
3. **④ 逐门执行**：控制器 `follow_route` 目前只处理"当前场景的直接出边"；
   寻到多跳路径后要支持**按步推进**（每到一个房间再取下一步）。
4. `_original_rooms.json` 加 `scope` 改名（旧待办，防"误当全表"）。
5. 路由覆盖 30.6% → 读 `obj_doorAny/W/X` 对象代码补边（旧待办，与 §4 同源）。
