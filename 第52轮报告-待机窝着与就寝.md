# 第52轮报告（下半）· 待机窝着 + 就寝 + 睡/醒台词迁移

> 上半（对话框一轮一轮 / 输入框常驻 / 清内置对话 / 捉迷藏双闸 / 7B 底座迁移）见提交
> `54398f0` 与 `.workbuddy/memory/2026-09-27.md`。本报告只覆盖下半。
> 时间：2026-09-27　回归：**PASS=2508 / FAIL=0 / 50 套件 / 全 IDENTICAL / exit=0**

---

## 0. 一句话结论

用户第 52 轮的十项需求，**已完成 9 项 + 1 项待裁定**；本轮下半新做的是
**第⑧项（静止时到快捷栏上沿待着）** 与 **第⑨项（23:00±10 分钟回房睡觉）**，
外加把**睡/醒/哼三组内置台词**补进第⑤项的迁移面。

**最重要的一条不是新功能，是一条根因**：第③项（"待机只会在静止超过 10 分钟后触发"）
**在真机上是句空话** —— 门限 600s 永远达不到。下面是证据。

---

## 1. ★★ 根因：待机动画"结构性地从未播放过"

### 1.1 现象
用户原话：「他这一直站着不动是什么情况，**像贴图似的**」。

### 1.2 代码链
```
update_animation()  静止分支：
    self._idle_loop_active = bool(self.idle_timer >= self.IDLE_LOOP_MIN_SECONDS)   # 600.0
        ▲                                       ▲
        │                                       └─ 第51轮按用户口径从 180 抬到 600
        └─ idle_timer 只在"不移动"时累加（update_movement 2936 行）

update_movement()  空闲分支：
    self.idle_timer += timer_dt
    if self.idle_timer >= self.max_idle_duration:
        self.randomize_movement_pattern()
        self.idle_timer = 0          # ← 每次漫游重启都清零
```
`randomize_movement_pattern()` 把 `max_idle_duration` 随机成 **2.0 ~ 12.0 秒**
（第51轮甚至把最长档从 25s **缩到** 12s）。

⇒ `idle_timer` 的最长可达值 ≈ **12 秒**，而门限是 **600 秒**。
⇒ `_idle_loop_active` **恒为 False**，"待机动画"**从未播放过**，
   宠物永远定格在站立首帧 —— 就是用户说的"像贴图似的"。

> 第51轮把休息时长从 25s 缩到 12s，方向是**反的**：休息越短 → `idle_timer`
> 被清零得越勤 → 门限越不可能达成。

### 1.3 处置
判据换成一个**不会被自主漫游清零**的量：**距上次用户互动**。

```python
self._idle_loop_active = bool(
    self._lounge_since is not None                      # ★ 待机状态（本轮新增）
    or self.idle_timer >= self.IDLE_LOOP_MIN_SECONDS)   # 旧路保留（round8_anim E1.3/E1.4 仍绿）
```

并新增**待机（窝着）状态机**：无互动满 10 分钟 → 进入待机 → **关掉随机漫游**
→ 走到窝点 → 一直待着；一有用户互动（或变忙）立刻退出。

★ 正/负控制：`B8b`（待机态 + idle_timer 仅 10s ⇒ **True**）＋ `B8c`
（非待机 + 700s ⇒ 仍 True，证明旧路没被删）。

---

## 2. 第⑧项：静止 → 到快捷栏（任务栏）上沿待着

### 2.1 关键设计：**工作区**，不是整块屏幕
用户原话：「**就算静止不动那也要来屏幕底下那个快捷栏上面待着吧**」。

难点在于既有几何口径全都会把宠物放到**任务栏底下**：

| helper | 取值 | 结果 |
|---|---|---|
| `_virtual_screen_rect()` | `SM_*VIRTUALSCREEN` 整块屏 | 含任务栏那一条 |
| `_desktop_floor_y()` | 虚拟屏底 − 窗口高 | 脚落在**任务栏后面** |
| **`_work_area_rect()`（新增）** | `QDesktopWidget.availableGeometry(self)` | **已扣掉任务栏** |

新增 `_taskbar_top_y()` = 工作区底 − 窗口高；`_lounge_perch_point()` 在此之上取
横向 72% 处，再走 `_clamp_pos_to_desktop()` 夹紧。

⚠️ 本轮**只新增**这两个 helper，**没有改** `_desktop_floor_y()` ——
"地面"（坠落/弹跳落地）与"快捷栏上沿"是两个不同的量，混为一谈会把
坠落落点一起抬高。判据 `A5c` 专门锁这条区分。

### 2.2 行为规则
| 条件 | 行为 |
|---|---|
| 距上次互动 < 600s | 照常自主漫游 |
| 距上次互动 ≥ 600s 且不忙 | **进入待机**：目标点＝窝点、`is_moving=True`、`_lounge_since` 记时 |
| 待机中 | **跳过 `randomize_movement_pattern()`**（否则刚坐下就被赶走） |
| 待机中 + 用户互动（`last_interaction_time` 变了） | 立刻退出 |
| 待机中 + 变忙（睡眠/游戏/物理过程/拖拽/施法/躲猫猫/陪看视频） | 立刻退出 |
| 窝点算不出（几何异常） | **不进入**，也不留半个状态（`B6`） |

"偶尔聊几句"沿用既有唯一通道 `start_autonomous_speech`（`AUTONOMOUS_SPEECH_MIN_INTERVAL=600`），
**没有**新开第二条说话通道。

---

## 3. 第⑨项：23:00 ± 10 分钟回房睡觉

### 3.1 口径拆解（逐句对应）
| 用户原话 | 实现 |
|---|---|
| "晚上11点左右（也就是上下10分钟）" | 触发窗口 = 23:00 ± `BEDTIME_JITTER_MINUTES(10)`；**用日期做种**随机落点（当日稳定、跨日不同） |
| "回自己的房间里" | `self.scene.switch(BEDTIME_HOME_SCENE)`，默认 `'desktop'`（本项目的家） |
| "除非有事（冒险，和我聊天等）" | `_bedtime_busy()`：`game_state.is_playing`／`has_active_conversation`／`_is_user_inputting`／`_ai_inflight`／物理过程／拖拽／施法/躲猫猫/陪看视频 |
| "回去睡觉" | `enter_sleep_mode(bedtime=True)` |

### 3.2 三个必须说清的判定
1. **窗口过后不补睡**：窗口内一直"有事" ⇒ 当晚就不睡了（不做"半夜突然回房"）。
2. **就寝必须能"升级"小憩**：白天 5 分钟无互动就会 `enter_sleep_mode()`
   （`max_sleep_idle_duration=300`），这几乎必然覆盖 23:00。
   ⇒ ① `_bedtime_busy()` **刻意不含 `is_sleeping`**（判据 `A7` 锁这条）；
     ② `_bedtime_tick()` 的调用点排在 `update_movement` 的"睡眠状态处理"**之前**（`A3d`）；
     ③ 已在睡 ⇒ 只置 `_bedtime_sleep=True` 并搬去窝点，**不重放入睡台词**。
3. **早上自动醒只对"就寝睡"生效**（`C8` / 反控制 `C8b`）：否则白天小憩会在
   7 点被叫醒，变成"刚躺下就起来"。

---

## 4. 第⑤项补漏：睡/醒/哼 三组内置台词

用户口径：「**还有把他内置的对话去掉！！！！**」「**聊天系统全权由7B接管，别放内置对话了，太木讷了**」。

| 位置 | 删掉的内置台词 | 改为 |
|---|---|---|
| `enter_sleep_mode` | `zzz... 晚安，做个好梦！` / `zzz... 我困了...` / `zzz... 好舒服...` | `speak_event("sleep_enter", None, "sleepy")` |
| `update_movement` 睡眠分支 | `唔...别吵...` / `嗯...再睡五分钟...` / `zzz...别闹...` | `speak_event("sleep_stir", None, "sleepy")` |
| `wake_up` | `嗯？什么事？` / `哎呀！我睡着了！` / `早上好！` | `speak_event("wake_up", None, "surprised")` |

一律 **`pool=None`**（不给罐头）——AI 不可用就安静地睡/醒，而不是甩一句写死的 zzz。
`event_speech.py` 新登记 3 个 kind（`sleep_enter` / `sleep_stir` / `wake_up`），
全 `TIER_AI`，旁白**只写动作描述、不含台词示范**（防 7B 照抄）。

### 4.1 连带改动：`s7_event_speech` 的 C1 计数锁
C1 是"**迁移面清单**"锁（字面量 19 + 动态 1 = 20）。本轮 +3 处合法迁移 ⇒
字面量 **22**、总数 **23**。**随迁移同步改数字**，并写明"不许放宽成 `>=`"
（放宽后删掉一半迁移点也不报红，锁就废了）。

---

## 5. 测试锁

新增 G2 套件 **`dialog_lounge52`**（`check52c.py`，**82 判据**，exit 0，已进 `HERMETIC_IDS`）。

### 5.1 判据在**判据侧**踩到的三个坑（都是"过窄 ⇒ 假红"）
| # | 症状 | 真因 | 处置 |
|---|---|---|---|
| 1 | `A2b` 取到的表达式是 `False` | `self._idle_loop_active` 在 `init_*` 里还有一次**初值赋值**，`ast.walk` 取"第一个匹配"拿错了 | 限定在 `update_animation` 内取；并写明这个坑 |
| 2 | `A7`/`A8` 报红 | 判据匹配到了**我自己写的注释**（注释里逐字引用了被删的台词、以及"不含 `is_sleeping`"） | `A8` 改用 `ast.Constant` 字符串集合判定；`A7` 用"剥注释+剥 docstring"的视图 |
| 3 | 修完 1、2 后 `A7b`/`A8b` **正控制**反而报红 | 我一开始用了"剥注释**和字符串**"的视图，而 `_idle_lounge_busy` 的属性表本身就是**字符串元组** ⇒ 一起被剥掉 | 同时备两套视图：`_code_only`（剥注释+字符串）与 `_no_comment`（只剥注释），按判据性质选 |

★ 第 3 条是本轮最有价值的教训：**"剥注释"和"剥字符串"必须分开，
用错哪一个都会在某一侧报假红**。

### 5.2 另一个坑：基线不许被"时间戳"污染
`B2` 的 detail 一开始打了 `_lounge_since` 的**原始 epoch 秒** ⇒ 基线每次 DIFF。
按既有纪律（"改基线 = 把环境噪声固化成预期值"）**改判据输出**而不是改基线。

### 5.3 正/负控制清单
`A2c`（合成旧写法必须被判"不含"）、`A5c`（`_desktop_floor_y` 仍走虚拟屏）、
`A7b`（同一判据套到 `_idle_lounge_busy` 必须说"含"）、`B3`（599s 不进）、
`B5b`（关掉"有事"就进）、`B6`（窝点算不出⇒不进且不留半个状态）、
`B8a/B8b/B8c`（真表达式三条）、`C5`、`C6`、`C7b`、`C8b`、`C9b`。

---

## 6. 回归

```
suite             exit  PASS  FAIL  比对
...
dialog_lounge52   0     82    0     IDENTICAL   （新增）
s7_event_speech   0     138   0     IDENTICAL   （C1 计数 20→23）
合计：PASS=2508 FAIL=0  套件=50
```
- 49 → **50** 套件；PASS 2426 → **2508**（+82）。
- 基线重建走 `--only <suite> --update` **合并模式**（`dialog_lounge52` / `s7_event_speech`）。
- 其余 48 个套件**逐条 IDENTICAL**。

---

## 7. 🔴 待用户裁定

1. ★★ **第④项：待机动画换成哪一组？**
   用户原话：「用那个额，我忘了在第几章了，反正是一个**电梯里，主角团都以放松些的姿态坐着**的
   那组动画……**我不知道在不在素材里**，所以你也可以去原作文件里找一下」。
   **结论：不在。** 取证方式：把 `deltarune_ralsei/` 的 **1111 张 PNG 全部按 390 个组**
   枚举，再按 `elev / lift / sit / seat / couch / chair / tea / relax / lounge / rest`
   十个关键词逐个扫。结果：
   - `elev*` / `seat*` / `chair*` / `relax*` / `lounge*` / `rest*` → **0 命中**；
   - `spr_ralsei_lift_*`（19 张）**不是坐着** —— 看图确认是 **Susie 把 Ralsei 举起来**
     （`spr_ralsei_susie_lift_head_*` 里能直接看见 Susie 抱着 Ralsei），站姿；
   - "坐着"的真实候选只有：**茶会坐姿**（`spr_tea_party_ralsei_sit_*`，6 种姿态）、
     **暗教堂坐姿**（`darkchurch_sit_happy/sad`）、**赛车坐姿**（`racing_sit`）、
     **沙发**（`couch_*` 6 张）、**跪坐**（`kneel_*`）。
   - 原作房间里的确**有电梯**（`room_cc_elevator` / `room_cc_prisonelevator` /
     `room_dw_mansion_elevator` 都在 `_index.json` 里），但**没有配套的"坐着"精灵**。
   ⇒ 已出对照图 `%TEMP%\idle_candidates_52.png`（9 行、每行带中文标注），请指认要哪一组。
   **我的建议**：`spr_tea_party_ralsei_sit_*` 的 6 种姿态本来就是"坐着 + 换姿势"，
   天然适合做**待机循环**（不会像单帧那样又变成"贴图"），最接近用户描述的观感。

2. **"他的房间"要不要换成城堡房间？**
   现在 `BEDTIME_HOME_SCENE = 'desktop'`（本项目的家＝桌面）。
   素材里确实有 `ralsei_room` 区域（「拉尔赛的城堡」，`ch2/3/4/5` 各有
   `dw_ralsei_castle_front / _1f / _2f / _3f`）。**这是内容决定，不擅自替用户选**。

3. **第⑤项剩余**：`main.py` 的娱乐建议 8 条 / 笑话 6 条 / 推荐游戏 5 条 /
   `entertainment_system` 成就祝贺 5 条 —— 我倾向**保留**（带成就名/规则，属功能播报），
   待用户拍板。

4. **`file_sheet_controller`（Excel 处理）是否整块去掉**（用户说"鸡肋"，但它是独立功能）。

---

## 8. 落地清单

| 文件 | 改动 |
|---|---|
| `ralsei_pet/src/main.py` | 待机状态机（常量/字段/`_work_area_rect`/`_taskbar_top_y`/`_lounge_perch_point`/`_idle_lounge_busy`/`_enter·_exit·_idle_lounge_tick`）+ 就寝（`BEDTIME_*`/`_bedtime_target_time`/`_bedtime_busy`/`go_to_bed`/`_bedtime_tick`）+ 两处 hook + `_idle_loop_active` 表达式 + 睡/醒/哼三处迁 `speak_event` |
| `ralsei_pet/modules/event_speech.py` | 登记 `sleep_enter` / `sleep_stir` / `wake_up`（TIER_AI + 旁白） |
| `code-quality-audit/第52轮-对话与人味收口/_tools/check52c.py` | 新增（82 判据） |
| `code-quality-audit/regress/run_all.py` | 注册 `dialog_lounge52` + 加进 `HERMETIC_IDS` |
| `code-quality-audit/regress/baseline.json` | 重建 `dialog_lounge52`、`s7_event_speech`（合并模式） |
| `code-quality-audit/人味改造-2026-09-18/verify_s7_event_speech.py` | C1 计数 20→23（含"不许放宽成 >="的说明） |
