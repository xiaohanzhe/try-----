# 代码质量复审报告（第五轮）

- 审查日期：2026-09-13
- 审查对象：`ralsei_pet`（Ralsei 桌面宠物，Windows + Python 3.11 + PyQt5 5.15 + pywin32）
- 审查基线：工作区当前状态（`HEAD = 036fc6b` + 12 个文件未提交改动，其中 `main.py` 约 511 处）
- 审查人角色：资深软件测试工程师 / 软件开发工程师
- 审查范围：隐藏 bug、边界条件、重复编码、未来扩展陷阱、异常处理完整性，以及**代码与实际负责功能是否相符**
- 结论口径：**以用户视角为准**，只把用户能感知到的行为算作问题；纯装饰性债务单独归类

---

## 一、结论摘要

本轮采用「静态基线 → 7 组并行只读深审 → **字节码级/功能级独立验证** → 定点修复 → 回归验证」的流程，
**独立发现并修复了 1 个此前四轮全部漏掉的 P0 级功能致命缺陷**，以及 3 个 P1、3 个 P2。

| 级别 | 数量 | 概述 |
|---|---|---|
| **P0** | 1 | `_tick_spell_flow` 引用未定义名 `current_time` → 施法流程必然抛异常，**文件/文件夹永远打不开、躲猫猫游戏永久卡死、宠物进入无法自恢复的僵直状态** |
| **P1** | 3 | 对话上下文劫持（2 处机制）使真实桌面操作分支不可达；主链关键词抢占产生不可达死分支；"游戏大师"成就名实不符 |
| **P2** | 3 | Excel COM 失败残留僵尸进程；多显示器副屏（负坐标）窗口被误杀；`resize_window_smoothly` 阻塞主线程 |
| 已复核为误报/已防御 | 4 | 上一轮报告与代理疑点中的 4 条经复核不成立，见第四节 |
| 未修复遗留（不阻塞） | 见第五节 | 185 个零引用方法、`memory_system` 落盘键缺失、成就条件恒真、`randint` 边界、`.bak` 副本等 |

**用户视角结论**：修复后，施法/打开文件/躲猫猫三条核心交互链路恢复可用；对话中"内存/cpu/优化/清理/备份"
等指令会真正执行而非回套话；驱动型能力（如单实例、原子写、回收站保护）经复核均有效。残余项均为装饰性债务或
需产品决策的语义问题，不构成用户可见故障。

---

## 二、审查方法（本轮实际执行，全部可复现）

| # | 步骤 | 工具/产物 | 结果 |
|---|---|---|---|
| 1 | 全量编译（Python 3.11 真实环境 + 3.13） | `compileall` | ✅ exit 0 |
| 2 | 静态缺陷扫描（未定义名 / 未用导入 / 未用局部变量 / 无占位 f-string） | `pyflakes 3.4.0` | **抓出 3 处 `undefined name 'current_time'`** |
| 3 | 零引用符号穷举（去注释/字符串后统计标识符引用面） | `scan_deadrefs.py` | 33 文件 / **775 个 def，其中 188 个零引用** |
| 4 | 假阳性过滤（剔除 Qt 事件回调等框架调用） | `filter_deadrefs.py` | 185 项可疑死代码 + 35 组同名重复定义 |
| 5 | 7 组并行**只读**深审（不改码，避免并发写冲突） | 5 个子代理覆盖持久化/网络、Win32 与文件安全、对话情绪、AI 养成、main 状态机与 UI | 46 条原始发现 |
| 6 | **字节码级验证**：证明 `current_time` 是 `LOAD_GLOBAL` 且模块顶层从未绑定 | `probe_spell_namerror.py` | ✅ 已证实必抛 `NameError` |
| 7 | **功能级回归验证**：16 项断言（对话真实动作触达 / 施法回调 / 成就去重） | `verify_round5_fixes.py` | ✅ **16/16 通过** |
| 8 | 全模块导入冒烟 + 不变量检查 | `smoke_import_round5.py` | ✅ 22/22 模块导入成功，0 失败 |

> 审查过程遵循「只读优先、验证后再改」：所有代码修改均在**独立验证确认**之后进行，改完重新跑第 1/7/8 步。

---

## 三、本轮新发现与修复

### 3.1 【P0 · 已修复 · 前四轮全部漏检】`_tick_spell_flow` 引用未定义名 `current_time`

**位置**：`ralsei_pet/src/main.py` → `_tick_spell_flow()`（修复前为第 8120、8132 行）

**现象（用户可感知）**：
- 双击/说话让 Ralsei **用魔法杖打开文件或文件夹**：他会走过去、开始施法，然后**施法动画永久定格，文件永远不打开**；
- **躲猫猫游戏**：走到屏幕中央后说要"变出 5 个小盒子"，盒子**永远不会出现**，游戏既无法继续也无法结束；
- 之后宠物**不再走动、不再发呆、不再玩耍**（看起来"卡死了"），只有手动拖它/点它/让它跳一下才能恢复；
- 有控制台时每 100ms 刷出一段堆栈。

**根因（字节码级证据）**：

```python
# main.py  修复前的 casting 分支
now = time.time()                     # ← 第 8054 行，函数内定义的名字是 now
...
elif self._spell_stage == 'casting':
    if ... or self._spell_cast_start_time is None:
        self._spell_cast_start_time = current_time          # ← 8120 行：无此局部变量
    ...
    cond_time_fallback = (current_time - getattr(self, '_spell_cast_start_time', current_time)) > 5.0   # ← 8132 行
```

反汇编证据（`probe_spell_namerror.py`）：

```
LOAD_GLOBAL current_time      ← 以全局名解析，而非 self.current_time / 局部变量
模块顶层是否绑定过 current_time : False
>>> 已证实：运行到该行必然抛 NameError。
```

即：**该方法内没有 `current_time` 这个局部变量，模块顶层也从未定义同名全局**（第 773/815 行的
`self.current_time` 是**属性**，与裸名 `current_time` 无关）。`cond_time_fallback` 这一行位于 casting
分支中**无条件执行**，因此**每一次施法、每一个 tick 都必然抛 `NameError`**。

**故障链（完整闭链）**：

1. `_cast_spell_then()`（躲猫猫）或 `_start_open_with_spell()`（打开文件）置 `_spell_stage='casting'`；
2. `update_animation`（100ms 定时器）末尾 `try: self._tick_spell_flow() except: traceback.print_exc()` —— 异常被吞，**`_spell_stage` 保持 'casting'**；
3. 第 8142 行起的清理与 `_spell_finish_cb()` 调用**永不执行** → `open_file()` 内定义的 `_real_open_file` 永不触发 → **文件不打开**；
4. `_hide_create_obstacles_after_spell()` 永不执行 → 5 个障碍物文件夹永不创建，`_hide_stage` 永久停在 `'creating'`；
   而 `start_hide_and_seek_game()` 开头有 `if _hide_stage is not None: return False` → **游戏再也开不起来**；
5. `_cast_spell_then` 已调用 `autonomous_agent.suspend()`，而恢复逻辑在未被执行的收尾块里 → **自主代理永久挂起**；
6. `pet_ai.trigger_action()` 首行判断 `if _spell_stage is not None: return` → **宠物所有自主动作被冻结**；
7. 唯一脱困路径：用户拖拽 / 触摸 / 触发跳落 → 下一 tick 走 `_spell_interrupted_reason()` 的中断分支清理。

> 补充说明：`open_file()` 外层的 `try/except` **兜不住**这个异常——它只覆盖 `_start_open_with_spell()` 的同步调用，
> 而 `NameError` 发生在之后的定时器 tick 里。

**为何前四轮漏检**：第三、四轮报告均把 `_tick_spell_flow` / 躲猫猫全链标记为"精读、防护完整"，
但该缺陷是一个**跨函数的拼写/作用域错误**，肉眼看注释与结构都"很完整"，只有静态分析（未定义名）
或运行时才能暴露。本轮正是靠 `pyflakes` 兜住。

**修复**（`main.py`，`now` 是本函数第 8054 行已定义的 `time.time()`，语义完全等价）：

```python
if not hasattr(self, '_spell_cast_start_time') or self._spell_cast_start_time is None:
    self._spell_cast_start_time = now                     # 原为 current_time
cond_time_fallback = (now - getattr(self, '_spell_cast_start_time', now)) > 5.0   # 原为 current_time
```

**验证（`verify_round5_fixes.py`，探针逐帧驱动真实方法体 11 帧）**：

| 断言 | 结果 |
|---|---|
| 反汇编中已无 `LOAD_GLOBAL current_time` | ✅ offsets=[] |
| casting tick 不再抛 `NameError` | ✅ err=None |
| 施法完成回调被真正调用（文件才会被打开） | ✅ `cb_called=True`，`frames_seen=11` |
| `_spell_stage` 已复位（不再永久卡在 casting） | ✅ `stage=None` |

---

### 3.2 【P1 · 已修复】对话上下文劫持：真实桌面操作分支不可达（两处机制）

**位置**：`ralsei_pet/modules/dialogue_system.py` → `generate_response()` / `_get_context_response()`

**机制一：本轮输入被当成"上下文"读回**

原顺序是先 `update_context(user_input)` 再 `_get_context_response(...)`。而 `update_context` →
`_analyze_user_input` 会把**当前输入**的意图写进 `context["last_intent"]`。于是紧接着的
`_get_context_response` 读到的"上下文"其实**就是本轮输入**，命中
`last_intent == "system_status"` 后返回一句套话并**提前 `return`**。

后果：`generate_response` 主链中真正调用
`desktop_interaction.get_system_resources() / optimize_system() / clean_temp_files() / backup_data()`
的分支**永远不可达**。用户问"内存多少 / 帮我优化 / 清理垃圾 / 备份数据"只会收到一句闲聊，**动作一次都不执行**。

**机制二：`last_topic` 用"领域词"判定承接，把指令当续聊**

`_is_followup_input()` 只要输入**含有**该话题的领域词就判定为"承接"。上一轮聊过 `system_status`
（领域词含"系统"）后，用户再说"帮我优化一下系统" → 被判为承接 → 返回模板并提前 return，同样阻断动作分支。

**修复**（两处，最小改动）：

```python
# 1) 顺序修正：上下文检查必须早于 update_context（上下文 = 上一轮）
context_response = self._get_context_response(user_input_lower, current_emotion, emotion_intensity)
if context_response:
    return context_response
self.update_context(user_input)          # 记录本轮输入，供下一轮承接

# 2) 意图闸门：只有"承接式"输入（general_chat：嗯嗯/好的/还有呢/继续）才用上下文模板，
#    携带明确意图的输入一律交给主链执行真实动作
if self._detect_intent(user_input_lower) != "general_chat":
    return None
```

**验证**：

| 输入 | 修复前 | 修复后 |
|---|---|---|
| "内存使用率是多少呢" | 套话 | ✅ 调用 `get_system_resources()`，回复含真实数值 |
| "帮我优化一下系统吧" | 套话 | ✅ 调用 `optimize_system()` |
| "清理一下垃圾文件" | 套话 | ✅ 调用 `clean_temp_files()` |
| "帮我备份数据" | 套话 | ✅ 调用 `backup_data()` |
| "还有呢"（承接语） | 上下文回复 | ✅ 仍为上下文回复（**无回归**） |

---

### 3.3 【P1 · 已修复】主链关键词抢占 → 产生不可达死分支

**位置**：`dialogue_system.py` → `generate_response()` 的 if/elif 链

在 `generate_response` 的顺序匹配链中，**宽泛名词分支排在具体动作分支之前**，造成两类问题：

| 现象 | 抢占者 | 被遮蔽者 |
|---|---|---|
| "帮我优化一下**系统**" → 去查系统资源，不执行优化 | 第 1094 行 `"系统" in ...` | 第 1120 行 `elif "优化"` |
| "清理一下垃圾**文件**" → 回"要打开桌面上的文件吗？" | 第 973 行 `"文件"/"文档"` | 第 1127 行 `elif "清理"` |

更严重的是：第 1075 行 `elif "整理" in ... or "清理" in ...`（纯寒暄分支）**排在前面**，
使第 1127 行的"清理"**动作分支彻底不可达**——这是一条真正的**死分支**，且属于典型的重复编码
（"清理"在两处出现，语义相反：一处只说话，一处真干活）。

**修复**：把"优化 / 清理 / 备份 / 恢复"四个动作分支**整体前移到 `"文件"` 分支之前**，实现"动词优先于名词"；
并删掉第 1075 行重复的 `"清理"`（只保留 `"整理"`），消除死分支。

**验证**：`优化分支位置 34491 < 文件分支位置 35534 → 动作优先: True`；且 V1c/V1d 由 FAIL 转为 PASS。

---

### 3.4 【P1 · 已修复】"游戏大师"成就与描述不符

**位置**：`ralsei_pet/modules/entertainment_system.py` → `_check_game_achievements()`

```python
# 修复前（原注释自承"这里简化处理，实际需要检查所有游戏是否都玩过"）
if self.activity_stats['games_played'] >= len(self.games) and ...:
```

`games_played` 是**累计游玩次数**，因此**重复玩同一个游戏凑够次数即可解锁"游戏大师"**，
与成就名称/描述（须玩遍所有游戏）严重不符。这是"代码与实际负责功能不符"的典型样本。

**修复**：新增去重集合 `activity_stats['games_played_ids']`（含旧存档 `setdefault` 兼容），
判定改为 `len(played_ids) >= len(self.games)`。

---

### 3.5 【P2 · 已修复】新建 Excel 表格缺 `finally`，失败残留僵尸 `EXCEL.EXE`

**位置**：`main.py` → `handle_file_operation()` 的"新建表格"分支

原实现 `workbook.SaveAs(...)` → `workbook.Close()` → `excel.Quit()` 之间**没有 `finally`**。
当 `SaveAs` 失败（目标被 Excel 占用 / 桌面无写权限 / 磁盘满 / 文件名非法）时异常直接抛出，
`excel.Quit()` 永不执行 → 留下**隐藏的 EXCEL.EXE 僵尸进程**并可能锁住文件。

同文件的 `fix_excel_format()` / `fill_names_in_excel()` **已经**用 `finally` 处理了同类问题，
此处遗漏属于**一致性缺口**。已补齐 `try/finally` + 双层防御性释放。

---

### 3.6 【P2 · 已修复】多显示器：副屏（负坐标）窗口被误杀

**位置**：`ralsei_pet/modules/desktop_interaction.py` → `get_all_visible_windows()`

```python
screen_width  = GetSystemMetrics(78)   # SM_CXVIRTUALSCREEN —— 只给"尺寸"
screen_height = GetSystemMetrics(79)
...
if (rect[0] > screen_width or rect[1] > screen_height or rect[2] < 0 or rect[3] < 0):
    return True                        # 丢弃
```

`GetWindowRect` 返回的是**虚拟屏幕绝对坐标**，而上面两个 API 只给**尺寸**。当副屏位于主屏**左侧或上方**时，
副屏上窗口的坐标含负值，其右/下边界 `rect[2]`/`rect[3]` 即为负数 → **整窗被丢弃**。
后果：宠物无法在这些窗口边框上"建楼"、`nearby` 感知缺失。

而且**上方注释明确声称**"用虚拟屏幕尺寸（多显示器）替代主屏尺寸，**避免副屏窗口被误过滤**"——
声称的能力与实现不符；同项目 `floor_manager.py` 已正确使用 `SM_X/YVIRTUALSCREEN`(76/77)，
两处实现不一致。

**修复**：补取虚拟屏原点，改用「窗口矩形与虚拟屏矩形是否相交」判定：

```python
virtual_left, virtual_top = GetSystemMetrics(76), GetSystemMetrics(77)
virtual_right, virtual_bottom = virtual_left + screen_width, virtual_top + screen_height
...
if (rect[2] <= virtual_left or rect[0] >= virtual_right or
        rect[3] <= virtual_top or rect[1] >= virtual_bottom):
    return True
```

> **备注（追责用）**：本项在**第二轮审查**（见 `.workbuddy/memory/2026-09-12.md`）中已被明确指出
> （"`desktop_interaction.py:1006` 拿 78/79 当边界未减原点 76/77 → 左侧/上方副屏窗口被 `rect[2]<0`
> 全部误过滤"），但一直未修复；第三、四轮亦未复查。本轮终于落地。同批被指出但仍未修的相关项还有
> `main.py` 内 20+ 处 `availableGeometry()`（仅主屏）与 `_virtual_screen_rect()`（多屏）两套坐标体系混用，
> 见第五节。

---

### 3.7 【P2 · 已修复】`resize_window_smoothly` 阻塞主线程

**位置**：`desktop_interaction.py` → `resize_window_smoothly()`

循环 50 步、每步 `time.sleep()`，**全程不处理事件**，`duration`（默认 1.0s）内 GUI 完全冻结；
而同文件 `move_window_smoothly()` 每步都调用了 `QApplication.processEvents()` —— 两处做法自相矛盾。
已补齐 `processEvents()`，保持与同文件一致。

---

## 四、复核为「误报 / 已防御」的项（说明为何不动）

为避免把猜测当结论，以下项经**亲自读码复核**判定不成立，特此记录：

| 来源 | 疑点 | 复核结论 |
|---|---|---|
| 子代理 | `memory_system.learn_new_skill` 的 `random.choice(new_skills)` 空列表 `IndexError` | **误报**：该语句位于 `if new_skills:` 守卫之内（第 496-505 行），空列表不会进入 |
| 子代理 | `dialogue_ui._on_ai_reply` 在工作线程直接操作 QWidget，会随机崩溃 | **已防御**：`main.py` 工作线程只 `emit` 一个 `pyqtSignal`，Qt 自动排队回主线程执行，非跨线程访问 |
| 子代理/上轮 | `reset_special_states` 漏复位 `is_happy`/`is_splat` | **影响为零**：该函数**全项目零调用**（属死代码），不构成运行期问题；已并入第五节死代码清单 |
| 子代理 | Atom 写（`os.replace`）跨盘非原子 | **已防御**：临时文件与目标同目录，`os.replace` 为原子替换 |
| 子代理 | 桌面删除/回收站失败会静默 `os.remove` 永久删除 | **已防御**：现实现回收站失败即保留原文件，无 `winshell` 缺失时降级永久删除的回退 |

---

## 五、未修复的遗留问题（不阻塞，按影响排序）

> 以下均**不影响当前用户主路径**，故未改动，仅给建议。完整清单见证据文件。

### 5.1 零引用代码规模

穷举结果：**775 个函数/方法中 188 个全项目零引用**（剔除 Qt 事件回调等框架调用后仍有 **185 个**）。
其中**属于应用自身编排逻辑、值得优先清理**的：

| 类别 | 示例（`main.py`） | 影响 |
|---|---|---|
| 情绪触发封装 | `trigger_surprise` / `trigger_shy` / `trigger_unhappy` / `trigger_victory` / `trigger_teasplash` | 5 个情绪入口全部未接线（动画本身仍可由动画名播放，故非用户可见故障） |
| 物理/楼层 | `_is_falling_through_window`、`update_floor` | "坠落穿透窗口"逻辑与楼层更新逻辑从未生效；`update_floor` 与 `check_window_movement` 重复且**已漂移**（第 2390 行注释亦自承"全项目无任何调用点"） |
| 视频/游戏反应 | `check_video_windows` / `check_game_windows` / `react_to_game` / `watch_video` / `react_to_video_content` | 整个"对游戏/视频窗口做反应"集群**完全未接线** |
| 文件情绪 | `react_to_file_emotionally` / `follow_file` / `react_to_file_deletion` | 未接线 |
| 其他 | `get_element_at_pos`、`check_desktop_element_at_target`、`start_mouse_drag`、`create_person_name_table`、`check_dragged_file`、`check_recycle_bin`、`move_to_edge_and_shrink`、`suggest_game`、`init_api_client`、`_async_api_request`、`on_animation_complete` | 未接线（`on_animation_complete` 本体就是 `pass`） |
| 模块层 | `pet_interaction.py` 整个模块（`handle_press/release/move`、`set_drag_active` 等）、`desktop_interaction` 的 `create_folder`/`copy_file`/`cut_file`/`drag_file`/`double_click_file`/`right_click_file`/`scheduled_task` 系列 | 模块整体未接线 |

**建议**：按"删除或显式标注 `# legacy, unused`"二选一处理，避免下一位维护者误以为功能已生效；
`update_floor` 这类**已有漂移副本**的建议直接删除。

### 5.2 `memory_system.save_memory` 落盘键缺失（潜伏）

`save_memory()` 实际只落盘 `long_term_memory / experience / level` 三个键；
`self.memory_strength`（由 `update_memory_strength()` 修改，并在第 558 行紧接着调用 `save_memory()`）
**从不写入文件** → 重启后记忆强度静默归零，且第 558 行那次 `save_memory()` 是**无效写入**，与函数语义不符。
`learning_rate` 同理。**建议**：把这两个字段纳入载荷并做旧存档兼容。

### 5.3 其他潜伏项（无生产调用方，接线前须处理）

- `memory_system.query_memory`：`time_range` 含 `None` 时 `None <= timestamp` → `TypeError`；`memory['timestamp']` 直接索引 → 缺键即 `KeyError`。
- `memory_system.get_associated_memories`：`memory_id.lower()` 对非字符串入参 → `AttributeError`。
- `social_growth_system`：`'first_touch': lambda s: s['level'] >= 1` **恒为真** → "初次触摸"无需触摸即自动解锁，名实不符。
  更根本的是 `_check_achievements` 里 30 项成就**全部用等级近似**，其注释承诺的"由对应事件调用 `unlock`"
  **在全工程不存在**；第 318 行的 `interaction_types` 表（含 `'touch'` 等互动定义）**从未被使用**。建议要么补上事件式解锁，要么修正成就描述与条件。

### 5.4 边界与装饰性

- `main.py` 第 2914-2915 行 `random.randint(100, screen_geom.width() - 200)` 未加下限保护：屏幕宽/高 < 300px 时 `a > b` → `ValueError`。（第四轮报告已提出，本轮**仍未修**；`pet_ai.py` 同类表达式已用 `max(51, ...)` 保护，属同类不一致。）
- `main.py` `check_recycle_bin` 使用硬编码 `QPoint(100, 100)  # 模拟位置` 计算"接近回收站"——该分支**从未被调用**；若将来接线，宠物在屏幕左上角会被误判为"靠近回收站"而恐慌。
- `pet_ai.check_action_triggers`（约 85 行）为 `laugh/dance/sing/pose/tea` 等设计了大段情绪与状态匹配逻辑，但 `trigger_action` 的白名单（第 519-526 行）只放行 `idle/walk_*/run_*`——因此 `state == "interact"`／`"play"` 时**任何动作都无法触发**，该段逻辑实质失效（`energy`/`hunger` 算出后也未使用）。
- 源码树内散落历史副本：`src/main.py.bak`、`src/main.py.backup_before_fix`、`src/main.py.backup_before_deadcode_cleanup`、`modules/desktop_interaction.py.bak`（后者确认**不会**被导入/误加载，但易误提交）。
- `pyflakes` 装饰性问题：若干未用导入、无占位 `f-string`、未用局部变量（`sheet`、`edge_threshold`、`texture`、`scale_x/scale_y`、`file_type`、`file_ext`、`current_hour` 等）；`desktop_interaction.py` 模块级 `shutil` 被 6 处函数级 import 遮蔽。
- `desktop_interaction.resize_window_smoothly` 的 `scale_x`/`scale_y` 计算后未使用（死计算）。

### 5.5 建议一并确认

- `src/__pycache__/main.cpython-311.pyc` 仍被 git 跟踪（第三轮曾清理过被跟踪的 `.pyc`）。建议 `git rm --cached` 并确认 `.gitignore` 生效。

---

## 六、专项：代码与实际负责功能是否相符

这是本轮被明确要求核查的维度。以下是**"声称"与"实际"不一致**的完整清单：

| # | 位置 | 声称（注释/文档/命名/产物） | 实际 | 处理 |
|---|---|---|---|---|
| 1 | `main.py::_tick_spell_flow` | 注释完整描述"数 11 帧 → 触发回调 → 切回 idle"，第三/四轮报告标注"防护完整" | 引用未定义名 `current_time`，永远抛异常，回调从不触发 | **已修（P0）** |
| 2 | `dialogue_system` 系统 / 优化 / 清理 / 备份分支 | 代码里写着调用 `get_system_resources()` 等真实操作 | 被上下文模板与宽泛名词分支遮蔽，**不可达** | **已修（P1）** |
| 3 | `entertainment_system` "游戏大师" | 成就名/描述 = 玩遍所有游戏 | 实际只看累计次数，重复玩同一游戏即解锁（原注释自承"简化处理"） | **已修（P1）** |
| 4 | `desktop_interaction::get_all_visible_windows` | 注释："用虚拟屏幕尺寸…避免副屏窗口被误过滤" | 用尺寸比对绝对坐标，副屏负坐标窗口被丢弃 | **已修（P2）** |
| 5 | `desktop_interaction::resize_window_smoothly` | 与 `move_window_smoothly` 同属"平滑动画" | 前者阻塞主线程、后者不阻塞，同一语义两种实现 | **已修（P2）** |
| 6 | `main.py::handle_file_operation` 新建表格 | 与同文件另两处 Excel 操作应同为"安全释放 COM" | 独缺 `finally`，失败即泄漏僵尸进程 | **已修（P2）** |
| 7 | `social_growth_system` 成就系统 | 注释："首次触发即解锁（由对应事件调用 `unlock`，这里做兜底检查）" | **不存在**任何事件式调用；`first_touch` 条件恒真；`interaction_types` 表从未使用 | 已报告（5.3） |
| 8 | `main.py::check_recycle_bin` | "检查是否接近回收站" | 硬编码 `QPoint(100,100)` 模拟位置，且**全项目零调用** → 该功能完全未生效 | 已报告（5.4） |
| 9 | `main.py::update_floor` | 函数名/注释表明负责楼层更新 | 零调用；逻辑已迁至 `check_window_movement`，本函数成为漂移副本 | 已报告（5.1） |
| 10 | `main.py::_is_falling_through_window` | 命名表明实现"坠落穿透窗口"判定 | 零调用 → 该物理能力从未生效 | 已报告（5.1） |
| 11 | `pet_ai::check_action_triggers` | 85 行为各类表演动作设计情绪/状态匹配 | 被 `trigger_action` 白名单拦截，interact/play 状态永不触发 | 已报告（5.4） |
| 12 | `memory_system::save_memory` | 注释"保存记忆" | 丢弃 `memory_strength`/`learning_rate`，且调用方以为已持久化 | 已报告（5.2） |
| 13 | `main.py` 视频/游戏/文件情绪反应群 | 方法名表明具备"对游戏/视频/文件做反应" | 整群零调用，从未生效 | 已报告（5.1） |
| 14 | `README.md` 项目结构 | 只列 8 个模块 | 实际 22 个模块（AI 对话、情绪、养成、自定义、娱乐、自主代理等均未记录） | 建议更新 |

---

## 七、验证矩阵（本轮全部）

| 验证项 | 命令/脚本 | 结果 |
|---|---|---|
| 全量编译（Python 3.11 + 3.13） | `compileall` | ✅ exit 0 |
| 静态缺陷扫描 | `pyflakes` | ✅ 修复后**无 `undefined name`**，仅剩装饰性项 |
| 零引用符号穷举 | `scan_deadrefs.py` | ✅ 775 def / 188 零引用 |
| 字节码级证据（`LOAD_GLOBAL current_time`） | `probe_spell_namerror.py` | ✅ 已证实（修复前） |
| 功能回归（对话真实动作 / 施法回调 / 成就去重） | `verify_round5_fixes.py` | ✅ **16/16 通过** |
| 全模块导入冒烟 | `smoke_import_round5.py` | ✅ 22/22 导入成功，0 失败 |
| 不变量：`_tick_spell_flow` 无裸 `current_time` | `smoke_import_round5.py` | ✅ 0 处 |
| 不变量：动作分支优先于名词分支 | `smoke_import_round5.py` | ✅ True |
| 编译产物零污染 | `git diff --numstat` | ✅ 已还原 `main.cpython-311.pyc` |

---

## 八、本轮改动文件清单

| 文件 | 改动 |
|---|---|
| `ralsei_pet/src/main.py` | ① `_tick_spell_flow` 的 `current_time` → `now`（**P0**）② 新建表格分支补 `try/finally` 释放 Excel COM |
| `ralsei_pet/modules/dialogue_system.py` | ① `update_context` 后移到上下文检查之后 ② `_get_context_response` 顶部新增意图闸门 ③ 优化/清理/备份/恢复四个动作分支前移到名词分支之前 ④ 删除重复的"清理"死分支 |
| `ralsei_pet/modules/desktop_interaction.py` | ① 窗口过滤改用虚拟屏矩形相交判定（补 76/77 原点）② `resize_window_smoothly` 循环内补 `processEvents()` |
| `ralsei_pet/modules/entertainment_system.py` | ① 新增 `activity_stats['games_played_ids']`（含旧存档兼容）② 游戏结束记录去重 id ③ "游戏大师"判定改为玩遍所有游戏 |

**审查证据与脚本**：`code-quality-audit/第五轮/`
（`scan_deadrefs.py`、`filter_deadrefs.py`、`probe_spell_namerror.py`、`verify_round5_fixes.py`、
`smoke_import_round5.py`、`_evidence/` 下的原始输出与 7 份子报告 `01`~`06_*.md`）

---

## 九、给下一轮的建议优先级

1. **P1**：`social_growth_system` 成就体系——补事件式解锁或修正 30 项成就的条件与描述（当前"等级近似"让多数成就语义失真）。
2. **P2**：`memory_system.save_memory` 落盘键补全（含旧存档迁移）。
3. **P2**：把 185 个零引用方法做一次"删除 / 标注"清理，重点先删 `update_floor`（已有漂移副本）与整块未接线的视频/游戏反应群。
4. **P3**：统一 `randint` 下限保护写法为 `max(low, ...)`；清理 `.bak` 副本与被跟踪的 `.pyc`。
5. **P3**：把 `main.py`（约 8.7 千行单类）按"移动物理 / 桌面交互 / 游戏 / 施法"拆分，否则后续每一轮审查的边际成本都在上升。

---

*审查过程说明：全程只读优先；运行时验证均使用 `%TEMP%`/探针桩，未移动或删除任何用户数据文件；
代码修改仅限上表 4 个文件，且每处修改均有对应的独立验证断言。*
