# 代码质量复审报告 · 第七轮 — 文档化启动路径崩溃修复

- 日期：2026-09-14
- 范围：`ralsei_pet/` 启动链路（`src/main.py` + `modules/*` 导入契约）
- 结论：**发现并修复 1 个"按 README 方式启动即崩"的确定性缺陷（R7-1）**；
  已把该缺陷转成常驻回归套件（`round7_launch`），堵住 G2 的一个检测盲区。
- 回归：7 套件 / **158 PASS / 0 FAIL**，全部 `IDENTICAL`（见 §6）。

---

## 1. 结论先行

| 编号 | 缺陷 | 严重度 | 触发条件 | 状态 |
|---|---|---|---|---|
| R7-1 | 按 README 记载的 `cd src && python main.py` 启动，在 import 期直接 `ModuleNotFoundError` 崩溃，**窗口根本不出现** | 高（"能跑起来"这条底线被打破，且只在真实启动路径暴露） | 每次都触发 | 已修复并加回归 |

一句话根因：`modules/desktop_interaction.py` 顶层有一句**未加保护**的
`from logger_utils import get_logger`，而 `logger_utils.py` 本身就在 `modules/` 里；
`src/main.py` 却只把**项目根**（`ralsei_pet/`）放进了 `sys.path`，**没放 `modules/`**。

---

## 2. 缺陷现象（可复现）

按项目 README「## 安装和运行 / 完整版本」的方式启动：

```text
cd ralsei_pet\src
python main.py
```

得到（真实输出，见 `_recon/fix_before_utf8.txt`）：

```text
Traceback (most recent call last):
  File "...\ralsei_pet\src\main.py", line 98, in <module>
    from modules.desktop_interaction import DesktopInteraction
  File "...\ralsei_pet\modules\desktop_interaction.py", line 19, in <module>
    from logger_utils import get_logger
ModuleNotFoundError: No module named 'logger_utils'
```

退出码 1，无任何窗口创建，宠物无法启动。

---

## 3. 根因分析

### 3.1 两条并存的导入风格

`src/main.py` 第 87-93 行：

```python
project_root = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
sys.path.insert(0, project_root)          # 只加了 ralsei_pet/
...
from modules.sprite_loader import SpriteLoader      # 包内导入（需要 ralsei_pet/ 在 path）
```

于是 `modules` 被当作**包**导入；而 `modules/*.py` 内部引用同目录的兄弟模块时，
用的是**扁平导入**（`from logger_utils import get_logger`）——这要求
`ralsei_pet/modules/` **自己**也在 `sys.path` 上。

### 3.2 只有一处漏了保护

`modules/` 下共 21 处 `from logger_utils import get_logger`。
其中 **20 处**都包在 `try/except ImportError` 里降级到 `logging`，
**唯独 `desktop_interaction.py:19` 是裸的**（且正好是 `main.py` 第 98 行要导入的那个模块）。
即：全项目只有这一个"裸导入"，恰好落在启动必经之路上。

### 3.3 为什么历轮回归完全没发现（关键盲区）

G2 的每一个套件都在导入 `main` 之前**同时**预置了 `src/` 与 `modules/`
（见 `code-quality-audit/第六轮/verify_round6_fixes.py` 第 20-22 行）：

```python
sys.path.insert(0, os.path.join(BASE, "src"))
sys.path.insert(0, os.path.join(BASE, "modules"))     # ← 恰好掩盖了这个差异
```

也就是说：**回归脚本自动补上了 `modules/`，而真实启动路径里它并不在**。
四条 verify + 两条 smoke 全部通过，却对"真实启动会不会崩"毫无覆盖。
这正是本项目反复要抓的那类"测试与真实路径不一致"的陷阱。

---

## 4. 修复

### 4.1 `modules/desktop_interaction.py`（第 19 行起）

```python
try:
    from logger_utils import get_logger
except ImportError:  # 与其余 modules/*.py 保持一致：未预置 sys.path 时降级
    import logging

    def get_logger(name):
        return logging.getLogger(name)

log = get_logger("desktop_interaction")
```

理由：与另外 20 个模块的既有写法**完全一致**，零新概念，最小改动面。

### 4.2 `src/main.py`（第 89 行后）

```python
# 修复：把 modules/ 也放进 sys.path。modules/*.py 内部用的是"扁平导入"
# （如 `from logger_utils import get_logger`），只有 modules/ 本身在 sys.path 上
# 才能命中真实模块。此前按 README 记载的 `cd src && python main.py` 启动，会在
# import 期直接 ModuleNotFoundError 崩掉；而回归脚本恰好预置了 modules/，
# 于是掩盖了这一类问题（已纳入第七轮）。
# 用 append 而非 insert：让标准库始终优先，仅当名字无法解析时才落到 modules/。
_modules_dir = os.path.join(project_root, 'modules')
if _modules_dir not in sys.path:
    sys.path.append(_modules_dir)
```

理由：4.1 只让程序"不崩"，但会让 `desktop_interaction` 的 logger 退化成
纯 console（拿不到带文件 handler 的"真"`logger_utils`），损失可诊断性。
4.2 才是**根因修复**——让真实 `logger_utils` 在所有模块里都能被解析。
`append` + 去重判断，保证标准库优先且重复导入无副作用。

> 两处配合：4.2 修根因、4.1 兜底，即使有人单独 `import desktop_interaction`
> （不经 `main`）也不会崩。

---

## 5. 验证

### 5.1 直接复现（同一命令，改前 / 改后）

| | 命令（**不设** `PYTHONPATH`，只放 `src/`） | 结果 |
|---|---|---|
| 改前 | `python -c "sys.path.insert(0, r'...\src'); import main"` | `ModuleNotFoundError: No module named 'logger_utils'`，exit=1 |
| 改后 | 同上 | `IMPORT_OK` + `MODULES_ON_PATH=True`，exit=0 |

### 5.2 真机端到端（README 方式）

`cd ralsei_pet\src && python main.py`（不设任何 `PYTHONPATH`）：

- 进程存活，窗口枚举到 `title="Ralsei Pet"`、`class_name=Qt5152QWindowToolSaveBits`、
  `visible=true`、PID 47216（`_recon/win_doc.txt`、`_recon/doc_present.txt`）。
- 即"文档化启动方式"恢复正常，这是本次修复的最终验收。

---

## 6. 回归（G2）—— 新增常驻套件，堵住盲区

新增 `code-quality-audit/第七轮/verify_round7_launch_import.py`，
并以 **只放 `src/`** 的方式起**真实子进程**去 `import main`，
专门复现"文档化启动"路径（而非像其它套件那样预置 `modules/`）。
5 项断言：

| 编号 | 断言 |
|---|---|
| R1 | 真实子进程里，仅 `src/` 在 path 时 `import main` 必须成功 |
| R2 | `main` 自身确实把 `modules/` 加进了 `sys.path` |
| R3 | 源码级：`main.py` 含 `modules` 入 path 的逻辑（防有人删掉） |
| R4 | 源码级：`desktop_interaction.py` 的导入已被 `try/except` 包裹 |
| R5 | 全量：`modules/` 下**不得**再出现裸的 `logger_utils` 顶层导入（防复发） |

已登记进 `code-quality-audit/regress/run_all.py` 的 `SUITES`，并重建基线。
全量结果（`_recon/g2_final_utf8.txt`）：

```text
suite            exit   PASS   FAIL   比对
round5_smoke     0      22     0      IDENTICAL
round5_verify    0      16     0      IDENTICAL
round6_verify    0      39     0      IDENTICAL
round7_launch    0      5      0      IDENTICAL   ← 新增
s1_anim_miss     0      17     0      IDENTICAL
s2_anim_json     0      40     0      IDENTICAL
s3_alias_legacy  0      19     0      IDENTICAL
合计：PASS=158 FAIL=0  套件=7
```

修复对既有 6 套件输出**逐字节无变化**（全部 `IDENTICAL`），退出码 0。

---

## 7. 真机（屏幕自动化）侧记

本轮顺带用 `screen-automation`（小助手 CLI）在**真实桌面**上做了观测与验证尝试。
结论如下，供后续排期参考。

**可用（已验证）**

- `cli window list-visible` 能枚举窗口，宠物窗口的 handle / class / DPI / 所在显示器
  全部可读（DPI 144，单显示器 2560×1600）。
- `cli task begin --title "Ralsei Pet"` / `task observe` / `task capture` /
  `screen capture --region` 均可用；`task observe` 的 OCR 能读到窗口内文字。
- 宠物在真机上**确实会自主漫游**：多次采样其 `window_region` 发生明显位移，
  证明 `update_movement` 在真实桌面上工作正常（不只是离屏模拟）。
- 抓到一条真实运行日志：宠物在 21:14:42 尝试 `walk_down_sleep` → 回退 `walk_down`，
  随后 32s 静止不动；结合日志可判定这是**进入了睡眠/休息态**（正常行为），并非卡死。

**受限（本环境做不到，如实记录）**

1. **无法用模拟拖拽做"甩飞/抛物线"真机验证**。宠物窗口是
   `Qt.FramelessWindowHint | Qt.Tool` 的**透明**窗口，只有精灵像素是不透明的；
   在窗口中心落点会命中透明区、事件穿透到下层窗口，`task drag` 抓不住它。
2. **宠物窗口是"非置顶"的**（仅 `main.py:2702` 的某条分支临时置顶），
   在 Agent 驱动 WorkBuddy 的会话里会被 WorkBuddy/webview 大窗口遮住，
   区域截图常只截到遮挡窗口；`SetWindowPos(HWND_TOPMOST)` 在本沙箱里被静默忽略。
3. **窗口 handle 会变**（同一进程先后出现 788832 / 5836590 / …），
   `task begin --handle` 不可靠，**应按标题绑定**。
4. 本机只有**单显示器**，"多显示器夹紧不把宠物拉回主屏"这条只能靠离屏断言覆盖
   （`round6_verify` 的 F 组），真机无法复现多屏场景。

上述 1/2/3 属"验证手段受限"，不影响 §4 修复与 §6 回归的结论；
"甩飞/抛物线/多屏"三条行为修复仍以 `round6_verify` 的 39 项断言为准。

---

## 8. 其余观察

- 真机运行日志里出现 `desktop_interaction` 的
  `打开失败，文件不存在: C:\Users\23002\Desktop\Mineradio`
  —— 属于桌面交互主动尝试、目标不存在而正常记录，非缺陷。
- `resolve_cli.ps1`（skill 自带）是 UTF-8 无 BOM，本机 PowerShell 5.1 按 GBK 读取
  会在中文引号处解析失败；本轮一律**直连 exe 绝对路径**绕过（不影响功能）。

---

## 9. 证据索引

- 修复前复现：`code-quality-audit/第七轮/_recon/fix_before_utf8.txt`
- 修复后：`code-quality-audit/第七轮/_recon/fix_after_utf8.txt`
- 文档化启动成功：`code-quality-audit/第七轮/_recon/win_doc.txt`、`doc_present.txt`
- G2 全量：`code-quality-audit/第七轮/_recon/g2_final_utf8.txt`、`g2_update.txt`
- 真机轨迹/睡眠：`_recon/trajectory_out.txt`、`_recon/petlog_after.txt`
- 互斥量探针/定向清理：`_recon/probe_mutex.py`、`_recon/kill_pet_py.py`
- 新增回归套件：`code-quality-audit/第七轮/verify_round7_launch_import.py`

> `_recon/` 为历轮约定的只读侦察区（已在 `.gitignore`），不随仓库提交；
> 本报告已把关键内容内联，报告本身自足。

---

## 10. 提交

- `fix(launch): modules/desktop_interaction 顶层裸导入导致文档化启动崩溃`
  —— `desktop_interaction.py` 加 `try/except` 降级；`main.py` 追加 `modules/` 进 `sys.path`
- `test(regress): 新增 round7_launch 套件 + 重建 G2 基线（7 套件 / 158 PASS）`
