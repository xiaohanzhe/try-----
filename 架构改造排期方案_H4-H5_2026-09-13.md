# 架构改造排期方案：H4 上帝类拆分 / H5 动画配置化

> 生成日期：2026-09-13
> 来源：`code_review_report_2026-09-13.md`（H4 §271 / H5 §293 / 第三阶段建议）与 `代码质量复审报告_2026-09-13_第五轮.md` §九.5
> 本文件只做**规划与基线度量**，未改动任何生产代码。

---

## 0. 定位与边界

前五轮审查修的是**缺陷**（止血：崩溃、数据丢失、不可达分支），验证方式可以做到"单点断言 + 独立回归脚本"。
H4/H5 是**结构性改造**，验证方式是"全功能等价性",成本量级不同，因此两者不能放进同一批 diff。

**范围**：`ralsei_pet/src/main.py`（H4）、`ralsei_pet/modules/sprite_loader.py`（H5）。
**不在范围**：185 个零引用方法的清理本身（作为 H4 的前置条件，见 §3 G1）、成就体系、`save_memory` 落盘键。

---

## 1. 为什么必须单独排期

| 理由 | 说明 |
|---|---|
| 无法单点验证 | 拆分的正确性判据是"行为完全不变"，只能靠全功能回归；单点断言覆盖不到 |
| 改动面 = 全项目 | `RalseiPet` 被 22 个模块中的多数反向依赖，改一处、影响面横跨所有功能 |
| 归因困难 | 混进缺陷批后，一旦回归，无法区分是"缺陷修复"还是"搬家"引起的 |
| 缺安全网 | `src/` 下 9 个 `test_*.py` 都是手工冒烟脚本，项目**没有可一键运行的回归套件**——这是当前最大的障碍 |

---

## 2. 基线量化（本次实测，脚本可复现）

证据：`code-quality-audit/架构改造-H4H5/_evidence/`（`main_class_profile.txt`、`sprite_mapping_gap.txt`、`animation_name_contract.txt`）
脚本：`code-quality-audit/架构改造-H4H5/scan_refactor_baseline.py`、`scan_animation_contract.py`（只读）

### H4

| 指标 | 实测值 |
|---|---|
| `main.py` 总行数 | 8794 |
| `RalseiPet` 起始行 / 直接方法数 | 第 122 行 / **186 个** |
| 方法体总行数 | 8286（占全文 **94.2%**） |
| `self.<attr>` 不同属性名 | **438 个** |
| 其中被写入（Store）过 | 269 个 |
| **被 ≥3 处写入（跨方法共享可变状态 = 真耦合点）** | **106 个** |
| 最长方法 | `update_animation` 536 行 → `update_movement` 428 → `mouseMoveEvent` 289 → `check_nearby_windows` 191 → `init_movement` 189 → `_tick_spell_flow` 179 |

关键结论：**拆分的瓶颈不是代码量，而是那 106 个被多方法读写的属性。** 不做属性归属表就动手，结果一定是新模块之间互相 import 或大量回调 RalseiPet。

> **⚠ 本表是 2026-09-13 的冻结基线，主树此后已变。** 2026-09-16 G3 实测同口径为：
> `main.py` **9588 行**（原 8794）、`RalseiPet` 起于**第 342 行 / 191 个方法**（原 122 / 186）、
> `self.<name>` 名字全景 **458**，其中**真属性 286**、`self.foo()` 调用位 172。
> §2 各数字仅用于"改造前 vs 改造后"的对比基准，**引用现值请用 `H4-G3_属性归属表_2026-09-17.md`**。
> 冻结证据仍保留在 `_evidence/main_class_profile.txt`，未被覆盖。
>
> **2026-09-17 二次复扫（ast 口径，脚本 `scan_method_index.py`）**：`main.py` **9588 行**、
> `RalseiPet` **191 个方法 / 方法体 8772 行**。方法级清单（含起止行 / 行数 / Wave 归属）
> 见 `_evidence/method_index.{txt,json}`。零引用筛查工具 `scan_zero_refs.py`
> （AST 计名字引用点，**不做字符串匹配**，避免注释/文档串误命中）→ `_evidence/zero_refs.txt`。

### H5

| 指标 | 实测值 |
|---|---|
| `animation_mapping` 键（动画组） | **109** |
| 引用帧文件（去重） | 363 |
| 素材目录 PNG 实际数量 | **1106** |
| mapping 引用但磁盘不存在 | **0**（此前 walk_up_blush 类问题已修） |
| 磁盘存在但未被任何动画组引用 | **743 / 1106（67%）** |
| 代码里直接写死的动画名字面量 | 24 个，全部被 mapping 覆盖 |
| **f-string 动态拼接点** | 12 处 / 9 种模板（`walk_{dir}`、`run_{dir}`、`walk_{tea}`、`{anim_type}_{new_dir}`…） |

关键结论：**mapping 不是"冗余的配置"，它是三条语义契约的唯一载体**：
1. **别名**——`happy`→laugh、`sad`→cry_start、`fall_mad`/`splat_mad` 共用同一份素材；这些名字与文件名**不同名**，自动扫描推不出来。
2. **故意缺省**——`walk_up_blush` / `walk_up_butler_unhappy` 因其帧文件在素材包中不存在而被**有意移除**，以便走前缀回退；这个"缺失"是设计，不是遗漏。
3. **回退契约**——`change_animation` 依赖组名做前缀回退与优先级比较（`walk`/`run`/`idle`/`laugh`/`sing`/`pose` 等类别前缀）。

因此原报告的"用 `scan_and_group_assets` 自动替代硬编码"**不能直接执行**：743 个未引用 PNG 一旦全量入组，命名空间会从 109 膨胀到数百，且会把灰块占位帧问题重新引入。

> **实测补充（2026-09-13，S1 阶段量出）**：这一点后来被数据坐实了。运行时 `sprites` 实际有
> **489 组**：mapping 的 109 个语义名 + `scan_and_group_assets` 多建的 **380 个"文件名前缀"组**
> （1084 帧对象，其中 82 组与 mapping 100% 重复加载）。那 380 组**没有任何代码按名字请求过**。
> 详见 `H5-S1_动画名自检_实施与验证报告_2026-09-13.md` §5.1 与 `_evidence/autoscan_cost.txt`。

---

## 3. 前置闸门（不满足就不要开工）

| 闸门 | 内容 | 判据 | **状态（2026-09-16 复核）** |
|---|---|---|---|
| **G1** | **零引用代码先处置**：185 个应用层零引用方法做"删除 / 标注 `# legacy, unused`"二选一，优先删 `update_floor`（已有漂移副本）与整块未接线的视频/游戏反应群 | 拆分时不再把死代码搬进新模块 | **未完成（1/185）**：第十五轮已删排期点名的优先项 `main.py::update_floor`（零调用漂移副本）及其不可达尾部；其余 184 个未处置，**卡在 §8 决策 2**（删除 vs 标注）。注：188/185 是第五轮口径，主树此后改了很多（现 9588 行），**开工前必须重扫一次**。已于 2026-09-17 重扫，见下。 |
| **G2** | **安全网先建**：把 `src/test_*.py` 9 个手工脚本 + 五轮的 `verify_round5_fixes.py`/`smoke_import_round5.py` 收敛为**一条命令可运行的回归基线**，覆盖：启动、动画切换、拖拽、对话真实动作、施法回调、躲猫猫、两款游戏 | 改造前后输出**逐字节一致** | **✅ 完成**：`code-quality-audit/regress/run_all.py`，**21 套件 / 799 条断言 PASS**，归一化哈希判定 + merge 模式基线（`baseline.json`），`--update --only` 可只固化变动套件。 |
| **G3** | **属性归属表**：438 个 self 属性落成一张表，按「窗口几何 / 物理 / 动画 / 情绪 / 会话 / 子系统引用」分区；106 个跨方法写入属性**逐条定归属** | 没有这张表不许进入 Wave 1 | **✅ 完成（2026-09-16）**：交付物 `H4-G3_属性归属表_2026-09-17.md`。口径修正后为 **286 个真属性 / 16 个分区 / 未归类 0**；真耦合点 **111 条**逐条定归属；必备接口 **138 条**按目标模块成清单；Wave 1 内部互写仅 **4 条**（W1-1 ↔ W1-2）。扫描器 `scan_attribute_ownership.py` + 生成器 `make_g3_report.py`，自检 X1–X7 全 PASS。 |
| **G4** | **动画名契约固化**（H5 的产物） | 见 §4，天然成为 H4 的护栏，故 H5 建议先行 | **✅ 完成**：H5 S1→S2→S3 全部落地（`H5-S1_..._2026-09-13.md`、`H5-S2-S3_..._2026-09-13.md`），`assets/animations.json` schema=1，别名/legacy 已显式化。 |

> **批次 A 结算（2026-09-16）**：G2 / G3 / G4 达成，**G1 未完成**。
> 方案字面上只对 G3 写了"没有这张表不许进入 Wave 1"（已满足），
> 但 G1 的存在理由是"拆分时不再把死代码搬进新模块"——**Wave 1 的 W1-5（17 个 `check_*`）、
> W1-4、W1-3 恰好是最可能有整块零引用方法的三项**，
> 所以进入 Wave 1 前至少要按"每拆一项就对该项做一次零引用筛查"来兜（见 §6 批次 B 备注），
> 或者先把 G1 整体做完。二者取一，由 §8 决策 2 一并定。

> **G1 重扫结果（2026-09-17，AST 口径 `scan_zero_refs.py`）**
>
> 1. 排期点名的 **W1-5（17 个办公 `check_*`）已不复存在** —— 第六轮随"删除内置台词入口"一起删掉，
>    留痕见 `main.py:5002-5003`；`check_browser_windows` / `check_ppt_windows` /
>    `check_excel_table_needs` 全项目 **AST 零引用**确认。等价能力现由 `modules/desktop_interaction.py`
>    （`check_ppt_files()` 等）承担。→ **W1-5 从 Wave 1 表划掉。**
> 2. 本轮新查出 **1 个零引用方法**：`create_person_name_table`（`main.py:4057`，56 行）全项目零引用。
> 3. Wave 1 其余各项均有真实调用点（不再是"整块未接线"）：清单见 `_evidence/zero_refs.txt`。


---

## 4. H5 方案：动画配置化（建议先做）

### 4.1 目标结构

```
ralsei_pet/assets/animations.json     # {schema_version, groups:{名:{frames,loop,offset,alias_of?,legacy?}}}
```
- `animation_mapping` 的**名字集合与语义不变**，只是来源从代码变成 JSON；
- 别名（happy/sad/fall_mad…）升为显式字段，不再靠"再写一遍文件名"表达；
- `scan_and_group_assets` **降级为"补漏报告器"**——只输出"磁盘有、配置无"的清单供人工确认，**不自动接管**；
- 加载顺序：JSON → schema 校验 → 文件存在性校验 → 失败则回落内置表（保证可回滚）。

### 4.2 三步改动（每步一个独立 commit，可单独回滚）

| 步 | 内容 | 风险 |
|---|---|---|
| **S1** | **启动自检（纯加日志，零行为变更）**：`change_animation` / `get_sprite` 未命中即 `WARNING` + 计数器；对 9 种拼接模板做闭集校验（`dir ∈ {up,down,left,right}`、`anim_type ∈ {walk,run}`）。产出一份"未命中清单" | 极低 |
| **S2** | **抽取 JSON + 加载器（等价迁移）**：脚本从现有 `animation_mapping` 生成 JSON（原注释转入 `meta` 字段保留），加载器读取并校验。**断言：JSON 还原的 dict 与原硬编码 dict 深度相等** | 低（等价性可机器验证） |
| **S3** | **显式化 alias 与 legacy**：85 个"静态零引用"键中，剔除动态可达的 `walk_*`/`run_*`/`walk_tea_*` 后，其余标注 `legacy: true` | 低 |

**S1 必须最先做**——因为 §2 已证明"当前 0 缺失是碰巧，不是被保证"：9 种动态拼接模板让配置缺失在静态上完全不可见。

### 4.3 验收

1. `JSON 生成的 mapping == 原硬编码 dict`（断言通过）；
2. 启动日志无"未命中动画名"告警；
3. 回归基线（G2 产物）输出与改造前一致；
4. 跑一轮完整交互后，S1 计数器未命中数 = 0。

### 4.4 回滚

删/改 JSON 或令 schema 校验失败 → 自动回落内置表，行为与改造前完全相同。

---

## 5. H4 方案：上帝类拆分

### 5.1 拆分原则

**不按代码长度切，按"状态所有权"切。**
判据只有一个：**该模块能否在不反向引用 `RalseiPet` 的前提下独立工作？**
若必须回调查询宿主状态 → 用项目已有模式：`pyqtSignal` 排队回主线程，或传入回调（现有 `_start_open_with_spell(path, kind, cb)` 已是正确范式）。

### 5.2 分层与施工顺序（低耦合优先）

**Wave 1 —— 零共享状态的独立子系统**（收益最大、风险最低，每项一个 PR）

> **⚠ 下表行号已于 2026-09-17 用 `scan_method_index.py` 全部刷新**（`main.py` 8794→**9588** 行）。

| 项 | 位置（2026-09-17 实测） | 规模 | 状态 / 备注 |
|---|---|---|---|
| **W1-3 游戏 → `GamesController`** | `main.py` **6186–6449**：`start_rock_paper_scissors` 6186 / `play_…` 6213 / `determine_…_winner` 6275 / `end_…` 6291 / `start_guess_number` 6330 / `play_guess_number` 6364 / `end_guess_number` 6427 | **7 个方法 / 258 行，块内闭合** | **列为第一项**。外部入口仅 `dialogue_ui.py:1445/1449/1453`；状态 `game_state` / `guess_number_game` 无跨模块写入 |
| W1-4 视频观看 → `VideoWatchController` | **4427–4751**：`check_video_apps` 4427 / `identify_video_apps` 4446 / `start_watching_video` 4479 / `_start_video_watching_loop` 4582 / `_update_video_watching` 4588 / `_react_to_video` 4613 / `stop_watching_video` 4674 / `suggest_watching_video` 4706 | **8 个方法 / 289 行** | 入口 `check_entertainment_needs:4829`（留在宿主）。注意分组器漏标了中间 3 个 `_*video*` 私有方法，规模应含它们 |
| W1-1 施法流程 → `SpellFlowController` | **8748–9040**：`_spell_interrupted_reason` 8748 / `_tick_spell_flow` 8775(183) / `_cast_spell_then` 8959 / `_start_open_with_spell` 9004 | **4 个方法 / 290 行** | 9 个 `_spell_*` 属性 |
| W1-2 躲猫猫 → `HideAndSeekController` | **9050–9402**：`start_hide_and_seek_game` 9050 / `_abort_hide_and_seek` 9079 + 10 个 `_hide_*` | **12 个方法 / 342 行** | 与 W1-1 有 4 条属性互写（G3 已定位）；`_hide_ralsei`（445 行）是**托盘隐藏**，属别处，勿并入 |
| W1-6 文件/表格操作 → `FileSheetController` | 存活：`handle_file_operation` 4114(143) / `fix_excel_format` 4298 / `fill_names_in_excel` 4351 / `check_{file,text,image}_content` 8076·8100·8142 | ~370 行 | **先处置死代码**：`create_person_name_table` 4057(56 行) 与 `check_excel_table_needs` 均**全项目零引用**；且与 `modules/desktop_interaction.py` 有重复，须先比对再合并 |
| ~~W1-5 办公应用检测群 → `OfficeAppDetector`~~ | ~~3266–3656，17 个 `check_*`~~ | — | **❌ 划掉：第六轮已删**（留痕 `main.py:5002-5003`，AST 零引用确认）。等价能力在 `modules/desktop_interaction.py` |

**Wave 1 施工顺序（按"自包含程度 + 块内闭合度"）：W1-3 → W1-4 → W1-1 → W1-2 → W1-6。**
W1-3 之所以放第一：7 个方法物理连续（6186–6449）、只被 `dialogue_ui` 三处进入、
不写任何 `_spell_*`/`_hide_*`，是唯一"零互写"的一项，最适合先把"转发壳 + 回归一致"这套手法跑通。


**Wave 2 —— 与 QWidget 事件耦合的交互层**

| 项 | 位置 | 规模 |
|---|---|---|
| W2-1 鼠标交互 → `MouseInteractionController` | `get_ralsei_body_part` 4727 / `mousePressEvent` 4785 / `mouseMoveEvent` 4940 / `mouseReleaseEvent` 5230 / `mouseDoubleClickEvent` 5303 / `update_mouse_drag` 1732 | ~660 行；牵动 12 个拖拽类属性 |

**Wave 3 —— 核心循环（最后做，且必须先引入状态数据类，否则只是搬家）**

| 项 | 位置 | 规模 |
|---|---|---|
| W3-1 动画 → `AnimationController` | `update_animation` 6699 / `change_animation` 6186 / `animation_priorities` | 536 + 109 行；H5 的配置即其输入 |
| W3-2 移动 → `MovementController` | `update_movement` 1189 / `randomize_movement_pattern` 955 / `generate_new_move_target` 1050 / `init_movement` 621 | ~850 行 |
| W3-3 物理 → `PhysicsController` | `start_jump` 2028 / `handle_jump` 2228 / `handle_gravity_fall` 2604 / `handle_fall` 2725 / `start_fall` 2491 / `check_window_movement` 2356 | ~770 行；10 个 `fall_*`/`is_*falling` 相位变量 |

### 5.3 每一波的统一施工手法（"转发壳"）

1. 新建 `ralsei_pet/modules/<x>_controller.py`；
2. `main.py` 中原方法体改为**一行转发**，**保留原方法名**（避免 186 个方法 / 全项目调用点一次性重写）；
3. 跑回归基线；
4. 全部稳定后，下一波再删除转发壳。

好处：每一步 diff 可控、可单独回滚、回归失败能立刻定位到单个 Controller。

### 5.4 每波验收

- G2 回归基线输出与改造前**逐字节一致**；
- 结构断言：新 Controller **不 import `main`**；
- 记录 `RalseiPet` 方法数 / 行数下降曲线（并入每轮审查报告）；
- 五轮报告已指出的"注释与代码不符"项，不得在搬迁过程中新增。

### 5.5 明确不做

- 不改行为、不顺便重构业务逻辑、不同时改 UI 布局；
- 改造期冻结功能新增；
- **不追求一次拆完**：Wave 3 若阻力过大，允许长期停在"Wave 1+2 完成"——此时 `main.py` 已从 8794 行降到约 4500–5000 行，边际收益已经拿到大半。

---

## 6. 建议批次与开工/完成判据

| 批次 | 内容 | 开工条件 | 完成判据 | 回滚点 |
|---|---|---|---|---|
| **A** | G1 死代码处置、G2 回归基线、G3 属性归属表 **+ H5 全量（S1→S2→S3）** | 本方案确认 | 上 §4.3 四条全过 | 每个 commit 独立 |
| **B** | Wave 1 六项（W1-1…W1-6，每项一 PR） | A 完成；回归基线可用 | 每项：回归一致 + 新模块不 import main | 单项 PR |
| **C** | Wave 2（W2-1 鼠标交互） | B 完成 | 同上 | 单项 PR |
| **D** | Wave 3（W3-1/2/3）——**重新评估后再定** | C 完成 + 属性表覆盖 Wave 3 字段 | 同上 | 单项 PR |

> **批次状态（2026-09-16）**：A 的 G2/G3/G4 已达成，**G1 未完成**。B 的取舍见 §3 末"批次 A 结算"：
> 要么先补完 G1，要么在每项 PR 内部加一次**该项专属的零引用筛查**（成本更低、更精准，倾向后者）。
> C/D 未开工。G3 已把 Wave 1 的接口阻塞面收敛到 **4 条**（W1-1 ↔ W1-2 互写 `_hide_*`/`_spell_*`），
> 其余 134 条接口的另一侧都是"留在宿主"或 Wave 2/3 模块 —— 属性不搬即无反向依赖。

> 工作量以 **commit / PR 数**衡量，不预估日历时间——H4 的进度取决于回归基线质量，而非编码速度。

---

## 7. 与现有审查序列的关系

- 缺陷审查（第六轮、第七轮…）照常进行；改造期间以"结构变更"为主题时，**同一批内不混入缺陷修复**；
- 改造期若发现 P0 缺陷，在改造 diff 之外单独提交，避免归因混淆；
- 每完成一波，在对应轮次审查报告中记录"结构断言 + 行数曲线"，使改造进度可审计。

---

## 8. 3 项决策 —— **已裁定（2026-09-17，技术细节由我拍板）**

1. **顺序 → 先做 H4-Wave 1。**
   理由：H5（S1→S2→S3）**已完成**，"H5 先行"的原始动因已经消化；G4 动画名护栏也已在位，
   继续等 H5 没有意义。Wave 1 里唯一与 H5 有交集的 W1-3（游戏切动画）直接走既有 `animation` 名。
2. **G1 处置方式 → 不整体做 185 项，改为"每项 PR 内做该项专属零引用筛查"**（方案 §3 末的倾向项）。
   理由：整体清理 185 项要动大量与 Wave 1 无关的代码，回归面大而收益分散；
   每项 PR 自带筛查则"拆到哪就清到哪"，成本更低、更精准，且失败可定位到单项。
   落地工具已就位：`scan_zero_refs.py`（AST 口径）→ `_evidence/zero_refs.txt`。
   本轮已按此口径清出 1 个新的全项目零引用方法（`create_person_name_table`，56 行），
   连同 G3 的 3 个悬空读与 59 个死参数候选，**在下一次触碰相关模块时一并处置并留痕**。
3. **Wave 3 → 说明确不在本次范围**。Wave 1+2 完成后重新评估（维持原倾向）。

> **下一步（已开工）**：W1-3 游戏 → `GamesController`。开工即含本项零引用筛查
> （已跑：7 个方法全部有真实调用点，入口为 `dialogue_ui.py:1445/1449/1453`）。

---

*证据与脚本：`code-quality-audit/架构改造-H4H5/`（只读脚本 + `_evidence/` 原始输出）。
2026-09-17 新增：`scan_method_index.py`（方法索引）、`scan_zero_refs.py`（零引用筛查）。*
