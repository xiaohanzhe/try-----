# Ralsei 桌宠 · 深度代码审查报告（独立验证版）

- 审查对象：`ralsei_pet/`（约 20,000 行 Python）+ 入口 `run.py`
- 审查方式：6 路并行模块精读 + 逐条**回源核实**（所有结论均带文件:行号，并已用实际代码复核）
- 审查维度：隐藏 Bug / 边界条件 / 重复编码 / 扩展性隐患 / 异常处理完整性
- 结论口径：**P0 = 崩溃、数据错误或核心功能完全失效；P1 = 明显错误或偶发故障；P2 = 隐患与可维护性**

---

## 摘要

代码整体已完成过一轮"防御性加固"（大量 `try/except`、JSON 原子写、损坏回退），因此**常规操作路径基本不会崩**。但本轮发现 **3 个 P0、14 个 P1**，其中 3 个是**整块功能静默失效**（写了但永远不会执行），2 个是**注释声称已修复、实际未修**的死代码。

同时，本轮**推翻了 3 条初筛结论**（见文末第五节），它们经核实并非缺陷，不应误改。

---

## 一、P0 级问题

### P0-1 全局异常兜底缺失 —— 槽函数内任何异常都会杀掉整个进程

`main.py:132` 只有 `atexit.register(self.cleanup_on_exit)`，全项目**没有** `sys.excepthook`，也**没有** `app.aboutToQuit` 连接。PyQt5 ≥ 5.5 的默认行为是：**Qt 槽函数中未捕获的 Python 异常会调用 `qFatal()` → `abort()`，进程直接退出**（不是弹个报错框，是桌宠瞬间消失）。

而项目里存在真实的未防护调用点：

`dialogue_ui.py:796-799`
```python
# 文件操作
if self.handle_file_commands(user_input):
    return
```
→ `dialogue_ui.py:1169-1175`（同一文件内，**唯独这个分支**没有 try）
```python
if "打开" in raw:
    result = self.parent.handle_file_operation(raw)   # 无 try/except
```
对照：同文件 `1204-1216`、`1217-1222`、`1179-1192` 等分支**全都**包了 `try/except`。`handle_file_operation`（`main.py:3640`，130 行）内部含大量 `re` 正则与文件系统操作，一旦抛出未捕获异常，就会顺着 `handle_file_commands` → `send_message` 这个 **Qt 槽**冒泡，直接终结进程，用户输入框里的内容一并丢失。

**修复（两步，成本极低、收益最高）**
```python
# main.py 启动处
import sys, traceback
def _excepthook(t, v, tb):
    _log.error("未捕获异常", exc_info=(t, v, tb))
    try: traceback.print_exception(t, v, tb)
    except Exception: pass
sys.excepthook = _excepthook
```
并把 `dialogue_ui.py:1173` 补上 `try/except`（与同文件其他分支对齐）。

---

### P0-2 多显示器坐标体系混用 —— 副屏完全不可用

项目里存在**两套互斥的屏幕坐标方案**：

- **正确方案**：`main.py:7588 _virtual_screen_rect()`，用 `SM_XVIRTUALSCREEN(76)/Y(77)/CX(78)/CY(79)` 取虚拟桌面（含左侧负坐标副屏）。它的 docstring 自己写明了问题：
  > "所有自主移动的屏幕夹取都用它，而不是主屏 availableGeometry——否则右侧副屏目标走不到（每帧被拉回主屏）、左侧负坐标副屏区域完全不可达。"
- **错误方案**：`QApplication.desktop().availableGeometry()`，**只返回主屏**，且用 `max(0, ...)` 把坐标硬夹到主屏范围。

而"错误方案"遍布全项目 **20+ 处**：`main.py:249, 1057, 1304, 1652, 2070, 2097, 2256, 2336, 2550, 2611, 2798, 2846, 2906, 4062, 7031, 7537, 7722, 7733, 8038, 8130, 8185, 8365`。

**后果**：多显示器用户的桌宠，只要走到主屏以外的屏幕，每帧都会被 `max(0, min(x, width-w))` 拽回主屏——表现为**在屏幕边缘卡住抖动**，右侧副屏永远上不去。这是多屏用户的一等功能失效。

**同根因的第二处**：`desktop_interaction.py:943-944, 1006-1008`
```python
screen_width  = win32api.GetSystemMetrics(78)   # 只拿了"宽"，没拿"原点"
screen_height = win32api.GetSystemMetrics(79)
...
if (rect[0] > screen_width or rect[1] > screen_height
    or rect[2] < 0 or rect[3] < 0):     # 用 0 当虚拟桌面的左/上边界
    return True
```
正确写法在 `command_manager.py:58-63`（拿了 76/77 减原点）。当副屏位于主屏**左侧或上方**时，虚拟桌面原点为负，这些窗口的 `rect[2]` 或 `rect[3]` 也会是负值 → **被当作"屏幕外窗口"全部过滤掉**，Ralsei 既看不见也站不上去。

**修复**：统一改用 `_virtual_screen_rect()`；`desktop_interaction` 的过滤改为
```python
vx, vy = GetSystemMetrics(76), GetSystemMetrics(77)
vw, vh = GetSystemMetrics(78), GetSystemMetrics(79)
if rect[0] > vx + vw or rect[1] > vy + vh or rect[2] < vx or rect[3] < vy:
    return True
```

---

### P0-3 拖拽"甩飞 / 弹跳"整套物理是死代码 —— 永远不可能触发

这是本轮**最具体的功能性 Bug**，且是"写了 50 行、一行都不会执行"。

`main.py:4896-4899`（拖拽移动分支末尾）
```python
# 保存上次拖拽位置用于速度计算
if not hasattr(self, '_last_drag_pos_prev'):
    self._last_drag_pos_prev = target_pos
self._last_drag_pos_prev = self._last_drag_pos      # ← 无条件覆盖
```
关键在于 `main.py:4857` 刚刚执行过 `self._last_drag_pos = target_pos`。因此第 4899 行让
**`_last_drag_pos_prev` 恒等于 `_last_drag_pos`**。

再看释放侧的物理计算 `main.py:4917-4922`：
```python
if hasattr(self, '_last_drag_pos'):
    if hasattr(self, '_last_drag_pos_prev'):
        dx = self._last_drag_pos.x() - self._last_drag_pos_prev.x()   # 恒为 0
        dy = self._last_drag_pos.y() - self._last_drag_pos_prev.y()   # 恒为 0
        release_speed = (dx ** 2 + dy ** 2) ** 0.5                    # 恒为 0.0
```
于是三个分支全部恒假：
- `main.py:4925` `if release_speed > 150:` → 甩飞（`is_falling` / `jump_ball` 动画 / 惯性滑行）**永不执行**
- `main.py:4948` `elif release_speed > 20:` → 弹跳（`_bounce_params` + `_bounce_timer`）**永不执行**
- `main.py:4967` `if release_speed > 10:` → 旋转阻尼 **永不执行**

**叠加的第二个缺陷**：这段物理反馈写在 `mouseMoveEvent` 的 `else` 分支里（`main.py:4809/4909`，即"鼠标移动但左键未按下"），而 `mouseReleaseEvent`（`main.py:5115-5186`）**完全没有处理释放物理**，只重置了 `_is_being_dragged` / `_drag_speed`。也就是说，即使修好 `_last_drag_pos_prev`，效果也只能在"松手之后**再移动一次**鼠标"时才触发——用户甩出 Ralsei 后手一停就没反应。

**修复**
```python
# 1) 删掉 main.py:4896-4899，改为在 set 之前先保存旧值
prev = getattr(self, '_last_drag_pos', None)
self._last_drag_pos_prev = prev if prev is not None else target_pos
self._last_drag_pos = target_pos

# 2) 把 4916-4973 的物理反馈抽成 _apply_release_physics()，
#    在 mouseReleaseEvent 里（清理状态之前）调用一次
```

---

## 二、P1 级问题（已核实）

| # | 位置 | 问题 | 后果 / 触发场景 |
|---|---|---|---|
| 1 | `social_growth_system.py:483-518` | **进化阶段跳级**：`_check_level_up` 只在"跳到的那一级"调一次 `_check_evolution`，而后者门槛是 `level % 5 == 0` | 经验 380→580 时等级 4→6，**5 级形态永久跳过**；后续 10 级时 `target_stage=3` 而 `stage=1`，直接 1→3，**2 阶段形态永远无法获得**。等级与形态不符 |
| 2 | `social_growth_system.py:520-557` | **成就条件表缺项**：`conditions` 只定义 12 个，但 `achievements_list`(143-270) 里还有 `level_10 / level_20 / evolution_master / explorer / creator / storyteller / social_butterfly`；且全项目 `_unlock_achievement` **只有一处调用**（565 行内部） | 这些成就**永远无法解锁**，满级玩家也看不到"成长大师" |
| 3 | `memory_system.py:141-178` | **关联记忆恒返回空**：`add_memory`(55-59) 存的字典只有 `timestamp/type/content`，**没有 `id` 键**，而 `get_associated_memories` 第 148 行判 `if 'id' in memory` | 条件恒 False → `target_memory` 恒 None → 函数恒返回 `[]`。**功能完全失效**。另 156/172 行 `target_memory['content'].lower()` 未做 `isinstance` 防护（114 行已防护），content 为 dict 时 `AttributeError` |
| 4 | `dialogue_system.py:1166` vs `1176` | **分支顺序错误**："不喜欢"包含子串"喜欢"，1166 行先命中 | "我不喜欢吃辣" → 回复"我也喜欢！这真的很有趣呢！"（情感判断反向）。1176 的 dislike 分支对含"不喜欢"的输入**不可达** |
| 5 | `dialogue_system.py:747`（同类 799、1186） | **运算符优先级错误**：`"饿" in u or "吃" in u and "蛋糕" not in u` ≡ `"饿" in u or ("吃" in u and "蛋糕" not in u)` | "我饿了想吃蛋糕" 因含"饿"命中 food 分支，本应走 dessert(860)。语义与作者意图不符 |
| 6 | `main.py:7892` | **施法兜底超时不可达**：`cond_timeout_fallback` 条件为 `frames_seen >= expected * 2` = **22**，但 `spr_ralsei_spell_*.png` 实测**恰好 11 张**（`spr_ralsei_spell_0..10`，`spell_left` 同为 11），去重计数上限就是 11 | 兜底分支**恒假**，等于没有安全网。而 `_spell_interrupted_reason`(7736) 只处理 drag/touch/physics/anim_mismatch，**没有墙钟超时**。一旦帧计数因任何原因到不了 11，施法永久悬挂 → 文件打不开、躲猫猫卡死。建议加 `if now - self._spell_start_time > 5.0: 强制完成` |
| 7 | `main.py:6342, 6357` | **主线程阻塞**：`psutil.cpu_percent(interval=0.1)` + `socket.create_connection(('www.baidu.com',80), timeout=2)` | 被 `update_ai → _handle_agent_commands → get_commands` 周期性调用，每次卡 0.1~2.1 秒，UI 冻结 |
| 8 | `main.py:515-517` + `1728` | `api_control_timer` 每 **2 秒**在主线程调 `api_client.get_commands()`，而 `HTTPLocalAI.get_commands`(212-218) → `_post_json` 是**同步 HTTP** | 一旦配置了 `commands_endpoint`，主线程每 2 秒卡一个网络往返。项目已有现成的 `_api_result` 跨线程信号范式(`6411-6430`)却未使用 |
| 9 | `weather_system.py:128-136` + `desktop_interaction.py:357` | 主线程同步网络探测（`timeout=1.5` / `timeout=1`），前者在 `get_weather_response` 内被主线程调用 | 无网/弱网时 GUI 周期性假死 |
| 10 | `config_manager.py:22, 166-182` | **打包后配置写只读目录**：`os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", config_file)`，PyInstaller 下 `__file__` 指向 `_MEIPASS` 临时只读目录，`os.replace` 必抛 `OSError` | 异常被 `except` 吞掉 → 返回 False → **用户的配置永远存不下**，下次启动回退默认值，且全程静默无提示。`logger_utils.py:34`(logs/)、`sprite_loader.py:19-20`(素材根目录) 同病 |
| 11 | `main.py:4112-4116` | **定时器泄漏**：`_start_video_watching_loop` 每次 `QTimer(self)` 新建并覆盖引用，未 stop 旧的 | `start_watching_video` 被调用两次（连续识别到两个视频窗口）→ 旧定时器仍在跑，每 5 秒重复触发 `_update_video_watching` → 反应翻倍 |
| 12 | `command_manager.py:51-68` | `x = command.get('x', 0)` 后虽经 win32 钳制，但**当 x 本就在范围内且为 float 时**，`min/max` 返回浮点，`QPoint(x, y)` 抛 `TypeError` | 被 `execute_command` 顶层 except 吞掉 → 命令**静默失效**，Ralsei 不动且无任何提示。应 `QPoint(int(round(x)), int(round(y)))` |
| 13 | `desktop_interaction.py:301` | **平台判断写反**：`if platform.system() == "Windows": psutil.getloadavg()`，但 `getloadavg()` 在 Windows 上不存在（Unix 专属） | 异常被 `except` 吞掉 → `load_average` 永远缺失；而 Linux/macOS 反而不采集。应为 `!= "Windows"` |
| 14 | `main.py:8376-8406` | **单实例保护被破坏 + `input()` 陷阱**：检测到已运行后先 `CloseHandle` 再 `input("按回车键退出...")`。有控制台时**卡住等回车**；用 `pythonw` 启动（无控制台）时 `input()` 抛 `EOFError`，被第 8408 行 `except Exception` **吞掉**，流程继续走到文件锁分支 | 结果：**第二个实例照样启动**，单实例保护失效；同时留下用户看不懂的卡顿。应直接 `sys.exit(0)`，删掉 `input()` |

**补充（P1 边缘，建议一并处理）**
- `main.py:1234-1248`：`update_movement` 的特殊状态门只拦 `is_jumping / is_gravity_falling / is_falling / is_recovering`，**未拦 `is_splat`**（`main.py:2511` 置位，`6710` 才复位）。摔扁动画约 2 秒期间宠物仍会自主走动。
- `main.py:41` + `64-72`：`PerformanceMonitor.record_time` 用**无上限 append**，而 `update_movement`(1156) 与 `update_animation`(6480) 都被 `@monitor_performance` 装饰、由 30ms 定时器驱动 → **约 66 次/秒** 持续写入，永不清理。长开机挂机必然内存膨胀。改用 `collections.deque(maxlen=300)`。
- `emotion_system.py:198`：`elapsed = current_time - self.last_emotion_change` 未 `max(0, ...)`。用户回拨系统时间或休眠唤醒后 delta 异常巨大/为负，情绪被一次性清零或"反向衰减"。
- `emotion_system.py:461`：复合情绪 `max(-100, min(100, value))` 允许负值，而 `get_current_emotion` 用 `abs()` 取强度 → 负复合情绪会被误当成"高强度正向"选动画。应为 `max(0, ...)`。
- `entertainment_system.py:306`：`end_game` 未校验 `difficulty`，`.index()` 可抛 `ValueError`（`start_game` 校验了，`end_game` 没有）。
- `entertainment_system.py:221`：读档用**整字典替换** `activity_stats`，旧存档缺 `music_composed` 等键时后续 `+= 1` 直接 `KeyError`（`social_growth` 有 `_to_int` 强转，此处没有）。
- `autonomous_agent.py:233-287`：`tick()` **无顶层 try**，一旦 `_pick_target` / `change_animation` 抛错会冒泡到定时器回调，自主行为链断裂。

---

## 三、P2 级问题

### 3.1 重复编码（可机械消除的量最大的一类）

1. **17 个 `check_*` 办公场景函数几乎逐行复制**：`main.py:3245-3582`（`check_ppt_windows / check_excel_windows / check_word_windows / check_task_management / check_email_management / ...`）。每个都是同一模板：`import random` → `current_hour = int(time.strftime("%H"))` → 构造 help_choices → `random.choice` → `add_dialogue` → `show_dialogue`。**新增一个场景要复制约 30 行**；"工作时段 `9<=hour<18`"这个业务规则散落在十余处，改一次要改十几处。
   → 抽成 `_offer_help(title, work_choices, leisure_choices, emotion)`，时段规则收敛到单个常量函数。
2. **`identify_ppt_windows / identify_excel_windows / identify_word_windows` 三份近似代码**（`desktop_interaction.py:2514 / 2566 / 2618`），且 `find_desktop_workerw_hwnd` 与 `_probe_desktop_icon_positions` 重复了整条 `WorkerW/SysListView32` 查找链。
3. **`auto_organize` 分类块重复**（`desktop_interaction.py:1462-1474` 与 `1547-1559`）。
4. **`open_file` / `open_folder` 近乎逐行重复**（`main.py:7389-7424`），各含一个 `_real_*` 内嵌函数 + 相同的 try/except 包裹 `_start_open_with_spell`。
5. **猜拳 / 猜数字两套游戏初始化重复**（`main.py:5579` 与 `5729` 的 `game_state.update({...})`）。
6. **`clamp` 内联重复**：`emotion_system`（多处 `max(0,min(100,...))` / `max(-100,min(100,...))`）、`energy_hunger:67-68`、`social_growth`、`customization` 各写一份。应抽公共 `clamp(v, lo, hi)`。
7. **存档读写模板重复**：emotion 不入档，其余 4 个系统各写一份 `load_*_data/save_*_data`（open + json + try/except），**且只有 social 做了类型强转**——这正是上文 P1「entertainment 读档 KeyError」的根因。建议统一 `_safe_load/_safe_save` 或基类。

### 3.2 未来扩展会踩的坑

- **平衡参数全部硬编码**：情绪衰减速率、饥饿/精力阈值、经验奖励、进化阶段、情绪增量散落在各模块常量与字典里。加一个情绪要改 3~5 处。建议数据驱动（`emotions.json` / 配置表）。
- **双情绪系统并存**：旧 `self.emotions`（`main.py:730-740`）与 `emotion_system`（`858-882`）两处真相源，靠 `update_mood` 每 5 秒单向同步（`_sync_emotions_to_system`）——长期必然分歧。应废弃其一。
- **魔法数字**：躲猫猫障碍位 `cx±120, cy±180` 写死（`main.py:8133-8136`）；`expected = 11`（spell 帧数）写死；`window_width <= 100` 等阈值散落。
- **写死中文/本地化**：`desktop_interaction.py:749` `desktop.GetDetailsOf(item, 15) == "文件夹"` —— 非中文 Windows 返回 `"Folder"`，导致 Shell 路径永久失效并回退 `os.listdir`。
- **`QApplication.desktop()` 已废弃**（Qt6 移除），全项目 20+ 处，迁移 Qt6 时需批量替换为 `QApplication.primaryScreen()`。
- **`show_dialogue(text)` 与 `add_dialogue()+show_dialogue()` 两种风格混用**（`main.py:7071/7078/7282/7283` 等）。虽然当前签名 `show_dialogue(message=None, face_type=None)`（`dialogue_ui.py:664`）**兼容**，但风格分裂会持续诱发误用。
- **跨模块隐性契约**：`ai_driver.py:200` 依赖 `main.py` 定义的 `_api_result` 信号与槽约定；`pet_ai` 混用 `energy_hunger.get_energy()` 与 `.energy`（`pet_ai.py:107` vs `207`）。缺 `hasattr` 守卫时异常被吞、行为静默失效。

### 3.3 资源与工程卫生

- **仓库被调试产物污染**：根目录 70 个 `.py`，其中约 30 个是 `test_*.py / diag*.py / _audit_*.py / _verify_*.py` 一次性脚本；`ralsei_pet/` 下还有 17 个 `test_window_detection_*.py` 系列。另有 3 个主程序备份：`main.py.bak`、`main.py.backup_before_fix`、`main.py.backup_before_deadcode_cleanup`。建议清理或移入 `tools/`。
- **孤儿标志**：`self.is_dragging`（`main.py:1228`）在 1-3800 行内无任何读取点，实际拖拽用的是 `_is_being_dragged` / `is_dragging_mouse`。语义混乱，易误改。
- **`desktop_interaction.py:51` 的 `update_timer` 从未 `stop`**：模块重建时旧定时器泄漏。
- **`QTimer.singleShot` 链在退出后可能触发**（`main.py:4692, 7364, 7532, 8349, 8373`）：`cleanup_on_exit` 只停命名定时器，未取消 singleShot，对象销毁后回调可能引发 "wrapped C/C++ object deleted"。

---

## 四、已确认「无问题」的方面（不必再改）

- **API Key 安全**：无硬编码；`_auth_headers` 仅在非空时加 `Authorization`；失败日志只打 URL/异常，**不泄露 key**。
- **对话 UI 无 HTML 注入**：`add_dialogue`(359-372) 对消息统一 `html.escape`，并处理了 `None`/非字符串。
- **AI 对话是异步的且并发安全**：`chat_with_ai`(6432) 与 `_async_api_request`(6411) 均用 `daemon=True` 线程 + `self._api_result` 信号投递回主线程；回调随信号携带（非全局闭包），`dialogue_ui` 还有 `_ai_seq` 序列号防止迟到回复串话。**实现正确**。
- **删除操作是安全的**：`delete_file` 先弹模态确认框（`main.py:7563`，默认 `No`）；`desktop_interaction.delete_file` 默认走 `SHFileOperation + FOF_ALLOWUNDO`（**送回收站**），且回收站失败时**明确返回 False 并保留文件**，已堵住"用户以为可恢复、实际被永久删除"的旧坑。
- **JSON 持久化健壮**：`save_memory` 用临时文件 + `os.replace` 原子替换；`load_memory` 对非 dict/缺键做默认结构补全；`config_manager` 对损坏 JSON/`UnicodeDecodeError`/非 dict 根节点均回退默认并备份。
- **日志不会无限增长**：`logger_utils` 用 `TimedRotatingFileHandler(backupCount=7)`；配置备份有 `MAX_CONFIG_BACKUPS=10` 清理。
- **`cleanup_on_exit` 确实被注册**：`main.py:132 atexit.register(...)`。
- **无危险反序列化/命令执行**：全程无 `eval/exec/pickle/os.system`；LLM 输出走关键词分发而非直接当路径/命令执行；记忆路径由 `__file__` 固定拼接，无路径穿越。
- **跳跃/重力/摔倒状态机复位完整**：`handle_jump` 在 `progress>=1.0` 必置 `is_jumping=False`；`handle_gravity_fall` 落地/触底必置 `is_gravity_falling=False`；`start_fall` 与 `start_falling` 用 `is_jumping/is_falling` 互斥防重入。除零已防护（`jump_duration<=0` 提前返回）。
- **`random.choice` 空列表**：业务路径基本都有 `if xxx:` 前缀保护。
- **`sprite_loader` 缓存有 LRU 上限**（`cache_limit=1000`），加载失败返回非 null 占位图，不会向上传 `None`。

---

## 五、被本轮推翻的初筛结论（**请勿据此修改**）

谨慎起见单独列出——这 3 条在初筛中被报为 P0/崩溃级，但**回源核实后不成立**：

| 初筛结论 | 核实结果 |
|---|---|
| "`dialogue_ui.py:819` 在主线程同步调 `chat_with_ai`，会冻结 UI 最长 30 秒（P0）" | **不成立**。`main.py:6447-6464` 明确用 `threading.Thread(daemon=True)` 异步执行，并 `emit` 回主线程。`dialogue_ui.py:822-832` 还有 `_ai_seq` 序号做并发保护。 |
| "`main.py` 中约 8 处 `show_dialogue(text)` 参数不匹配会 `TypeError` 导致功能失效（P1）" | **不成立**。`dialogue_ui.py:664` 签名为 `show_dialogue(self, message=None, face_type=None)`，**接受可选文本参数**，调用合法。 |
| "施法流程因 `spell` 帧数可能 < 11 而永久卡死（P1）" | **部分不成立**。实测素材 `spr_ralsei_spell_0..10` / `spell_left_0..10` **恰好各 11 帧**，`cond_ideal` 可达。但由此**反而确认**了另一个真问题：兜底条件 `frames_seen >= 22` 在这 11 帧上限下**永远不可能成立**（见 P1-6）。 |

---

## 六、修复优先级建议

**第一优先（半天，收益最高）**
1. 加 `sys.excepthook`（P0-1）—— 一步消除"桌宠突然消失且无日志"。
2. 补 `dialogue_ui.py:1173` 的 try/except（P0-1）。
3. `config_manager` / `logger_utils` / `sprite_loader` 改 `%APPDATA%` 写路径 + `sys._MEIPASS` 读回退（P1-10）—— 决定打包版能不能用。

**第二优先（1~2 天）**
4. 修 `_last_drag_pos_prev` 覆盖 + 把释放物理移到 `mouseReleaseEvent`（P0-3）。
5. 多屏坐标统一到 `_virtual_screen_rect()`，含 `desktop_interaction` 的过滤条件（P0-2）。
6. `social_growth` 进化逐级补判 + 成就条件补齐（P1-1、P1-2）。
7. `memory_system` 加 `id` 字段或改按内容匹配（P1-3）。
8. `dialogue_system` 分支顺序与括号（P1-4、P1-5）。
9. 施法加墙钟超时，删掉恒假的 `cond_timeout_fallback`（P1-6）。

**第三优先（持续重构）**
10. 主线程网络/IO 全部移入后台线程（P1-7/8/9）。
11. `PerformanceMonitor` 改 `deque(maxlen=...)`；`video_watching_timer` 先 stop。
12. 抽取重复代码（17 个 `check_*`、3 个 `identify_*_windows`、`open_file/open_folder`、`clamp`、存档读写模板）。
13. 清理 30 个调试脚本与 3 个 `main.py` 备份。

**验收建议**：每修一项后至少覆盖这几条用户路径 ——
① 多显示器（副屏在左/在右）下让桌宠自由行走；② 快速拖拽后甩出 Ralsei；③ 输入"我不喜欢吃辣"、"我饿了想吃蛋糕"；④ 连续触发两次"打开文件"；⑤ 升到 5/10/20 级看形态；⑥ 打包成 exe 后改设置并重启。
