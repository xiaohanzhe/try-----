# Ralsei 对话 AI「人味」诊断 + 训练方案

诊断日期：2026-09-18（本机真实系统时间；工作跨 09-17 深夜 ~ 09-18 凌晨）
范围：`ralsei_pet/` 对话链路（Ollama 本地模型 → `api_client` → `main.chat_with_ai` → `dialogue_ui`）
证据：`code-quality-audit/人味诊断-2026-09-18/_evidence/`（3 份原始输出 + 2 个只读探针脚本）
本轮性质：第一~七节为**只读诊断**（当时未改动任何工程文件）；**第九节起为施工记录**
（用户拍板 L1 全做之后）。回归锁：G2 套件 `persona_chat`
（`code-quality-audit/人味改造-2026-09-18/verify_persona_chat.py`）。

---

## 一、结论先行

1. **「没活人味」的主因不是模型不行，是提示词没接对。** 同一个 3B 模型，只换提示词，回复从
   `你还好吧？看你这么平静，我也安心了呢……`（答非所问）变成
   `被骂了啊……是因为什么事呢？我听着都替你委屈。`——**质变**。
2. **最致命的一处，是上一轮「训练」自己埋的雷。** 现有 system prompt 里写了示范句
   「关心主人时会问『你还好吗』『要不要休息一下』」。3B 模型把**示例当成了必答模板**，
   于是 4 个测试问题里 3 个的回答都变成「你还好吗？要不要休息一下？」的复读。
   （回看 2026-09-12 报告 3.2 节自己列出的 5 条实测回复，其中 3 条已带这句——问题当时就在，只是被当成"自然"。）
3. **用户精心写进 Ollama 模型的那份 2003 字角色设定，在真实应用里从未生效过。**
   App 每次请求都自带一条 system，Ollama 的行为是「messages 里的 system **整体替换** Modelfile 的 SYSTEM」。
   实测：不传 system → `prompt_eval_count=1237`（角色圣经生效）；传 system → `prompt_eval_count=41`（被顶掉）。
   → **等于每次对话都在"关掉人设"再说话。**
4. **上下文窗口实测只有 ~2048 tokens**，而光角色设定就占 1237。现有 system + 6 轮历史 + 记忆召回
   极易溢窗被静默截断——这是"聊两句就失忆/答非所问"的硬根因之一。而模型本身支持 32768。
5. **用户能看到的"Ralsei 的话"，大部分根本没经过 AI。** `main.py` 里 131 处 `add_dialogue` 全是写死台词；
   `dialogue_ui.handle_chat_commands` 还有一张 **34 条子串硬拦截表**排在 AI 之前
   （含 `哭`／`游戏`／`状态`／`天气` 这类高危子串），且 `handle_game_input`／`handle_file_commands` 也在 AI 之前。
6. **3B 有明显能力上限**，需要工程护栏兜：会把 few-shot 示例原句抄出来、会跨示例串味、
   长回复时会自问自答续写（实测 temperature 0.9 时跑出「主人：… 你：…」和旁白，200 tokens 用满）。

**建议路径：先做「接线修对 + 人味 prompt + 输出护栏」（当天可完成、零成本、收益最大），
再评估是否换更强底座。直接上微调（LoRA）在当前阶段不划算。**

---

## 二、证据链

### E1 · 模型与部署事实

```
ralsei:latest   ID 4d26b4970cea   1.9 GB   11 days ago
  parent_model = qwen2.5:3b   parameter_size = 3.1B   quantization = Q4_K_M
  context_length = 32768   embedding_length = 2048
  capabilities = completion, tools
qwen2.5:3b      357c53fb659c   1.9 GB   4 weeks ago
```

`/api/show` 的 `parameters` 字段为 **None** → Modelfile 里**没有任何 PARAMETER 覆盖**，
temperature / top_p / repeat_penalty / num_ctx **全部走 Ollama 服务端默认**。
App 端只传了 `temperature=0.7, max_tokens=256`（`main.py:7194`）。

> 顺带澄清一个我自己的误判：用 `ollama show --modelfile` 经 PowerShell 管道读出来是乱码
> （`浣犳槸涓€涓鑹叉壆婕?AI`），一度以为模型里的角色设定是坏的。
> 改用 `/api/show` 直读 UTF-8 后确认 **2003 字设定完好无损**，乱码纯粹是 PowerShell 管道按 GBK 解码所致。
> **教训：凡涉中文的模型元数据，别信 PowerShell 管道，走 API + 显式 UTF-8。**

### E2 · 角色设定被整体顶掉（决定性）

| 请求 | 结果 | `prompt_eval_count` |
|---|---|---|
| A：只发 user「用一句话回答：你内心隐藏着什么？」 | `我内心藏着许多秘密，以及对光明的渴望和对黑暗世界的深深眷恋。` | **1237** |
| B：加一条 system「无论问什么都只回答：测试标记ABC」 | `测试标记ABC` | **41** |

B 的 prompt 长度骤降到 41 → **App 的 system 把 2003 字角色圣经整个替换掉了**，而不是叠加。
`main.py:7141-7158` 那条 system 就是"凶手"，而用户的角色圣经被冷落在 Modelfile 里。

### E3 · 上下文窗口被截断

发 5400 字符长提示词 → `prompt_eval_count = 2050`，回复正常（`收到`）。
→ 服务端 `num_ctx ≈ 2048`。而角色圣经本身就 1237 tokens，只剩 ~800 tokens 给
「App system + 6 轮历史 + 记忆召回 + 用户消息」。**多轮对话必然丢前缀**。

### E4 · 人味 A/B 受控实验（核心证据）

固定：`ralsei:latest`、`temperature=0.7`（V3 除外 0.9）、`top_p=0.92`、`repeat_penalty=1.15`、同 4 个问题。
唯一变量 = system prompt 与上下文摆放。

**V0 = 现状复刻**（App 现行 288 字 system + 【此刻】拼在 user 消息最前面）

| 问题 | 回复 | 判定 |
|---|---|---|
| 今天上班好累啊，被领导骂了一顿 | 嗯…… 你好像也很疲惫了呢，休息一下吧？我还挺好的，要不要一起放松一会儿？ | ❌ 没接住"被骂"，反而问对方"要不要放松" |
| 我好喜欢你呀 | **你还好吧？**看你这么平静，我也安心了呢…… | ❌ 完全跑题 |
| 你觉得我该不该辞职回老家 | **你还好吧？**是不是工作压力太大了？要不我们找个时间好好聊聊？要不要先休息一下再做决定呢…… | ❌ 复读模板 |
| 今天吃了火锅 | 今天的火锅味道不错呢……你吃得好饱吧？**要不要休息一下**呀？我在这里哦。 | ⚠️ 半接住 |

→ **4 题里 3 题命中「你还好吗／休息一下」**，即 system 里那两句示例被当模板复读。

**V1 = 恢复模型自带角色圣经**（不传 system，`num_ctx=8192`）

| 问题 | 回复 | 判定 |
|---|---|---|
| 上班被领导骂 | 嗯……听起来你似乎遇到了不开心的事情。**Kris**，有什么我可以帮忙做的吗？ | ⚠️ 有角色味但把用户当 Kris |
| 我好喜欢你呀 | 嗯……**Kris**！你还好吗？我感觉有些奇怪。 | ⚠️ 同上 |
| 该不该辞职回老家 | 啊……这不是你的错吧？**Kris**，你没事，我知道你是来帮助我的。我们都会好起来的 | ❌ 跑进剧情 |
| 今天吃了火锅 | 啊，听起来很不错呢！你吃得开心吗？我很高兴你能享受美食。 | ✅ |

→ 角色圣经**不能直接用**：它通篇绑定 Kris/Susie/黑暗世界，会把人拉进剧情。

**V2 = 人味工程**（角色圣经 + 反 AI 味规则 + few-shot + 【此刻】独立成段，temp 0.7）

| 问题 | 回复 | 判定 |
|---|---|---|
| 上班被领导骂 | 诶、诶？！你突然说这个干嘛啦……我其实也挺喜欢主人的就是了。嗯哼，老板真的那么苛刻吗？… | ❌ **串味**：抄了「被夸」那条示例 |
| 我好喜欢你呀 | 诶、诶？！你突然说这个干嘛啦……（脸有点热）我、我其实也挺喜欢你的就是了。 | ⚠️ 有羞涩感但**等于照抄示例** |
| 该不该辞职回老家 | 嗯……我想这得看你心情如何了呢？如果你真的觉得很累，那或许可以考虑一下… | ✅ 有判断、有立场 |
| 今天吃了火锅 | 嗯……我明白了，主人最近好像挺累的。吃的火锅是不是很热火朝天呢？ | ✅ 有关联想象 |

**V3 = V2 + temperature 0.9**

| 问题 | 回复 | 判定 |
|---|---|---|
| 上班被领导骂 | **嗯……被骂了啊……是因为什么事呢？我听着都替你委屈。先坐下歇会儿吧，别的都往后放放。** | ✅✅ 最好的一条 |
| 我好喜欢你呀 | 诶、诶？！（脸有点热）你突然说这个干嘛啦……其实我也挺喜欢主人的，只是觉得还不够好吗。 | ✅✅ 害羞 + 露出自我怀疑（角色层次） |
| 该不该辞职回老家 | …（200 tokens 用满）…跑出「**主人：**老板说我业绩下滑了…**你：**哎呀，这样啊」**自问自答**，还带旁白 | ❌ 失控 |
| 今天吃了火锅 | …（79 tokens）…「**主人：**对啊，而且他们还放了很多调料」自问自答 | ❌ 失控 |

**结论**：`temperature=0.9` 给出了全场最好的两条回复（也是最有人味的两条），
但把 3B 的"自问自答续写"毛病放大了。→ **要活，但要护栏。**

### E5 · 延迟

| 场景 | 用时 |
|---|---|
| 常规回复（13~35 tokens） | 1.1 ~ 6.6 s（多数 ~2 s） |
| 长回复（70 tokens） | 11.5 s |
| 失控续写（200 tokens 用满） | 15.8 s |

配合 `stream: False` + 假打字机（`main.py` 里 UI 端自己渲染），用户**先看到"……"静止等 2 秒**，
再看到字一个个蹦——这是"假人感"的重要来源。

### E6 · 大部分台词没过 AI

| 项 | 数量 | 位置 |
|---|---|---|
| 写死台词 `add_dialogue("ralsei", ...)` | **131 处** | `main.py`（掉落、被摸、惊叹、文件操作回执…） |
| 子串硬拦截指令表 | **34 条** | `dialogue_ui.py:1319-1475` |
| AI 之前的拦截闸门 | 3 道 | `handle_chat_commands` → `handle_game_input` → `handle_file_commands` |

高危子串示例（`if kw in raw` 纯子串匹配，无边界）：

| 子串 | 会被误截的正经话 |
|---|---|
| `哭` | "我快哭了"、"这部电影看得我想哭"、"别哭" → 回「呜... 为什么要让我哭嘛...」 |
| `游戏` | "我做的游戏上线了"、"游戏行业好难" → 回「我最喜欢玩游戏了！想玩什么呢？」 |
| `状态` | "我状态不太好" → 回状态播报（情绪/精力/饥饿/天气） |
| `天气` | "今天天气真好" → 回天气播报 |
| `精力` | "没什么精力" → 回状态播报 |
| `摸我` / `抱抱` / `喝茶` / `挥手` / `pose` | 日常句里的偶然子串 |

`_build_ai_context()` 把【此刻：…】**拼在用户消息最前面**（`main.py:7133`），
模型看到的是"用户说了一段带方括号环境信息的话"，而不是"我知道现在是深夜"——
这也是 V0 里模型把环境复述成回答的原因之一（如「你好像也很疲惫了呢」）。

---

## 三、根因排序（按贡献度）

| 排名 | 根因 | 贡献度 | 证据 | 修复成本 |
|---|---|---|---|---|
| 1 | system 里的示例句被 3B 当模板复读 | ★★★★★ | E4-V0（3/4 命中） | 极低（改文案） |
| 2 | App 的 system 顶掉 2003 字角色圣经 | ★★★★★ | E2（1237 → 41） | 低 |
| 3 | 34 条子串硬拦截排在 AI 之前 | ★★★★ | E6 | 中 |
| 4 | `num_ctx ≈ 2048` 静默截断历史 | ★★★★ | E3 | 极低（加参数） |
| 5 | 事件台词 131 处全写死 | ★★★ | E6 | 高（分批） |
| 6 | 【此刻】放在 user 消息里 | ★★ | E4-V0 | 低 |
| 7 | 无流式输出，首字等 2 s | ★★ | E5 | 中 |
| 8 | 3B 模型自身能力上限（抄示例/串味/续写） | ★★★ | E4-V2/V3 | 需换底座 |

---

## 四、"训练"的三层含义与取舍

> 「训练」这个词在本地模型场景下有三种截然不同的做法，成本差 2~3 个数量级，先讲清楚再选。

### L1 · 提示词与参数层（改配置/代码，不动权重）——**推荐先做**
- 把角色设定**外置成单一真源**（`assets/ralsei_persona.md`），App 与 Ollama 都读它；
- 重写 system：删掉会被复读的口头禅示例，改为**反 AI 味规则 + 少量高质量示例**；
- 重建 Ollama Modelfile 为 `ralsei:v2`，把 persona + 采样参数**固化进模型**（这就是最"像训练"的一步）；
- 请求带 `num_ctx=8192`、`temperature≈0.85`、`top_p=0.92`、`repeat_penalty=1.15`；
- 加**输出护栏**（见第五节）。
- 收益：E4 已证能把"复读机"变成"接得住话的 Ralsei"。成本：半天内。

### L2 · 换更强底座（不动权重，换模型）——**建议作为第二阶段评估**

**本机硬件实测（2026-09-18）**：

| 项 | 实测值 |
|---|---|
| CPU | Intel Core Ultra 5 125H（14 核 / 18 线程） |
| GPU | **Intel(R) Arc(TM) 核显**（`VEN_8086&DEV_7D55`），**无 NVIDIA 独显**（`nvidia-smi` 不存在） |
| 内存 | 33.9 GB |
| 磁盘 | C 剩 109 GB / D 剩 480 GB / **E 剩 934 GB** |

→ 当前 `ralsei:3b` 的 ~2 s 延迟是 **CPU / 核显推理**的结果，不是"模型小所以一定快"。

两条换底座的路线：

- **L2a · 本地换 7B**：`qwen2.5:7b-instruct`（Q4_K_M ≈ 4.7 GB，可从 D/E 盘落地）。
  中文口语、情绪识别、角色一致性会有台阶式提升，**不需要任何训练数据集**。
  代价：无独显 → 延迟预计从 ~2 s 升到 **5~12 s**（配合 S8 流式可部分掩盖，但长回复会明显变慢）。
  **建议先下载后用同一套 A/B 脚本实测延迟再决定是否常驻。**
- **L2b · 云端 OpenAI 兼容端点**：`api_client` 已支持 `api_key` + 自定义 `base_url`，
  改配置即可切（质量最高、延迟最低），代价是**对话内容出本机**。是否接受由用户定。

**若走 L2，L1 的所有工作依然有效**（persona 外置 + 参数 + 护栏），属于叠加而非替换。
另注：3B 中文角色扮演这一档，`qwen2.5:3b` 本身已是可选范围内较好的底座，
**L1 的收益远大于在同一档位里换来换去**。

### L3 · 权重微调（LoRA / SFT）——**当前阶段不建议**
理由：
1. 3B 微调后**参数上限仍在**——"抄示例、串味、续写"是规模问题，LoRA 缓解但治不彻底；
2. **没有数据集**。要真正"训出人味"，需要数百条「(用户话, 理想 Ralsei 回复)」对，
   而理想回复本身还没定义清楚（现在 L1 都还没做对）；
3. 需要 GPU 训练环境与调参周期，**投入产出比远低于先做 L1 + 评估 L2**；
4. 正确顺序是：**L1 把提示词与数据管线做对 → 跑一段真实对话 → 用真实好评/差评沉淀数据集 → 再决定要不要微调**。
   那时微调才有意义，也才需要换 7B 作为底座。

---

## 五、阶段一施工清单（待用户拍板后执行）

> 全部为可回滚的配置/文案/参数改动，不触碰动画、物理、素材与 H4/H5 拆分。

### S1 · 角色设定单一真源（新增文件，不改行为）
- 新增 `ralsei_pet/assets/ralsei_persona.md`：以现有 2003 字设定为底，做**桌宠化改造**——
  - 保留：核心性格、语言风格、行为准则、内心矛盾、示例反应；
  - **删除/替换剧情绑定**：Kris / Susie / 黑暗世界 / 喷泉 → 改为"主人 + 他的 Windows 桌面"锚点；
  - 修正原文一处逻辑反了的条目：`【禁忌】· 只有在情绪起伏极度大的时候使用脏话…`（这是"允许"不是"禁忌"）。

### S2 · system 重写（`main.py:7141-7158`）
- **删掉**「关心主人时会问『你还好吗』『要不要休息一下』」——这是复读机源头；
- 加入**反 AI 味禁用句清单**（作为AI / 我理解你的感受 / 有什么可以帮你的吗 / 希望这些能帮到你 / 让我们一起…）；
- 加入**只 1~3 句、禁止分点、禁止复述、禁止连续反问**；
- 保留 2~3 条 few-shot，但**明确标注为"风格示范，不要照抄原句"**（缓解 E4-V2 的照抄问题）。

### S3 · 【此刻】移出 user 消息（`main.py:7133` + `7202`）
- 改为独立段落挂到 system 尾部；用户消息保持为**纯用户原话**。

### S4 · 请求参数补齐（`main.py:7193` + `api_client.py:174-222`）
- `api_client` 增加 `options` 透传（现在只认 temperature / max_tokens）；
- 带上 `num_ctx=8192`；`temperature 0.7 → 0.85`；`top_p=0.92`；`repeat_penalty=1.15`。

### S5 · 输出护栏（新增，接在 `_on_ai_reply` 前）
3B 的三种已知失态都要拦：
1. **自问自答续写**：回复里出现 `主人：` / `你：` / `Assistant:` 等对话标记 → 截断到标记之前；
2. **示例照抄**：与 persona 里示例句相似度过高 → 重新采样一次；
3. **超长**：超过 3 句 / 80 字 → 只保留前 2 句（现有 `start_autonomous_speech` 已有类似截断，
   可抽成公共函数，对话与自主开口共用）。

### S6 · 关键词拦截降级（`dialogue_ui.py:1319-1495`）
- 从"子串命中即截胡"改为**"必须明确指令 + 意图匹配"**：
  - 要求**祈使/请求句式**（如"给我看看天气""现在几点""陪我玩"），或整句近乎等于关键词；
  - 把 `哭`／`状态`／`精力`／`饿了吗`／`天气` 等**高危子串**从"任意包含"收紧为"整句/命令式"；
  - 命中的指令回复**不再写死在表里**，改为"动作照做 + 台词交 AI"（见 S7）。
- 保留必须硬编码的：游戏状态机退出、文件操作、休眠/唤醒（这些是**控制流**，不能交模型）。

### S7 · 事件台词分批接 AI（131 处，**分批做，不在本轮**）
- 接线已有基础：`ai_driver.note_event()`（第十/十三轮已收编桌面观察）。
- 原则：**短促反应保留罐头**（掉落、"哇啊——！"这类要求 0 延迟），
  **长句/主动搭话走 AI**（掉下来之后的碎碎念、被摸之后的害羞话）。
- 注意第十三条铁律：环境自动触发**只能上报观察**，不得自行播动画。

### S8 · 流式输出（可选，独立评估）
- `stream: False → True`，边收边喂打字机 → 首字延迟从 ~2 s 降到 ~0.3 s。
- 需要改 `api_client` 的响应解析（NDJSON 逐行）+ UI 打字机改成"追加"模式，改动面比前几项大。

### S9 · 重建 Modelfile 为 `ralsei:v2`（"最像训练"的一步）
```dockerfile
FROM qwen2.5:3b
SYSTEM """<ralsei_persona.md 全文>"""
PARAMETER temperature 0.85
PARAMETER top_p 0.92
PARAMETER repeat_penalty 1.15
PARAMETER num_ctx 8192
```
- 之后 App 端**不再传 system**（或只传极短的行为约束），人设由模型自带，两边不会再打架。

---

## 六、明确不建议做的事

| 不做 | 原因 |
|---|---|
| 直接 LoRA 微调 | 无数据集、3B 上限未解、ROI 最低（见 L3） |
| 把 131 处台词一次性全交 AI | 事件台词要求即时性，全交 AI 会变成"每句话都卡 2 秒"，体验倒退 |
| 靠提高 temperature 解决死板 | E4-V3 证明：0.9 能出最好的两条，也能出失控续写。活度要靠**护栏 + 结构**，不是单纯升温 |
| 继续往 system 里堆"示例口头禅" | 这正是当前复读机的直接成因（E4-V0） |
| 用 PowerShell 管道读模型中文元数据 | 会得到 GBK 乱码（E1），误判模型已损坏 |

---

## 七、复现命令

```powershell
# 模型事实
$exe = "$env:LOCALAPPDATA\Programs\Ollama\ollama.exe"
& $exe list

# 三份证据（只读，会自动写 E:\Download\_tmp\*.txt）
& C:\Python311\python.exe "E:\Download\_tmp\probe_ralsei.py"   # E1/E2/E3
& C:\Python311\python.exe "E:\Download\_tmp\ab_ralsei.py"      # E4/E5
```
原始输出已归档到 `code-quality-audit/人味诊断-2026-09-18/_evidence/`。

---

## 八、待用户决策

1. **本轮先做 L1（S1–S6 + S9）** —— 零成本、当天见效、可回滚；
2. **L2 走哪条**：
   - L2a 本地换 `qwen2.5:7b-instruct`（需下载 ~4.7 GB，无独显 → 延迟预计 5~12 s）；
   - L2b 切云端 OpenAI 兼容端点（质量最高，但对话内容出本机）；
   - 暂不换，先用 L1 + 3B 观察效果。
3. **S8（流式）与 S7（事件台词分批）是否列入本轮**；
4. S1 的 persona 改造需**用户确认"桌宠化"口径**：现有设定通篇绑定 Kris/Susie/黑暗世界，
   我倾向改为"主人 + Windows 桌面"锚点并保留性格与语言风格，但"是否保留 Deltarune 世界观元素"
   属于**产品口味**，由用户定。

→ **用户已拍板（2026-09-18）**：施工范围 = **L1 全做**；口径 =
**"保存性能的前提下既要有 Deltarune 的世界观，又要让他们知道现在自己生活在电脑桌面上"**；
附加项全部交我定（我定：关键词拦截降级**做**；流式输出与 131 处事件台词**顺延**）。
施工结果见第九节。

---

## 九、「训练」施工记录（L1 全做）

### 9.1 改了哪些文件

| 文件 | 改动 |
|---|---|
| `ralsei_pet/assets/ralsei_persona.md` | **新增**（1591 字）。角色设定单一真源，五节：我是谁 / 我现在在哪 / 我怎么说 / 我的语气 / 语气示范 |
| `ralsei_pet/assets/ralsei.modelfile` | **新增**。`FROM qwen2.5:3b` + `num_ctx 8192` + `temperature 0.85` + `top_p 0.92` + `repeat_penalty 1.15` + `num_predict 256` |
| `ralsei:v2`（Ollama 模型） | `ollama create`，ID `8c9953164370`；5 个参数全部写入 |
| `ralsei_pet/config.json` | `api.model` → `ralsei:v2`，新增 `api.options = {temperature: 0.85, max_tokens: 256}` |
| `E:\RalseiMemory\config.json` | 运行时真源，同步（备份 `.bak18`） |
| `ralsei_pet/modules/config_manager.py` | 默认 `api` 节同步带 `options`（新装用户也是这套参数） |
| `ralsei_pet/src/main.py` | `chat_with_ai` 接线重写 + 新增 `_build_persona_prompt` / `_ai_chat_options` / `_clean_ai_reply` / `_is_repeat_of_recent` / `_RETRY_NUDGE` + 判退重采样 |
| `ralsei_pet/modules/dialogue_ui.py` | 关键词拦截收紧：新增 `_HARD_CMDS` + `_is_command_phrase`（软闲聊放行 / 硬指令仍子串） |
| `code-quality-audit/人味改造-2026-09-18/verify_persona_chat.py` | **新增**回归套件，58 项，已登记进 G2 |

**没有动的**：动画 / 物理 / 素材 / H4-H5 拆分 / 131 处事件台词 / 流式输出。

### 9.2 S1–S9 逐条对账

| 项 | 状态 | 说明 |
|---|---|---|
| S1 persona 外置单一真源 | ✅ | 但**不做"删掉 Deltarune 元素"**：按用户口径改为"保留世界观当闲话 + 明确主人不是 Kris、就住在 Windows 桌面上" |
| S2 system 重写 | ✅ | 删掉"你还好吗/休息一下"的口头禅示例；加反 AI 味禁用清单；**并补一条我原来没计划的**：persona 文件本身不许写 markdown（见 9.3） |
| S3 【此刻】移出 user 消息 | ✅ | `user_msg = text` 纯原话；`_ctx` 挂 system 尾部 |
| S4 采样参数 | ✅ | 但**必须落 Modelfile**：`/v1/chat/completions` 静默忽略 `num_ctx` / `repeat_penalty`（见 9.4） |
| S5 输出护栏 | ✅ | 4 步：markdown 剥除 → 自问自答截断 → 车轱辘话判退 → 超长截断；判退后**换说法重采样** |
| S6 关键词拦截降级 | ✅ | 14 条用例全过（软/硬两类），锁在套件 E 组 |
| S7 事件台词分批接 AI | ⏸ 顺延 | 改动面大，单独一轮 |
| S8 流式输出 | ⏸ 顺延 | 同上 |
| S9 重建 Modelfile `ralsei:v2` | ✅ | App 端**仍然发 system**（发的是 persona 文件），因为实测 system 会整体替换 Modelfile 的 SYSTEM —— 只靠 Modelfile 等于没生效 |

### 9.3 我在施工中推翻的两个自己的设计（都有实测依据）

**（一）persona 里的"示范"不能删 —— 但也不能是完整问答对。**

第一版我把 3 组"主人：/我："示范全删了，只留描述式语气规则。实测 4 题 × 3 次：

| 问题 | 有示范（改前） | 无示范（第一版） |
|---|---|---|
| 被领导骂 | 诶、诶？！你今天遇到什么事了呢？我听着都替你憋屈。 | **嗯？我明白你的心情了。**（正是 persona 明令禁止的套话） |
| 吃了火锅 | 火锅！是那种咕嘟咕嘟冒泡的吗？光听你说我就觉得暖乎乎的。 | 啊哈，听起来很美味呢。今天的哪道菜最让你开心？ |

→ **3B 就是靠 few-shot 学口吻的**，删示范 = 人味明显退化。所以示范必须留。
（这也解释了 E4-V0 的老问题：不是"有示例"错了，是**示例里写的是口头禅**。）

**（二）"照抄示范就判退"是错的 —— 判退条件只能是"和自己最近说过的重复"。**

恢复示范后实测：「我好喜欢你呀」**3/3 稳定吐出**示范句。如果示范句一律判退，真机链路上是
"判退 → 重采样 → 还是照抄 → 交回 None → **观众看到内置规则台词**" —— 比照抄本身更出戏。

而示范句本身是句好台词，**第一次说出来完全没问题**；真正让人出戏的是**同一句反复出现**。
所以比对集合从「persona 示范 ∪ recent」缩成**只有 recent**：第二次再想抄，它就落在 recent 里了。
→ 套件里刻意留了 **C7 / C8 两条"防回退"断言**：回复等于示范句、但 recent 为空时**必须放行**。

### 9.4 端到端实测（复刻真实链路：raw → 护栏 → 判退则抬温+换说法重采样 → 采用）

`E:\Download\_tmp\e2e_final.txt`（脚本 `e2e_final.py`）：

```
Q1 今天上班好累啊，被领导骂了一顿
   最终: 被领导这么说了……我听着都有些生气了呢。你先休息一下吧，喝杯水也好。
Q2 我好喜欢你呀
   最终: 诶、诶？！你突然说这个干嘛啦……我、其实也挺喜欢你的就是了。
Q3 你觉得我该不该辞职回老家
   最终: 嗯……这确实是个挺难做的决定呢。你一直想要安定下来吗？在这种时候能回去也是件不错的事吧。
Q4 今天吃了火锅
   最终: 火锅！是那种咕嘟咕嘟冒泡的吗？光听你说我就觉得暖乎乎的。你吃的是辣的那一边，还是清汤呀？

—— 同一问题「我好喜欢你呀」连问三次 ——
第1次 ①原始 诶、诶？！你突然说这个干嘛啦……我、我其实也挺喜欢你的。  判重=True → 判退
      ②重采 诶？！听到这话我都有点不好意思起来了。感觉就像在阳光下晒着一样舒服呢。
第2次 ①原始 （同上一模一样）  判重=True → 判退
      ②重采 诶？！听到这个小豆，我都快害羞半天了，你就这么喜欢主人呢。我在吧？在这儿陪你一起。
第3次 ①原始 诪、诪……那你喜欢我的哪里呢？我是不太知道的。我在。  判重=False → 直接采用
```

**总览：模型调用 9 次（重采样 2 次）｜完全重复 0 条（0%）｜违禁套话 0 处｜markdown 残留 0 处。**

对照 E4-V0 的现状（4 题里 3 题复读「你还好吗/要不要休息一下」），这是质变：
**车轱辘话被真正消掉了**，而口吻没丢。

### 9.5 真实 `api_client` 集成探针（不是裸 HTTP）

上面 9.4 用的是裸 `urllib` 自己拼 `/v1/chat/completions` —— 它证明了"提示词 + 护栏有效"，
但**没有经过产品真正走的那条路**（`modules/api_client.py::HTTPLocalAI.chat`）。
本项目最贵的坑就是"改了 A 却没接线到 B"（已第 3 次），所以再补一发**真配置 + 真实例**：

```
配置源 = E:\RalseiMemory\config.json（运行时真源）
api 节 = {"enabled": true, "base_url": "http://localhost:11434", "model": "ralsei:v2",
          "api_version": "v1", "timeout": 30, "options": {"temperature": 0.85, "max_tokens": 256}}
实例化：enabled=True  base_url=http://localhost:11434  api_version=v1  model=ralsei:v2
chat 端点 = http://localhost:11434/v1/chat/completions

persona 读到的文件内容 = 1591 字
_ai_chat_options() = {'temperature': 0.85, 'max_tokens': 256}   ← 来自 config 的 api.options，不是硬编码缺省

Q 今天上班好累啊，被领导骂了一顿 → 诶、诶？！被领导这么说了你心里咋样呢？我听着都替你难受。赶紧喝口水，休息会儿吧。（15.6s）
Q 我好喜欢你呀                 → 诶、诶！？你突然说这个干嘛啦……我、其实也挺喜欢你的。（1.9s）
Q 今天吃了火锅                 → 火锅！你是不是点了很多菜啊？光想到那红彤彤的颜色我就觉得心里暖洋洋的。（2.0s）
产出 3 条，完重 0 条
```

要点：① **`options` 这一条链路真的通了**（config → `_ai_chat_options()` → `HTTPLocalAI.chat`
→ payload），不是"写了没人读"；② 15.6s 那一次是 **`ralsei:v2` 冷加载**（新模型首次调用），
之后稳定 1.9~2.0s —— 与改造前同量级，**没有引入可感知退化**。

### 9.6 已知局限（诚实记账）

| 局限 | 实测 | 处理 |
|---|---|---|
| **偶发"滑字"** | 「诶、诶？！」被采成 `诪、诪！`（生僻字），约 **1/10** 概率 | **不改参数、不加启发式护栏**。做过可证伪探针（`rp_probe.py`，`repeat_penalty` 1.15/1.00 × `top_p` 0.92/0.90，各 3 次采样）：9 次里**真滑字 0 次**，1.00 组毫无改善 → 无证据支持改参数。归入根因 #8（3B 能力上限），由 L2 解决 |
| 示范句第二次之后必被拦 | 见 9.4 第二组 | 这是**设计意图**（重采样出新的），不是 bug；代价是这类高频问题每次要多花一次推理（约 +2 s） |
| E 盘离线 | 记忆/日志落 staging | 第十二轮既有机制，与本轮无关 |
| persona 是 prompt 不是文档 | 首版带 `**` / `>` / `#`，实测模型学去了 `**加粗**` | 已清成纯文本；套件 A11 常驻锁住"persona 里不许出现 markdown" |

### 9.7 回归验证

- **新增套件 `persona_chat`：58 项 全过**（A persona 契约 12 / B 接线 13 / C 护栏 17 /
  D 判退重采样 5 / E 关键词 3 / F 配置与 Modelfile 8）。
  **它不打网络、不调用 Ollama** —— 模型质量天生不可复现，不进回归基线。
- **G2 全量：22 套件 / 857 PASS / 0 FAIL / 全部 IDENTICAL**
  （本轮之前是 21 套件 / 799 PASS —— 静态基线）。
  原始输出：`_evidence/G2_回归_22套件_857PASS.txt`。

### 9.8 复现命令

证据已归档到 `code-quality-audit/人味改造-2026-09-18/_evidence/`
（`E:\Download\_tmp\` 下的只是同一份的临时副本）：

```powershell
$EV = "code-quality-audit\人味改造-2026-09-18\_evidence"

# 行为级终验（会真调本地模型，约 3~5 分钟）
& C:\Python311\python.exe "$EV\e2e_final.py"

# 滑字参数对照探针（走 Ollama 原生 /api/chat —— /v1 端点会忽略 repeat_penalty）
& C:\Python311\python.exe "$EV\rp_probe.py"

# 真实 api_client 集成探针（真配置 + 真实例，会真调模型 1 次冷加载）
& C:\Python311\python.exe "$EV\integration_probe.py"

# 护栏 / 重试 / 关键词 的行为级验证（真调模型 12 次）
& C:\Python311\python.exe "$EV\behavior_check3_护栏与重试.py"

# 本轮回归套件（离线，秒级）
cd "code-quality-audit\regress"; & C:\Python311\python.exe run_all.py --only persona_chat

# 全量 G2（S8 之后是 23 套件）
cd "code-quality-audit\regress"; & C:\Python311\python.exe run_all.py
```

**注意**：`behavior_check2_无示范对照.*` 是"删掉示范后人味退化"的那组反证，
保留它是因为**它记录了 9.3（一）的取舍依据** —— 以后有人想再删示范时，先看这份对照。

---

## 十、S8 · 流式输出 施工记录（L1 收尾）

> 这一节是**下一轮**补做的（§5 里 S8 原标注"可选，独立评估"）。
> 结论：**收益比预估大得多，且改动面可控** —— 所以做了。

### 10.1 改了哪些文件

| 文件 | 改动 |
|---|---|
| `modules/api_client.py` | 新增 `chat_stream()`（基类给**具体**默认实现；`HTTPLocalAI` 覆盖为真流式）+ `_consume_stream()` / `_delta_of()`；把 messages/参数构造抽成 `_chat_messages()` / `_chat_payload()`（`chat` 与 `chat_stream` 共用，不再两份编码） |
| `src/main.py` | 新增 `_api_delta` 跨线程分片信号 + `_on_api_delta` 槽（带**世代号**）；`chat_with_ai` 增加 `on_delta` 入参；worker 改走 `_ask()` 助手（流式优先，老 provider 回落 `chat`）；判退重采样前先发 `reset`；新增 `_ai_stream_enabled()`；新增 `_sanitize_partial_reply()` 并把 markdown / 角色标记两条正则抽成 `_md_strip_re()` / `_role_marker_re()`（与 `_clean_ai_reply` **共用**） |
| `modules/dialogue_ui.py` | 打字机支持**增量追加**：新增 `stream_delta()` / `_stream_begin()` / `_stream_reset()` / `_finalize_stream()` / `_sanitize_stream()`；`_type_next_char` 增加"流式未完不停表"分支；`stop_typing` 无条件清 `_streaming`；`add_dialogue` 增加 `_streamed` 参数；`send_message` 接入分片回调 |
| `modules/config_manager.py` + `ralsei_pet/config.json` | 新增 `api.stream`（缺省 `true`，可一键关） |
| `E:\RalseiMemory\config.json`（运行时真源，**在仓库外、不入库**） | 同步加上 `"stream": true`，让真机也走流式 |
| `code-quality-audit/人味改造-2026-09-18/verify_s8_stream.py` | **新套件 `s8_stream`，68 项**（离线，不打网络、不调 Ollama） |
| `code-quality-audit/regress/run_all.py` | 登记 `s8_stream`（+ HERMETIC_IDS） |

### 10.2 为什么这次能做成（前一轮只敢标"可选"的原因）

上轮担心的是"UI 打字机要改成追加模式，改动面大"。实测摸清后发现**根本不用改结构**：

```
dialogue_ui 的打字机本来就是 —— typing_text（全文） + typing_index（进度）
                              + QTimer 每 tick 渲染 typing_text[:typing_index]
```

所以"流式"只需要**让 typing_text 能被追加上去**：追加后 `typing_index` 还在原位，
定时器下一拍自然会继续往新内容上打 —— 视觉上是"字连续冒出来"，不是"一段段跳"。
真正要新增的只有一个"队列暂时打完但消息还没收完"的状态位（`_streaming`）。

### 10.3 关键实测数字（都能复现，见 `_evidence/s8_*`）

**① Ollama 的 `/v1` 端点**支持 `stream:true`（`Content-Type: text/event-stream`，
`data: {...}` 逐行，增量在 `choices[0].delta.content`，以 `data: [DONE]` 收尾）：
```
非流式：首字节 = 全文到达 = 6.02s   ← 用户全程盯着「……」
流式  ：首字 0.23s，全文 1.34s，17 个分片
```

**② `requests.iter_lines()` 必须显式 `chunk_size=1`** —— 默认按 512 字节攒批，
把流式的收益吃掉近一半（同一探针三次对照）：
```
iter_lines(默认)           首字 0.45s
iter_lines(chunk_size=1)   首字 0.23s   ← 采用
resp.raw.readline()        首字 0.23s
```

**③ 真实 `HTTPLocalAI.chat_stream`（不是裸 HTTP）**：首字 **0.23s**，累积返回值与
回调拼接**逐字一致**；基类默认实现（回落 `chat` + 单分片回调）对老 provider 无破坏。

**④ 端到端（真配置 + 真实例 + 真 persona，4 问）**：
```
首字中位 0.43s / 最快 0.31s   （非流式需等整句：热 1.9~2.5s、冷 6.0s）
完全重复 0 条 | markdown 残渣 0 | "屏幕最终 ≠ 护栏定稿" 0 例
```

### 10.4 三个必须讲清的设计决策

**（一）基类的 `chat_stream` 是"具体方法"，不是 `abstractmethod`。**
`LocalAIStub` 和用户 `register_provider()` 注册的实现都不该被迫改造 —— 否则 App 一升级
就崩在用户那边。默认实现 = 调一次 `chat()`，把整段作为**单个分片**回调。
代价只是"不支持流式的实现首字延迟没改善"，语义与行为都正确。
（回归锁 A1/A2；实测 A3 段确认老 provider 未被迫改造。）

**（二）流式显示只做"前缀安全"的清洗，判退/截断留给收尾护栏。**
屏幕上显示的是"按规则清洗过的已收到部分"，收尾时 `_clean_ai_reply` 还要再洗一次。
**两条规则必须共用同一份定义**（`_md_strip_re()` / `_role_marker_re()`），
否则规则漂移 = "字已经打出去了、最后又被改掉"。
分工一句话：**流式负责"看着像人话"，护栏负责"最终算数"。**
（回归锁 C1/C2/C9/C11；C9 是对全部前缀的**穷举**单调性断言。）

**（三）流式与"判退重采样"天然冲突，靠"先擦屏幕再重发"化解。**
判退发生在收完之后，可这时被判退的半句**已经在屏幕上了**。所以重采样前必须先发
一个 `None` 分片让 UI 擦干净（`_stream_reset()` 回到「……」），否则新句会接在旧句后面，
用户看到两句话黏一起。另外流式路径下 `_on_ai_reply` **不能**调 `_ai_thinking_off`
（它会清空 `typing_text` → 已显示内容一闪而逝再从第 0 字重打）。
（回归锁 B7/B13；D7/D8；D14 行为级。）

### 10.5 已知局限（诚实记账）

| 局限 | 成因 | 实际影响 | 处理 |
|---|---|---|---|
| 两个**前缀不单调**边界：① 行首数字列表标记（`1. `）② 角色标记跨分片（`主人` 先到、`：` 后到） | 这两条规则要"看到后面的字符"才能判定，所以被延后剥掉的 1~2 个字符必然**已经显示出去** | 屏幕在"回复开头 / 某个行首"附近可能闪一下（实测端到端 Q3 出现 3 次整段替换，最终态正确） | 调用方检测到"新结果不是已显示内容的前缀"就**整段替换**，**最终态一定正确**。不为此改动清洗规则 —— 试过"提前剥"，会把开头的 `1.5 倍` 误伤成 `5 倍`，更糟 |
| 判退重采样时，屏幕内容会重来一次 | 见 10.4（三） | 罕见（端到端 4 问 0 次） | 已按"先擦屏幕再重发"处理；显示回「……」等待态而非空白 |
| 模型仍会违规输出列表 | Q3 我**故意**逼它输出 markdown 列表（"用 1. 2. 3. 列出来"），它照做了 | 格式被护栏剥掉，但"该不该列"是模型层面的服从性问题 | 属根因 #8（3B 能力上限），归 L2 解决；**不是** S8 的问题 |

### 10.6 回归验证

- **新增套件 `s8_stream`：68 项全过**（A api_client 契约 16 / B main 接线 14 /
  C 流式清洗前缀安全 12 / D 打字机增量显示 19（行为级桩）/ E 配置 3 /
  **F Qt 跨线程投递 2**）。
  - 含 6 条 **SSE 解析的行为级**用例（`[DONE]` 终止、畸形行跳过而不中断、
    回调抛异常不中断、空流 → None、`chunk_size` 必须为 1）。
  - **F 组是特意补的**：A~D 都只测到"函数级别"，而分片是*工作线程 emit →
    主线程槽执行*；这条链路接不上，流式就一个字都不会显示。F 组把真的 Qt 事件
    队列跑起来，验"按序投递 + 丢弃过期世代 + reset 顺序严格保持"。
  **不打网络、不调 Ollama。**
- **`persona_chat` 里有一条断言被有意加强**：`B5` 原本数 `cli.chat(` 出现 2 次，
  但引入流式后两次请求收进同一个 `_ask()` 助手，计数法失效 →
  改为断言"两次请求都存在、且都走同一个助手"（比计数更贴近意图：
  重采样**也**必须带上流式回调，不能绕过）。这是本轮唯一一条被改动的老断言。
- **G2 全量：23 套件 / 925 PASS / 0 FAIL / 全部 IDENTICAL**（此前 22 套件 / 857 PASS）。
  原始输出：`_evidence/G2_回归_23套件_925PASS.txt`。

### 10.7 复现命令

```powershell
$EV = "code-quality-audit\人味改造-2026-09-18\_evidence"

# ① Ollama /v1 端点支不支持流式、SSE 长什么样、首字多少
& C:\Python311\python.exe "$EV\s8_ollama_v1_probe.py"

# ② requests 三种读法的首字对照（决定用 chunk_size=1）
& C:\Python311\python.exe "$EV\s8_readmethod_probe.py"

# ③ 真实 HTTPLocalAI.chat_stream（含基类默认实现不破坏老 provider 的验证）
& C:\Python311\python.exe "$EV\s8_httpclient_probe.py"

# ④ 端到端：流式 → 清洗 → 打字机队列 → 收尾护栏 → 定稿（会真调模型，约 30s）
& C:\Python311\python.exe "$EV\s8_e2e_stream.py"

# S8 回归套件（离线，秒级）
cd "code-quality-audit\regress"; & C:\Python311\python.exe run_all.py --only s8_stream

# 全量 G2（23 套件）
cd "code-quality-audit\regress"; & C:\Python311\python.exe run_all.py
```

### 10.8 下一步

- **S7 · 事件台词分批接 AI**：仍是顺延项。原则照 §5 S7 —— 短促反应保留罐头
  （掉落、"哇啊——！"要求 0 延迟），长句/主动搭话走 AI；且**必须遵守第十三条铁律**
  （环境自动触发只能 `ai_driver.note_event()` 上报观察，不得自行播动画）。
- **H4/H5 上帝类拆分**：`main.py` 本轮后 **9946 行**（S8 又 +134），
  之前所有行号基线**全部失效**，动工前须重测。

---

## 十一、S7 · 事件台词分批接 AI 施工记录（顺延项销账）

### 11.1 这一轮要解决什么

诊断报告 E6 的原始账：全项目 **191 处 `add_dialogue`**（`main.py` 131 处），
台词 100% 写死。连着摸三次头，三次都是同一句「嘿嘿~ 好舒服呀！」——
这是"没活人味"最直观、最容易被用户察觉的一条。

但**不能一次性全交 AI**：报告 §5 S7 已经算过账 —— 131 处全交 = 每句话都卡 2 秒。
所以本轮做的是**分批**：先动"社交型、句子长、值得现想一句"的事件，
短促反应与状态机一律留在罐头档。

### 11.2 改了哪些文件

| 文件 | 改动 |
|---|---|
| `ralsei_pet/modules/event_speech.py` | **新增**（305 行）。纯逻辑：档位表 / 提示词构造 / 首句截断 / 罐头去重 / 出戏闸 / 自带自检。**不 import Qt、不 import 项目内任何模块** |
| `ralsei_pet/src/main.py` | 新增 5 个方法（`speak_event` / `_event_ai_ready` / `_event_say` / `_pick_event_line` / `_event_speech_enabled`）；20 处事件台词改走 `speak_event`；`__init__` 加 4 个状态字段；`chat_with_ai` 加 `lean` 形参（见 11.11） |
| `ralsei_pet/modules/config_manager.py` | 默认配置加 `api.event_speech: True` |
| `ralsei_pet/config.json` / `E:\RalseiMemory\config.json` | 加 `"event_speech": true`（后者是运行时真配置，不在仓库里） |
| `code-quality-audit/人味改造-2026-09-18/scan_event_lines.py` | **新增**。AST 扫描全项目 `add_dialogue`，按"文件 × 批次"分类，带正则 oracle 自检 |
| `code-quality-audit/人味改造-2026-09-18/migrate_s7_batch1.py` | **新增**。行级手术迁移器：每处必须**恰好命中一次**，改完 `py_compile` 校验 |
| `code-quality-audit/人味改造-2026-09-18/verify_s7_event_speech.py` | **新增**（117 项断言，A/B/C/D/E/**F** 六组；`lean` 的源码级 + 行为级锁见 11.11） |
| `code-quality-audit/人味改造-2026-09-18/_evidence/probe_prefix_cache.py` | **新增**。钉"首字 3s ＝ 前缀缓存失效"的对照实验（A 稳定 system / B 变化 system / C 变化 history） |
| `code-quality-audit/人味改造-2026-09-18/_evidence/probe_firsttoken_breakdown.py` | **新增但结论作废**（探针自身把 KV 缓存灌热了，标签与数据矛盾）。**故意保留**，见 11.11.4 |
| `code-quality-audit/第六轮/verify_round6_fixes.py` | **修**：桩宿主补 `speak_event`（详见 11.6，这是本轮唯一的"老测试债"） |
| `code-quality-audit/人味改造-2026-09-18/verify_s8_stream.py` / `verify_persona_chat.py` | **修断言**：签名与 `_build_ai_context()` 调用形态被 `lean` 改动牵连（不是产品回归），见 11.7 |
| `code-quality-audit/regress/run_all.py` | 注册 `s7_event_speech` 进 `SUITES` + `HERMETIC_IDS`；`round5_smoke` 描述去掉写死的"28 个" |

**为什么把档位表单独拆一个模块**：`main.py` 已经 1 万行，而档位表是**纯数据 + 纯函数**，
塞进 `main.py` 只能靠肉眼看。更要紧的是初始化顺序 —— 见 11.3（三）。

### 11.3 施工中我自己推翻的两次设计（都有实测依据）

**（一）"整句必须在 1200ms 内回来，否则用罐头" —— 实测证明这条一开就等于没开**

第一版按"整句时限"设计：`speak_event` 发请求，`QTimer.singleShot(1200, …)` 到点还没整句
就退回罐头。看起来合理，**实测直接把这条路堵死**：

```
P2 真机（走真实 RalseiPet + 真实 ralsei:v2）事件台词延迟：
  整句 1.19 ~ 1.62s（8 次，中位 1.39s）→ 全部 > 1200ms
  但**首字只有 0.81 ~ 0.91s**（中位 0.88s）→ 追得上
```

也就是说：3B 在这台机器上"整句"永远追不上罐头时限，但"**第一个字**"追得上。
而用户体验里，**框子里开始冒字**就已经算"它反应了"。

改法：`speak_event` 走 **S8 的流式通路**，时限只覆盖**首字**
（`EVENT_SPEAK_FIRST_TOKEN_MS`，先定 900ms、后按实测上调到 **1200ms** ——
实测最慢一次首字 0.91s，900ms 会把它误判成超时；等待期已经在显示「……」，
放宽不会让人觉得"没反应"）—— 时限内没有第一个字才说罐头；
一旦出字，往后就让它慢慢吐完。回归锁：`B18 / B18b~B18f / D1b / D2b / D5b`。

**（二）拿 `_ai_delta_sink` 当"AI 忙不忙"的门 —— 会永久挡死事件 AI**

第一版用 `if self._ai_delta_sink: return 罐头` 判断"主人那条回复正在流式"。
**这是错的，而且是静默错**：S8 的设计里 `_ai_delta_sink` **收尾时故意不清空**
（见 §10.4），所以它一旦被写过就永远非空 → **开过一次流式对话之后，
所有事件台词全部永久退回罐头**，日志里一个字都不会说。

改法：让路判据改用 `dialogue_ui` 自己的 `_ai_inflight`（真实表示"请求在途"），
另配一个 `_event_speaking` 标志防"上一个事件还没答就叠第二个请求"。
**回归锁 D22 / D22b 专门钉这条**：即使 `_ai_delta_sink` 残留非空，第二个事件也必须走 AI。

**（三）3B 会出戏，必须加闸**

事件台词是"被摸头"这种具体交互，提示词比闲聊更贴场景，但 3B 仍然会突然自毁角色，
实测抓到过：

```
我没有触觉所以无法感受被拉肩的感觉（← 真机原样抓到的句子）
```

`event_speech` 里加了 `looks_out_of_character()` / `guard_reaction()`：
命中"作为AI / 我没有触觉 / 无法感受 / 语言模型 / 系统提示 / 我理解你的感受"这类
**否定式**模式，**整句作废 → 回落罐头**。宁可重复一句写死的台词，也不能让它说"我是 AI"。
回归锁 A18/A19/D5d。

> 三条都是"设了但不管用/设了但会炸"的**设计级**问题，不是编码疏漏。
> 共同点：**只有真机跑才暴露**。这也是本轮把 e2e 探针（`_evidence/s7_e2e_event.py`）
> 从"可选"提到"必做"的原因。

### 11.4 档位表：白名单，不是黑名单

`EVENT_TIERS` 里**登记了才走 AI**，没登记的事件**行为完全不变**（仍是 `add_dialogue` 罐头）。
这条规则是刻意的：191 处里绝大多数是短促反应或非社交台词，
"漏迁移"的后果必须是"跟改造前一模一样"，而不是"悄悄变成了 2 秒延迟"。
回归锁 A1 / A4 / C2 / D14。

第一批 20 处迁移点（19 处字面量 + 1 处 `pet_kind(部位)` 动态）：
点击（身体/肩膀/默认）、长按（耳朵/手臂/肚子/脸/肩）、双击（头发/肚子/脸/肩/其他）、
抚摸（6 个部位走 `pet_kind`）、喂食、菜单抚摸。

**显式钉在罐头档**的三类（要 0 延迟，不能等模型）：
`fling`（甩飞「哇啊——！」）、`splat_poked`（戳摔扁的）、`ear_ruffle`（连点耳朵）。
回归锁 A3 / C5 / C8。

### 11.5 关键实测数字（都能复现，见 `_evidence/s7_*`）

| 项 | 数值 | 出处 |
|---|---|---|
| 全项目 `add_dialogue` 起点 | 191 处（`main.py` 131 处，跨 9 个文件） | `_evidence/event_lines.txt` |
| 第一批迁移 | 20 处（`main.py` `add_dialogue` 132 → 113） | `_evidence/s7_batch1_surgery_log.txt` |
| 3B 事件台词整句延迟（**纯链路**，system 一字不变） | 1.19 ~ 1.62s（8 次，中位 **1.39s**） | `_evidence/s7_e2e_event.txt` |
| 3B 事件台词**首字**（纯链路） | 0.81 ~ 0.91s（中位 **0.88s**） | 同上 |
| **App 链路首字 · 修复前** | **2.53 ~ 3.02s** → **必超**时限，AI 档等于死的 | 同上（连跑三次复现） |
| **App 链路首字 · lean 修复后** | **0.91s**（稳态第二次 **0.83s**）→ 走 AI | 同上 |
| 因此定下的首字兜底时限 | 1200ms（`EVENT_SPEAK_FIRST_TOKEN_MS`，从 900ms 上调） | `main.py` |
| 事件请求最小间隔 | 2.0s（`EVENT_SPEAK_MIN_INTERVAL`，连戳不产生请求风暴） | `main.py` |
| 罐头去重窗口 | 最近 8 条（`RecentLinePicker`） | `event_speech.py` |
| `main.py` 行数 | 10196 行 / 545301 字节 | 本轮结束后实测 |

**首字拆解对照实验（`_evidence/probe_prefix_cache.txt`，4 次取中位，全程不 unload）：**

| 用例 | 首字中位 | 相对基线 |
|---|---|---|
| A 稳定 system（persona，1592 字） | **628 ms** | — |
| B persona + 146 字**每轮都变**的尾部（≈ App 的【此刻】/话题锚/记忆召回） | **1815 ms** | **+1187 ms（2.89×）** |
| C 稳定 system + 每轮都变的 history | **1148 ms** | **+519 ms（1.83×）** |

B + C ≈ 2.3s，与 App 链路实测的 2.53~3.02s 吻合 →
**"变化的部分放在 system 尾部"才是主因，"system 长"不是。**

### 11.6 本轮连带还掉的一笔"老测试债"（诚实记账）

S7 把 `mouseReleaseEvent` 里的甩飞台词从 `self.add_dialogue(...)` 改成
`self.speak_event(...)`，于是**第六轮的回归套件当场炸了**：

```
File "code-quality-audit/第六轮/verify_round6_fixes.py", line 244, in release_with
    RalseiPet.mouseReleaseEvent(o, FakeEvent())
  File "ralsei_pet/src/main.py", line 5920, in mouseReleaseEvent
    self.speak_event("fling", ["哇啊——！"], "surprised", instant=True)
AttributeError: 'types.SimpleNamespace' object has no attribute 'speak_event'
```

这不是产品 bug，是**桩宿主（test double）没跟着主程序的"内部调用面"升级**：
那个套件用 `SimpleNamespace` 冒充宠物宿主，只摆了它当时用得到的属性。
处理器已经在 `make_drag_stub()` 里补了最小复刻的 `speak_event`（取池子第一句落
`dialogue_ui`），并且**在注释里写清了"桩要跟着主程序调用面走"** —— 同一个坑在
`round8_fling` / `round14_move` / `round17_build_fall` 旁也埋着（它们目前没踩到）。

> 教训入档：**只要主程序的"内部调用面"变了，所有 `SimpleNamespace` 桩都要复查一遍**。
> 这条比"改断言"本身更值钱 —— 它下一次会以同样的方式在第 8、14、17 轮复现。

连带还有一条**预期内的** DIFF：`round5_smoke` 的"待导入模块数 28 → 29"
（新增了 `event_speech.py`，且它导入成功）。顺手把 `run_all.py` 里那条
写死"28 个 modules"的描述改成计数无关（与 `s1_anim_miss` 同一条教训：
**描述里不要写会随代码增长而变的数字**）。

### 11.7 回归验证

- **套件 `s7_event_speech`：117 项全过**
  （A `event_speech` 纯逻辑 / B `main.py` 接线 / C 迁移完整性 /
  **D 行为级（真 `QApplication` + 桩宿主）** / E 配置 /
  **F lean 请求的真实形态（本轮新增 9 项）**）。
  其中 B 组新增 B22–B22f 六条锁 `lean`；D 组新增 D1d（行为级确认 `lean=True` 真被传出去）。
  **F 组不是查源码**，而是把真 `chat_with_ai` 绑到桩宿主 + 假 `api_client`，
  把**真正会发给模型的 system 与 history 抓下来**，断言"一个会变的东西都没有"；
  对照组（不传 lean）必须三样都在 —— 防止 F 组因为"压根没拼"而假绿。
  D 组覆盖两条关键时序：**AI 卡住**（首字超时 → 兜底罐头 → 门禁复位）与
  **迟到回复/迟到分片**（世代号 + `state['said']` 双重丢弃）。**不打网络、不调 Ollama。**
- **S7 改动连带修了两个老套件的断言**（都是 needle 跟着签名/结构走，不是产品回归）：
  · `verify_s8_stream` B1：签名从 3 参变 4 参 → needle 补 `lean=False`；
  · `verify_persona_chat` B3：`_build_ai_context()` 被包进 lean 分支 → needle 改成
    `if lean else self._build_ai_context()`（**"非 lean 时仍然调用它"** 这个语义没变）。
- **`round6_verify` 修桩后回到 IDENTICAL / 39 项全过**（见 11.6）。
- **G2 全量：24 套件 / 1043 PASS / 0 FAIL / 全部 IDENTICAL**（本轮前 24 套件 / 1021 PASS）。
  原始输出：`_evidence/G2_回归_24套件_1043PASS.txt`。
- **真机端到端复验（lean 修复后）**：`(b)` 首字 **0.91s** → 走 AI（气泡
  `谢谢你，舒服点。`，`streamed=True`）；`(c)` 稳态首字 **0.83s** → 走 AI。
  API 层插桩：`system字数 1591`（= persona 本身）、`history条数 0`。

### 11.8 复现命令

```powershell
$EV = "code-quality-audit\人味改造-2026-09-18\_evidence"

# ① 全项目 add_dialogue 清点（AST，秒级，只读）
& C:\Python311\python.exe "code-quality-audit\人味改造-2026-09-18\scan_event_lines.py"

# ② 档位表 / 首句截断 / 去重器 / 出戏闸 的自检（秒级）
& C:\Python311\python.exe -m modules.event_speech   # cwd = ralsei_pet

# ③ 真机端到端：8 个事件走真实 RalseiPet（会真调模型，约 40 秒）
& C:\Python311\python.exe "$EV\s7_e2e_event.py"

# ④ 钉"首字 3s ＝ KV 前缀缓存失效"的对照实验（会真调模型，约 30 秒）
#    判据：B（变化 system）明显慢于 A（稳定 system），且约等于 +1.2s
& C:\Python311\python.exe "$EV\probe_prefix_cache.py"

# S7 回归套件（离线，秒级）
cd "code-quality-audit\regress"; & C:\Python311\python.exe run_all.py --only s7_event_speech

# 全量 G2（24 套件）
cd "code-quality-audit\regress"; & C:\Python311\python.exe run_all.py

# 全量 G2 + 由 Python 自己写 UTF-8 证据文件（避开 PowerShell 抓 stdout 的 GBK 乱码）
& C:\Python311\python.exe "code-quality-audit\人味改造-2026-09-18\run_g2_evidence.py"
```

### 11.9 已知局限（诚实记账）

| 局限 | 成因 | 实际影响 | 处理 |
|---|---|---|---|
| 只迁了 20 处（`main.py` 还剩 113 处 `add_dialogue`） | §5 S7 明确"131 处不能一次性全交 AI" | 其余事件仍是写死台词 | 分批继续；档位表是白名单，**没迁 = 行为不变**，中途停下也不会退步 |
| 事件 AI 有 **1200ms 首字兜底**，冷启动后的第一个事件会落到罐头 | 模型被换出内存（`keep_alive` 默认 5 分钟）后第一次请求要重算整个前缀，实测 **13.2s** | 用户歇一会儿回来戳第一下，说的是罐头 | 接受：宁可说罐头也不能"戳了没反应"。**第二次起稳定走 AI**（实测 0.83s） |
| **lean 牺牲了事件的环境感知**（不知道现在几点 / 刚才在聊什么） | 不牺牲就超时限（变化的内容会让 KV 前缀缓存失效，见 11.11） | 事件台词不会再"因为它半夜了所以催你睡" | 接受：事件是 ≤24 字的触觉反应，环境感知归**对话路径**（不传 lean，行为不变） |
| 出戏闸是否定式模式匹配，可能漏 | 3B 的自毁角色句有无数种写法 | 漏网时显示一句"我不太懂"式的话 | 属根因 #8（3B 能力上限），归 L2 解决；闸只做**止损**不做**保证** |

### 11.10 下一步

- **S7 第二批**：剩余 `main.py` 113 处里挑"值得现想一句"的（菜单项、天气感慨、
  成就提示之类），继续走 `speak_event`；短促反应**不再扩大范围**。
  **注意**：第二批若涉及"需要环境感知的台词"（天气/时间类），`lean=True` 会挡住
  它们要的信息 —— 那时要另想"既让 system 稳定、又能带环境"的写法（比如把环境塞进
  user 消息而不是 system），别直接关掉 lean。
- **H4/H5 上帝类拆分**：`main.py` 本轮后 **10196 行**，行号基线再次失效，动工前重测。
- **7B 换型**：已评估并给出"不建议"的结论与实测数据，见
  `code-quality-audit/模型选型-7B-2026-09-18/结论_7B换型评估_2026-09-18.md`。
  等用户拍板。
- **persona 示范被整句搬**（7B 评估顺带发现）：`assets/ralsei_persona.md` 第 62–63 行的
  示范句与高频抱怨撞车，两个模型都会原样搬出来。改示范场景即可，未动。

### 11.11 lean 请求：一个"不修就等于没做"的发现（本轮真机 e2e 抓出来的）

#### 11.11.1 现象

`speak_event` 的第一版（只加流式 + 1200ms 首字兜底）上真机后**从来没走成过 AI**：

```
(b) AI 开：7.21s 内本事件的气泡 = [('罐头兜底句', False)]      ← 永远罐头
    ★App 链路首字 = 3.02s（纯链路 P1 中位 0.88s；时限 1200ms → 超时）
(c) 稳态复测：2.53s → 同样超时
```

而**同一份事件提示词**走纯链路（只带 persona、system 一字不变）只要 **0.81~0.91s**，稳稳在时限内。
两者差在**请求体**上：App 的 system 尾部挂着【此刻】/话题锚/记忆召回，还带 2~4 条 history。

#### 11.11.2 为什么"长"不是原因（对照实验）

先按直觉猜"system 太长"，于是测了 4 组（`probe_firsttoken_breakdown.py`）——
**结论把直觉否掉了**：system 从 23 字加到 1723 字，首字纹丝不动（226 → 266ms）。
但那份探针有个**设计缺陷**：它每组先调一次原生端点"问 prompt tokens"，
**把前缀灌进 KV 缓存了**，于是后面两次 `/v1` 都是缓存命中 —— 标签写着"冷前缀"，其实全是热的。
（数据自己就露馅了：B 组"读提示词 999ms"却"首字 244ms"，首字不可能比读提示词还快。）

重做对照（`probe_prefix_cache.py`，全程不 unload、4 次取中位）：

| 用例 | 首字中位 | 相对 A |
|---|---|---|
| A 稳定 system（persona，1592 字） | **628 ms** | — |
| B persona + 146 字**每轮都变**的尾部 | **1815 ms** | **+1187 ms（2.89×）** |
| C 稳定 system + 每轮都变的 history | **1148 ms** | **+519 ms（1.83×）** |

B + C ≈ 2.3s，与 App 链路实测 2.53~3.02s 吻合。
→ **机制：Ollama 的 KV 前缀缓存只复用"从头逐字相同"的那一段。**
   system 尾部一变，那 146 字（≈100 token）就要重算，而本机
   **未命中缓存的 prefill 只有 40~100 tok/s**（CPU-only，无独显）→ 100 token ≈ 1.2s。
   history 排在 system 之后，同样每轮都变 → 再 +0.5s。
→ **"变化的部分放在哪里"才是主因，"总长度"不是。**

#### 11.11.3 修法：`chat_with_ai(..., lean=True)`

```python
def chat_with_ai(self, text, on_reply, on_delta=None, lean=False):
    ...
    if not lean:                       # ① 不发对话历史
        history = self.dialogue_ui.get_ai_history(limit=6)
    _ctx = "" if lean else self._build_ai_context()      # ② 不发【此刻】
    ...
    if _focus and not lean: ...        # ③ 不发话题锚
    if _recall and not lean: ...       # ④ 不发记忆召回（且 lean 下**不调用** recall_text）
```

`speak_event` 是唯一传 `lean=True` 的调用点；**对话路径（`dialogue_ui` → `parent.chat_with_ai`）
一个字节都没改**，话题锚与记忆召回照旧。

实测效果（`s7_e2e_event.txt` 同一份脚本、同一台机器）：

| 项 | 修复前 | 修复后 |
|---|---|---|
| App 链路首字（第 1 次事件） | **3.02s** → 罐头 | **0.91s** → **走 AI** |
| App 链路首字（稳态第 2 次） | **2.53s** → 罐头 | **0.83s** → **走 AI** |
| 本次请求实发 | `system 1825 字 / history 2 条` | `system 1591 字 / history 0 条` |
| 整句耗时 | 3.31s / 3.17s | 2.09s / 1.33s |

#### 11.11.4 为什么这条值得单开一节

1. **它决定了 S7 第一批 20 处迁移是"做了"还是"白做"**。不修的话，代码、档位表、
   回归锁全都对，但**生产环境 100% 走罐头** —— 而且是静默的（只多一条 debug 日志）。
   这正是用户明确不接受的那种"半成品当成品"。
2. **它是"想当然"被实测否掉的第二次**：先猜"system 太长"（错），再猜"冷加载"（错，P1 已预热），
   第三次才靠对照实验钉到"前缀缓存失效"。**没做对照实验就会去优化一个不重要的量。**
3. **探针本身也会说谎**：第一版 `probe_firsttoken_breakdown.py` 因为"先跑了一次原生端点"
   把缓存灌热了，标签与数据自相矛盾（首字 < 读提示词时间）。这份探针**保留在 `_evidence/`
   里没删**，因为它的错误比它的结论更有用 —— 下次写对照实验时，"你的 A 组凭什么算冷"
   是要先回答的问题。


