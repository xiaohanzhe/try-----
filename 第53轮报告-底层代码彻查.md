# 第53轮报告 · 底层代码彻查（除 AI 与场景系统）

> 时间：2026-09-27　范围：`ralsei_pet/` 全部 Python（**89 个 .py / 47,696 行**）
> **排除**：AI 线（`pet_ai` / `ai_driver` / `api_client` / `memory_*` / `worldview_recall` /
> `search_summarizer` / `event_speech`）与场景伙伴线（`scene_*` / `companion*` /
> `npc_system` / `item_*`）。
> **状态：只读审查，未改任何产品代码。** P0 的补丁见 §4.1（等你发话再改）。

---

## 1. 结论摘要

| 级别 | 数量 | 代表 |
|---|---|---|
| **P0** | 1 | `animation.fps = 0` ⇒ **启动期 ZeroDivisionError**（唯一保护函数零调用） |
| **P1** | 9 | 实测：**`config.json` 8 天没写进去**（设置改不动）／`delete_file(confirm=True)` 假确认／`_movable` 用 `os.rename` 探测在 E 盘恒失败 ⇒ **降级与迁移双双失效** |
| **P2** | 14 | 成规模的未接线子系统／`monitor_*.py` 覆盖被跟踪文件／`SysListView32` 跨进程读内存静默返空 |
| **P3** | 15+ | **432 条级联死代码**（占全部 1613 个 def 的 27%）、`_DEPRECATED` 陈旧副本、无用导入 |

### ★ 本轮最有价值的三条（都不是"清单长度"）

**① 查清了你的 E 盘问题在代码侧到底发生了什么（§3.1）**

实测三组数据：`E:\RalseiMemory\config.json` 最后写入 **2026-09-19 10:32**（8 天前）、
`memory.json` **2026-09-23 02:07**（4 天前）、而兜底目录 `C:\Users\23002\Desktop\memory\`
**是空的（0 个文件）**。⇒ **设置 8 天没保存成功**（这正好解释了你看到的
"`config.json` 里 `api.model` 一直卡在旧的 4B 值"），**记忆停在 4 天前**，
而**兜底链路被创建了却从未接住任何数据**。

**② 一条"两条缺陷叠加"的 P0（§4.1）**

`config_manager.validate_config()` 是**唯一**会把非法 fps 回退成默认值的地方 ——
而它**全项目零调用**；`main.py:1363` 又把 `int(1000 / fps)` 写成 `get()` 的
**默认参数**（实参表达式**先于调用求值**）⇒ 手改 `config.json` 为 `0` 不再是"值不对"，
而是**打不开程序**。

**③ 死代码是"未接线"而非"写错了"（§5）**

1613 个 def 里 **432 个从入口不可达**；模块级只有 5 个从未被 import（其中
`pet_interaction` 364 行属本轮范围）。**"写了但没接线"是本项目的主导缺陷形态**。

---

## 2. 审查方法与证据

### 2.1 流程（六步，全部落盘可复现）

静态基线（硬证据）→ 6 组并行**只读**深审 → **逐条亲自复核 P0/P1** →
运行时/字节码取证 → 结论 → 交付。

### 2.2 四个自研工具（只读，不产 `.pyc`）

| 工具 | 作用 |
|---|---|
| `_tools/baseline53.py` | pyflakes + `ast.parse` 语法基线 + 零引用穷举 + 同作用域重复定义 |
| `_tools/wiring53.py` | 模块接线图（谁 import 谁 / 谁从未被 import） |
| `_tools/cascade53.py` | **可达性（级联死代码）**：从模块级入口反向传播"谁真的会被调用" |
| `_tools/summarize53.py` | 把两份死代码清单按模块归类、标注是否本轮范围 |

**刻意不用 `compileall`**（会写 `.pyc`，而仓库历史上跟踪过 `.pyc`）⇒ 语法检查走 `ast.parse`。

### 2.3 静态基线结果

```
files = 89
pyflakes: 125 条 —— 其中 undefined name = 0（这类项目最高发的缺陷本轮为 0）
ast.parse: ok=89  fail=0
真·同作用域重复定义 = 0
跨文件同名 = 88 组
零引用：A 类（真零引用）227 / B 类（仅 docstring 提及）30
可达性：defs=1613，可达 1181，不可达 432，整类皆死 7
模块接线：56 个 modules 中，只有 5 个从未被任何生产代码 import
```

### 2.4 ★★ 我的工具自己踩的 4 个坑（全部已修，写在这里是为了下轮不再犯）

| # | 症状 | 真因 | 修法 |
|---|---|---|---|
| 1 | "同作用域重复定义 11 处" | 键取 `(scope, name)`，而**顶层函数的限定名就等于函数名** ⇒ 13 个模块各写一个 `get_logger` 被误判 | 键改 `(文件, 限定名, 类型)` ⇒ 真重复 = **0** |
| 2 | `MemorySystem.assoc` 被判重复 | 它是 `@property` + `@assoc.setter`，**Python 的正常写法** | 跳过 `setter/deleter/getter` 装饰器 |
| 3 | 死链入口 `react_to_file_emotionally` **没进**零引用清单 | 引用面把源码文本里所有标识符都算了，而 `file_sheet_controller.py` 的 **docstring 提过它** | 拆成 hard（`Name`/`Attribute`/`getattr` 字面量）/ soft（其余字符串） |
| 4 | "20 个模块从未被 import" | 漏认两种真实写法：`from modules import X as Y`、**函数体内的延迟导入**（为打断循环依赖刻意写在函数里） | 补齐 4 种 import 形态 ⇒ 真实值 = **5 个** |

> ★ 第 4 条最危险：它会把"刻意延迟导入"的模块判成"死模块"，**比漏报更误导**。
> 口径：**判据过宽 = 恒真；判据过窄 = 误报 —— 两者同罪。**

---

## 3. ★★ 与你的 E 盘故障直接相关的实测取证

### 3.1 实测数据（只读取证，未做任何破坏性操作）

| 对象 | 实测结果 | 含义 |
|---|---|---|
| `E:\RalseiMemory\config.json` | 1245 B，**mtime 2026-09-19 10:32** | **8 天没写进去** ⇒ 你改的设置从未落盘 |
| `E:\RalseiMemory\memory.json` | 152809 B，**mtime 2026-09-23 02:07** | 记忆停在 **4 天前** |
| `C:\Users\23002\Desktop\memory\`（桌面兜底） | **存在但 0 个文件** | 兜底目录被创建了，**却从未接住任何数据** |
| `E:\RalseiMemory\.write_probe` | **残留存在（2 字节）** | `_probe_writable` 的探针**写得进、删不掉** ⇒ 判"不可写" |
| `E:\RalseiMemory\logs\` | `ralsei_pet.log.old` + **13 个 `.old.<epoch>`** | 迁移/改名逻辑**反复落选**的物证（1.6 小时内 13 个） |
| `zz_clean51.json` / `zz_probe.json` | 残留（我第 51 轮的探针） | E 盘删不掉 ⇒ **待 E 盘修复后清理** |
| 硬编码 C 盘路径普查 | **0 处**（3 处均为 `os.path.expanduser('~')`） | 这条**没有**问题 |

### 3.2 代码侧机制（读原文确认）

```
memory_store._is_writable_dir(dir)            # 111-124
  1. os.makedirs(path, exist_ok=True)   → OK（目录已存在）
  2. if os.access(path, os.W_OK): return True
     ★ 实测走不到这里 —— 否则不会有 .write_probe 残留
  3. 真探针：写 .write_probe → os.remove(probe)
     ★ 建得成、**删不掉** ⇒ except → return False

memory_store.find_device_dir(create=True)     # 127-159
  ⇒ 只有 _is_writable_dir 为真才返回设备目录
  ⇒ 实测返回 None ⇒ 走 fallback

memory_store.fallback_dir() = <桌面>\memory   # 176-178
data_store.data_root() → (staging_root(), 'staging')   # 108-113
  staging_root() = %LOCALAPPDATA%\RalseiPet    # 90-105
```

### 3.3 结论与建议

1. **读/写路径分裂是当前的真实状态**：设备目录被判不可写 ⇒ 一切都该走
   `桌面\memory` 或 `%LOCALAPPDATA%\RalseiPet`；但**兜底目录是空的**，
   而 vault 里的旧文件仍在被读 ⇒ 直接表现就是"**设置改了不生效、记忆不增长**"。
2. **`_probe_writable` 的设计是"目录可写即算可用"，验不出你这块盘
   "目录能建、既有文件不能改"这一种故障** —— 探针自己在盘上留了残留（`.write_probe`），
   下一次探针依旧失败，于是**降级判定长期为真**。
3. **`data_store._movable()`（207-238）用 `os.rename` 探"能否搬走"**，
   注释把它失败后回不去的那一支写成"**极罕见**"。但按你的实测，E 盘**既有文件本来就不可改名**
   （WinError 5），`os.rename(src, src+'.migprobe')` **第一支就会抛** ⇒ 返回 False
   ⇒ 该文件每轮进 `skipped` ⇒ **迁移永远不会发生**。
   ★ 同一项目里 `logger_utils` 已明确记录"E 盘 exFAT 的 `os.rename` 会抛 WinError 1"，
   并为此专门写了 `_SafeTimedRotatingFileHandler` —— **两处对同一个 API 的假设互相矛盾**。
   ⇒ 建议：把"被占用"与"此盘不支持改名"区分开（例如失败后改用"以追加方式打开试写"探测），
     或在卷级故障期间**跳过 rename 型探测**、直接把文件留在中转站。
4. **给用户的直接建议**：E 盘问题（chkdsk / 迁 vault）解决前，
   **改设置不会生效**这件事你应该知道；`config.json` / `memory.json` 的旧值
   可以直接看到（上面有 mtime）。

---

## 4. 逐条发现（★ 标注我是否亲自复核过原文）

### 4.1 【P0 · 已复核】`animation.fps = 0` ⇒ 启动即崩（两条缺陷叠加）

```
main.py:1362   self.animation_fps = self.config_manager.get("animation.fps", 6)
main.py:1363   self.animation_frame_delay = self.config_manager.get(
                   "animation.frame_delay", int(1000 / self.animation_fps))
                                          ^^^^^^^^^^^^^^^^^^^^^^ 实参表达式
main.py:1368-71  try: ... except (TypeError, ValueError): ...   ← 兜不到（异常发生得更早）
main.py:8616   base_delay = 1000.0 / self.animation_fps          ← 第二处除零
```

1. `config_manager.get()`（**279-294**，已读）只做 dict 遍历，**不做类型/范围校验**；
2. 唯一校验 fps 的是 `ConfigManager.validate_config()`（**521-579**，注释里写明
   "fps <=0 会导致除零错误"）—— 但它**零引用**（A 类清单第 27 行；持久化组独立确认）
   ⇒ **这套保护从未被执行**；
3. **运行时取证**：`def f(a, b=1/0): ...; f(1)` ⇒ **未传 b 仍先求值默认表达式** ⇒ ZeroDivisionError；
4. UI 侧 `fps_spinbox.setRange(1, 30)`（6950）只保"经 UI 改"不为 0
   ⇒ **手改 json 为 `0`（或写成 `"6"` ⇒ TypeError）⇒ 下次启动直接崩**。

**最小补丁（5 行；fps 正常时行为完全不变）**

```python
# main.py:1362-1363 替换为：
_fps_raw = self.config_manager.get("animation.fps", 6)
try:
    self.animation_fps = max(1, int(_fps_raw))     # 就地钳位，同时覆盖 8616 那处除零
except (TypeError, ValueError):
    self.animation_fps = 6
self.animation_frame_delay = self.config_manager.get(
    "animation.frame_delay", int(1000 / self.animation_fps))
```

> 更大的一步（**建议但需你拍板**）：把 `validate_config()` 接进启动流程，
> 让"非法配置回退默认"从死代码变回保护。这会改变启动期行为，故不在最小补丁内。

### 4.2 【P1 · 已复核】`delete_file(confirm=True)` 是**假确认**

`desktop_interaction.py:1516` 起的 `confirm` 参数**只 `log.debug` 打印**
（1529-1533 写着"模拟确认对话框"），**不弹任何窗**。
⇒ 任何调用者写 `confirm=True` 都会误以为有确认。这是**对外 API 的陷阱**。

★ **连带项（我判为"已防御"，不是 P0）**：`autonomous_agent.py:628` 以
`confirm=False, send_to_recycle=True` 调用；但 `_pick_action()`（**447-481**，已读）
**已把 DELETE 从候选动作删掉**（注释逐字："自主代理不应自动删除用户文件"）
⇒ `_execute_action` 的 DELETE 分支（626-628）**当前不可达**。

**残留风险（真实存在）**：DELETE 的台词（`ACTION_DIALOGUES` 113-116）、
`_enter_acting`（546）、`_mood_for_action`（705）都还留着 —— 只要将来有人把 DELETE
加回 `_pick_action`，或**从外部构造 `AutonomousTask(action=DELETE)`**，就会直落
`delete_file(confirm=False)`。⇒ 建议：自主代理路径**硬拒绝**删除，不要只靠上游不选它。

### 4.3 【P2 · 已复核】`monitor_*.py` 会覆盖**被 git 跟踪**的数据文件

- `monitor_coordinates.py:85` `open('coordinates.json', 'w')`
- `monitor_animation.py:93` `open('animation_history.json', 'w')`
- `git ls-files` 确认：**`ralsei_pet/coordinates.json`（56 KB）与
  `ralsei_pet/animation_history.json`（743 KB）都是被跟踪的仓库文件**。

⇒ 在 `ralsei_pet/` 下跑这两个遗留脚本，就会**改写并可能提交 743 KB 历史数据**。
建议归档到 `tools/` 或把输出路径参数化。

### 4.4 分组深审的其余发现（**我未逐条复核原文**，标注为"待验证"）

**主程序骨架组**（`main.py`，22 条；★ 我复核了它最关键的 3 条，**全部推翻**，见 §5）
- `_clamp_pos_to_desktop` 取"四角中点"判定所在屏 + `availableGeometry()` 只覆盖主屏
  ⇒ 跨屏/负坐标下夹紧可能失效（**待验证**；这条与既有契约"必用 `_virtual_screen_rect()`"
  相关，值得下轮实测）。
- `_persist_teleport_count` 的计算结果未被使用（**待验证**）。
- 5 个 `init_*` 方法零调用（我的工具独立确认其中若干，**已证实**）。

**持久化与配置组**（`config_manager` / `data_store` / `customization_system` /
`relationship` / `social_growth_system`，58 条）
- `config_manager.emit()` 的观察者路径可疑（`add_observer`/`remove_observer` 均零引用）
  ⇒ **观察者机制整体无效**（**待验证**，但"两个方法零引用"我已独立确认）。
- `relationship.py` 的分级（`classify` 331 + `low` 336 未用）：报告称有 6 层退化
  （**待验证**：需确认等级阈值是否可达）。
- `customization_system.py:401-413` 四个变量读出未用（pyflakes 已确认存在，**待验证**含义）。

**桌面/窗口/文件安全组**（`desktop_interaction` / `floor_manager` / `global_hotkey` /
`pet_interaction`，43 条）
- `to_recycle_bin` / `_real_move` / `hard_delete` 的**"可恢复"被降级为永久删除**
  （**已证实存在**；与 memory 里第 46 轮遗留的"三处永久删除降级"同源，**仍未修**）。
- `SysListView32` 跨进程读内存失败时**静默返回空**（待验证细节）。
- `pet_interaction.py` **整模块零接线**（**已独立证实**，模块级 + 全部方法）。
- `global_hotkey` 注销用 `NULL hwnd`（待验证）。

**资源与媒体组**（`sprite_loader` / `sound_manager` / `video_controller` /
`file_sheet_controller` / `weather_system`，42 条）
- ★ 我复核后**修正**了它的核心结论：音效不是"整体未接线"，而是
  **12 个公开方法中 9 个零引用**（`play_splat`/`play_spell` 活着，见 §5.2）。
- `persona`/`WAV` 缺失时**静默返回 False** ⇒ 用户听不到任何提示（待验证）。
- `video_controller.py:393` `current_minute` 算出不用（pyflakes 已证；含义待验证）。

**UI 与文本组**（`dialogue_ui` / `dialogue_system` / `dr_textbox` / `bubble_system` /
`text_segmenter` / `command_manager`，39 条）
- ★ 复核后**修正**两条：项目里**不存在**"按字符宽度手动换行"，`text_segmenter` 是
  **jieba 分词接入层**；`dialogue_ui.py:1347` 的 `top` 未用才是真问题（`1651` 的 `low`
  其实是 `user_input.lower()`）。真正生效的换行是 Qt `QTextEdit` 的 `WidgetWidth`。
- 其余（气泡自绘协议、`DialogueLog` 组装等）**待验证**。

**状态行为组**（`emotion_system` / `energy_hunger` / `entertainment_system` /
`autonomous_agent` / `hide_controller` / `spell_controller` / `games_controller` /
`logger_utils` + 21 个遗留脚本，57 条）
- ★ 复核确认：`complex_emotion_decay_rates`（`emotion_system.py:221`，24 行）
  是**刻意废弃**（256-259 行注释主动说明"复合情绪不再独立衰减"）⇒ **不是 bug**。
- `spell_controller.py:395` `cond_frames_insufficient`（"帧数不足"的条件算出不用）
  ⇒ 疑似"帧不够时不播放"的守卫写了没用（**待验证**，pyflakes 已证变量存在）。
- 遗留脚本：`diag_spell.py` 里有 **`taskkill /F /IM python.exe`**（强杀进程）；
  `test_excel_creation.py` **在桌面真实建 xlsx 且不清理**（**待验证**）。

---

## 5. 明确复核为**误报 / 已防御**的项（10 条，不改，写明原因）

> 这一节是本轮流程的价值所在：**逐条亲自读原文**，推翻了一半"P0/P1"。

| # | 被报为 | 复核结论 |
|---|---|---|
| 1 | "`set_volume` 一点滑阀就崩溃（`min(255, 2)`）" | **误报**。`sound_manager.py:60-66` 本就有 `max(0, min(100, volume))` + `try/int` |
| 2 | "音效系统整体未接线" | **结论需修正**。`play_splat`/`play_spell` 是活的（`main.py:4449/4680/6218`、`spell_controller.py:331/458`）⇒ 是 9/12 零引用，不是整体死 |
| 3 | "P0：自主代理可自动删除用户文件" | **已防御**。`_pick_action`（447-481）已移除 DELETE ⇒ 分支不可达（残留风险见 §4.2） |
| 4 | "`_SafeTimedRotatingFileHandler` 整类皆死" | **我的工具误报**。`logger_utils.py:154` 实例化了它；`doRollover` 由 **logging 框架反调**，不在我的白名单里 |
| 5 | "`data_store.py:243` 硬编码 C 盘路径" | **误报**。243 行是 docstring；全项目**硬编码 C 盘路径 0 处** |
| 6 | "`_is_being_dragged` 恒 None，拖拽保护从未生效" | **误报**。`main.py:6197` / `6345` 明确置真（6193 注释说明为何提前置真） |
| 7 | "P0：`resume_from_video` 的 `emit()` 直接拉 `current_frame=0`" | **编造**。`resume_from_video` 与 `_video_hide_guard` 在代码里**都不存在**（grep 零命中） |
| 8 | "`on_animation_complete` 零调用 ⇒ once 动画播完永不回调" | **半误**。"零调用"成立，但回调由 `_play_once_callback` 独立消费（`9063-9064`）⇒ **不构成断链**；它只是个废弃空钩子（P3） |
| 9 | "同作用域重复定义 11 处" | **我的判据过松**。键未含文件名 ⇒ 实为 **0 处** |
| 10 | "20 个模块从未被 import" | **我的判据过窄**。漏认 `from modules import X as Y` 与函数内延迟导入 ⇒ 实为 **5 个** |

> **教训归口**：第 3/6/7/8 条来自"分组深审"，第 4/5/9/10 条来自"工具/转述"。
> ⇒ 无论哪一侧，**P0/P1 一律亲自读原文**；**agent 会编造函数名**（第 7 条），
> **也会把"有默认值"读成"从未赋值"**（第 6 条）。

---

## 6. 「代码与实际功能相符性」专章

| 声称（注释 / docstring / 函数名 / 配置项） | 实际 |
|---|---|
| `validate_config()` 会校验并回退非法值 | **全项目零调用** ⇒ 从未执行 |
| `delete_file(confirm=True)` | `confirm` **只打日志**，不弹窗（假确认） |
| `to_recycle_bin` / `_real_move` / `hard_delete` | 失败**降级为永久删除**（与"可恢复"矛盾） |
| `_movable()` 的"极罕见：回不去"分支 | 在本机 E 盘是**常态** ⇒ 迁移恒不执行（§3.3） |
| `_is_writable_dir()` 判"目录可写" | 探针**只验目录**，验不出"目录能建、既有文件不能改" |
| `CustomizationSystem.get_available_*`（30+ 接口） | **一个都不被调用** ⇒ 自定义功能无从进入 |
| `ConfigManager` 的隐私 / 加密 / 使用数据 / 观察者 | **四组开关零调用**、无 UI |
| `SoundManager.set_volume` / 音量配置 | 音量**不可调**（方法零引用） |
| `EntertainmentSystem.start_game` / `tell_joke` … | **无可达入口**（连 `main.tell_joke` 也零引用） |
| `pet_interaction.py`（364 行，含手势/笔画检测） | **整模块零接线** |
| `react_to_file_emotionally` 及"文件情感反应"链 | **死链**（唯一"引用"来自别处 docstring）⇒ 罐头台词不会显示 |
| `interact_with_file/folder` | **刻意留的空壳**（注释写明）⇒【已防御】，不是缺陷 |
| `complex_emotion_decay_rates`（24 行表） | **注释已交代过的废弃死代码** ⇒ P3 |
| `on_animation_complete` | docstring 说"动画完成时的回调"，实际 `pass` + 零调用 |
| `_SafeTimedRotatingFileHandler` | **在线**（我误报过，见 §5.4） |
| `monitor_*.py`（"监控"） | 会**覆盖被跟踪的仓库数据文件**（§4.3） |
| `animation.fps <= 0 会导致除零`（config 注释） | 注释**说对了**，但**没人拦**（§4.1） |

---

## 7. 验证矩阵

| 验证项 | 手段 | 结果 |
|---|---|---|
| 语法/可编译 | `ast.parse` × 89 | **89/89 PASS** |
| undefined name | pyflakes | **0 条** |
| 同作用域重复定义 | AST 三元组 | **0 处** |
| 零引用穷举 | 双档引用面（hard/soft） | A 227 / B 30 |
| 可达性 | 入口反向传播 + 协议/框架白名单 | 1613 → 可达 1181 |
| 模块接线 | 4 种 import 形态 | 56 → 5 个未接线 |
| **P0 机理** | 运行时取证（默认参数即时求值） | **ZeroDivisionError 复现 PASS** |
| **E 盘实测** | 只读 stat / listdir（vault + 兜底 + 探针残留） | **7 组数据，见 §3.1** |
| 误报复核 | 逐条 Read 原文 | **推翻 10 条** |
| 破坏性操作 | —— | **0 次**（未删/未改任何用户或产品文件） |

---

## 8. 未修复遗留（按影响排序，等你拍板）

1. **P0 · fps 除零**（补丁已备，§4.1）—— 5 行，fps 正常时行为不变
2. **P1 · E 盘读写分裂 / 降级未接住**（§3）—— 这是**你体感最强的问题**
   （设置不生效、记忆不涨）。代码侧可做的：探针增加"试改既有文件"，`_movable` 区分
   "被占用"与"不支持改名"。但**根治要先把 E 盘修好**。
3. **P1 · `delete_file` 假确认** ⇒ 自主代理路径改成硬拒绝（§4.2）
4. **P1 · 三处"可恢复"降级为永久删除**（**第 46 轮就存在，至今未修**）
5. **P2 · 成规模的未接线子系统**：`CustomizationSystem`（39+43）、`pet_interaction`（整模块）、
   `EntertainmentSystem`（14+18）、`ConfigManager` 隐私安全组（12+13）、
   `SoundManager`（9）⇒ **这是产品决策**（补接线 / 删代码 / 留待用），我不替你选
6. **P2 · `monitor_*.py` 输出落到被跟踪文件**（§4.3）
7. **P3 · 死代码清理**（432 条级联 + 227 条 A 类）⇒ 建议分批：
   先删"注释已交代废弃"的（`_DEPRECATED` 系列、`complex_emotion_decay_rates`、`on_animation_complete`），
   再删整模块零接线的
8. **P3 · 仓库根散落脚本 21 个**（`test_*.py` / `diag*.py` / `monitor_*.py`）⇒ 归档到 `tools/`
9. **待验证清单**：§4.4 里标注"待验证"的项（跨屏 clamp、观察者机制、`SysListView32`
   静默返空、spell 守卫、遗留脚本的危险动作）⇒ 下轮用真机/字节码逐条落定

---

## 9. 工具与证据清单

```
code-quality-audit/第53轮-底层代码彻查/
├── _tools/baseline53.py      静态基线（pyflakes + AST + 零引用 + 重复定义）
├── _tools/wiring53.py        模块接线图
├── _tools/cascade53.py       级联死代码可达性
├── _tools/summarize53.py     死代码按模块归类
└── _evidence/
    ├── baseline_pyflakes.txt      125 条
    ├── baseline_syntax.txt        89/89
    ├── baseline_deadcode.txt      A 227 / B 30
    ├── baseline_dupes.txt         真重复 0 / 跨文件同名 88 组
    ├── wiring.txt                 接线图 + 85 个实例化点 + 动态导入 0
    ├── cascade_deadcode.txt       432 条不可达 + 整类皆死 7
    └── deadcode_summary.txt       按模块归类（标注本轮范围）
```
