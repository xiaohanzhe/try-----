# 代码质量复审报告 · 第十八轮（2026-09-17）

**主题：「建楼」缺口计划 · 批次 B —— 补上"层高闸门"（G3），并把换层全部接上跳/攀爬**

前置文档：`建楼要求复检与缺口计划_2026-09-17.md`（第十六轮，只读复检 + 4 批次计划）
上一批：`代码质量复审报告_2026-09-17_第十七轮.md`（批次 A：G2 下落判据 + G1 生气动画时长）
本轮执行：**批次 B（G3）**。

---

## 一、结论先行

| 项 | 结果 |
|---|---|
| 批次 B 缺口 | **G3 修完**：走进更高/更低楼层不再"静默提升 / 静默下降" |
| 本轮自检套件 | `code-quality-audit/第十八轮/verify_round18_climb.py` —— **53 项 PASS / 0 FAIL**（组 A–I + X0 自检） |
| G2 全量回归 | **21 套件 / 799 PASS / 0 FAIL / 全 IDENTICAL**（先只固化本轮新套件，再全量复跑复验） |
| 有意的输出变更 | **4 处**（`s1_anim_miss` / `s2_anim_json` / `s3_alias_legacy` / `round8_anim`），**全部只是"动画组数 109→112"引起的计数行**，PASS 计数与断言一条没变 |
| 用户可见的渲染变更 | **无**：`frame_container_size` 仍 `(222, 110)`（两处来源都是），自动扫描并入策略没动 |
| 真机 | 起得来、跑 30s **0 ERROR / 0 WARNING**、`112 组动画` 从 `animations.json` 载入、jieba 正常 |
| 证据编码 | `check_evidence_encoding.py --strict`：**0 损坏 / 0 告警**（7 个文本证据文件） |
| 提交 | **`4eae2d8`**（25 文件 / +1952 −21）；push `8c739df..4eae2d8`，`ls-remote` 复核远端 == 本地 |

一句话：**"站桌面上水平走进窗口就被瞬移上去"这条最违反常识的行为没了 —— 现在要么跳、要么爬，够不着就老实待在原层。**

---

## 二、G3：`check_window_movement` 的"静默提升"

### 根因

```python
old_h = old_floor.get('platform_height', 0)
new_h = new_floor.get('platform_height', 0)
if new_h < old_h:   ... start_falling()
elif new_h > old_h: _log.debug("楼层升高 …")      # ← 只有一行 debug，然后照旧赋值
self.current_floor = new_floor                     # ← 无论哪种情形都会走到
```

`get_current_floor(pos)` 返回的是"包含当前位置的**最前面**那块楼板"。宠物从桌面水平走进某个
窗口的可见区域时，`new_floor` 会直接变成那一层（甚至一次变好几层）——**不跳、不落、不摔**，
只是一个瞬时的层级跳变，连 z 序一起抬上去。

### 改法：加一道"这个人能不能换层"的闸门

```python
walk_induced = (new_h != old_h
                and bool(getattr(self, 'is_moving', False))
                and self.floor_manager.is_floor_valid(old_floor))
if walk_induced and self._start_climb_transition(old_floor, new_floor):
    return                                     # ← 已起跳/起爬，本拍结束
if walk_induced and new_h > old_h:
    return                                     # ← 够不着 → 保持原楼层（不退回静默提升）
if new_h < old_h:  ... start_falling()          # 够不着的下楼 → 服从重力（要求⑭）
elif new_h > old_h: ... 直接站新板              # 被动成因（要求⑪）不变
self.current_floor = new_floor                  # 只剩"非走路引起"才走这里
```

**入口条件只用既有属性**（`is_moving` + `is_floor_valid(old_floor)`），不为闸门新增任何状态 ——
历史回归套件用轻量桩驱动 `check_window_movement`，桩里没有 `is_moving`（`getattr` 取到 False）
→ 它们仍然走"被动"那条路，**行为逐字节不变**（这就是 21 套件全 IDENTICAL 的原因）。

四种情形一次说清：

| 情形 | 谁引起 | 走向 |
|---|---|---|
| 自己走进**更高**楼层 | 走路 | **跳/爬**过去（够不着 → 留原层） |
| 自己走进**更低**楼层 | 走路 | **跳**过去（够不着 → 服从重力掉落） |
| 用户关窗 / 挪楼板 | 被动 | 坠落链路不变（生气 ≥5s / ≥3s，批次 A 已锁） |
| 被更高的新窗口盖住 | 被动 | 直接站新板、不跳不掉（要求⑪） |

---

## 三、"跨度低用跳、跨度高用爬"

### 判据只在一处

用户口径：**"在楼层跨度较低的时候跳，跨度高的时候爬"**。
`start_jump` 是**唯一**同时看得到"起跳层"和"目标层"的地方，所以选型放在那里，两条入口
（层高闸门 / 贴边起跳）共用一份判据，避免各写一份漂移：

```python
CLIMB_SPAN_JUMP_MAX = 5                     # 相邻一层（platform_height 每层 +5）
def _jump_kind_for_span(span): return 'jump' if abs(int(span)) <= 5 else 'climb'
```

`_jump_anim_override` 只在选型后写一次，`handle_jump` 每帧**优先读它**再回落 `jump` /
`jump_ball` —— 否则第一帧切了攀爬素材，第二帧就被 `jump` 顶掉（"白切"）。H5 断言锁这条。

### 素材（用户指定，本轮登记）

| 组 | 帧 | 来源 |
|---|---|---|
| `climb_right` | 5 | 用户素材 `spr_ralsei_climb_1_0~4` |
| `climb_left` | 5 | **由上一组水平镜像落盘**（用户："相反方向的你就给他翻转一下就好"） |
| `climb_front` | 6 | 用户素材 `spr_ralsei_climb_0_degrees_0~5`（没有朝后的 ——"那样也用不上"） |

三组都进了**两张表**：`sprite_loader._builtin_animation_mapping` 与 `assets/animations.json`
（H5 S2 深度等价契约，`verify_s2` PASS=40）。

5 张镜像素材由一次性脚本生成（真实现在 `code-quality-audit/第十八轮/make_climb_left_assets.py`，
`QImage.mirrored(True, False)`，**幂等**：目标已存在且校验通过就跳过、不重写已入库文件），
**真落盘、真被跟踪**，不是靠 `alias_of` 顶替。

> 这条"左右两组文件名不同"（G5）**本身不成立**：把源图**原样复制**过去同样满足它，而"没镜像"
> 恰恰是唯一要防的事。所以 G10 改成**逐像素断言镜像关系**（采 5×5 点比较
> `left(x,y) == right(w-1-x, y)`），并配 G10b 自检"该判据对**未镜像的同一张图**必须报不符" ——
> **断言要能区分"做对了"和"什么都没做"**，否则它是恒真的假绿。

> **实渲染证据**：`_evidence/climb_frames_preview.png` —— 用真 `SpriteLoader` 把三组画出来
> （不是"字符串在表里"，而是"运行时真能画出这几张图"）。三行分别是朝右 / 朝左（镜像）/
> 朝前，**没有一个是灰块占位**。行序与色带图例见同名 `.txt`。

### 落点要"预留水平距离"

用户：**"要预留一定距离哦，别看着和垂直起跳一样"**。做法是**先让开再吸附**：

1. `_climb_hdir` 定"让开方向"（优先沿用走路朝向 `previous_direction`，否则按目标层几何）；
2. 在 `cur ± CLIMB_HORIZONTAL_RUN(90px)` 各取一个候选点；
3. `_climb_probe_landing` 把候选点吸附进目标层的**可见区域**（`nearest_visible_point`）
   并校验 `floor_visible_contains` + 位移上限 `CLIMB_LANDING_MAX_TRAVEL(320px)`；
4. `_climb_landing` 两个方向各试一次，按 `(位移是否 ≥ CLIMB_MIN_RESERVE, 位移大小)` 择优。

吸附有可能把水平位移吃掉（目标层在这一侧很窄），所以"两个方向都试 + 设最小保留量"是必需的 ——
A5/C5 断言的就是"落点与宠物确实留开了距离"。

---

## 四、摔扁门槛收紧到"落差 ≥ 两层"

用户口径：**"那个摔扁的机制需要在层数比较高且掉下来而非主动下来的时候才会触发哦"**。

- "**主动下来**"这一半**天然不成立**：主动下楼走的是跳/爬链路，压根不进坠落状态机；
- "**层数比较高**"这一半是新加的：落差 ≥ `FALL_SPLAT_MIN_DROP = 10`（两层）。

```python
def _landing_drop_height(pet, landed_floor):   # 起点层高 − 落点层高，夹到 ≥0
def _should_splat_on_landing(pet, landed_floor):
    return fall_speed > 150 and _landing_drop_height(pet, landed_floor) >= 10
```

`handle_gravity_fall` 的**两个**落点（窗口层 / 桌面）都改调这一个入口，裸的
`if self.fall_speed > 150:` 消失（H6 断言）。速度门槛沿用改前口径 —— 不引入新手感变量。

起点层高由 `start_falling` / `start_fall` 在**进入坠落时**记一次（`_fall_from_height`），
不另存第二份真源。

> F7 是这轮新加的**防御断言**：`_landing_drop_height` 必须夹到 ≥0。缺 `_fall_from_height`
> 时按 0 起算，落点若是窗口层就会算出**负数**。“落差”是物理量不是有向坐标差 —— 负值虽不会
> 误触发摔扁（`>= 门槛` 恒假），但任何按距离用它的人（阈值、惯性、音效强度）都会静默算错。

---

## 五、为什么**不**把"跨层"拆成逐层小跳（本轮最重要的一个取舍）

用户还说过："**他也不能一次性跳上跨度很高的楼层，需要用各个方向的攀爬动画去切换高低楼层**"。
最直觉的实现是"先跳到隔壁那层，再跳下一层"。**在这套几何下这样做会把宠物卡死**：

楼层名次**就是 z 序**。宠物之所以被判到第 N 层，正因为它的位置落在**最前面**那块楼板的
**可见区域**里 —— 而那正是"后面的第 N-1、N-2 层被它挡住"的地方，**中间层在该位置没有可见区域**。
于是"逐层"的第一步就吸附失败（`floor_visible_contains` 过不去）→ 每步都 False →
**宠物永远上不去**：静默提升是没了，但换成了"进不去窗口"，比改前更糟。

所以本轮对跨层采用：**换素材、不换落点** —— 跨度 > 一层时用攀爬素材，落点仍取目标层的
可见区域，并用 320px 的位移上限卡住，观感是"爬上去"而不是"一步蹦到顶层"。

这条推理**没有停在注释里**，而是被 I 组断言钉住了（`I1`：站在前层可见区域里的位置，
同一位置的下层**没有**可见区域；`I2`：两层名次确实不同）。谁以后动了可见区域 / 名次口径，
这两条先红，提醒回头读 `_start_climb_transition` 的 docstring 再决定要不要重做逐层攀爬。

---

## 六、验证

### 6.1 本轮自检套件（53/53）

`code-quality-audit/第十八轮/verify_round18_climb.py`，九组：

| 组 | 内容 |
|---|---|
| **A** | 上楼：真的起跳 / **没有静默提升**（`current_floor` 仍是桌面）/ 落点在可见区域 / **预留了水平距离** / 不起跳时也不坠落 |
| **B** | 跨度判据：跨度 10 → 用 `climb_right`；跨度 ≤ 一层 → 仍 `jump`；爬向 = 横向 left/right、无横移 → front |
| **C** | 下楼：走进更低楼层 → **起跳**而不是重力掉落 / 目标是那块更低的楼板 / 下落方向识别正确 / 也预留距离 |
| **D** | 被动成因不被误伤：关窗（`is_moving` 真/假两种）仍是 `start_falling('floor_removed')`；要求⑪ 仍不跳不掉 |
| **E** | 闸门兜底：上楼够不着 → **保持原楼层**；下楼够不着 → **服从重力**（且不带"用户行为"起因） |
| **F** | 摔扁门槛：一层不摔 / 两层摔 / **落点不是桌面也摔** / 速度不够不摔 / 落差 0 不摔 / 缺属性防御 / **夹到 ≥0** |
| **G** | 素材与登记：三组在 JSON 里 + 首帧正确 / 三组素材**全在磁盘上** / 左右两组**不是同一批文件** / **G10 逐像素断言镜像关系**（+G10b 判据自检）/ 在库返回名字、不在库返回 None、未知方向退 front、**连 `sprite_loader` 都没有也不抛异常** |
| **H** | 源码级：闸门与选型各只有一处 / 摔扁判定收敛成唯一入口 / 不再有裸的 `fall_speed > 150` |
| **I** | 设计前提（第五节那条推理的断言化） |

> 行为级断言优先：A/B/C/D/E 组都是用**真 `FloorManager` 的几何**（可见区域矩形相减 /
> `nearest_visible_point` / `adjacent_lower_floor`）驱动**真 `check_window_movement`**，
> 只把 `update_floors`（会枚举真窗口）冻结成空操作，保证断言不随"测试机上开着什么窗口"漂移。

### 6.2 G2 全量回归（21 套件 / 799 PASS / 0 FAIL / 全 IDENTICAL）

新增套件 `round18_climb` 已挂进 `code-quality-audit/regress/run_all.py`（第 21 个）。

**固化方式刻意只更新自己那一条**（`--update --only round18_climb`）：merge 模式下全量
`--update` 会把"本轮之外任何一处意外漂移"也一并固化成新基线，把真回归悄悄洗白；
只更新本轮那条，其余 20 条保持原样，下一遍的比对才有鉴别力。

四处有意变更**全部只是组数引起的计数行**，算术对得上：

| 套件 | 变更 | 解释 |
|---|---|---|
| `s1_anim_miss` | 组 109→112、载入 489→493、帧 1461→1482、样本 501→505 | +3 登记组 +1 自动扫描前缀组 |
| `s2_anim_json` | 同上；导出 21319→22156 字节、表内帧 377→393 | 表内 +16 = 5+5+6（正是三组帧数） |
| `s3_alias_legacy` | 同上 | legacy 仍 28 组、别名仍 4 组，没多没少 |
| `round8_anim` | 组 109→112、载入 489→493（**28/28 断言不变**） | 同上 |

**+4 个运行时组 = 3 个表登记组 + 1 个自动扫描前缀组**：
新落盘的 5 张 `spr_ralsei_climb_left_*` 除了被登记组吸收，还会被
`scan_and_group_assets` 按**文件前缀**另立一组 `spr_ralsei_climb_left`
（既有行为，`spr_ralsei_climb_1`…`climb_6` 那些组也是这么来的，一直如此）。
**+21 帧 = 16（三组登记帧）+ 5（那个前缀组）**。

**关键：`frame_container_size` 仍是 `(222, 110)`，两处来源都是** —— 新增帧（20x40~28x44）
都小于容器，没有把画布撑大。这是 H4/H5 明令不能动的东西（自动扫描一旦停用就会变成
`(136, 71)`，属**用户可见**变更）。

### 6.3 真机（本轮已跑）

`& C:\Python311\python.exe src\main.py`（真窗口、真桌面）→ 存活 30s 后按 pid 收掉：

- 日志只有两行启动 INFO：`已启用 jieba 分词` + `[anim-config] 已从 animations.json 加载 112 组动画`；
- **该时段 0 ERROR / 0 WARNING**（前面的 ERROR 全是 s2/s3 套件故意喂坏 JSON 留下的）；
- 进程干净退出，无残留（`killed=[18952] still alive=[]`）。

**只能用户做的肉眼确认**（离屏验证不到观感）：

1. 站桌面上水平走进某个**更高的**窗口区域 → 应当是**起跳 / 攀爬过去**，不是瞬移；
2. 走进**更低的**楼层 → 应当是**跳下去**，不是"自由落体";
3. 从**一层**窗口掉到桌面 → **不该**摔扁；从**两层及以上**掉下来 → 才摔扁；
4. 攀爬素材的观感（朝右 / 朝左镜像 / 朝前）是否符合预期。

### 6.4 证据

`code-quality-audit/第十八轮/`：

| 文件 | 用途 |
|---|---|
| `verify_round18_climb.py` | 本轮自检套件（53 项，已进 G2） |
| `run_round18.py` / `run_round18_g2.py` | 跑套件 / 跑 G2 并**由 Python 自己写**证据（不走 shell 捕获） |
| `make_climb_preview.py` | 用真 `SpriteLoader` 出**素材实渲染图**（另存 `_evidence/climb_frames_preview.png` + 图例 txt） |
| `make_climb_left_assets.py` | 生成/校验镜像素材（幂等；**逐像素断言镜像关系**，不是只比尺寸） |
| `_evidence/` | 套件输出、G2 两遍输出、4 份 DIFF 原文、素材渲染图与图例、镜像校验报告 |

**证据文件一律由 Python `open(..., 'w', encoding='utf-8')` 直接写**，不走 PowerShell 捕获 —— 见教训 5。

---

## 七、有意**没有**动的东西

| 项 | 状态 |
|---|---|
| 批次 C：G5 `climb_to_top_window()` 仍走裸窗口路径 | 未动（第十五轮遗留） |
| 批次 D：三条"有意偏差"（>400px 不跟随改失足 / `MIN_FLOOR_VISIBLE_AREA` / "立刻"=1 秒节拍） | 只登记，不改 |
| 逐层攀爬（"只能相邻"的字面实现） | **本轮判定不可行**，理由与断言见第五节 |
| `scan_and_group_assets` 的自动并入 | 一行没动（停用会产生用户可见的画布变化） |
| 跳跃几何 / z 序 / 可见区域判定 / `floor_manager` | 一行没碰 |

---

## 八、教训

1. **"测试自己坏了"比"测试没写"更危险（本轮最值钱的一条）**。
   G7 是"素材不在库 → 返回 None"的**负控制**，它一路是绿的 —— 但它是**假绿**：
   桩写成扁平的 `SimpleNamespace(sprites=...)`，而函数读的是 `pet.sprite_loader.sprites`，
   于是**恒返回 None**，负控制必然通过，等于没测。是**配对的正控制 G6 红了**才把它揪出来。
   → 正/负控制必须成对出现；只留负控制时，"测试自己坏了"可以一直绿下去。
   **同一族的第二个样本**：G5 断言"左右两组帧文件名不同"，而**原样复制**照样通过 ——
   于是加了 G10（逐像素证镜像）+ G10b（证该判据对未镜像的图会报错）。
   **定式：每条断言都要能回答"如果这件事根本没做，它还会绿吗？"**

2. **源码级断言含字符串字面量时，必须走 `func_lit` 而不是 `func_code`**。
   `func_code` 会把 STRING token 一起剥掉，而 `getattr(self,'_jump_anim_override',None)`
   这个 needle 里恰好有一个字符串字面量（属性名）→ 剥完变成 `getattr(self,,None)`
   → **永远搜不到 → 假红**。这是**第 4 次**踩同类坑（第八轮定过"验证脚本教训"）。
   本轮不再靠"记得"，而是加了 **X0 自检**把两个视图的语义差别钉住：
   `func_code` 搜不到字面量、`func_lit` 搜得到、两者都搜不到注释。
   **工具的语义本身也要有断言** —— 否则它一变形，下面所有断言一起失真。

3. **失败详情要能诊断，`detail=None` 等于没说**。
   G6/G8 第一版 detail 写的是 `None`，红了只看到 `<<< None`，完全不知道是"名字不对"
   还是"桩不对"。改成打印实际返回值 + 库里的键，一眼就看出是桩形状问题。
   → 断言失败时应该**把现场交出来**。

4. **离屏环境没有字体**：Qt 会打 `QFontDatabase: Cannot find font directory`，
   `QPainter.drawText` **静默不画**（素材对照图第一版整行标题一个字都没有）。
   → headless 下出图**不能靠文字**，改用色带 + 外部图例（`.txt`）。

5. **PowerShell 捕获原生 stdout 会毁掉中文**：第一遍跑套件用 `Out-File` 收，读回来是
   "涓婃ゼ" 这种乱码 —— 能猜出 PASS/FAIL，但**不能当证据**。项目里早就有现成套路
   （`第十七轮/run_round17.py` 的"让 Python 自己写"），本轮**踩完了才想起来去找**。
   → 先找项目里已有的工具/套路，再动手写。
   **同一族的坑本轮踩了第二次**：写提交信息又用了 `Out-File -Encoding utf8`（PS 5.1 加 BOM →
   标题首位多一个不可见字符），而且**第一次修的姿势也是错的** —— `Write` 工具覆盖一个
   **原本带 BOM** 的文件时会**保留 BOM**，所以必须**换新路径**重写才干净（校验：读前 3 字节）。
   **这两条规则本来都写在项目记忆里** —— 说明"记下来"不等于"会照做"：
   凡是**每次都要用的固定动作**（写证据、写提交信息），应该让它**没有出错的余地**（走脚本/模板），
   而不是靠临场记得。

6. **列表推导里手滑多打一层，traceback 只给行号**：`[n for n in (... for n in n) if ...]`
   → `NameError: name 'n' is not defined`，报错点在推导式内部，看不出是"多打了一层"。
   → 先展平再筛，别把嵌套推导写成一行。

7. **"落差"这类物理量要夹到物理合法域**（见第四节 F7）：负数不会当场出错，但会静默污染
   所有未来使用者。**能在源头夹干净的就别留给调用方**。

---

## 九、下一步

- 批次 B 闭环。剩批次 C（`climb_to_top_window` 收编）/ D（三条有意偏差）。
- 用户原话提到的"原计划"仍是 **H4/H5 上帝类拆分**（`架构改造排期方案_H4-H5_2026-09-13.md`）：
  `main.py` 仍是 8794 行 / `RalseiPet` 186 方法 / 438 个 `self` 属性（106 个被 ≥3 处写）。
  建楼批次做完就该回到它。
