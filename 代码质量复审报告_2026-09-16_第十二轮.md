# 代码质量复审报告 — 第十二轮（2026-09-16）

**主题**：jieba 分词接入 + 存储统一（"E 盘是最终存储，本地只作中转站"）
**用户原话**：① "帮我安装上jieba然后接入" ② "所有他所产生的文件在本地最多算是中转站，只有在 E 盘才是最终储存的地方"
**验证**：本轮自检 **54 PASS / 0 FAIL**；G2 回归 **16 套件 / 579 PASS / 0 FAIL / 全 IDENTICAL**

---

## 一、结论（先说结果）

两件事都落地了：

1. **jieba 已接入**（0.42.1，装在 `C:\Python311`）。走第十一轮预留的
   `conversation_focus.set_segmenter()` 注入点，是**软依赖**：没装 / 加载失败 / 切词抛异常
   一律静默回落内置词法。抽词碎片基本清零 —— 六句语料 **56 → 14 词条**，
   `学习编程需要花不少时间` 从 **13 个碎片**收成 `学习 / 编程 / 需要 / 不少 / 时间`，
   `塞尔达` 不再被切成 `塞尔 / 尔达`。

2. **存储统一**：新增 `data_store.py`，把"数据放哪"收成一个唯一入口。
   之前只有 `memory.json` 在 E 盘，其余 **6 类运行时产物全在程序目录里堆着**；
   现在 7 类产物**全部路由到"最终存储"**（E 盘在线时是 `E:\RalseiMemory\`，
   离线时是本地中转站 `%LOCALAPPDATA%\RalseiPet\`），并能在 E 盘回来时自动回迁。

**顺带纠正一条我上一轮的错判**：我曾说 E 盘是"Fixed 固定盘、可安全当下载目录"。
**这个结论不完整 —— 它会掉线。** 本轮施工途中它就从 `Get-Volume` 里消失了
（只剩 C/D，`Get-Disk` 只有一块 NVMe）。所以"本地中转站"不是洁癖，是**可用性必需**：
没有它，E 盘一拔程序就写不了数据。用户这条原则定得对。

---

## 二、交付物

| 文件 | 变更 | 说明 |
|---|---|---|
| `ralsei_pet/modules/data_store.py` | **新增** 约 260 行 | 存储解析层：vault（E 盘）/ staging（本地中转站）、`artifact_path`、`app_file`、`migrate_from_staging` |
| `ralsei_pet/modules/text_segmenter.py` | 新增（沿用 + 补两处） | jieba 适配层：软依赖、后台预热、用户词表、调用/回落计数、词典缓存路由 |
| `ralsei_pet/modules/logger_utils.py` | 改 | 日志目录走 data_store；加**重入护栏** `_initializing`；兜底值不缓存 |
| `ralsei_pet/modules/config_manager.py` | 改 | `config.json` 走 `data_store.app_file` |
| `ralsei_pet/modules/customization_system.py` | 改 | `customization_config.json` 同上 |
| `ralsei_pet/modules/entertainment_system.py` | 改 | `entertainment_data.json` 同上 |
| `ralsei_pet/modules/social_growth_system.py` | 改 | `growth_data.json` 同上 |
| `ralsei_pet/src/main.py` | 改 | 启动接入 jieba + 数据回迁；崩溃日志走 data_store |
| `code-quality-audit/第十二轮/verify_round12_store.py` | **新增** 54 项 | 自检套件（已挂进 G2） |
| `code-quality-audit/第十二轮/probe_smoke12.py` 等 4 个探针 | 新增 | 冒烟 / 路径 / 导入顺序 / 日志 |
| `code-quality-audit/regress/run_all.py` + `baseline.json` | 改 | 挂入 `round12_store`；新增 jieba 耗时归一化规则；基线重建 |

---

## 三、jieba 接入

### 3.1 为什么单独一层适配器

`conversation_focus` 被**话题锚（对话注意力）与记忆抽词两处共用**，是"抽不到词整条链路哑掉"
的关键模块（第十一轮契约）。所以它**不许有硬依赖**：jieba 只是"可选的更好边界判断"，
没有它也必须能跑。适配层还负责三件事：

- **后台预热**（守护线程）：jieba 首次建前缀字典约 0.6 s，不预热的话"用户发的第一句话"会卡顿；
- **降级事实可见**：`calls` / `fallbacks` 计数 —— 呼应第八轮教训"防御性 except 必须配异常计数"；
- **可选用户词表** `jieba_userdict.txt`：每行 `词 [词频 [词性]]`，专治"内置词法把 `塞尔达`
  切成 `塞尔/尔达`"这类专名残渣。文件不存在就跳过，不是错误。

**过滤分工刻意做窄**：适配器只管"词边界"，停用词/长度/去重仍归
`conversation_focus._from_segmenter()` —— 那是**唯一一份**策略实现，不能再有第二份。

### 3.2 实测对比（证据 `_evidence/round12_smoke.txt`）

| 句子 | 内置滑窗词法 | jieba |
|---|---|---|
| 我明天想写点代码 | `代码` | `写点` `代码` |
| 今天想聊聊塞尔达这游戏的剧情 | `塞尔` `塞尔达` `尔达` `游戏` `剧情` | `聊聊` **`塞尔达`** `游戏` `剧情` |
| 学习编程需要花不少时间 | 13 个碎片（含 `习编/习编程/编程需/程需/少时/少时间`） | **5 个**：`学习` `编程` `需要` `不少` `时间` |
| 今天下雨会有点冷吧 | `天下` `下雨` | `下雨` |
| 刚喝了杯咖啡感觉还行 | `咖啡` `咖啡感` `啡感` | `咖啡` |
| 明天开会吗 | `天开` `开会` | `开会` |
| 这件事很重要 | `件事` `重要` | `这件` `重要` |

**六句词条总数 56 → 14**；第十一轮那份"残渣账本"里的
`习编 / 习编程 / 编程需 / 程需 / 少时 / 少时间 / 咖啡感 / 啡感 / 天下 / 天开 / 尔达`
**全部清零**（自检 A5 断言）。

### 3.3 未解决的（诚实）

- jieba 会把 `写点` `写累` `这件` 这类**口语搭配**当词切出来。它们不是跨词碎片，但也谈不上
  是好关键词 —— 属于分词库的词典粒度问题，靠**用户词表**或后续停用词表收敛，本轮不动。
- 装了 jieba 会多占几十 MB 常驻内存 + 9.2 MB 词典缓存。缓存已改到数据目录（见 §四）。

---

## 四、存储统一：E 盘是最终存储，本地只作中转站

### 4.1 两层模型

```
最终存储 vault :  E:\RalseiMemory\           ← 卷标命中"肖翰哲"的盘（复用 memory_store 的设备发现）
本地中转站 staging: %LOCALAPPDATA%\RalseiPet\  ← E 盘离线时的落脚点
```

**读路径永远 vault 优先**；E 盘不在就落本地中转站，等它上线后由 `migrate_from_staging()` 搬过去。

`memory_store` 保持原样（`memory.json` 仍在 `E:\RalseiMemory\`，桌面兜底仍按第九轮用户原话
`<桌面>\memory`）—— 它的语义被自检 C5 钉住，本轮**没动**。

### 4.2 7 类运行时产物，全部收口

| 产物 | 改造前 | 改造后 |
|---|---|---|
| `memory.json` | `E:\RalseiMemory\`（第九轮已做） | 不变 |
| `logs/ralsei_pet.log`（+轮转） | `ralsei_pet/logs/` | 数据根 `logs/` |
| `logs/crash.log` | `ralsei_pet/logs/` | 数据根 `logs/` |
| `config.json` | `ralsei_pet/` | 数据根 |
| `customization_config.json` | `ralsei_pet/` | 数据根 |
| `entertainment_data.json` | `ralsei_pet/` | 数据根 |
| `growth_data.json` | `ralsei_pet/` | 数据根 |
| jieba `jieba.cache` | `%TEMP%` | 数据根 `cache/` |

**收编历史遗留只复制、不搬走**：`config.json` / `customization_config.json` / `memory.json`
在仓库里是**被 git 跟踪的默认模板**，搬走会让 git 显示"默认配置被删了"。所以 `app_file()`
第一次运行时**复制**一份到数据根，程序目录那份原地不动（自检 B6/B7/B8）。

### 4.3 迁移的五条安全约定

与 `memory_store.migrate_from_fallback` 同一条铁律：

1. **先探测源文件能否搬走**（`_movable`：原地改名再改回）—— 被占用就跳过，留待下次；
2. 先 `copy`，**校验长度一致**，通过之后才删源；任一步失败就保留源文件；
3. 目标已有同名的：比 mtime 留新的，**落选者**挪 `<名字>.old` 留档；
4. 只在目录**确实空了**才删空目录，绝不递归硬删；
5. vault 与 staging 是同一目录时直接拒绝搬运。

第 1 条是本轮新增的：日志文件在程序运行时是**打开的**，Windows 上搬不动。
不先探测就会出现"复制成功、源删不掉"→ 同一份数据在两边各留一个。自检 B16 覆盖。

### 4.4 踩到的真坑：`logger_utils ↔ data_store` 初始化环

**症状**：`logs/` 解析到程序目录 `ralsei_pet/logs`，而不是数据根。

**根因**：`data_store` 在模块顶层 `from logger_utils import get_logger; _log = get_logger(...)`。
而 `logger_utils` 初始化时要解析日志目录 → `import data_store` → 此刻 `data_store` **还没执行完**，
`artifact_path` 不存在 → 抛错被吞 → 日志目录被**永久钉死在兜底值**上。

**修法（双层）**：
- `data_store` 改用**惰性日志器** `_LazyLogger`（`__getattr__` 里才去要 logger）——
  import 期不再回头碰 logger，环直接断开；
- `logger_utils._log_dir()` **失败时不缓存**兜底值（只缓存成功结果），并把 `_init_logging`
  拆成"带重入护栏的外壳 + 真正的实现"，防止万一再出现环时无限递归。

**回归锁**：自检 C2 用**子进程**跑"先 import data_store"的顺序，断言日志目录仍落在数据根内
（证据 `_evidence/round12_import_order.txt`：两种顺序结果一致）。

---

## 五、验证

```powershell
# 本轮自检（54 项）
C:\Python311\python.exe code-quality-audit\第十二轮\verify_round12_store.py

# 四个侦察探针（各自写 UTF-8 到 _evidence/）
C:\Python311\python.exe code-quality-audit\第十二轮\probe_smoke12.py
C:\Python311\python.exe code-quality-audit\第十二轮\_recon\probe_paths.py
C:\Python311\python.exe code-quality-audit\第十二轮\_recon\probe_order.py
C:\Python311\python.exe code-quality-audit\第十二轮\_recon\probe_logger.py

# 全量回归（16 套件；必须用 C:\Python311\python.exe）
C:\Python311\python.exe code-quality-audit\regress\run_all.py
```

**结果**：自检 54 / 0；G2 **16 套件 579 PASS / 0 FAIL / 全 IDENTICAL**。
本轮唯一的基线变更：`round5_smoke` 模块数 **25 → 27**（新增 `data_store` / `text_segmenter`，
两个都导入 OK），另给归一化器加了一条 jieba 耗时规则（`Loading model cost X seconds` 每次都不同）。

---

## 六、未做与局限（诚实清单）

1. **E 盘离线，vault 分支没做真机验证**。本轮全程 E 盘不在线：staging 分支是**真机实测**的，
   vault 分支（`E:\RalseiMemory\`）只用 `RALSEI_MEMORY_DIR` 环境变量**模拟**验证。
   等 E 盘接回来需要补一次真机确认（预期：无需改动即可直接落到 E 盘）。
2. **E 盘离线期间的运行产物会留在本地中转站**——这是原理上不可避免的（盘不在就写不进去）。
   E 盘回来后由启动时的 `migrate_from_staging()` 搬走；运行中被占用的日志文件会跳过，下次再搬。
3. **程序目录里还积着一批历史开发日志**（`diag*.log` / `run*.log` / `smoke_test*.log` 等约 450 KB，
   已被 `.gitignore` 覆盖），本轮**没有删** —— 删除属破坏性动作，留给用户决定。
   注意其中 `ralsei_pet/logs/ralsei_pet.log.2026-09-07` **是被 git 跟踪的**（历史误入库）。
4. **`ralsei_pet/memory.json` 仍是仓库里的遗留文件**（第九轮遗留的旧位置副本），未清理。
5. `text_segmenter.py` 上一轮会话中已存在但从未提交，本轮沿用其设计并补了两处
   （词典缓存路由、`describe()` 文案），未推翻重写。

---

## 七、下一步

1. **E 盘接回后补真机确认**：启动一次宠物，核对产物是否直接落在 `E:\RalseiMemory\`，
   以及本地中转站里的东西是否被搬空。
2. **可选**：清理程序目录里的历史开发日志（需用户点头）。
3. **可选**：往 `jieba_userdict.txt` 加专名词（塞尔达、海拉鲁之类），把 §3.3 的口语搭配再收一轮。
4. 第十轮遗留：`change_animation` 长→短回退 vs `update_animation` 内嵌块只取前两段。
