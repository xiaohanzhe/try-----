# 第47轮报告 · ch1/2/4/5 房间全量复查 + 寻路"人味化"

> 用户本轮原话：
> 「复查一下1,2,4,5,章的room加起来到底有多少，**别漏了**，并且确保**每一个都能用**，且**符合原作逻辑**，对了，那个寻路要**符合真人逻辑，别和机器人一样**」
>
> 四个问题逐一给出**可复算**的答案，全部数字来自四个权威源交叉重算（`_tools/` 里的脚本），不手抄、不抽样。

---

## 0. 结论先行

| 用户问题 | 结论 | 判据（可复算） |
|---|---|---|
| ch1/2/4/5 的 room 一共多少 | **原作 1,005 间**（≠ 产品场景数） | `_room_geometry.json.stats`：147+278+328+252 |
| 别漏了 | **逐章下标 0..n−1 一个不缺** | `verify_rooms47.py` A3 断言 |
| 每一个都能用 | **1,014 个场景载体/注册/命名/几何 100% 通过** | U1–U5 体检全过（仅 `desktop` 的 room_id 非 int，属设计） |
| 符合原作逻辑 | **未覆盖 41 间 = 真缺口 10 + 孤立 31** | 判据 = **原作门图**（不是名字猜） |
| 寻路别像机器人 | **可见硬拐角 318→107、最深回溯 228.4px→0.6px、逐对 0 劣化** | 改前快照 vs 工作区真 A/B（193 组） |

**★ 一句话**：房间数没问题、场景都能用;**唯一的真缺陷在"房间内行走轨迹"**——平滑器会把路"甩过头再拐回来"（最坏倒走 228px ≈ 半间房），已修好并加锁。

---

## 1. 房间到底有多少（"别漏了"）

### 1.1 两个不同的量，别混

| 量 | 值 | 来源 |
|---|---|---|
| **原作房间总数（五章）** | **1,251** | `_room_geometry.json.stats`（键 = `"<章>:<Data.Rooms 下标>"`） |
| ├ ch1 / ch2 / ch3 / ch4 / ch5 | 147 / 278 / 246 / 328 / 252 | 同上 |
| **★ ch1/2/4/5 合计（用户点名）** | **1,005** | 147+278+328+252 |
| **产品场景总数** | **1,014** | `_index.json` 全部章节键（含 `desktop` 章 1 个） |
| ├ ch1 / ch2 / ch3 / ch4 / ch5 / desktop | 128 / 236 / 187 / 261 / 201 / 1 | 逐章累计 = 1,013 + 桌面 1 |
| **★ ch1/2/4/5 产品场景** | **826** | 1,013 − 187(ch3) |

> ⚠️ **坑**：`_index.json.chapters` **含有 `desktop` 键**（桌面 = 独立前置章）。统计总数必须遍历**全部章节键**，否则少算 1（本轮踩过，`_tools/census_rooms47.py` 已修）。

### 1.2 "别漏了"的可判定形式

对每一章，把几何表里该章的房间下标取出来排序，断言 **等于 `0..n−1` 的完整区间**（缺一个就报红）。结果：**五章全部连续无缺**。这条也是回归锁 `rooms_round47` 的 A3。

### 1.3 逐章分类与覆盖

```
章         原作   system    debug   其它 playable   产品已覆盖    ★未覆盖
ch1      147      15        0      0      132        128           4
ch2      278      16       31      0      231        223           8
ch3      246      16       24      0      206        187          19
ch4      328      17       35      0      276        260          16
ch5      252      18       20      0      214        201          13
--------------------------------------------------------------------------
合计     1251      82      110      0     1059        999          60
```

- `system`（82）+ `debug`（110）= **192 间不可玩**（原作里就是系统房/调试房，**按原作逻辑不该做**）。
- playable 1,059 − 已覆盖 999 = 未覆盖 60；其中 **ch3 占 19**（不在本轮范围）。

### 1.4 ★ ch1/2/4/5：未覆盖 41 间 = 真缺口 10 + 孤立 31

**判据 = 原作门图**（`_room_graph.json`）里这间房有没有边：
- **有边 ⇒ 原作里能进 ⇒ 必须做**（叫"真缺口"）；
- **无边 ⇒ 原作里也进不去 ⇒ 按原作逻辑不该做**（叫"孤立"）。

**★ 真缺口 = 10 间**（ch1/2/4/5）：

| 章:下标 | 房间名 | kind | in/out |
|---|---|---|---|
| ch1:33 | `room_school_unusedroom` | town_life | 1/1 |
| ch1:133 | `room_shop1` | other | 1/0 |
| ch1:134 | `room_shop2` | other | 1/0 |
| ch2:54 | `room_school_unusedroom` | town_life | 2/1 |
| ch2:229 | `room_shop1` | other | 1/0 |
| ch2:230 | `room_shop_ch2_music` | other | 1/0 |
| ch2:231 | `room_shop_ch2_swatch` | other | 1/0 |
| ch4:60 | `room_school_unusedroom` | town_life | 1/1 |
| ch5:49 | `room_school_unusedroom` | town_life | 1/1 |
| ch5:79 | `room_DARKbase_GMS2` | dark | 1/0 |

**孤立 = 31 间**（`in=0 且 out=0`）：全是 `*test` / `*_old` / `intro_ch*` / `myroom_dark` 之类，例如 `room_overworldBulletEnemyTest`、`room_lerptest`、`room_boardtest`……**按原作逻辑"进不去就不做"**。

> ch3 另有 3 间真缺口（`ch3:51`、`ch3:69`、`ch3:79`）**不在本轮范围**——用户口径：第三章黑暗喷泉开在 Kris 家，本期不做。

**与第 38 轮 `cls` 交叉**：这 10 间的 `cls` = **maybe 9 / nonscene 1** ⇒ 就是当年留的"待裁定"，至今没裁（见 §4 待裁定①）。

---

## 2. 每一个都能用吗（逐场景可用性体检）

对 **1,014 个场景逐个**跑五项检查：

| 检查 | 结果 |
|---|---|
| U1 载体文件缺失 | **0** |
| U2 载体里未注册该场景 | **0** |
| U3 `name` 为空 | **0** |
| U4 `original_room_id` 非法 | **1**（仅 `desktop`——桌面不是原作房间，属设计） |
| U5 几何表未命中 | **0** |

> ★ **关键坑（本轮修正）**：场景有**两种载体**——
> ① 独立文件 `<scene_id>.json`（`_index` 里 `file` 有值）；
> ② **分片文件 `_zone.<章>.<区>.json`（顶层键 `scenes`）—— 共 926 个，这些记录里 `file` 是空串！**
> 只按 `file` 字段找文件会把 926 个分片场景**误报"缺文件"**。判据已修：`fname = rec['file'] or '_zone.<ch>.<area>.json'`。

**结论：1,014 个场景，可用性 100% 通过。没有"不能用"的场景。**

### 2.1 背景素材覆盖率（如实报告，**不属于"不能用"**）

- **有背景 235 / 无背景 779（覆盖率 23.2%）**。
- 779 个无背景场景**能正常打开、能寻路、能渲染**，只是背景退化为占位（斜纹），需要**重跑 UTMT 重新导出素材**才能补齐（见 §4 待裁定②）。

---

## 3. 符合原作逻辑吗（判据必须是"原作门图"）

### 3.1 命名启发式**不够用**（这是本轮刻意验证的一件事）

有人可能会想"名字里带 `test` 的就是调试房，跳过就行"。**实测只有 74% 覆盖**：

- 孤立房（该跳过的）里，只有 **74%** 的名字带调试/演出特征；
- ★★ **同名不同命**：`room_shop1` 和 `room_DARKbase_GMS2` **同时出现在"有门（真缺口）"和"无门（孤立）"两侧**。

⇒ **结论：判据只能是原作门图，不能用名字猜。** 这条写成了回归锁 `rooms_round47` 的 C2/C3。

### 3.2 三类结论都落到"原作逻辑"上

- **真缺口 10 间** —— 原作门图有边 ⇒ 原作里能进 ⇒ 必须做（§1.4）。
- **孤立 31 间** —— 原作门图无边 ⇒ 原作里也进不去 ⇒ 不做（`in=0 且 out=0`，C4 断言）。
- **不可玩 192 间** —— `_room_order.json.kind` ∈ {system, debug} ⇒ 系统/调试房，不做。

---

## 4. 寻路"别和机器人一样"（本轮真正的缺陷与修复）

### 4.1 先定位：缺陷在"房间内行走"，不在"房间之间"

| 层 | 模块 | 本轮实测 |
|---|---|---|
| 房间**之间**（门 → 门） | `scene_pathfind.py` | 改成字典序 A\*（优先少门、少折返）——**5,798 组起终点与旧 BFS 完全一致**（门数不一致 0、折返更多 0） |
| 房间**之内**（像素轨迹） | `scene_walk.py` | ★ **真凶在这里** |

> A\* 改造在**当前数据上不改变任何一条实际路线**，但**保留**（更稳、且为未来拓扑变化做准备）。这样处理是诚实的：没有谎称"改了 A\* 就更像人"。

### 4.2 缺陷本体

`smooth_keep_walkable` 的回退分支会把**原始顶点插到当前点"身后"**，平滑后轨迹出现**往回走**的抖动：

- 最深回溯 **228.4 px**（≈ 半间房宽）；
- 转角 **174°~180°**（等于"走过头再原地拐回来"）；
- 193 组可规划配对里，**74 组**平滑后硬拐角比原折线**还多** ⇒ 平滑在帮倒忙。

### 4.3 修复（`scene_walk.py`，`+143 / −1`）

新增两层后处理，接在 `smooth_keep_walkable` 末尾：

1. **`_monotone_forward`** —— 按**原折线弧长**单调过滤，删"倒退 > `BACKTRACK_TOL`（=半个格步 5px）"的点；**删点前先 `_segment_clear` 回验**（不允许为顺而穿墙）；删不动时把跳过的点**接回来**，不整体放弃。
2. **`_despike`** —— 只削"拐一下又拐回来"的**尖刺**（转角 ≥ `DESPIKE_MIN_TURN = 45°` 且抄近路安全）。

> ★ `45°` 是**刻意**的：缓弯一律保留——用户反对的另一种走法是**"一直直线走"**（E1b 断言"缓弯原样保留"）。
> ★ 修 `_despike` 时踩过一个 bug：锚点用 `cur[i-1]` 会拿"被删的点"当前一个点，抄近路安全校验验错段 ⇒ **造出 8 处穿模**；改成 `a = nxt[-1]`（上一个**保留**点）后穿模归零。

### 4.4 真 A/B 实测（改前快照 vs 工作区，同一批 193 组配对）

| 指标 | 原折线 | 改前（快照） | **改后（工作区）** |
|---|---|---|---|
| 可见硬拐角（≥80° 且两腿 ≥5px） | 355 | **318** | **107** |
| 最深回溯 | — | **228.4 px** | **0.6 px** |
| 穿模 | — | 0 | **0** |
| 残留"倒走" | — | — | **0 条** |

逐对：硬拐角 **更少 83 / 相同 106 / 更多 4**；回溯 **更浅 69 / 更深 0**。

**关于那 4 例"改后更多"（如实列出，不藏）**：

| 章:房 | 改前→改后 | 各自原折线拐角 | 判定 |
|---|---|---|---|
| ch1:19 | 0→1 | 2 / 2 | 仍不劣于原折线 |
| ch2:29 | 0→1 | 3 / 3 | 仍不劣于原折线 |
| ch2:35 | 0→1 | 2 / 2 | 仍不劣于原折线 |
| ch5:30 | 0→1 | 2 / 2 | 仍不劣于原折线 |

⇒ 这 4 例是"改前恰好平滑到 0 个拐角，改后留了 1 个"，但**改后仍优于各自的原折线**（原折线有 2~3 个）。**没有任何一组改后劣于原折线**。

### 4.5 回归锁（防回退）

| 套件 | 断言 | 关键判据 |
|---|---|---|
| `walk_round47` | **33** | 真实全量 348 组 / 残留倒走 ≤10 且**每一条都是"删了会穿墙"的** / 硬拐角 355→107 且**逐对 0 劣化** / 逐点逐段不穿模 / **负控制**：改前版本必然更差（228.4px、318 拐角） / 合成夹具：倒走删不动必须保住、无脑删会穿墙、缓弯原样保留 |
| `rooms_round47` | **22** | 原作 1,251 / 四章 1,005 / 逐章下标不缺 / 产品 1,014（含 desktop）/ 真缺口 10 间**集合精确相等** / 两种载体 / 命名启发式 74% 不够用 / 同名不同命 |

---

## 5. 改动清单

**代码（2 个）**

| 文件 | 改动 | 说明 |
|---|---|---|
| `ralsei_pet/modules/scene_walk.py` | `+143 / −1` | 新增 `_monotone_forward` / `_despike` / `_turn_deg`，常量 `BACKTRACK_TOL`、`DESPIKE_MIN_TURN`、`DESPIKE_PASSES` |
| `ralsei_pet/modules/scene_pathfind.py` | `+80 / −23` | 等权 BFS → 字典序 A\*（少门优先、少折返次之） |

**回归（2 个）**

| 文件 | 改动 |
|---|---|
| `code-quality-audit/regress/run_all.py` | `+58`：`SUITES` 追加 `rooms_round47` / `walk_round47`；`HERMETIC_IDS` 同步加 |
| `code-quality-audit/regress/baseline.json` | `+12`：**只增不改**（新增 2 套件，42 个既有套件哈希逐条不变） |

**新增目录**：`code-quality-audit/第47轮-房间全量与寻路人味化/`
- `verify_rooms47.py`（22 断言）/ `verify_walk47.py`（33 断言）/ `recheck47.py`（自我复检，28 项）
- `_tools/` 8 个探针与普查脚本
- `_evidence/` 房间普查、场景可用性体检、A/B 对照、复检结果、改前快照（`_scene_walk_HEAD47.py`，sha1 `8cfc6afe…`）

---

## 6. G2 全量回归

```
合计：PASS=2154  FAIL=0  套件=44
```

- 原基线 42 套件 / 2,099 PASS ⇒ 本段 **+2 套件 / +55 PASS**（rooms22 + walk33）。
- `pathfind_round45` 35/35、`walk_round45` 90/90 全绿（未回退）。

---

## 7. 🔴 待用户裁定

1. **★ 10 个真缺口房间是否补做？**
   - 9 个 `cls=maybe`（当年"待裁定"至今没裁）+ 1 个 `nonscene`；
   - 4 个 `room_school_unusedroom` 有真背景 `bg_unusedclass`；`room_shop*` 的 `bg_sprites=[]`（没素材）。
   - **补做 = 需 UTMT 重导素材 + 新建场景 + 加门边**。

2. **★ 779 个无背景场景是否重导素材落地？**（用 UTMT 重跑五章，补齐背景；工作量大，需要拍板做不做）

3. **★ ch3/4/5 门图目前只采了 46/328、46/252 ⇒ 是否重跑 UTMT 补采？**
   - 门图不全 ⇒ 这几章的可达性判断**如实报"走不到"**（第 45 轮已如实报告 ch4 `torhouse`/ch5 `krisroom` 缺口）。

4. **上一轮遗留**：伙伴系统 P3 接线 / P4 人设；#48 节点+数值差路由、#49 原作 BGM/可交互物、#50 ch3 room 门控。

---

## 8. 复检（改过重要核心文件后的自证）

复检脚本 `code-quality-audit/第47轮-房间全量与寻路人味化/recheck47.py`，结果落 `_evidence/复检47.txt`。

**`=== RESULT: PASS=28  FAIL=0 ===`**

| 项 | 结果 |
|---|---|
| ① 可编译（`ast.parse`，**不产 `.pyc`**——复检不许改变被测状态） | PASS（7 个文件） |
| ② 结构 / 接线自检 | PASS（`smooth_keep_walkable` 真调两层 / `heapq` 在位 / 两套件注册进 `SUITES` 与 `HERMETIC_IDS`） |
| ③ 无 BOM / 无 U+FFFD | PASS（bad=0 / bad=0） |
| ④ 恒真判据复查 | PASS（无"恒真且无守卫"的 `check`） |
| ⑤ 逐令牌回验 | PASS（8 个源码令牌 + 快照 sha1 + 报告数字回证据文件核对） |
| ⑥ 工作区干净 + 回归 | PASS（无意外删除；G2 2,154/0） |

> ★ 复检过程中**判据自己也报过一次红**：第一版 ④ 写"文件里不许出现 `check(..., True)`"，
> 把 `verify_walk47.py:155` 的**前置条件**写法（上一行已判 `False` 并 `return 1`）误判为恒真。
> 按"判据报红先怀疑判据"的原则，把判据改成**"常量 `True` 必须存在同名守卫分支"**，误报消除。
> 这正是"判据过窄 = 会误报，与过宽 = 恒真同样要防"的又一次实例。
