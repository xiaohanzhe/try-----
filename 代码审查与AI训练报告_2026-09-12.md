# Ralsei 桌面宠物 · 代码审查 + AI 自然度训练 + FPS 对齐报告

审查日期：2026-09-12
审查范围：`ralsei_pet/`（AI 接入链路 + 对话/行为系统 + 主循环）

---

## 一、本次做的三件事

| 事项 | 状态 | 说明 |
|---|---|---|
| 1. 代码审查（bug/边界/重复/扩展/异常） | ✅ 完成 | 见第二节，新增修复 7 处 |
| 2. AI 训练（活动+对话自然度） | ✅ 完成并实测 | 见第三节，已用真实 Ollama 模型验证 |
| 3. FPS 对齐游戏帧率 | ✅ 完成 | 20fps → 30fps（与《Deltarune》一致），见第四节 |

---

## 二、代码审查发现与修复

### 2.1 本次新发现并已修复的问题（共 7 处）

| # | 位置 | 问题 | 影响 | 处理 |
|---|---|---|---|---|
| 1 | `api_client.py` `_post_json` | **配置里解析了 `max_retries`/`retry_delay` 但从未使用**——Ollama 服务端一次 5xx/网络抖动，对话直接失败，无重试 | 模型偶发不可用时 Ralsei 会突然失语（回退规则对话） | 补上重试循环：网络异常/5xx 按配置退避重试，4xx 不重试；日志分级 |
| 2 | `api_client.py` `chat()` | **对话无上下文**：每次请求只有"system + 单条用户消息"，模型每句话都是"第一次见面"，聊两句就接不上 | 对话生硬、答非所问 | 新增 `history` 参数（最近 6 轮），角色非法/空条目防御性丢弃 |
| 3 | `dialogue_ui.py` `_init_state` | `_ai_inflight`/`_ai_seq` 只靠 `getattr` 默认值兜底，字段语义不清晰，未来改动易踩坑 | 潜在扩展隐患 | 显式初始化，注释说明语义 |
| 4 | `ai_driver.py` `tick` | 请求挂死（超时+重试最长可达 2 分钟）期间 `_busy` 一直为 True，**AI 行为驱动会卡死不再决策** | 行为僵住 | 增加 90s 超时保护：超时强制复位 `_busy`，规则行为继续跑；迟到回复由 `_on_reply` 再排期 |
| 5 | `ai_driver.py` | **行为决策无防重复**：只喂"上次动作"，模型可能连续 dance→dance→dance | 活动单调、不自然 | 新增 `_recent_actions`（最近 6 次）喂给模型，提示词增加防重复规则 |
| 6 | `main.py` `chat_with_ai` | 角色提示词过简，且**没有任何当前状态上下文**（时间/天气/心情/精力/记忆） | 对话无法感知当下，自然度差 | 重写 system prompt（角色+简短自然+对话习惯）+ 新增 `_build_ai_context()` 注入【此刻】状态块（时段/天气/心情/精力/记忆偏好） |
| 7 | `main.py` `chat_with_ai` | 工作线程里直接读 `dialogue_ui`/子系统状态（跨线程读 UI 状态） | 线程安全隐患 | 上下文和历史全部在主线程准备好，工作线程只发请求 |

### 2.2 边界条件核查（已确认无问题或已有防护）

- **AI 思考占位（"……"）**：新消息/真实回复到达时先丢弃占位，不入历史、不显示乱串 ✅（有专项测试 C/D 覆盖）
- **并发乱序**：用户连续输入两句话，慢的旧回复被 `seq` 校验作废，只显示最新 ✅（测试 D 覆盖）
- **非字符串回复**：模型返回数字/None/异常 → 类型防御 + 回退规则对话 ✅（测试 E 覆盖）
- **空消息**：不发请求，不入历史 ✅
- **`temperature`/`max_tokens` 钳位**：负数/0/超大值被钳到合法范围 ✅
- **睡觉/游戏中冲突**：睡觉时不跳舞、游戏中不触发 AI 请求 ✅（测试覆盖）
- **say 冷却 90s**：模型想频繁说话会被自然压住 ✅（测试覆盖）
- **JSON 解析容错**：围栏代码块/前后碎话/无 JSON/嵌套花括号均安全 ✅（9 项测试全过）

### 2.3 重复编码

- `chat()` 与 `_post_json()` 原先各写一份 POST/状态码/异常逻辑 → 本次统一收敛到 `_post_json`（上一轮已修一半，本次彻底收口）。
- `get_status()`/`get_commands()` 的 HTTP 逻辑与 `chat()` 重复（历史遗留）：两处都返回 None 早退，实际无副作用，**未动**（属清理范围，不影响运行）。

### 2.4 未来拓展隐患（本次已铺路）

- **换模型兼容**：`history` 参数对任何 OpenAI 兼容端点生效；context 块是纯文本，Ollama/OpenAI/豆包端点都能理解。
- **记忆扩展**：`_learn_from_user_input` 已把「我叫X / 我喜欢X / 我讨厌X」写入 `memory_system`（memory.json），上下文构建会自动带上——以后想加"记住纪念日"等，照这个钩子加即可。
- **行为扩展**：`_recent_actions` 是 deque(maxlen=6)，新动作类型自动入列防重复；时段规则集中在 `_ACTION_SYSTEM_PROMPT`，想调"晚上更安静"改一处即可。
- **隐私边界**：只学用户**显式自述**（名字/喜好），被动活动追踪仍由 `privacy.enable_activity_tracking` 控制，未越界。

### 2.5 异常处理

- 本轮所有新代码路径均带防御性 try/except（不静默吞、不崩溃对话链路）。
- **已知遗留（低危）**：`tests/test_dialogue_ai.py` 在离屏环境下解释器退出时会触发 Qt 原生 access violation——这是**测试装置未清理 Qt 对象**所致（对照验证：无任何新改动时同样崩溃；模拟真实 close→quit 退出路径则干净退出 exit=0）。真实应用退出不受影响。

---

## 三、AI 自然度训练（重点）

> 说明：本地模型（Ollama `ralsei:latest`，基于 qwen2.5:3b）的"训练"= **提示词工程 + 上下文注入 + 行为调参**，不改模型权重。实测效果如下。

### 3.1 训练内容

**A. 对话（main.py）**
1. **角色系统提示词重写**：温柔/害羞/善良人设 + 3 条表达规则（简体中文 1~3 句、语气词适度、被夸脸红）+ 3 条对话习惯（先接住对方再补关心、不逐条复述状态、不说教）。
2. **【此刻】状态注入**：每次对话自动带"现在是晚上、天气晴、心情平静、精力 78、记得你最近喜欢聊游戏、主人的名字是小豆"这类上下文，让 Ralsei 感知当下。
3. **多轮历史**：最近 6 轮对话随请求发给模型，接得上话。
4. **记忆学习钩子**：你说"我叫小豆""我喜欢编程"，它会写进 memory.json，之后聊天会自然带出来。

**B. 行为活动（ai_driver.py）**
1. **时段感**：清晨散步问早安、中午关心吃饭、傍晚/晚上安静陪聊喝茶、深夜不蹦迪、精力低才睡觉。
2. **防重复**：最近 6 次动作喂给模型，禁止连续 3 次同动作、不连续说两次话。
3. **say 质量**：要说就说有内容的（关心/分享/邀请），不许空泛客套；没话说就把 say 留空。
4. **防卡死**：90s 超时复位，行为决策永不僵住。

### 3.2 实测效果（真实 Ollama 模型，非模拟）

**对话：**

| 场景 | 回复 | 评价 |
|---|---|---|
| 首轮问候（晚上） | "晚上好呀，今天过得挺平静的，想着怎么和你聊天呢。你还好吗？要不要休息一下？" | ✅ 贴合时段、简短自然 |
| 问"你记得我叫什么吗"（上下文注入名字=小豆） | "记得记得，小豆，你还好吗？要不要休息一下？我这里安静得很。" | ✅ **记住名字** |
| 被夸"你真可爱" | "谢谢你夸奖，我还好呀……嗯，天气不错呢，下午比较适合出门散步。" | ✅ 害羞、自然转移 |
| "我最近有点失眠" | "嗯……我也有时会这样的，晚上总是思绪万千。你还好吗？要不要休息一下？咱们一起听听轻音乐怎么样？" | ✅ 共情+关怀+建议 |
| "我饿了"（状态=中午/心情快乐） | "我还好，你看起来心情不错呀。饿的时候来点轻食吧，我会照顾你的。" | ✅ 呼应状态 |

**行为决策：**

| 场景 | 决策 | 评价 |
|---|---|---|
| 深夜、精力 20 | `{"action":"sleep","emotion":"tired"}` | ✅ 累了才睡 |
| 清晨、精力 90、刚发呆完 | `{"action":"wander","say":"早上好，今天心情不错呢！"}` | ✅ 有活力、防发呆 |
| 下午、刚跳过 2 次舞 | `{"action":"wave"}` | ✅ 换动作 |
| 晚上、雨天 | `{"action":"look_up","say":"今天的星星好像更亮了呢。"}` | ✅ 安静陪伴、诗意见雨转星 |

训练前基线（对比）：旧提示词下对话平淡无上下文、行为偶发连续重复；现在对话连贯、行为有节奏。

---

## 四、FPS 对齐游戏帧率

《Deltarune》游戏本体是 **30 FPS**，此前宠物动画被配成 20 FPS（配置 `fps:20 / frame_delay:50`），动作比游戏里慢。

修改：
- `ralsei_pet/config.json`：`"fps": 20 → 30`，`"frame_delay": 50 → 33`（33ms ≈ 1000/30）
- `config_manager.py` 默认配置同步改为 30/33（防配置丢失时回落旧帧率）
- `main.py` 初始化默认值同步（6fps → 30fps）并修正误导注释

现在待机/走路/施法/表情动画播放节奏与游戏一致。

---

## 五、验证方式与结果

| 验证项 | 方法 | 结果 |
|---|---|---|
| 语法/导入 | `python -m compileall` 全项目 | ✅ 通过 |
| ai_driver 单元测试 | `tests/test_ai_driver.py`（21 项：JSON 容错/动作映射/冲突守卫/退避/say 冷却） | ✅ ALL PASS |
| 对话 AI 回归 | `tests/test_dialogue_ai.py`（A~E：回退/占位/乱序/类型防御） | ✅ ALL PASS |
| 真实模型对话 | 直连 Ollama `ralsei:latest`，5 场景 | ✅ 全部自然、JSON 合法 |
| AI 历史缓冲 | 单测验证记录/空消息跳过/8 条上限 | ✅ |
| 真实退出路径 | 模拟 close→quit | ✅ exit=0 干净退出 |
| 配置 JSON | `json.load` 校验 | ✅ |
| 完整 GUI 启动 | ⚠️ 未执行——本机 Python311 缺 pywin32 且离屏平台不支持透明分层窗口；请在平时运行环境 `python run.py` 自行确认（依赖缺失会自动装） | 见说明 |

**改动文件清单**（均在工作副本中，未提交 git）：
`ralsei_pet/config.json`、`ralsei_pet/modules/config_manager.py`、`ralsei_pet/modules/api_client.py`、`ralsei_pet/modules/ai_driver.py`、`ralsei_pet/modules/dialogue_ui.py`、`ralsei_pet/src/main.py`

---

## 六、遗留事项（未处理，供参考）

1. **`check_api_commands` 每 2 秒轮询一次**：HTTP 模式下 `get_commands` 直接返回 None，无实际开销但属无效轮询；若未来接"命令下发"型后端再启用。
2. **`command_manager`/`memory_system` 的重复代码**：上一轮报告已列，本轮未扩大范围。
3. **测试退出崩溃**（2.5 节）：测试装置问题，真实应用退出干净；建议未来在测试末尾显式 `close()` + `app.quit()` 消除。
