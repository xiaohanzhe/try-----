# 场景系统 P0 骨架 实施与验证报告

**日期**：2026-09-22
**轮次**：第二十八轮（承接第二十七轮 AI 失真治理）
**主导指令（用户原话，verbatim）**：

> 按你的想法来就好，对了，你到时候了解好整个系统架构，我后期会给他添加场景系统，也就是把原作的世界搬到桌面上，你要留好拓展接口哦（桌面也会被我当成一个场景，你自己看一下原作的场景切换这类的，为后期添加做准备）

---

## 0. 一句话结论

**接口已在位，行为零变化。** 新增 2 个模块 + 3 份数据文件 + 4 处接线，回归锁 79 项全绿，
G2 由 24 套件 / 1169 PASS 扩到 **25 套件 / 1250 PASS / 0 FAIL / 25 全 IDENTICAL**，
`main.py` 净变化 **+33 / −1**（那一行还是 `_CONTROLLER_ATTRS` 的元组追加，不是删除逻辑）。

---

## 1. 指令拆解与交付对照

| 用户要求 | 落点 | 状态 |
|---|---|---|
| 「了解好整个系统架构」 | `场景系统架构地图_2026-09-22.md`（12 章 · `main.py` 8876 行分段表） | ✅ |
| 「你自己看一下原作的场景切换」 | `场景系统扩展接口设计方案_2026-09-22.md` §0 抄/不抄对照表（源自 `code.deltarune.wiki` GML 实证） | ✅ |
| **「你要留好拓展接口哦」**（本轮重点） | **`modules/scene_system.py` + `modules/scene_controller.py` + `assets/scenes/*` + `main.py` 4 处接线** | ✅ **本轮新增** |
| 「桌面也会被我当成一个场景」 | `desktop.json` 走**同一套 schema**、索引里走**同一套三级结构**、控制器**零 desktop 特判** | ✅ |

**为什么"文档写完"不等于"接口留好"**：本项目最贵的坑是「函数写对了但产品用不上」（踩过 4 次），
其镜像同样是坑 —— **「接口写好了但没人能调」**。所以本轮把接口**真的落到代码里**：
能被 import、能被 `self.scene.xxx` 调到、能过 G2 回归。

---

## 2. 交付物清单

### 2.1 新增文件（4 个代码/数据 + 1 套回归）

| 文件 | 行数/大小 | 职责 |
|---|---|---|
| `ralsei_pet/modules/scene_system.py` | 约 530 行 | **数据层**：读 JSON + 4 个纯函数 + `SceneState`。禁 Qt / 禁项目内 import |
| `ralsei_pet/modules/scene_controller.py` | 约 270 行 | **接线层**：`SceneController`，双向转发 + `load`/`switch`/`resolve_objects`/`description` |
| `ralsei_pet/assets/scenes/_index.json` | — | 场景总索引（chapter/area/scene 三级，`default_scene=desktop`） |
| `ralsei_pet/assets/scenes/_anchors.json` | 12 个锚点 | 命名锚点表（借原作 `marker` 机制） |
| `ralsei_pet/assets/scenes/desktop.json` | — | **桌面场景**（P0 零可渲染项 → 零行为变化的前提） |
| `code-quality-audit/场景系统-P0/verify_scene_p0.py` | 约 600 行 / **79 项** | 回归锁（已进 G2） |

### 2.2 `main.py` 四处接线（**一个字符都没删**）

| # | 改动 | 位置 | 为什么必须 |
|---|---|---|---|
| 1 | `from modules.scene_controller import SceneController` | `main.py:339` 后 | import 期就要可用 |
| 2 | `_CONTROLLER_ATTRS` 追加 `'scene'` | `main.py:512` | 宿主转发壳才认它 |
| 3 | `self.scene = SceneController(self)` | `main.py:761` 后 | 此时 floor_manager / customization 已就绪；P1 渲染层需要的 `sprite_loader` 也早就在 |
| 4 | **6 个状态字段预声明** | `main.py` 状态声明区（`_hide_*` 段之后） | ⚠️ **漏了就状态劈两份**（见 §5.1） |

预声明的 6 个字段（**唯一真源全在宿主，控制器不持有任何场景状态**）：

```python
self._scene_index = None      # load_index() 的结果
self._scene_state = None      # 当前 SceneState
self.current_scene = None     # 当前场景 id
self.scene_objects = []       # 当前可见物件缓存（P1 渲染层消费）
self._scene_anchors = {}      # 全局命名锚点表
self._scene_loaded = False    # 索引是否已尝试加载（幂等守卫）
```

---

## 3. 核心接口（P1 要用的东西，现在就能调）

```python
# 宿主侧（经转发壳，调用点零改动）
self.scene.load()                      # 读索引 + 加载默认场景（幂等）
self.scene.switch('castle_town_square')# 切场景（失败保持当前场景）
self.scene.ready                       # bool：索引可用 + 有当前场景
self.scene.available_scenes()          # ['desktop', ...]
self.scene.resolve_objects(screen_rect)# P1 渲染层入口：已排序 + 已带屏幕坐标
self.scene.description()               # AI 注入唯一出口（'' = 整段不注入）

# 数据层（纯函数，可单独测）
resolve_anchor(obj, screen_rect, anchors)   # 锚点 → 屏幕像素
pick_variant(entry, tick)                   # 多帧候选 → 这一拍画哪张
depth_of(obj)                               # 绘制排序键（越小越先画）
visible_objects(scene, screen_rect, anchors)# 过滤 + 排序 + 补坐标
```

### 3.1 三条设计律（都在回归锁里有正/负控制成对断言）

1. **算不出就返回 `None`，绝不伪装成 `(0, 0)`** —— `(0,0)` 把"我算不出来"包装成
   "物件就该在左上角"，是本项目最怕的静默降级。锁 C5。
2. **坏元素跳过，不作废整件** —— 一个坏帧不该让整个物件消失。锁 D6 / G2。
3. **未知条件键一律放行** —— `cond` 是渐进增强，老场景包不该被将来的新条件误杀。锁 F7。

### 3.2 桌面 = 一等场景（用户硬要求）

四条证据，全部有断言：

| 判据 | 锁 |
|---|---|
| `desktop.json` 的 `schema_version` 与作品内场景一致 | K1 |
| 索引里走**普通的 chapter/area/scene 三级结构**（不是特例分支） | K2 |
| **控制器里零 `desktop` 特判**（源码级：不出现 `'desktop'` 字面量） | K3 |
| 与物理层口径对齐（`floor_manager.desktop_floor` 早就是一等楼层） | K4 |

---

## 4. 施工期发现的两个"新问题"与处置

### 4.1 ★ 素材口径：本项目素材**一半在仓库外**（已写进模块 docstring）

`sprite_loader` 的 `sprite_dir` 解析到 `<仓库根>/deltarune_ralsei/`，即
`ralsei_pet/` 的**上一级**；而场景 JSON 住在 `ralsei_pet/assets/scenes/`。
**同一个 JSON 里按前缀分两套口径**，由 `kind` 字段决定：

| `kind` | 路径基准 |
|---|---|
| `sprite` | 仓库根（与 `SpriteLoader.sprite_dir` 同源） |
| `image` / `prop` / `bg` / 未知 | 所在目录（素材跟着场景包走 = 安全侧） |

`resolve_asset_path()` 是这两套口径的**唯一翻译点**。

### 4.2 ★ 素材缺口（**唯一阻塞项，需用户裁定**）

实测清点：

| 目录 | 内容 |
|---|---|
| `<仓根>/deltarune_ralsei/` | 1111 张 PNG —— **全是 Ralsei 角色精灵**，零背景/地图 |
| `<仓根>/ralsei_face/` | 50 张表情 |
| `<仓根>/textbox/` | 3 张对话框 + 1 SVG |
| `ralsei_pet/assets/sprites/`、`assets/faces/` | **两个空目录** |
| 全仓库搜索 `bg_` / `tileset` / `room_` / `map_` | **零命中** |

**结论：场景必须有背景/物件素材来源，现阶段一个都没有。** 三条出路：
**A** 程序化生成（纯色/渐变/几何氛围，零版权风险，立即可用）／
**B** 用户提供截图（最贴原作风味，需用户导出）／**C** 重绘。
**推荐 A+B**（A 先把骨架跑通，B 后期增量替换）。**开工 P1 前需要用户点头。**

---

## 5. 验证

### 5.1 回归锁 `scene_p0`：**79 项全绿**

| 组 | 内容 | 项数 |
|---|---|---|
| A | 模块存在 + 零依赖纪律（禁 Qt / 禁项目内 import）+ **负控制**（合成越界样本必须被抓） | 7 |
| B | 数据文件契约（3 份 JSON / 索引解析 / 路径穿越拦截 / 负控制回落） | 13 |
| C | `resolve_anchor`：pos 优先 / 锚点命中 / 兜底 / 多屏负坐标 / 非法→`None` | 6 |
| D | `pick_variant`：单帧 / 轮播 / frames 别名 / 空→`None` / 坏元素跳过 | 7 |
| E | `depth_of`：显式 / y 推导 / 基准 / 垃圾值不抛 | 4 |
| F | `visible_objects`：过滤 3 条 / 排序 / 补坐标 / 不改原 dict / 稳定排序 | 10 |
| G | `SceneState`：容错 / 坏元素 / 锚点清洗 / `describe` 唯一入口 | 5 |
| H | 双向转发：`ready` / 幂等 / 切失败保持 / **状态不劈裂** / 转发通 / `AttributeError` | 13 |
| J | `main.py` 接线：import / `_CONTROLLER_ATTRS` / 实例化 / 6 字段预声明 | 10 |
| K | 桌面一等场景（同级同构，无特判） | 4 |

**施工期探针自身的 4 个 bug（都是"探针写错"，不是产品错）**，已逐条修掉并留痕：

1. **D6** —— 期望值算错（`[None,'a',7,'b']` 洗成 `['a','b']` 后 tick=1 → `'b'`，我写成了 `'a'`）。
2. **F4** —— 口算错。`far`(y=10)→999990、`near`(y=900)→999100、`cond_yes`(**无 y**)→基准 1000000。
   顺带暴露一条**该被写进设计文档的口径**：「无深度信息排最后」（要它靠前就显式写 `depth`）。
3. **H10** —— 用 `code_only_src()` 做字符串匹配 `name != 'p'`，而这个清洗器**会剥掉 STRING token**
   → 被洗成 `name!=`（字面量没了）→ **断言恒假**。改 **AST 口径**，并配负控制（漏护栏的合成源码必须判 False）。
4. **J2** —— 同一个坑的另一面：`"'scene'" in CODE_MAIN` **必定假红**（元组被洗成 `('','','',...)`）。
   改 **AST 取元组元素**。

> ⚠️ **这是本项目第 5 次踩"源码断言别用字符串字面量"这个坑。** 铁律复述：
> **能上 AST 就上 AST**；`code_only_src()` 只适合断言**标识符/运算符**，断言**字面量**必须走
> `code_no_comment()` 或 AST。

### 5.2 G2 全量回归

| 指标 | 施工前 | 施工后 |
|---|---|---|
| 套件数 | 24 | **25**（新增 `scene_p0`） |
| PASS | 1169 | **1250**（+79 新套件 / +2 `round5_smoke`） |
| FAIL | 0 | **0** |
| 比对 | 24 IDENTICAL | **25 全部 IDENTICAL / DIFF=0 / rc=0** |

**施工中唯一的基线变更（已处置）**：`round5_smoke` diff 显示 `待导入模块数: 36 → 38`，
新增两行 `[ OK ] scene_controller` / `[ OK ] scene_system`。这是**预期变化** ——
该套件逐个 import `modules/*.py` 并打印计数，**模块数天然随新增模块而变**
（套件自己的描述里就写着「模块数随新增模块而变，勿把具体数字写进描述」）。
处置：`--only round5_smoke --update` 用**合并模式**重建该套件基线
（确认其余 23 个套件的基线条目一条没少 —— 合并模式正是防"一键抹除"的那道修），
随后 `--update` 全量重建，把 `scene_p0` 也纳入基线。

证据：`code-quality-audit/场景系统-P0/_evidence/`（见 §8）。

### 5.3 零行为变化的直接论证

1. `desktop.json` 的 `bg` / `bgm` / `objects` **全为空/null** → 即使 P1 渲染层接上，画面也**一个像素都不会变**。
2. P0 的 `switch()` **只写宿主状态字段，不动画面、不注册定时器、不碰物理**。
3. `paintEvent` 的唯一动作仍是 `fillRect(全透明)`；像素仍只由 `sprite_label.setPixmap()` 提供。
4. G2 既有 24 套件**输出逐字节不变**（最强判据）。

---

## 6. 明确没做的事（边界）

| 不做 | 为什么 |
|---|---|
| **不接渲染层** | P0 是骨架。接渲染 = 改画面 = 丢掉"零行为变化"这条最强判据 |
| **不动物理** | 楼层 / 甩飞 / 抛物线一律不碰（勿回退契约） |
| **不自己播动画** | 特殊动画只能经 `ai_driver` 决策或用户显式交互（用户第八轮明令） |
| **不注入 AI 上下文** | P1 才加，且**只加一句场景描述**（保 K3 锁 + Ollama KV 前缀缓存） |
| **不夺 customization_system 的权** | P0 只留接口；接管留到 P1（那是官方预留的接入坑位） |
| **不抄原作的 640×480 锁画布 / 一屏一房间 / `scr_destroyoutside` / 摄像机跟随** | 桌面是**任意尺寸、多屏、非固定**的环境，照抄会直接坏掉 |

---

## 7. 待用户裁定

| # | 事项 | 影响 |
|---|---|---|
| 1 | **场景美术素材来源**（A 程序化 / B 你提供截图 / C 重绘） | 🔴 **P1 开工前唯一阻塞项** |
| 2 | 场景粒度（一块屏 = 一个场景？还是"区域"内可横向延展？） | 影响 `resolve_anchor` 是否需要"区域矩形"概念 |
| 3 | 是否要 BGM | 现 `sound_manager` **只有 3 个音效、无 BGM 能力** → 要 BGM 得先扩它 |
| 4 | 原作版权尺度（直接复用原作素材图 vs 只借鉴风格） | 决定 P1 能不能直接用 `ralsei_face/` 那类素材 |

---

## 8. 证据索引

| 文件 | 内容 |
|---|---|
| `code-quality-audit/场景系统-P0/_evidence/scene_p0_run.txt` | 回归锁 79 项输出 |
| `code-quality-audit/场景系统-P0/_evidence/G2_after_p0.txt` | G2 全量回归（重建基线那次） |
| `code-quality-audit/场景系统-P0/_evidence/G2_verify_sync.txt` | G2 同步复跑 → **25 套件全 IDENTICAL** |
| `code-quality-audit/场景系统-P0/_evidence/G2_baseline.txt` | 施工前基线（24 套件 / 1169 PASS） |
| `code-quality-audit/场景系统-P0/_evidence/baseline_before_p0.json` | 施工前的 `baseline.json` 快照（可回滚） |
| `code-quality-audit/场景系统-P0/_evidence/main_py_diff.txt` | `main.py` 全部改动（+33 / −1） |
| `场景系统架构地图_2026-09-22.md` | 只读勘察的现状地图（项目根） |
| `场景系统扩展接口设计方案_2026-09-22.md` | 三级模型 / 切换管线 / P0–P4 分期（项目根） |
