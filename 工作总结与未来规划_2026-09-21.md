# 近期工作总结与未来规划

**日期**：2026-09-21
**项目**：ralsei_pet（Deltarune Ralsei 桌面宠物）
**统计口径**：本文所有数字均从仓库实际状态读出（git log / 文件字节数 / G2 基线 JSON），非估算。

---

## 一、总体进度概览

| 维度 | 数值 |
|---|---|
| 已推进轮次 | 第 1 ~ 26 轮（另有若干轮收尾/记忆提交） |
| 提交总数（截至 `eb7658a`） | 见 `git rev-list --count HEAD` |
| 工作区状态 | **clean**，与 `origin/main` 完全一致 |
| G2 回归套件 | **24 套件 / 1164 PASS / 0 FAIL**，全 IDENTICAL |
| `main.py`（宿主） | 10481 行 → **8987 行**（−1494 行，**−14.3%**） |
| 已抽出控制器 | **5 个**（games / video / spell / hide / file_sheet） |
| 项目根报告 | **36 份** |
| `modules/*.py` | 37 个 |

---

## 二、近期工作（第 22 ~ 26 轮）

### 第 22 轮 · 回答篇幅约束（2026-09-20）
发现一道"看起来在工作"的闸一直在砍正常回答：`AI_REPLY_MAX_CHARS` 原值 150 是当年 3B 模型拍的，
换到底座 `qwen3:4b` 后**这个值反而在截断正常回答**。

- 用 `measure_token_ratio.py` 实测推导新值 **220**（可算，不是拍的）
- 教训：**先怀疑夹具，再怀疑被测物**（同类错第 3 次）

### 第 23 轮 · 真机探针保真度（2026-09-20）
探针复刻不真 → **报假问题**。本轮确立"判据分三路拿"的设计，新增锁 **W1–W8**。

### 第 24 轮 · H4/H5 Wave 1 六项全做完（`953c8ee`）
上帝类拆分的第一次集中冲刺，**六项全部落地并推送**。

### 第 25 轮 · 跨项技术债清理（`8436fe1`）
- 守卫写法收敛 / `GamesController` 补 `__setattr__` / `main.py` 8 处读守卫加强
- `scan_zero_refs.py` 过期清单标 `[已移除]`
- 新增两条铁律：**detail 里放任何数字都会漂**（§5.3）、**探针源码清洗器用 `ast.unparse`**（§5.4）

### 第 26 轮 · W1-7 收口（`bf8feaa`，2026-09-21）
把 `handle_game_input` 搬进 `GamesController`，**Wave 1 业务方法至此全部归位**。

本轮最有价值的**不是**这次搬运，而是揪出了两个"旧判据不可信"的问题：

**① W1-3 的 A/B 是「假对照」（鉴别力为零）**

| 项 | 真正的父版本 `d8e2878^` | 当时用的 `_evidence` 快照 |
|---|---|---|
| 行数 | 10481 | 10530（**+49，零删除**） |
| 差异 | — | 3 hunks，**全是 W1-3 自己新增的转发壳** |
| 7 个方法 | — | **仍定义在 `RalseiPet` 上**（B 组跑的是直连） |

也就是说，那一轮 A/B 比的是「有壳无搬」vs「有壳已搬」—— **一个结构上不可能改变行为的维度**。
它报"全绿无差别"，而且**非常像真的**。

> **新增 A/B 失效谱第 4 形态**：*参照系不是父版本*。
> **推论（断言分层）**：A/B 的尺子必须**两版都该全绿** ⇒ **描述"搬运本身"的归属断言不能放进共享 e2e**
> （否则 A 组必红，A/B 报的是"断言放错层"而不是产品缺陷）。**归属断言进专测，行为断言进 e2e。**

**② 一个潜伏的假绿**：提取探针用裸文本匹配 `'self.game_state =' in 源码`，唯一命中是**注释**。
→ 第 6 次踩"源码级断言必须先剥注释/字符串"。修法：**AST 级判据 + 反向控制**。

**③ 一个环境事实（长期影响）**：本机 `core.autocrlf=true` 且仓库**无 `.gitattributes`**
⇒ 仓库存在**两套换行口径**：

| 口径 | 数量 | 代表文件 |
|---|---|---|
| 双口径（blob=LF / worktree=CRLF） | **14** | `dialogue_ui.py`、`ai_driver.py`、`pet_ai.py` … |
| 单口径（都是 LF） | **58** | `main.py`、所有 `*_controller.py` |

后果：`git status` 对这 14 个文件**看不见差异**；做逐字节参照系**只能用 `git cat-file blob`**。

### 收尾 · 记忆体系维护（`eb7658a`）
`MEMORY.md` 达 16364 B 触发注入上限截断 → 按铁律「**先补详版再压速查**」处理：
详版补 §8.5（W1-6）/ §8.6（W1-7）/ §13（收口索引），速查本压回 **16348 B**。

---

## 三、H4/H5 上帝类拆分 · Wave 1 收口

| 项 | 控制器 | 规模 | 提交 | 日期 |
|---|---|---|---|---|
| W1-3 | `GamesController` | — | `d8e2878` | 09-20 |
| W1-4 | `VideoController` | — | `2d288c9` / `cc03ca4` | 09-20 |
| W1-1 | `SpellFlowController` | — | `26bb8c8` / `9770e39` | 09-20 |
| W1-2 | `HideAndSeekController` | 12 方法 / 342 行 | `e245f13` | 09-21 |
| W1-5 | office 死代码清理 | — | — | — |
| W1-6 | `FileSheetController` | 4 业务 / 469 行 | `953c8ee` | 09-21 |
| **W1-7** | `handle_game_input` → `games` | 37 行 / 1754 字符 | **`bf8feaa`** | **09-21** |

**成效**：`main.py` **10481 → 8987 行（−14.3%）**；`RalseiPet` 方法数 **175 → 170**。

### 沉淀：W1 搬运铁律 8 条
① `self` 传出去救不了 → `QTimer(self.p)`；② 控制器**必实现 `__setattr__`**（必跳过 `'p'`）；
③ **新状态名在宿主 `init_systems()` 预声明**（不是 `__init__`）；④ `_log` 用 `_log_()`；
⑤ 锁的定位器跟代码搬家；⑥ `_log.` → `self._log_().`；⑦ `os.`/`time.`/`random` 是"裸 Attribute 根"（只扫裸 Call 看不见）；
⑧ 桩打在「方法体里 `self` 的那张类」上。

### 沉淀：分组器四类偏差
`scan_method_index.py` **只是线索，不是范围定义**：漏标 / 误纳 / **跨区散布** / 方案自身错。
最危险的是**漏标第 8 方法** → 照索引施工会留下"被搬走的方法所调用的方法" → 运行期 `AttributeError` 被 `except` 吞掉 = **静默功能失效**。

---

## 四、人味改造线（第 19 ~ 23 轮）

| 项 | 内容 |
|---|---|
| 人设单一真源 | `assets/ralsei_persona.md`（每次对话读出来当 `system`） |
| 底座 | `ralsei:v3`（`qwen3:4b-instruct-2507-q4_K_M`） |
| 世界观 | `assets/ralsei_worldview.md` + `worldview_recall.py`（按需召回，**无命中返回空串**） |
| 关系演进 | `relationship.py`（四档 distrust→guarded→warming→friend，**信任度不可见**） |
| 护栏 | `_clean_ai_reply` 四道闸（0a 括号动作 → 0b 剥 markdown → 0c 禁说清单 → 判退重采样） |
| 回归锁 | `persona_chat`(153) / `s8_stream`(69) / `s7_event_speech`(135)，**不联网、不调 Ollama** |

**最关键的一条认知**：persona **写在 Modelfile 里一次都不生效** —— Ollama 用 messages 里的 system
**整体替换** Modelfile 的 SYSTEM。所以 `ralsei_persona.md` 是**本机唯一可行的"训练"杠杆**（真 LoRA 已结账：本机不可行）。

---

## 五、未来规划

### P0 · 立即可做

**① 「预声明规则」升格为 H4/H5 正式施工口径**
它是搬运铁律 ③ 的核心，目前仍散落在各份施工报告里 —— 应抽成独立的一节正式口径。

**② 铁律 6–9 条 + A/B 失效谱（三形态 → 四形态）写进总口径文档**
本次新增的第 4 形态（参照系不是父版本）与"断言分层"尚未写成正式文档。

**③ W1-6 的 B 块 3 个 `check_*_content` 交给 H4「文件反应」专项**
已在上一轮明确留给该专项，属于有意的延后而非遗漏。

### P1 · 需要用户参与

**④ 真机手感实测（只能用户做）**
三件事一直是**契约级 + 单元级已锁、真机未验**：
- 宠物被前台窗口遮挡的情况
- 上下移动与落地手感
- 走到屏幕边缘掉下去不生气、但关窗仍生气

### P2 · 需要先议后动

**⑤ 仓库换行口径（加 `.gitattributes`）**
本机 `core.autocrlf=true` 且无 `.gitattributes` ⇒ 14 个文件双口径。
**这是改变仓库约定的动作**，会影响所有协作者与历史 diff，**应先议后动**，不宜我单方面拍板。

### 长期遗留（不阻塞）

- `climb_to_top_window()` 仍走裸窗口路径（故意没动：它挑最前面的窗口，按定义不会被盖住）
- `code-quality-audit/` 编码告警存量**已冻结归档**
- 日志滚动：**旧诊断实测不成立** → **不凭"应该会坏"改全局日志行为**
- S7 第二批 ~11 处候选：**不阻塞**、不会让 Ralsei 变话多
- `.bak` / `.backup_*` 备份文件未删（等用户点头）
- `E:\Download\_tmp\` 留存历轮临时文件（**因用户此前明确指示不要重试批量删除，故未动**）

### 建议的下一步

按优先级，**建议先做 P0 的 ①②**（都是纯文档/口径收敛，无产品风险，且能把本轮最贵的教训固化下来），
完成后请用户做 **④ 真机实测**，再讨论 **⑤**。

---

## 六、需要用户决策的事项

1. **真机手感实测**：是否近期安排？（这是唯一卡在用户侧的关键验证）
2. **仓库换行口径**：是否加 `.gitattributes` 统一为 LF？（改变仓库约定）
3. **`E:\Download\_tmp\` 清理**：约 50 个历轮临时文件，是否清理？
4. **`.bak` 备份文件**：是否删除？
