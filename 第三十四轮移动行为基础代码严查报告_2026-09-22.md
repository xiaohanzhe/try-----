# 第三十四轮 · 移动 / 行为基础代码严查报告

**日期**：2026-09-22
**范围**：桌面宠物「移动 / 行为」基础代码（用户当前唯一主轴）
**性质**：**只读审查 + 定点修复**（审查阶段未改动产品文件；修复阶段按下述清单定点改动）
**G2 基线**：`PASS=1359 FAIL=0`，26 套件全 IDENTICAL（改动前已确认）
**G2 结果**：`PASS=1427 FAIL=0`，**29 套件全 IDENTICAL**（最终；新增 `round34_movement` 36 项 + `round34_floor_manager` 15 项 + `round34_store_config` 17 项）
**真机**：`src/main.py` 启动 40~42s 存活、RSS 192.3MB（含首帧预热）、**零 Traceback / 零 ERROR / 零 `Logging error`**
**证据目录**：`code-quality-audit/第34轮-移动行为基础代码严查/_evidence/`

---

## 〇之二、修复执行情况（本轮已落地）

| 编号 | 改动 | 文件 | 等价性论证 |
|---|---|---|---|
| **F1** | 删除 `mouseEnterEvent` / `mouseLeaveEvent`（假 Qt 钩子） | `main.py` | 三重实证其从未执行过；功能已由 `mouseMoveEvent` 覆盖 |
| **F2** | 删除 `fall_start_time` 全部 6 处写点 | `main.py` | AST 双向扫描：0 读点、modules 零引用 |
| **F3** | 合并 `init_movement` 内 5 组重复赋值 | `main.py` | ⚠️ 保持运行时等价（`max_fall_duration` 取 **2.0**，非先出现的 5.0） |
| **F4** | `start_fall` 四分支 `is_moving=False` 提取到分支前 | `main.py` | 四分支值一致且后续无改写；`idle_timer` 的真实差异**刻意保留** |
| **F5** | 新增 `_SafeTimedRotatingFileHandler`，兜住 rollover 失败 | `logger_utils.py` | 正负控制成对：原生吐 `Logging error`、安全类不吐且继续写盘 |
| **F6** | 跳跃动画一律 `jump_ball` | `main.py` | 用户口径；`has_ball` 恒假 ⇒ 原 `jump` 分支永远生效，`jump_ball` 是死分支 |
| **F7** | ★ `_index_of_floor` 退化分支 `i-1` → `0 if i==0 else i-1` | `floor_manager.py` | **行为级对照**：修复前落点被"上吸"到更高层 / `adjacent_lower_floor` 误报 None；修复后均正确 |

**回归锁**：新增 G2 套件 `round34_movement`（36 项）+ `round34_floor_manager`（15 项）。
**鉴别力体检**：故意破坏 5 处（假钩子/死字段/初值 5.0/裸 handler/`return i-1`），**5/5 全部被抓住**（rc=1）；
每次体检后**逐字节核验文件已还原**。

### F5 的来由（真机实测暴露，值得单独记）

真机启动日志里出现：

```
--- Logging error ---
Traceback (most recent call last):
  File "C:\Python311\Lib\logging\handlers.py", line 435, in doRollover
    self.rotate(self.baseFilename, dfn)
OSError: [WinError 1] 函数不正确。:
  'E:\RalseiMemory\logs\ralsei_pet.log' -> 'E:\RalseiMemory\logs\ralsei_pet.log.2026-09-21'
```

**根因**：`TimedRotatingFileHandler.doRollover()` 在 `emit()` 内部被调用（即**每次写日志**都可能触发），
而项目日志落在 **E 盘（exFAT）**，其 `os.rename` 跨日归档会抛 `WinError 1`。
`logger_utils._init_logging_impl()` 的 `try/except` **只包住了 handler 的构造**，包不住运行期 rollover。

**症状**：每次 rollover 都往 stderr 吐一大段 Traceback + 本次写盘被放弃；日志**永远不切割**，
`backupCount=7` 形同虚设（旧日志无限追加）。

**修复**：子类覆盖 `doRollover()`，失败时把 `rolloverAt` 前推一天并静默降级为"继续写当前文件"——
日志不丢、不再刷 Traceback、失败每天只尝试一次。

---

## 〇、结论速览

按用户要求「严查基础代码有没有纰漏」，本轮共查实 **5 类真问题、1 类待议**，
并额外在真机验证时发现 **1 个活跃缺陷（日志 rollover）**：

| # | 问题 | 类型 | 危害 | 处置 |
|---|---|---|---|---|
| 1 | `fall_start_time` 是纯死字段 | 死字段 | 无行为危害 | ✅ **已删**（6 处写点） |
| 2 | `mouseEnterEvent`/`mouseLeaveEvent` 非 Qt 钩子名 | 死代码 | 无行为危害，是扩展陷阱 | ✅ **已删**（已实测证伪） |
| 3 | `init_movement` 内重复赋值（5 组） | 误导性冗余 | 无行为危害，注释语义误导 | ✅ **已合并** |
| 4 | 7 个 trigger/状态函数零调用 + 5 个孤儿状态位 | 死代码 | 无行为危害 | 按批处置（本轮未动） |
| 5 | 回收站反应整条链断裂 | 死链 | **功能从未生效** | 归 H4 专项（第26轮已记） |
| 6 | 空闲分支状态覆盖不全 | 防御性不一致 | 当前无危害 | 补条件 + 注释（本轮未动） |
| **7** | **日志 rollover 在 E 盘（exFAT）失败** | **活跃缺陷** | **日志永不切割 + 反复刷 Traceback** | ✅ **已修**（F5） |

**关键判断：本轮未发现任何「正在造成错误行为」的移动/行为 bug。**
唯一一个"活跃"缺陷在日志层（F5），不在移动链路。

> ⚠️ 这个结论与我的第一直觉不同——我原以为会挖出活跃 bug。
> 实际是因为第 13~18 轮的「建楼」系统已经把这些链路修得很扎实
> （注释里能查到每一次修复的起因）。

---

## 一、★ 真纰漏 1：`fall_start_time` 是纯死字段

**证据**（`_evidence/01_...txt` + `_evidence/_r34_deadcheck.txt`）：

```
写点 6 处，读点 0 处（含 getattr 动态读全扫描也无）
  L1102  self.fall_start_time = None      ← init_movement，初值 None
  L1202  self.fall_start_time = 0         ← init_movement，改成 int 0（类型漂移）
  L2863  self.fall_start_time = time.time()   ← start_falling
  L3445  self.fall_start_time = time.time()   ← start_fall
  L3550  self.fall_start_time = time.time()   ← trigger_splat
  L5492  self.fall_start_time = time.time()   ← mouseReleaseEvent（甩飞）
```

**三重验证其为死字段**：
1. AST 读写分离统计：read=0
2. `getattr(self, '<name>')` 全项目动态读扫描：**87 个属性名里没有它**
3. 逐行全文检索：6 处全是写

**顺带发现的类型漂移**：`None`(L1102) → `0`(L1202) → `time.time()`(float，运行时)。
即使将来有人接线，初值类型也不自洽。

**处置**：删除全部 6 处写点。
理由 = 项目铁律「**"没改也没人调用"要么接线、要么删**」。
若想保留，正确做法是接线成"坠落已历时 = now - fall_start_time"的诊断量——
但 `fall_duration` 已经在承担这个职责（且是 `+= elapsed_time` 累加，更稳），
所以**删**更符合"单一真源"。

---

## 二、★ 真纰漏 2：`mouseEnterEvent` / `mouseLeaveEvent` 不是 Qt 钩子

**这是本轮最有价值的发现**，因为它属于「无报错、无日志、回归锁也抓不到」的静默失效。

### 2.1 三重实证（`_evidence/04_...txt` + `_evidence/_r34_event_probe.txt`）

| 证据 | 结果 |
|---|---|
| ① 真实事件派发（`app.sendEvent` + `QEnterEvent`/`QEvent.Leave`） | 只命中 `enterEvent`/`leaveEvent`；`mouseEnterEvent`/`mouseLeaveEvent` **一次都没命中** |
| ② 基类成员实证 | `hasattr(QWidget,'enterEvent')=True`；`hasattr(QWidget,'mouseEnterEvent')`=**False** |
| ③ 命名对照 | 同文件其他 6 个钩子（`mousePressEvent`/`mouseMoveEvent`/`mouseReleaseEvent`/`mouseDoubleClickEvent`/`moveEvent`/`paintEvent`）**全部正确** |

⇒ Qt 事件按**方法名精确查找**，拼错的钩子**从未执行过一次**。

### 2.2 危害评估（不夸大）

`mouseEnterEvent`(L5698) 做的事：`setCursor(PointingHandCursor)` + `on_mouse_hover()`
`mouseLeaveEvent`(L5703) 做的事：`setCursor(ArrowCursor)`

| 项 | 是否真缺失 | 依据 |
|---|---|---|
| 光标变手型 | ✗ **不缺** | `mouseMoveEvent` L5316-5317 已逐帧实现；L5424 恢复箭头 |
| `on_mouse_hover()` | ✗ **不缺** | L5707-5713 该函数体第 8 轮已清空，只剩 `return` |

⇒ 定性：**永久失效的冗余实现，零当前危害**。
但它是**扩展陷阱**：未来往这两个方法里加逻辑会**静默失效**，
且 G2 抓不到（错误在"名字"层面，不在行为层面）。

> 💡 **过程价值**：我差点把它报成"光标不变手型"的活跃 bug。
> 靠**读原文**（发现 `mouseMoveEvent` 已有正确实现）才拦下。
> 这正是 MEMORY.md「报红先自问"夹具真把破坏写进去了吗"」的实践。

**处置**：**删除**两个方法（方案 A，推荐）。
理由：已有另一套正确实现，属纯冗余；删比重命名更干净。
备选方案 B（重命名为 `enterEvent`/`leaveEvent`）会"新增一次进/出时光标设置"，
虽幂等等价，但 `enterEvent` 在 Qt5 中可能被子控件/重绘频繁触发，需实测，不划算。

---

## 三、★ 真纰漏 3：`init_movement` 内 `max_fall_duration` 冗余赋值

**证据**（`_evidence/01_...txt`）：

```
init_movement (L1067~L1259) 内：
  L1161  self.max_fall_duration = 5.0
  L1204  self.max_fall_duration = 2.0   ← 覆盖，且中间无任何读取点
                                        ⇒ 最终初值 = 2.0，L1161 纯冗余
```

**危害不只是"多写一行"**：它给人"初值是 5.0"的误导。
真实语义：初值 2.0 ⇒ dazed 阶段时长 = `max(1.0, 2.0 - 2.0)` = **1.0s**
（若真按 5.0 走会是 3.0s —— **行为差异 2 秒**）。

### 3.1 同时发现的"三类重复赋值"（幂等，非 bug）

| 字段 | 赋值次数 | 值 | 性质 |
|---|---|---|---|
| `is_falling` | 3（L1100/L1158/L1198） | 全 False | 幂等，可读性问题 |
| `is_recovering` | 2（L1101/L1159） | 全 False | 幂等 |
| `fall_duration` | 2（L1160/L1200） | 全 0.0 | 幂等 |
| `max_fall_duration` | 2（L1161=5.0 / L1204=2.0） | **值不同** | ★ 误导性 |

**处置**：合并去重。若要动，必须配断言锁住"合并后最终值不变"。

### 3.2 ⚠️ 重要的反面结论：`start_fall` 里的 4 处**不许删**

```
L3480 = 3.0 (window_move)     L3491 = 5.0 (fall_from_window)
L3501 = 5.0 (fall_off)        L3511 = 3.0 (默认)
```

**判据（两条坠落链路的对称性，极易看错）**：

| | `start_falling()` L2852 | `start_fall()` L3429 |
|---|---|---|
| 状态位 | `is_gravity_falling=True`、`is_falling=False` | `is_falling=True`、`is_gravity_falling=False` |
| 分派到 | `handle_gravity_fall` | `handle_fall` |
| 读 `max_fall_duration`？ | **✗ 不读** | **✓ 读**（L3815 / L3836） |

⇒ `start_falling` 里那句（第 17 轮已删）是死的；
但 **`start_fall` 里这 4 句是活的，绝不能一起删**。
同一个属性名，在两条链路上"一读一不读"——这是本项目最容易误判的点。

---

## 四、★ 真纰漏 4：死函数与孤儿状态位

### 4.1 产品文件内零调用的函数（经逐名核实）

**扫出 25 个，但第一版探针误报 31 个** → 见 §六"探针教训"。

确认真孤儿：

| 函数 | 行号 | 性质 |
|---|---|---|
| `_is_falling_through_window` | L2908 | ★自研碰撞检测旧启发式，**已被建楼系统取代**（与第 15 轮删掉的 `find_support_below`/`is_on_floor_edge` 同类残留）；注释自称"简化处理"⇒ **不是权威判据**（契约②：`floor_visible_contains` 才是唯一判据） |
| `move_to_edge_and_shrink` | L4307 | 移动行为类，零消费者 |
| `check_recycle_bin` | L8456 | 回收站检查，零消费者（见 §五） |
| `start_mouse_drag` | L2202 | 仅测试脚本引用，产品内零调用。⚠️ 但同群的 `initiate_auto_mouse_drag` 是**活的**（`main.py:987 timer.connect`）—— 别一起删 |
| `react_to_file_emotionally` | L8296 | 第 26 轮已归 H4「文件反应」专项，**有既定处置计划**，勿擅自删 |
| `suggest_game` / `tell_joke` | L4423/L4408 | AI 对话技能类，零消费者 |
| `init_api_client` | L6031 | 零消费者 |
| `trigger_laugh` / `trigger_surprise` / `trigger_shy` / `trigger_unhappy` / `trigger_victory` / `trigger_teasplash` / `reset_special_states` | L3962~L4016 | 7 个状态触发器全零调用 |

**❌ 明确不是孤儿（靠跨文件/动态绑定扫描才找回来，别误删）**：

| 函数 | 活调用点 |
|---|---|
| `initiate_auto_mouse_drag` | `main.py:987` `auto_mouse_drag_timer.timeout.connect(...)` |
| `handle_window_operation` | `dialogue_ui.py:1802` `self.root.handle_window_operation(raw)` |
| `close_bilibili` | `video_controller.py:399` `self.close_bilibili()` |

### 4.2 孤儿状态位（一旦置 True 永不复位）

| 状态位 | 复位路径 | 消费者 | 定性 |
|---|---|---|---|
| `is_surprised` | ✓ 有（2.0s，L8043） | 有 | 健康 |
| `is_happy` | ✓ 有（3.0s，L8050） | 有 | 健康 |
| `is_shy` | ✓ 有（2.5s，L8056） | 有 | 健康 |
| `is_unhappy` | ✓ 有（2.0s，L8062） | 有 | 健康 |
| `is_laughing` | ✗ **无** | **无** | 孤儿 |
| `is_teasplashed` | ✗ **无** | **无** | 孤儿 |
| `is_victorious` | ✗ **无** | **无** | 孤儿 |
| `is_rolling` | ✗ 无（只 init 时置 False） | 无 | 孤儿 |
| `is_sliding` | ✗ 同上 | 无 | 孤儿 |

唯一"看起来能复位"的是 `reset_special_states()` —— 但它**零调用**。

**危害评估（不夸大）**：这些位本身**零消费者** ⇒ **当前零行为危害**。
但属**扩展陷阱**：未来若有人读 `is_laughing` 做判断，会拿到"永久 True"的错值。

> 附注：`trigger_laugh()` 同时设 `is_happy=True` + `happy_timer=0`，
> 而 `is_happy` **有消费者**（3s 后复位）⇒ 这条链是通的 ✓
> 即"笑→开心 3 秒"是生效的，只有 `is_laughing` 本身是孤儿。

---

## 五、★ 真纰漏 5：回收站反应整条链断裂

```
check_recycle_bin()            L8456   ← 零调用 ✗
    └─ 内部调用
       _move_away_from_recycle_bin()   L8502 调用 / L8507 定义
           └─ 内部写
              self.is_moving = True    L8519   ← 死写点（第 2 个）
```

**`check_recycle_bin` 既零调用又无定时器绑定** ⇒ 整条"宠物对回收站的反应"功能**从未生效**。

**处置**：归入 H4「文件反应」专项（第 26 轮已记录，`react_to_file_emotionally` 同批）。
**本轮不动**——避免与既定专项计划冲突。

---

## 五之二、★ 真纰漏 6（活跃缺陷）：日志 rollover 在 E 盘失败 → 日志永不切割 + 反复刷 Traceback

**这条是本轮唯一"正在造成错误行为"的缺陷**，且**不是靠静态审查发现的**——
是**真机验证**（跑 `src/main.py` 12 秒抓 stdout）时从启动日志里捞出来的。
这印证了一条方法论：**静态审查找"结构问题"，真机跑才能找"环境相关缺陷"**。

### 现象（真机原始日志）

```
2026-09-22 23:39:12 [INFO] ralsei_pet.text_segmenter — 已启用 jieba 分词（用户词表: 无）
--- Logging error ---
Traceback (most recent call last):
  File "C:\Python311\Lib\logging\handlers.py", line 74, in emit
    self.doRollover()
  File "C:\Python311\Lib\logging\handlers.py", line 435, in doRollover
    self.rotate(self.baseFilename, dfn)
  File "C:\Python311\Lib\logging\handlers.py", line 115, in rotate
    os.rename(source, dest)
OSError: [WinError 1] 函数不正确。:
  'E:\RalseiMemory\logs\ralsei_pet.log' -> 'E:\RalseiMemory\logs\ralsei_pet.log.2026-09-21'
```

### 根因（三层）

1. **触发时机**：`TimedRotatingFileHandler.doRollover()` 由 `emit()` 内部调用 ——
   也就是**每次写日志**都可能触发。异常因此发生在"运行期"，而不是"初始化期"。
2. **环境**：项目日志落在 **E 盘**（`E:\RalseiMemory\logs\`，按第九轮"E 盘为唯一最终存储"的设计），
   而 **E 盘是 exFAT**，其 `os.rename` 跨日归档会抛 `WinError 1`。
3. **保护缺位**：`logger_utils._init_logging_impl()` 里的
   ```python
   try:
       file_handler = TimedRotatingFileHandler(...)   # ← try 只包住"构造"
   except Exception as e:
       print(f"[日志] 文件日志初始化失败，仅使用控制台: {e}")
   ```
   **只包住了 handler 的构造**，包不住 `doRollover`。

### 后果

- 每次写日志若恰逢 rollover 判定点 → 都在 stderr 吐一大段 Traceback + **放弃本次写盘**；
- **日志永远不切割**：`ralsei_pet.log` 无限追加，`backupCount=7`（保留 7 天）**形同虚设**；
- E 盘下长期运行 → 单文件持续膨胀。

### 修复（F5）

在 `logger_utils.py` 新增 `_SafeTimedRotatingFileHandler`，覆盖 `doRollover()`：

```python
def doRollover(self):
    try:
        super().doRollover()
    except Exception:
        # 降级为"沿用当前文件"：把下次切割时间往后推一天，
        # 避免每次写日志都重试一次（高频写入下会疯狂刷失败日志）。
        import time as _time
        self.rolloverAt = int(_time.time()) + 86400
```

并把初始化处的裸 `TimedRotatingFileHandler` 换成此类。

**权衡说明**：这是"**降级而非修复根因**"——exFAT 的 `os.rename` 限制无法绕过（除非改存储位置或改归档方式，
那属于存储线的事，会引入更大改动面）。选择降级是因为：
日志的价值在**内容**而非**文件名**；宁可"不切割、继续写同一个文件"，也不要"每次写都失败"。

### 正负控制（成对，有鉴别力）

| 判据 | 负控制（原生 handler） | 正控制（安全类） |
|---|---|---|
| 吐 `--- Logging error ---` | **True**（stderr 406 字符） | **False**（stderr 0 字符） |
| 失败后日志仍写盘 | —— | **True**（`pos-one` 在文件里） |
| `rolloverAt` 前推 | —— | **True** |

已纳入回归锁 `round34_movement` 的 [E] 段，并在鉴别力体检中确认"换回裸 handler 会报红"。

### 真机复验（含一次"探针假象"的反转，必须记录）

修复后真机复验，`Logging error` **仍然出现** —— 但栈变了：

```
修复前：handlers.py:74 emit → doRollover → rotate → os.rename
        OSError: [WinError 1] 函数不正确。
修复后：logging/__init__.py:1114 emit → self.flush()
        OSError: [Errno 22] Invalid argument
```

⇒ `doRollover` 那条栈**已彻底消失（F5 生效）**；剩下这条是 **`StreamHandler.flush()`（控制台 handler）**。

**再换一种抓取方式复核**（`stdout=PIPE` 而不是 `stdout=open(file)`）：

```
Logging error present? **False**
Traceback present?      **False**
bytes = 398（全是正常启动日志）
```

**结论**：那个 `Errno 22` 是**我的探针假象** —— 用 `stdout=open(path,'w')` 把子进程 stdout
接到父进程持有的文件对象上，`StreamHandler.flush()` 在该接法下会 `EINVAL`。**不是产品缺陷。**

> ⚠️ **如果当时只看第一次结果，我会误判"F5 无效"并去改一个本来正确的实现。**
> 这是本轮第 2 次踩「**探针不保真 = 报假问题**」（第 1 次是零调用扫描误报率 55%）。
> 完整对照见 `_evidence/06_F5真机复验取证.txt`。

---

## 六、★ 本轮方法论产出：探针误报率 55%

**这条比任何单个纰漏都更该记住。**

零调用扫描**第一版报 56 个"死函数"**，其中包含
`update_movement` / `update_animation` / `_on_reply` / `update_ai`
—— **这些天天在跑**（用户亲眼看到宠物在动）。

**根因**：探针只收集 `ast.Call` 的 func 名。
而 Qt 的定时器/信号是 **`timer.timeout.connect(self.foo)`** ——
`self.foo` 是 `ast.Attribute`，**不是 `ast.Call`** ⇒ 全部漏掉 ⇒ 假死报。

**修法**：被"引用" = `ast.Call` 的 func ∪ **任何 `ast.Attribute` 的 attr** ∪ 任何 `ast.Name`；
且**必须跨文件**扫 `modules/*.py`
（`dialogue_ui.py:1802 self.root.handle_window_operation(raw)` 就是靠跨文件扫描才找回来的）。

**修正后 56 → 25，误报 31 个（55%）。**

⇒ 第 N 次验证 MEMORY.md §4：
「**探针不保真 = 报假问题**」「**必须拿产品函数逐条复核，不能只看探针计数**」。

---

## 七、已核实「不是问题」的项（防止后续误改）

### 7.1 状态互斥纪律 —— 全部 ✓ 干净

```
is_falling + is_gravity_falling   ✓ 无同函数共现（两种坠落互斥，判据正确）
is_sleeping + is_moving           ✓
is_jumping + is_falling           ✓
is_jumping + is_gravity_falling   ✓
is_splat + is_sleeping            ✓
```

正当的"同时置 True"组合（复核后都合理）：
- `trigger_splat()` → `is_falling` + `is_splat`（落地进摔扁阶段）
- `handle_fall()` → `is_recovering` + `is_splat`（爬起时清 splat）
- `trigger_laugh()` → `is_happy` + `is_laughing`（笑=开心）

### 7.2 `update_movement` 热循环 —— 质量高于预期

已核实正确的关键设计（**勿动**）：
1. **dt 截断到 0.1s**（防瞬移）—— 正确
2. `_last_desktop_elem_check` 赋值在 `try` **外** —— 注释说明"防抛错后每 30ms 全量重试"，**刻意设计，正确**
3. `base_move_distance = new_speed` **不乘 elapsed_time** —— 注释明确"speed 语义=像素/帧"，乘 dt 会四舍五入为 0（太空滑步）⇒ **正确且关键**
4. 到达时必须 `force=True` 切 idle —— note 说 walk/run 优先级 2 > idle 1，不带 force 会被拦（"走着走着突然定住"）⇒ **正确且关键**
5. `_cached_screen_geom` + `_virtual_screen_rect()` —— 多屏契约①，正确
6. spell 阶段不在此切方向动画 —— 避免与 `_tick_spell_flow` 双算法打架（斜向抽搐），正确

---

## 八、低优先级疑点（记录，本轮不改）

| # | 位置 | 疑点 | 判定 |
|---|---|---|---|
| 1 | L2005-2006 | clamp 时若 `width() > screen_geom.width()`，`max(left, ...)` 会钉在 left ⇒ 右侧出屏 | 极端条件（DPI+超大尺寸），罕见 |
| 2 | L2084-2103 | 空闲分支的 elif 链**未覆盖** `is_gravity_falling` / `is_recovering` / `is_sleeping` | 当前不会出问题（上面已 return）⇒ **防御性不一致**，建议补条件+注释，**但需先实测有无漏网路径** |
| 3 | L3483/3495/3505/3515 | `start_fall` 四分支各写一次 `is_moving = False` | 幂等，可提到分支外（可读性） |
| 4 | L5698/L5703 | （已升级为纰漏 2） | — |

---

## 九、下一步

### 已完成（本轮）

| 优先级 | 动作 | 状态 |
|---|---|---|
| P1 | 删 `fall_start_time` 全部 6 处写点 | ✅ 完成（F2） |
| P1 | 删 `mouseEnterEvent`/`mouseLeaveEvent` | ✅ 完成（F1） |
| P1 | 修日志 rollover 失败（真机暴露的活跃缺陷） | ✅ 完成（F5，**原先不在计划内**） |
| P2 | 合并 `init_movement` 重复赋值（5 组） | ✅ 完成（F3） |
| P2 | `start_fall` 四分支的 `is_moving=False` 提外 | ✅ 完成（F4） |
| —— | 新增回归锁 `round34_movement`（36 项）+ 鉴别力体检 4/4 | ✅ 完成 |

### 待办（未动）

| 优先级 | 动作 | 风险 | 说明 |
|---|---|---|---|
| P3 | 死函数批量清理（`_is_falling_through_window` / `trigger_laugh` / `reset_special_states` 等 7 个） | 中 | 需逐个确认语义；`react_to_file_emotionally` / 回收站链归 H4 专项 |
| P3 | 空闲分支补 3 个状态条件（`is_gravity_falling` / `is_recovering` / `is_sleeping`） | 低 | **先实测**再改，当前无危害 |
| P3 | 5 个孤儿状态位（`is_laughing` / `is_teasplashed` / `is_victorious` / `is_rolling` / `is_sliding`）的复位 | 低 | 一旦置 True 永不复位；需先确认是否有意为之 |

**每处改动都必须**：配独立验证断言 + 正/负控制成对 + 跑 G2 全量回归。

---

## 十、本轮完成度

### 严查范围

| 区块 | 行数 | 状态 |
|---|---|---|
| `init_movement` | ~190 | ✅ 细查完 |
| `update_movement` | ~450 | ✅ 细查完（六项关键设计核实正确） |
| `check_window_movement` | 137 | ✅ 细查完 |
| `start_fall` / `start_falling` | ~120 | ✅ 细查完（两条坠落链路「一读一不读」已定性） |
| `handle_fall` | ~205 | ✅ 细查完（四阶段） |
| `trigger_splat` / `reset_special_states` / `trigger_laugh` | ~60 | ✅ 细查完 |
| `mousePressEvent` / `mouseMoveEvent` / `mouseReleaseEvent` / `mouseDoubleClickEvent` | ~490 | ✅ 细查完（含甩飞全链路） |
| `update_animation` 的状态计时器段 + 缩放/倾斜段 | ~140 | ✅ 细查完 |
| `update_bounce` | ~40 | ✅ 细查完 |
| `logger_utils` 日志初始化 | ~60 | ✅ 细查完（**查出 F5 活跃缺陷**） |

**结论：移动/行为基础代码的严查范围已走完**，共 4 批取证 + 1 轮真机验证。

### 未完成（如实记录）

1. **生命周期 / 存储 / 配置线**（原 task #13）未开始 —— `data_store` / `config_manager` / `memory_store` /
   启动初始化环 / 退出清理链路。
2. **交互线的深度部分**（原 task #14）部分完成 —— 鼠标四事件已细查，但 `show_interaction_menu`
   的菜单项接线、`initiate_chat` 等入口未逐条验。
3. **`is_moving` 的 19 个写点**已归类，但 `react_to_desktop_element`(L4095) 等读点未逐条验语义。
4. 用户「这一阶段完全 ok 才进下一阶段」的门 —— **移动/行为小节已达标**（G2 全绿 + 真机存活 +
   零活跃 bug）；整体"基础代码"门未到，因为 1/2/3 还没走完。

---

## 十一、★ 第 34 轮追加：楼层实现细查（用户指令「仔细检查那个楼层的实现」）

**对象**：`ralsei_pet/modules/floor_manager.py`（27 969 B / 633 行 / `FloorManager` 24 方法，全文通读）
**关联**：`desktop_interaction.py`（窗口枚举）＋ `main.py` 的接入点
（`check_window_movement` / `_start_climb_transition` / `start_jump` / `handle_jump` /
`_follow_floor_move` / `_sync_window_cache_from_floor`）

### 11.1 结论速览

**查出 1 个真缺陷（已修，F7）＋ 1 处口径不一致（低危，不改）＋ 三条核心不变量经复核成立。**

### 11.2 ★ F7：`_index_of_floor` 退化分支返回 `-1` 不是安全哨兵（真缺陷，已修）

**源码**（`floor_manager.py` L477-494，修复前）：

```python
for i, floor in enumerate(all_floors):
    if floor['platform_height'] <= cur_h:
        return i - 1          # ← i == 0 时返回 -1
return len(all_floors) - 1
```

**判据错在哪**：`i - 1` 在本函数语境里表示「**当前楼层高于第 i 层**」。
但 `i == 0` 时它返回 **-1**，而 **两个消费方都不把 -1 当"未找到"**，
而是当成"**比最高的活楼层还高**"：

| 消费方 | `idx = -1` 的实际效果 | 正确语义 |
|---|---|---|
| `get_drop_destination`（L506 `range(current_index+1, n)`） | `range(0, n)` ⇒ 从**最高**活楼层起向下扫 ⇒ 第一块能接住的就是最高的那块 ⇒ **宠物被"上吸"到更高的层** | 落到**下方第一块**能接住的板 |
| `adjacent_lower_floor`（L570 `if idx < 0: return None`） | 直接 `None` ⇒ 语义是"**已在最底层、下面没楼板了**"，可桌面明明在下面 ⇒ **有下层却报"到底了"** | 返回相邻的下一层 |

> 注：`_floor_identity` 的 docstring 里**恰好记着同型的"凭空被上吸"历史缺陷**（第十三轮修过一次）。
> 本缺陷是那条的历史遗留复现路径 —— 那次只修了"用 dict 内容比较"，没修"退化成 -1"。

**触发条件（真机可达）**：宠物站在**当前最高的那个窗口**上 → 用户把这个窗口**关掉** →
`self.current_floor` 仍持有那只已消失窗口的旧 dict（`window_hwnd` 已不在 `underlying_windows` 里），
而 `update_floors()` 重建后「最高活楼层」比它低 ⇒ 按高度找"`<= cur_h`"的第一项就是 `i == 0` ⇒ 返回 -1。

**受控取证（正控制 vs 被测，只改 `current_floor` 的取值）** ——
布局刻意用**重叠窗口**（真实桌面常态）：`h=10` 占 y300..699、`h=5` 占 y500..899，宠物 (200,600) 同落两块板内。

| | 正控制（`current_floor` = 活楼层 h=10） | 被测（`current_floor` = 已消失的 h=15） |
|---|---|---|
| `_index_of_floor` | 0 | **-1** |
| `get_drop_destination` | **h=5** ✅（下面第一块） | **h=10** ❌（被"上吸"一层） |
| `adjacent_lower_floor` | **h=5** ✅ | **None** ❌（谎报"下面没楼板"） |

证据：`_evidence/11_floor_index_受控取证v4.txt`（修复前）、`12_floor_index_修复后复验.txt`（修复后 → 两项都回到 h=5）。

**修复**（保持 `i >= 1` 的原语义，只规范 `i == 0` 这一支）：

```python
return 0 if i == 0 else i - 1
```

**为什么是 0 而不是别的**：0 = 「落在最高活楼层**之上**」，
于是 `current_index + 1 == 1` ⇒ 向下扫时从**第二高**的活楼层开始，
正好对应"关掉最高的楼板 → 掉到下一个活楼层或桌面"的既有口径（§4.9 第十四/十七轮）。

**回归锁**：新套件 `round34_floor_manager`（15 项），**全部调用产品真函数**，正负控制成对
（[A] 退化分支 + [B] 落点单调性 + [C] 相邻下层 + [D] 正常路径 `i>=1` 不许被改坏 + [E] 桌面口径）。
**鉴别力体检**：把修复行回退成 `return i - 1` → **6 项报红（rc=1）**；还原后**逐字节核验一致**、15/15 全绿。
证据：`_evidence/14_鉴别力体检_floor.txt`。

### 11.3 ⚠️ 口径不一致（低危，**本轮不改**）

`_rect_tuple` / `_cut` / `visible_subrects` 全程用**半开区间**（`right = left + width`），
而 `nearest_visible_point` 与 `get_jump_destinations` 用 **`QRect.right()` / `.bottom()`**（= `left + w - 1`，**闭区间**）⇒ 潜在差 1px。

**实测影响面（`_evidence/13_闭区间混用影响面核验.txt`）**：**可忽略**。原因：
`nearest_visible_point` 的输出随后要过 `floor_visible_contains`，而后者用的是**同一套 `QRect.contains`**（也是闭区间）
⇒ **内部自洽**，吸附结果永远落在"它自己认为可见"的区域里，不存在"吸到区域外"。

⇒ 差 1px 与 `MIN_FLOOR_VISIBLE_AREA = 40*40 = 1600 px²` 的判据尺度相差 3 个数量级，
**不可能改变任何"站得住 / 站不住"的结论**。**故只记录、不动**（改动会引入无收益的风险面）。

### 11.4 经复核**成立**的核心不变量（勿动）

| 不变量 | 复核结论 |
|---|---|
| `visible_subrects(target, blockers)` 两两不重叠 + 并集精确 | ✅ `_cut` **构造性**保证 ⇒ **可见面积可直接求和**（`MIN_FLOOR_VISIBLE_AREA` 判据因此才成立） |
| `platform_height = (n - i) * 5`，桌面恒 0 | ✅ 窗口最低层 = 5，**桌面 0 = 唯一最大层高差基准** ⇒ 与"下跳显式 `adjacent_lower_floor`、桌面返回 None（不穿透）"自洽 |
| `_generate_floors` 只在**成立的楼层**里累加遮挡者 | ✅ 可见面积跌破阈值 ⇒ 该层"暂时不存在"且**不再遮挡更低窗口** —— 与 §4.9 口径完全一致 |

### 11.5 楼层线遗留（并入 P3 技术债）

| # | 位置 | 事项 | 判定 |
|---|---|---|---|
| 1 | `get_floors_above` / `get_jump_destinations` | 闭区间 vs 半开区间口径统一 | 低危（实测无危害），**建议与 P3 同批处理** |
| 2 | `_index_of_floor` | ~~退化分支返回 -1~~ | ✅ **本轮已修（F7）** |
| 3 | `_index_of_floor` | 依赖调用方自律的隐式契约（已加 docstring 说明） | 记录，暂不动 |

---

## 十二、第 34 轮追加：跳跃动画统一 `jump_ball`（用户口径）

### 12.1 用户口径（逐字）

> 「他**跳跃动画统一用 jump_ball 代替，只有摔下去的时候用原来的**」

### 12.2 ★ 关键发现：`has_ball` 是**恒假字段**

全项目**只有 `main.py:1170` 一处赋值**：`self.has_ball = False` —— **永远不为 True**。

后果：所有 `if self.has_ball: "jump_ball" elif: "jump"` 分支里，
**`jump_ball` 那条是死的**，实际**永远走 `jump`**。这解释了"为什么跳跃动画一直是 `jump`"。

### 12.3 改动点（3 处，均在 `main.py`）

| # | 位置 | 改前 | 改后 |
|---|---|---|---|
| 1 | `start_jump` 起跳准备帧 | `jump_ready` | **`jump_ball`** |
| 2 | `handle_jump`（L3049 附近） | `if has_ball → jump_ball / else → jump` | **写死 `jump_ball`** |
| 3 | `update_animation` 的 `jump_phase` 推进 | `ready`→`jump_ready`、`jumping`→`jump` | 两阶段都 → **`jump_ball`** |

⚠️ **不受影响**：`_jump_anim_override`（"跨度大"选型，`climb_*` 攀爬素材）**优先于**上述默认值 —— 那是独立机制的选型。
⚠️ **未改**：`L919-921` 帧率表、`L236 / L6278 / L6357 / L6399 / L7738` 动画名清单（字典键 / 注册，不能动）。

> **决策留痕**：`jump_ready` → `jump_ball` 是**我按"统一"字面口径做的判断**
> （原文只点名 `jump_ball`，没说 `ready` 帧怎么办）。理由是"整个跳跃过程只有一种素材"最贴合"统一"。
> **用户可一句话推翻**（改回一行即可）。

### 12.4 真机（离屏）验证 —— 走**产品真对象**

证据：`_evidence/15_跳跃动画真机离屏验证.txt`，**10/10 PASS**。

```
[A] start_jump 起跳准备帧
  [PASS] start_jump 期间切到过 jump_ball（实测序列 ['jump_ball']）
  [PASS] start_jump 期间**不再**切 jump_ready（实测 ['jump_ball']）
[B] has_ball 开关不再影响跳跃选型（对照组应一致）
  [PASS] has_ball=False 与 True 的跳跃动画序列一致（['jump_ball'] vs ['jump_ball']）
[C] 坠落链路（"摔下去"）仍用原来的素材
  [PASS] start_falling(reason=None)           → ['fall']      （非 jump_ball）
  [PASS] start_falling(reason='floor_removed')→ ['fall_mad']  （非 jump_ball）
```

**B 组的意义**：把 `has_ball` 分别置 False / True，跳跃动画序列**完全一致** ⇒
证明"恒假条件已被去掉"，不再存在"分支永远走另一条"的隐蔽性。
**C 组的意义**：用户要求的例外（「只有摔下去的时候用原来的」）**被真正保住** ——
常规坠落 `fall`、用户抽楼板 `fall_mad`，两者都没有被改成 `jump_ball`。

### 12.5 素材侧核验

- `assets/animations.json` 中 `jump_ball` 已注册：**4 帧**（`spr_ralsei_jump_ball_0..3.png`），`climb_front/left/right` 亦在册；
- 素材文件实际存在于仓根 `deltarune_ralsei/`（非 `assets/sprites/`，该目录为空）；
- 真机启动日志：`已从 animations.json 加载 112 组动画（legacy 28 组，schema=1）`，
  且收尾行 `本轮运行未出现未命中的动画名（已加载 493 组）` ⇒ **`jump_ball` 无 miss**。

---

## 十三、存储 / 配置层细查（task #13，生命周期线）

**范围**：`data_store.py`（302 行）、`memory_store.py`（342 行）、`config_manager.py`（578 行）、
`logger_utils.py`（212 行，第 34 轮已修 F5）、`lazy_log.py`（55 行）。
**方法**：全文通读 + **全部调用产品真函数**的受控探针（沙箱一律用本地 NTFS `tempfile.mkdtemp()`，
E 盘 exFAT 不当探针沙箱）。

### 13.1 结论速览

| # | 问题 | 严重性 | 触发频率 | 处置 |
|---|---|---|---|---|
| Q1b | `.corrupt.*` 配置备份**从未被收敛** | 中（磁盘垃圾永不自愈） | 每次配置损坏 | **已修** |
| Q3b | `data_store` 留档名固定 `.old` → **静默覆盖** | 中（丢历史留档） | 每次"目标更新"搬运 | **已修** |
| Q4b | `memory_store` 留档名固定 → **静默覆盖** | **中高**（丢记忆留档） | **每次启动都可能** | **已修** |
| Q5 | 残留 `.migprobe` → `_movable` 恒 False | 低（不丢数据，只滞留） | 极端情况 | **已修** |
| R1 | 修 Q4b 引入的时间戳留档**破坏隐私 reset** | 高（若漏修） | 隐私清理时 | **同步修** |

### 13.2 Q1b：`.corrupt.*` 备份一份都不清（真缺陷）

**源码**：`config_manager._prune_old_backups()` 原先 `prefix = f"{self.config_file}.backup."`
—— **只匹配 `.backup.`**。而 `_load_config` 在配置损坏时会写下 `<config>.corrupt.<ts>`（L192）。
⇒ **`.corrupt.*` 从不参与剪枝**。

**受控取证**（`16_存储配置层探针.txt` Q1 段）：造 15 个 `.backup.*` + 15 个 `.corrupt.*`，
调产品真函数后实测 **`.backup.*=10`（已收敛）/ `.corrupt.*=15`（一份没清）**。

**触发路径真实可达**：配置一旦被同步工具 / 手改写坏一次就留一个，**永不自愈**。
这与该函数注释里自己记的"程序目录里积压了 106 个 `config.json.backup.*`，纯垃圾"
**是同一类问题 —— 当年只修了一半**。

**修法**：抽 `_prune_by_prefix(prefix, keep)`，`_prune_old_backups` 对
`('.backup.', '.corrupt.')` **两类分别**收敛（各自独立计数，互不挤占配额）。

### 13.3 Q3b / Q4b：留档名固定 → 静默覆盖（真缺陷，两条同源）

**`data_store.migrate_from_staging`**（原 L244）与 **`memory_store.migrate_from_fallback`**（原 L278/L286）
都用**固定名**做留档：前者 `dst + '.old'`，后者 `memory.old.json`。
`shutil.copy2` 到已存在的路径会**直接覆盖** ⇒ 第二次有落选者时，**第一次的留档永久消失**，
而调用方毫无察觉（`result['moved']` 两条记录长得几乎一样）。

**受控取证**（同文件 Q3b / Q4b 段）：
- `data_store`：round2 后 `.old` 内容从 `STAGING-OLD` 变成 `STAGING-OLD-2` → 第一份没了；
- `memory_store`：round2 后留档从 `desktop1` 变成 `desktop2` → 同理。

★ **Q4b 的触发频率远高于 Q3b**：`migrate_from_fallback` 由 `memory_system` 在
**每次启动**时调用（`memory_system.py:79`）；只要反复出现"设备版更新、桌面版落选"，
每一轮都会把上一轮留档覆盖掉 —— **只有最后一次落选者能存活**。

**修法**（保持"用户可见的约定名"这一产品契约）：
- 新增 `_archive_name(path)` / `_rollback_path(target_dir)`：**首选仍是约定名**
  （`.old` / `memory.old.json`），**被占用时**退到带时间戳的 `<约定名>.<ts>`；
- 轮转语义 = **约定名永远是最新一次落选者**，更早的在 `<约定名>.<ts>` 里；
- 连带修正 `_copy_verified` 的判据：`basename == ROLLBACK_FILENAME` 精确比对会**漏掉带后缀的那批**，
  改为 `startswith`。

**为什么不用"直接改名加时间戳"**：约定名在模块 docstring 里是对用户写明的
（"落选的那份挪成 `memory.old.json` 留档"），改成纯时间戳会破坏这个可见契约。

### 13.4 Q5：残留 `.migprobe` 把文件**永久**钉在中转站（真缺陷）

**源码**：`data_store._movable(src)` 用 `os.rename(src, src + '.migprobe')` 试改名来判断占用。
其"改回来"那步失败时**自己注释里承认**会残留 `.migprobe`（"极罕见：回不去就把探针当正式文件留着"）。
而**残留之后**：每次再来改名，`os.rename` 都因**目标已存在**抛 `OSError` →
`_movable` **恒返 False** → `migrate_from_staging` 每轮把它列进 `skipped`
→ 该文件**永远搬不进最终存储**，`staging_has_data()` 也恒为 True
（"本地只作中转站"的口径就此落空）。

**受控取证**（同文件 Q5 段）：造残留 `.migprobe` 后 `_movable` 返回 `False`，源文件仍在。

**修法**：动 rename **之前**先清残留探针；清不掉才判为不可搬（保守，不误搬被占用的文件）。

### 13.5 R1：修 Q4b 引入的**隐私回归**（同步修，重要）

`memory_system.reset_all()` 是**隐私口径**（"清空**全部**记忆，**含留档副本**"，供
"退出时清理数据"使用）。它原先精确删 `memory.old.json` —— 而 13.3 的修法会产生
`memory.old.json.<ts>` ⇒ **改完 Q4b 后，reset 会漏删时间戳留档，磁盘上残留旧记忆**。

⇒ **同步修**：`reset_all` 枚举同前缀（`startswith(ROLLBACK_FILENAME + '.')`）一并清掉。
（★ 这是"修 A 引入 B"的典型 —— **改留档命名策略必须回头检查所有读/删留档的地方**。）

### 13.6 复核成立（未改）

- `data_store` **import 期零磁盘操作**（第五轮冒烟前提），且被 `logger_utils` 反向依赖时
  一律走 `lazy_log.LazyLogger` → 初始化环已断（第 12 轮锁 `round12_store` 覆盖）；
- `memory_store._is_writable_dir` 两级判定：快路径 `os.access` **不落文件**，
  避开宿主沙箱"同路径删除配额"（第 13 轮 11 个假 DIFF 的真凶），**修复有效**；
- `config_manager._save_config` **原子写**（`.tmp` + `os.replace`）+ 失败回滚内存，
  `set()` / `update()` / `reset_config()` 三处都做快照回滚，**一致**；
- `vault → staging` 的迁移五条安全约定（先探测可搬 → copy → **校验长度** → 删源 → 只删空目录）
  与 `memory_store` 同源，**逐条复核成立**。

### 13.7 回归锁与鉴别力

- 新增**第 29 套件** `round34_store_config`（17 项，全部调产品真函数）；
- **鉴别力体检 4/4 全部抓破坏**（`18_鉴别力体检_store.txt`）：
  逐条回退 Q1b/Q5/Q3b/Q4b → 分别报红 **2 / 1 / 2 / 2** 项、`rc=1`；还原后逐字节一致、17/17 PASS。

> ⚠️ 体检 v1 报的"还原逐字节一致：False"是**核验判据自己说谎**的又一例。
> v2 改用**体检前的快照副本**做参照系（不再拿内存字符串），归零。
> 通则复述：**核验失败时，先怀疑核验，再怀疑被测物。**

### 13.8 本线 G2 / 真机

- 全量 G2：**PASS=1427 FAIL=0，29 套件全 IDENTICAL，rc=0**（新增 `round34_store_config` 17 项，
  首次建立基线）；
- 真机：`src/main.py` 启动 **40s** 存活、峰值 RSS **192.3MB**、
  **零 Traceback / 零 ERROR / 零 `Logging error`**；
  日志正确落在 `E:\RalseiMemory\logs\ralsei_pet.log`（vault 优先路径有效）。
