# 代码质量复审报告 — 第十轮（2026-09-15）

**主题**：分层关联图 + 受控多跳检索（用户方案落地）
**提交**：`e9f6b00`（已推送，远端 `ad36ba4..e9f6b00`，`ls-remote` 核验一致）
**验证**：本轮自检 **98 PASS / 0 FAIL**；G2 回归 **14 套件 / 459 PASS / 0 FAIL / 全 IDENTICAL**

---

## 一、结论（先说结果）

用户给的多跳联想方案**已全部落地并纳入回归**，包括：分层边、hub 节点惩罚、路径打分、
PPR、分层过滤（每跳/路径/重排/预算）、反馈学习边权、离线巩固、噪声率·有用率·成功率评估、
LLM 验证钩子。

但"试一试"的过程里，**实测推翻了我自己的两处原始设计**（详见 §三），这两处如果不实测
是发现不了的 —— 一个会让多跳联想在真实数据上**整体失效**，另一个会让路径过滤**形同虚设**。
两处都已修正并加了回归锁。

**一句话现状**：检索机器已经建好且可验证；当前精度瓶颈不在机器，而在**喂给它的关键词质量**
（见 §五，有量化数据）。

---

## 二、交付物

| 文件 | 规模 | 说明 |
|---|---|---|
| `ralsei_pet/modules/memory_graph.py` | 566 行 / 21 方法 | **新增**。分层关联图 + 受控多跳检索管线，零第三方依赖（纯 `math`/`heapq`/`time`） |
| `ralsei_pet/modules/memory_system.py` | 1564 行（+约 300 行） | 接线：`recall()` 重写为 5 段管线；新增反馈/指标/巩固/验证器接口；修 1 个真 bug |
| `code-quality-audit/第十轮/verify_round10_graph.py` | 644 行 / 98 项 | 自检套件（已挂进 G2） |
| `code-quality-audit/第十轮/demo_recall_showcase.py` | 147 行 | 端到端演示：真实中文 + 真实抽词，**不打桩** |
| `code-quality-audit/第十轮/probe_real_recall.py` | 146 行 | 真机取证：跑真实记忆文件，只读取证、写盘落临时目录 |
| `code-quality-audit/第十轮/_evidence/*.txt` | 4 份 | 自检输出 / G2 输出 / 真实语料演示 / 真机探针（UTF-8 原始输出） |
| `code-quality-audit/regress/run_all.py` + `baseline.json` | — | 挂入 `round10_graph` 套件；基线重建（2 处有意变更，见 §四） |

---

## 三、被实测修正的两处（本轮最有价值的部分）

### 修正 1：静态 tier 门槛会把多跳"整体锁死"

**原设计**：`HOP_TIER_FLOOR = {1: 弱, 2: 中, 3: 强}` —— 第 2 跳只许中边以上（"越走越严"）。
**实测**：用 17 条真实中文对话建图 → 82 个词节点、**460 条边，其中 452 条是弱边（98.2%）**。
真实对话里"同一个词再次同框"极少，于是"第 2 跳门槛=中边"让多跳几乎全部走不通，
实测**只剩 hop=1 结果**，"从写代码想到咖啡"这种最典型的联想完全出不来。

**改为**：`{1: 弱, 2: 弱, 3: 中}` + **中转资格**规则 —— **弱边可以到达，但不能继续出发**
（出发必须踩过中边以上）。

理由：只共现 1 次的边，往往来自断词切出来的噪声词；拿它当中转站，联想会一路滑走。
实测效果：`写代码 --(n=2 中边)--> 咖啡` 这类"重复出现过的真概念"仍是桥，两跳照样通；
而噪声词搭的桥被掐断。回归锁见自检 `G8b` / `G11` / `R2b`。

### 修正 2：PPR 用"加法"会绕过路径过滤

**原设计**：把 PPR 分按权重**加**进关键词分（`kw_score[k] += PPR_MIX * ppr_norm`）。
**实测被自检 `R2c` 抓到**：一条两跳路径分只有 0.0125（< 门槛 0.02，本该被剪），
但 hub 通过 PPR 送了约 0.05 分，把被剪掉的词**又塞回了候选集** —— "路径过滤保可信"失效。

**改为**：PPR 只做**加权**（`kw_score[k] *= 1 + PPR_MIX * ppr_norm`），
**放行与否永远只由路径分决定**。这样 PPR 仍是"融合打分"，但不再是一条旁路。

---

## 四、验证

### 自检（98 项，五组）

| 组 | 覆盖 |
|---|---|
| G（26 项） | 分层（弱共现+强反馈→中边；重复共现+强反馈→强边）、边权单调性、旧格式归一、共现对称性、每点邻居上限、节点裁剪、hub 惩罚（高频词权重 < 罕见词 × 0.6）、每跳 tier 过滤、有界子图、跳数上限、路径打分/降噪/早剪/上限、PPR 归一、稀疏语料可达性、hub 惩罚可量化 |
| F（18 项） | 反馈调边权（正反馈抬、负反馈减且幅度更小）、重排去重（重叠/同日）、离线巩固（老化降级/剪枝/对称修复/跨天补边/统计）、`tune` 按有用率自动收紧或放松、样本不足不动参数、图自检 |
| R（14 项） | 多跳真的想起"没提过的词"、直接命中排最前、`via` 可解释、跳数入账、无关片段不进来、弱桥不当中转、中桥两跳通行、hub 中转压分、极小预算不塌、图缺失降级、旧格式兼容 |
| M（22 项） | `recall_token`、噪声率/有用率/成功率口径、反馈回填抬权、未知 token 安全、按层按跳分桶、落盘往返（含 `weak_penalty`）、清空、离线巩固一键触发、出口文案带"怎么想到的"、关键记忆优先、LLM 验证器 7 项（默认不调用 / 预算为 0 不调用 / 给额度才调用 / 额度用尽不再调用 / 抛异常保留原结果且不计数 / 可摘除） |
| X（10 项） | 回归守卫：`forget_cycle` 不再对 dict 边取 `int`、`assoc` 是属性、召回走路径分+PPR、预算参与、巩固接入 `update`、图模块零第三方依赖、五层过滤都在、**校验器自检**（防 `code_only` 把 `import heapq` 粘成 `importheapq` 导致负向断言假通过） |

### G2 回归

14 套件 / 459 PASS / 0 FAIL / **全 IDENTICAL**。两处基线 DIFF 都是本轮有意变更：

1. `round5_smoke` 24 → 25：新增模块 `memory_graph` 被冒烟套件扫到。
2. `round5_verify`：`save_memory` 落盘键新增 `recall_stats`（该套件靠正则列键）。

### 真机取证（诚实记录）

- `E:\RalseiMemory` 目录存在、卷标 `肖翰哲` 正确，但**目录为空** —— 第九轮做过一次
  写盘测试（`--write-test`）后被 reset 清空，之后宠物没再启动过，所以**没有真实累积记忆**。
- 回退到项目内 `ralsei_pet/memory.json`：537 字节、旧格式、**fragments/keys/assoc 全为 0**
  （本质上是个空 stub）。
- 因此真机取证只能证明"读取路径可用 + 源文件未被改动"（`size`+`mtime` 前后一致 `True`），
  **不能证明真实数据上的召回质量**。这一点必须如实标注，不能当作已验收。
- 连带发现：`F:x` 之类的旧路径不成立，`probe_real_recall.py` 已改成"E 盘无文件就自动回退到
  项目内旧文件"，并打印来源，避免以后误以为"读到了真机数据"。

### 端到端演示（真实中文、真实抽词、无桩）

用 17 条一周对话跑完整链路，暴露了当前的真实水位（原文见
`_evidence/round10_demo_realtext.txt`）：

- 图：82 节点 / 460 边 / 强边 2、中边 6、**弱边 452**。
- 多跳**已经能出结果**（`{1: 4, 2: 1}`），但那条 hop=2 的结果是
  `写代码 → 写代 → 爬山记得带水` —— **语义漂移**。

---

## 五、诚实的局限与下一步（当前精度瓶颈）

**瓶颈是"喂进图里的关键词质量"，不是检索机器。** 量化证据：

1. 抽词器（第九轮 `conversation_focus.extract_keywords`）产出的是**字符窗口片段**，
   真机上出现大量非词节点：`写代` / `点代` / `写点代` / `要不` / `要休` / `雨会` / `鲁下`。
   对"想写点代码"抽到的是 `写点、点代、代码、写点代、点代码`。
2. 由于**同一个噪声片段会随同一句话反复出现**，它自己也能攒成"强边"，于是**通过了中转资格**，
   继续当中转站 → 漂移（这正是 §三-修正 1 里 `写代` 仍然漏过的那一类）。
3. 短句 + 7 个关键词 → 两两共现 = 每句最多 21 条边，**每句话都接近一个团**，
   于是"谁和谁都连" —— 图**几乎没有区分度**。这也是 `咖啡`(0.0294) 与
   `爬山`(0.0205) 的路径分**几乎相同**的原因：**在现有词汇表上，任何阈值都分不开这两者**。

**结论：再调阈值/权重是徒劳的，必须改输入。** 建议的下一步（按性价比排序）：

| 优先级 | 事项 | 预期收益 |
|---|---|---|
| P0 | **关键词抽取换成语义边界可靠的方法**（短语优先 / 最长匹配 / 停用表 + 词性；或接入分词库），并让"记忆关键词"与"话题锚"共用同一套 | 直接消灭 `写代` 类节点，图规模与噪声大幅下降 |
| P0 | **降低边密度**：一句话里只连"核心词 ↔ 其他词"（星型）或只连相邻词（链型），不再全互连 | 打破"谁和谁都连"，恢复区分度 |
| P1 | 用 `forget_cycle`/`consolidate` 把"从未再被共现的噪声节点"尽快剪掉；把 `EDGE_TTL` 缩短做实验 | 长期自净 |
| P1 | 真机上先跑一段时间攒出真实记忆，再用 `probe_real_recall.py` + `recall_report()` 评估噪声率/有用率 | 拿到真实基线，替代现在的合成语料 |
| P2 | 接上 LLM 验证器（钩子已就绪，`set_verifier` + 预算 `max_llm_calls`），用它在漂移可控前兜底 | 精度补丁 |

**未做（明确划出边界）**：
- 没有改 `conversation_focus.py`（第九轮已验收，改它会牵动 54 项断言，且属独立课题）。
- 没有接任何真实 LLM 调用（验证器默认关闭是刻意设计：离线优先、零额外开销）。
- 没有在真机上跑 GUI 验证（记忆检索不涉及窗口；行为验证靠自检 + G2）。

---

## 六、设计 ↔ 用户要求对照

| 用户要求 | 实现位置 | 备注 |
|---|---|---|
| 强边：关联性极强 | `_TIER_BASE[strong]=1.0`，`tier_of` 由共现次数≥3 或（≥2 且反馈≥0.5）判定 | |
| 中边：语义相似、同主题 | 共现次数=2 或反馈≥0.15 | |
| 弱边：只有极少关联 | 共现次数=1 且反馈<0.15 | **实测 98% 的边落在这里**（见 §五） |
| hub 节点惩罚 | `node_weight()` = IDF × 度惩罚，权重 ∈ [0.25,1]，参与路径连乘 | 自检 `G6/G12/R2e` 可量化 |
| 路径打分而不是只看相似度 | `paths()`：分 = Π(边权×节点权) × `HOP_DECAY^跳数` | 结果带 `nodes/hops/tier`，可解释 |
| 分层记忆 | 第九轮三层（fragments / keys / digests）+ 本轮 `assoc` 升级为分层图 | |
| 离线巩固 | `consolidate()`：老化→升降级→剪枝→对称修复→跨天补边→体量控制；`update()` 按 `CONSOLIDATE_INTERVAL` 触发 | |
| PPR | `ppr()`：有界子图上固定重启概率/迭代数 | **只加权不放行**（§三-修正 2） |
| 反馈学习边权 | `feedback()` + `memory_system.recall_feedback()` | 负反馈幅度减半，`tanh` 压缩 |
| 评估噪声率/有用率/成功率 | `recall_report()`，按层、按跳分桶 | 落盘跨会话累积 |
| 每跳过滤防语义漂移 | `HOP_TIER_FLOOR` + 中转资格 + 节点权下限 | §三-修正 1 |
| 路径过滤保可信 | `min_path_score` **扩展时即剪**（早剪） | |
| 重排去重降冗余 | `rerank()`：关键词 Jaccard ≥0.6 去重 + 同日上限 | 同日默认 3 条（不卡死 limit=3） |
| LLM 验证挡无关 | `set_verifier()` + 预算 `max_llm_calls` | **默认关闭**；为 0 时永不调用 |
| 预算控制防失控 | `RecallBudget`：跳数/节点/每点出边/路径数/候选数/耗时/LLM 调用数 | 极小预算下仍不崩（自检 `R4`） |

---

## 七、顺带修掉的真 bug

- **`forget_cycle` 对 dict 边取 `int(v)`**：第九轮的裁剪代码 `sum(int(v) for v in kv[1].values())`
  在本轮 `assoc` 变成"边记录 dict"后必然 `TypeError`；它被外层 `try` 吞掉，
  后果是**遗忘周期后半段（含 `_build_index()`）全部静默跳过** —— 索引不再重建，
  召回会持续用旧索引。改为交 `MemoryGraph.cap_keys()` 自己按边权裁剪（自检 `X1` 锁死）。
- **`assoc` 改属性**：图内部换字典对象时，外部旧引用会失效（"图变了、字典没变"这类同步 bug）。
  现在是 `assoc -> self._graph.edges` 的属性视图（自检 `X2`）。

---

## 八、复现命令

```powershell
# 本轮自检（98 项）
C:\Python311\python.exe code-quality-audit\第十轮\verify_round10_graph.py

# 全量回归（14 套件 / 459 项）
C:\Python311\python.exe code-quality-audit\regress\run_all.py

# 端到端演示（真实中文、无桩）
C:\Python311\python.exe code-quality-audit\第十轮\demo_recall_showcase.py

# 真机记忆只读取证（写盘落临时目录，源文件 size+mtime 会被核验）
C:\Python311\python.exe code-quality-audit\第十轮\probe_real_recall.py
```

> 注意：必须用 `C:\Python311\python.exe`（PyQt5/bs4 所在解释器）。
> 托管 venv（3.13）跑 `round5_smoke` 会因缺 `bs4` 假 FAIL。
