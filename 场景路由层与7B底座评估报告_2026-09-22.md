# 场景路由层与 7B 底座评估报告

**日期**：2026-09-22
**轮次**：第二十九轮
**对应提交**：`52d5514`（产品+文档）、`4dc2306`（记忆落档）

> ⚠️ **本文第 1 章的 7B 性能结论已被第三十轮实测修正，见下方「第三十轮实测更新」。**
> 第 2、3 章（场景路由层、原作地图线路）**不受影响，仍然有效**。

---

## ★ 第三十轮实测更新（2026-09-22 晚，会覆盖第 1 章部分结论）

7B **已下载完成并实测**，4B 也用**产品真实 persona** 重测了。三个结论需要改写：

### ① 第 29 轮"4B 首字 0.6~0.7s 合格"**不成立**（那是裸测值）

| 模型 | 场景 | prompt tok | 首字 |
|---|---|---|---|
| 4B | 短 system（裸测） | 21 | 0.33s |
| 4B | **产品真实 persona** | 2322 | **36.82s** |
| 4B | persona + 历史（缓存命中） | 2363 | 1.91s |
| 7B | 短 system（裸测） | 43 | 1.39s |
| 7B | **产品真实 persona** | 2050 | **96.48s** |
| 7B | persona + 历史（缓存命中） | 2050 | 0.20s |

★ **教训**：**测性能必须用产品真实输入**。第 29 轮用简化输入代替产品输入，
给出了**反向结论**（"合格" → 实际超门槛 10 倍）。

### ② 第 1.3b 的「决定首字的是 prompt 长度」**不准确** —— 真变量是 **KV 前缀缓存**

同一份 prompt（2050 token，一字未改）：

| | 首字 |
|---|---|
| 冷启动（缓存未命中） | **103.6s** |
| 缓存命中（只尾巴不同） | **0.55s** |
| 缓存命中（完全重复） | **0.19s** |

⇒ **差 190 倍，而 prompt 长度完全相同。** 长度只是"冷启动成本"的量尺，
**真正决定用户体感的是「前缀缓存有没有命中」**。

### ③ 变化段的**位置**决定缓存能复用多长 —— 实测 **29.4×**

先喂热前缀，再只改一个变化段（唯一变量 = 位置，prompt 均 3629 token）：

| 变化段位置 | 改一次的代价 |
|---|---|
| **靠前**（紧跟 persona） | **73.44s** |
| **末尾**（挪到 system 最尾） | **2.50s** |

⇒ 机制：Ollama 只复用「**从 prompt 开头起、逐字相同**」的最长前缀。
⇒ **产品现行的 system 组装顺序（`main.py:6722-6816`）已经是位置最优的** ——
第 20 轮就为此把世界观改成按需召回、把关系 brief 排在临时状态之前。
**本轮实测正好印证并量化了那个设计的正确性。**

### ④ 7B 能否达标：**不能**（在产品真实链路上）

| 场景 | prompt tok | 首字 | 判定 |
|---|---|---|---|
| 完整 system + 6 轮历史（冷） | 2457 | 120.27s | OVER |
| 同一 system 再来一轮 | 2478 | 3.22s | OVER |
| 【此刻】变了 | 2456 | 8.28s | OVER |
| **lean（只发 persona）** | 2332 | **1.88s** | **OK** |

⇒ **7B 只有「事件台词（lean）」一条路能进 1~3s**，正常对话 3~8s。
⇒ **用户的真问题不是"模型太小"，而是"每轮都在冷启动"。**

### ⑤ 结论改写（覆盖 §1.6）

- ❌ 撤回「**先砍 prompt 长度**」的建议 —— 用户口径：`prompt 尽量完整`。
  **而且实测证明不必砍**：缓存与 prompt 长短无关，只与「前缀是否逐字稳定」有关。
- ✅ **「首字 1~3s」的正确解法 = 让前缀缓存稳定命中**：
  ① **变化段恒在 system 末尾**（产品已满足）；② **persona 恒在开头且逐字不变**（已满足）；
  ③ **别让会话冷下去**（常驻会话 / 预热请求）。
- ⚠️ **Vulkan 后端（核显）救不回冷 prefill** —— 冷启动 96~225s 量级靠后端补不上；
  它只能改善"已经在算"的那一段。**要治的是"别让它冷"。**

### 证据

`code-quality-audit/场景系统-P0/_evidence/`：
`ttf_measure.txt` / `ttf_measure_7b.txt` / `ttf_product_chain_7b.txt` /
`cache_hit_verify.txt` / `cache_position_ab2.txt`（`cache_position_ab.txt` 是失败留档）

---

## 0. 结论先行

用户本轮提了三件事，**结论分别是**：

| # | 用户要求 | 结论 |
|---|---|---|
| 1 | 「可以给他接 7B 试试，等待时间 1~3s 内我能接受」 | ⚠️ **前提需纠正 + 结论不乐观**。本机**没有 32GB 显存**，「32GB」是内存；GPU 是**核显**且 Ollama 未启用；7B 在**当前配置下大概率超标**（详见 §1） |
| 2 | 「给模型训练在什么语境下怎么走路线，去哪个场景……你只需要留好拓展的接口」 | ✅ **已交付**。`scene_routing.py` + `_routes.json` + 106 项回归锁，路由规则**零改 Python 即可扩展**（详见 §2） |
| 3 | 「一切根据原作……如果你可以看到原作的地图线路那你就可以先进行第一版规划」 | ✅ **已经看到并抄下来了**。原作第 1–5 章**真实房间表**已落档（详见 §3），并据此定了我们场景的登记方式 |

**本轮三个最硬的发现**：
1. **「32GB 显存」是 32GB 内存** —— GPU 是 Intel Arc **核显**，Ollama 跑在**纯 CPU** 上（§1.1–§1.2）
2. **真正的瓶颈是 prompt eval，不是模型大小** —— 长 prompt 要 **35~47 秒**，换 7B 只会更慢（§1.3b）
   ⚠️ **第三十轮修正**：更准确的说法是「**瓶颈是"前缀缓存未命中"**」，长度只是其量尺（见上）
3. **4B 实测 219 次**：8.07 tok/s；12 tok=1.49s / 20 tok=2.48s / 40 tok=4.96s（§1.3）

**G2 终态**：`PASS=1358 FAIL=0 套件=26` / **26 套件全 IDENTICAL** / `rc=0`

---

## 1. 关于 7B：先纠正一个前提，再谈性能

### 1.1 「32GB 显存」实际是 32GB 内存（核显共享）

**实测证据**（`_evidence/hw2.txt`、`gpu_deep.txt`、`gpu_mem.txt`）：

| 项 | 实测值 |
|---|---|
| CPU | **Intel Core Ultra 5 125H**（Meteor Lake，14 核 / 18 线程） |
| GPU | **Intel(R) Arc(TM) Graphics** |
| GPU 硬件 ID | `PCI\VEN_8086&DEV_7D55` → **Meteor Lake-P 核显（iGPU）**，非独显 |
| 内存 | 8 × 4GB LPDDR5-6400 = **31.6 GiB**（`nvidia-smi` 不存在，无 NVIDIA 独显） |
| 机型 | LENOVO 83D4（笔记本） |

⇒ **「32GB 显存」这个数字对不上实物**。核显用共享内存，没有独立的 32GB VRAM。
这个区别是决定性的：**如果真有 32GB 显存，7B 会轻松进 1~3s；而核显不会。**

### 1.2 Ollama 现在**跑在纯 CPU 上**，核显完全没用上

**证据**（`ollama_backend.txt`）：

```
msg="inference compute" id=cpu library=cpu compute="" name=cpu
    total="31.6 GiB" available="20.2 GiB"
```

**空跑实测**（`ollama_backend_probe.txt`）：

```
size_vram = 0            ← 显存占用 0，纯 CPU
tok/s     = 13.94
```

**Ollama 目录下只有 CPU 后端**（`gpu_deep.txt`）：
`ggml-cpu-alderlake.dll` / `ggml-cpu-icelake.dll` / … —— **没有** `ggml-vulkan.dll`、
`ggml-cuda.dll`、`ggml-dml.dll`。
（系统里 `vulkan-1.dll` 存在，驱动也是新的 32.0.101.8331，但 Ollama 这个版本没带 Vulkan 后端。）

### 1.3 实测性能（**219 次真实推理**，从 Ollama 服务器日志全量抽取）

**方法**：不抽样、不猜外推 —— 直接解析 `%LOCALAPPDATA%\Ollama\server.log`
里的 `slot print_timing` 行，把**每一次**推理的 `prompt eval time` / `eval time` /
`total time` 全部抽出来（`_evidence/4b_server_log_perf.txt`，219 条完整样本）。

**结论（4B = `ralsei:v3` / `qwen3:4b-instruct-2507-q4_K_M`，纯 CPU）**：

| 口径 | 实测值（avg） | 区间 |
|---|---|---|
| eval 速度 | **8.07 tok/s** | 7.21 ~ 13.55 |
| **12 tok 整句** | **1.49s** | 0.89 ~ 1.66 |
| **20 tok 整句** | **2.48s** | 1.48 ~ 2.77 |
| **40 tok 整句** | **4.96s** | 2.95 ~ 5.55 |

★ 注意：**4B 的"整句"在长回复时已经超过 3s**（40 tok → 4.96s）。
用户说的「1~3s」如果指**首字延迟**，4B 合格；如果指**整句说完**，4B 长回复时就已经超了。

### 1.3b ★★ 真正的瓶颈不是生成速度，而是 **prompt eval**

这是本轮从日志里读出来的、**比"7B 快不快"重要得多**的发现：

| prompt 长度 | prompt eval 耗时 |
|---|---|
| ~10 tok（裸测） | **116 ~ 700 ms** |
| 140 tok | ~3.4 s |
| 2320 ~ 2670 tok | **35 ~ 41 s** |
| 3042 tok | **47.7 s** |

同一个模型，**差 100 倍**。而我们的实际链路（人设 `ralsei_persona.md` + 世界观召回 +
关系状态 + 近期对话 = 2000+ token）**正好落在最慢那一档**。

⇒ **决定首字延迟的是 prompt 长度，不是模型大小。**
换 7B 只会让 prompt eval 更慢（同样长度下算力需求更高），**不会改善首字**。
**这是"换 7B"这条路最难辩驳的反证。**

### 1.4 7B 的外推（**非实测，见 1.7 诚实标注**）

按参数量 1.75×（速度约 1/1.75）测算，基准取实测 avg 8.07 tok/s：

| 速度 | 12 tok | 20 tok | 40 tok |
|---|---|---|---|
| **4.61 tok/s** | 2.60s | **4.34s** | 8.67s |

⇒ **整句口径大概率超 3s**（20 tok 就要 4.34s）；首字延迟要看 prompt eval，见 1.3b。

### 1.5 ★ 还有两个"劝退项"（比速度更硬）

**(a) 内存很紧。** 31.6 GiB 总内存，当前**空闲只有 9.7 GiB**（`ollama_backend.txt`，
2026-09-22 01:11 实测）。7B q4_K_M 权重 4.7GB + KV cache（8192 上下文）约 1~2GB
+ Ollama 运行时 —— **能装下，但没有余量**。同时开桌宠（PyQt）+ 浏览器时可能触发换页。

**(b) 用户说的"失真"问题，7B 未必解决。** 第 27 轮的结论（§6 人味线）是：
失真的**头号来源是 persona 里可逐字搬走的固定例句，不是模型大小**。
换成 7B 不会自动修掉这个 —— 而换成 7B 会带来"更慢 + 更平"的风险
（第 28 轮的 Modelfile 注释里已记过一次：`7B(qwen2.5) 也更干净，但更慢且更平`）。

### 1.6 建议（待用户裁定）

**不建议现在就换 7B 做默认底座。** 四个理由，**强度递增**：

1. 速度收益为负（§1.4）；
2. 内存无余量（空闲 9.7 GiB，§1.5a）；
3. 失真问题另有主因（persona 固定例句，不是模型大小，§1.5b）；
4. ★ **7B 治不了真瓶颈** —— 真瓶颈是 **prompt eval**（§1.3b）。
   我们的链路 prompt 有 2000+ token，4B 要 35~47 秒；**7B 在同样长度下只会更慢**。
   ⇒ **换 7B 是"换了个更慢的瓶颈"，不是"解决瓶颈"。**

**更有性价比的三条路**（按推荐度排序）：

1. ★ **先试 Vulkan 后端** —— 核显 Arc（Meteor Lake，8 Xe 核）跑 4B 的 Vulkan 推理，
   速度可能翻倍，**且不增加内存压力**。**这是唯一能同时改善速度和保留 4B 质量的方案。**
   需先验证 Ollama 0.34.2 是否带 Vulkan 支持（当前安装目录里**没有** `ggml-vulkan.dll`）。
   ⚠️ **第三十轮补充**：Vulkan **救不回冷 prefill**（冷启 96~225s 量级），
   它只能改善"已经在算"的那一段。**要治的是"别让它冷"。**
2. ~~★ **砍 prompt 长度**~~ ❌ **第三十轮已撤回** —— 用户口径：`prompt 尽量完整`。
   且实测证明**不必砍**：同一份 2050 token 的 prompt，冷启 103.6s / 缓存命中 0.55s，
   **差 190 倍，而长度完全相同** ⇒ 真变量是「前缀缓存是否命中」，不是长度。
   **本轮最值得投的方向改判为：让前缀缓存稳定命中**（见文首「第三十轮实测更新」⑤）。
3. **保持 4B，把精力放回 persona 与采样参数** —— 第 27 轮已证明"删掉固定例句"能把
   开头复读从 62% 压到 33%，这条杠杆还没用尽。
4. **7B 作为"可切换选项"保留**，不进默认 —— 用户想对比时手动切。
   ⚠️ **第三十轮结论**：7B 产品链路首字 1.9~8.3s，**只有 lean（事件台词）达标**。

**需要用户拍板的**：
- 是否要我试 **Vulkan 后端**（需要改 Ollama 启动环境变量，属于可控改动）；
- 7B 是否要**设为默认**（已下完、已注册，可随时切）；
- ~~是否认可「先砍 prompt 长度」这个方向~~ ❌ 已由用户否决（`prompt 尽量完整`）。

### 1.7 ⚠️ 诚实标注：7B 下载现状 **+ 我一度读错了它**

> **本节记录一个我自己的判断错误，因为它比结论本身更值得留痕。**

**当前状态（2026-09-22 02:35 实测）**：
7B（`qwen2.5:7b-instruct-q4_K_M`）**仍在下载中，未就绪**。

```
total     = 4683073952 (4.361 GB)
completed = 1895267576 (1.765 GB)   ← 40.5%
```

**我之前的误判**：我一度认为"4.68GB 的 blob 已下满、Ollama 正在卡死合并分片"。
**这是错的。** 真相是：

- Ollama 的 `blobs/sha256-xxxx-partial` 是**稀疏 / 预分配文件** ——
  **文件大小（4.361 GB）= 预分配的总长，不是已下载量**。
  ⇒ **绝不能按文件大小判断下载进度**，必须看 `/api/pull` 流里的 `completed` / `total`。
- 那 16 个 `-partial-0` ~ `-partial-15` 文件**每个只有 58~68 字节** ——
  它们是**进度标记**，不是数据分片。我把它们当成分片，又进一步坐实了错判。
- 实际下载速度 **约 70 KB/s**（代理 7897 对 Ollama 的 CDN 很慢），
  剩余 2.6 GB 按此速度需 **~11 小时**。
- `server.log` 里那条 `part 8 attempt 0 failed: unexpected EOF, retrying in 1s`
  是**正常的重试**，不是"卡死"。

**教训**：★ **"进度文件的大小"和"进度"是两件事** ——
在稀疏文件、预分配、并行分片这三种机制下，文件大小都可以与进度**完全脱钩**。
**要拿进度就去看那个系统自己的进度 API**，别对着文件系统猜。

**为什么不在本轮死等**：
1. **7B 在当前配置下本来就大概率不达标**（§1.3b + §1.4）；
2. 下载不影响本轮任何交付物 —— 路由层、回归锁、G2、报告**全部已完成**；
3. 已挂**后台常驻推进器**（`_pump7b.py`，最多跑 6 小时，每 30s 把
   `completed`/`total`/速度/ETA 写进 `_evidence/pull_7b_progress.txt`），
   **就绪后即可补跑真机实测**，以实测数字覆盖 §1.4 的外推值。

**§1.4 依然是外推值，不是实测值。** 这一点在模型跑通前不会变。

---

## 2. 场景路由层（已交付）

### 2.1 交付物

| 文件 | 说明 |
|---|---|
| `ralsei_pet/modules/scene_routing.py` | **新增**。路由数据层：读 `_routes.json` + `match()` 等纯函数。零依赖（禁 Qt / 禁项目内业务模块） |
| `ralsei_pet/assets/scenes/_routes.json` | **新增**。路由规则表，**改路由零改 Python** |
| `ralsei_pet/modules/scene_controller.py` | **扩充**。新增 7 个路由方法（`load_routes` / `context_snapshot` / `pick_route` / `follow_route` / `destinations` / `destinations_text` / `route_reason`） |
| `ralsei_pet/src/main.py` | **扩充**。预声明 3 个路由状态字段 |
| `code-quality-audit/场景系统-P0/verify_scene_routing.py` | **新增**。回归锁 **106 项** |
| `原作场景线路研究_2026-09-22.md` | **新增**。原作 1–5 章真实房间表 |

### 2.2 分层（本模块存在的全部理由）

```
语境（用户说了什么 / 发生了什么 / 宠物什么心情）
    ↓  match()        —— 纯函数，106 项回归锁守着
一条路线（route）     —— {to: 场景, reason: 为什么, priority: 多急}
    ↓  follow_route()
切到某个场景（scene_id）
```

**路由是数据不是代码**：全部规则在 `_routes.json`。用户想加一条
「下雨天就去屋檐下」，只需改 JSON。

### 2.3 匹配语义

| 键 | 语义 |
|---|---|
| `when_scene` / `when_area` / `when_chapter` | 相等判定；`"*"` = 任意 |
| `when_mood` / `when_event` | 单个或数组，命中任一即可 |
| `when_keywords` | **OR 语义**（与其余键的 AND 不同，刻意如此） |
| 排序 | `priority` 升序 → **命中条件数降序** → 声明序 |

### 2.4 三条设计律（全部配正/负控制成对）

1. **匹配不到 → 返回 `None`，绝不伪造一条默认路线**。
   `None` 的语义是「**没有任何理由换场景**」→ 调用方该原地不动。
2. **坏规则跳过，不作废整张表** —— 一条写错不该让所有场景都去不了。
3. **未知条件键一律放行**（渐进增强）——
   将来加 `when_weather` / `when_hour` 时，老版本读到新规则不该崩。
   ⚠️ 方向是刻意选的：放行的风险"能被观测到"，拒绝的风险是"静默失效"。

### 2.5 零行为变化（P0 判据的延续）

路由层**没有自动调用方** —— 不注册定时器、不挂事件链路、不自己播动画、
只有 `follow_route` 允许调 `switch`。全部由回归锁 H1–H5 守着（含负控制）。

### 2.6 一个真实价值：`destinations()` 会过滤未登记场景

`destinations()` 拿 `_index.json` 过滤 —— 防止 AI 被送往一个「路由表写了但场景
不存在」的地方。那会 `switch()` 失败，表现为"走了一半停住"，很难查。

---

## 3. 原作地图线路（已抄下来）

来源：`code.deltarune.wiki` 的 GML 反编译 `scr_roomname`。
完整表在 `原作场景线路研究_2026-09-22.md`。

### 3.1 从 GML 读出来的机制（决定我们怎么设计）

1. **房间 ID 是扁平整数**，`roomname` 由 id 反查显示名。
2. **同一 id 在不同章是不同房间** ⇒ **id 必须与 chapter 联合才唯一**
   ⇒ 印证我们的三级模型（chapter 必须显式存，不能靠 id 前缀推）。
3. ★ **`stringsetsubloc` 是两段式命名** ——
   `stringsetsubloc("~1 - Atrium", "Dark Sanctuary", key)` 渲染成
   **「Dark Sanctuary ~1 - Atrium」**（父地点 + 子地点）。
   ⇒ 我们 `describe()` 输出「章节、区域、场景」是**同一个结构**，方向对了。
4. ★ **原作没有显式"区域"数据结构** —— 区域是从显示名的 ` - ` 前缀**推出来的**。
   我们把它显式化成 area 层，**这是比原作强的地方**，保持。

### 3.2 房间表规模

| 章 | 房间数 | 主要区域 |
|---|---|---|
| Ch1 | 20 | Castle Town / Field / Forest / Card Castle |
| Ch2 | 20 | Cyber Field / Cyber City / Queen's Mansion |
| Ch3 | 8 | Dark World / TV World / Cold Place |
| Ch4 | 19 | Hometown / Dark Sanctuary / 2nd、3rd Sanctuary |
| Ch5 | 20 | Garden / Cliffs / Flower Castle |
| **合计** | **87** | —— |

### 3.3 ★ 为什么现在**不**把原作场景登记进 `_index.json`

**素材还没做**（`__main__` §7 待裁定：`deltarune_ralsei/` 里 1111 张 PNG **全是
Ralsei 角色精灵**，全仓库搜 `bg_` / `tileset` / `room_` / `map_` **零命中**）。

现在登记原作场景 = 切过去是**一个空场景** = 「世界突然变白」，**比不切更糟**。
所以 `_routes.json` 里只登记 `desktop`，其余留待素材到位。

### 3.4 落地步骤（三步，零改代码）

1. **登记**：往 `_index.json` 加一章 / 一区 / 若干场景；
2. **定义**：在 `assets/scenes/` 放 `<scene_id>.json`；
3. **打通**：往 `_routes.json` 加一条规则（`to` + `when_*` + `reason`）。

---

## 4. 一个被回归锁逮到的问题（值得记）

扩充控制器后，**`scene_p0` 套件的 A6 断言立刻报红**：

```
[FAIL] A6 scene_controller.py 只 import 标准库 + scene_system
       <<< imports=['logging','scene_routing','scene_system']（越界=['scene_routing']）
```

这是**回归锁正常工作**，不是误报。`scene_routing` 确实是新增的 import，A6 的
白名单是**显式**的，必须人工扩。

处置：把 `scene_routing` 加进白名单，并**补一条负控制**（`A6b`，确认白名单没宽到
"什么都放得过"）。套件 79 → **80 项**。

★ 这个"麻烦"是**刻意保留的** —— 它逼每次放宽都成为一次有意识的决定。
如果白名单当初写成"只要不是 Qt 就放行"，这次就会静默通过。

---

## 5. 验证

| 项 | 结果 |
|---|---|
| `scene_routing` 回归锁 | **106/106 PASS** |
| `scene_p0` 回归锁 | **80/80 PASS**（79 → 80，新增 A6b 负控制） |
| G2 全量 | **`PASS=1358 FAIL=0 套件=26`** / **26 全 IDENTICAL** / `rc=0` |
| 基线变更 | `round5_smoke` 38→39（模块数）、`scene_p0` 79→80、`scene_routing` 106 新增；**合并模式更新，其余 23 条一条没少** |

### 本轮的探针 bug（自查自纠，2 个）

| # | 现象 | 真因 | 性质 |
|---|---|---|---|
| J3 | 断言「含 `Queen's Mansion`」恒假 | 我用的是**弯撇号** `\u2019`，研究记录里是**直撇号** | 探针写错 |
| J6 | 断言「指向 `_index.json`」恒假 | 研究记录当时**确实没写** `_index.json` 的联动说明 | **真实缺口** |

★ J6 是**产品侧的真实缺口**（不是探针错）—— 已补上「怎么把这张表变成我们的场景」
一节。**两个都报了红，但一个该改探针、一个该改数据** —— 必须先读原文再改。

---

## 6. 待用户裁定

1. **7B**：装好备用（不设默认）？还是不要？**是否要我试 Vulkan 后端**（核显加速，
   唯一能同时提速又保质量的方案）？
2. **场景美术素材来源** —— 仍是 P1 开工前唯一阻塞项（A 程序化生成 / B 用户截图 /
   C 重绘，**推荐 A+B**）。
3. 场景粒度 / 是否要 BGM / 原作版权尺度（三项沿用第 28 轮，未变）。

---

## 7. 证据索引

`E:\Download\场景系统侦察\_evidence\`：

| 文件 | 内容 |
|---|---|
| `hw2.txt` / `gpu_deep.txt` / `gpu_mem.txt` | 硬件实测（CPU / 核显 ID / 内存条 / 显存） |
| `ollama_backend.txt` | Ollama `inference compute` 行（`library=cpu`）+ 内存余量 |
| `ollama_backend_probe.txt` | `size_vram=0` + tok/s 实测 |
| `ab_7b_perf.txt` | 4B 三条真实场景的流式首字/整句实测 |
| `scene_routing_run.txt` | 106 项路由回归锁输出 |
| `scene_p0_diag.txt` | A6 报红与修复后的 80/80 |
| `G2_after_routing.txt` | G2 全量输出 |
| `baseline_update_routing.txt` | 基线合并更新的完整过程（含"一条没少"核验） |
| `manifest_probe.txt` | 7B 下载状态与 manifest 结构 |
