# 第 25 轮报告：跨项技术债清理（W1-6 施工期发现的四项）

日期：2026-09-21
范围：H4/H5 架构改造 · 收官后的**技术债清算轮**（不新增功能，只还债）
判据：G2 回归 24 套件 IDENTICAL + 专项探针 27/27 PASS

---

## 0. 本轮做了什么（一句话）

Wave 1（W1-1 ~ W1-6）施工过程中陆续暴露出四处"**当下不致命、但会持续误导后来人**"的技术债，
本轮把它们一次清掉：两处守卫写法不一致、一处同构缺失、一处**会主动产出假线索**的静态清单。

---

## 1. 四项改动

### 1.1 `stop_watching_video` 的守卫写法收敛

**位置**：`ralsei_pet/modules/video_controller.py`

**问题**：同一个"定时器是否已建"的判断，在同一文件里用了两套写法——

| 位置 | 原写法 |
|---|---|
| `stop_watching_video` | `if hasattr(self, 'video_watching_timer'):` |
| `_start_video_watching_loop` | `if not hasattr(...) or self.video_watching_timer is None:` |

`video_watching_timer` 已在宿主 `main.py` 的 `init_systems` 区**显式预声明为 `None`**，
所以"非 None"才是真判据；`hasattr` 只测"名字存在"，且遇描述符/属性会触发副作用。

**改法**：

```python
# 判据与 _start_video_watching_loop 里的 `if timer is not None:` 保持同一套：
# 该属性已在宿主 init_systems 区**显式预声明为 None**，所以「非 None」才是真判据。
# 不用 hasattr —— 它只测名字存在、且遇描述符/属性会触发副作用。
if getattr(self, 'video_watching_timer', None) is not None:
    self.video_watching_timer.stop()
```

**故意**保留 `_start_video_watching_loop` 里的 `hasattr`：那是 **create-if-absent** 分支，
`hasattr` 在此语义正确（且带 `or ... is None` 兜底）。该文件 `hasattr(` 计数 = 1。

---

### 1.2 `GamesController` 补齐 `__setattr__` 护栏（同构债务）

**位置**：`ralsei_pet/modules/games_controller.py`

**问题**：五个控制器里，spell / hide / video / file_sheet 都有 `__setattr__` 写转发护栏，
**只有 `GamesController` 没有**。W1-3 拆分时之所以没暴露问题，是因为当前所有写入都是
**就地字典变更**（`self.game_state.update({...})`、`self.game_state["k"] += 1`）——
就地变更**不触发** `__setattr__`。

但这是一处**潜伏债务**：只要有人把

```python
self.game_state.update({'x': 1})
```
重构成
```python
self.game_state = {'x': 1}
```

赋值就会**静默落进控制器自己的 `__dict__`**，宿主读到旧对象 → **状态劈成两份**，
而 G2 看不见（两边行为在测试里恰好一致）。

**改法**：插入与其余四个控制器逐字同构的护栏（含 `'p'` 必须跳过，否则控制器丢掉宿主引用）：

```python
def __setattr__(self, name, value):
    if name != 'p':
        pet = self.__dict__.get('p')
        if pet is not None:
            if name in pet.__dict__ or any(
                    name in klass.__dict__ for klass in type(pet).__mro__):
                setattr(pet, name, value)
                return
    object.__setattr__(self, name, value)
```

---

### 1.3 `main.py` 读守卫统一为 `getattr(..., None) is not None`

**位置**：`ralsei_pet/src/main.py`（8 处）

**问题**：`hasattr(self, X)` 在 main.py 里有 **27 处**，语义并不相同，机械替换必出事。
先用 AST 做了**三分类**：

| 角色 | 形态 | 处理 |
|---|---|---|
| **CREATE** | `if not hasattr(self, X): self.X = ...` | 保留（语义正确）11 处 |
| **DELATTR** | `if hasattr(self, X): del self.X` / `delattr` | 保留（语义正确）7 处 |
| **READ** | `if hasattr(self, X):` 之后**读** `self.X` | **改写** 8 处 |

改写为 `getattr(self, X, None) is not None`（NOT 形式为 `... is None`）。

**改写点**：`last_movement_end_time`、`previous_direction`、`_last_env_update`、
`_cached_screen_geom`、`fall_slide_speed_y`、`_last_drag_pos`、`_fall_phase`、`_bounce_params`。

**刻意保留**：`_speed_variation`（DELATTR）、`_is_being_dragged` 的**两处回写守卫**
（`self._is_being_dragged = False`）——后者是"赋值后回写"，不是读守卫，改了就变味。

**为什么这是**加强**而不是等价改写**：`hasattr` 对"值为 `None`"的属性返回 `True`；
`getattr(...) is not None` 才是"值可用"。凡下游要做算术/索引/比较的点，
"值可用"才是真契约。这也是本轮**唯一**改了判据强度的地方，因此配了行为级探针（见 §2）。

**改后 AST 复核**：`hasattr-if` 总 19 处 → {CREATE 11, READ 1, DELATTR 7}；
剩下那 1 处 READ 即 `_is_being_dragged`（回写守卫，正确保留）。

---

### 1.4 `scan_zero_refs.py`：静态清单过期 → 假线索（**最值得记的一条**）

**位置**：`code-quality-audit/架构改造-H4H5/scan_zero_refs.py`

**问题**：G1 筛查脚本里手维护的 `WAVE1` 清单**已经过期**，里面混着
**全仓库根本没有定义**的方法名。原实现的下游判断是：

```python
refs = 收集到的引用点
if not refs: print("引用点 0")     # ← 与"真的死代码"一模一样
```

于是**清单过期**和**发现死代码**这两种完全不同的事，输出长得一模一样 ——
G1 每跑一次就吐三条幽灵线索，把真发现淹没掉。

**改法**：

1. 重写 `WAVE1`：删掉从未存在的 `create_person_name_table`；补上 W1-1 `_cast_spell_then`、
   W1-2 `_hide_ralsei`、W1-6 `_open_desktop_item_by_name`；**故意保留** W1-5 三条
   （`check_browser_windows` / `check_ppt_windows` / `check_excel_table_needs`）当作标记的活体演示。
2. `scan()` 额外收集 `defined` 集合（`FunctionDef` / `AsyncFunctionDef` / `ClassDef` 的名字），
   返回三元组 `(hits, defined, parse_fail)`。
3. 输出分流：

```python
if not has_def:
    # 清单里有、仓库里没有 → **清单过期**。这不是死代码线索！
    lines.append("  %-38s [已移除] 全仓库无定义（静态清单过期，非死代码）" % n)
    continue
```

**效果（立竿见影）**：新标记一上线就暴露出 **W1-5 整组三条**全是幽灵条目 ——
`check_browser_windows` / `check_ppt_windows` / `check_excel_table_needs`
**全仓库无定义**，即 W1-5 的静态清单从来没对上过真实代码。此前每一轮 G1 输出里
那三行"引用点 0（疑似死代码）"**全是假的**。

---

## 2. 验证

### 2.1 G2 回归（唯一安全性判据）

| 时点 | 结果 |
|---|---|
| 改动前基线 | PASS=1164 FAIL=0 套件=24 |
| 改动后 | PASS=1164 FAIL=0 套件=24，24/24 IDENTICAL |

**唯一一次 DIFF 及其处置**：`round8_anim`，差异仅一行：

```
-[PASS] G1.6 ... <- len=20885
+[PASS] G1.6 ... <- lock=True drag=True
```

`G1.6` 的**断言本体是内容级的**（两个 needle 都命中 + 前者位置在前），与源码长度无关；
`len=20885` 只是塞进 `detail` 的**诊断数字**，而 `_um` 是 `update_movement` 的**源码片段**——
本轮往 main.py 加注释 → 片段变长 → **断言全 PASS 却是字节 DIFF**。

这正是既有铁律「**描述/断言里别写会随代码增长的数字**」的经典复演，且**历史上就踩过**
（那条 `len=20885` 本身就是上一次"想加定位线索、结果给了个易变数字"的产物）。

**修法经三次迭代**（每次都被实证推翻）：

| 版本 | detail | 结果 |
|---|---|---|
| v1 | `len(_um)` | 随源码长度变 → 假 DIFF |
| v2 | `order=%d<%d in 文件名 \| head=首行` | 吃到别的代码段的怪字符串 → **更大的**假 DIFF |
| v3 | `order=%d<%d`（序号） | 序号是**绝对偏移**：needle 之前加一行就 +N → 照样漂 |
| **v4 终版** | `lock=True drag=True`（纯布尔） | **与代码增长完全解耦** |

**v3 → v4 的转折靠 A/B 实证**（不是推理）：

```
A: len(_um)=20903  detail='lock=True drag=True'
B: len(_um)=21371  detail='lock=True drag=True'   (B 插入了 12 行噪声)
OK: len(_um) Δ = 468（尺子有鉴别力）
PASS: detail 在源码增长 12 行后**保持不变** ← 解耦成立
```

关键设计：**先断言尺子有鉴别力**（`len` 必须真的变，否则 `SystemExit`）——否则就成了
"自己跟自己比"的伪验证（既有铁律「A/B 先断言 A ≠ B」）。

**基线更新**：`--update --only round8_anim`（**合并模式**）。
核验：`suites` 24 个套件一个不少；`round8_anim` 的
**基线哈希 = 现算哈希 = `.baseline.txt` 哈希 = `799d974d05a395c0`**，三重一致。

### 2.2 专项探针 `verify_round25_cleanup.py`（**27 PASS / 0 FAIL**）

守 G2 结构上看不见的部分，静态 + 行为双层：

- **静态**：V1–V4（video 守卫写法）、G1–G5（含"五个控制器都有 `__setattr__`"）、
  M-getattr ×8、M1、M2（AST 分类：READ 型残留必须为 0）
- **行为**：B1–B7，用 `FakeHost` 断言宿主转发、`'p'` 保留、MRO 兜底读
- **B8 是反向控制**：子类把 `__setattr__` 换回 `object.__setattr__`，证明该护栏**有鉴别力**
  （否则"通过"什么也证明不了）

**教训（探针自己的 `code_only()` 迭代 5 版，全部是探针 bug、产品代码始终正确）**：
1. 正则按行剥引号 → 跨行三引号 docstring 没剥掉（假绿）+ `'p'` 被抹掉 → 12 红
2. `tokenize` 剥**所有** STRING → **needle 本身被抹掉** → 13 红
3. `tokenize` 只剥 COMMENT/docstring → tokenize 给每个 token 补空格 → 15–16 红
4. 手写空格归一状态机 → 仍 16 红
5. **`ast.unparse`** —— 标准答案：注释天然消失、docstring 是 `Expr(Constant(str))` 可显式摘除、
   调用里的字符串字面量保留、输出规范无多余空格 → **27 PASS / 0 FAIL**

**通则**：别手搓源码清洗器；**源级断言绝不用 `"字面量" in 源码`**；
清洗器**不能把 needle 也洗掉**。

### 2.3 G1 零引用筛查（行为验证）

```
### W1-5 office(old, 方案表里的)
  check_browser_windows    [已移除] 全仓库无定义（静态清单过期，非死代码）
  check_ppt_windows        [已移除] 全仓库无定义（静态清单过期，非死代码）
  check_excel_table_needs  [已移除] 全仓库无定义（静态清单过期，非死代码）
```

其余项均报真实引用点（如 `_cast_spell_then` 4 点、`_hide_ralsei` 1 点、
`_open_desktop_item_by_name` 1 点），无幽灵线索。

---

## 3. 过程中的两个自伤（记下来）

### 3.1 证据文件被自己的重定向污染

重生成 `_evidence/zero_refs.txt` 时，命令**自相矛盾**：既让脚本 `io.open(OUT, "w")` 写该文件，
又让 PowerShell `*>` 重定向到**同一个路径** → 句柄冲突 →
`PermissionError: [Errno 13]` → 脚本把 Traceback 写进该文件。
**更糟**：我随后把 PowerShell 的错误输出当成"证据"转成了 UTF-8，
于是证据文件里装的是 Traceback。

**处置**：用正确姿势重跑（重定向到别处），恢复干净 UTF-8 证据。

**通则**：**别让脚本和 shell 重定向写同一个文件**；
**落盘前先确认内容是"结果"而不是"错误输出"**。

### 3.2 读错 JSON 层级，误报"基线里没有该套件"

校验脚本读 `d["round8_anim"]`，而 `baseline.json` 是
`{"version", "note", "suites": {...}}` 嵌套结构 → 顶层只有 3 个键 →
误判 "`round8_anim` 不在 baseline 中"。改用 `d["suites"]` 后一切吻合（24 套件齐）。
**通则**：与铁律 7 同族 —— **见到异常先怀疑自己的判据**。

---

## 4. 本轮净产出

**改动的文件**

| 文件 | 性质 |
|---|---|
| `ralsei_pet/modules/video_controller.py` | 守卫写法收敛（1 处） |
| `ralsei_pet/modules/games_controller.py` | 补 `__setattr__` 护栏（同构） |
| `ralsei_pet/src/main.py` | 8 处读守卫加强 |
| `code-quality-audit/架构改造-H4H5/scan_zero_refs.py` | 清单校正 + 过期标记 |
| `code-quality-audit/第八轮/verify_round8_anim.py` | G1.6 detail 去易变量 |
| `code-quality-audit/regress/baseline.json` | `round8_anim` 合并更新 |
| `code-quality-audit/架构改造-H4H5/verify_round25_cleanup.py` | **新增**：27 项行为探针 |
| `code-quality-audit/架构改造-H4H5/_evidence/zero_refs.txt` | 证据重生成 |
| `code-quality-audit/架构改造-H4H5/_evidence/verify_round25.txt` | 证据重生成 |

**门禁状态**

- G2：24/24 IDENTICAL，**PASS=1164 FAIL=0**
- 专项探针：**27 PASS / 0 FAIL**
- `compileall`：通过

---

## 5. 仍未做完的事（交接）

1. `handle_game_input` 搬进 `GamesController`（W1-3 遗留）
2. "预声明规则"升格为 H4/H5 正式施工口径
3. 把 4 条新铁律 + A/B 失效谱三形态写进总口径文档
4. W1-6 的 B 块 3 个 `check_*_content` 留给 H4「文件反应」专项
5. **真机手感实测（需用户侧）**
