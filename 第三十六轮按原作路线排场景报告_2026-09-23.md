# 第三十六轮：按原作路线排场景（场景系统数据面落地）

日期：2026-09-22 ~ 2026-09-23
范围：`ralsei_pet/assets/scenes/`（数据层）+ 回归锁 + 报告
未改：`main.py` / `scene_system.py` / `scene_controller.py` / `scene_routing.py` 一行业务代码

---

## 0. 一句话结论

**场景系统「按原作路线」的数据面已经全部落地并通过验证**：88 个场景（桌面 + 5 章 87 个原作房间）逐条可回溯原作 `room_id`，26 条路由按原作剧情顺序推进，桌面是与作品内场景**同级同构**的一等场景。G2 回归 **32 套件 / PASS=1500 / FAIL=0**。
**唯一缺口是美术**：用户选定 B 方案（自己用 UndertaleModTool 导出原作背景图），导出清单已备好；图片放进 `bg/` 即生效，**零改代码**。

---

## 1. 本轮起点：素材勘察发现「素材里没有场景图」

用户指令：

> 开始按照原版的路线和 `C:\Users\23002\Desktop\项目文件夹\niko的秘密\DELTARUNE_183049\DELTARUNE` 文件内的素材开始排序，场景系统完全遵循原作的逻辑，把桌面当成一开始的默认场景就好

### 1.1 实测清点结果（证据：`_evidence/DELTARUNE素材勘察.txt`）

| 项 | 数值 |
|---|---|
| 文件总数 | **921** |
| 总体积 | **4.23 GB** |
| `.ogg` | 454 |
| `.mp3` | 205 |
| `.flac` | 204 |
| `data.win` | 12（各章 GameMaker 数据包） |
| `.json` | 21（含 `lang/` 对话文本） |
| `.mp4` | 7 |
| **图片** | **2**（`1\cover.png`、`2\cover.gif`，均为章节封面） |

**结论：该目录零场景背景素材。** 原作背景图封在 GameMaker 的 `data.win` 内（`FORM … GEN8` 头，GameMaker Studio 2 格式），不解包拿不到。

### 1.2 处置：先停，不在错误前提上施工

按项目铁律「探针不保真 = 报假问题」「先自己查再问」，**没有**假设"素材里有图"就开写。向用户给出 A/B/C/D 四个选项：

- **A** 我用 UndertaleModTool 反编译 `data.win` 取散图（重、需装工具、耗时长）
- **B** 用户自己导出背景图，我先做不依赖美术的部分 ★**用户选此项**
- **C** 先用纯色/渐变占位图，后续替换
- **D** 不用原作图，另找素材

用户回复：

> 我选B，但在此之前，你先继续干其他的，等我导出好了就叫你

**授权口径**：先干不依赖美术的部分；导出好后再替换，替换过程**不改代码**。

### 1.3 顺手确认的两件事

- `lang/*.json` 有**真实对话文本**（ch1 6242 键）→ 后续 persona / 世界观线的可用素材
- `mus/` 有 **339 个 BGM** → 场景 `bgm` 字段直接引用原作文件名（证据：`_evidence/原作BGM清单.txt`）

---

## 2. ★ 一手依据：把原作房间表数据化

「按原作路线」不能凭印象。溯源到 `code.deltarune.wiki` 的 `scr_roomname` 反编译结果，落成 `assets/scenes/_original_rooms.json`。

### 2.1 从 GML 反编译实证到的四条原作机制

写进该文件的 `meta.how_original_works`，作为后续所有推导的依据：

1. **房间 ID 是扁平整数** —— 全游戏一张表，不是分层结构。
2. **章号进万位** —— `chapter * 10000 + local_id`（如 ch2 的房间是 `2xxxx`）。所以"章"是从 ID 数值区间推出来的。
3. **显示名是两段式** —— `stringsetsubloc` 渲染成「父地点 ~1 - 子地点」。**原作没有显式的"区域"结构**，"区域"是从显示名的 ` - ` 前缀**反推**出来的。
4. **`room_goto` 只负责"换"** —— 画什么由新房间的 Create 事件决定。⇒ **路由（去哪）与渲染（画什么）必须分开**，这正是本项目 `scene_routing.py`（纯数据层）与 `scene_controller.py`（接线层）分层的原作依据。

### 2.2 覆盖度（实测）

| 章 | 房间数 | 区域数 |
|---|---|---|
| ch1 | 20 | 7 |
| ch2 | 22 | 6 |
| ch3 | 9 | 4 |
| ch4 | 20 | 9 |
| ch5 | 21 | 8 |

---

## 3. 交付物一：88 个场景

### 3.1 `_index.json` 改写

从「只有 `desktop` 一个场景」→ **6 章 / 88 场景**：

| 章 | 场景数 |
|---|---|
| desktop | 1 |
| ch1 | 19 |
| ch2 | 21 |
| ch3 | 8 |
| ch4 | 19 |
| ch5 | 20 |
| **合计** | **88** |

`default_scene` **仍为 `desktop`** —— 对应用户原话「把桌面当成一开始的默认场景」。

`meta` 新增两个留痕键：`original_rooms_file`、`how_scenes_were_derived`。

### 3.2 87 个场景 JSON（桌面外每个原作房间一个）

每个文件结构：

```json
{
  "schema_version": 1,
  "scene_id": "ch1.field.field_great_door",
  "name": "原野·大门",
  "chapter_id": "ch1", "chapter_name": "第一章",
  "area_id": "field",  "area_name": "原野",
  "bg": "bg/ch1_field_field_great_door.png",
  "bgm": "field_of_hopes.ogg",
  "ambient": null,
  "objects": [],
  "anchors": {},
  "transition": null,
  "_comment": {
    "original_room": {"chapter": "ch1", "room_id": 49, "display_name": "Field - Great Door"},
    "why_bg_is_a_placeholder": "...",
    "why_objects_is_empty": "..."
  }
}
```

三个 `_comment` 键是**刻意的留痕**：说明背景为何是占位、`objects` 为何为空（美术/物件待后续），避免日后误读成"漏写了"。

### 3.3 ★ 生成时踩的坑：脏 scene_id

第一版 `norm_id` 兜底对未映射名字产出了脏值：

| 脏值 | 病灶 |
|---|---|
| `kris_s_room` | 撇号残渣 |
| `queen_s_mansion` | 撇号残渣 |
| `2nd_sanctuary_1_atrium` | **数字开头**（非法标识符风格） |

**处置**：把全部名字补进 `NAME_MAP` 作为权威（`kris_s_room` / `noelle_s_house` / `queen_s_mansion_*` / `sanctuary_2_*` / `sanctuary_3*`），并**删掉 `norm_id` 兜底** —— 未映射名字改为 `[ERROR]` + 跳过，**不再静默产出脏 id**。

校验结果：88 个 `scene_id` **零脏值**。

---

## 4. 交付物二：26 条路由

`assets/scenes/_routes.json`，全部按原作剧情顺序手工编排。

### 4.1 ★★ 本轮最值钱的发现：「兜底压死剧情」

**第一版写法**（我最初的直觉写法）：

```json
{"to": "desktop", "priority": 10, "when_scene": "*"}
```

**症状**：所有剧情路线形同虚设。

**复现**（用产品真函数 `route_target()`，不是探针）：

在 `ch1.field` 语境下，两条规则同时命中：
- `desktop` 那条：`priority=10, score=1` → **赢**
- `ch1.forest.forest_entrance` 那条：`priority=120, score=2` → 输

**根因**：`scene_routing.match()` 的排序键是

```
(priority 升序, -score 降序, 声明序)
```

**`priority` 是第一排序键，压过 `score`。** 兜底给 `priority=10` 就等于给所有路线判了死刑。

**修复** —— 分层优先级（写进 `meta.priority_layers` 留痕）：

| 区间 | 用途 |
|---|---|
| 100–199 | 第一章剧情推进 |
| 200–299 | 第二章剧情推进 |
| 300–399 | 第三章剧情推进 |
| 400–499 | 第四章剧情推进 |
| 500–599 | 第五章剧情推进 |
| 700–799 | 软触发（心情 / 聊天关键词）—— 低于剧情，高于兜底 |
| 800–899 | 场景内细节推进 |
| 900–999 | 无事可做时的归处（回桌面） |
| 10000 | 兜底（`_fallback`） |

`meta` 另加 `critical_warning_priority_beats_score`（把这个坑写在数据里）与 `known_limitation`（暂无 `progress` 概念，属 P2）。

### 4.2 15 条主线推进链（回归锁逐条断言）

ch1 克里斯的房间 → 城堡镇 → 原野大门 → … → 纸牌城堡王座 → **第二章赛博原野** → … → 第五章花园，共 15 条，外加软触发（关键词/心情）与归处规则。

### 4.3 路由自检抓到 5 个悬空引用

第一版有 5 条 `to` 指向未登记场景（如 `queens_mansion_entrance`，正确名是 `queen_s_mansion_entrance`）。

**处置**：生成脚本内置「每条 `to` 必须在 `_index.json` 登记」自检。修正后 **26 条全部已登记**。

---

## 5. 交付物三：背景占位约定（B 方案的关键设计）

**目标**：用户导出图后**零改代码**即可生效。

**做法**：
- 新建空目录 `assets/scenes/bg/`
- 88 个场景各自声明 `"bg": "bg/<scene_id 点转下划线>.png"`
- 文件**尚不存在**。渲染层按占位符处理；用户把图按名放进 `bg/`，**下一次启动即生效**

**导出清单**已落两份：
- `code-quality-audit/第36轮-按原作路线排场景/_evidence/背景图导出清单.txt`
- `ralsei_pet/assets/scenes/bg/_导出清单与命名约定.txt`（放在用户要放图的目录里，便于对照）

清单按章分组，每行给出：`scene_id` / 原作 `room_id` / 原作显示名 / **要存成的文件名**。例：

```
ch1.field.field_great_door    room=49   Field - Great Door   -> ch1_field_field_great_door.png
```

---

## 6. 验证

### 6.1 新回归锁 `scene_route_original`（50 项，全绿）

五段判据，**每段配正/负控制**：

| 段 | 守什么 | 项数 |
|---|---|---|
| **A** | 原作依据在位：文件存在/可解析/五章齐全/每场景可回溯 `room_id` + A9 负控制（抹掉 `original_room` 判据必须失败） | A1–A9 |
| **B** | 桌面仍是一等场景：`default_scene=='desktop'` / 同构三级 / **控制器源码不含 `'desktop'` 字面量**（不开后门）+ B6 合成源码负控制 | B1–B6 |
| **C** | 路由按原作推进：**15 条推进链**逐条断言 + C7/C8 负控制 | C1–C8 |
| **D** | ★**兜底不许压死剧情**：**D4 用产品函数复现第一版坏写法证明本锁有鉴别力** + D5 正控制 + D6 meta 留痕 | D1–D6 |
| **E** | 背景占位口径：每场景有 bg 占位 / 统一 `bg/` / 不伪装成没素材 / 路径口径正确 | E1–E4 |

**D4 是本轮锁的价值核心** —— 它不是"断言现状"，而是"断言那个真实缺陷不会再回来"：

```python
_bad_v1 = [
    {'to': 'desktop', 'priority': 10, 'when_scene': '*'},
    {'to': 'ch1.field.field_great_door', 'priority': 110,
     'when_area': 'castle_town', 'when_chapter': 'ch1'},
]
check('D4 负控制：第一版坏写法（兜底 priority=10）确实会把剧情路线挡死',
      sr.route_target(_hit_v1) == 'desktop', ...)
```

### 6.2 真机产品路径验证（防"函数写对了但产品用不上"）

`verify_live_product_path.py`：在**产品进程内** `M.RalseiPet()` 起来（offscreen + `RALSEI_MEMORY_DIR` 隔离），经转发壳 `pet.scene` 访问。

**实测结果**：

```
1. 宿主状态字段（构造后未初始化场景）:
   _scene_loaded = False   _scene_index is None = True   current_scene = None
2. 经转发壳 self.scene:
   self.scene.ready = False
   self.scene.available_scenes() 条数 = 0
   self.scene.destinations_text() 行数 = 20
3. 切到原作场景（走产品 switch）:
   switch('ch1.field.field_great_door') = True
   切后 describe() = '第一章 · 黑暗世界、希望与梦想之原、原野·大门'
   切后 bg = 'bg/ch1_field_field_great_door.png'
   切后 bgm = 'field_of_hopes.ogg'
4. 往返切换:
   switch(ch2.cyber_city.cyber_city_entrance) = True
   switch(desktop)                            = True
   switch(ch5.garden.garden_beginning)        = True
   switch(desktop)                            = True
5. 产品路由判定:
   pick_route(mood=sleepy) = 'desktop'
6. close() 完成，无异常
```

**结论：产品路径真的能切原作场景、真的能往返，不是"只测试脚本能跑"。**

### 6.3 ★ 顺手修掉一个技术债：H3 写死数据反模式

加 88 场景后 `scene_p0` 套件 **80→79 PASS、1 FAIL**，DIFF 在 H3。

**根因**：原断言写死 `available_scenes() == ['desktop']` —— 只在"索引刚好多只有 1 个场景"时成立。这与项目既有反模式同源（「模块数会随新增模块而变，勿把具体数字写进描述」）。

**修复**：改断言**真正契约** —— 数量与索引一致 + 有序 + 含 desktop。对"加场景"免疫，但对"漏登记 / 不排序 / 丢 desktop"仍报红。

```python
_expected = sorted((_idx_now.get('scenes') or {}).keys())
ok('H3 available_scenes() 列出索引里的场景（数量一致 + 有序 + 含 desktop）',
   bool(_got) and _got == _expected and _got == sorted(_got) and 'desktop' in _got, ...)
```

结果：**80/80 恢复**。

### 6.4 G2 全量回归

```
suite                exit  PASS  FAIL  比对
round5_smoke          0     39     0   IDENTICAL
round5_verify         0     16     0   IDENTICAL
round6_verify         0     39     0   IDENTICAL
round7_launch         0      5     0   IDENTICAL
s1_anim_miss          0     17     0   IDENTICAL
s2_anim_json          0     40     0   IDENTICAL
s3_alias_legacy       0     19     0   IDENTICAL
round8_dialogue       0     20     0   IDENTICAL
round8_floor          0     17     0   IDENTICAL
round8_fling          0     31     0   IDENTICAL
round8_anim           0     28     0   IDENTICAL
round9_focus          0     54     0   IDENTICAL
round9_memory         0     51     0   IDENTICAL
round10_graph         0     98     0   IDENTICAL
round11_input         0     64     0   IDENTICAL
round12_store         0     58     0   IDENTICAL
round13_build         0     72     0   IDENTICAL
round14_move          0     41     0   IDENTICAL
round15_cleanup       0     30     0   IDENTICAL
round17_build_fall    0     18     0   IDENTICAL
round18_climb         0     53     0   IDENTICAL
persona_chat          0    156     0   IDENTICAL
s8_stream             0     69     0   IDENTICAL
s7_event_speech       0    138     0   IDENTICAL
scene_p0              0     80     0   IDENTICAL
scene_routing         0    106     0   IDENTICAL
scene_route_original  0     50     0   IDENTICAL   ← 本轮新增
round34_movement      0     36     0   IDENTICAL
round34_floor_manager 0     15     0   IDENTICAL
round34_store_config  0     17     0   BASELINE
round34b_move_beat    0     13     0   BASELINE
round34c_privacy_wire 0     10     0   BASELINE

合计：PASS=1500 FAIL=0  套件=32
```

**基线处置**：`scene_p0` 因断言文本有意变更、新套件未入基线，两条用 **`--only <suite> --update`（合并模式）** 各自重建，确认其余 27 个旧套件基线**一条没少**。

---

## 7. 交付清单

### 新增

```
ralsei_pet/assets/scenes/
  _original_rooms.json             原作房间表（五章全量，含四条机制说明）
  bg/                              背景图目录（空，含导出清单）
  ch1.*.json … ch5.*.json          87 个场景文件
code-quality-audit/第36轮-按原作路线排场景/
  verify_scene_route_original.py    50 项回归锁
  verify_live_product_path.py       真机产品路径验证
  _evidence/DELTARUNE素材勘察.txt
  _evidence/原作BGM清单.txt          339 个
  _evidence/真机产品路径验证.txt
  _evidence/真机产品路径验证_原始输出.txt
  _evidence/G2_after_36.txt
  _evidence/背景图导出清单.txt
```

### 修改

```
ralsei_pet/assets/scenes/_index.json       1 场景 → 88 场景
ralsei_pet/assets/scenes/_routes.json      0 → 26 条
code-quality-audit/regress/run_all.py      插入新套件
code-quality-audit/regress/baseline.json   两条合并更新
code-quality-audit/场景系统-P0/verify_scene_p0.py   H3 改断真实契约
```

### 未改（刻意）

`main.py` / `scene_system.py` / `scene_controller.py` / `scene_routing.py` —— **一行业务代码都没动**。本轮纯数据面 + 回归锁。

---

## 8. 待裁定 / 阻塞项

| 项 | 级别 | 状态 |
|---|---|---|
| **背景图** | **阻塞 B 方案** | 等用户导出；清单已备，放入即生效，零改代码 |
| `progress` 概念（房间级推进） | P2 | 现路由无"进度"概念，只按 区域/章节/心情/关键词 匹配；原作剧情推进需要它 |
| `pet_interaction.py`（365 行零引用） | 待裁定 | 建议暂缓（既没接线也没人调用） |
| F35-1 / F35-4 修复归属 | P2 / P3 | 建议并入 P3 技术债批量处理 |
| P3 技术债包 | P3 | 排队中 |

---

## 9. 下一轮建议

1. **用户导出背景图前**：可先做 `progress` 概念（P2）—— 它是"按原作剧情推进"从"能切场景"到"按流程走"的关键缺口
2. **导出后**：把图放进 `bg/`，跑一遍 `verify_scene_route_original.py` 的 E 段确认占位被真实文件满足
3. **物件（objects）与锚点**：原作房间里还有 NPC / 可交互物，现全为空数组，是 P1 候选
