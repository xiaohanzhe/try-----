# 代码质量复审报告（第二十六轮）· 第一次复检

> **复审日期**：2026-09-21
> **复审性质**：**只读复审**（用户明确选择「先报告再定」——本轮**不改任何产品代码**）
> **复审范围**：全项目（`ralsei_pet/src/main.py` + `ralsei_pet/modules/` 共 37 个模块 + 对话链路 + H4/H5 拆分契约）
> **复审基线**：`main.py` 482686 B / 8987 行 / md5 `493b48454c878056021df74dd9d04264`
> **回归基线**：G2 **24 套件 / 1164 PASS / 0 FAIL / 全部 `IDENTICAL` / exit==0**（本轮重跑确认，代码当前是绿的）
> **上一个提交**：`04da6e6`（H4/H5 施工总口径文档）

---

## §0 结论先行

| # | 结论 | 一句话 |
|---|---|---|
| 1 | **H4/H5 Wave 1 拆分质量：合格** | 5 个控制器 `__setattr__` **逐字同构**、预声明 **0 漏项**、`_CONTROLLER_ATTRS` **双向完全匹配**、跨控制器调用链 **全部可解析**。**未发现高/中危问题。** |
| 2 | **人味改造线：7/8 条契约成立** | persona 读文件 ✅ / 无 markdown ✅ / 无成对示范 ✅ / 判退比 recent ✅ / 护栏五步+重采样 ✅ / 流式 chunk_size=1 ✅ / 抽样参数落 Modelfile ✅ |
| 3 | ❗ **1 处口径疑点（需用户裁定）** | 「AI 关闭必须沉默、不回落内置台词」在**自主开口**链路成立；但在**用户主动打字聊天**的 AI 失败分支**会回落内置台词库**（`dialogue_ui.py:1259-1270`）。**这是本轮最高优先级的待裁定项。** |
| 4 | ⚠️ **4 个确认死代码** | `react_to_file_emotionally` / `follow_file` / `react_to_file_deletion` / `check_dragged_file` —— **全项目零调用**（已逐一验证） |
| 5 | ⚠️ **1 处"假绿"重灾区** | `main.py:6849-6916` **连续 7 个 `except Exception: pass`** 构建 AI 上下文 → 上下文静默缺失 → 表现为"AI 变笨"，无任何痕迹 |
| 6 | ⚠️ **1 条制度性缺口** | 「防御性 except 必须配异常计数」这条例律**只存在于 `text_segmenter.py` 的注释里**，全项目其余防御性捕获均无计数 |

**总体判定：本轮无「阻断级」缺陷。** 建议处置优先级见 §4。

---

## §1 H4/H5 Wave 1 拆分专项复审（本轮重点）

> **数据来源**：独立只读深审（4 个方向）+ 本人关键位置逐一复核。**全部行号经 `Read` 确认。**

### 1.1 `__setattr__` 同构性 —— ✅ 通过

| 控制器 | 位置 | 跳过 `'p'` | 判据 | 递归风险 |
|---|---|---|---|---|
| `GamesController` | `games_controller.py:146-153` | 是 | `pet.__dict__` 或 `type(pet).__mro__` | 无 |
| `VideoController` | `video_controller.py:197-204` | 是 | 同上 | 无 |
| `SpellFlowController` | `spell_controller.py:200-205` | 是 | 同上 | 无 |
| `HideAndSeekController` | `hide_controller.py:148-155` | 是 | 同上 | 无 |
| `FileSheetController` | `file_sheet_controller.py:138-145` | 是 | 同上 | 无 |

**五份实现结构上完全等价**（仅注释文字不同）。**没有硬编码白名单**、**没有无限递归写法**
（全部用 `self.__dict__.get('p')` 直取，未用 `hasattr(self,...)` / `getattr(self.p,...)`）。

> 注：W1-4 报告曾记「`GamesController` 补同构 `__setattr__` 作未来护栏」为待办 —— **本轮实测已补齐**，该待办可关闭。

### 1.2 宿主预声明区完整性 —— ✅ 通过（0 漏项）

扫描 5 个控制器全部 `self.<name> = ` 赋值目标，回查宿主 `init_systems()` / `__init__`：

| 未预声明候选名 | 首次赋值 | 已在宿主预声明? |
|---|---|---|
| `_spell_seen_frame` | `spell_controller.py:274` | ✅ `main.py:786` |
| `_spell_cast_start_time` | `spell_controller.py:384` | ✅ `main.py:787` |
| `_hide_search_started_at` | `hide_controller.py:353` | ✅ `main.py:803` |
| `_hide_search_checked` | `hide_controller.py:354` | ✅ `main.py:804` |
| `_hide_moving_cb` | `hide_controller.py:252` | ✅ `main.py:805` |
| `_hide_moving_cb_stage` | `hide_controller.py:253` | ✅ `main.py:806` |

**结论：候选漏预声明 = 0 项。** 两个曾被点名的组（`_spell_*` L786-787、`_hide_*` L803-806）预声明**已补齐**，
且注释明确记录了补齐原因（引用 W1-4 报告第五节铁律 3）。**§1 预声明规则已系统性落实。**

### 1.3 `_CONTROLLER_ATTRS` ↔ `init_systems()` 同步性 —— ✅ 双向匹配

| `_CONTROLLER_ATTRS`（`main.py:512`） | `init_systems()` 赋值 | 一致 |
|---|---|---|
| `games` | `main.py:743` | ✅ |
| `video` | `main.py:746` | ✅ |
| `spell` | `main.py:751` | ✅ |
| `hide_seek` | `main.py:755` | ✅ |
| `file_sheet` | `main.py:761` | ✅ |

反向核对：`init_systems()` 内创建的控制器**恰好 5 个**，无多余、无遗漏 → **不存在静默失效名字**。

### 1.4 跨控制器调用链 —— ✅ 全部可解析

| 调用方 | 被调名 | 目标 | 能否解析 |
|---|---|---|---|
| `spell_controller.py:282` | `_abort_hide_and_seek` | 兄弟（`hide_controller.py:203`） | ✅ 第 3 条白名单 |
| `games_controller.py:461` | `_abort_hide_and_seek` | 兄弟 | ✅ |
| `hide_controller.py:271/326/427` | `_cast_spell_then` | 兄弟（`spell_controller.py:436`） | ✅ |

共 **3 个调用点 / 2 个名字**，**全部能被控制器 `__getattr__` 第 3 条白名单解析**（目标名确实在兄弟控制器**类**上定义）。
宿主链（`open_file`/`open_folder`、`dialogue_ui`、`desktop_interaction`、`game_state` 等）走另一条链，已分开区分。

> ⚠️ **唯一"怀疑未验证"项**：`file_sheet_controller.py:117-122` 虽实现了第 3 条扫描，
> 但其方法体**当前无跨控制器调用**（注释亦自认"本项当前用不到"）→ 该分支**运行时未被执行过**。
> **不是缺陷**，但若未来新增互调，需实测覆盖。**（低危）**

### 1.5 拆分切开的东西（裸名 → 潜在 `NameError`）—— ✅ 未发现

5 个控制器方法体内的裸根（`os` / `time` / `random` / `QPoint` / `QRect` / `QTimer` / `shutil`）
**全部有覆盖**：要么模块级 import（`spell_controller.py:108-115`、`hide_controller.py:84-87`、`video_controller.py:118-125`），
要么方法体内局部 import。**未发现 `NameError` 风险**（铁律 7 已落实）。

### 1.6 转发壳遗留 —— ✅ 干净

* 对 5 个控制器全部方法名做 `^\s+def <name>\(` 全量搜索，`main.py` 中**零命中** → **宿主已无控制器同名实现，无重复逻辑**。
* **无自转发成环**：控制器方法均被外部真实调用（`dialogue_ui.py:1249/1658/1662/1673/1724`、`main.py:4340/4347/4487/5935/5938/5941/7979/8402/8423` 等）。
* `main.py` 拆分后**未发现未用 import**。

---

## §2 人味改造线契约核对

> 独立只读审计 + 本人复核关键位置。**契约 1–8 逐条核对。**

| # | 契约 | 状态 | 证据 |
|---|---|---|---|
| 1 | 人设单一真源 = 从 `assets/ralsei_persona.md` 读文件（非硬编码） | ✅ 成立 | `main.py:6967-6991` 用 `PERSONA_REL_PATH`(`:6951`) `open()` 读 + 缓存 + `_PERSONA_FALLBACK`(`:6957`) |
| 2 | persona 不许有 markdown（`#`/`-`/`**`） | ✅ 成立 | `ralsei_persona.md:1-136` 用全角 `——` 分节；Grep 未命中行首 `#` / `- ` / `**` |
| 3 | A12 三条接法规则 + 无 `主人：`/`我：` 成对示范 | ✅ 成立 | 三条规则 `persona:99-107`；碎片按场合绑定 `:113-122`；`我：` 零命中（`主人：` 仅出现在**代码正则白名单** `main.py:7089`，非 persona） |
| 4 | 判退只比对 recent | ✅ 成立 | `_is_repeat_of_recent`(`main.py:7247-7289`) 只遍历 `recent`；对话链路传 `recent=[c for _r,c in history if _r=='assistant']`(`:6780`) |
| 5 | `_clean_ai_reply(reply, recent=)` 五步 + 判退必重采样 | ✅ 成立 | 护栏 `:7148-7245`；重采样 `:6801-6815`（抬温度 + `_RETRY_NUDGE`） |
| 6 | S8 流式 `chunk_size=1`；护栏最终算数；重采样前 `_stream_reset()` | ✅ 成立 | `api_client.py:335` `iter_lines(chunk_size=1)`；前缀安全 `main.py:7094`；判退前擦屏 `:6806-6807` |
| 7 | 自主说话唯一入口；**AI 关闭必沉默不回落** | ⚠️ **部分成立** ← **见 §3.1** | `start_autonomous_speech`(`main.py:4577`) **成立**（`:4590-4592` 直接 return False）；但 `dialogue_ui.py:1259-1270` **会回落内置台词** |
| 8 | 抽样参数落 Modelfile | ✅ 成立 | `ralsei.modelfile:35,38,39,42,45` 有 `num_ctx/temperature/top_p/repeat_penalty/num_predict`；`_ai_chat_options`(`main.py:6993-7015`) 只发 temperature + max_tokens |

**另发现 2 处轻微不一致（非回归，建议顺手修）**：

* **`start_autonomous_speech` 的 `_on_reply`（`main.py:4620`）调 `_clean_ai_reply(reply_text)` 未传 `recent`**
  → 自主开口路径**跳过契约 4 的车轱辘判退**。对话链路（`:6796/6813`）已正确传 `recent`。护栏方法本身成立，仅调用点少传参。**（低危）**
* `speak_event`（`main.py:4792-4799`）的 `pool` 参数：传了 `pool` 时"AI 不可用/超时/判退 → 说内置台词"。
  属 S7 档位**白名单**设计（未登记的行为完全不变），契约 1–8 未直接覆盖 → **不算回归**，但若"全权交给 AI"是全局口径，此处的 `pool` 调用点需一并复核。**（需用户口径）**

---

## §3 问题清单（按影响面排序）

### 🔴 §3.1 【需用户裁定】AI 失败时，用户主动对话会回落内置台词库

**现象**：`dialogue_ui.py` 的 `submit_input` 有**两条**回落路径都调 `_rule_reply()`：

| 行号 | 触发条件 | 代码 |
|---|---|---|
| `dialogue_ui.py:1325` | AI 回复为空 / 被判退且重采样也没救回来 | `_rule_reply()` |
| `dialogue_ui.py:1346` | `use_ai == False`（AI 未启用）**或** `chat_with_ai` 抛异常 | `_rule_reply()` |

而 `_rule_reply()`（`:1259-1270`）调 **`self.parent.dialogue_system.generate_response(user_input, emotion_system)`**
—— 即 `dialogue_system.py`（629–1273 行，约 650 行）那套**写死的罐头皮**，
内含契约 1 明令禁说的「主人」「有什么我可以帮你的吗」等同款句式（`:36/146/704` 等）。

**为什么这是"待裁定"而不是"确定的回归"**：
* 用户此前的口径原文是「**AI 关闭必须沉默、不得回落内置台词**」——
  但该口径的**语境**是**自主开口**（`start_autonomous_speech`），那里它**已严格成立**（`:4590-4592`）。
* **用户主动打字问 AI** 时"AI 没答上来"该不该保持沉默，**代码无法自我判定** —— 从用户视角，
  自己打了字却收到一片沉默，体验可能比收到一句罐头更差。**这是产品决策，不是缺陷。**

**影响面**：仅「用户主动打字 + AI 未启用/失败」分支。自主开口链路**不受影响**。
**代价**：若裁定为"必须沉默"，需改 `_rule_reply()` 在 `not use_ai` 时早退；会失去"AI 没连上时还能聊两句"的兜底。
**建议**：**先问用户**。若追求一致性，可折中——AI **未启用**时沉默（配置态，用户自己选的），
AI **已启用但请求失败**时给一句**极短的非罐头皮**提示（如"……让我想想。"）。

### 🟠 §3.2 【确认死代码 4 个】全项目零调用（已逐一验证）

| 方法 | 位置 | 引用计数 | 判定 |
|---|---|---|---|
| `react_to_file_emotionally` | `main.py:8205` | **0 次真实调用**（仅 `file_sheet_controller.py:15` docstring 提及） | **确认死代码** |
| `follow_file` | `main.py:8232` | **0 次真实调用**（仅 `file_sheet_controller.py:15` docstring 提及） | **确认死代码** |
| `react_to_file_deletion` | `main.py:8238` | **0 次真实调用**（仅 `file_sheet_controller.py:16` docstring 提及） | **确认死代码** |
| `check_dragged_file` | `main.py:8588` | **0 次**，且方法体是 `pass`（空实现占位残留） | **确认死代码** |

**验证方法**：全项目（`src/` + `modules/`，含测试脚本）逐名 `\b<name>\b` 正则扫描，
区分「定义处」与「调用处」，排除 `.bak` / `.backup_*`。

**附加发现（支撑 `follow_file` 确为死代码）**：
全项目 **`dropEvent` / `dragEnterEvent` / `setAcceptDrops` 全部 0 命中** ——
即**根本没有 Qt 拖拽入口**。`follow_file` 是这条链的**唯一生产者**，但它自己没被调用；
它写入的 `dragged_file` / `is_following_dragged_file` 只被 `main.py:1725/1792/1849` 当"清理"消费
→ **生产者缺失，消费者空转**。（`main.py:766-768` 仍有 `self.dragged_file = None` 的初始化。）

**❗ 易误判澄清**：`interact_with_file`(L8075) / `interact_with_folder`(L8104) / `check_file_content`(L8123)
**不是死代码** —— 调用链是活的：
```
main.py:4124  if random.random() < 0.25        ← 25% 概率的桌面元素交互分支
main.py:4131      self.interact_with_file(element)
main.py:8096          content_reaction = self.check_file_content(file_path)
```
另有 `main.py:4128 self.interact_with_folder(element)`。
⇒ 这正印证了 W1-6 的判断：**它们属「文件反应」，与「文件操作」是两种职责，不可删、不可随手搬。**

### 🟠 §3.3 【假绿重灾区】`main.py:6849-6916` 连续 7 个 `except Exception: pass`

**现象**：构建 AI 对话上下文时，7 个数据源各自的 `try` 块**全部** `except Exception: pass`，**无任何日志**：

| 行号 | 数据源 |
|---|---|
| `:6849-6850` | 当前时段（早晨/深夜…） |
| `:6855-6856` | 天气 |
| `:6861-6862` | 心情 |
| `:6878-6879` | 精力 / 饥饿（分档） |
| `:6891-6892` | 载体状态（掉落/走动/站窗口/桌面） |
| `:6903-6904` | 被冷落时长 |
| `:6915-6916` | 记忆偏好（用户名 / 最近喜欢聊什么） |

**危害**：任一数据源出问题 → 上下文**静默缺失** → 表现为「**AI 变笨/答得没头没尾**」，
而日志里**没有任何痕迹**，排查时完全不可见。这是本项目「假绿」纪律的典型受害场景。**（中-高危）**
**建议**：每处 `pass` → `_log.debug("上下文分片<名称>获取失败: %s", e)`，与项目其它 `# 修复：原先静默吞噬` 的既有做法保持一致。

### 🟡 §3.4 【制度性缺口】「防御性 except 必须配异常计数」未落地

**现象**：该纪律的**唯一文字出处**是 `modules/text_segmenter.py:19` 的注释；
**实际实现只有 `text_segmenter` 一个模块**。全项目其余约 150 处防御性 `except` 均只有 `_log.debug`、**无计数器**。
**危害**：单条 `debug` 日志在正常运行时被日志级别过滤 → 长期"静默" → 与 `pass` 的实际效果接近。**（中危）**
**建议**：**不必全量改造**（成本高、收益递减）。建议只在**关键路径**加计数：
① AI 上下文构建（§3.3）② 记忆读写 ③ 网络请求 ④ 定时器回调。

### 🟡 §3.5 【数据安全】隐私清理走永久删除，不走回收站

**现象**：`main.py:8726 / 8735 / 8744` 的 `os.remove(memory_file / growth_file / entertainment_file)`
—— 「退出时清理数据」走**永久删除**。

**对照**：项目在别处做得**很好** —— `desktop_interaction.py:1509-1554` 明确注释
「回收站移动失败时**绝不**静默降级为 `os.remove` 永久删除」，这正是用户担心的场景且**已被正确处理**。

**危害**：用户误触 `clear_data_on_exit` 后数据**不可恢复**。**（中危，属设计取舍）**
**建议**：与 §3.1 一样**先问用户**——"退出清理"是否也走回收站。若走回收站，需先确认回收站 API 在退出路径上不会阻塞。

**同类**（中危）：`hide_controller.py:227/441`、`spell_controller.py:293` 的
`shutil.rmtree(..., ignore_errors=True)` —— 绕过回收站，且 `ignore_errors` 又叠一层静默。
**但需注意**：这些删的是**游戏过程创建的临时文件夹**（躲猫猫障碍物等），非用户数据 → **危害等级应下调**，建议只补日志。

### 🟡 §3.6 【中危，误报已排除】裸 `pass` 的**真实**分布

初判列出的若干"高危静默吞异常"，经**逐一读原文复核**后**大部分是误报**（因为它们是**清理**逻辑，真正因已在上层记录）：

| 位置 | 初判 | **复核结论** |
|---|---|---|
| `config_manager.py:239-240` | 高危 | ❌ **误报** —— 这是 `except (OSError,...)` 里**清理 `.tmp`** 的 `pass`，**真正原因已在 `:235 _log.error` 记录**。**正确模式。** |
| `memory_store.py:316-317` | 高危 | ❌ **误报** —— 清理 `src + '.tmp'` 的 `pass`；真实失败原因已在 `:308/309` 记录（`_log.warning` + `result['reason']`） |
| `memory_store.py:324-325` | 高危 | ⚠️ **低危** —— 删空目录失败（非递归删），影响面小；建议补 debug 日志 |
| `memory_system.py:508-509 / 981-982 / 1196-1197` | 高危 | ⚠️ **待复核** —— 未逐一读原文，**不据此下结论** |
| `desktop_interaction.py:123-124 / 1342-1343` | 高危 | ⚠️ **中危** —— DWM 窗口属性 / 窗口枚举失败静默：窗口检测失效会让宠物"站在空气上"，用户看到诡异行为却无报错 |

> **❗ 这正是项目纪律「先读原文再看计数」的又一次价值体现** —— 若只看 `except...pass` 的计数，
> 「config_manager 保存失败被吞 = 数据丢失风险」这个结论**非常像真的**，但**实际是错的**。
> **本轮已把误判订正，避免了一次无谓改动。**

**其余**：`main.py` 另有 8 处裸 `pass`（`:1670/3757/4651/6374/8826/8838/8919/8926`），**均为低危**（清理/边界）。

### 🔵 §3.7 【低危】资源管理（整体良好）

**正面发现（值得记录）**：
* **原子写做得很好**：`memory_system.py:541-546`（注释直言"原实现直接 open(w) 覆写会损坏整个记忆文件"）、
  `config_manager.py:229-231`、`customization_system.py:109-113`、`entertainment_system.py:258-262`、`relationship.py:220-222`
  —— 均 `tmp + os.replace`，失败清理 `.tmp`。
* **定时器退出清理成对**：`cleanup_on_exit`(`main.py:8657-8673`) 集中停 13 个定时器 + join `ai_thread(timeout=2)`。
* **线程 GUI 安全**：`main.py:6582-6601` 注释记录了"工作线程无 Qt 事件循环 → 改走 `pyqtSignal`"的正确修复。

**低危项**：
* `main.py:4961` `painter = QPainter(self)` 在 `paintEvent` 中**无显式 `end()`** → 依赖析构。
  若中途抛异常，Qt 会打印 `QPainter::begin: Painter already active`。**建议加 `try/finally`。**
* `main.py:5504` `_bounce_timer`、`main.py:935` `dialogue_init_timer` 等动态创建的定时器
  **不在** `cleanup_on_exit` 的白名单里 → 靠 `QTimer(self)` 父子关系兜底。**可接受。**
* `diag_spell.py:12` `_log_f = open(LOG_FILE,'w')` 无 `with`/`close()` —— **诊断脚本，不影响产品**。

---

## §4 处置建议（按优先级）

| 优先级 | 项 | 类型 | 动作 | 需用户裁定? |
|---|---|---|---|---|
| **P0** | §3.1 AI 失败回落内置台词 | **口径** | 明确「用户主动聊天」分支是否也适用"沉默"口径 | ✅ **必须** |
| **P1** | §3.3 七连 `pass`（AI 上下文） | 缺陷 | 7 处补 `_log.debug`（机械、零风险） | 否 |
| **P1** | §3.2 四个死代码 | 清理 | 按「要么接线、要么删」处置（`check_dragged_file` 是空实现，最无争议 → **建议直接删**） | ✅ 删除需点头 |
| **P2** | §3.6 `memory_system` 三处 + `desktop_interaction` 两处 | 待复核 | 先读原文复核，再决定是否补日志 | 否 |
| **P2** | §3.4 关键路径异常计数 | 制度 | 只在 4 条关键路径加计数，不全量改造 | 否 |
| **P2** | §3.5 隐私清理走回收站 | 取舍 | 确认"退出清理"是否走回收站 | ✅ **必须** |
| **P3** | §2 `_clean_ai_reply` 未传 `recent`（自主开口路径） | 缺陷 | 补传 `recent`（一行） | 否 |
| **P3** | §3.7 `QPainter` 无 `end()` | 健壮性 | 加 `try/finally` | 否 |

**Wave 2 前置建议**：§1 复审全绿 ⇒ **H4/H5 Wave 1 可判定为"稳定"**，
排期方案 §5.3 第 4 步（删转发壳）的**前提已具备**——但**删壳是独立一波，建议单独评估、不顺手做**。

---

## §5 本轮"未做"与"边界"（诚实声明）

* **本轮为只读复审，未修改任何产品代码**（用户选择"先报告再定"）。
* 未做**真机手感实测**（遮挡 / 上下动手感 / 边缘掉落不生气而关窗生气）→ **属用户侧动作**，需真机。
* §3.6 中 `memory_system.py` 三处标为**「待复核」**——**未逐一读原文，不据此下结论**。
* A/B 探针的**结构性盲区**（铁律 1/2/3 在 A/B 中不可见）已在总口径文档 §3.4 记录；
  本轮 §1.6 的结论依据**静态符号解析** + 既有 e2e 实证，未新跑 e2e。
* **未使用**任何批量删除、未触碰 `E:\Download\_tmp\` 存量文件。

---

## §6 附：证据与溯源

| 事实 | 出处 |
|---|---|
| G2 24 套件 / 1164 PASS / 0 FAIL / 全 IDENTICAL | 本轮重跑 `code-quality-audit/regress/run_all.py`，exit=0 |
| `main.py` 482686 B / 8987 行 / md5 `493b48454c878056021df74dd9d04264` | 本轮探针 |
| 5 控制器 `__setattr__` 位置与同构性 | 独立只读深审 + 本人 `Read` 复核 |
| 四个死代码的零引用 | 全项目 `\b<name>\b` 扫描（排除 `.bak`） |
| `dialogue_ui.py:1259-1270 / 1325 / 1346` 回落链 | 本人 `Read` 复核 |
| `main.py:6849-6916` 七连 `pass` | 本人 `Read` 复核 |
| `config_manager.py:234-241` 误报订正 | 本人 `Read` 复核 |
| 人味契约 1–8 | 独立只读审计 + 本人复核关键位置（`main.py:6967-6991` / `persona` / `api_client.py:335`） |
| 原子写 / 回收站 / 线程信号 正面发现 | 独立只读审计 |

---

**（本报告为只读复审产出；§4 的 P0/P1 项需用户裁定后方可施工。）**
