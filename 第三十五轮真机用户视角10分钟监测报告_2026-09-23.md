# 第三十五轮：真机整体运行 + 用户视角 10 分钟监测报告

**日期**：2026-09-23
**环境**：Windows 11 / Python 3.11（`C:\Python311\python.exe`）+ PyQt5
**仓库基线**：`bdaf1a1`
**屏幕**：2560×1600 物理 / 150% 缩放（`availableGeometry` 2560×1528）

**用户指令（原话）**：

> 「最后整体跑一遍程序，连续以用户视角监测10分钟，并以人类逻辑来判断他所做出的动作是否合理，通过所有功能测试后如果没有当前需要完成计划那就可以进入构造场景系统的任务了」

---

## 一、结论速览

| 项 | 结果 |
|---|---|
| ① 功能测试（G2 回归锁） | **PASS=1450 / FAIL=0 / 套件=31，rc=0** ✅ |
| ② 真机整体运行 | 干净启动，零崩溃、零 Traceback ✅ |
| ③ 连续 10 分钟用户视角监测 | 已完成（300 样本 / 599.2 秒，双通道）✅ |
| ④ 人类逻辑合理性判读 | **8 条判据全 PASS** ✅ |
| ⑤ 新发现缺陷 | **2 条**（F35-1 P2、F35-4 P3） |
| ⑥ 可否进入场景系统 | **可以**（附 3 个待裁定项，见 §六） |

**一句话**：宠物真机连续跑 10 分钟，**移动、动画、方向、速度、状态机、活动节律全部合理**；新发现 2 条轻微缺陷（都与「动画名 / 动画回退」有关），不影响移动系统正确性。

---

## 二、功能测试（G2 基线）

```
suite            exit   PASS   FAIL   比对       说明
合计：PASS=1450 FAIL=0  套件=31
rc= 0
```

31 个套件、1450 条断言全绿，零失败、零比对漂移。**这是"通过所有功能测试"的第一半证据。**

---

## 三、监测方法（双通道）

第一次只用窗口矩形，得出「89 秒零移动」的**错误结论** —— 实际是撞上 27 秒休息期。**只看几何看不到 `is_moving` / `idle_timer`**，无法区分"设计内静止"与"缺陷"。⇒ 补第二条通道。

| 通道 | 内容 | 脚本 | 落盘 |
|---|---|---|---|
| A 窗口几何 | 每 2s 枚举宠物窗口真实 rect | `monitor_10min.py` | `10min_窗口采样.jsonl` |
| B 内部状态 | 每 2s 读 `is_moving`/`idle_timer`/`current_animation`/定时器/自主代理 | `probe_run_10min.py` | `10min_内部状态探针.jsonl` |

---

## 四、合理性判读（8 条判据）

### 判据 1：位于屏幕内 → **PASS**

```
位置范围 x[19,2440] y[20,1553]（屏幕 2560x1600）
真正越界: 0 处
```
> 唯一一处 `x=-37` 经邻域核验：伴 `size 138x94 → 116x80` 的**换帧**（`anim=act`），
> 属 `_anim_anchor_offset` 锚点补偿，**非移动越界**。
> `y` 最大值 1553 落在 1528~1600 的**任务栏带**，因 clamp 用 `screenGeometry`（设计选择），不算越界。

### 判据 2：真在漫游 → **PASS**
```
累计位移 7809 px（>可视区对角线 2981 px）；不同坐标 43 个
```

### 判据 3：动画面向 == 移动方向 → **PASS**
```
不一致 0 处（walk_*/run_* 的方向段与 current_direction 逐条一致）
```

### 判据 4：静止时 walk/run 残留 → **有 2 条轻微缺陷（见 §五）**
```
(a) 清醒残留 3 处 / 10 分钟  → F35-4（P3）
(b) 睡眠残留 8 处 / 10 分钟  → F35-1（P2）
```

### 判据 5：速度 → **PASS**
```
n=24  min=5  max=177  mean=99 px/s（上限约 264）
```

### 判据 6：状态机互斥 → **PASS**
```
跳跃/坠落三类互斥状态并存 0 处
```

### 判据 7：活动分布 → **PASS**
```
sleeping    214 (71%)
walking      50 (17%)
performing   34 (11%)
jumping       2 ( 1%)
移动占比 8%
```
**读作**：走一段 → 停下看一会 → 做个小动作 → 累了睡一段时间。**四态交替，不是永动机也不是死站桩**，符合 Ralsei 温柔害羞的设定。

### 判据 8：睡眠行为 → **PASS**
```
首次入睡 t=173s（max_sleep_idle_duration=300s）
睡眠持续至观测结束（7.1 分钟）
```

### 完整轨迹（可复核，节选）

| 时刻 | 位置 | 状态 | 动画 |
|---|---|---|---|
| t=1.6→9s | (2391,1410)→ | walking | idle / jump_ball / land |
| t=27→49s | →(268,121) | walking → performing | walk_left → look_up |
| t=75→115s | →(30,1500) | walking | walk_left |
| t=131→137s | →(965,1058) | walking | walk_right → walk_left |
| t=173s | (27,32) | **入睡** | idle |
| t=173→599s | (27,32) 不动 | sleeping | idle（偶发 walk_down/walk_right，见 F35-1） |

---

## 五、本轮发现的新缺陷

### F35-1（P2）：`_sleep` 动画名拼错 ⇒「小憩走路」从未生效

**真机告警**：
```
[WARNING] sprite_loader — [anim-miss] 动画名不存在: 'walk_down_sleep' → 回退 'walk_down'
[WARNING] sprite_loader — [anim-miss] 动画名不存在: 'walk_left_sleep' → 回退 'walk_left'
```

**根因（两处叠加）**：
1. `main.py:7638` 写 `f"{base_animation}_{self.current_direction}{animation_suffix}"`，
   `suffix="_sleep"` 时产出 `walk_down_sleep` 等；
   而 `assets/animations.json` 里**真名是 `sleep`（单组）**，不存在任何 `{base}_{dir}_sleep`。
2. `L7631` 读 `is_sleeping_walk` 决定后缀，`L7646` **同一帧之后**才更新它 ⇒ 状态切换那帧用旧值。

**影响**：**「小憩走路」特性从未播过专属帧**。真机上表现为 —— 宠物入睡后**偶尔保持走路姿势的第一帧（静止）**，10 分钟观测 8 次（`t=235/241/247/253/577/583/589/595s`）。画面**仍正确**（回退成功），只是白做一次查找 + 日志告警。

**处置建议**：改名为 `sleep`，并把 `is_sleeping_walk` 的更新移到读它之前。P2，可并入下一轮。

### F35-4（P3）：`play_animation_once` 多数调用未传 `restore_to` ⇒ 静止时残留走路姿势

**根因**：`main.py:8114`
```python
self.next_animation = restore_to or self.current_animation
```
`restore_to` 缺省是「播放前那一个」。若播放前恰是 `walk_*`，**播完一次性动画（`act`/`look_up` 等）后就恢复成走路姿势**，而宠物实际是静止的。

**影响面**：全项目 51 处调用，**仅 8 处传了 `restore_to`**（L1413/3037/3658/3844/3846/3858/3860/7740）。实测 10 分钟出现 3 次（`t=31.3/55.3/61.2`）。

**与既有修复同源**：第 34 轮已修「摔倒爬起来必须显式 `restore_to="idle"`」（L8102-8103 注释写明），但**当时只覆盖了摔倒链路**。

**处置建议**：P3。可给 `play_animation_once` 加"静止态默认回 idle"的兜底，一次性覆盖 40+ 处调用点。

### 排除项（核查后判定非缺陷，留痕以免重查）

| 编号 | 疑似问题 | 核查结论 |
|---|---|---|
| F35-2 | 单实例锁失效（双进程并存） | **正常**。第二个实例被 `QMessageBox` 模态框拦住（无人点击故挂起）。我第一次用 `OpenMutex` 判据错误 |
| F35-3 | `_virtual_screen_rect` clamp 失效 | **撤销**。产品进程内 `IsProcessDPIAware()=True`，`GetSystemMetrics` 与 Qt 一致均为 2560×1600 |

---

## 六、关键方法论发现（本轮最有价值的部分）

### ★ 屏幕坐标跨进程比对，必须先确认 DPI 感知状态一致

本机 **150% 缩放**：

| 调用上下文 | `IsProcessDPIAware()` | `GetSystemMetrics` |
|---|---|---|
| 纯 Python / ctypes 脚本 | **False** | **1707 × 1067**（逻辑像素 = 2560/1.5） |
| 产品进程（QApplication 已建） | **True** | **2560 × 1600**（物理像素） |

`GetSystemMetrics` 的返回值**取决于调用进程的 DPI 感知声明**。我的采集脚本非 DPI 感知 ⇒ 读到 1707；产品是 Qt 应用 ⇒ 读到 2560。

**教训**：拿辅助进程读到的屏幕尺寸去核对产品进程的窗口坐标 ⇒ **必然误报越界**。这正是本项目铁律「**先怀疑核验判据，再怀疑被测物**」的第 5 次实例。

### 本轮共 5 次「判据失真」（全部不是产品问题）

| # | 我的第一版判据 | 真相 |
|---|---|---|
| 1 | 只看窗口矩形 ⇒ "89 秒零移动 = 移动系统坏了" | 撞上 27s 设计内休息期 |
| 2 | `OpenMutex` 成功 ⇒ "单实例锁失效" | API 语义错（须 `CreateMutex`+`GetLastError`） |
| 3 | 拿 win32 值核对 Qt 坐标 ⇒ "29 条越界" | 两个进程坐标系不同 |
| 4 | 宽前缀分类器 ⇒ "96 个动画名缺失" | 绝大多数是变量名/状态名，分类器太宽 |
| 5 | 单次 `GetSystemMetrics` 读数 ⇒ "F35-3 clamp 失效" | 非 DPI 感知进程的读数，不适用于产品 |

**共同模式**：每次都先怀疑产品，实际**五次全是我方工具/判据的问题**。「**能力自评失准比能力不足更危险**」。

---

## 七、证据清单

全部在 `code-quality-audit/第35轮-真机用户视角10分钟监测/`：

**`_evidence/`（原始数据 + 判读）**
- `10min_窗口采样.jsonl` / `10min_内部状态探针.jsonl` —— 10 分钟原始采样（通道 A/B）
- `3min_内部状态探针.jsonl` —— 首轮 3 分钟采样
- `10min_日志增量.txt` / `10min_采集摘要.txt`
- `35_行为合理性核验_最终.txt` —— 3 分钟版 8 判据
- `36_动画名比对v2_精确口径.txt` —— AST 精确口径（79 处字面量，零缺失）
- `36_动画名比对_代码vs资源.txt` —— v1 宽口径（方法论对照，说明"分类器太宽"）
- `37_F35-1复核_拼名全集.txt` —— 拿产品拼名逻辑枚举全集
- `39_/40_完整10分钟判读*.txt` —— 10 分钟判读（含 v2→v3 修正过程）
- `41_F35-3判定_产品进程内读数.txt` —— **F35-3 撤销的决定性证据**

**脚本**：`monitor_10min.py`、`probe_run_3min.py`、`probe_run_10min.py`、`verify_behavior.py`、`verify_10min*.py`、`check_anim_names*.py`、`check_f35_1.py`、`judge_f35_3.py`

---

## 八、下一步

**"通过所有功能测试"已达成**：G2 1450/31 全绿 + 真机 8 判据全 PASS。

按用户指令，可**进入构造场景系统的任务**。进入前待裁定：

1. **场景美术素材来源** —— 第 28 轮起的唯一 P1 阻塞项
2. `pet_interaction.py`（365 行、12988 B，**零外部引用**，AST 已确认）→ 删除 / 接线 / 暂缓（建议**暂缓**）
3. F35-1 / F35-4 的修复归属：随 P3 技术债批量做，还是并入场景系统轮次
