# 代码质量复审报告 · 第九轮（2026-09-15）

本轮不是"审查 + 修 bug"，而是**用户提的四项产品要求**的落地与验证。四项已全部完成并纳入回归基线。

## 一、结论摘要

| # | 用户原话 | 做法 | 落地文件 | 验证 |
|---|---|---|---|---|
| ① | "把那个 AI 整的有些注意力哈，别到时候聊着一个话题呢突然就切换了" | 轻量**话题锚**：把"我们现在在聊什么"显式写进系统提示词；**换题权只归用户** | 新增 `modules/conversation_focus.py`，`main.chat_with_ai` 注入 | 20 项 |
| ② | "对话框在 20s 内不输入会自动消失哦，但一旦鼠标点在输入栏上那就不能消失" | 20s 无输入 → 淡出；**用户正在输入 / 鼠标压在输入栏 / 模型正在思考 → 一律不消失** | `modules/dialogue_ui.py` | 14 项 |
| ③ | "确保那个 10 分钟自动触发一次聊天的那个机制不会打断当前进行的聊天" | 加"正在聊天"闸门（最近 150s 有对话往来即算），自主开口与 AI 行为脑的插话都跳过 | `main._autonomous_speech_allowed`、`ai_driver._say` | 4 项 |
| ④ | "记忆系统就像人一样…选择性记住关键和日常小片段…根据零星片段重构场景…遗忘机制…存肖翰哲(E)，没检测到就存桌面 memory，接上再搬进去并清桌面" | 三层拟人记忆（片段/关键/日摘要）+ 联想召回 + 遗忘曲线 + 复习强化 + 位置决策与安全搬运 | 新增 `modules/memory_store.py`，重写 `modules/memory_system.py` 的记忆层 | 51 项 |

**验证合计**：第九轮两套自检 **105 PASS / 0 FAIL**；G2 一键回归 **13 套件 / 360 PASS / 0 FAIL，全部 IDENTICAL**；真机探测确认记忆落到 `E:\RalseiMemory\memory.json`。

## 二、① 对话注意力（不再聊着聊着突然换题）

**病因**：`chat_with_ai` 之前只把"最近 6 轮原文"丢给模型，**没有"我们在聊什么"这个念头**。模型每轮都在重新判断，一句里出现别的东西就可能顺着跑偏。

**做法**：`ConversationFocus` —— 刻意做成轻量、可解释、**不需要模型**的跟踪器：

- 词法：中文 2/3 字滑窗 + 停用词；再剪掉"滑窗碎片"（首尾是功能字，或内部夹着强功能字的一律丢）。
- 归属判定：新话与当前话题做关键词**覆盖度**比较；`这个/它/继续/然后/刚才…` 这类接力标记直接判延续。
- **换题必须由用户发起**：只有用户自己带来一组新关键词才换题；Ralsei 自己说的话永远不会换题。
- 悬置事项：Ralsei 问了问题而用户还没答 → 下一轮提示里带一句"你刚才问过……还没回答，别重复原话"。
- 唯一出口 `brief()` 返回一段中文，被拼进系统提示词；无话题时返回空串。

**取舍**：没用 LLM 做话题判定（每轮多一次推理、还不可解释）；也没用分词器（避免引入 jieba 依赖）。阈值 `NEW_TOPIC_COVERAGE=0.34` 是经验值，集中在类常量里便于调。

**顺带修掉一个真问题**：话题标签原先取 `keywords[0]`，"今天想聊聊塞尔达这游戏的剧情"会得到标签「今天」。改为"取最长且最早的关键词"→ 得到「塞尔达」。自检 A18/A19/A20 就是为这条加的。

## 三、② 对话框 20 秒自动消失

- `AUTO_HIDE_MS = 20000`，单次触发、到点复查（被拦则 1 秒后再查）。
- **三种"有动静"一律不隐藏**：输入框有焦点或有内容 / 鼠标压在输入栏上 / 模型正在思考。
- 鼠标判断用**光标全局坐标**而不是 `underMouse()`：后者在鼠标悬停于输入栏的**子控件**（发送按钮、输入栏边缘）时为 False，会漏判。`_mouse_on_input(global_pos=None)` 留了坐标注入点，便于自检。
- 打字机每完成一句、`show_dialogue`、点一下对话框，都会**重新起一次 20 秒**——否则"模型想 8 秒 + 打字 5 秒"会吃掉整个窗口，回复刚到就秒消失。
- 鼠标按下只重置隐藏倒计时，**不动"最后活动时间"**：后者表示"真的在聊天"，是 ③ 的判据，两者不能混。

## 四、③ 10 分钟自主开口不打断聊天

`has_active_conversation(window=None)`：模型在思考 / 正在打字 / 用户在输入 / 最近 `ACTIVE_CONVERSATION_SECONDS=150` 秒内有对话往来 → 算"正在聊天"。

两个入口都加了闸门：
- `main._autonomous_speech_allowed()`：正在聊天 → 本次不插话（原来的 10 分钟节流保留）。
- `ai_driver._say()`：AI 行为脑的自主说话在聊天中跳过，**但动作不受影响**（走路/看窗口等仍然照做）。

## 五、④ 拟人记忆系统

### 5.1 三层结构（选择性记忆）

| 层 | 内容 | 寿命 |
|---|---|---|
| 片段 `fragments` | 日常小片段：每一轮对话、行为、经验 | 会淡忘，上限 240 条 |
| 关键记忆 `keys` | 升级/成就/重要日子/主人的重要事 | **永不主动遗忘**，上限 300 条（兜底） |
| 日摘要 `digests` | 那天的"印象"：聊过什么 + 最难忘的一句 | 保留 60 天 |

外加**联想图** `assoc`（词 → 共现邻词），是"⭐由零星片段重构场景"的物理基础。

### 5.2 显著性（什么值得记）

`_salience()`：类型基础分 + **主人说的比自己说的重**（+0.15 / −0.10）+ 带情绪的经历更难忘（emoji 强度 +0.12~0.18）+ 关键词多/话长信息量大 − 太短没信息（−0.12）。

### 5.3 遗忘（防爆 + 不失忆）

- **艾宾浩斯式衰减**：`strength *= exp(-Δt/τ)`，`τ = 86400·(2 + 6·显著性)` —— 越重要忘得越慢。
- **日摘要兜底**：超过 2 天的日子先压成一句"那天的印象"，**细节忘了、梗概留下**（这是人的样子）。
- **三条保护**：关键记忆不删；最近 24 小时的片段不压不删；删除前先把那天的印象写进摘要。
- **硬上限**：仍超 240 条时按"强度 × 显著性"留最强的。
- 另修 B4：`learning_rate` / `memory_strength` 原先不落盘，重启即丢 —— 现已持久化。

### 5.4 召回与场景重构

两步（模仿人脑）：① 倒排索引直接命中（**不扫全表**，`词 → 片段下标`）；② 沿共现图**扩散**，所以"你没提的事"也能被带出来。

- 被想起来的片段会**强化**（`strength` 向 1 靠拢）——复习效应，这是"不失忆"的关键。
- `recall_text(cue)` → 一段中文「【零星的回忆】…」，拼进系统提示词；**无命中就返回空串**，绝不硬编。
- `reconstruct_scene(cue)` → 把同一天的片段聚起来拼成叙述（"那大概是 X 的傍晚，我们正聊着……我记得你说过「…」"）。

### 5.5 存储位置（E 盘 / 桌面兜底）

`modules/memory_store.py` 只做"放哪儿 + 什么时候搬运"：

- 优先级：`RALSEI_MEMORY_DIR` 环境变量 → **卷标含"肖翰哲"的盘**下的 `RalseiMemory\` → `<桌面>\memory\`。
- 设备回来时（`update()` 里每次检查）把桌面兜底的记忆**搬进设备并清掉桌面副本**。
- **搬运的安全约定**（这是本模块唯一会删东西的地方）：只碰 `memory.json` 与它的半成品 `.tmp`；**先复制成功 + 校验通过，再删源**，任何一步失败就保留源文件；两边都有内容时保留 `saved_at` 较新的那份，**被淘汰的那份挪到 `memory.old.json` 留档**；只在目录确实空了才删空目录，绝不递归删。
- 升级不失忆：新位置没有记忆时，从旧版 `ralsei_pet/memory.json` 继承（旧文件不删，它不在桌面）。

**真机探测**（`probe_memory_location.py`，见 `_evidence/probe_location.txt`）：

```
E:\  label=肖翰哲            → device_dir = E:\RalseiMemory   device_present = true
resolve → E:\RalseiMemory\memory.json
fallback → C:\Users\23002\Desktop\memory
```

## 六、验证与回归

```
第九轮·注意力（A/B/C 三组）        54 PASS / 0 FAIL
第九轮·记忆（S/M/F/R/G/P/I 七组）   51 PASS / 0 FAIL
G2 一键回归                        13 套件 / 360 PASS / 0 FAIL / 全部 IDENTICAL
pyflakes                            memory_store / memory_system 零告警
```

产物：
- `code-quality-audit/第九轮/verify_round9_focus.py`、`verify_round9_memory.py`、`probe_memory_location.py`
- 证据：`code-quality-audit/第九轮/_evidence/`（`verify9_all.txt`、`g2_round9_final2.txt`、`probe_location.txt`、`pyflakes9.txt`）

**自检抓到的一个真 bug**：`memory_store.migrate_from_fallback` 在"设备版更新"分支里，把**胜者**抄进了 `memory.old.json`（等于白抄一遍还覆盖掉真正的旧数据）。自检 M4 抓到，已改为留档**落选者**。

**G2 的两处有意 DIFF（已用 `--update` 重建基线）**：
1. `round5_smoke`：模块数 22 → 24（新增 `conversation_focus.py`、`memory_store.py`），全部导入 OK；
2. `round5_verify`：`save_memory` 落盘键增多（新增 schema/记忆层/学习率等）——这正是 B4 修复的预期结果。

## 七、已知限制 / 未做

- **"不失忆"的边界**：低显著度、久未提起的**细节**会被忘掉（这是设计目标）；留下的是日摘要与关键记忆。若用户希望某类内容永不遗忘，把它走 `remember_key()` 即可（目前 `level_up`/`achievement_unlocked`/`evolution`/`completed_task` 等已在免遗忘名单）。
- **联想图只做 1 跳扩散**（多跳会显著拉高噪声；当前 `ASSOC_PER_KEY=12`）。
- 记忆的**语义**能力依赖关键词共现，没有向量检索 —— 换取的是"零额外依赖、可解释、可离线"。
- 落盘有 2 秒节流（`SAVE_THROTTLE`），极端断电可能丢最后 2 秒的片段（关键记忆走强制写）。
- 真机 GUI 只做了"存储位置 + 导入启动"级别的验证；20s 自动隐藏的**视觉**效果未在真机上肉眼确认（窗口是透明非置顶 FramelessWindow，易被遮挡）。

## 八、变更清单

新增：
- `ralsei_pet/modules/conversation_focus.py`
- `ralsei_pet/modules/memory_store.py`

修改：
- `ralsei_pet/modules/dialogue_ui.py`（话题锚接入、自动隐藏重做、`_note_memory`）
- `ralsei_pet/modules/memory_system.py`（拟人记忆层、存储位置、B4/B8 修复、`reset_all`/`autosave`）
- `ralsei_pet/modules/ai_driver.py`（聊天中不插话、行为决策带焦点简报）
- `ralsei_pet/src/main.py`（自主开口闸门、注入焦点与回忆、退出清理走统一入口）
- `code-quality-audit/regress/run_all.py`（新增两套件）、`baseline.json`（重建）
