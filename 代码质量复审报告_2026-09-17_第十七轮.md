# 代码质量复审报告 · 第十七轮（2026-09-17）

**主题：「建楼」缺口计划 · 批次 A —— 把"下落路径"修对（G2 判据 + G1 生气动画时长）**

前置文档：`建楼要求复检与缺口计划_2026-09-17.md`（第十六轮，只读复检 + 4 批次计划）
本轮执行：**批次 A（G2 + G1 一起修）**，即计划里排在最前面的那两条。

---

## 一、结论先行

| 项 | 结果 |
|---|---|
| 批次 A 缺口 | **2/2 修完**（G2 下落判据、G1 生气动画时长） |
| 本轮自检套件 | `code-quality-audit/第十七轮/verify_round17_build_fall.py` —— **18 项 PASS / 0 FAIL** |
| G2 全量回归 | **20 套件 / 746 PASS / 0 FAIL / 全 IDENTICAL**（两遍：`--update` 固化后复跑复验） |
| 有意的输出变更 | **仅 1 处**：`round14_move` 的 G1 断言**改名**（"时长"→"**派生**时长"），行为断言未变 |
| 其余 19 套件 | **逐字节一致**（含 `round8_fling` / `round6_verify` / `round15_cleanup` 这些真跑坠落路径的） |
| 证据编码 | `check_evidence_encoding.py`：**0 损坏 / 0 告警** |

一句话：**复检时发现的两条"实际不成立"的要求，现在行为级成立了**，而且没有把任何别的行为带偏。

---

## 二、G2：下落判据从"新楼层是不是窗口"改成**层高比较**

### 根因

`check_window_movement` 触发坠落的那道门写的是：

```python
if old_floor.get('type') == 'window' and new_floor.get('type') != 'window':
```

也就是说 **只有"下面变成桌面"才会掉**。而要求原文第 30 行给的例子恰恰是另一种：

> "如果我把窗口B（记事本）关掉，3楼消失，宠物如果原来在上面，就会掉到2楼（浏览器）上。"

记事本关掉后**下方还有浏览器** → `get_current_floor` 立刻把楼层换成浏览器（**仍是窗口**）
→ 整段分支被跳过：**不坠落、不播动画、不生气**，宠物直接"瞬移"到了浏览器那一层。

第十五轮接活的 `is_floor_valid`（用来区分"自己走出去"与"用户抽走楼板"）在这条路上
**根本没被问到**（调用计数 0）——判据写对了，但门条件把它挡在外面。

### 改法

把判据换成**层高比较**（`platform_height`），一次覆盖四种情形：

| 情形 | 层高 | 行为 | 起因 |
|---|---|---|---|
| 自己走出窗口边缘，下面是桌面 | 5 → 0 | 摔（常规动画） | 无 |
| 用户关掉窗口，下面是桌面 | 5 → 0 | 摔（**生气** ≥5s） | `floor_removed` |
| 用户关掉窗口，**下面还有窗口** | 10 → 5 | 摔（**生气** ≥5s） | `floor_removed` ← **本次修复** |
| 被更高的新窗口盖住 | 5 → 10 | **不摔**，直接站新板 | —（要求 ⑪ 保持不变） |

起因为分流仍在 `is_floor_valid(old_floor)`：**窗口还在 ⇒ 自己走出去**（常规）；
**窗口没了 ⇒ 用户抽走楼板**（生气）。落点由 `handle_gravity_fall` 里的
`get_drop_destination` 按可见区域结算（第十三/十四轮已锁，本轮没动）。

> 为什么不用"离边缘 ≤N px"：楼层检查是 **1 秒节拍**，宠物一秒能走几百像素，距离阈值必漏判。
> "窗口在不在"与节拍无关，稳。

---

## 三、G1："生气动画至少 5s / 3s"从**三处叠加失效**修成真的成立

### 根因（三个原因叠加，缺一不可）

| # | 原因 | 后果 |
|---|---|---|
| a | 时长只写在 `self.max_fall_duration`，而它**全项目只有 `handle_fall` 会读**；`start_falling()` 偏偏把 `is_falling` 置 False（坠落走 `handle_gravity_fall`） | 那句 `5.0` 是**死参数**，`handle_fall` 根本不跑 |
| b | 即便跑，落地 `trigger_splat()` 又把 `max_fall_duration` **覆盖成 3.0** | 5.0 又被抹掉 |
| c | `handle_fall` 的 splat 阶段时长是**硬编码 1.0s**；且落地时 `trigger_splat()` 恒切普通 `splat`，把坠落途中刚播上的 `fall_mad` **在落地那一瞬顶掉** | 生气素材 `splat_mad` **全项目零调用** |

三条叠起来，用户实际看到的是：**掉下去的途中闪一下生气，落地立刻被普通摔扁取代，1 秒后开始揉头。**

### 改法：由起因 `_fall_reason` **纯派生**，不再另存一份状态

新增两个**模块级**辅助函数（文件头，紧挨 `_FALL_STATE_ATTRS`）：

```python
FALL_MAD_REASONS = ('floor_removed', 'window_move')
FALL_SPLAT_HOLD = {'floor_removed': 5.0, 'window_move': 3.0}   # 要求第36/37行
FALL_SPLAT_HOLD_DEFAULT = 1.0                                   # 与改前逐字节一致
FALL_MAD_ANIMATION = 'splat_mad'

def _fall_splat_hold(pet):      # 时长：由 pet._fall_reason 派生
def _splat_animation_name(pet): # 素材：用户造成的摔落 → splat_mad，否则 splat
```

四个调用点全部改成"只读"：

| 位置 | 改动 |
|---|---|
| `start_falling` | 删掉 `max_fall_duration = 5.0/3.0`（死参数），只保留 `self._fall_reason = reason` |
| `start_fall` | 同上，只保留起因（原先连 `_fall_reason` 都没有 → 第37行那条路读不到起因） |
| `trigger_splat` | 素材改调 `_splat_animation_name(self)`；`max_fall_duration` 只服务"晕乎时长"旧口径 |
| `handle_fall` | splat 阶段阈值 `1.0` → **`_fall_splat_hold(self)`**；落地素材改调 `_splat_animation_name(self)` |
| `mouseReleaseEvent`（甩飞） | **显式复位 `_fall_reason = None`**（否则上一次关窗的起因会让这次甩飞也栽进生气版并待满 5s） |

改后的时间线（关窗为例）：**天上掉（fall_mad）→ 落地生气 `splat_mad` 停满 5s → 晕乎 1s → 爬起来 1.5s**。
自查：无起因（自己掉下去 / 被甩飞）仍是 **1.0s**，与改前**逐帧一致**，没有把普通摔倒一起拉长。

### 为什么放**模块级函数**而不是类方法（本轮踩过的坑）

第一版写成了 `RalseiPet._splat_animation_name(self)`。G2 一跑：
`round6_verify`（39 项断言掉到 20）与 `round8_fling` **当场 `AttributeError` 崩在半路** ——
这些历史套件用**轻量桩**（`SimpleNamespace` / 手写 stub）驱动 `RalseiPet.handle_fall(stub, ...)`，
桩里没有这个新方法。改成模块级函数后，桩不需要补任何东西（函数由 `main` 的全局命名空间解析），
两个套件**一字不差地恢复 IDENTICAL**。

> 这与项目既有约定一致：`_FALL_VELOCITY_ATTRS` / `_FALL_STATE_ATTRS` 当初放模块级，
> 注释里写的就是"便于离屏测试用轻量 stub 直接调用"。

### 顺带消掉一份冗余状态

第一版还写了 `self._splat_hold` 实例属性（起因在哪里知道、时长就在哪里写）。
定稿改成**纯派生**：唯一真源是 `_fall_reason`，`_fall_splat_hold(pet)` 现算。
理由是项目头号铁律"**消除双真源**"（`current_floor` 权威 / `current_window` 派生缓存那套），
属性一旦多存一份就有脱同步的可能。

---

## 四、验证

### 4.1 本轮自检套件（18/18）

`code-quality-audit/第十七轮/verify_round17_build_fall.py`，三组：

- **A G2 层高判据**：A1 关窗后下方还有窗口 → `start_falling(reason='floor_removed')`（**复检原例**）
  / A2 关窗后落桌面 / A3 自己走出边缘不带起因 / A4 被更高新窗盖住 → 不摔（要求 ⑪ 未破）
  / A5 源码级：`new_floor.get('type')` 已消失 / A5b 层高比较已就位。
- **B G1 生气动画**：B1 落地切 `splat_mad` / B1b 非用户行为仍切 `splat` / B2 时长由起因派生
  / **B3 行为级时间推进**：真跑 `trigger_splat` + `handle_fall`，量出"进入晕乎的时刻"
  = **5.0s**（关窗）/ **3.0s**（挪楼板）/ **1.0s**（无起因）/ B4 全程没被普通 splat 顶掉
  / B5 死参数已清 / B5b 无第二份真源。
- **C 单一入口 / 未误伤**：C1 素材判定只有一个入口且两处都调它 / C2 甩飞显式复位起因 / C3 素材确实在库里。

> B3 是本轮最关键的断言：**它不是"检查某句赋值"，而是把阶段机真的推进 100 帧、量出停留时间。**
> 复检时的问题恰恰是"断言了写变量、没断言行为"，所以这里必须用行为量。

### 4.2 G2 全量回归（20 套件 / 746 PASS / 0 FAIL / 全 IDENTICAL）

- 新增套件 `round17_build_fall`（18 项）已挂进 `regress/run_all.py`，基线 `--update` 后**复跑一遍复验**，
  两次都 `EXIT=0`。
- 唯一有意变更：`round14_move` 的 G1 断言**改名**
  （`且时长 ≥5s` → `且派生时长 ≥5s`）。
  原来那条断言写的是 `_p_user.max_fall_duration >= 5.0` —— 而那条赋值正是**死参数**：
  **旧断言当时是 PASS，却完全没锁住"至少 5s"这个要求**。这类"断言写变量"的断言，本轮一并纠正。
- 第十六轮那份只读复检套件**不入 G2**（它断言的是修复**前**的缺陷状态），已在它的文件头
  加注"历史快照：修复后再跑，那几条 GAP 断言必然翻转为 FAIL，这是设计如此、不是回归"。

### 4.3 真机肉眼确认（**只能用户做**）

本轮是离屏行为级验证 + 真窗口无关的合成窗口表；以下三条建议在真机上各看一眼：

1. 站窗口上 → 关掉那个窗口，**下方还有别的窗口**时：应当**掉下去并生气 ≥5s**（改前是"瞬移"到下面那层）。
2. 挪动它所在的窗口（>30px）：应当**生气版摔倒 ≥3s**（改前落地就变普通摔扁）。
3. 走路踩空、被甩飞：**应当还是常规摔**，时长没变长。

---

## 五、有意**没有**动的东西（批次 B/C/D 仍是待办）

| 批次 | 内容 | 状态 |
|---|---|---|
| **B** | G3「走路无层高闸门」：站桌面上水平走进窗口的可见区域会被**直接提升**到该层，不跳不摔 | **等用户拍板**（收紧 / 补过渡动作 / 维持现状），未动 |
| C | G5 `climb_to_top_window()` 仍走裸窗口路径 | 未动（第十五轮遗留，建议收编） |
| D | 三条"有意偏差"（>400px 不跟随改失足 / `MIN_FLOOR_VISIBLE_AREA=1600px²` / "立刻"=1 秒节拍） | 只登记，不改 |

另外，**改动范围严格限定在"下落路径"**：跳跃几何、`floor_manager`、z 序、可见区域判定
本轮一行没碰 —— G2 里那 19 个套件逐字节一致就是证据。

---

## 六、教训

1. **"判据写对了"不等于"判据被问到"**：`is_floor_valid` 第十五轮就接活了、逻辑也没错，
   但它所在的分支被上游门条件（`new_floor.type != 'window'`）挡住 → 调用计数 0。
   查这类问题最快的办法是**数调用**，不是读代码。
2. **同一份状态别存两处**：第一版把时长写成 `_splat_hold` 属性 + `max_fall_duration`，
   几乎立刻出现"该读哪个"的混乱。定稿改成由 `_fall_reason` 纯派生，问题消失。
3. **给产品代码加"实例方法"时要想着轻量桩**：`handle_fall` 被多个历史套件用 `SimpleNamespace` 驱动，
   加一个方法就会崩掉三个套件。**模块级函数是这类"被判据调用的纯函数"的正确落点**（第 3 次踩同类坑：
   第十五轮 `is_floor_valid` 补桩、第十六轮 `method_lit` vs `code_only`）。
4. **断言要断行为，不要断赋值**：round14 那条 `max_fall_duration >= 5.0` 静静绿了一年多，
   而要求从未成立。**能推进状态机量出来的，就别只检查一行赋值。**

---

## 七、下一步

- 批次 A 闭环。**用户原话提到的"原计划"= H4/H5 上帝类拆分**（`架构改造排期方案_H4-H5_2026-09-13.md`），
  本轮做完这批建楼缺口后回到它。
- 批次 B（G3 层高闸门）需要用户先拍板方向，不阻塞 H4/H5 起步。
