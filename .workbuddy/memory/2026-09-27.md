# 2026-09-27 工作日志（第51轮：移动系统体检与修复 · 托盘常驻 · 可见行走前置勘察）

> 本会话从 09-26 晚跨到 09-27 凌晨，前半段见 `2026-09-26.md`。本文件记第51轮全貌。

## 一、用户口径（逐字）
- 主任务：「继续手头的活，然后，你先开始运行程序，自己动手测一下到底能不能从桌面到
  其他地方（所有room，当然，不包括第3章）还有ralsei是否可以寻路，是否穿模，是否出现
  room显示不稳定等。测完并修好bug后添加一个功能……」
- 两次纠偏：「我让你测试是指**实测**，实际上的操控 ralsei 去各个场景（**也就是我也能看到
  的那种**）而不是你在后台自己跑」
- 本段新增：「他现在光有那个移动的动画，没有实际移动，这样吧，你自己到时候也整体查一下
  他的移动系统这类的，**用屏幕自动化来看，而不是后台日志**，对了，**测完了记得关掉**，
  还有，**以后预留一个后台运行的图标在托盘里，方便我关**」

## 二、移动系统「光有动画没移动」——已定位并修复（真机实测）
- 观测通道：`EnumWindows`+`GetWindowRect` 每 0.25s 取真实屏幕矩形；只认标题 `Ralsei Pet`；
  连续 3 次白/绿/粉像素 ≥50 才开采（**判据自证**，第一版抓到 138×94 非宠物窗口，180s 数据作废）。
- 修复前 240s：`T_moving_pct`=21.2%、`T_still_silent`=167.8s(70%)、`walk_px_median`=**49.5px**
  （Ralsei 自身 38px 宽 ⇒ 一次只挪 1.3 个身位）、`longest_rest`=33.5s、`total_travel`=6326px；
  「原地播动画」仅 **0.8%** ⇒ **否定**"动画与位移不同步"。
- 真因三条叠加：① 单次目标距离过小（sad/tired 仅 `randint(30,100)`）② 休息过长且会再排一轮
  ③ `IDLE_LOOP_MIN_SECONDS = 180.0` 使静止期画面**完全冻结**（此项是用户明确要求，未动）。
- 修复（`ralsei_pet/src/main.py` 3 处）：移动距离上调 5 档（sad/tired 30~100→120~280、
  shy 50~150→150~340、curious 80~250→160~380、excited 100~300→180~420、其他 80~250→150~360）；
  移动概率 0.7/0.3/0.6/0.2/0.4→0.85/0.5/0.7/0.35/0.55；休息时长 (3,8)…(10,25)→(2,5)…(5,12)；
  首次移动起始概率 0.6/0.2/0.3→0.75/0.45/0.5。
- 修复后同口径 240s：`T_moving_pct` 21.2%→**39.2%**、`walk_px_median` 49.5→**146.1px**（≈3.8 身位）、
  `longest_rest` 33.5→**19.1s**、`total_travel` 6326→**10823px**、`T_still_silent` 167.8→130.2s。
- 复检 `_tools/recheck51.py`：**PASS=15 / FAIL=0**（含回退守卫 `IDLE_LOOP_MIN_SECONDS = 180.0`）。

## 三、托盘常驻图标（用户要求③）
- 原 bug：`tray.show()` 只在 `_hide_ralsei()` 里调用 ⇒ **没隐藏过的用户整个会话看不到托盘图标**。
- 现：建好即常驻 + `setToolTip` + 右键菜单「显示/隐藏/——/退出」；「退出」走
  `cleanup_on_exit()→tray.hide()→QApplication.quit()`（原直接 quit 会留单实例互斥残留）；
  「显示」不再顺手 `tray.hide()`；首次启动弹一次气泡（落 `ui.tray_hint_shown`）。
- 取证：`_evidence/motion_audit51_petwin_ready.png` 抓到气泡文字。
- 测试进程：每轮采集脚本末尾 `kill_tree`，事后独立复核 **残留=0**。

## 四、★★ 重大附带发现：E 盘既有文件在**文件系统层**不可写（未修，等用户处置）
- 产品真机日志（非构造）：`config_manager [WinError 5] config.json.tmp -> config.json`；
  `memory_system [WinError 5] ×3`；`[Errno 13] 'E:\RalseiMemory\logs\ralsei_pet.log'`。
- `E:\RalseiMemory\config.json` 的 mtime 停在 **2026-09-19 10:32** ⇒ **一周多以来所有配置与
  记忆写入全部失败**（Ralsei 记不住新东西；运行时 `api.model` 仍是 `ralsei:v3`，改不回 v4）。
- 取证矩阵：新建文件/子目录 OK、新文件写/删 OK、读 OK；`r+`/`a`/`w`/`rb+` 全 PermissionError；
  `rename`/`remove`/`replace` 全 WinError 5；`GetFileAttributesW` READONLY=**0**（非只读属性）；
  `os.access(W_OK)`=True（**Windows 上的假判据**）；同卷 `E:\Download\_tmp` 老文件同样不可写。
- ⇒ 定性 **exFAT 卷级问题**，与 vault/权限/属性/进程占用均无关。修法：`chkdsk E: /f` → 无效则
  迁 vault 到 C 盘（属契约变更）。**我不擅自动盘，等用户执行。**

## 五、★ 可见行走前置勘察（本轮新做，只读探针，不碰产品代码）
1. **门坐标存在**：在场景自己的 `objects` 里 —— `src` 前缀 `obj_door<L>` = 门本身、
   `obj_marker<L>` = 从那条边进来时的落点，`pos` = 房间局部像素坐标。
   锚点自检 PASS：`ch1:45` 的 `obj_doorA(246,192)` → `ch1.castle_town.castle_front` 的
   `obj_markerA(500,884)`；负对照 `desktop` 两表皆空 PASS。
2. **覆盖率**（`_room_graph.json` 782 条边逐条核对）：门坐标 758/782(96.9%)、落点 689/782(88.1%)、
   **两端都齐（可逐门执行）689/782 = 88.1%**。分章：ch1 232/238(97.5%)、ch2 251/323(77.7%)、
   ch3 74/79、ch4 65/72、ch5 67/70。未命中模式单一 = `obj_doorW/X/D`、`obj_doorA_musfade` 等
   **非 ±1/±2 特殊门**（原作不靠 marker 定位）⇒ **不必重跑 UTMT**。
3. **桌面那段没有几何，只能"切"不能"走"**：`desktop.json` objects=[] bg=null；
   `_room_geometry.json` **无 desktop 条目**（1251 个房间条目全是作品内房间）；`_routes.json` 里
   `desktop` 只在 `_fallback`。⇒ 正确形态：桌面→`ch1.room_town_north` 是**转场**，之后每段才是走。
4. **坐标换算唯一真源**：`main.py:2155 _pet_target_rect()` 的逆 ——
   `窗口中心 x = 世界 x * 屏幕宽 / 房间宽`；量级 虚拟屏2560/房间640 = **4 倍**。
5. **判据自纠（留痕）**：第一版按「同房间内 door ⊇ marker」统计得 **356 个"字母不配对"**，
   是**判据过窄的误报**（样本 `ch1.cc_entrance` 只有 doorA+markerB 其实正常）⇒ 改**跨房间配对**。

## 六、回归与提交
- 全量 `run_all.py`：**PASS=2376 / FAIL=0，47 套件全部 IDENTICAL**（`walk_round47` 37/0、
  `round34_movement` 36/0）。
- 提交两次（均双向核验 PASS）：
  - `7f37d0d` 第51轮：移动系统真机体检与修复 + 托盘常驻图标 + E盘写入故障定性（67 files, +23745/-90）
  - `cf96ef0` 第51轮（续）：可见行走前置勘察 —— 门坐标普查 + 跨房间配对验证（7 files, +13496/-1）
- 已还原被测试误改的 `ralsei_pet/config.json`（备份在 `E:\Download\_tmp\config_json_repo_backup51.json`）。

## 七、下一步（"看得见的行走"，未做，等排期）
1. **实现"看得见的行走"3 步**：行走驱动（`plan_route_to().steps[].door` → 门坐标 →
   `scene_walk.plan_walk()` → 沿路径推窗口）／逐门执行（到达→`switch`→取 `obj_marker<L>` 续走）／
   托盘入口（「带 Ralsei 去…」+ 目的地列表）。
2. 第51轮主任务其余项：想象功能、灵魂光标、room 顺序/触发点对比、光世界套球+脱球文案、
   右键融入操控 Kris、背包 C 键。

---

# 第52轮（同日，用户新需求 10 项）

## 八、用户原话（逐字）
> 「别整这种对话框，就一轮一轮的而不是一次性全放出来，，你现在对话框就像是原作把一章节的
> 全部对话都放了进来，而事实上原作一次只放一轮对话，所以你要贴合原作哦，还有，预留出来输入框，
> 还有，他待机只会在静止超过10分钟后触发，对了改一下待机时候的动画，用那个额，我忘了在第几章了，
> 反正是一个电梯里，主角团都以放松些的姿态坐着的那组动画，我不知道在不在素材里，所以你也可以去
> 原作文件里找一下，还有把他内置的对话去掉！！！！并且去掉那个与文件夹交互的功能吧，感觉过于
> 鸡肋了，还有，他捉迷藏只是用户提出来才能玩而且必须是在桌面上。其次，他这一直站着不动是什么情况，
> 像贴图似的，而且就算静止不动那也要来屏幕底下那个快捷栏上面待着吧，就偶尔聊几句天这样的感觉，
> 其次，在晚上的时候他会自己回到自己的房间里除非有事（冒险，和我聊天等），要他就会在晚上11点左右
> （也就是上下10分钟）的时候回去睡觉，还有，我现在都没看到过7B模型启用，就是看不出哪句是他自己说的，
> 记住，聊天系统全权由7B接管，别放内置对话了，太木讷了」

## 九、拆解与状态

| # | 需求 | 状态 |
|---|---|---|
| 1 | 对话框**一轮一轮**显示（贴合原作） | 待做 |
| 2 | 对话框**预留输入框** | 待做 |
| 3 | 待机改为**静止 10 分钟**触发 | ✅ |
| 4 | 待机动画换**电梯里放松坐着**那组 | 待做（需查素材） |
| 5 | **去掉全部内置对话** | ⏳ 部分（5 模块 40+ 处，已清 2 处） |
| 6 | **去掉「与文件夹交互」功能** | ✅ |
| 7 | 捉迷藏：仅用户提出才玩 + 必须在桌面 | 待做 |
| 8 | 静止时**待任务栏上方** + 偶尔聊几句（治"像贴图"） | 待做 |
| 9 | 晚 23:00±10min **回房睡觉** | 待做 |
| 10 | **7B 真正接管聊天** | ✅ 根因已修 |

## 十、★★ 第 10 项根因（本轮最重要发现，直接回答"看不出 7B 启用"）

`ollama show` 实测：

| 模型 | 底座 | 参数量 | 大小 |
|---|---|---|---|
| `ralsei:v3` | qwen3 | **4.0B** | 2.5 GB |
| `ralsei:v4` | qwen2 | **7.6B** | 4.7 GB |

而运行时 `E:\RalseiMemory\config.json` 的 `api.model` = **`ralsei:v3`**（模板是 `v4`）。
⇒ **用户一直在跑 4B，不是 7B**，所以"看不出 7B 启用"是真的。

**为什么改不回 v4**：E 盘既有文件不可写（见第六节）⇒ 用户在设置里改成 v4 也**存不下来**。

**修法**（`config_manager.py` 的 `_load_config`，复用已有的 `needs_migration` 机制）：
只把**我们自己的旧默认值**（`ralsei:v1/v2/v3`、`local-model`）升到 `ralsei:v4`，
并把配套的 `base_url` 从 `:8000` 同步到 `:11434`；
**绝不碰**用户显式选过的任何其它模型。
验证：`_tools` 风格的四组夹具 —— 正控制 ×2 PASS、**负控制（用户显式选的
`qwen2.5:7b-instruct-q4_K_M` / `gpt-4o-mini`）不被覆盖** PASS。
写盘失败不影响本次运行（`merged_config` 是返回值）。

## 十一、已做 4 项的落点与证据

1. **待机 10 分钟**：`main.py` `IDLE_LOOP_MIN_SECONDS = 180.0` → **`600.0`**
   （用户改了口径，原"3 分钟"是旧要求）。
2. **7B 接管**：见第五节。
3. **删内置对话（video）**：`video_controller._react_to_video()` 原来随机抽 8 条罐头
   （"haha！这个好好笑！"…）直接 `add_dialogue` —— 正是该文件 `#15 对话全 AI 接管`
   TODO 点名的缺陷。现改为**只上报事件**（`note_event`）+ 记情绪，说什么由 AI 定。
4. **删「与文件夹交互」**：`main.py` `check_nearby_desktop_elements()` 里
   `random.random() < 0.25` 调 `interact_with_folder/file()` 那段**整个删除**；
   两个方法体换成空壳（保留签名防 AttributeError）。
   连带把同处的**浏览器硬编码台词**删掉，把"按扩展名给 16 条助手腔提示"那段
   用新类属性 `SHOW_FILE_HELP_HINTS = False` **停用**（没硬删，理由见下）。

## 十二、本轮踩的两个坑（务必记住）

1. **删 if 分支留下悬空 `else:` ⇒ `SyntaxError`**。
   我删 `if 'browser' in file_name: ...` 时**没读到块尾**，把配对的 `else:` 留下了
   （`main.py:5031`）。是 `ast.parse` 自检抓到的。
   ⇒ 教训：**删 if/else 结构前必须读到配对的 else/elif**；结构类改动优先用
   **开关**（本次最终就是加 `SHOW_FILE_HELP_HINTS = False` 而不是硬删）。
2. **判据过宽 ⇒ 误报**。我用 `'haha！这个好好笑！' in 源码` 断言"罐头表已删"，
   结果匹配到**我自己新写的 docstring 里的举例**，报 FAIL。
   ⇒ 改用 **AST 行为判据**（取方法体源码区间，检查里面有没有**真的调用** `dialogue_ui`）。
   这再次印证记忆 §4：「能上 AST 就上 AST」。

## 十三、下一轮第一件事：把"内置对话"清干净（5 个模块 40+ 处）

已定位的内置台词分布（用 `add_dialogue("ralsei", "字面量"` 扫出来的）：

| 模块 | 处数 | 性质 |
|---|---|---|
| `games_controller.py` | 14 | 石头剪刀布/猜数字的**规则提示 + 固定台词** |
| `hide_controller.py` | 11 | 捉迷藏的固定台词（用户还要改触发条件） |
| `file_sheet_controller.py` | 5 | Excel 处理（用户说"鸡肋"，**整块待议**） |
| `energy_hunger.py` | 5 | 累/饿的固定台词 |
| `entertainment_system.py` | 5 | 成就祝贺的固定台词 |

⚠️ 其中**游戏/捉迷藏的"请输入石头剪刀或布"属于功能反馈**，不是"聊天"；
直接静默会让功能不可用。**需要先定口径**（用户口径优先）：我的倾向是
「规则提示保留在**功能面板**里、不再经 `dialogue_ui` 混进对话流」，
纯情绪/闲聊/祝贺一律 **AI 化或删除**。

---

## 十四、本轮下半（待机窝着 + 就寝 + 睡/醒台词）— 完成并推送

### 14.1 ★★ 最重要的一条：**根因**，不是新功能
用户说「他这一直站着不动…**像贴图似的**」。根因 = **待机动画在真机上从未播放过**：

```
update_animation 静止分支: _idle_loop_active = idle_timer >= IDLE_LOOP_MIN_SECONDS(600)
update_movement 空闲分支:   idle_timer += dt
                           if idle_timer >= max_idle_duration:
                               randomize_movement_pattern(); idle_timer = 0
```
而 `randomize_movement_pattern` 把 `max_idle_duration` 随机成 **2~12 秒**
⇒ `idle_timer` 最长 ≈12s，门限 600s ⇒ **判据恒假**。
（第51轮把最长休息 25s→12s，方向是**反的**：休息越短、清零越勤、门限越达不到。）

**处置**：判据换成"距上次**用户互动**"（不会被自主漫游清零）——
`_idle_loop_active = (_lounge_since is not None) or (idle_timer >= 600)`。
旧路保留 ⇒ `round8_anim` E1.3/E1.4 仍绿。

### 14.2 第⑧项 待机窝着
- 新增 `_work_area_rect()`（`availableGeometry`，**已扣任务栏**）＋
  `_taskbar_top_y()`（= 工作区底 − 窗口高）＋ `_lounge_perch_point()`（横向 72%）。
- ⚠️ **没改** `_desktop_floor_y()`（"地面"≠"快捷栏上沿"；混用会把坠落落点一起抬高）。判据 A5c 锁这条。
- 状态机：无互动满 600s 且不忙 → 进待机（目标点=窝点、`is_moving=True`）→
  **关掉随机漫游** → 待着；一有互动/变忙立刻退出；窝点算不出 ⇒ 不进且不留半个状态。
- "偶尔聊几句"沿用既有唯一通道 `start_autonomous_speech`，**没开第二条通道**。

### 14.3 第⑨项 就寝
- 窗口 = 23:00 ± 10 分钟；落点**用日期做种**（当日稳定、跨日不同）。
- `BEDTIME_HOME_SCENE = 'desktop'`（**待用户裁定**是否换成 `ralsei_room` 城堡房间）。
- ★ 三个必须说清的判定：① 窗口过后**不补睡**；② **就寝必须能"升级"小憩**——
  `_bedtime_busy()` **刻意不含 `is_sleeping`**，且 `_bedtime_tick` 排在
  update_movement"睡眠状态处理"**之前**（否则白天 5 分钟小憩必然覆盖 23:00）；
  ③ 早上 7 点自动醒**只对"就寝睡"生效**。
- 每 tick 调用但内部 5s 节流。

### 14.4 第⑤项补漏：睡/醒/哼 9 句内置台词全部迁走
`enter_sleep_mode`（3 句 zzz）/ 睡眠分支（3 句哼哼）/ `wake_up`（3 句）
→ `speak_event(kind, None, face)`；`event_speech` 新登记
`sleep_enter` / `sleep_stir` / `wake_up`（TIER_AI，旁白只写动作）。
连带：`verify_s7_event_speech` 的 **C1 计数锁 20→23**（随迁移同步改数字，
**不许放宽成 `>=`**）。

### 14.5 判据侧的三个坑（都是"过窄 ⇒ 假红"）
① `_idle_loop_active` 在 `init_*` 里还有一次**初值赋值** ⇒ `ast.walk` 取第一个匹配拿错；
② A7/A8 匹配到了**我自己写的注释**（注释里逐字引用了被删的台词）⇒
   A8 走 `ast.Constant` 字符串集合，A7 走"剥注释+剥 docstring"；
③ ★★ 修完①②后**正控制反而报红** —— 我一开始用了"剥注释**和字符串**"的视图，
   而 `_idle_lounge_busy` 的属性表本身就是**字符串元组** ⇒ 一起被剥掉。
   **结论：`_code_only`（剥注释+字符串）与 `_no_comment`（只剥注释）必须分开备，
   用错哪一个都会在某一侧报假红。**
④ 另一坑：`B2` 的 detail 打了 `_lounge_since` 的**原始 epoch 秒** ⇒ 基线每次 DIFF。
   按既有纪律**改判据输出**，不改基线（改基线=把环境噪声固化成预期值）。

### 14.6 回归
新增 `dialog_lounge52`（**82 判据**，exit 0，进 `HERMETIC_IDS`）。
**G2 全量 = 50 套件 / PASS=2508 / FAIL=0 / 全 IDENTICAL / exit=0**。
（49→50 套件；2426→2508 PASS；基线走 `--only --update` 合并模式。）

### 14.7 🔴 待裁定
① **第④项待机动画**：用户描述的"电梯里主角团坐着"**素材里不存在** ——
   1111 张 PNG 按 390 组枚举 + 10 个关键词扫，`elev/seat/chair/relax/lounge/rest` 全 0；
   `spr_ralsei_lift_*` 是**Susie 把 Ralsei 举起来**（站姿）。真"坐着"只有
   茶会 sit（6 姿态）/ 暗教堂 sit / 赛车 sit / 沙发 / 跪坐。已出对照图请用户指认。
   **我的建议**：茶会 6 姿态最适合做待机循环（不会又变成单帧"贴图"）。
② "他的房间"是否换成 `ralsei_room` 城堡房间（有 `dw_ralsei_castle_front/_1f/_2f/_3f`）。
③ 第⑤项剩余（娱乐建议 8 / 笑话 6 / 推荐游戏 5 / 成就祝贺 5）我倾向**保留**（功能播报）。
④ `file_sheet_controller` 是否整块去掉。

（上半提交 `54398f0`；下半提交见 git log。）


