# 2026-09-19 — Ralsei 桌宠 · 第七轮续（S7 第二批前半落地 + 口径更正）

commit `b25e91d`（已 push，`ls-remote` 核验远端 == 本地）。

## 一、这一轮真正的起因
用户说「你说的口语化点呗，我听不懂你让我拍板什么」——上一轮我把"需您拍板"这件事讲得太技术、
而且**讲重了**。所以这轮先回去核源码，再动手。

## 二、口径更正（两处 overstatement，都已写进文档 §〇）
1. **"迁环境自动触发 → 绕过 600 秒节流 → Ralsei 明显变话多" —— 不成立。**
   `energy_hunger.check_status_changes` 本来就只在**档位跨越**时弹一次
   （`if energy_tier != self._prev_energy_tier:`，原注释"避免每个 tick 重复刷屏"）。
   说话**次数**由触发源决定，跟"走不走 AI"无关；变的只是措辞从固定句 → AI 现编。
2. **"`speak_event` 没有时间维度的节流" —— 不成立。**
   `main.py:5200` 有 `EVENT_SPEAK_MIN_INTERVAL = 2.0`，就是 `_event_ai_ready()` 尾部那道频率闸
   （回归锁 B10 / D18 一直在钉它）。

→ 结论：那 11 条环境/时间触发的台词**不阻塞、不会让 Ralsei 变话多**，只剩一个
「固定台词 vs AI 现编」的小偏好。可以随时做，也可以一直不做。

## 三、落地内容（3 个产品文件 + 1 个套件 + 1 份文档 + 基线）
| 文件 | 改动 |
|---|---|
| `ralsei_pet/modules/event_speech.py` | `EVENT_TIERS` 登记 `rest_start` / `eat_start`（TIER_AI）+ `EVENT_DIRECTIVES` 两条旁白 |
| `ralsei_pet/modules/energy_hunger.py` | `rest()`/`eat()` 的"开始"台词改走 `self.parent.speak_event(...)`，删掉原 `add_dialogue`+`show_dialogue`；**"拒绝原因说明"两句保持罐头不动** |
| `ralsei_pet/src/main.py` | `react_to_desktop_element` 里写死的助手腔「需要我帮你打开浏览器或搜索什么吗？」→「咦，是浏览器呀……我平时不太敢乱碰里面的东西呢。」（face `helpful`→`surprised`） |
| `code-quality-audit/人味改造-2026-09-18/verify_s7_event_speech.py` | 新增 C10/C11/C12 + D24/D24b/D24c/D25（117 → 124） |
| `S7_第二批候选分类_2026-09-18.md` | 顶部加 §〇 口径更正（§一/§三 原判保留留痕）+ §七 落地清单 |
| `code-quality-audit/regress/baseline.json` | 重建 |

**验证**：`s7_event_speech` 124 PASS / 0 FAIL；全量 G2 **1050 PASS / 0 FAIL / 24 套件 / 全 IDENTICAL**。

## 四、没动的（明确记录，防下一轮顺手扫过去）
- §三 那 11 条环境/时间触发台词（累饿 4 + 定时问候 4 + `wake_up` + `handle_fall` 恢复语 +
  `react_to_desktop_element`）—— 只剩偏好选择，不阻塞。
- `main.py:4000~4010` 的 `help_messages`（"需要我帮你控制这个PPT吗？"…）是**功能发现性提示**
  （真的在告诉用户能干什么），不是零信息的助手填充句 → **不在迁移范围**。
- H4/H5 §4.1 persona 示范去可抄性 / §4.2 对话链路禁语代码兜底（零模型成本的人味收益）—— 未动。

## 五、教训（写进 MEMORY.md 了）
1. **别把"某条链路没有节流"当结论写进决策文档** —— 落笔前先 grep 常量与调用点。这轮两处
   overstatement 都是没看 `speak_event` 尾部那道 `EVENT_SPEAK_MIN_INTERVAL` 闸。
2. **断言别写成"这个函数里没有 `add_dialogue`"**：同一函数里可能有**合法的**另一处直接显示
   （`rest()` 的"拒绝原因"分支）→ 判假红。要锁**具体那一处**（needle 不存在 + 计数 2→1 双条件）。
   C10 第一次就是这条踩的（`rest.add_dialogue=1`）。
3. **PowerShell 捕获 Python 的 UTF-8 stdout 毁证据（第 3 次踩）**：`Out-File -Encoding utf8` 收
   `run_all.py` → 乱码。正解：**直接 `Copy-Item` `regress/_out/<suite>.txt`**（Python 自写真 UTF-8），
   或先 `[Console]::OutputEncoding = [Text.Encoding]::UTF8` 再 Out-File。
4. **别只数 `main.py` 就以为迁移范围只有 main.py** —— batch 2 迁的是 `modules/energy_hunger.py`，
   套件 C1 的 main.py 计数不变，得另加断言。
5. PowerShell 里 `New-Item`/`Out-File` 到一个**不存在的父目录**会 exit 1 且无输出 ——
   脚本里报"命令完成但无 stdout"时先确认目录。

## 六、下一步候选（等用户点头）
1. §三 那 11 条要不要迁（一个偏好问题，我倾向：累饿播报迁（纯情绪）、定时问候也迁、`wake_up` 保持罐头）。
2. H4/H5 §4.1 + §4.2（零模型成本的人味收益，且 §4.2 是"用户看得见的兜底"）。
3. 真机确认：点"休息"/"喂食"按钮时那两句话现在是 AI 现编。

---

# 追加（同日稍晚）：用户决定「全交给 AI + 授权换模型」→ 底座换到 4B

用户原话：「都让AI现编，就是，别给内置语音了，顶多是口头禅这类的有固定，但目前他还没有，
所以，**如果AI能足够生动**，那就把所有对话全权交给AI」，随后「你现在可以换模型了，
也授权你去找到更符合的项目了」。

## 一、先把"够不够生动"验掉（用户的前提是个可证伪条件）
`probe_vividness.py`（生产同形：persona 作 system + lean 无上下文 + 过生产两道后处理）
→ `_evidence/probe_vividness.txt`。**3B 三类硬错**：
① 客服腔「早安，有什么我可以帮忙的吗？」（persona 第 40~41 行明令禁说）
② 主体错位（饿 →「你今天过得怎么样呢？」）③ 意思反转（已吃饱 →「我自己也饿坏了」）
④ 旁白化（「我扶着墙站了起来，拍拍身上的尘土。」）→ **前提不满足**。

## 二、三向对比定案（`probe_models_compare.py`）
先补拉 4B：`qwen3:4b-instruct-2507-q4_K_M` **从上一轮残留的 70% 分片续传，110 秒完成**
（`pull_model2.py` —— 关键修法是给 urlopen 设 timeout=30，**静默卡死也会抛异常**从而重连）。

| 模型 | 判退 | 事件首字 | 事件整句 |
|---|---|---|---|
| 3B ralsei:v2 | 1/28（另 1 条**穿闸漏网**） | 530ms | 1214ms |
| **4B qwen3-2507** | **0/28** | 829ms | 1815ms |
| 7B qwen2.5 | 0/28 | 1041ms | 1996ms |

7B **又慢又平**（「谢谢你。」「好痒啊……」）→ 无理由选。**定案：4B。**

## 三、落地
- **`ralsei:v3` = qwen3:4b-instruct-2507 + 原采样参数**，由 `assets/ralsei.modelfile` build；
  `config.json`（仓库 + 运行时 `E:\RalseiMemory\config.json`）都改成 `ralsei:v3`。
- **⚠️ 关键坑**：`api_client` **只发** temperature + max_tokens，top_p/repeat_penalty/num_ctx
  **全靠 Modelfile** → 只改 config.json 指向裸模型会**静默**换掉采样。必须重新 `ollama create`。
- **`speak_event(pool=None)` 新语义**：不传 pool = 不给内置台词 = 说不了就**沉默**（返回 ""）。
- **判退后自动重采样一次**（同提示词，保住 KV 缓存）；两次都判退 → 沉默。
- **三道输出闸**：出戏（原）＋ 客服腔禁语（新）＋ 旁白化（新）。
  禁语表**按动词块收、穷举语序变体** —— 3B 那句「有什么我可以帮忙的吗？」就是穿过了
  只写"有什么可以帮"的旧表（锁 A20b）。
- 文档 `底座换型决策_2026-09-19.md`。

## 四、回归
`s7_event_speech` 124 → **131**；`persona_chat` F1/F7 原本**钉死 `ralsei:v2`/`FROM qwen2.5:3b`**
→ 改成"ralsei 系列 + 有且只有一条 FROM"（**别在断言里写会随项目演进而变的常量**）。
全量 G2 **1057 PASS / 0 FAIL / 24 套件 / 全 IDENTICAL**。

## 五、教训（写进 MEMORY 了）
① **换底座 ≠ 改配置**：采样参数烘在 Modelfile 里，`api_client` 只发两项 —— 不重新 build 就静默漂移。
② **禁语表要按"动词块"穷举语序变体**，按整句收必漏。
③ **别在断言/描述里写会随项目演进的常量**（模型版本、模块数、动画组数）—— 本轮 F1/F7 就是现成的反例。
④ **Edit 报"成功"不等于落盘**：本轮两条 Edit 读回仍是旧值 → 改成"脚本批量替换 + 立刻回读断言"。

## 六、本轮之后的下一步
1. **批量迁移剩余台词**（`main.py` 112 处 + §三 11 条）—— 已成"去掉 pool + 登记 kind"的机械活。
   例外：**功能性回执里的精确数据**（文件名/页数/计数/游戏规则），4B 实测没丢但也只是"抽两次都对"，
   要不要为这类留原文兜底 → **待与用户单独讨论**。
2. `ollama rm ralsei:v2` 省 1.8GB —— **未执行，等用户点头**。
3. 真机验证 v3 观感（首字 0.83s 是**中位**；每种 kind **首次**触发是 1.3~1.9s）。

---

# 追加（同日第三次）：persona「口癖」节 + 载体层状态管理；Ollama 被它自己的自动更新搞坏

commit `79b9773`（已 push，`ls-remote` 核验远端 == 本地 == `79b9773`）。

用户口径：「OK，但感觉现在 4B 说出来的话的性格不像 ralsei 一样，你给训练一下，其他的可以删一删了」，
紧接着补：「可以从系统提示词精细化，载体层状态管理和LoRA微调试试」。

## 一、系统提示词精细化（"训练"的唯一实际杠杆）
persona 加「我的口癖」节 + 扩「这些话我不说」（客服腔 8 条 + 禁动作旁白 + 禁提"提示词/设定/扮演"）
+「我怎么说」补两条（别主动给建议列一二三；说完自检像不像"在给人回复"）。
**关键分寸**：口癖写成**倾向**而不是固定台词，并明写"这些是习惯不是台词，别原样说出去" ——
避上一轮"往 system 堆示例口头禅 → 3B 当模板复读"的雷。

## 二、载体层状态管理（新）
`_build_ai_context`：精力/饥饿三档；新增 掉落/走动/窗口上/桌面上（与驱动动画同一份状态）；
新增"被冷落多久"（`chat_with_ai` 写 `_last_user_chat_ts`，>30min 才提、<60s 提"刚聊过"，
**elif 互斥**——每句都撒娇才是"不像 Ralsei"；时间戳写在 `api_enabled` 判断**之前**，
否则开关一关一开就"把开关当成冷落"）；尾部加"别逐条念、别硬转成话题"用法约束。
位置仍在 system 尾部 → 人设那段 KV 前缀缓存仍命中；lean（事件）路径未动。

## 三、回归
- `persona_chat` 58 → **70**（新增 G 组 12 项：G0 工具语义自检 + G1–G6 源码级 + G7–G11 行为级；
  "子系统全缺也不抛"是常驻**负控制**，靠 G7–G10 正控制配对）。
- 全量 G2 **1069 PASS / 0 FAIL / 24 套件**，唯一 DIFF = persona_chat（有意，已重建基线）；
  其余 23 套件逐字节一致 → 改动没有外溢。
  证据 `_evidence/g2_after_state_2026-09-19.txt` + `g2_verify_after_state_2026-09-19.txt`。

## 四、⚠️ Ollama 被它自己的自动更新搞坏了（本轮最大的环境意外）
11:10 updater 发现 0.34.2 → 11:11:26 Inno 开始装 → 11:11:27
`Shutting down applications using our files. (forced)` → **日志到此为止，没装完**。
后果：`lib\ollama\llama-server.exe` 等**推理器二进制全被删**，`ollama.exe` 还是 09-09 旧版。
`/api/tags` 正常（6 个模型 blob 完好），`/api/chat` 一律 **HTTP 500**。
→ 桌宠现在是哑的（按新设计 AI 不可用时沉默）；`probe_ralsei_voice.py` 68 条全 500；
`ollama rm` 也用不了。取证与三条修复路径：
`_evidence/ollama_runner_missing_2026-09-19.md`。
**教训：`/api/tags` 通 ≠ 能推理。判"Ollama 是否可用"必须真发一次 `/api/chat`。**

## 五、LoRA —— 更正我此前"不可行"的说法
**错在没查工具链就先下结论**（正是 MEMORY 里"能力自评失准比能力不足更危险"那条）。
实查：训练栈**早就装好了** —— torch 2.3.0+cu121(cuda=False) / transformers 4.57.0 /
peft 0.17.1 / datasets 4.1.1 / accelerate 1.10.1 / bitsandbytes 0.48.1 / safetensors 0.6.2；
只缺 trl、sentencepiece。真正的瓶颈是：
① **没有任何真实对话语料**（`E:\RalseiMemory\logs\*` grep「本地AI/对话/回复」只有分词器三行）
   → 数据集只能自造，而自造 = 我写的 Ralsei → 天花板不比 persona 高。要真提升须拿**原作台词**。
② 需 HF 版 Qwen3-4B 权重（~8GB）；Ollama 里的是 GGUF，**不能直接训**。
③ 合并后转 GGUF（f16 ≈8GB，比现在 Q4 的 2.5GB 慢一截）+ CPU 训练以小时计。

## 六、顺手做掉的
- `MEMORY.md` **90.7KB → 16.3KB**（原文件已超注入上限被截断）；详细契约全文拆到
  `.workbuddy/memory/参考-契约与历轮（详版）.md`（37KB，**不自动注入**，需要时 Read 它）。
- 新增 `probe_ralsei_voice.py`（旧 persona vs 新 persona 的 4B 口吻 A/B；按 persona 分组跑
  以只付 2 次前缀重建）—— 等 Ollama 修好后即可跑出证据。

## 七、下一步（等用户决定）
1. **Ollama 修复**（装应用 = 用户的决定）：用户自己重启 Ollama，或授权我跑那个已下载的
   `%LOCALAPPDATA%\Ollama\OllamaSetup.exe`（用户级、无需管理员）。
2. 修好后：跑 `probe_ralsei_voice.py` 出 A/B 证据；再 `ollama rm` 旧模型
   （`ralsei:v2` / `ralsei:latest` / `qwen2.5:3b` / `qwen2.5:7b-instruct`，共省 ~10.4GB）。
3. LoRA：先定**数据集策略**（原作台词 vs 自造），再决定要不要下 HF 权重 + 跑 CPU 训练。

