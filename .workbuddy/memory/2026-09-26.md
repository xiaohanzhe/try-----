# 2026-09-26 工作日志

## 第47轮：ch1/2/4/5 房间全量复查 + 场景寻路「人味化」

**用户口径（本轮起点）**
> 「复查一下1,2,4,5,章的room加起来到底有多少，别漏了，并且确保每一个都能用，
>   且符合原作逻辑，对了，那个寻路要符合真人逻辑，别和机器人一样」

**结论**（全部证据落 `code-quality-audit/第47轮-房间全量与寻路人味化/_evidence/`）

1. **房间数**：原作五章 1,251；**ch1/2/4/5 = 1,005**（147/278/328/252）。
   产品场景 1,014（含 desktop 1；ch1~ch5 = 1,013），ch1/2/4/5 = 826。
   ★ 口径坑：`_index.json.chapters` 含 `desktop` 键，只遍历 ch1~ch5 会少算 1（已修）。
   ★「别漏了」= 逐章下标 0..n-1 一个不缺（真断言）。
2. **都能用**：1,014 个场景 U1~U5 全过（载体/注册/命名/几何），仅 `desktop` 的 room_id 非 int（设计如此）。
   ★ 两种载体坑：分片 `_zone.*.json` 记录里 `file` 为空串，只看 file 会误报 926 个「缺文件」。
   bg 覆盖 235/1,014（23.2%），779 个无 bg（不属「不能用」）。
3. **符合原作逻辑**：未覆盖 41 = **真缺口 10 + 孤立 31**。判据只能是**原作门图**
   （命名启发式只 74% 覆盖；`room_shop1`/`room_DARKbase_GMS2` 同名不同命）。
4. **寻路人味化**：缺陷在**房间内行走轨迹**（不在房间之间）。
   `scene_walk.py` 新增 `_monotone_forward` + `_despike`；真 A/B（193 组）：
   可见硬拐角 **318→107**、最深回溯 **228.4px→0.6px**、穿模 0、残留倒走 0；逐对 0 劣于原折线。
   `scene_pathfind.py` BFS→A\*：实测 5,798 组路线完全一致（如实结论，保留改动）。

**回归**：新增 `rooms_round47`(22) / `walk_round47`(33) 进 G2；`baseline.json` 合并更新（只增不改）；
**G2 = PASS=2154 / FAIL=0 / 44 套件**；复检 `recheck47.py` = 28/0。

**提交**：`5fbdd72`（+4014 / −23，24 文件），已 push，远端双向一致。

**教训（值得复用）**
- 复检判据自己报了一次红：`check(..., True)` 的"前置条件"写法被误判为恒真
  ⇒ 判据改成「常量 True 必须存在**同名守卫分支**」。
  再次印证：**报红先怀疑判据**、**判据过窄 = 会误报，与过宽 = 恒真同样要防**。
- `probe_ab47.py` 的 A 版来源从 `E:\Download\_tmp` 改成**仓库内冻结快照**：
  原写法在提交后 HEAD 前移 ⇒ A/B 变"新版比新版"**静默失效**；
  且回归判据不许依赖"用后即删"的临时区。
- `E:\Download\_tmp` 本次**不可写**（`OSError [Errno 22]`）⇒ 提交信息改写进 `.git/COMMIT_MSG_47.txt`
  （不进工作树、不被 `git add -A` 看到），用后即删。

**待用户裁定**（另有 2 条"待发送"任务未送达，等用户下条消息）
1. 10 个真缺口房间是否补做（需 UTMT 重导素材 + 建场景 + 加门边）。
2. 779 个无 bg 场景是否重导素材。
3. ch3/4/5 门图仅采 46/328、46/252 ⇒ 是否重跑 UTMT 补采。
---

## 第48轮：可交互道具 + 章节约束 + S 键菜单（「设置」不应用）

**用户口径（本轮起点，逐字）**
> 「对于里面可互动的道具也要做到可以互动，当然，道具效果不可带出当前章节的场景，也就回到光世界的时候暗世界的任何道具都会变成垃圾团里包含的东西，道具效果就直接消失，如果在其他场景使用非当前场景的道具那就显示"一股神秘的力量阻止了你"。对了，游戏里的背包这类的通过S键实现的菜单也要应用哦，但，设置不应用」

**五条需求 → 落地**
1. 可交互道具：`item_interact.py`（`InteractBus` + `Interactable`，照原作 `global.interact`）。
2. 效果不带出章节：回光世界 ⇒ 暗道具**不可逆**变「垃圾团」内容。
3. 异场景用非本场景道具 ⇒ 逐字文案「一股神秘的力量阻止了你」。
4. S 键菜单（道具/装备等）：`item_menu.py` + `item_menu_ui.py`。
5. 「设置」不应用：`INCLUDE_SETTINGS=False`（`obj_darkcontroller` 里 CONFIG 是独立兄弟分支，摘掉不碰其余页）。

**关键设计**
- 判定序 = **垃圾 > 异章节 > 异世界 > 场景门控**；世界归属用**原作房间 resource 名前缀**替代 `global.darkzone`（取证缺口）。
- `_worlds.json`：1,014 场景覆盖率 100%（light 202 / dark 809 / unknown 3，两侧反例 0）。
- **热键绝不注册裸 S**（裸 S 只在窗口有焦点时）⇒ `global_hotkey.py` 用 `ctrl+alt+S/E`。
- 数据层 `assets/items/` 共 310 条，全部由 GML 反编译提取（`extract_effects48.py`）。

**回归 / 体检**
- 新套件 `items_round48`（`verify_items48.py`，**53 条**，A~H 段）进 G2；`baseline.json` 合并更新。
- **G2 全量 = PASS=2212 / FAIL=0 / 45 套件 / 全 IDENTICAL**（= 上轮 44 套 2159 + 53）。
- 鉴别力体检 `disc_items48.py` **12/12 精确报红**、还原干净。
- 复检 `recheck48.py` **6/6 PASS**（含逐令牌回验 22 令牌 + 4 组数字）。
- 真机证据：端到端 `e2e_wire48.py` FAIL=[]；真实平台热键 `probe_hotkey48.py` PASS（实测 `WM_HOTKEY` **双投递**，已 0.25s 去抖兜住）。

**教训（值得复用）**
- 首跑 B2 报红 → 查出来是**判据错**（`remove_at` 前移把空洞挤到尾部，期望值写错），非产品错。
- 鉴别力体检 P1 揭出 **C3/C5 是恒真判据**（拿 `IS.MSG_MYSTERY` 比 `IS.MSG_MYSTERY`）⇒
  明确 **C2 = 「用户原话逐字」的单一锚点**，其余只守通路不守字面值。
- 新增零依赖模块会撞两处既有闸：`round5_smoke`（模块数）与 `scene_p0` **A6 白名单**（须手加 `_CONTROLLER_ATTRS` 同类白名单）——**「麻烦」是刻意的**。

**如实登记的缺口（8 条，全文见详版 §49.8）**：可交互道具类**当前无实例**（生成器只收有 sprite 实例）/ 队伍 HP 模型缺失 / 3 间未判定世界 / `ch1 light 201` kind 归成 `text`（无行为影响）/ 四页登记但不可用 / WM_HOTKEY 双投递 / 同房多件取登记首个 / 离屏 `winId()=1` 假句柄。

**记忆**：详版补 §49（含 §49.10 速查本下沉区）；速查本压缩至 **js_len=9901**（改前 10014，超限）——先补详版 + 逐令牌回验 + 净减，未塞新内容。

**待用户裁定**：补采可交互道具实例（需求 1 剩下一半）/ 队伍 HP 模型 / `_kind_of` 优先级 / ch4·ch5 门实例补采。

---

## 第49轮：主线/纯 NPC 分层 + 光世界「扭蛋球」容器

**用户口径（本轮起点，逐字）**
> 「对了，主线npc也就是非主角团的但有重大影响的人（包括toriel和asgore哦，其他的就是各个章节的boos和相对重要的那几个）可以给他们一个4B模型，当然，我也会提供对应的设定，你到时候和我说需要谁的就好，还有，纯npc，也就是没任何有重大帮助对话的人，就用4~10句内置对话就好，这个你自己写就行，对了，这些npc都可以跟着主角团走，但只有主线npc可以自己自主跟随，其他的npc需要主角同意或是要求才行，当然，这些npc都不可脱离暗世界或是进入不属于他们的暗世界，还有，ralsei也不可以脱离暗世界，除非是用，额，就第3章里出现过一很多扭蛋的那个球对吧，做一个4个方向的旋转动画然后在ralsei需要在光世界的时候让kris给他使用，然后就相当于把那个球套在了他身上，还有，球本身不可见的地方需要能遮住ralsei，所以你要做一下代码，还有，ralsei是在球里面，所以不要穿模，要时刻保持在光世界时候在球里面，所以即使是球可见度部分看到ralsei也要有那种透过塑料看人的感觉，也就是给他上一层滤镜，对了，其他主角团的人和lancer也可以进去，要求和上述一样，只不过他们可以随时脱下来（除了lancer）」

**15 条需求 → 落地**（逐条验收表见报告 §1）
1. 主线 NPC 给 4B：`_registry.json` `tier='main'` + `model='ralsei-npc:4b'` + `needs_setting=True`。
2. 纯 NPC 4~10 句内置对话：`_dialogue.json` **18 组 × 4 句**（本项目撰写）。
3. 都能跟主角团走：`can_follow()` 两类都 True。
4. 只有主线 NPC 自主跟随：`FollowPolicy.AUTONOMOUS`。
5. 其他 NPC 需同意/要求：`CONSENT` + `request()→pending→respond()`。
6. 不可脱离暗世界：`world_gate(npc,'light')` ⇒ `leave_dark_world`。
7. 不可进非本属暗世界：`scene_chapter(scene_id) ∈ npc.chapters`。
8. Ralsei 不可脱离，除非用球：`escape_via_bubble` + `carried=True`。
9-15. 球：4 向旋转（**90° 均分整圈**）/ Kris 套球 / 分层遮挡 / 不穿模钳制 / 塑料滤镜 / 主角团+Lancer 可进 / 随时可脱（**除 Lancer**）。

**关键设计**
- ★★ **「遮住/透出」用分层绘制，不是 mask**：球切三层，角色夹在**下半后层(2)** 与 **下半前层(3)** 之间，最后盖**上罩(1)**（`DRAW_ORDER=(2,3,1)`）——逐字取自 `ch3.obj_tenna_board4_gacha_Draw_0.gml`。
- ★ **球的身份靠排除法**：`obj_ch3_ballcon`＝对话气泡框（排除）；`obj_ch3_gachapon`＝扭蛋机；★ **`obj_tenna_board4_gacha`/`obj_ch3_GSC07_gacha`** 才是"把角色罩进球里"的对象（`spr_dw_tv_gachaball_transparent`，4 帧 62×62 原点 31,31）。
- 原作本就支持三人进球：`actor_target` 1409=Kris/1411=Susie/1412=Ralsei。
- **不可脱 / 必留球 是两张表**（`NEVER_EJECTABLE=(lancer,)` vs `must_stay_inside`）——体检 P3 证实职责真的分开。
- 世界归属**复用第48轮 `_worlds.json`**（1,014 场景）；`scene_id` 缺失时**不做章节硬判**（不猜）。
- 跟随轨迹数学**复用第46轮 `companion.py`**，本模块只管策略与门控。

**交付物**：产品代码 `npc_system.py`/`bubble_system.py`（零依赖 P0~P1）；数据 `assets/npc/{_registry.json,_dialogue.json}`+`assets/bubble/*.png`(**43**)；取证 `第49轮-NPC与球容器/_evidence/gml/`（**381 个 .gml**）+ 工具 9 个。

**回归 / 体检 / G2**
- 回归锁 `verify_npc49.py`（**82 条，A~H 八段**）进 G2；`HERMETIC_IDS` 加 `npc_round49`；`baseline.json` 合并更新 `round5_smoke`+`npc_round49`。
- **G2 全量 = PASS=2296 / FAIL=0 / 46 套件 / 全 IDENTICAL**（上轮 45 套 2214 + 新套 82）。
- 鉴别力体检 `disc_npc49.py` **12/12 精确报红**、还原干净。
- 复检 `recheck49.py` **9 项 FAIL=0**（含逐令牌 28 组 + 数字 6 组）。

**教训（值得复用）**
- **A1 假红**：模块级 `try: import` 的 AST 父节点是 `Try` 不是 `Module` ⇒ 判据改按「是否位于函数体内」判。
- ★★ **G4 正负两侧都踩过**：首版拿被测常量自比 ⇒ **恒真**；改行为断言后又踩 **恒假**（`angle_for_direction` 取模永远回不到 +360）；最终靠"字面锚点 + 结构断言"落地。
- ★ **产品语义修正**：「4 个方向」原拍 45° ⇒ 只覆盖 135°、转不满一圈 ⇒ 改 **360/4=90°**。
- **体检 P10 判据失明**：`len(os.listdir())` 把 `xxx.png.hidden` 也算进去 ⇒ 改**只数真 PNG**。
- **recheck49 ④ 假红**：正则命中了自己注释里的示例 ⇒ 改 **AST** 找真实调用。

**如实登记的缺口（8 条，全文见详版 §50.8）**：未接线到产品主流程（`scene_controller`/`scene_render` 未挂，**玩家看不到球**）/ 4B 模型未建 / 纯 NPC 对话未逐角色考据 / 动画名表未接 `sprite_loader` / Lancer 每向只 1 帧（进球后无行走动画）/ `escape_via_bubble` 只对 Ralsei / 球变体 3 帧导出未用 / **未做真机视觉验收**。

**记忆**：详版补 §50（50.1~50.10）；速查本加 §50 指针（§10 索引 + §11 第 7 条），js_len **9901 → 9998**（**余量仅 2**，仍在 10000 内）⇒ 按"不影响读取就不动"口径**保持不压**，但**下轮再加指针前必须先腾空间**。无 BOM / 无 U+FFFD。

**待用户裁定**
1. ★★ **等 14 位主线 NPC 设定** ⇒ 建 4B 模型 `ralsei-npc:4b`。清单（报告 §7）：Toriel / Asgore / Lancer / King / Queen / Spamton / Berdly / Noelle / Tenna / Rouxls / Gerson / Mike / Roaring Knight / Flowey。**只给「语气 + 禁忌」即可开工**。
2. 接线优先级（缺口 1）：`scene_controller` 挂 NPC/球 + `scene_render` 真画三层球。
3. 纯 NPC 对话是否逐角色考据（缺口 3）。


---

## 第50轮（球容器**接线** + 世界门控三规则 + 4 处既有锁缺陷修复）

**用户口径（第49轮需求表第16项，逐字）**：「16项要，但**只有 ralsei 能自由在其他暗世界走**，
**其他人无法通过球去其他世界**，只能去光世界或是回原先他们的暗世界（**这个不需要他们套上球**）」

**做了什么**
- **三规则落码**：`npc_system.world_gate` 重写（光世界谁都能去/不需球，唯一例外 Ralsei 必须靠球；
  暗世界只进自己登记的章；跨暗世界只有 Ralsei）；新增 `REASON_LIGHT_NEEDS_BUBBLE`、
  `DARK_ONLY_IDS`/`is_dark_only`/`light_needs_bubble`/`free_dark_roam`；
  `bubble_system.grants_foreign_dark()` **恒 False**（写成函数以便正面断言）。
- **接线闭环**：`scene_render._bubble_items`（每球 4 条：back→角色→front→top，帧 2/3/1）+
  `split_bubble_layers`（后层→`scene_canvas`、前层+滤镜→新控件 `BubbleOverlay` + `raise_()`）+
  `scene_canvas._paint_bubble/_paint_bubble_filter`（`cover=1-alpha=0.12` 塑料滤镜，**不换 pixmap**）+
  `main._active_bubbles/_update_bubble_overlay/toggle_bubble`；`SCENE_LAYER_ENABLED=True`（第18项）。
- **实测事实**：球三层帧尺寸不同（整球 62×62／top 60×48／back 60×51／front 60×47）；
  UTMT 导出**无 per-frame offset** ⇒ 改按**球心居中**；球缩放由 `fit_scale(角色像素)` 反推（禁硬用 1.55）。

**回归 / 体检**
- 新增 `verify_wire50.py`（**76 判据 A~H**）进 G2（offscreen + HERMETIC）；`disc_wire50.py` **12/12 精确报红**。
- 第49轮套件按新口径修订 E1/E2/E13 ⇒ **82/82**。
- **G2 全量 = 47 套件 / PASS=2369 / FAIL=0 / 全 IDENTICAL**。
- 顺带修掉 4 处**既有锁**缺陷：`canvas_round44` A3（裸子串→AST import）、
  `canvas_round44` E4a（口径 False→True）、`pathfind_round45` E6（去掉**行号**漂移量）、
  `routes_order44` C 段（**读 E 盘临时区** ⇒ 改为读仓库内 `_evidence/rooms44/ch*.json`，E 盘仅兜底）。

**环境事件**：本轮中途 **E 盘整体掉线**（`E:\Download` 不可达）⇒ 恰好暴露"套件依赖外部临时区"的
基线不封闭问题；后 E 盘**已恢复**（`drw` 源数据完好）。临时文件在掉线期借用 `%TEMP%`。

**记忆**：详版补 **§51**（51.1~51.10，含**下沉区**接收速查本搬来的三段原文）；
速查本**结构性压缩**（§7 两段下沉 + §11 项 1/2/3 按用户第50轮裁定改写 + 删与 §10 重复的历轮口径）
⇒ js_len **9998 → 9988**（余量 12）；**逐令牌回验 LOST=0**；无 BOM / 无 U+FFFD / 纯 LF。

**遗留**：`toggle_bubble` 无交互入口｜球每帧 offset 未导出｜实机目视未验收（`E` 盘掉线亦一度阻塞）。
