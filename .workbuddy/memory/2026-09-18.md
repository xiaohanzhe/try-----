# 2026-09-18 工作日志（Ralsei 桌面宠物）

## 本轮：Ralsei 对话 AI「人味」诊断 + 训练方案（只读，未改工程文件）

用户："现在你想必也能看出来他这个AI没有活人味，你给看看怎么训练一下"

### 交付
- 报告：`Ralsei对话人味诊断与训练方案_2026-09-18.md`（项目根）
- 证据：`code-quality-audit/人味诊断-2026-09-18/_evidence/`
  （`probe_ralsei.py` / `ab_ralsei.py` + 3 份原始输出 + `ralsei_modelfile.txt`）
- 提交 `bc49043`（6 文件 / +969），**已 push 并 ls-remote 核验远端 == 本地 == bc49043**。

### 硬结论（6 条证据）
1. **E1 模型事实**：`ralsei:latest` = `qwen2.5:3b` 衍生（3.1B / Q4_K_M / ctx 32768）；
   `/api/show` 的 `parameters` 为 **None** → temperature/top_p/repeat_penalty/**num_ctx 全走服务端默认**。
2. **E2 决定性**：**App 自带 system 会整体替换 Modelfile 的 SYSTEM**（不是叠加）。
   实测 `prompt_eval_count`：不传 system = **1237**（2003 字角色圣经生效）；
   传 system = **41**。→ 用户写的角色圣经**从未生效过**。凶手：`main.py:7141-7158`。
3. **E3 窗口截断**：5400 字符长提示词 → `prompt_eval_count = 2050` ⇒ **num_ctx ≈ 2048**，
   而角色设定占 1237 → 历史/记忆必被静默截断。
4. **E4 A/B 受控实验**（同模型同问题同温度，只换提示词）：
   - **V0 现状：4 题中 3 题复读「你还好吗／要不要休息一下」** —— 源头就是现行 system 里
     把口头禅写成了示例，3B 直接当模板抄。**回看 2026-09-12 报告的 5 条实测回复，其中 3 条已带这句**
     —— 问题当时就在，只是被当成"自然"了。**这是本轮最重要的发现：上一轮"训练"自己埋的雷。**
   - V1 恢复角色圣经：有角色味但**把用户当 Kris、跑进剧情**（设定通篇绑定 Kris/Susie/黑暗世界），不能直接用。
   - V2 人味工程：质变，但 3B **照抄 few-shot 原句 + 跨示例串味**。
   - V3 V2+temp0.9：给出全场最好的两条（羞涩 + "只是觉得还不够好吗"的角色层次），
     但 Q3/Q4 **失控自问自答续写 + 跑旁白**，200 tokens 用满。
5. **E5 延迟**：常规 1.1~6.6s、长回复 11.5s、失控 15.8s；配 `stream: False` 假打字机
   → 用户先静止看 2 秒"……"，是"假人感"来源。
6. **E6 大部分台词没过 AI**：131 处写死 `add_dialogue`；**34 条子串硬拦截**排在 AI 之前
   （`哭`/`游戏`/`状态`/`天气`/`精力` 等高危子串，纯 `in` 匹配无边界）；
   `_build_ai_context()` 把【此刻】拼在**用户消息最前面**（`main.py:7133`）。

### 结论与建议
- **主因是提示词没接对，不是模型不行**：同 3B 只换提示词即从"答非所问"→"接得住话"。
- 三层"训练"取舍：**L1** 提示词/参数层（推荐先做，当天见效、零成本）→
  **L2** 换底座（L2a 本地 7B / L2b 云端端点）→ **L3** 权重微调（**当前不建议**：无数据集 + 3B 上限未解）。
- 阶段一施工清单 S1~S9 已写入报告（persona 外置、system 重写、此刻移出 user 消息、
  `options` 透传 + `num_ctx=8192`、输出护栏、关键词拦截降级、事件台词分批、流式、重建 Modelfile 为 `ralsei:v2`）。
- **待用户决策**：本轮范围（L1 / L1+L2a / L1+L2b）、persona 是否保留 Deltarune 世界观元素、S7/S8 是否入本轮。

### 硬件事实（为 L2 决策采集）
- CPU **Intel Core Ultra 5 125H**（14C/18T）／GPU **Intel Arc 核显**（`VEN_8086&DEV_7D55`）
  ／**无 NVIDIA 独显**（`nvidia-smi` 不存在）／RAM 33.9 GB。
- 磁盘：C 剩 109.4 GB / D 剩 479.6 GB / **E 剩 934.2 GB**。
- → 3B 的 ~2s 延迟是 **CPU/核显**推理结果；7B 可行但预计 5~12s。

## 教训（本轮新增）
1. **别用 PowerShell 管道读模型的中文元数据**：`ollama show --modelfile` 经管道读出来全是 GBK 乱码
   （`浣犳槸涓€涓鑹叉壆婕?AI`），我一度误判"模型里的角色设定坏了"。
   改走 **Ollama REST API + 显式 UTF-8** 直读，确认 2003 字设定完好。**凡涉中文元数据，走 API 不经管道。**
2. **注入的 `current_time` 会滞后于系统真实时间**：本轮注入值写"2026-09-16 00:22"，
   `Get-Date` 实测 **2026-09-18 00:07**。**落盘前必须用 `Get-Date` 校准**，
   否则报告/日志文件名日期就是错的（本轮已 `git mv` 改名 + `--amend` 修正）。
3. **提示词里的"示例口头禅"对 ≤3B 模型是毒药**：会被当成必答模板复读。
   小模型的 few-shot 必须标"风格示范，不要照抄"并配输出护栏（去重/截断/重采样）。
4. **A/B 受控实验是判断"到底是模型不行还是接线不对"的唯一办法**：
   固定模型/问题/温度，只换提示词，一次就分清了。
5. **GitHub 主站会整段不可达而 `api.github.com` 仍正常**：
   此时 `git push` 必失败（9 次尝试 + 7 个备用 IP 全 000），但 **api.github.com 通**。
   处置：**提交照做（本地不丢）→ 报告网络阻塞 → 稍后重试**。本轮 ~25 分钟后网络自行恢复，
   一次 push 即成功。**别因为 push 失败就怀疑凭据或改配置。**

---

## 续：L1 全做 —— 施工 + 实测（用户拍板后同一轮完成）

用户拍板：施工范围 = **L1 全做**；口径 = **"保存性能的前提下既要有 Deltarune 世界观，
又要让他们知道现在自己生活在电脑桌面上"**；附加项交我定
（我定：关键词拦截降级**做**；S7 事件台词 / S8 流式 **顺延**）。

### 交付
- 提交 **`ee39227`**（19 文件 / +2080），**已 push，`ls-remote` 核验远端 == 本地 == ee39227**。
- 报告追加**第九节「训练」施工记录**（含 9.3 两次自我推翻、9.4 端到端实测、9.5 已知局限、9.7 复现命令）。
- 证据归档 `code-quality-audit/人味改造-2026-09-18/_evidence/`（9 份：3 脚本 + 3 输出 + G2 全量输出…）。
- **新增 G2 套件 `persona_chat`**：`code-quality-audit/人味改造-2026-09-18/verify_persona_chat.py`，
  **58 项全过**；**不联网、不调 Ollama**（模型质量天生不可复现，不进基线）。

### 改了什么
`assets/ralsei_persona.md`（新增，1591 字，五节）＋ `assets/ralsei.modelfile`（新增）
＋ `ralsei:v2`（ollama create，5 参数落地）＋ `config.json` / `E:\RalseiMemory\config.json`
/ `config_manager.py`（model + `api.options`）＋ `main.py`（接线重写 + 4 个新方法 + 判退重采样）
＋ `dialogue_ui.py`（`_HARD_CMDS` + `_is_command_phrase` 关键词收紧）。

### 本轮最重要的两次「自我推翻」（都靠实测，别再改回去）
1. **persona 示范不能删**。第一版删掉 3 组完整问答对 → 人味**明显退化**
   （被领导骂 → 「嗯？我明白你的心情了。」正是禁令里的套话；火锅 → 「啊哈，听起来很美味呢。」）。
   3B 就是靠 few-shot 学口吻的。反证留档 `_evidence/behavior_check2_无示范对照.*`。
2. **"照抄示范就判退"是错的**。恢复示范后「我好喜欢你呀」**3/3 稳定吐示范句**；
   若一律判退 → 真机链路是 *判退→重采样→还是照抄→交回 None→**观众看到内置规则台词***
   （`dialogue_ui._on_ai_reply` 在 reply 为空时 `_rule_reply()`），比照抄更出戏。
   → 判定集合从「persona 示范 ∪ recent」**缩成只有 recent**：示范句第一次说没问题，
   出戏的是**同一句反复出现**；第二次再抄它就落在 recent 里了。
   套件留了 **C7/C8 两条防回退断言**（等于示范句 + recent 为空 → **必须放行**）。

### 关键实测数字
- 端到端 9 次调用（重采样 2 次）：**完全重复 0 条 / 违禁套话 0 处 / markdown 残留 0 处**。
  「我好喜欢你呀」连问三次：前两次抄示范被拦并换成
  「诶？！听到这话我都有点不好意思起来了。感觉就像在阳光下晒着一样舒服呢。」，第三次直接出新句。
- **G2 全量 22 套件 / 857 PASS / 0 FAIL / 全 IDENTICAL**（此前 21 套件 / 799 PASS）。
- **滑字探针（否定结论）**：`诪、诪` 本应是 `诶、诶？！`，约 **1/10** 偶发。
  用 `repeat_penalty` 1.15/1.00 × `top_p` 0.92/0.90 各 3 次采样（**必须走原生 `/api/chat`**）：
  9 次里**真滑字 0 次**，1.00 组无任何改善 → **无证据支持改参数**，归入 3B 能力上限（L2 解决）。
  另：我的滑字判据把想要的结巴「我、我」误报成缺陷 → 计数全是噪声。**先读原始文本，再信计数。**

### 教训（续，本轮新增）
6. **`/v1/chat/completions` 静默忽略 `num_ctx` 与 `repeat_penalty`**（顶层/options 都无效）；
   只有原生 `/api/chat` 的 `options` 或 Modelfile 的 `PARAMETER` 生效。
   → 采样参数**必须落 Modelfile**；参数 A/B 必须走原生端点，否则会得到"改了没区别"的假结论。
7. **persona 文件是 prompt，不是文档**：它是逐字进模型的，写 `**`/`#`/`>` 会被 3B 照学
   （实测输出过 `**听到也让我心里暖暖的**`），而对话框是逐字打字机渲染 → 星号原样显示给用户。
8. **护栏必须能在"忠实复刻链路的 e2e"里验收**：直接调护栏函数会得到"成功率 0%"的假象（没走重试）。
   e2e 要把**已采用的回复**累积成 `spoken` 当后续 `recent`，不能用"上一轮的原始输出"。
9. **测试桩漂移 ≠ 真回归**：`round6_verify` 的 B2/B3/B7/B8 全挂，根因是
   `start_autonomous_speech._on_reply` 新依赖 `_clean_ai_reply`，而第六轮的 `SimpleNamespace` 桩
   没绑该属性 → AttributeError 被宽 `except` 吞掉 → 把"已发起"误判 False。
   修法：① 回调内对护栏加防御 try/except；② 护栏方法内部**不依赖任何 `self.` 实例属性**
   （只用类属性/局部量），桩上可直接跑。**（先看 g2 输出的时间戳再下结论——我差点对着修复前的旧数据debug。）**

### 收尾（同一轮尾段）
- **真实 `api_client` 集成探针**（补最贵的坑"改了 A 没接线到 B"）：真配置 `E:\RalseiMemory\config.json`
  ＋真实例 `HTTPLocalAI.chat` 打一发 → 证明 `api.options` → `_ai_chat_options()` → payload 这条链路**真通**
  （不是"写了没人读"）。冷加载 **15.6s**（`ralsei:v2` 首次）后稳定 **1.9~2.0s**，与改造前同量级、**无可感知退化**。
  证据 `_evidence/integration_probe.py/.txt`。
- 报告补 **§9.5** 并修重号：`9.7 回归验证` / `9.7 复现命令` → 后者改 **`9.8 复现命令`**（并补入集成探针命令）。
- 提交 **`1de2971`**（3 文件 / +152）**已 push，`ls-remote` 核验 `MATCH=YES`（远端 == 本地 == 1de2971）**，工作区干净。
- 顺手核验：提交信息无 BOM（源文件首 3 字节 `100,111,99` = `doc`）；`git log` 经 PowerShell 管道必显 GBK 乱码，
  **核验中文提交信息要用 Python `subprocess` 显式 `decode('utf-8')`**，别信管道输出（老坑再现）。

### 顺延（未做，留给下一轮）
- ~~**S8 流式输出**~~ → **本日第二轮已做完，见下**。
- **S7 131 处事件台词分批交 AI**（仍未做）。
- **H4/H5 上帝类拆分**：主 `main.py` 已 9812 行（本轮又 +），先前的行号基线全部失效，动工前须重测。

---

## 第二轮（同日稍晚）：S8 · 流式输出

用户："继续按计划执行" → 落点是 §5 施工清单里剩下的 **S8（流式）**。S7 仍顺延（分批，改动面大）。

### 交付
- 提交 **`761b7e0`**（19 文件 / +2082 −76）**已 push，`ls-remote` 核验 `MATCH=YES`**，工作区干净，提交信息无 BOM。
- 报告追加 **§10「S8 · 流式输出 施工记录」**（10.1 文件清单 / 10.2 为什么这次能做成 / 10.3 实测数字 /
  10.4 三个设计决策 / 10.5 已知局限 / 10.6 回归 / 10.7 复现命令 / 10.8 下一步）。
- 新套件 **`s8_stream`：68 项全过**（A api_client 16 / B main 接线 14 / C 清洗前缀安全 12 /
  D 打字机增量显示 19 / E 配置 3 / **F Qt 跨线程投递 2**）。不打网络、不调 Ollama。
- **G2 全量：23 套件 / 925 PASS / 0 FAIL / 全 IDENTICAL**（此前 22 套件 / 857 PASS）。

### 关键实测数字（证据 `_evidence/s8_*`）
- Ollama `/v1` **支持 `stream:true`**（`text/event-stream`，增量在 `choices[0].delta.content`，`[DONE]` 收尾）：
  非流式**首字节=全文=6.02s**，流式**首字 0.23s** / 全文 1.34s / 17 分片。
- **`requests.iter_lines()` 必须显式 `chunk_size=1`**：默认按 512B 攒批 → 首字 0.45s（吃掉近一半收益）；
  `chunk_size=1` 与 `resp.raw.readline()` 都是 0.23s。
- 真实 `HTTPLocalAI.chat_stream`：首字 0.23s，**累积返回 == 回调拼接**逐字一致。
- **端到端 4 问**：首字中位 **0.43s** / 最快 0.31s；完重 0；markdown 残渣 0；"屏幕最终 ≠ 护栏定稿" 0 例。

### 为什么上轮只敢标"可选"、这次却好做（重要）
上轮担心"UI 打字机要改成追加模式"。摸清后发现**结构本来就不用改**：
打字机 = `typing_text`（全文） + `typing_index`（进度） + QTimer 渲染 `typing_text[:index]`。
**只要让 `typing_text` 可追加**，`typing_index` 留在原位，定时器下一拍自然继续往新内容上打 →
视觉上是"字连续冒出来"。真正新增的只有一个状态位 `_streaming`（"队列暂时打完但消息还没收完"）。

### 三个必须守住的决策（都有回归锁）
1. **基类 `chat_stream` 是具体方法、不是 abstractmethod**（默认=回落 `chat()` + 单分片回调）——
   否则 `LocalAIStub` 与用户 `register_provider()` 的实现被迫改造，App 一升级就崩在用户那边。锁 A1/A2。
2. **流式显示只做"前缀安全"清洗**（剥 markdown / 截自问自答），判退与超长截断留给收尾 `_clean_ai_reply`。
   两条规则**共用同一份定义**（`_md_strip_re()` / `_role_marker_re()`），否则规则漂移=字打出去又被改掉。
   一句话：**流式负责"看着像人话"，护栏负责"最终算数"**。锁 C1/C2/C9/C11。
3. **流式与"判退重采样"天然冲突**：判退发生在收完之后，可被判退的半句**已经在屏幕上**。
   所以重采样前必须先发 `None` 分片让 UI 擦干净（`_stream_reset()` 回「……」），否则新句粘在旧句后面。
   且流式路径下 `_on_ai_reply` **不能**调 `_ai_thinking_off`（它清空 `typing_text` → 已显示内容一闪而逝再从头重打）。
   锁 B7/B13、D7/D8、D14。

### 新增教训（本轮）
10. **`requests.iter_lines()` 默认按 512 字节攒批**，会把流式的首字收益吃掉近一半（0.45s vs 0.23s）。
   要低延迟必须 `chunk_size=1`。
11. **`func_src` 式"首个 `def X(` 匹配"会取错函数**：①`def chat(` 会命中基类的抽象方法而不是子类实现；
   ②`def __init__` 会命中文件里第一个类的（DialogueUI 的 `__init__` 只有 7 行，打字机状态其实在
   `_init_state()` 里）。定位真实方法要**先切类段再找**，或直接断言整文件唯一 needle。
12. **needle 里含字符串字面量 → 用 `code_no_comment`；要排除 docstring 干扰 → 用 `code_only_src`**。
   两个工具各有用处，本轮两种错法各踩一次（B8 用错丢字面量、B9 需要用错会误命中 docstring）。
13. **断言必须带上中间的 `try:`**：`code_only_src` 把缩进全抹平，`ifnotstreamed:self._ai_thinking_off()`
   匹配不上实际代码（中间隔着 `try:`）→ 写成 `ifnotstreamed:try:self._ai_thinking_off()`。
14. **流式清洗不可能对所有文本前缀单调**：`1. `（要等点号后的空白）与 `主人：`（要等冒号）都是
   "需要看到后面字符才能判定"的规则，所以被延后剥掉的 1~2 字符**必然已经显示出去**。
   不要试图靠改规则消除它 —— 试过"提前剥"，会把开头的 `1.5 倍` 误伤成 `5 倍`，更糟。
   正解是**调用方检测"新结果不是已显示内容的前缀"就整段替换**（最终态一定正确）。
15. **Qt 跨线程投递必须单独验**：A~D 组只测到"函数级别"，而分片是*工作线程 emit → 主线程槽执行*；
   这条链路接不上，流式就**一个字都不显示**。用 `QObject` + 真 `pyqtSignal` + `processEvents()`
   起最小宿主，断言"按序投递 + 丢弃过期世代 + reset 顺序严格保持"。锁 F1/F2。
16. **`persona_chat` 的 B5 是"计数型断言"的教训**：原来数 `cli.chat(` 出现 2 次来证明"有重采样分支"，
   引入 `_ask()` 助手后计数失效。**计数法绑定了实现细节**，改成断言"两个请求点都存在且都走同一助手"
   ——更强（重采样也必须带流式回调，不能绕过）且更抗重构。

