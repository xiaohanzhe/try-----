# 2026-09-23 工作日志

> 说明：第 35/36 轮记在 `2026-09-22.md` 尾部（跨零点续写），本文件从**第 37 轮**起。
> ⏰ 本轮开局先用 `datetime.now()` 校准过真实时间：**2026-09-23 23:09**
> （注入的 `current_time` 显示 09-23 01:44，**滞后约 21 小时** —— 这条坑又踩了一次，
> 已固化在速查本 §1：「注入的 `current_time` 会滞后 → 先 `Get-Date` 校准」）。

---

## 第三十七轮：反编译原作素材，落地 87 张场景背景

**用户口径**（原话）：
> 「额，我不会导出，要不还是你自己反编译出来吧」　→ 授权我反编译 `data.win` 取素材
> 「我下载好了那个你需要的工具了」　→ UTMT CLI v0.9.2.0
> 「请继续执行任务」

承第 36 轮：「**开始按照原版的路线**…**场景系统完全遵循原作的逻辑**，**把桌面当成一开始的默认场景就好**」。

### 做了什么

1. **五章 `data.win` 全部反编译**（UTMT CLI v0.9.2.0）
   - dump 出精灵 **70,988**、纹理 **261**、房间表 **1,251** 间
   - 每章导出前后对**原始 `data.win` 做 sha256** ⇒ 五章全 `orig sha unchanged: True`
   - 房间结构一律在**副本**上分析（`load` 不带 `-o` 本就是只读的）

2. **87 个作品内场景全部落地背景图**
   - 12 张**原样提取**（真背景，一个像素不动）+ 72 张**平铺合成**（画布宽 1280 / 高按宽高比 clamp）
   - `how` 分布：`room.bg_layer` 9 / `area_table` 78；`OK=84 EXISTS=3 MISS=0 ERR=0`；内容去重后 44 份
   - 耗时 8.7 秒（修掉"逐个名字 glob"的 O(n²) 之后）

3. **把第 36 轮的恒真判据升级成真判据**（本轮必修项）
   - `check('E3 …尚不存在…', True, …)` → 真扫描 + **E3b/E3c 两个负控制**
   - 该套件 PASS **50 → 52**，基线用**合并模式**重建

### 本轮发现并修掉的三个问题

| # | 问题 | 性质 |
|---|---|---|
| 1 | G2 里两套被判 DIFF —— 逐字节看**只多了一行**「vault 日志不可写」+ 一个空行，PASS/FAIL 计数一字未变 | **沙箱环境噪声**（非产品缺陷）。已在 `run_all.py normalize()` 加 `_ENV_NOISE` 归一化，复跑即 IDENTICAL；**没有去改基线**（改基线 = 把环境问题固化成预期值） |
| 2 | `baseline.json` 只有 **29** 条而套件有 **32** 套 ⇒ 3 套一直只显示 `BASELINE`、**不做字节级比对** | 真缺口。已用合并模式登记 ⇒ **29 → 32** |
| 3 | 遗留项「ch1 `rooms_map.json` 需重跑」 | **不成立**。实测 `bad(UndertaleSprite)=0`、40 个背景层全是真名；批处理日志证明 ch1 早已用修正版 csx 重导出过一次。旁证：ch1 有 3 个场景命中 `room.bg_layer` |

### ★ 沙箱写入通道的精确签名（值得记）

```
新建文件：处处成功（%TEMP% / E:\Download\_tmp / E:\RalseiMemory\logs 全 OK）
删除文件：成功
覆写文件：本会话新建的 OK ／ 「本会话之前就存在的」一律 PermissionError
文件属性：mode=0o100666、readonly=False ⇒ 不是 ACL、不是只读位；os.rename 也报 WinError 5
```
⇒ **判据方向：先怀疑核验手段（沙箱），再怀疑被测物（产品）。**

### 记忆维护（系统强制项）

- `MEMORY.md` 速查本 **12270 → 11114 字符**（余量 886；上限约 12000 字符，超了会被**砍尾**=丢最新内容）
- 详版 **236598 → 263117 B**
- 手法：逐字改写被证明**无效**（只省 138 字符）⇒ 必须**结构性删除**（表格散列 → 紧凑块、去重合并）
- ★ 途中发现**真缺陷：悬空指针** —— 速查本与详版都在引用 `§19`，而**详版从来没有 §19 这一节**。
  按"先补详版再压速查"补写 §19（第 30 轮：产品链路首字实测 + 口径纠正，`aaa89f8`/`8b4d50c`），复验 40 个指针 `missing=[]`
- ★ 顺手修掉 skill `agent-memory-compaction` 里**自相矛盾**的判据（一处写"bytes ≤ 16384"、一处写"字符数约 12000"）⇒ 统一成字符口径

### 验证

**G2 全量：PASS=1502 / FAIL=0 / 32 套件 / 全部 IDENTICAL**（零 DIFF、零 BASELINE）
1502 = 第 36 轮的 1500 + 本轮 E3b/E3c 两条。

### 产物

- 报告：`第三十七轮原作素材反编译报告_2026-09-23.md`（项目根）
- 证据：`code-quality-audit/第37轮-原作素材反编译/_evidence/`（4 份，含《收尾核验_第37轮.txt》）
- 预览：`code-quality-audit/第37轮-原作素材反编译/场景背景预览.html`
- 管线脚本已归档仓库：`code-quality-audit/第37轮-原作素材反编译/_tools/`（4 个，避免临时目录一删就无法复现）
- 背景：`ralsei_pet/assets/scenes/bg/*.png`（87 张）

### 🔴 待裁定 / 下一步

1. **78/87 是"区域代表素材"近似** —— 请过一遍预览页确认语义对不对
2. **平铺画布口径**（宽 1280 / 高 `1280×clamp(rh/rw,0.5,0.9)`）是否接受
3. **`bg` 字段尚无渲染层消费** ⇒ "切场景真能看到背景"还没实现（P0 只写状态）
4. `progress`（房间级推进，P2）／ 是否要 BGM（`sound_manager` 无 BGM 能力）／ 原作版权尺度
