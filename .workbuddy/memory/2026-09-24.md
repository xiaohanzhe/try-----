# 2026-09-24 工作日志

> 承接 09-23（第 38 轮开局）。本轮实际跨 09-23 深夜 ~ 09-24 凌晨。
> 项目根报告：`第三十八轮场景系统全量落地与P0接线报告_2026-09-24.md`

## 一、记忆侧：修掉我自己的错误判据（重要，先做）

- 会话开头系统提示 `MEMORY.md has exceeded the size limit and was truncated during injection`。
  我据速查本里**自己写的旧判据**（"上限看**字符数**，实测 ~12000"）判定"影响读取"，
  把 `MEMORY.md` 从 **11,446 字符重写到 12,252 字符** —— **体积涨了 800，却丢了 4 个可检索令牌**：
  `真机打点落 CSV` / `真背景原样提取` / `chapter*10000` / `prefill 远慢于 decode`。
- **根因**：把自己"估"的数字当成了实测上限。
- **代码实证（权威）**：`resources/app.asar.unpacked/cli/dist/codebuddy-headless.js` 里
  `buildAgentMemoryPrompt` ⇒ `eh=200, ef=25e3` + `Buffer.byteLength(ea,"utf-8") > ef`
  ⇒ **上限 = 200 行 / 25,000 字节（utf-8 字节数，不是字符数）**，超限从尾部砍。
  实测事故后文件 = 111 行 / 20,932 字节 ⇒ **本来就在限内**。
  （另处 `eg=4e4`(40,000) 是 memory **工具**的单文件写作上限，别混用。）
- **那句服务端提醒在本地产物里查无此文案**（grep 全 `resources/` 无命中）⇒ 来自服务端链路，阈值未证实。
- 处置：
  ① 4 个令牌逐个回填速查本；
  ② 速查本头部错误判据改成代码实证口径；
  ③ **同轮修 skill `agent-memory-compaction`**（它写着同样的错判据），新增「上限的真实来源」一节 + 复现脚本；
  ④ 新增 `_tools/recheck_mem38.py`（**38 项 PASS / 0 FAIL**），判据新增 `bytes ≤ 25000` / `lines ≤ 200`，
     保留「假令牌必须查不到」作负面控制。
- 提交 `af12a3b`（已 push）。

## 二、P0 接线：场景系统真的跑起来了

- **问题**：`SceneController.load()` / `load_routes()` 早就写好，但**调用点 = 0**。
  产品进程实测 `_scene_loaded=False`、`current_scene=None` ⇒ 索引躺在磁盘上，等于不存在。
  这是「函数写对了但产品用不上」的**第 5 次**。
- **改动**（`ralsei_pet/src/main.py` `init_systems()` 末尾）：+18 行注释 + 2 行调用
  `self.scene.load()` / `self.scene.load_routes()`。顺序有意义（路由的 `destinations()` 要用索引过滤）。
- **为什么安全**：只做「读 JSON + 写宿主状态字段」，不注册定时器 / 不改渲染 / 不碰物理 / 不调 Qt；
  写入的 `current_scene` / `scene_objects` **零消费者**；两者「永不抛」且幂等 ⇒ 失败只降级。
- **真机验证 10/10 PASS**（offscreen，产品进程内）：
  证据 `第38轮/_evidence/真机_P0接线验证.txt`。含 L9 **负控制**（未调 `load()` 的控制器仍 False
  ⇒ 证明 True 来自接线而非构造自带）。
  L7 切到**区域分片里**的场景成功 ⇒ 证明 `switch()` 真把 `entry` 传给了数据层。

## 三、3 处代码瑕疵（每处配独立断言）

| # | 瑕疵 | 性质 | 修法 |
|---|---|---|---|
| 1 | `scene_routing._FALLBACK_PRIORITY = 10000` | **零引用死常量**（`_routes.json` 实为 100~900，无一用 10000） | 删除 + 在 `match()` 兜底分支注明「兜底不参与 priority 排序」 |
| 2 | `switch()` 里 `hasattr(self,'refresh_objects')` | `refresh_objects` **全仓无定义** ⇒ 恒假 ⇒ `scene_objects` 恒 `[]`；且与 `resolve_objects` 双名 | 改显式 `pet.scene_objects = []` + 注明由 `resolve_objects(screen_rect)` 填 |
| 3 | `main.py` 写"这 6 个"字段 | 实为 **9 个**（6 场景 + 3 路由） | 改 9 + 逐名列出；控制器 docstring 与 `run_all.py` 同步 |

- 新增断言（`场景系统-P0/verify_scene_p0.py`，80 → **90** 项）：
  J4 字段名 6→**9**；`J6/J6b` **P0 接线真的存在**；`J6c` 负控制（抹掉后判据必变假）；
  `J7/J7b` 全仓无 `refresh_objects` / `_FALLBACK_PRIORITY` **标识符**；
  `J7c/J7d` 负控制（合成源码必被识别 / **只在注释里的不算数**）。
- ⚠️ `J7b` 第一版**假红**：判据用纯文本扫，而**我的修复注释里**正好提到了 `_FALLBACK_PRIORITY`
  ⇒ 被自己的注释判红。改走 **AST** + 补 `J7d` 专守此点。

## 四、`_original_rooms.json` 修正（原作房间表）

- 依据 `_evidence/scr_roomname_覆盖核对.txt` + `ch5漏登记与ch2多登记.txt`：
  **ch2 多录 2 条（199/200）→ 删**；**ch5 漏录 5 条（205/222/224/225/230）→ 补**。
  修正后逐章条数 = **20/20/9/20/26**，与 `scr_roomname` 分支数一致。
- ch5 的 rooms 顺手按 id 升序排好（原先 144 排在 150 之后）。
- `ch5.areas.flower_castle` 并上 `"Top of Castle"` 前缀 —— 已核对：那 5 个房间在全量房间表里
  都是 `cls=scene, area_id=flower_castle` ⇒ **索引不需要新增区域**。
- `meta.corrections` 增补 4 条留痕；**文本级替换**（不 json.dump 重排），保持纯 LF。
- 新增回归锁（`第36轮/verify_scene_route_original.py`，55 → **57** 项）：
  `A4b` 逐章条数 == `scr_roomname` 分支数；`A4c` 每章 id 唯一。

## 五、回归与复检

- **G2：32 套件 / PASS=1517 / FAIL=0 / 全 IDENTICAL**（1505 → 1517）。
- 复检共 4 轮：21/21、36/36、11/11、15/15；真机 10/10；记忆 38/38。
- ★ **本轮 8 次报红/报绿全在判据侧**（目录名拼错 / 反面控制选错 / 口径定错 / 只数内存不查磁盘 /
  裸 JSON 形状弄错 / `A in rev[B]` / **被自己的注释判红** / **行号漂移**）。
  ⇒ `round34b_move_beat` 的 A2 判据消息**不再印绝对行号**，改报**处数**（否则 main.py 头部一插行就假 DIFF，
  而假 DIFF 会养成"看都不看就 --update"的坏习惯）。

## 六、仍未完成（需用户先定口径，不擅自动手）

1. **#17：926 个新场景的真实背景素材** —— 原作多数房间本就没背景精灵，硬造属**编造**，违反
   「算不出 → 绝不伪装」。当前刻意 `bg: null` + `bg_source: "none"`。
2. **路由重建**：现 26 条只覆盖 ch1~ch5 主线；1,014 场景的可达面未铺开（AI 能去哪是设计决策）。
3. `区域映射_复核.txt`（66 KB）待用户过目 —— 命名口径 = **不译**。
4. 78/87「区域代表素材」近似够不够 + 平铺画布口径是否接受。

## 七、提交

`6ea5998` / `f117aed` / `7e848f1` / `270e970` / `af12a3b` / 本轮收尾（P0 接线 + 3 瑕疵 + 房间表修正 + 报告）。

---

# 第 39 轮（同日续）：全量场景「真背景」落地 + 选层复核

> 项目根报告：`第三十九轮全量场景真背景落地与选层复核报告_2026-09-24.md`
> 用户口径：睡前提了「**我要睡了…我设置了几个等待发送的任务你就做吧**」+ 第 34 轮遗留
> 「测完了没问题后按计划自己推进」⇒ 连续自主推进，但**只做不需要用户拍板的那一半**。

## 八、起点：先核记忆，判定"不该压"

- 会话开头又出现 `MEMORY.md ... truncated during injection`。**先量再压**：
  `recheck_mem38.py` → **40 PASS / 0 FAIL**，实测 **21,844 字节 / 112 行**，在 200 行 / 25,000 字节内。
- 且**注入内容本身是旧快照**（里面还是 `PASS=1500` / 88 场景 / 无 §11）⇒ 提醒来自旧状态。
- ⇒ **本轮不压记忆**（按用户口径「不影响读取就别动」）。第 38 轮的教训：据错判据压缩＝净损失。

## 九、做了什么：只做"零编造"那一半

第 38 轮把场景扩到 1,013 个时，926 个新场景写 `bg: null` —— 那是刻意的诚实。
本轮把口径切成两半，**只落地能零编造的那半边**：

| 档次 | 判据 | 数量 | 动作 |
|---|---|---|---|
| **真背景** | 房间自带 `bg_sprite`/`asset_sprites` 且 ≥300×200 | **157** | ✅ 原样复制原作 PNG |
| 近似 | 只能靠"区域代表素材/章节兜底" | 856 | ⛔ **不动**（等口径）|

- 新增 PNG **148**；`bg/` **88 → 236**（`os.listdir` 真实计数）；0 出错；
  重新读盘复核 **235/235 字段一致**。
- 接线：87 锚点（补 `bg_source`/`bg_asset`）+ **148 分片条目**；顺手修正 **87 处过期注释**
  （`why_bg_is_a_placeholder` 写"文件尚不存在"，而第 37 轮早已生成 ⇒ 一句会误导后人的假话）。
- `git diff` = **+705 / −383**，**精确等于设计值**（148×3+87×3 / 148×2+87×1）
  ⇒ 文本级逐行替换**只在目标行动了手**。

## 十、判据的工程化：单一真源 + 可复现证据

- `_tools/bg_common.py :: classify()` = 真/近似判据的**唯一实现**（与第 37 轮 `pick_asset` 逐字同口径）。
  套件**不重写**判据，而是拿溯源表重建入参再复算。
- ★ **溯源表 `原作房间背景溯源.json` 蒸馏入库**（893 KB）：让套件**不依赖
  `E:\Download\_tmp`**（那是"用后即删"目录）。否则临时文件一清，套件**不是报红而是静默失去鉴别力**。
- 自检：溯源表重建 `classify` vs 原始 dump，**1,013 场景全部一致**。

## 十一、本轮判据侧 3 次失真 + 1 次我的错误推断（★ 教训）

| # | 表象 | 真凶 | 处置 |
|---|---|---|---|
| 1 | 溯源表自检 **844 条不一致** | 蒸馏尺寸表只装"被引用的精灵"，而 `chapter_tiles` 分支用**全量** `bg_*` ⇒ 重建掉档 | 尺寸表改装全量；转 **全部一致** |
| 2 | 新套件 B1/B3 报红 **78 条** | 判据**越权**：把"非真背景一律要求 `bg=null`"套到了 78 个第 37 轮口径的近似锚点上 | 改三分支判据（真/锚点近似/新增场景）|
| 3 | 负控制 B2 变"恒真" | 受害对象挑了**锚点**，而锚点分支不看真/近似差异 ⇒ 篡改不可见 | 受害对象换**分片真背景场景** + 补 B2b |
| 4 | H1 报红 **148 条** `load_scene→None` | **夹具违反产品契约**：喂裸 `_index.json` 条目（缺 chapter_id/area_id） | 改用产品函数 `load_index()` 的合并条目 |
| 5 | 我推断"素材是半分辨率、靠 `xscale` 放大" | **数据推翻**：87 个 320×240 里 **84 个房间本身也是 320×240**（宽比 1.00） | 交付物措辞全部改正 |

> 第 5 条最值钱：**"能直接算的别推算"**。不查房间尺寸，报告里就会留一句
> 听起来专业、其实错误的结论。正确结论是「**原作房间本身有尺寸分级**」。

## 十二、关键发现（要转成下一轮的行动）

1. ★ **真背景覆盖上限就是 15.5%**（157/1013）。856 个近似场景里 **634 个所属房间有瓦片层**；
   全量 1,251 房间中 **689 个有瓦片层** ⇒ 想更忠实只能做**按原作瓦片数据还原房间画面**。
2. ★ **P1 前置**：现有 `dump_rooms.csx` **没导出背景层几何**（`x/y/xscale/yscale/tiled/speed/depth/层序`）。
   实测 6 个房间的素材宽度不足房间宽一半（如 `dw_city_traffic_4` 素材 480×480 / 房间 4600×480）
   ⇒ 那些层是**平铺重复绘制**，光有图铺不满 ⇒ **P1 开工前必须扩 csx + 重跑五章 UTMT**。
3. **选层口径复核**：20 个房间有多个背景层；取到的不是最大层的**只有 1 个**；
   ⚠️ 2 处待复核（`torielclass` 取到**日语变体** `bg_lang_ja_torielclass`；
   `dw_castle_restaurant` 取到"柜台"层）—— **本轮不擅自改**（语言门控在 GML 里，没运行时证据）。

## 十三、验证

- 新套件 `verify_bg_round39.py`：**30 PASS / 0 FAIL**（8 负控制 + 3 正控制 + 4 项格式保真 + 产品路径 H1–H3），已进 G2。
- 全量 G2：**33 套件 / PASS=1547 / FAIL=0 / 全 IDENTICAL**（1505 → 1517 → **1547**）。
- 目视复核：157 格缩略总览图（`_evidence/真背景总览.png`）—— 绝大多数是可辨认的原作房间画面。
- `.pyc` 漂移 0；工作区干净。

## 十四、仍未完成（**必须用户拍板**）

- **Q1（最大一件）**：856 个场景的画面怎么办 —— A 保持留空 / B 按原作瓦片还原 / C 沿用区域代表素材近似。
- Q2：`ch1.kris_room` 的 bg 是 640×480 放大件（原作 320×240），是否换回原样。
- Q3：2 处选层待复核是否定向覆盖。Q4：78 个近似锚点是否与 Q1 一起升级。
- 路由重建（26 条只覆盖主线，可达面未铺开）—— 设计决策。

## 十五、第 40 轮：注入上限真相 + 速查本第一次「按真判据」压缩

### 1. 真相（第 38 轮的判据是错的）
- 管 `<repo>/.workbuddy/memory/MEMORY.md` 的是 **`MemoryCollector.getWorkingMemoryContent()`**，
  判据 `content.length > MAX_MEMORY_CHARS(=1e4)` ⇒ **上限 10,000 字符（JS UTF-16 码元）**，超限 `substring(0,1e4)` **砍尾**。
- 用户级 `~/.workbuddy/MEMORY.md` 上限 **`MAX_USER_MEMORY_CHARS = 4e3` = 4,000 字符**。
- ❌ 第 38 轮读到的 `buildAgentMemoryPrompt`（`eh=200` 行 / `ef=25e3` 字节）管的是 **`.codebuddy/agent-memory/<agent>/MEMORY.md`**，**与本项目无关**。
- 取证方法：文案只在 **`resources/app.asar`（317 MB 打包态）** 里、**不在** `.unpacked/` ⇒ 必须按字节 `blob.find(needle.encode('utf-8'))`，命中后切 ±3 KB 窗口解码。

### 2. 实测（本轮）
| 文件 | js_len | 上限 | 结论 |
|---|---|---|---|
| 项目速查本 `MEMORY.md` | **14,159** | 10,000 | **超 41.6%，§11 整章被砍** |
| 用户级 `~/.workbuddy/MEMORY.md` | 3,214 | 4,000 | 余量 786，**不动** |

### 3. 压缩结果
- **14,159 → 9,904 字符**（限 10,000 ⇒ 余量 96）／16,220 B／73 行；共 17 个中间态。
- 真正有效的不是"改字"（前 12 版每版只省 300~600），而是**整块下沉**：§10 历轮索引 → 指针、§7 与 §11 重复的 P1 前置细节合并、§3② 建楼细则改指 §4.9。
- **零令牌丢失**：旧版 266 个候选令牌在「新速查本 ∪ 详版」**真缺 = 0**；脚本报的 2 个均为误报／有意删除（`216` = 旧版错判据里的 `24,216 字节`；`§11.6` = 判据太窄，详版写的是 `### 11.6`）。

### 4. 回修的过期结论
- 用户级 skill `agent-memory-compaction`（5 处）：上限判据改成 `js_len ≤ 10000`、给出 `js_len` 定义、两套机制并列陈述、补复现脚本名。
- 详版 §39.11：加**推翻横幅**；`200 行 / 25,000 字节` 整段划线；第 38 轮那笔账重算（11,446 → 12,252 **按真判据仍超限** ⇒ 方向对但远远不够，「那次压缩是纯损失」**不成立**）。

### 5. 顺带发现（本轮只记录、不改）
- 详版 §8.5 / §8.6 / 「## 13. 第二十四~二十六轮索引」**被重复插入两份**（L1452–L1866 与 L1674 / L1707 / L1867 起）。

### 6. 本轮未做（需用户拍板，见详版 §23.13.3）
- Q1（856 场景画面）/ Q2（`ch1.kris_room` 放大件）/ Q3（2 处选层）/ Q4（78 近似锚点）/ 路由重建 / BGM / 版权尺度 / `.gitattributes`；
- **P1 硬前置**：扩 `dump_rooms.csx` 导出背景层几何 + 重跑五章 UTMT。

### 7. ★ 本轮第三次"判据自己说谎"：回验退化成"自己比自己"
- 提交**之前**跑令牌回验：父版本 266 个令牌 ⇒ 报红 2 个（`216` / `§11.6`），**可比**。
- 提交**之后**再跑同一个脚本：`git show HEAD:` 拿到的已是新版 ⇒ 检查变成**拿新比新**，
  `missing = 0` 是**恒真判据**（假绿）。**修法** = 显式取父提交
  `git cat-file blob 7b9d10a:.workbuddy/memory/MEMORY.md`。
- 证据：`code-quality-audit/第40轮-记忆注入上限与压缩/_evidence/`（`令牌回验.txt` + 两个脚本）。
- 已把 (a) 必须对父提交取旧版、(b) 令牌匹配别写太窄（`§11.6` vs `### 11.6`）两条写进
  用户级 skill `agent-memory-compaction`。

### 8. 提交
- `774f7df` 主提交（记忆三件套 + 推送日志）｜`1117597` 补推送日志｜`c009e56` 证据归档 + 报告 + 详版补记。
- 三次均 `ls-remote == origin/main == 本地`，**双向一致 = True**；收尾工作区干净。

---

## 第 41 轮：原作房间复现（排序/连接/动态效果）可行性取证

### 1. 用户新增口径（★ 硬约束）
> ① 「**记忆不要随便修改，三思而后行**」
> ② 「**所有 room 排序和连接都按照原作，相当于场景复现，连动态效果也要做（如喷泉特效这类的）**」

① 自定门槛：动记忆必须 (a) 实证超限或被要求 (b) 先补详版 (c) 逐令牌回验 (d) **改动守恒** (e) 说明必要性。
② 把第 39/40 轮悬置的 Q1 拍板 → **方向 = 场景复现**（非留空、非近似），且含**动态效果**。

### 2. 取证（ch1 = 147 房间；UTMT 在 data.win **副本**上 load，跑前后 sha256 一致）
```
层总数=1014  Instances 588 / Assets 234 / Background 191 / Tiles 1
几何非零的层=16
Sprites=1097  总帧数=2965  **多帧 sprite=523**
tilesets=59   其中 **22 个含多帧 GMS2TileIds**（样例 bg_schooltiles_tileset 108 帧 / 66666μs）
Code=1680  指令=290546  **room_goto 字面量=94**（+10 变量 +7 next +3 prev），不同目标房间=58
```

### 3. ★ 三处更正
- **瓦片数据其实在**：`LayerTilesData.TileData` 是 `UInt32[][]`（TilesX×TilesY）。上轮
  `dump_rooms.csx` 用 `ResolveName()` 处理，把 `UInt32[]` 打成字符串 ⇒ 五章 **89,237** 个瓦片元素
  全变 `"UInt32[]"`。**是脚本 bug，不是原作没数据** ⇒ Q1-B「瓦片还原」的死结解开。
- **层几何真名 = `XOffset/YOffset/HSpeed/VSpeed`**（+ 层序 = `Layers` 下标 + `LayerDepth`）。
  我上轮记的 `x/y/xscale/yscale/tiled_h/tiled_v/speed` 是**推测，错**。`xscale/yscale` 根本不存在。
- **ch1 画面主载体是 `Assets` 层 `LegacyTiles`**（234 层）而非瓦片层（仅 1 层）；
  上轮只导了精灵名、没导 `Tile{X,Y,SpriteDefinition,SourceX,Y,Width,Height,Depth,ScaleX/Y,Color,Tpag}` 几何。

### 4. 动态效果三条来源
A. Sprite 多帧（523 个）→ 按 `Textures[i].Texture`（UndertaleTexturePageItem）裁帧，`GMS2PlaybackSpeed` 播；
B. 瓦片动画（22 个多帧 tileset）→ 按 `GMS2FrameLength` 换 `TileData` 的 tile id；
C. 实例驱动（五章 **98** 个特效 obj：`obj_fountainkris`/`obj_darkfountain`/`obj_ch5_DW45_fountain`/…）→ 需复刻 GML 状态机或用帧序列近似。

### 5. 连接图两层
① `room_goto(字面量)` 静态可提（ch1 94 条）；② `Data.Rooms` 下标（next/prev 依据）；
③ 运行时 `scr_valid_room`/`scr_add_valid_room`/`scr_get_valid_room`/`scr_get_room_list`/`scr_get_room_by_id`
—— 原作自建「可达房间表」。⇒ 与 `scene_routing.py`（26 条）的关系：**原作图为真源**，我们的降为适配层。

### 6. ★★ 探针不保真（连接图 v1 是错的）
`Kind` 实际取值是 `"PushI"/"PushGlb"/"PushBltn"`（**无点**），我却当 `"Push"` 匹配；常量在
`ValueShort`(Int16)/`ValueLong`(Int64)，我却读 `ValueInt` ⇒ 107 条边里 **91 条垃圾值**
（`?-1610612620`、`?231188`）。改 `kind.StartsWith("Push")` + 按 `Type1` 选字段 ⇒ 94/104 落到真名、0 越界。
**教训：任何"提取成功"的结论，先看样例像不像真的。**

### 7. 复检（脚本 `recheck42.py`）
js_len=**9,933**（限 10,000，**余量 67**）／无 BOM／`fffd`=0／纯 LF／12 标题齐、`glued` 空／
**令牌回验 missing=0**／速查本独有=0／工作区干净（提交前）。
★ 修掉"判据太窄"：令牌匹配改成 `§` 可选（详版写 `### 11.5`，速查本写 `§11.5`）——同类误报**已第 3 次**。
宽匹配后 **missing=0、速查本独有=0**（此前报的 15~16 个"独有"全是同一类误报）。

### 8. 提交
- `655806a`（主：记忆三件套 + 报告 + 取证归档，11 files / +1536 −11）
  ｜`ls-remote == origin/main == 本地`，**双向一致 = True**；收尾工作区干净。
- 用户级 `~/.workbuddy/MEMORY.md` 追加「记忆不要随便修改，三思而后行」的**五道门槛**（跨项目偏好）。
- 产品 `src/` **零改动** ⇒ 本轮不跑 G2。

## 第 42 轮（同日续）：桌面接入与小镇联通 —— 原作「门机制」全解 + 五章拓扑

> 项目根报告：`第42轮报告-桌面接入与小镇联通.md`
> 取证归档：`code-quality-audit/第42轮-原作拓扑取证/{_tools,_evidence}/`（14 工具 + 24 证据 / 1.6 MB）
> 详版：`§43`

### 1. 用户口径（★ 硬约束，全部原话）
- 承接 41 轮：「**记忆不要随便修改，三思而后行**，然后，**记得所有 room 排序和连接都按照原作，相当于场景复现，连动态效果也要做（如喷泉特效这类的）**」
- 本轮新增：「**桌面作为初始 room，到时候联通小镇的 room 就好，联通的话就按照原作那个传送门来就好**」
- 我用 AskUserQuestion 追问出三个拍板：
  ① 「原作那个传送门」= **暗之泉（Dark Fountain）**（`obj_darkfountain` / `spr_fountainedge` / `visible:True`）
  ② 「小镇」范围 = 「**和原作一样，暗世界也做**」⇒ **推翻我第一次读 B 时做的"只联通小镇"收敛**
  ③ 桌面出去第一站 = **城北主街 `room_town_north`**
  ⇒ 最终范围 = **原作全部 1,251 间（含暗世界）**

### 2. ★★★ 最值钱的发现：原作「门」= 沿 `Data.Rooms` 下标位移
全部反汇编实证（`probe42.csx` / `probe42b.csx`）：
```
obj_doorA[/musfade]  -> room_goto_next()                              => index + 1
obj_doorB[/musfade]  -> room_goto_previous()                          => index - 1
obj_doorC[/musfade]  -> room_goto(room_next(room_next(room)))         => index + 2
obj_doorD[/musfade]  -> room_goto(room_previous(room_previous(room))) => index - 2
obj_doorE            -> room_next x3                                  => index + 3
obj_doorF            -> room_previous x3                              => index - 3
obj_doorX[/musfade]  -> 表驱动双向  if (room==X) room_goto(Y)
obj_doorW[/obj_doorw_musfade] -> 表驱动双向
obj_doorAny[/Horz/Vert] -> 完全没有代码 = 纯占位标记（830 个实例）
```
三条推论：
① **「排序」和「连接」在原作里是同一件事**（连接就是下标运算）—— 正对上用户那句「相当于场景复现」；
② **`obj_door*` 全部 `visible: False`** ⇒ 门**不渲染**，画面上的门画在**背景图/瓦片**里，门对象只是**不可见触发区**；
③ **落点由配对的 `obj_marker*` 决定**（`doorA` 出去 → 落在目标房间的 `markerA`）。

★ **方法论突破**：不逐条追 `room_goto` 的常量，而是**按 `Cmp`(源房间) 配对 `Call room_goto`(目标)** ⇒ 一次拿到「(源,目标)」二元组。
一致性验证：ch1 用此法得 `items=104`（94 字面量 + 7 next + 3 prev），**与 41 轮 edges41 完全吻合**。

### 3. 五章全量连接还原
| 章 | rooms | codes | with_room_goto | items | 有向边 | 无向去重 |
|---|---|---|---|---|---|---|
| ch1 | 147 | 1,680 | 44 | 104 | 238 | 123 |
| ch2 | 278 | 6,707 | 66 | 268 | 323 | 201 |
| ch3 | 246 | 9,258 | 106 | 165 | 79 | 47 |
| ch4 | 328 | 12,232 | 114 | 172 | 72 | 42 |
| ch5 | 252 | 18,016 | 91 | 136 | 70 | 42 |
| **合计** | **1,251** | **47,893** | **421** | **845** | **782** | **455** |

★ **ch1 现实世界完整拓扑 = 27 条无向边**（含实测坐标，全表在报告 §2 / 详版 §43.3）。
主链：`krisroom─A→krishallway─C→torhouse─X→town_krisyard─C→town_north─C→town_mid─C→town_south─A→town_school`。
★ ch1 **无门的 3 间**：`room_torroom(4)` / `room_town_northwest(8)` / `room_town_apartments(12)` ⇒ **原作 ch1 不可达**。

### 4. ★★ 小镇拓扑五章完全一致
`obj_doorX` / `obj_doorW` 的 town 表在五章**逐字相同** ⇒ **只需实现一套拓扑，五章共用**。
ch2+ 只新增店铺：`room_lw_conbini↔town_mid`（便利店）、`room_lw_police↔town_south`（警局）、
`room_lw_icee_pizza↔town_mid`（冰淇淋披萨）、`room_town_noellehouse`（诺艾尔家）。

### 5. 本轮新发现 / 更正（5 条）
- ★★ **`_original_rooms.json` 的 `id` == `Data.Rooms` 的 `index`**（实测 id 2 = `room_krisroom`、id 45 = `room_castle_town`）⇒ 两套数据可直接对齐。
- ★★ **五章「现实世界房间序列」完全一致**（都以 `room_krisroom / room_krishallway / room_torroom / room_torhouse / room_torbathroom / room_town_krisyard / room_town_northwest / room_town_north / room_beach / room_town_mid / …` 开头）⇒ **"排序按原作"的直接证据**。
- ★ `obj_doorAny`（830 实例）**完全无代码**；暗世界换房由 `obj_mainchara` 承担（ch2~ch5 各 7~8 处 `room_goto`，ch1 无）。
- ★ `obj_overworldc`（主控制器，702 指令）**不含任何 room 函数** ⇒ 横向走道切换不是主控制器做的。
- ★ `Data.Sequences = 0` ⇒ **"序列动画"这条路不存在**，动态效果只能靠 Sprite 帧 / 瓦片动画 / 实例。

### 6. 数据资产（自检全 PASS）
- `_room_order.json`（**122,612 B**）：1,251 间全序 `index/name/kind`
- `_room_graph.json`（**144,819 B**）：782 边 `src/dst/door/kind` + `rules` + `caveat`
- 自检：每章 `边数一致 / 下标合法 / 无自环 / 边内冗余名与表内名一致`；五章房间合计 = **1,251** ✓

### 7. 探针侧的坑（本轮 3 个，全是工具侧）
- `probe42.csx` **编译期 `CS7036`**：`ResName(GO(GO(inst,"ObjectDefinition")), "Sprite")` 内层 `GO` 少一个参数 ⇒ **CSX 任何编译错误整脚本挂掉、零输出**。
- `probe42.json` 无法 `json.loads`：C# `bool.ToString()` 给 `True/False`（大写） ⇒ Python 端 `.replace(': True', ': true')`。
- `emit42.py` `FileNotFoundError`：`graph42.json` 落在 `_tmp` 全局而非每章目录 ⇒ 顶部预加载。
- `cp` 归档报 `cannot stat`（2 个文件）：产物实际在 `_tmp`；且 `&&` 短路导致后续未执行 ⇒ **拆两段重跑**。

### 8. 复检（脚本 `recheck43b.py`）
- `MEMORY.md` js_len = **9,933**（限 10,000，余量 67）／无 BOM／`fffd`=0／纯 LF
- 详版 §43 结构 **43.1–43.8 齐**，二级标题链完整
- **逐令牌回验：旧版 328 令牌 → missing = 0**（3 条整路径为**组件回退 OK**，非真丢失）
  ★ 修正：`4.68`（7B 模型 4.68 GB）在压缩中真丢了，已**下沉到详版 §20.2**

### 9. 记忆侧（守恒式压缩，第 41 轮口径）
- 详版：追加 **`## §43`**（313,929 → 334,131 B，+147 行）
- 速查本：**整块下沉**（§7 十条并七条、§8 压两行、§11 五项合并、§3/§4/§5/§6 逐项收紧）
  ⇒ 逐字改写几乎不省字符（4 版仅省 100~370）⇒ 结论：**必须整块搬，别挤字**

### 10. 提交
- HEAD 之前 = `5365941`；本轮主提交 = **`921e1c5`**（43 files / +36,336 −59）
  —— 记忆三件套 + 报告 + 取证归档（14 工具 + 24 证据）
- 推送核验：`5365941..921e1c5  main -> main`；`ls-remote == origin/main == 本地` ⇒ **双向一致 = True**
- 提交信息逐字节核验：无 BOM / 2,687 B / 42 行**逐行一致 = True**；`parent = 5365941` ✓
- 产品 `src/` **零改动** ⇒ 本轮不跑 G2
- ★ 本轮完整交付物清单（38 文件 / 1,526,606 B 取证 + 报告 11,040 B + 详版 +§43）
  ⇒ 41 轮遗留的「连接边」缺口**已解决**，`dump_rooms.csx` 缺口从 5 项降为 4 项

---

## 第 43 轮（同日续）：原作「房间切换」与「背景移动」机制全解（反汇编取证）

### 1. 用户口径（本轮唯一新指令）
> 「**具体 room 切换和背景如何移动可以看原作的代码**」

⇒ 明确把"房间切换的具体表现"与"背景如何移动"指向**原作代码** = 要求反编译 `data.win`。
承接 42 轮（"连到哪"）⇒ 43 轮解决"**切的时候长什么样**"与"**画面怎么跟着动**"。

### 2. ★★★ 结论 1：背景移动 = 相机平移，不是视差
- ch1 **1014 个层**里，`HSpeed`/`VSpeed` 非零 = **0** ⇒ **零视差**
- `EffectEnabled=True` 的层 = 1014，但 `EffectType` 非空 = **0** ⇒ **无 GMS2 层特效**
- 16 个几何非零层全是 `Compatibility_Background_*`（GMS1→GMS2 迁移产物）⇒ 只有静态偏移
- ⇒ **背景/瓦片/实例全在同一房间坐标系，相机一移全都跟着移**

### 3. ★★★ 结论 2：相机尺寸与窗口
- `GeneralInfo.DefaultWindowWidth=640 / Height=480`、`Options.Scale=1`、**`GMS2FPS = 30`**（★决定所有计时换算）
- ch1 每房间 `view[0]`：**640×480 port 640×480 = 100 间**（暗世界）；**320×240 port 640×320 = 33 间**（现实世界，2× 放大）；320×240 port 640×480 = 7；1024×768 = 6；640×768 = 1
- `view[0].obj`：115 空 / 32 = `obj_mainchara` ⇒ **不能用 `view.ObjectId` 判断有无相机跟随**（真正跟随在代码里）
- **房间宽 > 320 的 = 121/147（82%）**；最宽 `room_dark_chase1` = 5400

### 4. ★★★ 结论 3：每帧相机更新（`gml_Object_obj_mainchara_Step_2`，319 指令）
```
现实世界（global.darkzone != 1）: wd = x - floor(view_wview[0]/2 - initwd/2) ; ht = y - floor(view_hview[0]/2 - initht/2)
暗世界（global.darkzone == 1）:   view_object[0] = -4（关内置跟随）; wd = x - floor(view_wview[0]/2) + 11 ; ht = y - floor(view_hview[0]/2) + 17
两者后都是四向钳制: 0 ≤ view_xview[0] ≤ room_width - view_wview[0] ; 0 ≤ view_yview[0] ≤ room_height - view_hview[0]
```
★ **硬跟随、无插值**（一帧到位）。

### 5. ★★★ 结论 4：相机操作唯一收口 = `__view_set_internal`
- GMS1 风格变量 → `__view_get(prop,index)`（476 次 / 123 调用者）→ `__view_set`（61 次 / 21 调用者）→ **`__view_set_internal`** ← 唯一调 `camera_*` 的地方（prop 0~16 分派器，224 指令）
- 6 个 `camera_set_view_*` **各只有 1 个调用者** = `gml_GlobalScript___view_set_internal`
- **`camera_create()` 只被 `gml_GlobalScript___init_d3d` 调 1 次** ⇒ **整章全局一台相机**，切房只改参数不重建

### 6. ★★★ 结论 5：房间切换完整时序（五章共用）
```
① 碰到 obj_door*（触发区；全部 visible=False ⇒ 门画在背景/瓦片里）
② Other_19（Room Start）: instance_create(0,0,obj_fadeout) + mus_volume 渐降 ; alarm[3]=14 ; alarm[2]=15 ; touched=1 ; interact=3
③ Alarm_3（第14帧）: snd_free(currentsong, 0, -5)   ← 停 BGM
④ Alarm_2（第15帧）: entrance=1 ; instance_create(0,0,obj_persistentfadein)[persistent=True] ; room_goto_next() / room_goto(room_next(room_next(room))) / 表驱动
⑤ 新房间 Room Start: persistentfadein 重算 length=10+room_w/4, height=10+room_h/4, x=y=-20
⑥ 每帧 Draw: image_alpha += -0.08 ; draw_sprite_ext(spr_pixel_white, image_index, x, y, length, height, 0, image_blend, image_alpha) ; image_alpha<=0 -> instance_destroy()
```
- `image_blend = 0`（纯黑）；**淡出 15 帧 = 0.500s + 淡入 1/0.08 = 0.417s ⇒ 共 ≈ 0.92s**（@30fps）
- **16 个 `obj_door*`（A~F/X/W，含 `_musfade`）逐字同构**；ch2+ 在 `Alarm_2` 多 `event_user(7)`、`Other_19` 多 `event_user(8)`
- ★ **无传送门式过渡**：`scr_transition` / `scr_fade` / `scr_room_change` **均不存在**

### 7. ★★★ 结论 6：对象索引【跨章不稳定】
| 章 | 对象总数 | `obj_fadeout` | `obj_persistentfadein` | `obj_mainchara` |
|---|---|---|---|---|
| ch1 | 353 | 143 | 145 | 30 |
| ch2 | 1328 | 193 | 195 | 84 |
| ch3 | 1732 | 1299 | 1301 | 1185 |
| ch4 | 1576 | 1170 | 1173 | 1050 |
| ch5 | 1733 | 1308 | 1311 | 1198 |

**16/16 关键对象索引全部不一致** ⇒ ★★ **跨章引用只能用【名字】**；旁证：`obj_loadscreen`/`obj_roomcontroller`/`obj_fadechain` 在 ch2~ch5 不存在 ⇒ **反向验证 42 轮 `_room_order.json` 用名字的正确性**。

### 8. ★★ 对象创建普查（ch1 = 1306 个 `instance_create*` 调用点）
`obj_dialoguer` 450 / `obj_writer` 70 / `obj_dbulletcontroller` 46 / **`obj_fadeout` 45** / **`obj_shake` 45** / **`obj_persistentfadein` 37** / `obj_soliddark` 24 / `obj_darkener` 21 / `obj_npc_room` 16 / `obj_regularbullet` 15
★ `obj_backgrounder_*` 是**按需创建**（非每房间常驻）；★ `obj_fadechain` 只被 `obj_skychain_Step_0` 创建 1 次 ⇒ **与换房无关**（更正第一版假结论）。

### 9. ★★ 方法论（本轮踩 5 个坑，全是"探针不保真"）
① 嵌套类型必须带外类名（`UndertaleRoom+View`，写错 ⇒ `CS0246` 整脚本挂、零输出）；
② ★ 每脚本**两个 code**：`gml_Script_X`（空桩 ins=0）／`gml_GlobalScript_X`（真体）；
③ 调用者索引必须用**全名**（`gml_Script_scr_depth` 69 命中 vs 裸名 0）；
④ ★★ GMS2 字节码实参**从右往左压栈**；⑤ ★★ 数槽位**必须把变量 push 也算进去**（连错两版）。
⇒ ★★★ **先设计三个"已知真值"锚点，解析器过锚点才允许用它的输出**（本轮三重锚点全命中）。

### 10. 复检 + 记忆侧 + 交付
- 复检（`recheck43c.py`，基准 `1440508`）**六类判据全 PASS**：无控制字符／12 标题齐+无超长行／无 BOM·无 FFFD·纯 LF／无占位／**逐令牌 missing = 0**／`js_len = 9,984`（限 9,999）
- 详版：追加 **`## §44`（44.1–44.9）** + 回填第 42 轮 `4.68` + §44.9 下沉回填 3 令牌；速查本：整块下沉（§7 十条并八）＋ 43 轮增量
- 交付：项目根 `第43轮报告-房间切换与背景移动机制.md`（4 处已更正）＋ `code-quality-audit/第43轮-原作门与相机取证/{_tools(18 个), _evidence(28 个)}/`
- 产品 `src/` **零改动** ⇒ 本轮不跑 G2


