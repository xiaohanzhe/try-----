# 2026-09-17 工作日志

## 第十五轮：「建楼」死代码清干净 + 一个被错判的坠落起因

报告：`代码质量复审报告_2026-09-17_第十五轮.md`；自检 `code-quality-audit/第十五轮/verify_round15_cleanup.py`（30 项）；
真机冒烟 `第十五轮/_probe_smoke.py`；证据 `第十五轮/_evidence/`（8 份）。提交 **`1b279d3`**（18 文件 / +1073 −283）。

用户原话："你自己检测完后就继续按照原计划进行吧" —— 即：上一轮报告 §八 的两条待办你自己拍板处理，
验完就往下走。于是我把范围扩成"**把建楼遗留的死代码清干净**"，并在过程中逮到一个真 bug。

### 起因：上一轮的两条待办 + 一条自己发现的
第十四轮报告 §八 留了 ① 清 `check_nearby_windows` 尾部旧裸矩形兜底段 ② 处置
`is_on_floor_edge` / `find_support_below` 两个零调用函数。查代码时又发现
**`update_floor()` 也是零调用**（第五轮 F3 早就点名过，且是 H4/H5 排期方案 §G1 的优先清理项），
以及 **`is_floor_valid()` 同样零调用**（第五轮死引用清单里有），而它恰好是修下面那个 bug 所需的判据。

### 删了什么（净减 228 行）
| 对象 | 行数 | 为什么删 |
|---|---|---|
| `main.py::check_nearby_windows` 尾部裸矩形启发式 | 154 | 第十三轮把口径换成"可见区域"后它是**旧口径的活样本**（跳到被遮部分 / 站在窗口上把目标声明成桌面 = 穿透）。第十四轮的早返回让它不可达，但谁挪掉早返回 bug 立刻复活 |
| `main.py::update_floor` | 67 | 零调用 + 与 `check_window_movement` 逐行重复且已漂移（多一段"桌面被新窗口覆盖"、少一段楼层身份判定） |
| `floor_manager::find_support_below` | 13 | `get_drop_destination` 的重复实现，零调用 |
| `floor_manager::is_on_floor_edge` | 9 | 无任何消费者 |

`main.py` 9373 → **9172** 行（+40 / −241）。

### 修了什么（真 bug，对应建楼要求第 36 行）
`check_window_movement` 里"脚下不再是窗口楼板"原来**一律**按 `reason='floor_removed'`（用户行为）→ 生气动画。
但有两种成因：
- 宠物**自己走到了楼板边缘之外**（窗口还开着）→ 要求原文是"就直直掉下去，落到下面第一块能接住的楼板"，
  **不算用户的行为** → 该用常规动画；
- 用户**把窗口关了**（楼板被抽走）→ 该用生气的那组 ≥5s。

原实现把前一种也判成"用户行为"，于是**用户越是用鼠标把宠物赶到窗口边缘、它越会生气** ——
与要求"如果是我的行为导致他摔到桌面上的就用生气的那个"**正好相反**。

修法：接活 `is_floor_valid(old_floor)` —— 窗口还在 ⇒ 自己走出去（常规）；窗口没了 ⇒ 用户抽走（生气）。
**为什么不用"离边缘 ≤N px"**：楼层检查是 **1 秒节拍**，宠物一秒能走几百像素，距离阈值必漏判；
"窗口在不在"与节拍无关，稳。`round8_floor` 的 W6b（关窗必须带 `floor_removed`）仍然 PASS，要求没被动。

### 有意保留（怕误删）
- `start_jump` 的裸窗口回落分支：**还有两个活调用方**（`climb_to_top_window()` / `command_manager.py:78`）→ 留。
  套件 A5b 专门锁这条。
- `update_floor` 独有的"桌面被新窗口覆盖 → `found_window` 情绪"：**它当前本就不可达**（`update_floor` 从来没被调用），
  所以删除没有造成回退。要不要恢复成新行为，留作待办。

### 验证
- 自检 30/30 PASS：A 死代码确实清了（含"不许误删"）/ B 跳跃单一入口（stub **故意不给**
  `desktop_interaction`，偷回老路立刻 AttributeError）/ C 坠落起因分流（行为级 + `is_floor_valid` 真被调用）
  / D 权威路径未误删。
- 真机启动路径冒烟（**真的 `RalseiPet()`**，不是 stub）：实例化 / 楼层刷新 / `check_window_movement` ×3 /
  `check_nearby_windows` / 重力坠落到桌面（44 帧落地）/ 跳跃规划 —— 全过。
- G2：18 套件 697 → **19 套件 728 PASS / 全 IDENTICAL / exit=0**。
  两处**有意变更**（diff 已逐行核对）：round13 71→72（C6 改在唯一权威 `floor_visible_contains` 上断言
  + 新增 C6b 锁"零调用重复实现不该长回来"）、round14 G5b 文案变更（不再断言死代码里的字符串）。
  其余 17 套件 IDENTICAL，**含 `round8_floor`：给它补了 `is_floor_valid` 桩但输出一字未变**（证明补桩没改变结论）。
- 证据编码 `--strict`：10 个文件 **0 损坏 0 告警**。

### 教训（已写进 MEMORY.md）
1. **别把断言写在死代码上**：第十四轮 G5b 断的是 `update_floor` 体里的字符串，而那个方法**从来没被调用过**
   → 断言永远为真、什么也没保护。**断言死代码 ≡ 断言一个常量。**
2. **大块删除用"行级手术 + 断言 + 编译校验"**，不要手工重排版：同名
   `get_all_visible_windows()` 全文件 10 处 → 必须断言"取到的是 `check_nearby_windows` 那一处、
   上溯最近的 `def` 就是它"。另注 `main.py` 是 CRLF、`floor_manager` 是 LF →
   一律 `newline=''` 读写、匹配时 `rstrip('\r')`。
3. **桩必须跟着真实方法面走（第 3 次）**：本轮 `check_window_movement` 要调 `is_floor_valid` →
   `第八轮/FakeFloorManager` 缺它。补上后输出一字未变，正好反证"桩只是方法面的镜像"。
4. **死代码不只是占地方**：`check_nearby_windows` 尾部那段是"口径错误的活样本"，留着等于把 bug 冻在原地等人复活。
   处置口径定为：**要么接线、要么删**，不留第三种状态。查死代码最省事的两个来源：
   `第五轮/_evidence/_deadrefs_filtered_view.txt` + H4/H5 排期方案 §G1。

### 遗留 / 待办
1. 真机肉眼确认（只能用户做）：走到楼板边缘掉下去**不该再生气**；关窗掉下来**仍然**生气 ≥5s；
   上下跳 / 落地动作与上一轮一致（本轮没动跳跃几何）。
2. `climb_to_top_window()` 仍走裸窗口路径（故意没动：它挑最前面那个窗口，按定义不会被盖住）。
   建议下一轮收编，或与用户确认这条命令还要不要。
3. `update_floor` 独有的 `found_window` 情绪当前不可达、本轮未恢复。
4. 程序目录三个本地备份：`main.py.bak` / `main.py.backup_before_fix` / `main.py.backup_before_deadcode_cleanup`
   （都被 `.gitignore` 覆盖、未入库）—— 与"程序目录历史开发日志"同类，**未擅自删**，等用户点头。
5. 历史证据编码 70 个文件（第五/六/八/九轮 + `架构改造-H4H5/`）仍在等用户点头。
6. **push 核验已闭环**：两次 push 自报成功
   （`21605d4..1b279d3 main -> main`、`1b279d3..cc8c7c5 main -> main`）；中途沙箱代理
   `127.0.0.1:57186` 对 github 持续 502（`ls-remote` 多次不通）；代理恢复后复核
   **远端 == 本地 == `cc8c7c5`**。教训：代理抖动时不要只看退出码，但也别放弃 —— 等它恢复补一次 `ls-remote`。

---

## 第十六轮（同日）：「建楼」要求**行为级复检** + 落缺口计划（只读，未改产品代码）

用户原话："复检一下到底实没实现那些要求，没有的话那就把它也加到计划里就好"。

对账对象是项目根那份 `"建楼"要求`（37 行）。前两轮（第十三/十四轮）各做过一次"逐条对表"，
但都是在**阅读代码**上下的判断；这轮改成**行为级**：把每条要求写成断言跑出来。

### 复检结论：13 条里 7 条已实现 / 5 条部分 / 2 条动画要求实际不成立 → 6 个缺口

- 已实现：①②③④⑥⑦⑫（证据 A1–A4b、B1–B4）
- 部分：⑤⑧⑨⑩⑪（缺口都在"下落路径"上）
- **⑬ 不成立**：要求第36行"生气动画至少 5s" —— `max_fall_duration` 全项目**只被 `handle_fall` 读**，
  但 `start_falling` 把 `is_falling` 置 False → 走 `handle_gravity_fall` → `handle_fall` 不跑
  → 那句 `5.0` 是**死参数**；落地 `trigger_splat()` 又用普通 `splat` 顶掉生气素材。
  实证：`splat_mad` 已配在 animations.json、也登记进两处"特殊动画"白名单，
  但**全项目零调用**（又一个"素材躺着没人播"）。
- **⑧/⑩ 缺口 = 要求原文第30行的例子**：`check_window_movement` 触发坠落那道门是
  `new_floor.get('type') != 'window'` → **只有"下面变成桌面"才掉**。
  "关掉记事本、下面还有浏览器"时不坠落、不播动画，宠物**瞬移**到浏览器那层。
  起因为分流而接活的 `is_floor_valid` 在这条路上**根本没被问到**（调用计数 0）。
  但落点几何是好的（`get_drop_destination` 会给出浏览器）→ **缺的是调用点，不是算法**。
- **⑤/④ 缺口**：走路无层高闸门 —— 站桌面上水平走进窗口的可见区域会被**直接提升**到该层，
  不跳不摔；`generate_new_move_target` 里完全不出现 `window`/`floor`（走路对窗口毫无感知）。
  修法需用户拍板（收紧 / 补过渡动作 / 维持现状）。
- 有意偏差（登记，不改）：⑨ 位移 >400px 不跟随改失足；`MIN_FLOOR_VISIBLE_AREA=1600px²`
  （要求 ③ 字面只说"完全盖住"才不存在）；⑩ 的"立刻"= 1 秒节拍。
- ⑦ 的修法方向定了但**没做**：把坠落判据从"new_floor 是不是窗口"改成**层高比较**
  （`new < old` → 摔），一次覆盖四种情形，且天然保留要求 ⑪「新窗口盖旧窗口→不摔」的例外。

### 交付
- `code-quality-audit/第十六轮/verify_build_req_audit.py`（只读复检套件，40 项断言：A 世界构建 / B 视野与跳跃 /
  C 重力与动态 / D 挪动窗口 / E 两条动画要求 / **F 缺口**）
  —— F 组**故意断言当前缺陷**（名字带 `[GAP]`），修好后必须翻转，防"缺口被悄悄修掉或当成已实现"。
- `_evidence/round16_audit_output.txt`（PASS=40 / FAIL=0）、`round16_g2.txt`
  （**19 套件 / 728 PASS / 0 FAIL / 全 IDENTICAL** —— 本轮没动产品代码，与基线逐字节一致）。
- 项目根 `建楼要求复检与缺口计划_2026-09-17.md`：逐条对账表 + 6 缺口 + **4 批次计划**
  （A：G2+G1 一起修"下落路径"；B：G3 层高闸门待用户拍板；C：G5 收编；D：三条偏差只登记）。
- 复检套件**不入 G2**（与第十三轮 `probe_*` 同规格）：它断言的是缺陷，挂进基线会把"断言缺陷"变成常态。
  批次 A 做完、F 组改写成"断言修好后的行为"之后再挂。
- 提交：`9b28526`（复检 + 计划）+ `53f0f8e`（补丁：证据去 BOM —— PS 5.1 的 `Out-File -Encoding utf8`
  **会加 BOM**，不合项目约定，改由 Python 剥掉重写）。`check_evidence_encoding.py` 现报"损坏 0 / 告警 0"。
  push 一次成功（`9b28526..53f0f8e main -> main`），`ls-remote` 复核**远端 == 本地 == `53f0f8e`** → 闭环。

### 教训
1. **"读代码对表"会漏两类东西**：① **调用点缺失**（G2：判据写对了，但那条分支的门条件把它挡在外面）；
   ② **参数写在不会被读到的那条路上**（G1：`max_fall_duration` 赋值在 `start_falling`、消费在 `handle_fall`，
   而两者互斥）。两者都只有**行为级断言**才现形。
2. **AST 统计 `self.<attr>` 的 Load 点**是查"死参数"最快的办法：`attr_consumers('max_fall_duration')`
   一次就给出 `{'handle_fall'}`，比全文 grep 赋值/读取清爽得多（赋值是 Store、不计入）。
3. **`code_only()` 会连字符串字面量一起剥掉** → 断言 `fall_mad` / `change_animation("splat")` 这类
   **必须用"只剥注释"的 `method_lit`**；本轮 E1/E2/G2b/G6 四条都因此先红后绿（第 2 次踩同一个坑，
   第一次是第十五轮的同一族问题 → 见 MEMORY「验证脚本教训」）。
4. **同一轮里"缺口断言"要显式标注**（名字带 `[GAP]` + 注释写"修复后本检查必须翻转"），
   否则绿色套件会被误读成"要求都满足了" —— 那正是这次复检要纠正的误判。

---

## 第十七轮（同日）：「建楼」缺口计划 · **批次 A** —— 把"下落路径"修对

报告：`代码质量复审报告_2026-09-17_第十七轮.md`；自检
`code-quality-audit/第十七轮/verify_round17_build_fall.py`（**18 项**，已进 G2）；
证据 `第十七轮/_evidence/`（3 份，编码 0 损坏 0 告警）。

用户原话："好的，那接下来继续按照原计划进行吧" → 我按"继续执行建楼缺口计划批次 A"理解；
用户随即澄清 **"原计划"指的是 H4/H5 上帝类拆分**，但**允许先把手上这批建楼缺口做完**
（"也可以先解决现在你所做的这些问题"）。所以本轮 = 批次 A，做完回 H4/H5。

### G2（原计划批次 A 的第一条）：触发坠落改**层高比较**

旧门条件 `old_floor.type=='window' and new_floor.type!='window'` → **只有"下面变成桌面"才掉**。
要求原文第 30 行的例子恰恰是另一种：关掉记事本后**下方还有浏览器** → `get_current_floor` 立刻把楼层
换成浏览器（仍是窗口）→ 整段被跳过：不坠落、不播动画、不生气，宠物**瞬移**到浏览器那层。
第十五轮接活的 `is_floor_valid` 在这条路上**根本没被问到**（调用计数 0）——**判据写对了，门把它挡住了**。

改法：`old_h/new_h = floor.get('platform_height')`，`new_h < old_h` → 摔；`new_h > old_h` → 不摔（要求 ⑪）。
四情形一次覆盖（走出边缘→桌面 / 关窗→桌面 / **关窗→下方还有窗口** / 新窗盖旧窗→不摔）。
起因分流保持 `is_floor_valid(old_floor)`：窗口还在 ⇒ 自己走出去（常规）；窗口没了 ⇒ 用户抽走（生气）。

### G1（第二条）：生气动画 ≥5s / ≥3s 从**三处叠加失效**修成真的成立

三处叠加：a) 时长写在 `max_fall_duration`，而它**全项目只有 `handle_fall` 读**，`start_falling` 又把
`is_falling` 置 False（走 `handle_gravity_fall`）→ **死参数**；b) 即便跑，`trigger_splat` 又覆盖成 3.0；
c) `handle_fall` 的 splat 阶段**硬编码 1.0s**，且落地 `trigger_splat` 恒切普通 `splat` → 把坠落途中刚播的
`fall_mad` 顶掉（`splat_mad` **全项目零调用**）。

改法：**由起因 `_fall_reason` 纯派生**，两个**模块级**函数（文件头）：
`_fall_splat_hold(pet)`（关窗 5.0 / 挪楼板 3.0 / 无起因 1.0）+ `_splat_animation_name(pet)`（用户造成 → `splat_mad`）。
`start_falling` / `start_fall` **只写起因**（删掉 `max_fall_duration` 写入）；`trigger_splat` / `handle_fall`
**只读**。另给**甩飞路径**（`mouseReleaseEvent`）补 `self._fall_reason = None` —— 否则上一次关窗的起因残留，
甩飞也会播生气版并停 5s。

### 为什么放模块级（本轮实测踩到的坑）

第一版写成 `RalseiPet._splat_animation_name(self)` → G2 一跑，`round6_verify`（39→20）与 `round8_fling`
**当场 `AttributeError` 崩在半路**：历史套件用轻量桩（`SimpleNamespace`）驱动
`RalseiPet.handle_fall(stub, ...)`，桩里没有这个新方法。改模块级后两套件**一字不差恢复 IDENTICAL**。
顺带把第一版另存的 `self._splat_hold` 属性也删了 —— 唯一真源就是 `_fall_reason`（项目头号铁律"消双真源"）。

### 验证

- 自检 **18/18**：A 组四情形 + 源码级判据已换；B 组落地素材 + **行为级时间推进**
  （真跑 `trigger_splat` + `handle_fall`，量出进入晕乎的时刻 = **5.0 / 3.0 / 1.0s**）；C 组单一入口 + 甩飞复位。
- G2：19 → **20 套件 / 728 → 746 PASS / 0 FAIL / 全 IDENTICAL**（`--update` 后复跑一遍复验，两遍 EXIT=0）。
  **唯一有意变更**：`round14_move` 的 G1 断言**改名**（"时长"→"**派生**时长"）。
  旧断言 `max_fall_duration >= 5.0` 断的是那条**死参数赋值** —— 一直是 PASS，却什么也没锁住。
- 第十六轮那份只读复检套件**不入 G2**（它断言的是修复**前**的缺陷），已在文件头加注"历史快照"。

### 教训

1. **"判据写对了"≠"判据被问到"**：查这类问题最快的办法是**数调用**，不是读代码。
2. **同一份状态别存两处**：时长/素材都由 `_fall_reason` 派生，立刻没有"该读哪个"的混乱。
3. **给产品代码加实例方法要想着轻量桩**（第 4 次同类坑）→ 纯函数落**模块级**。
4. **断言要断行为不要断赋值**：round14 那条绿了一年多而要求从未成立。能推进状态机量出来的，就别只查一行赋值。

### 遗留 / 待办

1. **真机肉眼确认（只能用户做）**：① 关掉站着的窗口且**下方还有窗口** → 应掉下去并生气 ≥5s；
   ② 挪动它所在的窗口 → 生气版摔倒 ≥3s；③ 走路踩空 / 被甩飞 → 仍是常规摔、时长没变长。
2. 批次 B（G3 走路层高闸门）仍**等用户拍板**；批次 C（`climb_to_top_window` 收编）、
   D（三条有意偏差登记）未动。
3. **下一步回"原计划"：H4/H5 上帝类拆分**（`架构改造排期方案_H4-H5_2026-09-13.md`，
   基线 `code-quality-audit/架构改造-H4H5/` 只读勿重测）。

---

## 第十八轮（同日）：「建楼」缺口计划 · **批次 B —— 补上层高闸门（G3）+ 换层全接跳/攀爬**

报告：`代码质量复审报告_2026-09-17_第十八轮.md`；自检
`code-quality-audit/第十八轮/verify_round18_climb.py`（**53 项**，已进 G2）；
证据 `第十八轮/_evidence/`（7 份：套件输出 / G2 两遍 / 4 份 DIFF / **素材实渲染图 + 图例**，编码 0 损坏 0 告警）。
**提交号：`4eae2d8`**（25 文件 / +1952 −21；push `8c739df..4eae2d8 main -> main` 一次过，
`ls-remote` 复核**远端 == 本地 == `4eae2d8`** → 闭环）。

### 本轮的用户口径（第十八轮拍板，全部落到代码里）

1. 楼层层高变换**必须靠跳**衔接（按字面收紧，不再"静默提升"）；
2. 走进**更低**楼层也一样 —— **下楼也用跳，不许用掉落**；
3. **预留水平距离**，"别看着和垂直起跳一样"；
4. 摔扁只在**层数比较高且掉下来（非主动）**时触发；
5. "**楼层跨度较低的时候跳，跨度高的时候爬**"；
6. 攀爬素材：`spr_ralsei_climb_1_*` 朝右 / `spr_ralsei_climb_0_degrees_*` 朝前 /
   反向"你给他翻转一下就好" / **没有朝后的**（"那样也用不上"）。

### 改了什么（`main.py` 为主）

- **层高闸门**（`check_window_movement`）：新增 `walk_induced = new_h != old_h and is_moving
  and is_floor_valid(old_floor)` → 先试 `_start_climb_transition`；**上楼够不着就保持原层**
  （不许退回静默提升），下楼够不着才服从重力（要求⑭）。入口**只用既有属性** → 历史轻量桩
  （无 `is_moving`）仍走被动路径 → 21 套件全 IDENTICAL 的原因。
- **跳/爬单点选型**：`start_jump` 里按 `_jump_kind_for_span(|Δph|)` 决定 —— ≤`CLIMB_SPAN_JUMP_MAX=5`
  用 `jump` 家族，更大用 `climb_*`；结果存 `_jump_anim_override`，`handle_jump` **每帧优先读它**
  （否则第二帧被 `jump` 顶掉＝白切）。两条入口共用一份判据。
- **落点"先让开再吸附"**：`_climb_hdir`（优先沿用走路朝向）→ 在 `cur ± CLIMB_HORIZONTAL_RUN(90)`
  各取候选 → `_climb_probe_landing` 吸附进**可见区域** + 位移上限 `CLIMB_LANDING_MAX_TRAVEL=320`
  → `_climb_landing` 两方向择优（`CLIMB_MIN_RESERVE=24`）。
- **摔扁门槛**：收敛成唯一入口 `_should_splat_on_landing`（两个落点共用）＝ 速度 >150
  **且** 落差 ≥ `FALL_SPLAT_MIN_DROP=10`（两层）。`_fall_from_height` 在进入坠落时记一次。
  `_landing_drop_height` **夹到 ≥0**（"落差"是物理量，不是有向坐标差 —— 负值会静默污染未来使用者）。
- **素材登记**：`climb_right/left/front` 进 `_builtin_animation_mapping` + `animations.json`；
  5 张 `spr_ralsei_climb_left_*` 由**水平镜像**一次性生成并**真落盘入库**（G5 断言与 right 组不是同一批文件）。

### 本轮最重要的取舍：**不做"逐层小跳"**（已断言化，勿回退）

用户还说过"不能一次性跳上跨度很高的楼层……只能相邻"。最直觉的"先跳隔壁那层再跳下一层"
**在这套几何下会把宠物卡死**：楼层名次＝z 序，宠物被判到第 N 层正因它的位置落在**最前面**那块板的
**可见区域**里 —— 那正是后面第 N-1/N-2 层**被挡住**的地方，中间层在该位置**没有可见区域** →
逐层第一步就吸附失败 → 每步 False → **宠物永远上不去**（"进不去窗口"，比改前更糟）。
故本轮对跨层**换素材不换落点**（攀爬素材 + 320px 位移上限）。
**I 组断言钉住前提**（I1：站在前层可见区域的位置，下层在同一位置没有可见区域；I2：两层名次确实不同）——
谁动了可见区域/名次口径，I 组先红，提醒回去读 docstring 再决定。

### 验证

- 自检 **53/0**，九组 A–I（+X0 源码工具自检）：A 上楼不静默提升 + 预留距离 / B 跨度选型与爬向 /
  C 下楼用跳不掉落 / D 关窗与要求⑪ 不被误伤 / E 闸门兜底两分支 / F 摔扁门槛（含夹非负）/ G 素材与登记
  （**G10 逐像素断言镜像关系 + G10b 判据自检**）/ H 源码级单一入口 / I 设计前提。
  > G5（"左右两组帧文件名不同"）**本身没有鉴别力** —— 把源图原样复制过去同样通过，而"没镜像"
  > 正是唯一要防的事。故补 G10 逐像素证镜像 + G10b 证"该判据对未镜像的图会报错"。
  > **定式：每条断言都要能回答"如果这件事根本没做，它还会绿吗？"**
- **G2：20 → 21 套件 / 746 → 799 PASS / 0 FAIL / 全 IDENTICAL**。
  固化**只更新自己那条**（`--update --only round18_climb`）：全量 `--update` 会把本轮之外的意外漂移
  一并固化成新基线、把真回归洗白。四处有意变更**全是组数引起的计数行**：
  `s1_anim_miss` / `s2_anim_json` / `s3_alias_legacy` / `round8_anim` —— 组 109→112、载入 489→493、
  帧 1461→1482、样本 501→505、导出 21319→22156 字节、表内帧 377→393。
  **+4 组 = 3 个表登记组 + 1 个自动扫描前缀组**（新落盘的 `spr_ralsei_climb_left_*` 按文件名前缀另立一组，
  既有行为，`spr_ralsei_climb_1`…`climb_6` 一直如此）；**+21 帧 = 16 + 5**。
  **`frame_container_size` 仍 `(222,110)`（两处来源都是）** —— 没碰 H4/H5 那条"用户可见画布"红线。
- **真机**：`src/main.py` 真窗口跑 30s → 日志只有 2 行启动 INFO（jieba + `animations.json 112 组`），
  该时段 **0 ERROR / 0 WARNING**，按 pid 干净收掉无残留。
- **素材实渲染图**（`_evidence/climb_frames_preview.png`）：用真 `SpriteLoader` 把三组画出来 ——
  朝右 / 朝左（镜像）/ 朝前，**无灰块占位**。

### 教训

1. **"测试自己坏了"比"测试没写"更危险**：G7（负控制）一路绿但是**假绿** —— 桩写成扁平的
   `SimpleNamespace(sprites=...)`，函数读的是 `pet.sprite_loader.sprites` → **恒返回 None** →
   负控制必然通过＝没测。是**配对的正控制 G6 红了**才揪出来。**正/负控制必须成对**。
   **同一族的第二个样本**：G5"左右两组文件名不同"——**原样复制**照样通过 → 补 G10（逐像素证镜像）
   + G10b（证该判据对未镜像的图会报错）。**定式：每条断言都要能回答"如果这件事根本没做，它还会绿吗？"**
2. **含字符串字面量的源码级 needle 必须走 `func_lit`**（`func_code` 剥 STRING →
   `getattr(self,'_jump_anim_override',None)` 变成 `getattr(self,,None)` → **假红**）。**第 4 次**踩。
   本轮不再靠"记得"：加 **X0 自检**把两个视图的语义钉住 —— **工具本身的语义也要有断言**。
3. **失败详情要能诊断**：detail 写 `None` 时红了只有 `<<< None`，分不清"名字错"还是"桩错"；
   改成打印实际值 + 库里的键，一眼定位。**断言失败要把现场交出来**。
4. **离屏环境没有字体**（`QFontDatabase: Cannot find font directory`）→ `QPainter.drawText`
   **静默不画**（对照图第一版整行标题一个字都没有）→ headless 出图**别靠文字**，用色带 + 外部图例。
5. **PowerShell 捕获原生 stdout 会毁掉中文**（第一遍 `Out-File` 收回来是"涓婃ゼ"乱码）→
   证据必须让 Python 自己 `open(..., 'w', encoding='utf-8')` 写；项目里早有现成套路
   （`第十七轮/run_round17.py`），**我踩完了才想起来去找 —— 先找项目里已有的工具再动手**。
6. **列表推导手滑多打一层**（`for n in n`）→ `NameError` 报在推导式内部，只给行号看不出原因 → 先展平再筛。

### 遗留 / 待办

1. **真机肉眼确认（只能用户做）**：① 走上更高窗口 → 应起跳/攀爬而非瞬移；② 走更低楼层 → 应跳下去
   而非自由落体；③ 从**一层**窗口掉到桌面**不该**摔扁，**两层及以上**才摔扁；④ 三组攀爬素材观感是否符合预期。
2. 批次 C（`climb_to_top_window` 收编）/ D（三条有意偏差登记）仍未动。
3. **逐层攀爬**（"只能相邻"的字面实现）本轮**判定不可行并留证**；若用户坚持，先解决"中间层被遮挡时怎么落脚"。
4. **下一步回"原计划"：H4/H5 上帝类拆分**（建楼批次做完就该回它）。

---

## H4/H5 架构改造 · **闸门 G3（属性归属表）交付** —— 批次 A 结算

报告：`H4-G3_属性归属表_2026-09-17.md`；脚本 `code-quality-audit/架构改造-H4H5/`：
`scan_attribute_ownership.py`（只读 AST 扫描，自检 **X1–X7 全 PASS**）+ `make_g3_report.py`（渲染器）；
证据 `架构改造-H4H5/_evidence/attribute_ownership.{txt,json}`。
提交 **`cca9666`**（G3 主体，6 文件 / +23964）+ **`5404b3f`**（日期更正，rename 99%）；
两次 push 均 **attempt 1 成功**，`ls-remote` 复核**远端 == 本地 == `5404b3f`**。

用户原话："接下来按照那个上帝模块拆分的那个计划继续吧" → 读
`架构改造排期方案_H4-H5_2026-09-13.md` 后确认：**批次 A 的四道闸门里只剩 G3 没做**，
而 §3 对 G3 的原话是"**没有这张表不许进入 Wave 1**"，所以本轮做 G3。

### 口径修正：v1 的"458 个属性"是虚数（最重要的一处）

v1 扫描对**所有** `self.<name>` 的 `Attribute` 节点记账，**包括 `self.foo()` 里作为 `func` 的那一个** →
方法名/Qt 内置全被当"属性读"灌进全集：`change_animation` 53、`play_animation_once` 43、`pos` 37、
`height` 35、`width` 31、`move` 19、`show`、`winId`… → `UNCLASSIFIED` 300 条里大半是噪声，**表不可用**。

v2 判据（可复算）：调用位记 **call** 不算 load；**从未被赋值 且（被调用过 或 是本类直接方法）**
⇒ 判为方法/Qt 内置，**整条移出属性表并单独成段**（禁止静默丢弃）。
结果：名字全景 **458 = 真属性 286 + 调用位 172（474 个调用点）**。
新增自检 **X5/X6/X7** 专门锁这条：排除段写次数必须为 0 / 与属性表交集为空 / 两段之和 == 原名集。

### 交付内容

- 分区 **16 个、未归类 0**：W3-3 物理 61 / W3-2 移动 47 / W2-1 鼠标 27 / CORE 情绪与状态 26 /
  W3-1 动画 25 / CORE 会话·AI 18 / 子系统引用 18 / W1-1 施法 13 / CORE 配置 UI 12 / W1-2 躲猫猫 10 /
  W1-4 视频 9 / CORE 环境 6 / CORE 托盘·进程 5 / CORE 窗口几何 5 / W1-3 游戏 3 / CORE 悬空读 1。
  方案 §3 要求的六类（窗口几何/物理/动画/情绪/会话/子系统引用）**全部有落点**。
- **真耦合点 111 条**逐条定归属（口径两算："写出现次数≥3" 111 条、"写入方法数≥3" 93 条；
  方案 §2 的 106 是前一口径的旧值，同量级）。
- **必备接口 138 条** = "归属模块之外仍有方法在写它" —— 这就是"新模块不得不反向 `import main`"
  的**唯一诱因清单**，按目标模块分好组（W3-3 52 / W3-2 23 / W2-1 20 / W3-1 13 / W1-1 11 / W1-2 8 /
  W1-4 8 / W1-3 3）。
- **Wave 1 内部互写只有 4 条**（`_hide_stage`/`_hide_obstacles`/`_hide_folder_path` ← W1-1 与
  `_spell_auto_suspended` ← W1-2）→ 这是 Wave 1 的**唯一真阻塞项**。其余 134 条的另一侧是
  "留在宿主"或 Wave 2/3，**属性不搬即无反向依赖**。
- **施工口径定案**：Wave 1 **只搬方法、不搬状态**（转发壳 + 宿主 API），故新模块不反向 import main、
  G2 基线可保持逐字节一致（方案 §5.3）。

### 顺带查出（交 G1，本轮只列不判）

- **悬空读 3 个**（读一个从未赋值的属性）：
  `_cached_scale_factor`（`getattr(self,'_cached_scale_factor',2.0)`，main.py:7675/7835 —— **缓存永远取默认 2.0**）；
  `ai_thread`（`hasattr` 守卫，main.py:8630 —— **8630–8632 收尾逻辑整段是死代码**，
  `ai_thread_running = False` 与 `join()` 都不会执行）；
  `is_being_thrown`（main.py:7562，`hasattr` 恒 False）。另有 **4 个类体常量**（合法，勿动）。
- **赋过值但从无读取（死参数候选）59 个** —— 只列不判（方案 §5.5 改造期不改行为）。

### 复核结论：**G1 并未完成**（不能把没做的写成做了）

查证：第五轮点名的 185 个应用层零引用方法，到第十五轮**只删了排期优先项
`main.py::update_floor`（零调用漂移副本）一个 → 1/185**。且 188/185 是第五轮口径，主树此后改了很多
（现 9588 行），**开工前必须重扫**。方案 §3 已如实加上"状态"列：
**G2 ✅ / G3 ✅ / G4 ✅ / G1 ✗（1/185）**，并在 §3 末给出"批次 A 结算"与两条可选路径
（先补完 G1，或每拆一项就对该项做一次专属零引用筛查 —— 倾向后者，成本更低更精准）。

### 教训

1. **"属性"与"调用位"是两个语法位置**，混在一个 `Attribute` 节点集合里统计必然虚高 ——
   **扫描器要先声明它统计的到底是什么**，并用配对自检（X5/X6/X7）把口径钉住，否则"表看起来齐全"是假的。
2. **长表交付物必须由脚本渲染**：111 + 138 = 249 行数字，手抄的出错概率高于人眼校验能力。
   扫描器（出证据 json）+ 渲染器（出报告 md）分离后，**"报告与证据不一致"这一态在结构上不存在**。
3. **自检自己也会 KeyError**：X5 初版去查已被删除的 `attr[a]`（`KeyError: 'init_ui'`）→
   自检要在**删除后仍可访问的快照**上做断言。**自检脚本本身也要能跑通，否则它是负资产。**
4. **注入的时间不可信**：系统给的 `current_time` 是 **2026-09-16 00:22**，而本机 `Get-Date` 与
   `git log` 时间戳都是 **2026-09-17 22:4x**（星期四）。我先按注入值把报告命名成 09-16，
   与同日第十五/十七/十八轮（全 09-17）不一致 → 已 rename 更正。**日期一律取自系统时间**
   （`make_g3_report.py` 已改成 `time.strftime`，不再写死）。
5. **PowerShell 回传 stdout 依旧不可用**（`git :` 那段 `NativeCommandError` 是 PS 把 git 的 stderr
   进度当错误记录，**退出码 0 才是判据**）；且**光看 `$LASTEXITCODE` 不够** ——
   第一版 `> $null` 把 Python 的退出码吞了、误判成成功，差点把"扫描器没跑成"当成"跑成了"。
   **凡"看起来成功"的步骤，都要有一条独立证据（文件 mtime / 内容变化）来证伪。**

### 遗留 / 待办

1. **Wave 1 是否开工、以及开工前是否先补 G1** —— 卡在方案 §8 决策 2（185 个零引用方法
   **删除 vs 标注 legacy**）。已把 G3 新查出的 3 悬空读 + 59 死参数候选并进该决策。
2. **§6 那 4 条 Wave 1 内部互写**（W1-1 ↔ W1-2 的 `_hide_*`/`_spell_*`）：合并两个 Controller，还是走信号。
3. W1-6（文件/表格）搬之前须先与 `modules/desktop_interaction.py` 比对去重（方案 §5.2 原话）。
4. 未动任何生产代码，故未重跑 G2（上次 **21 套件 / 799 PASS** 仍有效）。

---

## H5 §4.1 · **素材分组补漏报告器 + 单文件图库**（只读，未改配置）

脚本 `code-quality-audit/架构改造-H4H5/scan_asset_groups.py`（自检 **X1–X7 全 PASS**）；
证据 `_evidence/asset_groups.{txt,json}` + `scan_asset_groups.run.txt`；
图库 `素材分组总览_2026-09-17.html`（380 卡片 / 1111 内联缩略图 / 19 家族，**已 gitignore**）。
提交 **`1689e1f`**（5 文件 / +11776），push attempt 1 成功，
`ls-remote` 复核**远端 == 本地 == `1689e1f`**。

用户原话："我那个素材文件里有一堆动画，相应的动画组都有相同格式的命名，所以你到时候给把那些都分好组
然后给我展示，我会告诉你哪些不需要然后你看一下要求文件查缺补漏，有什么建议就和我说"
→ 三步流水线：① 分组+展示 ② 用户圈掉不需要的 ③ 要求文件查缺补漏给建议。本轮只做 ①。

### 实测基线（**勿重测**，2026-09-17）

| 项 | 值 |
|---|---|
| `deltarune_ralsei/*.png` | **1111**（0.85 MB，纯像素图，最大 176 KB） |
| 命名分组（`spr_<base>_<seq>`） | **380** |
| 家族（首 token） | **19** |
| `animations.json` 组数 / 被引用帧 | **112 / 379** |
| 已登记 / 部分引用 / 未引用 | **82 / 20 / 278** |
| 配置引用但磁盘缺失 | **0** |
| 另两个素材目录 | `ralsei_face` 50 PNG+1 txt、`textbox` 3 PNG+1 SVG+1 README |

家族分布（组数 / 已登记 / 部分 / 未引用）：`ralsei` 273(64/12/197)、`cutscene` 25(13/3/9)、
`susie` 21(0/0/21)、`tea` 17(2/5/10)、`board` 13(0/0/13)、`ralseib` 6、`teacup` 4、`ch3` 3、
`gameshow` 3、`dw`/`quiz`/`susieb`/`susiezilla`/`tiny` 各 2、`chefs`/`hunched`/`ja`/`mannequin`/`shootout` 各 1。

### 命名归组口径（可复算，勿凭直觉改）

- `spr_` 前缀 → 去 `.png` → **末段是纯数字 ⇒ 那是 seq，其余是 base**；否则整段是 base、seq=None。
  → `spr_ralsei_hurt.png` 与 `spr_ralsei_hurt_0.png` **同组**（seq None + 0）；
    `spr_ralsei_climb_1_0..4` ⇒ base `ralsei_climb_1`；`spr_ralsei_climb_0_degrees_0..5` ⇒ base `ralsei_climb_0_degrees`。
- **家族 = base.split('_',1)[0]**（够用且不易错）。X5 断 `unparse(parse(f)) == f` 保证可逆。
- **三态**：同组全部帧被引用 = 已登记；部分 = 部分引用；零 = 未引用。
- **别名链要解析**：`alias_of` 指向另一个**组名**（不是文件名），不展开会把复用组误判成未引用。

### 图库做法（可复用）

1111 张总 0.85 MB ⇒ **base64 全内联**，单文件 1.44 MB，**不需要 http server**（绕开 file:// 被拦的问题）。
每卡带勾选框 + 底部 textarea 自动汇总组名 + 复制按钮 → 用户圈完直接把列表粘回来，省掉来回猜。
像素图必须 `image-rendering: pixelated`，底衬棋盘格（PNG 有透明通道）。
**大且可复算的产物不入库**：`素材分组总览_*.html` 加进 `.gitignore`（每次重扫都变、1.4 MB），
只入库脚本 + 证据；重生成一条命令。

### 待办

1. **等用户圈定"不需要"的分组** → 再动 `animations.json`（本轮**刻意零回写**，H5 §4.1 定性为"报告器"）。
2. 用户圈完后做 ③：对账 `原始要求` / `要求` / `打开文件夹的要求` / `跳跃要求`（**空文件**）/
   `"建楼"要求` / `ralsei_pet/src/部分互动要求`，出"要求点名但磁盘/配置都缺"与"命名对不上"清单。
   已知线索：要求里点名的 `teacup_ralsei_land_*` 在磁盘上**没有**（磁盘只有 `teacup_ralsei` /
   `teacup_ralsei_tea` / `teacup_ralsei_tea2`）；要求 §11 的 **50 个 face 状态**在 `ralsei_face/`
   （**不在** `deltarune_ralsei/`，本扫描器不覆盖）。

---

## H5 §4.1 后续 · 攀爬家族复核（用户指认"不是我给的那两组"）+ 对话 AI 化现状调查

提交 **`39d7946`**（5 文件），push `ff1bf92..39d7946 main -> main` 一次成功；
**`ls-remote` 核验未完成**：两个代理同时抖动（Clash `7897` → `schannel: failed to receive handshake`；
沙箱代理 → `CONNECT tunnel failed, response 502`），连试 6 次均 128。
按既往教训**不等于失败**（push 自报成功即远端已收），待代理恢复补一次 `ls-remote`。

### 一、攀爬素材：我上轮的"朝右 = climb_1"给不出证据

用户原话："你刚才那会给我展示的攀爬的动画不是我给你的那两组啊，你再看看"。

新增工具 `架构改造-H4H5/make_asset_contact_sheet.py`（按命名分组放大铺成**带编号**对照图；
**PIL 直接 `ImageFont.truetype('C:\Windows\Fonts\msyh.ttc')` 可以拿到字体** —— 与"沙箱里 Qt
`QFontDatabase` 没字体、`drawText` 静默不画"是两件事，别再因为 Qt 那条而放弃在图上写字）。

**逐帧 md5 + 水平镜像比对**（`_evidence/climb_hash_mirror.txt`）给出三条硬结论：

1. `ralsei_climb_0_degrees` 的 6 帧 **== 基础 `ralsei_climb` 的 4 帧各自重复一次**（哈希全等，
   `134bb3fd28ba`/`7a48b6eae20a`/`71338e8d4863`/`7434f0a5196d`）→ **同一套图、只是节奏拉长**，
   不是"另一个朝向"。
2. `ralsei_climb_90_degrees` 的 6 帧 **== `climb_1`…`climb_6` 各自的第 0 帧** → `climb_1`~`climb_6`
   是**同一攀爬循环的六个角度变体**。
3. **除本轮生成的 `climb_left` 外，没有任何两组互为水平镜像** → 六组是六个独立角度，
   **不存在"左右对"**。
   （附带：`climb_poses` 第0帧 == `climb_0`；`climb_poses_alt` 第0帧 == `climb_2`，同为那批图。）

→ 磁盘上"朝向"这件事本来就是**六组**（`climb_1`~`climb_6`），上轮把它当成"两组之一（朝右）"
是**挑错了索引**。外观上 `0_degrees` 明显是背面/无脸那套（27x44 左右、帽大），
`climb_1`~`climb_6` 是中帧展开、能看见绿眼镜那一套（19–23 宽）。
**修法已想好但要等用户指认**：改 `animations.json` 里 `climb_right`/`climb_front` 的 `frames`，
并按新的"朝右"组重生成 `spr_ralsei_climb_left_*` 镜像（`第十八轮/make_climb_left_assets.py` 已幂等）。
**仍不回写配置** —— 等用户说了才动。

### 二、对话是不是"全权交给 AI"？—— **不是**（用户："我感觉还是很死板"）

用户问的是事实，答案要如实：**待办 #15「对话全面 AI 接管」确实没做**（`MEMORY.md:317`、
`2026-09-14.md:157`）。当前是 **"AI 优先 + 大量规则旁路 + 内置兜底"的混合体**。三个死板来源：

1. **关键词命令表在 AI 之前截胡**：`dialogue_ui.py:1055` 先试 `handle_chat_commands`（约 50 条
   关键词→固定回复，`1319-1497`），再试 `handle_game_input`（`1062`）/`handle_file_commands`
   （`1499-1593`）—— **命中即 return，AI 完全收不到**。而"自由闲聊"只是这条链走到底的最后一个分支
   （`1085` 起才 `use_ai = api_enabled and hasattr(chat_with_ai)` → `main.chat_with_ai` `main.py:7113`）。
2. **AI 失败/关闭时回落内置台词库**：`dialogue_ui.py:1124-1125` `_rule_reply` →
   `dialogue_system.generate_response`；那个文件是最大的写死池（问候 8 条 `29-38`、13 类模板各 8 条
   `41-192`、约 44 个话题×8 条 `205-522`、上下文模板 `525-581`、`663-1273` 里还有一堆
   `random.choice`）。另有 `emotion_system.get_dialogue_for_emotion:1180-1269`、
   `weather_system:40-65`、`desktop_interaction.get_special_file_reaction:2464-2519`、
   `autonomous_agent:104-137`。
3. **事件类台词 100% 规则生成**：`main.py` 约 131 处 `add_dialogue` 多为写死池；点击/抚摸身体部位
   `5250-5846`、摔落/惊醒 `3271-3358`/`3717`、桌面元素反应 `3930`、陪看视频 `4626-4627`
   （**已标 `TODO(#15)` @ `4637`**）、`pet_ai` 时段问候与文件搭话 `393-411`/`599/613/617`。

**已经按"全权 AI"做对的一条**：自主说话唯一入口 `main.start_autonomous_speech`（`main.py:5059`，
由 `check_initiate_dialogue` 每 15s 轮询、闸门 10 分钟 + 不打断聊天），**内容只由 AI 生成，
AI 不可用时保持沉默**（`5072-5074`）—— 这条不能回退。

**开关**：配置键 `api.enabled`（`config.json:4`，代码 `main.py:355`）；代码默认值 **False**
（`config_manager.py:57`），但本机 `config.json` 已是 `true`（本机 Ollama `ralsei`）。
AI 调用走线程（`main.py:7200` / `ai_driver.py:358`），底层 `HTTPLocalAI.chat` 是**同步 requests**
（`api_client.py:174/279`），**严禁主线程调用**（`177-180`）。

**UI 侧还在"假打字机"**：`dialogue_ui.py` 非纯展示，内含打字机/历史/AI 调用/命令解析；
固定 35ms（长文本 22ms）逐字 `782-784`、固定 20s 自动隐藏 `163`、固定初始 540×180 `188`、
正文上限 380 `274`、固定思考占位 `"……"` `157`；**`stream: False`**（`main.py:6607`）→ 先拿完整答复
再逐字，所以观感是"顿一下然后刷字"。

**上下文其实不差**（这点别说成"无状态单轮"）：`main.py:7133` `user_msg = _build_ai_context() + text`，
含时段/天气/心情/精力饥饿/用户偏好（`7202-7263`）+ 最近 6 轮历史（`7136`）+ 话题锚（`7165-7171`）
+ `recall_text` 记忆召回（`7176-7185`）。**局限**：历史只 6 轮（缓冲上限 8 `dialogue_ui.py:347`）；
**桌面观察不进 chat prompt**（只进 `ai_driver.note_event` `main.py:4640`）；
**`memory_graph.py` 在 `main.py` 里零引用** → 多跳联想那套**完全没有接进对话**。

**未定项（不要猜）**：除三条显式 `return` 外，其余 `add_dialogue` 调用点与 AI 回调之间
**没有统一串行化**，定时器与 AI 回调可能同时弹话；运行时顺序静态判不出来。

**下一步（等用户拍板）**：把命令表降级为"AI 之前的意图标注"而不是直接产出回复、
把事件台词交给 `ai_driver` 生成、开流式输出、把桌面观察与 `memory_graph` 接进 prompt。



