# 2026-09-22 工作日志

## 第二十七轮：AI 对话「失真」专项治理

**用户诉求**（原话）：「求你个事，一定要把他的AI模型训练的很好，我不想在和她聊天的时候
出现那种失真的样子，剩下的，你自己做吧，对了，尽量把东西存在E盘里就好」
→ 三件事：① 治失真（主任务，我自定方案）② 其余我自己决策 ③ 产物落 E 盘。

**commit `685f98b`**（6 files, +451/−44）→ push 成功 → `ls-remote` 硬核验 `SYNCED=True`。

### ★★ 核心结论：失真不是"模型不行"，是**提示词给了可逐字搬走的模板**

这是本轮最值钱的一句。真机取证（8 场景 × 3 次 = 24 条真实回复）：

```
开头复读 15/24 = 62%；任意位置复读 21/24 = 88%
开头复读分布（去重）：嗯——×5、唔……×4、是这样啊……×3、诶！真的吗？×3
```

**只有 4 个起手式覆盖全部 15 次命中** → 用户说的"失真"就是这个：
不是某句说错，而是**每条回复都从同一句起跳**。

根因：persona「碎片」区是**按场合分组 + 冒号 + 固定例句**
（「坏消息的时候用：是这样啊……／那……现在还好吗／…」）——
**"分组 + 冒号 + 例句"这个格式本身就在教模型"这个场合该说这句"。**

### A/B 对照（唯一变量 = 这一段写法）

`probe_ab_fragment.py`，同模型/同温度/同场景，**先断言 A ≠ B**：
  **A 现状 61% → B 拟改 28%**

★ **但 B 组剩的 28% 里 `诶！真的吗？×4` 仍占大头** ——
因为我在 B 里**还留了一句**「诶！真的吗？」当说明性例子。
**⇒ 只要那串字还在 persona 里，4B 就会搬。正式版一个例句都不留。**

改法：例句 → **情形描述**（`· 一急、一慌 → 重复同一个字`），
并把"每次现造"从暗示提升成**显式硬要求**（"第一句话的开头必须针对他刚说的那句现写，
不要固定用同一个字起头；连着两次都是'一时不知道说什么'，两次的开头也得长得不一样"）。

### 失真②：括号旁白有洞（`strip_action_parentheticals`）

只匹配**动作词开头**的括号；`（心里有点慌，怕自己做得不好）` 这类
**描述性旁白**全漏（实测 5 种形态）。`looks_like_narration` 只看**整句首字符**，
括号在句中根本不看。

★★ **本轮先做错了一版，回归锁当场抓出来（这是锁在正确工作）**：
第一版用**窄白名单**（只放过 `其实/……` 开头），结果**把合法心里话一起误杀** ——
C19 / A22c 报红：`（我想想该怎么说）`/`（抱歉，我不是故意的）`/`（笑不出来）`/
`（因为……我一直都是一个人）` 全被清掉。
**误杀 = 该说的话被吞掉，比漏过更糟**（项目里反复出现的取舍）。

修正：**改抓"形态特征"而不是"首词白名单"** ——
`_NARRATION_INNER` 匹配"从外部描述自己"的固定开头（`心里/语气/神情/沉默/愣了/…`）→ 清掉；
第一人称的话语（`其实…/因为…/抱歉…/我想想…`）→ **原样保留**。
判据从"只放过谁"翻成"抓谁"，这是本轮的方法收获。

### 失真③：伪造"俩人共同经历"

真实回复出现「**我们那时候总坐在石阶上**，谁也不说话」——用户从没去过黑暗世界。
persona 只说"可以当闲话提"，**没禁止**讲成共同经历 → 4B 把"我自己的过去"
写成了"我们的回忆"。**这是在编造用户的人生**，比语气问题严重。

改法：明令「屏幕对面这个人没有去过黑暗世界，那些事他都不在场…只能说成我自己的过去，
不能编成我们俩一起经历过」。验收：**编造共同经历 0 处**。

### 真机验收（改动后同批场景重跑）

| 指标 | 前 | 后 |
|---|---|---|
| 开头复读率 | 62% | **33%** |
| 复读种类 | 4 种 | 3 种（且不再是 persona 逐字给的整句，是模型自发的结巴起音） |
| 编造经历 | 有 | **0** |
| 括号旁白（产品口径） | 5 种全漏 | 全部被拦 |

★ **探针自报"括号残留 4 处"，逐条拿产品函数复核 = 3/4 是探针误报**：
探针分类器只查括号是否以 `其实/……` 开头 —— **犯的正是我第一版修复的同一个错**。
第 4 条 `（我好像也不太懂你说的是什么……）` 是**第一人称的话，本就该放行**。
**⇒ 教训："分类器写得太窄"和"闸有洞"长得一模一样**，必须拿产品函数逐条复核，
不能只看探针计数（与"先读原文再看计数"同源）。

### 未做 / 如实记录

开头复读 33% 仍高于理想值，但剩余 3 种都是**短促语气起音**
（`诶！`/`诶、诶？`/`我、我……`）—— 这类结巴**本来就是 Ralsei 的人设特征**
（原作台词里结巴是他的标志），彻底清零等于砍特征，与"别把心里话一起误杀"同一取舍。
**若用户实测仍觉模板感重，下一轮杠杆在采样侧（temperature/repeat_penalty）
而不是继续删 persona —— 但本轮无证据支持动它。**

### 回归锁 +4（1165 → 1169）

- **A12d** 示例区不得再含"按场合绑定的固定例句"（旧格式回归就会被搬）
- **A12e** 明令"不许把那边的往事编成俩人共同经历"
- **A22e** 描述性旁白被清掉（**正控制**）+ **A22f** 心里话仍一条不动（**反控制**，与 A22c 成对）
- **A12c 锚点更新**：`坏消息的时候用：` → `然后才是声音`
  （**契约变更非缺陷**：断的仍是"位置关系"，换名不降鉴别力）

★ **A22e/A22f 成对是设计要点**：单有正控制只证"洞补上了"，
必须有反控制才能证"没补出新的误杀"。

### 回归

**G2：24 套件 / 1169 PASS / 0 FAIL / 全 IDENTICAL / rc=0**
（基线 1165 → 1169；走 `run_all.py --update` 合并模式）

### 本段环境坑（全是老坑复发）

- Bash 工具又整体 127（`mkdir`/`tail`/`cd` 全 command not found）→ 全程 Python。
- ⭐ **`> "E:/Download/x.txt" 2>&1` 这种 Bash 重定向会静默失败**
  （报 `Invalid request code`，文件根本没建）→ 一律**用 Python 子进程捕获 + 自写 UTF-8**。
- PS 写文件前**父目录不存在会静默 exit 0 但文件没落**（`_tmp27` 首次就是）→ 先 `os.makedirs`。
- `git commit` 的 LF→CRLF warning 是 `core.autocrlf=true` 的正常提示，非错误。

### 收尾（本轮最后一步）

- **记忆提交 `51919da`**「记忆维护：第27轮落档 + MEMORY.md 补 AI 失真契约」→ `ls-remote` 核验
  `remote=51919da… local=51919da… SYNCED = True`。
- **最终态复跑 G2**（已提交状态上）：**`合计：PASS=1169 FAIL=0  套件=24`** / `IDENTICAL=24 DIFF=0 rc=0`。
- **临时件清理**：`E:\Download\_tmp27\` 本轮自建 8 个文件全删 + 目录已移除；
  **`E:\Download\_tmp` 存量完好未动**（用户已取消批量删除授权 —— 只删自己建的文件）。
  证据 `E:\Download\音27侦察\_evidence\cleanup.txt` / `cleanup_check.txt` / `final_state.txt` / `G2_final_state.txt`。
- **仓库工作区 clean**（`git status --porcelain` 无输出；`.pyc` 无漂移）。
- ⚠️ 收尾踩一次：把仓库根写成 `try - 副本\ralsei_pet`（**仓库根就是 `try - 副本`**，`ralsei_pet` 只是其子目录）
  → `run_all.py` 报 `No such file or directory`（rc=2）→ 修正路径后 rc=0。**跑 G2 的 cwd 是仓库根。**


---

## 第二十八轮：场景系统 P0（『留好拓展接口』落地）

### 主导指令（用户原话）

> 按你的想法来就好，对了，你到时候了解好整个系统架构，我后期会给他添加场景系统，
> 也就是把原作的世界搬到桌面上，你要留好拓展接口哦（桌面也会被我当成一个场景，
> 你自己看一下原作的场景切换这类的，为后期添加做准备）

### 判据

**P0 = 接口真的在位 + 不切场景时零行为变化。**
不做渲染层 —— 一旦 P0 就改画面，「零行为变化」这条最强判据就丢了，P1 的真回归会被淹没。

### 交付

新增（代码/数据）：

- `ralsei_pet/modules/scene_system.py` —— 数据层：读 JSON + 4 个纯函数 + `SceneState`。
  **禁 Qt / 禁项目内 import**（初始化环纪律，与 `event_speech.py` 同源）。
- `ralsei_pet/modules/scene_controller.py` —— 接线层：双向转发 + `load` / `switch` /
  `resolve_objects` / `description`。
- `ralsei_pet/assets/scenes/_index.json` —— 三级索引（chapter/area/scene），`default_scene=desktop`。
- `ralsei_pet/assets/scenes/_anchors.json` —— 12 个命名锚点（借原作 `marker` 机制）。
- `ralsei_pet/assets/scenes/desktop.json` —— **桌面场景**（P0 零可渲染项）。
- `code-quality-audit/场景系统-P0/verify_scene_p0.py` —— 回归锁 **79 项**。

`main.py` **四处接线**（净 +33 / −1，那 1 行删除只是元组行被替换）：

1. `from modules.scene_controller import SceneController`
2. `_CONTROLLER_ATTRS` 追加 `'scene'`
3. `init_systems` 里 `self.scene = SceneController(self)`
4. **6 个状态字段预声明**（`_scene_index` / `_scene_state` / `current_scene` /
   `scene_objects` / `_scene_anchors` / `_scene_loaded`）—— 漏了就状态劈两份，G2 看不见。

### 三个工程发现

1. ★ **素材两套口径**：`sprite_loader.sprite_dir` = `<仓根>/deltarune_ralsei/`
   （`ralsei_pet/` 的**上一级**），而场景 JSON 在 `ralsei_pet/assets/scenes/`
   → 同一个 JSON 里按 `kind` 分两套：`sprite` → 仓库根，其余 → 场景目录。
   `resolve_asset_path()` 是**唯一翻译点**。
2. ★ **三条设计律**（都有正/负控制成对）：① 算不出返回 `None`、**绝不伪装 `(0,0)`**；
   ② 坏元素**跳过不作废整件**；③ 未知 `cond` 键**一律放行**。
3. ★ **「无深度信息排最后」是刻意口径**：没有 `y` / `depth` 的纯逻辑锚点物件不该插到
   有明确空间关系的物件中间。要它靠前就显式写 `depth`。

### ❗ 本轮探针自身的 4 个 bug（都是"探针写错"，不是产品错）

| # | 现象 | 真因 |
|---|---|---|
| D6 | 期望值算错 | `[None,'a',7,'b']` 洗成 `['a','b']` 后 tick=1 → `'b'`，我写了 `'a'` |
| F4 | 口算错 | `far(y=10)` → 999990 最小，却排在 `near(y=900)` → 999100 后面 |
| **H10** | 断言**恒假** | 用 `code_only_src()` 匹配 `name != 'p'` —— 该清洗器**剥 STRING token** → 洗成 `name!=`（字面量没了） |
| **J2** | 断言**恒红** | 同一个坑另一面：`"'scene'" in CODE_MAIN` —— 元组被洗成 `('','','',...)` |

**⇒ 第 5 次踩同一个坑。新铁律上线：能上 AST 就上 AST。**
`code_only_src()` 只适合断言**标识符/运算符**；断言**字面量**必须走 AST 或 `code_no_comment()`。
处置：H10 / J2 改 AST 口径（`_setattr_skips_p()` / `_controller_attrs_tuple()`），
并给 H10 配**负控制**（漏护栏的合成源码必须判 False）。

### 回归

- 回归锁 `scene_p0`：**79/79 PASS**，`rc=0`。
- G2：**24 套件 / 1169 PASS → 25 套件 / 1250 PASS / 0 FAIL / 25 全 IDENTICAL / rc=0**。
- ⚠️ **唯一基线变更**：`round5_smoke` 的 `待导入模块数: 36 → 38`（新增两模块，均 `[ OK ]`）。
  该套件逐个 import `modules/*.py` 并打印计数 → **模块数天然随新增模块而变**
  （套件自身描述已写明「模块数随新增模块而变，勿把具体数字写进描述」）。
  处置：`--only round5_smoke --update`（**合并模式**）→ 确认其余 23 个套件基线条目一条没少 →
  `--update` 全量重建（把 `scene_p0` 也纳入）。
- 同步复跑确认：**25 套件全 IDENTICAL**。

### 提交

- **`0aba480`**「场景系统 P0：把『留好拓展接口』真的落到代码里」（18 files / +3675 / −3）。
- **`ls-remote` 核验 `SYNCED = True`**（`0aba480…` == `0aba480…`）。
- 提交信息前 3 字节 `229,156,186`（**无 BOM** ✓）。

### 🔴 待用户裁定

1. **场景美术素材来源** —— **P1 开工前唯一阻塞项**。实测：`<仓根>/deltarune_ralsei/`
   1111 张 PNG **全是 Ralsei 角色精灵**、`ralsei_face/` 50 张表情、`textbox/` 3 张；
   `assets/sprites/`、`assets/faces/` **都是空目录**；全仓库搜 `bg_` / `tileset` /
   `room_` / `map_` **零命中**。→ A 程序化生成 / B 用户提供截图 / C 重绘，**推荐 A+B**。
2. 场景粒度；3. 是否要 BGM（现 `sound_manager` **只有 3 音效、无 BGM 能力**）；4. 原作版权尺度。

### 环境

- Bash 工具仍整体 127 → 全程 Python。
- ⭐ **`> "E:/Download/x.txt" 2>&1` 这种 Bash 重定向会静默失败**（报 `Invalid request code`，
  文件根本没建）→ 一律**用 Python 子进程捕获 + 自写 UTF-8**（本日第二次踩）。
- ⭐ **经由 `python -c "..."` 传含反引号的文本会被 shell 吃掉**（`__dict__`、`(0,0)`、
  `scene_p0` 全会消失）→ **写带反引号的 Markdown 一律用 Write 工具落脚本文件再跑**，
  别用 `python -c` 内联。
- `E:\` 的 exFAT **不能建子目录**（`os.makedirs('E:/.../_tmp')` → `OSError [WinError 1]`）
  → 证据直接落 `E:\Download\场景系统侦察\_evidence\`（已存在的目录）。

---

## 第二十九轮（场景路由层 + 7B 底座评估）

**用户原话（verbatim）**：
> 对了，你可以给他接7B试试，只要他生成的等待时间在1～3s内我就可以接受，毕竟和真人对话也不是刚说出来他就回应嘛，我这个显存是32GB的，所以你可以尝试一下，对了，根据场景系统，你也得给模型训练在什么语境下怎么走路线，去哪个场景，当然，你只需要留好拓展的接口，等咱开始做场景系统你才能真正根据地图线路来规划，当然，一切根据原作，所以如果你可以看到原作的地图线路那你就可以先进行第一版规划，不行就后面再看，然后继续进行你所规划的就好

### 拆解与兑现

| # | 用户要求 | 兑现物 |
|---|---|---|
| 1 | 接 7B 试试（1~3s 验收） | 硬件勘察 + 219 次真实推理实测 + 外推 + 替代方案 |
| 2 | 语境 → 路线 → 场景 | `modules/scene_routing.py` + `assets/scenes/_routes.json` |
| 3 | 只留拓展接口 | **零自动调用方**；只有 `follow_route` 允许调 `switch`（锁 H1–H5） |
| 4 | 一切根据原作 | `原作场景线路研究_2026-09-22.md`（Ch1~Ch5 **87 间**房间表） |

### ★★★ 前提纠正（本轮最重要发现）

用户说「**我这个显存是32GB的**」——**实测不符**：

| 项 | 实物 |
|---|---|
| CPU | Intel Core Ultra 5 125H（Meteor Lake） |
| GPU | **Intel Arc 核显（iGPU）** `PCI\VEN_8086&DEV_7D55` |
| 独显 | **无**（`nvidia-smi` 不存在） |
| 内存 | 8×4GB LPDDR5-6400 = **31.6 GiB 内存** |
| 显存 | **无独立 VRAM**（核显共享内存） |

⇒「32GB」是**内存**不是显存。**不按错前提施工** → 完整勘察后如实上报 + 给替代方案。
**"能力自评失准"比"能力不足"更危险** —— 前提失准同理。

### ★★ Ollama 跑在纯 CPU 上

- `server.log` 的 `inference compute` = `library=cpu total="31.6 GiB" available="20.2 GiB"`
- 实测 `size_vram = 0`
- Ollama 目录**只有 `ggml-cpu-*.dll`**（alderlake / icelake / …），**无** `ggml-vulkan/cuda/dml.dll`
- 系统有 `vulkan-1.dll` + 驱动 32.0.101.8331 较新，但 **Ollama 0.34.2 未带 Vulkan 后端**

⇒ **核显完全没用上。** 唯一「同时提速又保质量」的方案 = **试 Vulkan 后端**（需改 Ollama 启动环境变量）。

### ★★ 4B 实测基线（**219 次真实推理**）

从 `%LOCALAPPDATA%\Ollama\server.log` 的 `slot print_timing` 行抽取（**非抽样**），
证据 `code-quality-audit/场景系统-P0/_evidence/4b_server_log_perf.txt`：

- eval 速度：min 7.21 / max 13.55 / **avg 8.07 tok/s**
- **12 tok = 1.49s**（0.89~1.66）
- **20 tok = 2.48s**（1.48~2.77）
- **40 tok = 4.96s**（2.95~5.55）

★★ **真瓶颈不是生成速度，而是 prompt eval**：
prompt 涨到 2000+ token（人设 + 记忆 + 世界观注入）时，**prompt eval 要 35~47 秒**；
短 prompt 时仅 116~700 ms。**同一模型差 100 倍。**
⇒ 用户说的「等待时间」若指**首字延迟**，那么**决定它的是 prompt 长度，不是模型大小**。

### 7B 外推（**参数量 1.75× → 速度约 1/1.75**）

≈ **4.61 tok/s**：12 tok = 2.60s / 20 tok = 4.34s / 40 tok = 8.67s。
⇒ **首字（短 prompt）大概率仍进 1~3s；整句会明显超 3s。**
⚠️ **全部为外推值，非实测**（本轮 7B 一直在下载重试中，未就绪）。

### ★ 两个劝退项（比速度更硬）

1. **内存**：当前空闲只剩 ~9.7 GiB（7B 权重 4.7GB + KV cache 1~2GB + 桌宠 PyQt）→ 装得下但**无余量**。
2. **"失真"7B 未必解决**：第 27 轮结论是失真**头号来源 = persona 里可逐字搬走的固定例句，不是模型大小**；
   且 Modelfile 注释已记过「7B(qwen2.5) 也更干净，但**更慢且更平**」。
⇒ **7B 是"换了瓶颈"，不是"解决了瓶颈"。**

### 建议顺序

1. **先试 Vulkan 后端**（核显跑 4B —— 唯一能同时提速又保质量）
2. 保持 4B，把精力放回 persona 与采样参数（第 27 轮的杠杆**未用尽**）
3. 7B 作为**可切换选项**保留、**不进默认**

### 路由层设计（本轮核心交付）

**路由是数据，不是代码** —— 规则全在 `_routes.json`，`scene_routing.py` 只做「读 + 匹配」。

- 分层：**语境 → `match()`（纯函数）→ route `{to,reason,priority}` → `follow_route()` → `switch(scene_id)`**
- **匹配语义**：`when_scene/area/chapter` 相等 + `"*"` 通配；`when_mood/event` 单值或数组（命中任一）；
  `when_keywords` **OR 语义**（与其余键的 AND 语义**刻意不同**）；
  排序 = `priority` 升序 → **命中条件数降序** → 声明序（稳定排序）
- **三条设计律**（全部配正/负控制成对）：
  1. 匹配不到返回 **`None`，绝不伪造一条默认路线**（`None` = 「没有任何理由换场景」→ 原地不动）
  2. **坏规则跳过，不作废整张表**
  3. **未知条件键一律放行**（渐进增强）
  ★ 方向刻意选的：**放行的风险"能被观测到"，拒绝的风险是"静默失效"**
- ★ **`_as_set()` 的坑**：`set("abc")` → `{'a','b','c'}` ⇒ 字符串一律当**单个值**
  （否则 `when_mood: "happy"` 会静默匹配上 `'h'`）
- ★★ **零行为变化**：路由层**没有自动调用方** —— 不注册定时器、不挂事件链路、不自己播动画、
  **只有 `follow_route` 允许调 `switch`**（锁 H1–H5，含负控制）
- ★ **`destinations()` 过滤未登记场景**（拿 `_index.json` 过滤）→ 防 AI 被送往
  「路由表写了但场景不存在」的地方（那会 `switch()` 失败，表现为「**走了一半停住**」，很难查）
- ★ **零依赖纪律**：`scene_routing.py` **禁 Qt、禁项目内业务模块**，只准标准库 + `scene_system`
  （`main.py` 在 **import 期**就 import `scene_controller`，回头 import 有反向依赖的模块会**把环接上**，已踩 4 次）
- ★★ **状态预声明铁律**：控制器会用到的字段**必须在宿主 `init_systems()` 预声明**
  （否则 `__setattr__` 判据不满足 → 状态劈两份，**G2 完全看不见**）。本轮 +3：
  `_scene_routes` / `_routes_loaded` / `_scene_route_reason`

### ★ 原作机制（GML 实证）

源 `code.deltarune.wiki` 的 `scr_roomname`：

1. 房间 ID 是**扁平整数**
2. **同一 id 在不同章是不同房间** ⇒ **id 必须与 chapter 联合才唯一**
   （**印证我们的三级模型，chapter 必须显式存**）
3. `stringsetsubloc("~1 - Atrium", "Dark Sanctuary", key)` 是**两段式命名**
   （渲染成「Dark Sanctuary ~1 - Atrium」= **父地点 + 子地点**）
   ⇒ 我们 `describe()` 输出「章节、区域、场景」是**同一结构**
4. ★ **原作没有显式"区域"数据结构** —— 区域是从显示名的 ` - ` 前缀**推出来的**
   ⇒ **我们显式化成 area 层是比原作强的地方**

**房间数**：Ch1=20 / Ch2=20 / Ch3=8 / Ch4=19 / Ch5=20，**合计 87 间**。

### ★ 为什么现在**不**登记原作场景

**素材还没做**（`deltarune_ralsei/` 1111 张 PNG 全是角色精灵，全仓库搜
`bg_`/`tileset`/`room_`/`map_` **零命中**）→ 登记了切过去 = **一个空场景** =
「**世界突然变白**」，**比不切更糟**。

**落地三步（零改代码）**：① `_index.json` 登记一章/区/场景 → ② 放 `<scene_id>.json`
→ ③ `_routes.json` 加一条规则。**三步全在数据层，不动一行 Python。**

### 被回归锁逮到的问题（这是好事）

**`scene_p0` 套件 A6 报红 —— 真实回归**：

```
[FAIL] A6 scene_controller.py 只 import 标准库 + scene_system
       <<< imports=['logging','scene_routing','scene_system']（越界=['scene_routing']）
```

处置：确认 `scene_routing` 是**平级的零依赖模块**（同纪律，有双份断言守）→
**显式**扩白名单 + **补 A6b 负控制**（防白名单宽到"什么都放得过"）。79 → **80 项**。
★ **保留这种"麻烦"是刻意的** —— 若白名单写成「只要不是 Qt 就放行」，这次会**静默通过**。

### 探针自身的两个 bug（处置方向相反）

- **J3**：我用**弯撇号** `Queen’s Mansion` 断言，研究记录里是**直撇号** `Queen's Mansion`
  → **改探针**（探针自身的坑）
- **J6**：研究记录**确实没写** `_index.json` 联动说明 → **改数据不改探针**
  （补「怎么把这张表变成我们的场景」三步 + 「与本项目现有文件的对应关系」表）
★ **两个都报红，一个该改探针、一个该改数据 → 必须先读原文再定。**

### ★ 提交信息校验：我自己的判据也错过一次

原以为判据是「前 3 字节 = `231,172,172`」——**错**。
`场` = U+573A = `E5 9C BA` = **229,156,186**（`231,172,172` 是别的字）。
★ **正确判据 = 逐字节比对**：`git cat-file commit HEAD` 的 message 段 **vs** 源文件字节
→ `IDENTICAL = True`（1379 == 1379）。**别记"魔法字节序列"，要比对。**

### 验证

- `verify_scene_routing.py` **106/106 PASS**（A 零依赖 / B 契约 / C 匹配语义 / D 设计律 /
  E destinations / F 接线（含 **F17 状态不劈裂**）/ G `main.py` 预声明 / H 零行为变化
  （含 **H5 只有 `follow_route` 允许调 `switch`**）/ J 原作房间表）
- `verify_scene_p0.py` **80/80 PASS**（79 → 80，A6 白名单 + A6b 负控制）
- **G2 终态：`PASS=1358 FAIL=0 套件=26` / 全 IDENTICAL / DIFF=0 / BASELINE=0 / rc=0**
  （施工前 25 套件 / 1250 PASS）
- 基线更新三步（`--only round5_smoke` → `--only scene_p0` → 全量），**每步核验「丢失的基线条目 = 无」**

### 提交

- **`52d5514`**「场景路由层与7B底座评估」（12 files / +2202 / −7）
- **`ls-remote` 核验 `SYNCED = True`**；工作区 clean
- 提交信息：`场` = `229,156,186`，**无 BOM**，逐字节 `IDENTICAL`

### 证据（`code-quality-audit/场景系统-P0/_evidence/`）

`hw2.txt` / `gpu_deep.txt` / `gpu_mem.txt` / `ollama_backend.txt` / `ab_7b_perf.txt` /
`scene_routing_run.txt` / `scene_p0_diag.txt` / `G2_after_routing.txt` / `baseline_update_routing.txt` /
`manifest_probe.txt` / `mem_check.txt` / `git_push_r29.txt` / **`4b_server_log_perf.txt`** /
**`probe_7b_state.txt`** / **`commit_msg_verify.txt`**

### 报告（项目根）

- `场景路由层与7B底座评估报告_2026-09-22.md`（7 章，含前提纠正与**外推值诚实标注**）
- `原作场景线路研究_2026-09-22.md`（87 间房间表 + 对应关系表）

### ⚠️ 未完成

**7B 仍在下载重试**：17 个并行分片已合并成 **1 个 4.361 GB blob**，但 `manifest` 未注册、
`api/tags` 无新模型。`server.log` 真因：
`download.go:299 msg="2bada8a74506 part 8 attempt 0 failed: unexpected EOF, retrying in 1s"`
⇒ **下载在重试循环里，不是卡死**。

**处置：不手工注册 manifest** —— 那会产出**损坏的模型**（分片未合并）。
改为**带代理重试推进 + 等待**。报告 §1.4 已标注**外推值非实测**。
**就绪后必须补跑真机实测覆盖。**

### 🔴 待用户裁定

1. **7B 怎么处理**：装好备用（不设默认）？还是不要？**是否要我试 Vulkan 后端**？
2. **「等待时间 1~3s」指首字延迟还是整句说完？** —— 这决定 7B 是否达标
   （首字大概率达标、整句大概率不达标）。
3. **沿用第 28 轮**：场景美术素材来源（**P1 开工前唯一阻塞项**）｜ 场景粒度 ｜ 是否要 BGM ｜ 原作版权尺度。

### 环境新知

- Bash 工具仍整体 127（`dirname` / `tail` / `grep` / `head` / `wc` 全 127）→ **全程 Python**。
- ❗**`2>$null` 在 Bash 工具里报 `ambiguous redirect`**（`$null` 是 PS 变量）→ 用 `2>/dev/null`。
- ❗**Bash 里调 PowerShell 被安全策略拦**（`Command blocked for security`）→ 用 **PowerShell 工具**。
- `wmic` 已从新系统移除 → 改 `Get-CimInstance`。
- ⭐ **Ollama 日志是性能数据金矿**（本轮 219 样本即由此来）。


---

## 第三十轮：7B 落地 + 「首字延迟」真相查明

**用户口径（verbatim，优先于我的技术判断）**：

> 我想要用7B目的就是让他贴合人物并且不出bug，所以prompt尽量完整，还有，1~3s当然是首字的延迟啦

> 现在E盘装上了，你可以继续了

三条硬口径：
① **换 7B 的动机 = 贴合人物 + 不出 bug**（质量，不是速度）；
② **`prompt 尽量完整`** —— 明确否决我第 29 轮"先砍 prompt 长度"的建议；
③ **「1~3s」= 首字延迟（TTF）**，不是整句说完。

### ★★★ 本段最重大发现：4B 产品链路首字是 36.8s，不是 0.6s

第 29 轮我报"4B 首字 0.6~0.7s 合格"——那是**裸测**（短 system，21 token）。
本轮改用**产品真实 persona**（2322 token）实测：

| 场景 | prompt tok | 首字 | 生成 tok/s | 整句 |
|---|---|---|---|---|
| 短 system | 21 | **0.33s** | 10.2 | 3.92s |
| persona（冷） | 2322 | **36.82s** | 8.4 | 2.63s |
| persona + 3 轮历史 | 2363 | **1.91s** | 8.3 | 2.54s |

★ **同一 prompt 长度差 19 倍**（36.82s vs 1.91s）⇒ 我第 29 轮的简化结论
「**决定首字的是 prompt 长度**」**被推翻** —— 真正的变量是「**KV 缓存是否命中**」。

**★ 教训（已进 MEMORY.md §5）：测性能必须用产品真实输入。**
用简化输入代替产品输入，会给出**反向结论**（第 29 轮"合格" → 本轮"超 10 倍"）。

### 7B 下载完成 + 首字实测

下载在**用户取消我的推进器之后**由 Ollama 进程自己跑完：
45 秒内 69.06% → 75.86%（6.73 MB/s），最后 `verifying sha256 digest` → `writing manifest`
→ `status=success`，220 秒收官。**7B 现已注册**（`api/tags` 可见）。

**7B 裸测**（`ttf_measure_7b.txt`，system 8699 字符**未裁剪**）：

| 场景 | prompt tok | 首字 | 生成 tok/s |
|---|---|---|---|
| 短 system | 43 | 1.39s | 8.25 |
| 完整 prompt（冷） | 2050 | **96.48s** | 5.65 |
| 完整 prompt + 历史（缓存命中） | 2050 | **0.20s** | 5.23 |

**7B 产品真实调用链**（`ttf_product_chain_7b.txt`，复刻 `main.py:6722-6816` 的 system 组装顺序）：

| 场景 | prompt tok | 首字 | 判定 |
|---|---|---|---|
| ① 完整 system + 6 轮历史（冷） | 2457 | **120.27s** | OVER |
| ② 同一 system 再来一轮 | 2478 | **3.22s** | OVER |
| ③ 【此刻】变了 | 2456 | **8.28s** | OVER |
| ④ **lean（只发 persona）** | 2332 | **1.88s** | **OK** |

⇒ **7B 在产品链路上首字 1.9~8.3s，只有 lean（事件台词）一条路达标。**
⇒ **7B 达不到用户 1~3s 门槛**（在纯 CPU、无 GPU 加速的前提下）。

### ★★★ 因果定位：「变化段位置」决定缓存能复用多长（29 倍）

第一次 A/B（`cache_position_ab.py`）**设计有缺陷**：4 次调用每次 system 都不同
→ 没有一次真正命中缓存 → 跑 13 分钟全是冷启动，结论行 `倍数 = 1.0×` **无意义**。
**如实记录这个失败**，重做 v2（`cache_position_ab2.py`）：**先喂热前缀，再只改一个变化段**。

| 变化段位置 | 改一次的代价 |
|---|---|
| **靠前**（persona 之后紧跟【此刻】） | **73.44s** |
| **末尾**（【此刻】挪到 system 最尾） | **2.50s** |
| | **倍数 = 29.4×** |

同一变化量、同一 prompt 长度（3629 token），**仅位置不同 → 首字差 29 倍**。

★ **机制**：Ollama（llama.cpp）只复用「**从 prompt 开头起、逐字相同**」的最长前缀。
变化段落在哪，缓存就从那断 ⇒ 后面所有 token 全部重算（prefill 是 decode 的约 10 倍慢）。
★ 对照：产品里 `persona`（**每次逐字不变**）是唯一稳定前缀 ⇒ **它越长，缓存越值钱**。

### 产品已有这套设计（本轮只是量化了它）

`main.py:6700-6816` 注释里有一整套「保 KV 前缀缓存」的设计，**第 20 轮就做了**：

- `lean`（事件台词）**只发 persona，一个字节都不变** → 注释记「首字回到 0.6s 量级」；
- 世界观**从常驻 system 前缀改成按需召回**（`~4380 → ~2800` token），
  理由明写「**保 KV 前缀缓存** —— 它一抽掉，骨架前缀更短且更稳」；
- **关系 brief 刻意排在所有临时状态之前**，注释：「关系比这些变化得慢得多，
  放在前缀更靠前的位置，跨轮更容易保持逐字相同，**KV 缓存复用得更长**」；
- 每轮必变的三段（`_build_ai_context` / `focus` / `recall`）**全部追加在 system 末尾**。

⇒ **产品组装顺序已经是位置最优的**。本轮实测正好**印证并量化**了第 20 轮的设计正确性。

### ★ 我的 A/B 首发假设也被数据推翻

v2 里我预期"变化段靠前 → 全量重算；末尾 → 前面可复用"，**方向对了**，
但我原以为靠前会退化到"全量冷启动（~190s）"量级，实测是 **73s** ——
说明 llama.cpp 并非全废，**中间还有部分复用/重叠匹配**（细节待查，不写进结论）。

### 结论（可给用户的）

1. **7B 达不到 1~3s**（产品链路 1.9~8.3s；除 lean 外全超）。
2. **4B 也达不到**（冷启 36.8s；只有缓存命中后 ~1.9s）。
3. ⭐ **「首字 1~3s」的解法不是换模型、也不是砍 prompt，而是「让 prefix 缓存稳定命中」** ——
   而这**恰好满足用户「prompt 尽量完整」的要求**：缓存与 prompt 长短无关，只与"前缀是否逐字稳定"有关。
4. ⭐ **唯一真矛盾**：persona 是稳定前缀（缓存之友），但**每轮注入的【此刻】/话题/记忆/关系
   天然把 system 打散**。要吃到缓存，必须保证「**变化段恒在末尾**」+「**persona 恒在开头且逐字不变**」。
5. **纯 CPU 是天花板**：7B 生成 5.19~5.59 tok/s、4B 8.07 tok/s；
   **Vulkan 后端（核显）压过裸测冷启，但压不过冷 prefill** —— 冷启 96~120s 量级靠后端救不回来。
   ⇒ **真正要治的是"别让它冷"**（常驻会话 / 预热 / 前缀稳定），不是换更大的模型。

### 证据（`code-quality-audit/场景系统-P0/_evidence/`）

`ttf_measure.txt`（4B 三场景）/ **`ttf_measure_7b.txt`**（7B 裸测三场景）/
**`ttf_product_chain_7b.txt`**（7B 产品链路四场景）/ **`cache_hit_verify.txt`**
（产品函数组装 + 缓存命中 190× ）/ **`cache_position_ab.txt`**（v1 失败，如实留档）/
**`cache_position_ab2.txt`**（v2 成功，29.4× ）+ 对应探针脚本 `_measure7b.py` /
`_cache_verify.py` / `_ttf_chain7b.py` / `_cache_position_ab.py` / `_cache_position_ab2.py`

### 回归

**G2：`PASS=1358 FAIL=0 套件=26` / 26 套件全 IDENTICAL** —— 本轮纯测量、未改产品代码，口径零漂移。

### 环境新知

- ⭐ **`/api/pull` 流式探测时提前 `break` → 日志报 `context canceled`**（**不是网络故障**！）。
  我曾据 `failed to get direct URL; backing off` 误判"下载卡在 69%" —— 真因是我自己断流。
  **正确做法：探测时保持连接读到 `success`，或至少连续采样 45s 再下结论。**
- ⭐ **Ollama 模型目录是 `C:\Users\23002\.ollama\models`**（**不是** `%LOCALAPPDATA%\Ollama\models`）。
- **7B 冷 prefill 成本**（纯 CPU）：2050 token ≈ 96~120s；3629 token ≈ 190~225s。
  这是**用户能感知到手感的天花板**。



---

## 第 32 轮（2026-09-22）：结巴治理 —— 「他没必要每句都结巴」

### 用户口径（verbatim）
> 他没必要每句都结巴吧，那只是偶然事件而已，会随着熟悉度提升慢慢下降，除非遇到什么紧急情况这类的（就是人会在什么时候结巴）

澄清后选择：**「太多了，要减少」**。

### 取证：先分清「真口癖」与「固定模板句」
12 组真实场景 × 产品真 persona（`ralsei:v4`）实测：

| 形态 | 出现率 | 判定 |
|---|---|---|
| **真结巴**（叠字/首词重复） | **8.3%**（1/12） | 人物真实性，**不砍** |
| 「诶？」起头 | **75%**（9/12） | **固定模板，必须砍** |
| 「诶？这样啊……」 | **50%**（6/12） | **固定模板** |
| 省略号软停 | 83% | 过多 |
| 反问结尾 | 75% | 过多 |

⇒ **用户说的「结巴」实测不是真结巴，真凶是「诶？」固定开场白模板**。
根因与第 27 轮同源：persona 把「诶」排在语气词第一位 ⇒ 模型每轮抓它。

### 处置
- `assets/ralsei_persona.md`：删掉全部可逐字搬走的字面量（`诶、诶？！` / `那个，我可能说得不太好……` / `我、我……`）；
  「我的口癖」节改为**情形描述 + 每次自己现造**，并明写「越紧张越明显，平常聊天基本不结巴」；
  新增「戒固定开场白」段（禁连用同一语气词起头 / 禁「这样啊／原来如此」复述式开场 / 禁每次反问收尾）。
- 体积：9748 → **9946 B**（K3 阈值 10000 B）。
- `modules/event_speech.py`：**顺手修掉 `（轻叹）` 真漏** —— `轻` 不在 `_ACTION_MODIFIERS`、
  `叹` 在 `_ACTION_VERBS`，中间那个「轻」让整条不匹配。
  修法：修饰语表补 `轻`、动词表补 `哼|嗤|摸|轻笑`；
  ❗**刻意不加裸 `笑`**（会误杀合法台词 `（笑不出来）`）。
- 加锁：`verify_s7_event_speech.py` 新增 **A22g**（防放宽误杀）+ 4 条新正例。

### A/B 实测（同批 12 组输入、同模型同参数）
| 指标 | 改前 | 改后 | 变化 |
|---|---|---|---|
| 诶起头 | 75.0% | **0.0%** | −75pp |
| 诶？这样啊 | 50.0% | **0.0%** | −50pp |
| 语气词起头 | 83.3% | **16.7%** | −66.7pp |
| 省略号 | 83.3% | **25.0%** | −58.3pp |
| 反问结尾 | 75.0% | 50.0% | −25pp |
| 真结巴（处/条） | 0.17 | 0.08 | **未涨 ✅** |

### 回归
**G2：`PASS=1359 FAIL=0` / 26 套件全 IDENTICAL**（`s7_event_speech` 137 → **138**）。
提交：`eb806e4`（产品+证据）+ `59cb2fe`（记忆落档），`SYNCED=True`。

### 教训固化
- ★★ **用户说「某口癖太多」时，先分清「真口癖」（人物真实性，别砍）和「固定模板句」（失真，必须砍）**；
  取证口径 = **用产品真 persona 打多组、分类计数**。
- ★ 我改完 persona 后回归锁抓到 **3 项我自己引入的 FAIL**
  （markdown `**` 被锁 A11 / 判据词 `不要整句搬` 被我删 / 体积超 10063 B）
  —— **回归锁正确工作的例证**。
- ★ **洞不只在「动词表不全」，「修饰语+动词」的组合形态是另一类洞**。
- ★ 函数真名 = `strip_action_parentheticals`（我起手猜的 `strip_action_parens` 是错的，第一次就 AttributeError）。
- ❗❗ **Bash 工具的**命令替换**会吞掉 heredoc / 内嵌脚本里的反引号对**（第 32 轮踩）：
  我用 `python -c "..."` 写含 `` `code` `` 的 markdown，
  **每一对反引号都被当成命令替换执行 → 内容整段消失 + 生成 2 个 0 字节垃圾文件**。
  ⇒ **凡是要写含反引号的文本，必须先落成 .py 文件再跑，绝不用 `python -c` 内联**。

### 未结（已写进 MEMORY.md §9 / §11）
- ❗**用户暗含两点尚未实现**：结巴率应**随熟悉度（trust）提升而下降** + **只在紧急/紧张时出现**
  ⇒ 方向 = 接进 `relationship.py` 的档位做**动态结巴率**，**待排期**。
- ⚠️ **7B 仍会伪造共同经历**（裸测出现「还记得那次我们为了一个谜题一起努力解谜…」），
  **护栏管不了它**（护栏只治括号旁白）→ persona 服从性问题，需继续观察。

### 记忆维护
`.workbuddy/memory/MEMORY.md` 结构性压缩 **12498 → 11852 字符**（原超注入上限被截断）：
合并逐字重复段、§3 并入 §0、§6 W1 铁律折叠为详版指针。
自检：标题齐全 / glued 空 / 无 BOM / U+FFFD=0。
