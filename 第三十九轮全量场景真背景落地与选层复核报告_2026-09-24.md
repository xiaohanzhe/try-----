# 第三十九轮 · 全量场景「真背景」落地 + 选层复核报告

日期：2026-09-24
本轮性质：**只做"零编造"的那一半** —— 凡原作房间自带背景精灵的，用原作 PNG 原样落盘并标注来源；
拿不到真背景的一律留空，第 37 轮那套"区域代表素材"近似**适用范围一点没扩大**。

---

## 0. 一句话结论

1,013 个场景里，**157 个能拿到真背景**（占 15.5%），本轮把其中 **148 张新图**落盘并把
`bg` / `bg_source` / `bg_asset` 三个字段接进场景数据；**856 个**场景的原作房间本来就没有背景精灵
（画面靠瓦片/物件拼），**本轮不填近似图**，等你定口径。
全量回归 **33 套件 / PASS=1547 / FAIL=0 / 全 IDENTICAL**。

---

## 1. 为什么是"只做一半"

第 38 轮把场景从 88 个扩到 1,013 个时，926 个新场景写的是 `bg: null` + `bg_source: "none"` ——
那是**刻意的诚实**：用"区域代表素材"把"我没有这张图"伪装成"就是这张图"，违反本项目
「算不出 → `None`，绝不伪装」的铁律。

所以本轮把口径切成两半：

| 口径 | 判定 | 本轮动作 |
|---|---|---|
| **真背景** | 房间自带 `bg_sprite` / `asset_sprites` 且宽高 ≥ 300×200 | ✅ **原样复制原作 PNG**，标注来源 |
| **近似** | 只能靠"区域代表素材 / 章节兜底平铺" | ⛔ **不动**（待定口径） |

---

## 2. 数字（全部落盘可查）

### 2.1 房间级（原作事实，来自 UTMT 反编译的 `rooms_map.json`）

| 章 | 房间 | 有背景层 | 有瓦片层 | 有实例 | 有资源层 | 全空 |
|---|---|---|---|---|---|---|
| ch1 | 147 | 36 | 1 | 146 | 4 | 0 |
| ch2 | 278 | 46 | 180 | 278 | 73 | 0 |
| ch3 | 246 | 35 | 136 | 246 | 102 | 0 |
| ch4 | 328 | 39 | 213 | 327 | 145 | 1 |
| ch5 | 252 | 29 | 159 | 251 | 153 | 1 |
| **合计** | **1,251** | **185** | **689** | **1,248** | **477** | **2** |

### 2.2 场景级（1,013 = 87 锚点 + 926 分片）

| 档次 | 数量 | 说明 |
|---|---|---|
| `room.bg_layer`（**真**） | **157** | 全部是房间自带背景层 |
| `area_table`（近似） | 700 | 第 37 轮手写的"区域代表素材" |
| `chapter_tiles`（近似） | 156 | 本章最大瓦片图平铺 |
| 未配上 | 0 | — |

### 2.3 落盘

| 项 | 值 |
|---|---|
| 新增 PNG | **148** |
| `bg/` 目录项数 | 88 → **236** |
| 出错 | 0 |
| 字段复核（重新读盘） | **235 / 235 一致** |
| 接线条目 | 87 锚点（补 `bg_source`/`bg_asset`）+ 148 分片条目 |
| 过期注释修正 | 87 处 |
| `git diff` | **+705 / −383**（= 设计值：148×3 + 87×3 新增；148×2 + 87×1 删除）|

> `+705/−383` 精确等于设计值，说明文本级逐行替换**只在目标行动了手**，其余字节未漂。

### 2.4 真背景素材的尺寸事实（★ 推翻了我自己的一个推断）

| 素材尺寸 | 场景数 | 其中房间尺寸 |
|---|---|---|
| 320×240 | 87 | **84 个房间本身也是 320×240**（宽比 1.00）|
| 640×480 | 25 | 22 个房间是 640×480 |
| 660×480 等 | 45 | 多为"素材比房间小"的平铺层 |

素材宽 / 房间宽：`≥1` 124 个 ｜ `0.6~1` 22 ｜ `0.3~0.6` 6 ｜ `<0.3` 5。
**结论：素材与房间基本 1:1；原作房间本身有尺寸分级**（320×240 小房间 / 640×480 标准 /
更宽的滚动房间），少数层是**平铺重复绘制**。

---

## 3. 判据的单一真源

"什么算真背景、什么算近似"的唯一实现是
`code-quality-audit/第39轮-全量场景背景/_tools/bg_common.py :: classify()`
（与第 37 轮 `build_bg6.py` 的 `pick_asset` **逐字同口径**）。

判定顺序（顺序即优先级）：

1. 房间自己的 `bg_sprite` 且尺寸够 → `room.bg_layer`（**真**）
2. 房间自己的 `asset_sprites` 且尺寸够 → `room.asset_layer`（**真**）
3. 区域代表素材表 → `area_table`（近似）
4. 本章最大 `*_tiles`，平铺 → `chapter_tiles`（近似）
5. 本章最大 `bg_*` ≥600×400 → `chapter_fallback`（近似）
6. 都没有 → `none`

**为什么不另写一份**：本项目吃过"同一判据两份实现 ⇒ 两种结论"的亏。回归套件因此
**不重实现 `classify()`**，而是拿仓库内的溯源表重建它的入参再复算。

---

## 4. 本轮的关键发现

### F39-1 ★ 真背景的覆盖上限就是 15.5% —— 想更忠实只能做"瓦片还原"

856 个近似场景里，**634 个所属房间有瓦片层**（`tiles`）、220 个只有物件实例。
全量 1,251 个房间里 **689 个有瓦片层**。

⇒ 「完全遵循原作」的忠实路径不是"区域代表素材"，而是**按原作瓦片数据把房间画出来**
（tileset 图集 + 每块瓦片的坐标/源矩形）。这是当前的忠实上限，工作量明显更大，**需要你点头**。

### F39-2 ★ P1 渲染层的前置需求（现有反编译脚本没导出）

现有 `dump_rooms.csx` 只导出了层的 `bg_sprite` 名字，**没导出背景层几何**：
`x` / `y` / `xscale` / `yscale` / `tiled_h` / `tiled_v` / `speed` / `depth` / 层顺序。

而实测有 6 个房间的素材宽度不足房间一半（例：`dw_city_traffic_4` 素材 480×480 / 房间 4600×480）
⇒ 那些层是**平铺/重复绘制**的，光有图无法正确铺满。
**P1 开工前必须扩 `dump_rooms.csx` 并对五章重跑一次 UTMT。**

### F39-3 选层口径复核：基本没问题，2 处待复核

- 157 个里房间有 **>1 个背景层**的 = 20 个；
- 取到的层**不是最大层**的 = **1 个**（`dw_mansion_ferris_wheel_post`，0.79）；
- 素材宽 < 0.5×房间宽的 = 6 个，全是宽滚动房间的平铺条（正常）；
- ⚠️ **2 处值得复核**（**本轮不擅自改**）：
  1. `ch1.hometown.torielclass` 取到 **`bg_lang_ja_torielclass`（日语变体）**，
     同房间还有 `bg_torielclass`；语言门控是 GML 代码决定的，我们没有运行时证据。
  2. `ch2.my_castle_town.dw_castle_restaurant` 取到 `bg_dw_bakery_counter`（柜台），
     同房间还有 `bg_dw_bakery`（同尺寸）。

### F39-4 本轮判据侧共 3 次失真 + 1 次我自己的错误推断

| # | 失真 | 真凶 | 处置 |
|---|---|---|---|
| 1 | 溯源表自检报 **844 条不一致** | 蒸馏尺寸表时只装了"被房间引用的精灵"，而 `chapter_tiles` 分支用**全量** `bg_*` ⇒ 重建后判据掉档 | 尺寸表改装全量 `inv_ch`；自检转为 **全部一致 ✓** |
| 2 | 新套件 B1/B3 报红 78 条 | 判据把"非真背景"一律要求 `bg=null`，**越权**管了 78 个第 37 轮口径的近似锚点 | 改成分三类判据（真 / 锚点近似 / 新增场景）|
| 3 | B2 负控制变成"恒真" | 受害对象挑了**锚点**，而锚点分支不看真/近似差异 ⇒ 篡改不可见 | 受害对象换成**分片真背景场景**，并补 B2b |
| 4 | H1 报红 **148 条**"load_scene 返回 None" | **夹具违反产品契约**：喂的是裸 `_index.json` 条目（缺 `chapter_id`/`area_id`）| 改用产品函数 `load_index()` 的合并条目 |
| 5 | 我推断"素材是半分辨率、靠 `xscale` 放大" | **数据推翻**：87 个 320×240 里 84 个房间本身也是 320×240 | 交付物措辞已改正（预览页 `半分辨率` 出现 0 次）|

> 第 5 条是这一轮最值钱的教训之一：**"能直接算的别推算"**。若不查房间尺寸，
> 报告里就会留一句听起来很专业、但其实是错的结论。

---

## 5. 本轮的决定（已加锁，防后人静默改回去）

| 决定 | 锁在哪 |
|---|---|
| D1 **近似档适用范围零扩大**：926 个新增场景只许 `none` 或真背景 | `verify_bg_round39.py` **E1** |
| D2 新增场景的真背景**必须真的配了图**（不许只标注不落图） | **E2** |
| D3 87 锚点的 `bg_source` 逐条等于第 37 轮**实际产出**的 `how` | **E3** |
| D4 近似档只存在于锚点里 | **E4** |
| D5 真背景场景的 `bg`/`bg_source`/`bg_asset` == 据原作事实重算 | **B1**（+B2/B2b/B4 负控制、B3 正控制）|
| D6 文本格式保真（无 BOM/U+FFFD、不混用换行） | **F1–F4** |
| D7 不覆盖既有文件（1 处遗留不一致只核验，见 Q2） | 构建脚本纪律 |

---

## 6. 验证记录（全部 PASS）

| 验证 | 结果 |
|---|---|
| 溯源表自检（重建 `classify` vs 原始 dump，1,013 场景） | **全部一致** |
| 新套件 `bg_round39` | **30 PASS / 0 FAIL**（含 8 个负控制 / 3 个正控制）|
| 落盘复核（重新读盘比对 235 条目） | **235 / 235** |
| 磁盘核验（`os.listdir` 真实计数） | 88 → 236（+148，精确）|
| 全量 G2 | **33 套件 / PASS=1547 / FAIL=0 / 全 IDENTICAL**（1505 → 1517 → 1547）|
| 目视复核（157 格缩略总览图） | 绝大多数是可辨认的原作房间画面 |
| `.pyc` 漂移 | 0 |

---

## 7. 产物清单

**代码 / 数据**
- `ralsei_pet/assets/scenes/bg/` —— **新增 148 张原作 PNG**（原样复制，零缩放/裁切）
- `ralsei_pet/assets/scenes/_zone.ch*.json`（30 个）+ `ch*.json`（87 个）—— 新增
  `bg` / `bg_source` / `bg_asset`，并修正过期注释

**工具（`code-quality-audit/第39轮-全量场景背景/_tools/`）**
- `bg_common.py` —— 判据**单一真源**（`classify` / `bg_filename` / `collect_scenes`）
- `probe_bg_coverage.py` —— 覆盖量化（独立复算，用于交叉验证）
- `probe_tiles.py` —— 房间显形来源分层（瓦片/实例/资源）
- `probe_anchor_facts.py` —— 87 锚点事实核查
- `probe_layer_pick.py` —— 选层口径复核
- `build_real_bg.py` —— 真背景落盘
- `wire_bg_sources.py` —— 场景 JSON 文本级接线
- `export_provenance.py` —— 溯源表蒸馏 + 自检
- `make_preview.py` —— 总览图 + 预览页

**证据（`_evidence/`）**
- `真背景导出清单.txt` / `.json`
- `近似档待裁定清单.json`（856 条，供你裁定）
- `背景字段接线清单.txt` / `.json`
- `原作房间背景溯源.json`（可复现判据的蒸馏事实表）
- `真背景总览.png` + `真背景总览_索引.txt`
- `背景覆盖量化.txt` / `选层复核.txt`

**回归**
- `code-quality-audit/第39轮-全量场景背景/verify_bg_round39.py`（30 项，已进 G2）
- `code-quality-audit/regress/run_all.py`（注册 `bg_round39`）

**预览**
- `code-quality-audit/第39轮-全量场景背景/第39轮-场景真背景预览.html`（157 张，可按章浏览）

---

## 8. 🔴 待你裁定

| # | 问题 | 选项 |
|---|---|---|
| **Q1** | **856 个场景的画面怎么办**（这是本轮最大的一件） | **A 保持留空**（最诚实，桌面场景只显示宠物）／ **B 按原作瓦片数据还原**（最忠实，需扩 csx + 重跑 UTMT + 写瓦片绘制，工作量大）／ **C 沿用第 37 轮的"区域代表素材"近似**（快，但画面是编的） |
| **Q2** | `ch1.kris_room` 的 bg 是 **640×480（2× 放大件）**，而原作是 320×240（其余 4 章同名场景都是原样） | 保持现状（只记录不一致）／ 换成原样 320×240 |
| **Q3** | 2 处选层待复核（Toriel 教室的**日语变体** / 餐厅的柜台层） | 保持现状 / 定向覆盖（需要 GML 语言门控证据才严谨） |
| **Q4** | 78 个近似锚点（第 37 轮产物） | 保持现状 / 与 Q1 一起决定是否也升级 |

---

## 9. 下一步建议（我的排序）

1. **等你回 Q1**（它决定后面所有渲染工作的方向）
2. 无论 Q1 选什么，**扩 `dump_rooms.csx` 导出背景层几何 + 瓦片坐标**并重跑五章 UTMT ——
   这是 P1 渲染层和"瓦片还原"共同的硬前置，属于纯准备工作，早做不亏
3. P1 渲染层（把 `bg` 真的画出来）—— 需要你先定"桌面上场景怎么摆"的形式

---

## 10. 本轮没做的事（明确边界）

- ❌ 没有给 856 个近似场景填任何图（等你定口径）
- ❌ 没有改 87 个既有图文件（只核验，见 Q2）
- ❌ 没有动 `scene_controller` / `scene_system` 的产品代码（本轮是**纯数据 + 素材**，零行为变化）
- ❌ 没有实现瓦片绘制（属 Q1 的 B 选项，工作量大，先问）
