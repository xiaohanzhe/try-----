# 代码质量复审报告 — 第十一轮（2026-09-15）

**主题**：联想输入端治理 —— 关键词抽取结构剪刀 + 建边拓扑实测择优
**承接**：第十轮报告 §五 的 P0 待办（"检索机器已建好，瓶颈在喂进去的词"）
**提交**：`e8795da`（代码+测试+证据+回归基线，已推送 `e352515..e8795da`，`ls-remote` 核验 MATCH）
**验证**：本轮自检 **64 PASS / 0 FAIL**；G2 回归 **15 套件 / 523 PASS / 0 FAIL / 全 IDENTICAL**

---

## 一、结论（先说结果）

第十轮把"多跳检索机器"建好了，但诚实标注了瓶颈：**机器没错，喂进去的关键词全是碎片**。
本轮就治这个输入端，做了两件事，都落盘、都量化、都进回归：

1. **抽词结构剪刀（P0-1）** —— 用"位置专员"式过滤替换原粗口径过滤。
   同一批 16 句里，抽出的词量 **94 → 57（-39%）**，`我明天想写点代码` 从
   `{写点, 点代, 点代码, 代码}` 收成 `{代码}`；建图节点 **81 → 46**，
   边密度 **3.64 → 3.00 边/点**。碎片（`点代/码写/天想写`）基本清零。

2. **建边拓扑实测择优（P0-2）** —— 先按"精确率 + 密度"选出了 `star`（55% / 0.83 边每点，
   都优于 clique 的 50% / 3.26），**但随后的多跳可达性闸门实验把它否掉了**：
   只有 `clique` 走得通 `代码 → 熬夜 → 咖啡` 这种跨句联想，`star/chain` 在强边下也断链。
   **最终保留 `clique` 为默认**，理由是"密度可以靠抽干净词 + 分层 + hub 惩罚 + 路径分去压，
   但断掉的多跳补不回来"。

**一句话现状**：输入端的碎片问题已治好；`clique` 的密度由词法治理兜底，而不是靠砍边。

---

## 二、交付物

| 文件 | 变更 | 说明 |
|---|---|---|
| `ralsei_pet/modules/conversation_focus.py` | 改（+约 140 行） | `extract_keywords` 重写为**三层择优**；新增 5 把位置剪刀、免伤名单、分词器注入点 |
| `ralsei_pet/modules/memory_graph.py` | 改 | 新增 `EDGE_TOPOLOGY` 全局档 + `edge_pairs()`；`add_cooccurrence` 收敛为 `_bump`+`_trim_nb`；模块头留档拓扑实测结论 |
| `code-quality-audit/第十轮/verify_round10_graph.py` | 改 | G4 断言改为**与拓扑无关**（原写法"歪打正着"靠 star 核心词碰巧成立）；删死变量 `_pairs` |
| `code-quality-audit/第十一轮/verify_round11_input.py` | **新增** 64 项 | A 抽词剪刀(29) / B 建边拓扑+多跳闸门(17) / C 集成(10) / D 回归守卫(8) |
| `code-quality-audit/第十一轮/probe_extract_before_after.py` | 新增 | 抽词前后对比 + 建边密度对比（写 UTF-8 到 `_evidence/`） |
| `code-quality-audit/第十一轮/probe_topology_precision.py` | 新增 | 拓扑 × 词法 的精确率对比（手标注真词表，7 seed） |
| `code-quality-audit/第十一轮/probe_multi_hop_gate.py` | 新增 | **决定性实验**：多跳可达性受控对比（三阶段 × 三拓扑） |
| `code-quality-audit/第十一轮/probe_corpus_nodes.py` | 新增 | 真实语料建图后的节点清单（残渣账本） |
| `code-quality-audit/第十一轮/_evidence/*.txt` | 5 份 | 自检 / G2 / 抽词前后 / 拓扑精确率 / 多跳闸门（原始 UTF-8） |
| `code-quality-audit/regress/run_all.py` + `baseline.json` | 改 | 挂入 `round11_input` 套件；基线重建 |

---

## 三、P0-1：抽词结构剪刀（治碎片）

### 问题复现

第十轮实测里，`extract_keywords('我明天想写点代码')` 给出 `{写点, 点代, 点代码, 代码}` ——
四个里有三个是**跨词碎片**（"写点代码"被按 2-gram/3-gram 切出中间片段）。
这些碎片进图就是噪声节点，爬虫式联想会顺着它们一路漂走。

### 方案：位置专员"剪刀"（每把只盯一个位置）

不做"一刀切的长黑名单"（会误伤真词），而是给每个**字符位置**配一把专属剪刀，
各司其职、互不牵连：

| 剪刀 | 盯的位置 | 内容 | 作用 |
|---|---|---|---|
| 多字停用词 | 任意 | `_STOP_MULTI`（= `_STOPWORDS` 里 len≥2 的） | `的时候` 之类整串直接切 |
| 头尾功能字 | 首/末字 | `_EDGE_FUNC` | `要不` `一下` 的头部助词片段 |
| 内部动词 | 中间字（仅 len≥3） | `_INNER_FUNC` + `_INNER_VERB` | `天想写` `点代` 的中间动词碎片 |
| 首尾动词 | 首/末字（strict） | `_EDGE_VERB` | `写点` `去爬` 的动词边缘碎片 |
| 尾部量词 | 末字（strict） | `_TAIL_FUNC` | `杯咖` `爬山记` 的尾部量词碎片 |

**免伤名单 `_KEEP_WORDS`**：真词（`重要/需要/开会/学习/记得/喝咖啡/杯子/睡觉` 等）
一旦命中就**绕过全部结构剪刀** —— 否则剪刀会把真词一起切碎（这就是"免伤"的意义）。

### 三层择优（安全网）

```python
def extract_keywords(text):
    if not text:
        return []
    out = _from_segmenter(text)          # ① 若注入了分词器，优先用它的边界
    if out:
        return out
    strict = _extract(text, strict=True) # ② 严格档
    if strict:
        return strict
    return _extract(text, strict=False)  # ③ 全空才回落宽松档（不返回空）
```

再加可插拔的 `set_segmenter(fn)`：将来接上 jieba/pkuseg 等真分词库时，
边界判断直接交给它，内置词法自动让位；分词器抛异常或返回空都**回落内置**，不抛错。

### 实测（16 句，证据 `_evidence/round11_extract_before_after.txt`）

| 句子 | 旧词法 | 新词法 |
|---|---|---|
| 我明天想写点代码 | `写点 点代 点代码 代码`（4） | `代码`（1） |
| 要不要休息一下啊 | `要不 要休 要休息 休息 休息一 息一`（6） | `休息`（1） |
| 写代码写累了就喝咖啡 | `写代 写代码 代码 代码写 码写 写累 喝咖 喝咖啡 咖啡`（9） | `代码 喝咖啡 咖啡`（3） |
| 学习编程需要花不少时间 | 17 | 13 |

**合计：94 → 57（-39%）**；建图节点 **81 → 46**；clique 边密度 **3.64 → 3.00 边/点**。
多跳结果从"种子 `写代码` → 飘到 `写点/点代/点代码`"+一堆 `写代→码写/喝咖` 噪声，
收敛为 `代码 → 喝咖啡 / 咖啡`（正确的跨句联想）。

---

## 四、P0-2：建边拓扑 —— 一次被自己实测推翻的"择优"

### 第一步：精确率实验（结论：star 更好）

用 11 句真实中文语料 × 7 个 seed，手标注"真词表"判精确率，拿每种拓扑的 top-5 终点打分：

| 拓扑 | 精确率 | 返回数 | 边/点 | 跳数 |
|---|---|---|---|---|
| clique | **50%** | 26 | 3.26 | {1:24, 2:2} |
| **star** | **55%** | 22 | **0.83** | {1:19, 2:3} |
| chain | **55%** | 22 | 0.86 | {1:22} |

`star` 与 `chain` 精确率都更高、密度低四倍。**直觉结论：换成 star，噪声更少。**

### 第二步：多跳闸门实验（**把上面的结论否掉**）

但精确率实验里"跨句联想"是**顺带出现**的，没专门测"多跳到底走不走得通"。
于是设计受控实验：同一组概念只改共现次数，强制 `代码` 与 `咖啡` **从未同句**，
中间靠 `熬夜` 当桥（`写代码写到熬夜了` / `熬夜就靠咖啡撑着`）：

| 拓扑 | stage1（弱边 n=1） | stage2（中边 n=2） | stage3（强边 n=3） | 判定 |
|---|---|---|---|---|
| **clique** | no（弱边不许中转） | **YES hops=2 score=0.1238** | **YES score=0.3196** | **PASS** |
| star | no | no | **no** | FAIL |
| chain | no | no | **no** | FAIL |

**根因**：`star` 是"核心词↔其他词"，一句话里的**非核心词之间没有直连**。
`代码` 和 `熬夜` 若都不算"核心词"，二者之间根本没边，路径长度直接超过跳数上限 ——
**哪怕给到强边也断链**。`chain` 同理（只连相邻词）。

### 决策：默认仍是 `clique`

密度高的问题（3.26 边/点）**用输入端解决**——抽干净词（节点 81→46）+ 已有的分层边、
hub 惩罚、路径打分去压制"谁和谁都连"；而不是靠砍边，因为**砍掉的多跳补不回来**。

`star/chain` 保留为可切换实验档（`EDGE_TOPOLOGY = 'star' | 'chain'`），
模块头明确留档"它们为什么不被默认选用"，免得下一轮又有人凭直觉改一遍。
回归锁：自检 B13–B17 直接钉住这条闸门（stage1 必须 no、stage2 必须 YES、star/chain 必须断链）。

---

## 五、残渣账本（诚实清单）

抽词剪刀治好了**结构性碎片**，但**没有分词库**就治不了**词典歧义**。剩余残渣（见
`_evidence/round11_corpus_nodes.txt`，11 句语料 / 34 节点）：

```
天下 天下雨 天开 塞尔 尔达 习编 习编程 编程需 程需 花不 不少时 少时 少时间 咖啡感 啡感 件事
```

这些是"跨词的合法相邻字"（如 `天下雨`、`习编程`），肉眼可辨但规则难判 —— 它们**不是**本轮
要消灭的目标（消灭它们要么靠真分词库，要么靠词频阈值）。它们的存在已在自检 C10 里
**显式登记**（"除真词 + 已知残渣外没有新碎片冒出来"），不是无视。

**下一步（第十二轮候选）**：接入一个轻量分词库（jieba 之类）走 `set_segmenter()` 注入 ——
注入点已经预留、异常/空返回都回落内置，风险可控；届时可比对"接库前/后"的残渣量。

---

## 六、设计与诉求的对应

| 用户诉求（第十轮报告 §五 P0） | 本轮落地 | 证据 |
|---|---|---|
| 关键词抽取换语义边界可靠的方法 | 5 把位置剪刀 + 免伤名单 + 三层择优 + 分词器注入点 | §三 / A1–A29 |
| 降低边密度 | 输入端治理（节点 -43%）+ 拓扑可配留档 | §三 / §四 / C1–C2 |
| （不该为降密度而牺牲能力） | 多跳闸门实验保住 clique | §四 / B13–B17 |

---

## 七、验证与复现

```powershell
# 本轮自检（64 项）
C:\Python311\python.exe code-quality-audit\第十一轮\verify_round11_input.py

# 三份侦察（各自写 UTF-8 到 _evidence/）
C:\Python311\python.exe code-quality-audit\第十一轮\probe_extract_before_after.py
C:\Python311\python.exe code-quality-audit\第十一轮\probe_topology_precision.py
C:\Python311\python.exe code-quality-audit\第十一轮\probe_multi_hop_gate.py

# 全量回归（15 套件，须用 C:\Python311\python.exe，否则缺 bs4 会假 FAIL）
C:\Python311\python.exe code-quality-audit\regress\run_all.py
```

**结果**：自检 64 / 0；G2 15 套件 523 PASS / 0 FAIL / 全 IDENTICAL。

---

## 八、未修项（留给后续）

1. **`change_animation` 长→短回退 vs `update_animation` 内嵌块只取前两段**（第十轮遗留，未动）。
2. **残渣治不干净**（§五）——需分词库，已预留注入点。
3. **`clique` 的绝对密度仍偏高**（46 节点 / 138 边）——已由分层 + hub 惩罚兜底，
   若后续实测发现"噪声联想"回潮，优先动路径分门槛而非拓扑。
