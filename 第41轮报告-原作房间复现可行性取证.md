# 第 41 轮报告 — 「按原作复现房间（排序 / 连接 / 动态效果）」可行性取证

**日期**：2026-09-24 02:3x　｜　**状态**：**只读取证，未改任何产品代码**（`src/` 零改动，故未跑 G2）
**用户口径（本轮新增，已记为硬约束）**：
> ① 「记忆不要随便修改，三思而后行」
> ② 「所有 room 排序和连接都按照原作，相当于场景复现，连动态效果也要做（如喷泉特效这类的）」

**取证范围**：chapter1（147 房间）为样本；UTMT CLI v0.9.2.0 在 `data.win` **副本**上 `load`，跑前后 sha256 一致（原始文件未碰）。

---

## 一、结论先行

**用户要的三件事，数据全部可得 —— 但现有的导出脚本漏了最关键的几块，且我上一轮记的字段名是错的。**

| 用户要求 | 数据是否可达 | 现状 |
|---|---|---|
| 所有 room **排序** | ✅ `Data.Rooms` 列表顺序即 `room_order` | 已有（`rooms_map.json` 的 `index`） |
| 所有 room **连接** | ✅ `room_goto(<字面量>)` 静态可提；运行时靠 `scr_valid_room` 一族 | **完全没有**（现有仅 26 条手写路由） |
| **场景复现**（画面） | ✅ 但缺 3 类数据（见下） | 只导了「层的名字 + 背景精灵名」 |
| **动态效果**（喷泉等） | ✅ Sprite 帧 + 瓦片动画帧 + 实例 | **完全没有** |

---

## 二、ch1 实证数字（147 房间）

```
层总数 = 1014        Instances 588 / Assets 234 / Background 191 / Tiles 1
几何非零(有视差偏移)的层 = 16
有 Instances 层 = 146/147 ；有 Assets 层 = 111 ；有 Background 层 = 147 ；有 Tiles 层 = 1

Sprites = 1097   总帧数 = 2965   多帧 sprite = 523   ← 动效原料
  帧数分布：1帧×573 / 2帧×174 / 4帧×138 / 6帧×44 / 3帧×41 / 5帧×33 / 8帧×23 …
tilesets = 59   其中 22 个含多帧 GMS2TileIds  ← 瓦片动画
  样例 bg_schooltiles_tileset: tile 20×20, 10 列, 108 帧, GMS2FrameLength=66666μs (15fps)

Code = 1680   指令总数 = 290546
room_goto 字面量 = 94   变量参数 = 10   next = 7   previous = 3
不同目标房间 = 58（ch1 共 147 间）
```

---

## 三、★ 三处必须更正的认知

### 1. 瓦片数据**其实在** —— 上轮脚本把它打成 `"UInt32[]"`

```
LayerTilesData {
   Background : UndertaleBackground   ← tileset（整张图）
   TilesX, TilesY : UInt32
   TileData : UInt32[][]              ← 真·瓦片矩阵，0 = 空
}
UndertaleBackground { Texture, GMS2TileWidth, GMS2TileHeight, GMS2TileColumns,
                      GMS2TileCount, GMS2TileIds[i].ID, GMS2FrameLength }
```
上轮 `dump_rooms.csx` 用 `CollectRefs(ly.TilesData,"TileData")` → `ResolveName()` 对 `UInt32[]`
返回**类型名**，于是 5 章 89,237 个瓦片元素全变成字符串 `"UInt32[]"`。
⇒ **是脚本 bug，不是"原作没数据"**。这直接解开了 Q1-B「按原作瓦片还原」的死结。

### 2. 层几何的真名 = `XOffset / YOffset / HSpeed / VSpeed`（不是我上轮记的那串）

| 我上轮记的（**推测，错**） | 实测真名 |
|---|---|
| `x` / `y` | `XOffset` / `YOffset` |
| `xscale` / `yscale` | **不存在**（缩放只在 `Tile.ScaleX/ScaleY`、实例 `ScaleX/ScaleY`、`BackgroundData.CalcScaleX/Y`） |
| `tiled_h` / `tiled_v` | `BackgroundData.TiledHorizontally` / `TiledVertically` |
| `speed` | `HSpeed` / `VSpeed`（层滚动）；动画速度是 `BackgroundData.AnimationSpeed` |
| 层序 | **`Layers` 列表下标**（+ `LayerDepth` 做绘制排序） |

ch1 只有 **16 个层**几何非零 ⇒ 视差/滚动是**少数情况**，不要按"全都要算"来设计。

### 3. ch1 的画面主载体是 `Assets` 层的 `LegacyTiles`，**不是**瓦片层

ch1 有 234 个 Assets 层、却只有 **1** 个 Tiles 层。画面靠的是 Assets 层里一个个散块：

```
Tile { X, Y, SpriteDefinition, SourceX, SourceY, Width, Height,
       TileDepth, ScaleX, ScaleY, Color, Tpag }
```
上轮导出只取了 `asset_sprites`（**精灵名，没有几何**）⇒ **画面复现的最大缺口在这里**。

---

## 四、动态效果（喷泉类）的三条来源，都在数据里

| 来源 | 数据结构 | 复现方式 | 规模（ch1 / 五章） |
|---|---|---|---|
| **A. Sprite 多帧** | `Sprite.Textures[i].Texture` → `UndertaleTexturePageItem`（源矩形） | 从 texture page 按 tpi 裁帧，按 `GMS2PlaybackSpeed` 播放 | 523 个多帧 sprite |
| **B. 瓦片动画** | `UndertaleBackground.GMS2TileIds[i].ID` + `GMS2FrameLength`(μs) | 每帧换 `TileData` 的 tile id | 22 个多帧 tileset |
| **C. 实例驱动** | 实例 `ImageIndex/ImageSpeed/ScaleX/Y/Rotation` + **对象事件的 GML** | 纯数据不够，需复刻状态机或用帧序列近似 | **98 个**特效类 obj |

**C 的实测样本**（含特效线索的实例名，共 98 个）：
`obj_fountainkris` / `obj_darkfountain` / `obj_ch2_scene25_fountain` /
`obj_ch4_DCA12_darkfountain` / `obj_ch5_DW45_fountain` / `obj_board_waterfall` /
`obj_climb_flamegenerator` / `obj_controller_dw_mansion_fire_paintings` /
`obj_ch2_room_cyber_maze_fireworks` / `obj_castle_cauldron_glow` / `obj_cliff_wind_animator` …

灰命名对象实测（`obj_fountainkris` 的 sprite = `spr_krisu_bright`；`obj_darkfountain` → `spr_fountainedge`）
⇒ 对象与精灵的引用链**可直接读出**，但"怎么动"在 GML 里。

---

## 五、连接的完整图景（两层）

```
① 静态层：room_goto(<字面量>)       —— ch1: 94 条边，58 个不同目标；可全量静态提取
② 顺序层：Data.Rooms 下标            —— room_goto_next ×7 / room_goto_previous ×3 的依据
③ 运行时层：scr_valid_room / scr_add_valid_room / scr_get_valid_room /
           scr_get_room_list / scr_get_room_by_id / scr_get_id_by_room_index
           （另 scr_room 被引用 132 次）—— 原作自己维护了一张「可达房间表」
```
⇒ **原作的"连接"是数据驱动 + 脚本两层**（和我们第 36 轮「路由是数据」的思路同构，但原作自己实现）。
⇒ 「按原作排序连接」= ①+② 静态可拿；③ 需要读这些脚本的数组初始化（可能要 GML 反编译）。

---

## 六、现有导出脚本 `dump_rooms.csx` 的缺口（4 项）

| # | 缺什么 | 影响 |
|---|---|---|
| 1 | 层几何 `XOffset/YOffset/HSpeed/VSpeed` | 视差/滚动层位置错（ch1 16 层） |
| 2 | **Assets 层 `LegacyTiles` 几何** | ch1 画面**基本画不出来**（234 层） |
| 3 | **`TilesData.TileData` 真实矩阵 + tileset 表** | 瓦片画面空缺（ch1 1 层 / ch2+ 数百层） |
| 4 | **实例 `X/Y/ScaleX/ScaleY/Rotation/ImageIndex`** | 场景里所有物件（门、桌、NPC、特效）位置全丢 |
| ＋ | 房间连接边 / Sprite 帧表 / tpi 表 | 排序连接 & 动态效果 |

---

## 七、建议的分级实施（**待用户拍板后再动**）

- **P1-a 静态画面**：扩导出（层几何 + LegacyTiles + TileData/tileset + 实例变换 + tpi 表）→ 渲染器按「层序 → depth」逐层画。
- **P1-b 动态层**：Sprite 帧（523）→ 瓦片动画（22 个 tileset）→ 实例动效（98 个 obj，**逐个复刻、按可见性排优先级**）。
- **P2 房间图**：`Data.Rooms` 顺序 + 五章 `room_goto` 全量边 + `scr_valid_room` 表；
  与现有 `scene_routing.py`（26 条）合并 —— **原作图为真源**，我们的 `when_*` 匹配降为"外部输入 → 定位房间"的适配层。

**需要用户拍板的 3 件（我无法单方面决定，涉及仓库体积/范围）**：
1. **数据落盘位置与体积**：全量导出（层几何 + 瓦片矩阵 + 实例 + 帧表）五章估算 **10~30 MB**；
   放仓库（`ralsei_pet/assets/scenes/_rooms/`）还是放外部目录 + 只留精简索引？
2. **实例动效范围**：98 个特效 obj 是全部复刻，还是先做"用户可见的高频"（喷泉、水面、火焰、闪烁）？
3. **推进顺序**：五章一次性 vs 逐章验收。

---

## 八、本轮取证产物（**都在临时区，未污染仓库**）

```
E:\Download\_tmp\probe41_fields.csx   反射探针（确证字段名）
E:\Download\_tmp\census41.csx         房间/层/Sprite/瓦片普查
E:\Download\_tmp\edges41.csx          连接图提取（Type1-aware，v2 保真）
E:\Download\_tmp\drw\<章>\probe41_fields.txt   字段真相（13,204 B）
E:\Download\_tmp\drw\<章>\census41.txt         普查结果（15,884 B）
E:\Download\_tmp\drw\<章>\edges41.json         连接边（10,379 B，94 条字面量）
```

**★ 一条方法论**：连接图提取的**第一版是错的** —— 我把 `Kind` 当 `"Push"` 匹配（实际是 `"PushI"`），
常量读了 `ValueInt`（实际常量在 `ValueShort`/`ValueLong`，要按 `Type1` 选）⇒ 107 条边里 **91 条是垃圾值**
（`?-1610612620`、`?231188` 这种）。**若不逐条复核，"94 条边"里的假数据会被当成事实写进方案。**
修正后 94/104 条全部落到真实房间名、0 条越界。
⇒ **探针不保真 = 报假数据**；任何"提取成功"的结论，必须先看**样例是否像真的**。
