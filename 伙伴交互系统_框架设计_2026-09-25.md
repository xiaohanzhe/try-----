# 伙伴交互系统 · 框架设计

> 第 46 轮产物。依据：`code-quality-audit/第46轮-原作代码通读/_evidence/原作系统取证46.md`
> 状态：**框架设计 + 骨架，尚未启用**（用户口径：「这个交互系统先设计成这样，我现在还不用，所以你先按照这个搭框架吧」）

---

## 0. 用户口径（原文，逐条对应到设计）

| # | 口径 | 落点 |
|---|---|---|
| 1 | 「嵌入一下和伙伴的交互系统，方便以后多宠物互相互动」 | §1 总架构、§3 名册 |
| 2 | 「做到和真人间对话一样就好」 | §4 消息模型（带打字机/表情/停顿） |
| 3 | 「我会准备另外两个 7B 的设定」 | §3 名册 = **主角 + 2 伙伴**（与原作 `scr_setparty(arg0, arg1)` 的两个队友位一致） |
| 4 | 「一般来说并不会有多个 AI 同时运行的场景；如果可以的话，多个一起运行也看不出端倪那也行」 | §5 **单线程轮转调度**（同一时刻只有一个在生成）⇒ 天然"看不出端倪" |
| 5 | 「要让他们能有自主互相聊天的功能，但不会导致 AI 之间分不清是谁和谁说话」 | §4.2 **发言人标识协议**（本节是全文档最重要的部分） |
| 6 | 「跟随系统按原作的就行，还是跟随 kris 如果 kris 在的话；当然，也可自主行动，只是在需要统一行动时跟随」 | §6 毛毛虫式跟随 + `follow_mode` 三态 |
| 7 | （承接）「背景音乐就用原作的吧」 | §7 BGM 层 |
| 8 | （承接）「该交互的还可以交互」 | §8 可交互物协议 |

---

## 1. 总架构（四层，自上而下）

```
┌───────────────────────────────────────────────────────────────┐
│  L4 场景/宿主层   main.py / scene_controller                   │
│      · 谁在场上（roster 激活哪些伙伴）                          │
│      · 切场景时联动 BGM 淡出（对应原作 obj_doorA_musfade）       │
└────────────────────────┬──────────────────────────────────────┘
                         │ 事件 / 状态
┌────────────────────────┴──────────────────────────────────────┐
│  L3 伙伴调度层   companion_roster.py（新）                      │
│      · 名册（name → 伙伴实例，含别名）                          │
│      · **轮转调度器**（同一时刻只有一个伙伴在"说话"）            │
│      · 跟随模式分发（follow / free / rally）                    │
└────────────────────────┬──────────────────────────────────────┘
                         │
┌────────────────────────┴──────────────────────────────────────┐
│  L2 对话层   companion_dialog.py（新）                          │
│      · Message 模型（from / to / text / expr / typer）          │
│      · **发言人标识协议**（§4.2，防混淆）                        │
│      · 与现有 `persona_chat` / `safety` 护栏对接                 │
└────────────────────────┬──────────────────────────────────────┘
                         │
┌────────────────────────┴──────────────────────────────────────┐
│  L1 实体层   companion.py（新，零依赖）                          │
│      · Companion 数据类（name / 别名 / 人设 / 说话实现 / 位置）   │
│      · 轨迹缓冲（毛毛虫跟随用）                                  │
│      · 可交互物基类 Interactable + on_interact()                │
└───────────────────────────────────────────────────────────────┘
```

**零依赖纪律（继承 `scene_walk` / `scene_pathfind`）**：
L1 只准标准库；L2 只准标准库 + L1；L3 只准标准库 + L1/L2。
**L1 违反 ⇒ import 期崩**（本项目已踩 4 次初始化环）。

★ **本轮施工时加的一条更严的约定**：三个模块**一律不许有函数内 import**，
依赖必须全部写在文件顶部。理由：局部 import 会让"依赖清单能否用 AST 一次看全"
取决于调用路径 —— 复检时正是靠这条抓到 `companion_roster.select_mode` 里
藏着的两个局部 import。判据见 `verify_companion46.A4`。

> ⚠️ **与上文的差异（有意）**：L2 原计划"可 import 项目内对话/护栏模块"，
> 实施时改成**只准标准库 + L1**，措辞类清洗改为**注入回调**（`DialogueLog(cleaner=...)`）。
> 两条理由：① `main.py` 在 import 期建控制器，L2 反向 import `main` / `dialogue_ui`
> 就把初始化环接上了；② `_clean_ai_reply` 是**宿主类的方法**，import 进来会让
> 本层离不开那个宿主、也无法离线回归。⇒ 职责不变，只把耦合方式从 import 改成注入。

---

## 2. 与原作机制的对应表

| 原作 | 我们 | 差异与理由 |
|---|---|---|
| `scr_anyface(name, msgno, emotion)` 名字 dispatch | `Companion.speaker_id` + 名册 `dispatch(speaker_id)` | 照抄；**多宠物必须显式 `from`**，比原作更强 |
| `scr_writetext(msc, msg, fc, typer)` | `Message(text, speaker, expr, typer)` | 照抄四元组 |
| `obj_caterpillarchara`（25 帧缓冲、`target=12+slot*12`） | `Companion.trace` 环缓冲 + `follow_lag` | 照抄；**不做独立寻路** |
| `scr_setparty(arg0, arg1)` | `Roster.activate([a, b])` | 照抄（正好 2 个伙伴位） |
| `scr_interact()` → `event_user(0)` | `Interactable.interact()` → `on_interact()` | 照抄 |
| `global.interact` 锁 + `onebuffer=5` 防抖 | 同名状态 + `INTERACT_COOLDOWN = 5` | 照抄数值 |
| `snd_*` 自建封装 | `BgmManager`（play/stop/fade/query） | 照抄职责分层 |

---

## 3. 名册（Roster）

```python
# 概念模型（L3）
Roster:
    _by_name: {speaker_id: Companion}     # 主键
    _aliases: {alias: speaker_id}          # 别名 → 主键（对应原作 "ral"/"ralsei"）
    _slots:   [Companion | None] * 2       # ★ 两个伙伴位（对应 scr_setparty 的 arg0/arg1）

    def resolve(name) -> Companion | None:
        """名字（含别名、大小写不敏感）→ 伙伴。找不到返回 None（**绝不猜**）。"""

    def activate(names) -> (激活的伙伴, 未解析的名字)
        """对应 scr_setparty。未解析的名字**如实报出**，不静默忽略。"""
```

**主键选择**：**字符串 id**，不是序号 —— 与原作一致
（依据：`scr_anyface` 用 `string_lower(_speakerC)` 比较名字；`scr_get_plat_followers` 用 `name` 字段；
`c_actormoveparty` 用 `scr_findactor("kris")`）。

---

## 4. 对话层

### 4.1 Message 模型

```python
Message:
    from_id:   str            # ★ 必填。说话人 speaker_id
    to_id:     str | None     # 可选。定向说话（私聊/点名）时为对方 id
    text:      str
    expr:      str | int      # 表情变体（对应原作的 emotion 参数）
    typer:     int            # 打字机速度（默认 5，同原作 scr_writetext）
    kind:      str            # 'speech' | 'narrate' | 'action' | 'system'
```

### 4.2 ★★★ 发言人标识协议（全文档最重要）

**问题**：多宠物自主聊天时，AI 会不会"分不清谁在跟谁说话"？

**根因分析**（三条，逐条给对策）：

| 失败模式 | 为什么会发生 | 对策 |
|---|---|---|
| **A. 无主消息** | 模型输出里没写说话人，渲染层靠"上一条是谁"猜 | **`from_id` 必填**。缺失 ⇒ 消息**不进历史**，渲染 `[未知说话人]` 并记警告 |
| **B. 张冠李戴** | 把 A 的发言归到 B 名下（上下文里 A、B 的话混在一起） | 进 prompt 的**每一行**都写成 `「<名字>」<文本>`；**不允许裸文本入 prompt** |
| **C. 自称混乱** | A 在文本里自称成 B 的名字 | 人设里**显式告诉**它自己的名字；护栏加"自称校验"（说了别的伙伴的名字 ⇒ 判退重采样） |
| **D. 代他人发言** | 一条消息里出现 `Susie：滚` 这种"别人开了口"的行 | `check_reply()` 的**外资说话人检测**（只认行首 + 只认冒号/收尾括号） |

> ★ 实装后**措辞有微调**：进 prompt 的行格式是 `[名字] 文本` / `[名字→名字] 文本`
> （不是 `「名字」`）。理由：`[..]` 与 `→` 都是 ASCII/单字符，不与该项目的
> "禁 markdown、少符号"口径冲突，且**点名（`→`）一眼可辨** —— 这正是
> 用户要的"谁跟谁说话"的可见答案。

> ★ 失败模式 D 是**实装阶段新发现的第 4 条**（设计时只写了 A/B/C）。
> 触发场景：模型（尤其小模型）会把"对话"写成剧本格式，一条回复里替两个人发言。
> 检测规则**刻意收窄**（只认行首 + 只认 `：`/收尾括号），因为
> `Susie说她不喜欢这里` 是**正当转述**，判成违规会导致无谓重采样。
> 收窄后的正/负控制成对断言见 `verify_companion46.G3~G5`。

**协议规则（四条铁律）**：

1. **`from_id` 必填，且必须是名册里能 `resolve()` 到的 id（含别名归一）**。
   解析失败不猜、不默认，**如实丢弃并记日志**。
2. **进上下文的每一行都带名字前缀**。格式统一为：
   ```
   [kris] 我们去哪儿？
   [ralsei] 我…我想去城北看看。
   [susie] 随便。
   ```
   ⇒ 与现有 `persona_chat` 的 `recent` 结构对接时**加一层格式化**，不改其内部。
3. **`to_id` 只在"点名"时使用**（`ralsei → susie`）。为 `None` 表示"对所有人说"。
   渲染层据此决定是否高亮/加箭头（**这也是"谁跟谁说话"的可见答案**）。
4. **自主聊天时，调度器保证"上一条不是自己说的"** ⇒ 天然形成 A↔B 往返，
   不出现"A 自问自答"（这正是现有 `_clean_ai_reply` 护栏在单宠场景下防的那类失真）。

### 4.3 与现有护栏的关系

现有 `_clean_ai_reply(reply, recent=)` 已处理：括号 / markdown / 禁说"主人" / **自问自答** / 判退 / 超长。
伙伴系统**不改它**，只在其**外层**加：
- 前置：`from_id` 注入 system 最末尾（★ 保持 KV 前缀缓存命中，见性能铁律）
- 后置：**自称校验**（新增，§4.2-C）

---

## 5. 自主对话调度（"看不出端倪"的关键）

**原则：同一时刻只有一个 AI 在生成。**

```python
class ChatScheduler:
    """轮转式自主对话调度。任一时刻最多一个伙伴处于 GENERATING。"""
    def tick(self, now):
        if self._busy: return                    # ★ 已有在生成 ⇒ 直接返回（不并发）
        speaker = self._next_speaker()           # 轮转 + 话题状态
        if speaker is None: return
        self._busy = True
        self._emit(speaker, ...)                 # 异步请求，完成后 _busy = False
```

**为什么这样"看不出端倪"**：
- 用户口径是"一般不会多个 AI 同时跑"。单线程轮转 ⇒ **永远不会并发**，
  内存/CPU 峰值 = 单个 7B，**表现与单宠完全一致**。
- 若将来要真并行：只需把 `_busy` 换成"每伙伴一个 busy 位" + 一个串行化的输出队列，
  **对外行为不变**。⇒ 现在的设计**不堵死**将来。

**轮转规则**：
- 上一条的 `from_id` **不能**是下一个 speaker（⇒ 天然 A↔B）
- 话题过期 / 无人接话 ⇒ 停（不硬凑，符合"不要为显得勤快而说话"）
- 冷却：两次自主发言之间至少 N 秒（避免刷屏）

---

## 6. 跟随系统

### 6.1 三态（对应用户口径）

| 模式 | 行为 | 触发 |
|---|---|---|
| `FOLLOW` | 跟随主角（毛毛虫式，滞后采样） | **统一行动**时；或主角在移动且距离超阈值 |
| `FREE` | 自主行动（现有 `autonomous_agent` 那套） | 主角静止 / 各自活动时 |
| `RALLY` | 向主角聚拢（比 FOLLOW 更紧） | 要切场景 / 要对话前 |

**默认策略**（用户口径"当然，也可自主行动，只是在需要统一行动时跟随"）：
`FREE` 为常态，**`FOLLOW` 只在"需要统一行动"时切入**。
"需要统一行动"的判据（可配置）：主角开始移动 / 即将切场景 / 主角发起对话。

### 6.2 毛毛虫式跟随（照抄原作）

```python
# L1，零依赖
class Companion:
    TRACE_LEN = 25          # ★ 与 obj_caterpillarchara 的 remx[25] 一致
    def __init__(self, slot):
        self.trace = deque(maxlen=TRACE_LEN)     # 主角位置历史
        self.follow_lag = 12 + slot * 12         # ★ 与 scr_makecaterpillar 一致
```

```python
# L3，每帧
def step_follow(roster, leader_pos):
    for c in roster.active():
        c.trace.append(leader_pos)                       # 全员共享同一条轨迹
    for c in roster.active():
        if c.mode == FOLLOW and len(c.trace) >= c.follow_lag:
            c.pos = c.trace[-c.follow_lag]               # ★ 直接取"滞后 N 帧"那一点
```

> ★ **实装时修正的下标语义**（第 46 轮）：`trace[-1]` 是**刚压进去的当前帧**
> （对应原作 `remx[0] = obj_mainchara.x`，下标 = **年龄**），所以"12 帧前"
> 是 `trace[-13]` 而不是 `trace[-12]`。差一帧肉眼看不出来，但会让
> "滞后帧数 = 12"这句照抄声明**变成假话** ⇒ 改成 `sample_at(lag) = trace[-(lag+1)]`
> 并用 `verify_companion46.C6` 钉死（0..99 递增序列 ⇒ `sample_at(12).x == 87`）。
>
> ★ 与插值版的区别：原作另有 `scr_caterpillar_interpolate`
> （`remx[i] = 主角 + (自己 - 主角) * (i / target)`），那是**插值**读法，
> 需要额外知道"每帧追赶多少"这个速度常数。我们选**滞后采样**，
> 因为它只需要 `target` 这一个已取到的数。

> **为什么不做独立寻路**：跟随者走的是**主角走过的路** ⇒ **天然不撞墙、不卡住**，
> 且与用户"别太死板"不冲突（轨迹本身就是自然的曲线）。
> 第 45 轮的 `scene_walk` 仍然有用 —— 它服务于 **`FREE` 模式的自主任意移动**
> 和 **`RALLY` 时的快速靠拢**（那时需要真寻路）。

**暗世界缩放**：`darkmode=1 ⇒ image_xscale=2`（原作行为）⇒ 我们的伙伴也跟着缩放。

---

## 7. BGM 层

```python
class BgmManager:            # 对应原作 snd_* 封装
    play(name, loop=True)    # snd_play + snd_loop
    stop(fade_ms=0)          # snd_stop（带淡出）
    fade_to(name, ms)        # ★ 换房联动（对应 obj_doorA_musfade）
    pause() / resume()       # snd_pause / snd_resume
    is_playing(name) -> bool # snd_is_playing
    set_volume(v)            # snd_volume
```

**与场景系统接线**：`scene_controller.switch()` 成功切场景后 ⇒
查该场景的 `bgm` 字段 ⇒ `BgmManager.fade_to(scene.bgm)`。
（`_index.json` 的 entry 已有 `bg` 字段；`bgm` 字段待加。）

---

## 8. 可交互物协议

```python
class Interactable:                  # L1 基类
    def interact(self, actor):        # 对应 scr_interact()
        if global_interact_locked: return False
        global_interact_locked = True
        return self.on_interact(actor)   # 对应 event_user(0)

    def on_interact(self, actor) -> bool:
        raise NotImplementedError

    def update(self, dt):             # 对应 obj_readable_Step_0 的解锁逻辑
        if self._dialog_done():
            global_interact_locked = False
            actor.interact_cooldown = INTERACT_COOLDOWN   # ★ = 5（照抄原作）
```

**照抄的三个数值/状态**：`global.interact`（锁）、`myinteract`（对象态）、`onebuffer = 5`（防抖帧）。

---

## 9. 实施计划（分阶段，每阶段可独立验收）

| 阶段 | 内容 | 验收 | 状态 |
|---|---|---|---|
| **P0** | L1 `companion.py`（Companion + Interactable + trace 缓冲） | 回归锁：模型/别名解析/轨迹滞后 | ✅ **已完成** |
| **P1** | L2 `companion_dialog.py`（Message + 发言人协议 + 自称校验） | 回归锁：§4.2 四条铁律逐条正/负控制 | ✅ **已完成** |
| **P2** | L3 `companion_roster.py`（名册 + 轮转调度 + 跟随三态） | 回归锁：不并发、A↔B 往返、跟随滞后正确 | ✅ **已完成** |
| **P3** | 接线：`main.py` / `scene_controller` 挂 BGM + 交互锁 | 真机起，观察日志 | ⏸ **待用户说"可以用"** |
| **P4** | 用户提供 2 个 7B 人设后，灌入名册 | 人味审计（复用 `ollama-persona-audit`） | ⏸ **待用户给人设** |

**P0~P2 已于第 46 轮完成**（不依赖用户的人设）：
- 模块：`ralsei_pet/modules/companion.py`（L3 的 `CompanionStage` 门面也在 roster 里）
- 回归锁：`code-quality-audit/第46轮-伙伴交互框架/verify_companion46.py` → **216/0 全绿**，
  已进 G2（套件 id `companion_round46`）
- 鉴别力体检：`disc_companion46.py` → **17/17 破坏全部报红且命中期望关键词**，
  还原后逐字节一致

**P0~P2 未做（刻意）**：不碰 `main.py` / `scene_controller` / 任何 UI ——
用户口径是「这个交互系统先设计成这样，我现在还不用，所以你先按照这个搭框架吧」。
⇒ 现在加的是**孤立的、零接线的**三个模块，对现有产品的行为影响 = **0**
（G2 42 套件全部 IDENTICAL 就是这条的硬证据）。

---

## 10. 已知风险 / 待拍板

1. **新模块会撞既有闸**：`round5_smoke`（模块计数）与 `scene_p0` 的 **A6 白名单**（`_CTL_ALLOWED`）。
   加模块时必须同步更新，"麻烦"是刻意的（设计成"必须显式登记"）。
   —— **本轮实测结果**：`round5_smoke` 计数 45 → 48，已用
   `--only round5_smoke --update`（**合并模式**）重固基线。
   ❗过程中发现**第 45 轮遗留的基线漂移**：`round5_smoke` 的基线还停在
   **44**（加 `scene_walk.py` 之前），45 轮那轮没刷新 —— 也就是说
   "第 45 轮 G2 全 IDENTICAL"这个结论，在 `round5_smoke` 这一条上其实**不成立**。
   `_CTL_ALLOWED` **无需改**：本轮没动 `scene_controller.py` 的 import
   （P0~P2 是零接线模块）。
2. **自主对话的"话题从哪来"** 已给出可插拔点（`ChatScheduler(topic_source=...)`，
   返回空 ⇒ 不开口），但**默认策略仍未拍板** —— 是纯随机开场、还是由场景/事件驱动
   （"走到教堂了"）？⇒ 建议：**场景/事件驱动**（与第 45 轮的"事件决定目的地"同源），
   待拍板。插拔点已实现并有正/负/异常三态断言（`verify_companion46.J9~J9c`）。
3. **伙伴的动作空间**：只跟随 + 说话，还是也能像主角一样自由走动？
   ⇒ 影响 `FREE` 模式的实现规模，待拍板。
   **当前实现**：`FREE` = "不动，但继续记录主角轨迹"（`K4b` 断言）——
   真自由走动要等 P3 接 `scene_walk`。
4. **原作版权**：文本/音乐只做参考与自建，**不搬运**。
5. **ch3 暂不投入使用**（用户口径）⇒ 伙伴系统也**不把 ch3 场景作为自主目的地**。
6. **`global.interact` 与宿主现有"对话框打开中"状态的关系未接线** ——
   P3 时要确认两边不会各锁一把（不然会出现"对话框开着但还能触发告示牌"）。
