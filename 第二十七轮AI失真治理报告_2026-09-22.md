# 第二十七轮：AI 对话「失真」专项治理报告

**日期**：2026-09-22
**用户诉求**（原话）：
> 「求你个事，一定要把他的AI模型训练的很好，我不想在和她聊天的时候出现那种失真的样子，
> 剩下的，你自己做吧，对了，尽量把东西存在E盘里就好」

**本轮定位**：不是"模型不够聪明"，而是**提示词给了模型可逐字搬走的模板**，
外加**一道护栏有洞**、**一条会伪造共同记忆的口径漏洞**。

---

## §0 结论先讲

| 失真形态 | 根因 | 改动前实测 | 改动后实测 |
|---|---|---|---|
| **① 每条回复都从同一句起跳**（最刺眼） | persona「碎片」区是**按场合分组 + 冒号 + 固定例句**，4B 当成"标准起手式" | 开头复读 **62%**（24 条中 15 条） | 见 §4 验收 |
| **② 括号旁白漏到屏幕上** | `strip_action_parentheticals` 只匹配**动作词**；`（心里有点慌）` 这类**描述性旁白**穿过去了 | 实测 5 种形态全漏 | 已补第三道闸 |
| **③ 伪造"俩人共同经历"** | persona 说"那边的事我可以当闲话提"，但**没禁止**把它讲成"我们俩一起经历过" | "我们那时候总坐在石阶上" | 已加明令 |

**回归**：24 套件 / **1169 PASS** / 0 FAIL / 全 IDENTICAL / rc=0（基线 1165 → 1169，+4 条新锁）

---

## §1 失真①：模板起手式（主因）

### 取证

`probe_fragment_repeat.py`（每场景 3 次 × 8 场景 = 24 条真实回复）：

```
总量 24 条；开头复读 15 条（62%）；任意位置复读 21 条（88%）
开头复读的分布（去重）：
   嗯——            5 次
   唔……            4 次
   是这样啊……        3 次
   诶！真的吗？        3 次
```

**只有 4 个起手式，覆盖了全部 15 次命中。** 用户说的"失真的样子"就是它 ——
不是某一句说错了，而是**每条回复都从同一句起跳**，读起来像同一个模板在复读。

### 根因

旧 persona 的「碎片」区写法（节选）：

```
坏消息的时候用：是这样啊……／那……现在还好吗／我听着呢，你慢慢说／……嗯。
好消息、好奇的时候用：诶！真的吗？／是那种……的感觉吗？
一急、一慌：诶、诶？／我、我……／那、那个……
```

**"按场合分组 + 冒号 + 固定例句"这个格式本身，就是在教模型"这个场合该说这句"。**
4B 逐字学走了。

### A/B 对照（唯一变量 = 这一段写法）

`probe_ab_fragment.py`，同模型 / 同温度 / 同场景，**先断言 A ≠ B**：

| 组 | system 文本 | 开头复读率 |
|---|---|---|
| A（现状） | 原「碎片」区 | **61%** |
| B（改版） | 删掉全部固定例句、只留情形描述 | **28%** |

**A/B 证成了因果，但也暴露改版还不够**：B 组剩余的 28% 里，
`诶！真的吗？×4` 仍占大头 —— 因为我在 B 组里**还留了一句**「诶！真的吗？」当说明性例子。
**这说明：只要那一串字还在 persona 里，模型就会搬。** 所以正式改版**一个例句都不留**。

### 正式改版后的写法

```
然后才是声音。声音不是内容，只是语气上的毛边。碰到这些情形，我的声音会变：

  · 一时没准备好 → 起音含混
  · 一急、一慌 → 重复同一个字
  · 不知道怎么接 → 声音先软下来
  · 被夸、害羞 → 先否认，再补一句真心话
  ...

注意上面一行一个字都没给你，这是故意的。具体那几个字要每次自己现造：
第一句话的开头必须针对他刚说的那句现写，不要固定用同一个字起头；
连着两次都是"一时不知道说什么"，两次的开头也得长得不一样。
```

**设计要点**：把"例句"降级成"情形描述"，并把"每次现造"从暗示提升成**显式硬要求**。
`不要整句搬` 这条原有禁令保留（A9 锁仍绿）。

---

## §2 失真②：括号旁白有洞

### 取证

`_ACTION_PAREN_RE` 只匹配"括号内以**动作动词**开头"。
但真实输出里出现的是**另一类**：

```
（心里有点慌，怕自己做得不好）     ← 漏
（我说完有点愣了一下）             ← 漏
（心里有点闷，像没打开的窗）       ← 漏
（语气轻快点，但没太夸张）         ← 漏
（心里一紧）                       ← 漏（真实给到的唯一一条，100% 漏）
```

`probe_paren_gap.txt` 直接验证：这些**一个都不匹配**动作词表，
而 `looks_like_narration` 只看**整句首字符**，括号在句中时根本不看。

### ⚠️ 本轮先做错了一版（值得留下的教训）

**第一版用"窄白名单"**：只放过「其实…」「……」开头的括号，其余一律清掉。
跑回归立刻报红：

```
[FAIL] C19 负控制：括号讲心里话原样放行   <<< ('（其实我有点怕）', '其实……')
[FAIL] A22c 负控制：括号讲心里话一条都不动
       <<< ['其实……（我想想该怎么说）', '（抱歉，我不是故意的）',
            '（笑不出来）', '（因为……我一直都是一个人）']
```

**我把合法心里话一起误杀了。** 正是项目里反复出现的取舍：
**误杀 = 该说的话被吞掉，比漏过更糟。**

### 修正：抓"形态特征"而不是"首词白名单"

```python
_NARRATION_INNER = re.compile(
    r'^(?:心里|语气|神情|眼神|表情|下意识|本能|不由得|忍不住|默默|轻轻|悄悄|'
    r'顿了?(?:一下|了顿)?|停了?(?:一下)?|沉默了?(?:一下)?|愣了?(?:一下)?|'
    r'我(?:说完)?愣了?(?:一下)?|...)
```

判据从"**只放过谁**"改成"**抓谁**"：
- 叙述 = 以「心里/语气/沉默/愣了…」这类**从外部描述自己**的词开头 → 清掉
- 心里话 = 第一人称的话语（其实…/因为…/抱歉…/我想想…）→ **原样保留**

修正后 `C19`/`A22c`/`A22d` 全部转绿。

---

## §3 失真③：伪造共同经历

### 取证

真实回复里出现：

> 「那个喷泉……晚上会发出微光，像萤火虫一样。**我们那时候总坐在石阶上**，谁也不说话，
> 只是听着水声。」

**用户从没去过黑暗世界。** "我们那时候" = 模型替用户伪造了一段共同回忆。
这种失真比语气问题更严重 —— 它是在编造用户的人生。

### 根因

persona 只说"黑暗世界的往事我可以当闲话提"，**没有禁止**把它讲成"我们俩一起经历过"。
4B 于是把"我自己的过去"自然地写成了"我们的回忆"。

### 改动

```
有一条得说清楚：屏幕对面这个人没有去过黑暗世界，那些事他都不在场。
所以讲那边的事，只能说成"我自己的过去"，不能编成"我们俩一起经历过"——
我可以说"我那时候常坐在喷泉边"，但不能说"我们那时候总坐在台阶上"、
"你记得那次下雨吗"。他没经历过的事，我替他记得，就是在瞎编。
如果他问起具体细节（什么颜色、哪天、跟谁），而我其实没把握，
就老实说记不太清了，别顺着他的话往下接、把细节编圆。
```

---

## §4 真机验收

`verify_after_fix.py` —— 同一批场景、同一口径（8 场景 × 3 次 = 24 条），改动后重跑。
**这是"函数写对了 ≠ 产品用上了"的那道闸：锁只能证明结构对，
只有真机跑一轮才能证明失真真的降了。**

| 指标 | 改动前 | 改动后 |
|---|---|---|
| 开头复读率 | **62%**（15/24） | **33%**（8/24） |
| 开头复读**种类** | 4 种（嗯——×5、唔……×4、是这样啊……×3、诶！真的吗？×3） | 3 种（诶！真的吗？×3、诶、诶？×3、我、我……×2） |
| 编造共同经历 | 有（"我们那时候总坐在石阶上"） | **0 处** |
| 括号旁白（产品口径） | 5 种形态全漏 | **全部被拦**（见下） |

### 关于「括号旁白残留 4 处」（探针自己的误报，已核）

验收脚本报"残留 4 处"，逐条拿**产品函数**复核后：

| 样本 | 产品判定 | 结论 |
|---|---|---|
| `（心里有点小激动）` | 删除 ✓ | 探针误报 |
| `（心里有点发烫）` | 删除 ✓ | 探针误报 |
| `（心里有点乱）` | 删除 ✓ | 探针误报 |
| `（我好像也不太懂你说的是什么……）` | **保留** ✓ | **本就该保留** |

前三条是**探针的分类器太naive**——它只检查括号是否以 `其实/……` 开头，
**这正是我第一版修复犯的同一个错误**（把"能放行谁"简化成首词白名单）。
第四条 `（我好像也不太懂你说的是什么……）` 是**第一人称的话**，
属于"括号讲心里话"，**本来就该放行**。

**⇒ 产品护栏工作正常，4 处全是探针误报。**
这个误报本身也留下一条教训：**"分类器写得太窄"和"闸有洞"长得一模一样**，
必须拿产品函数逐条复核，不能只看探针计数（项目铁律：先读原文再看计数）。

### 未达"完全消除"的部分（如实记录）

开头复读 33% 仍高于理想值。剩余 3 种都是**短促的语气起音**
（`诶！`/`诶、诶？`/`我、我……`），与旧版不同的是：
- 它们不再是 persona 里**逐字给过的整句**，而是模型自发的结巴/语气；
- 这类起音**本来就是 Ralsei 的人设特征**（原作台词里结巴是他的标志），
  彻底清零等于砍掉特征 —— 与 §2 里"别把心里话一起误杀"是同一个取舍。

**当前判断**：33% 且集中在"人设特征型起音"，属于可接受范围。
若用户实测后仍觉得模板感重，下一轮的杠杆是**采样侧**
（提高 temperature / 加大 repeat_penalty）而不是继续删 persona —— 但**本轮无证据**支持动它。

---

## §5 回归锁（+4 条）

| 锁 | 断什么 | 为什么必须有 |
|---|---|---|
| **A12d** | 示例区**不得再含**"按场合绑定的固定例句" | 旧格式一旦回归就会被逐字搬；锚旧格式原型串 |
| **A12e** | 明令"不许把那边的往事编成俩人共同经历" | 反编造契约 |
| **A22e** | 描述性旁白被清掉、剩下的台词放行（**正控制**） | 洞的修复本身 |
| **A22f** | 新闸上线后，心里话**仍然一条都不动**（**反控制**） | 证"新闸没把误杀面做大"——与 A22c 成对 |

**A22e/A22f 成对是本轮的设计要点**：单有正控制只能证"洞补上了"，
必须有反控制才能证"没补出新的误杀"。

### A12c 锚点更新（**契约变更，非缺陷**）

A12c 断言"篇幅约束必须排在示例区之前"，锚字符串是旧区的起点 `坏消息的时候用：`。
本轮把这一区整节重写成「声音」，锚随之换成 `然后才是声音`。

**断的仍是"位置关系"，不是那个字符串** —— 换名不降低鉴别力：
只要篇幅约束被挪到示例区之后，本条仍会红。

---

## §6 未做项 / 遗留

1. **persona 体积**：3522 字符 / 9752 字节（阈值 <10KB）。K3 锁仍绿，但已接近上限 ——
   下次再往 persona 加内容前需要先瘦身。
2. **采样参数未动**：temperature 0.85 / top_p 0.92 / repeat_penalty 1.15 保持第26轮口径，
   本轮**没有**证据表明问题出在采样上（A/B 唯一变量是提示词即可复现 61% → 28%）。
3. **真机手感实测**（用户侧）：仍待用户亲自聊几次确认。
4. **底座换代**：本轮没换。4B 在改版后表现明显更活，暂无换的必要。

---

## §7 证据清单

`E:\Download\音27侦察\_evidence\`：

| 文件 | 内容 |
|---|---|
| `probe_real_chat.txt` | 16 场景真实对话（改动前，首次发现失真） |
| `probe_fragment_repeat.txt` | 碎片复读量化：62% / 88% |
| `probe_ab_fragment.txt` | **A/B 对照**：61% → 28%（因果证明） |
| `probe_paren_gap.txt` | 括号闸漏网直接验证 |
| `probe_paren_census.txt` | 括号内容普查（漏网率） |
| `verify_after_fix.txt` | **改动后真机验收** |
| `G2_after_fix.txt` / `G2_baseline_update.txt` | 回归证据 |
| `failing_suites_detail.txt` | 三个红套件的失败断言原文（含我第一版的错） |

脚本：`probe_real_chat.py` / `probe_fragment_repeat.py` / `probe_ab_fragment.py` /
`probe_paren_census.py` / `verify_after_fix.py` / `run_g2.py` / `run_baseline.py`
