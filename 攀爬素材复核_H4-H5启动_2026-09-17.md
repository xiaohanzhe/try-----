# 攀爬素材复核 + H4/H5 启动（2026-09-17）

> 提交 `a8bd975`　｜　推送已核验：远端 == 本地 == `a8bd975`（`ls-remote` 两个代理均通）
> 详细证据：`code-quality-audit/架构改造-H4H5/_evidence/`

---

## 一、攀爬素材：结论翻转 —— **配置本来就是用户指定的那两组**

**权威证据**：用户当年 `AskUserQuestion` 的答复**原文**（会话 jsonl）：

> 「`spr_ralsei_climb_1_0~4.png` 这是朝向右边的素材；`spr_ralsei_climb_0_degrees_0~5.png`
> 这是朝向前面的素材；然后相反方向的你就给他翻转一下就好，没有朝后面的因为那样也用不上」

| 用户要求 | 现状 | 判定 |
|---|---|---|
| 朝右 = `spr_ralsei_climb_1_0~4.png` | `climb_right` = `spr_ralsei_climb_1_0..4`（5 帧） | ✅ 一致 |
| 朝前 = `spr_ralsei_climb_0_degrees_0~5.png` | `climb_front` = `spr_ralsei_climb_0_degrees_0..5`（6 帧） | ✅ 一致 |
| 相反方向"翻转一下就好" | `climb_left` = 上一组**水平镜像真落盘**（5 帧，非 `alias_of` 顶替） | ✅ 一致 |
| 没有朝后的（用不上） | 不注册任何 180° 变体 | ✅ 一致 |

**双证**

1. 运行时渲染 `第十八轮/_evidence/climb_frames_preview.png`：三行分别是**侧视朝右 / 镜像朝左 /
   正面朝前**，`missing_or_null = 无`、`anim_miss_report = []`、`frame_container_size = (222,110)`（未退化）。
2. 项目自带闸门 `round18_climb`：**PASS 53 / FAIL 0 / 与基线 IDENTICAL**，其中含"逐像素断言镜像关系
   `left(x,y)==right(w-1-x,y)`"及其**负控制自检**（对未镜像的同一张图必须报不符）。

**结论：`animations.json` 与 `sprite_loader._builtin_animation_mapping` 均无需改动。**

### 撤回上一轮的错判

上一轮回复（`39d7946`）里的三条，第 2、3 条作废：

- ❌「所以 `climb_1`~`climb_6` 是同一循环的六个角度变体，我挑错了索引」——**"存在更多变体" ≠ "用户给的是别的"**。
- ❌「`0_degrees` 是背面/看不见脸那套」——与用户口径冲突，且 0°/45°/90° 是**转角命名**，0° = 正面。
- ✅ 仍成立：`climb_0_degrees`(6) 与基础 `climb`(4) 同图；`climb_90_degrees`(6) = `climb_1~6` 各第 0 帧；
  `climb_left` ⇔ `climb_1` 逐帧严格镜像（全盘唯一一对）。

**根因**：拿"磁盘上的冗余素材"去推翻"用户明确指名的两组"。

### 磁盘上未注册的攀爬变体（不是错误，供确认）

`ralsei_climb`(4) · `climb_2`…`climb_6`(5×5) · `climb_45_degrees`(6) · `climb_90_degrees`(6) ·
`climb_poses`(3) · `climb_poses_alt`(3)。
→ **若"朝右/朝前"其实指上面某一行，给一个行号即可**，改 `animations.json` 两处指向并按新的"朝右"组
重生成镜像（`第十八轮/make_climb_left_assets.py` 幂等）。

### 顺带修正一条能力误判

**PNG 可以直接读**（本轮读了 `climb_sheet.png`、`climb_frames_preview.png` 与自制的 20× 放大图，全部成功）。
此前"图片一律读不了"的结论是错的 —— 以后判朝向/外观**先看图**，不要用哈希 + ASCII 点阵硬推
（点阵只能看轮廓与尺寸，判不了朝向与前后脸）。

---

## 二、H4/H5：闸门重扫 + 三项决策裁定 + Wave 1 开工准备

### 新工具（只读，`code-quality-audit/架构改造-H4H5/`）

| 工具 | 作用 | 证据 |
|---|---|---|
| `scan_method_index.py` | ast 方法索引（起止行 / 行数 / Wave 归属） | `_evidence/method_index.{txt,json}` |
| `scan_zero_refs.py` | **AST 计名字引用点**（不做字符串匹配，防注释/文档串误命中） | `_evidence/zero_refs.txt` |

### 基线刷新（方案 §2 的 8794 行 / 186 方法已过期）

`main.py` **9588 行**；`RalseiPet` **191 个方法 / 方法体 8772 行**。

### G1 重扫结果

1. **排期点名的 W1-5（17 个办公 `check_*`）已不复存在** —— 第六轮随"删除内置台词入口"一起删除，
   留痕 `main.py:5002-5003`；`check_browser_windows` / `check_ppt_windows` / `check_excel_table_needs`
   **全项目 AST 零引用**确认。等价能力现由 `modules/desktop_interaction.py`（`check_ppt_files()` 等）承担。
   → **W1-5 从 Wave 1 表划掉。**
2. 本轮新查出 **1 个全项目零引用方法**：`create_person_name_table`（`main.py:4057`，56 行）。

### 三项决策（技术细节由我拍板，已写入方案 §8）

1. **顺序 → 先做 H4-Wave 1**（H5 S1–S3 已完成，"H5 先行"的动因已消化，G4 护栏在位）。
2. **G1 → 不整体清 185 项，改为"每项 PR 内做该项专属零引用筛查"**（方案 §3 末的倾向项；工具已就位）。
3. **Wave 3 明确不在本次范围**（Wave 1+2 完成后重新评估）。

### Wave 1 施工顺序（行号为本轮实测）

**W1-3 → W1-4 → W1-1 → W1-2 → W1-6**

| 项 | 位置 | 规模 | 备注 |
|---|---|---|---|
| **W1-3 游戏 → `GamesController`** | 6186–6449（7 方法） | 258 行 | **第一项**：块内闭合、零互写，入口仅 `dialogue_ui.py:1445/1449/1453` |
| W1-4 视频 → `VideoWatchController` | 4427–4751（8 方法） | 289 行 | 入口 `check_entertainment_needs:4829` |
| W1-1 施法 → `SpellFlowController` | 8748–9040（4 方法） | 290 行 | 9 个 `_spell_*` |
| W1-2 躲猫猫 → `HideAndSeekController` | 9050–9402（12 方法） | 342 行 | 与 W1-1 有 4 条属性互写；`_hide_ralsei`(445) 是托盘隐藏，勿并入 |
| W1-6 文件表格 → `FileSheetController` | 存活 2+1+3 方法 | ~370 行 | **先处置死代码**（`create_person_name_table` 等） |
| ~~W1-5 办公检测 → `OfficeAppDetector`~~ | — | — | **❌ 已删除，划掉** |

**W1-3 开工即含本项零引用筛查**（已跑：7 个方法全部有真实调用点，不存在整块死代码）。
