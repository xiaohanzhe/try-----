# 第 43 轮报告 —— 原作「房间切换」与「背景移动」机制取证

> 触发口径（用户原话）：「**具体 room 切换和背景如何移动可以看原作的代码**」
> 取证对象：`data.win`（GMS2，五章副本）·工具：UTMT CLI v0.9.2.0 ·**全程只 load 不写回，前后 sha256 一致**
> 归档：`code-quality-audit/第43轮-原作门与相机取证/{_tools,_evidence}/`
> 详版：`.workbuddy/memory/参考-契约与历轮（详版）.md` **§44**

---

## 0. 结论先行

| 问题 | 答案 | 判据 |
|---|---|---|
| **背景怎么移动？** | **不是视差层，是相机平移**。背景层被"钉"在房间坐标系上，相机一动背景就跟着滚 | ch1 **1014 个层里只有 16 个**几何非零，且 **HSpeed/VSpeed 全 = 0**；`EffectType` 全 null |
| **相机多大？** | **现实世界 320×240（2× 放大到 640×480 窗口）；暗世界 640×480（1:1）** | `view[0]` 分布：640×480×100 间 / 320×240×40 间；`GeneralInfo.DefaultWindow = 640×480` |
| **谁在动相机？** | `obj_mainchara` 的 **End Step 每帧写 `view_xview[0]/view_yview[0]` 并钳制到房间边界** | `gml_Object_obj_mainchara_Step_2`（319 指令，全量反汇编） |
| **相机操作收口在哪？** | **单点**：`__view_set(prop,index,val)` → `__view_set_internal` → `camera_set_view_*` | 6 个 `camera_set_view_*` **各只有 1 个调用者** = `gml_GlobalScript___view_set_internal` |
| **房间切换长什么样？** | **黑幕淡出（0.42s）→ 停 BGM → 建 persistent 黑幕 → `room_goto` → 新房间每帧 alpha−0.08 淡入（0.42s）** | `obj_doorA_musfade` 的 `Other_19`/`Alarm_3`/`Alarm_2` + `obj_persistentfadein`（**persistent=True**） |
| **切换全程多久？** | **≈ 0.92 秒**（30 fps） | `GMS2FPS = 30`；`alarm[3]=14`、`alarm[2]=15`、`fadespeed=±0.08` |
| **有没有"传送门/进度条"式过渡？** | **没有**。不存在 `scr_transition` / `scr_fade` / `scr_room_change` | `callers43.txt` 明写「该 code 不存在」 |
| **落点怎么定？** | 目标房间**预摆好的 `obj_marker*` 实例**；`scr_dark_marker()` 可动态造标记 | `obj_markerA..F/X/W/r..w` 共 14 个 marker 对象；`scr_dark_marker` = `instance_create(obj_marker)` + `depth=-9` + 2× 缩放 |

**★ 最省事的一条**：**「排序」「连接」「相机」「过渡」四件事全都只依赖"房间的名字 + 房间的宽高"**，不依赖任何美术资源 ⇒ 桌面端要复现原作观感，**不需要**原作素材也能先把机制跑对。

---

## 1. 房间切换机制（完整时序，五章共用）

### 1.1 一个门 = 一个不可见触发区

| 事实 | 判据 |
|---|---|
| 画面上的"门"**不是对象画出来的** | **全部 `obj_door*` 的 `Visible = False`**（`objmap43.txt`）。门的美术在**背景图/瓦片**里 |
| 门对象只负责"碰到就换房" | `obj_doorA_musfade` 全部代码 = `Create_0`(2) + `Alarm_3`(5) + `Alarm_2`(12) + `Other_19`(37) |
| 换房方向 = 沿 `Data.Rooms` **下标位移** | 第 42 轮已实证（`doorA/B/C/D/E/F` = ±1/±2/±3；`doorX/doorW` = 表驱动双向） |
| 落点 = 目标房间里的配对 marker | `obj_doorA` → 目标房间的 `obj_markerA` |

### 1.2 完整时序（反汇编，ch1 / ch2 双侧验证）

```
① 玩家碰到 obj_door*（触发区）
   ↓
② Other_19（= Room Start）:
      touched == 0 时:
        interact        = 3
        instance_create(0, 0, obj_fadeout)        ← 黑幕对象（ch1=143, ch2=193）
        mus_volume(..., -5, ..., currentsong)     ← BGM 音量渐降
        alarm[3] = 14                             ← 30fps ⇒ 0.467s
        alarm[2] = 15                             ← 30fps ⇒ 0.500s
        touched = 1
   ↓
③ Alarm_3（第 14 帧）:
      snd_free(currentsong, 0, -5)                ← 停 BGM
   ↓
④ Alarm_2（第 15 帧，此时屏幕已全黑）:
      entrance = 1
      instance_create(0, 0, obj_persistentfadein) ← ★ persistent=True，跨房间存活（ch1=145, ch2=195）
      room_goto_next()  /  room_goto(room_next(room_next(room)))  /  doorX·doorW 表驱动
   ↓
⑤ 新房间 Room Start（Other_4）:
      obj_persistentfadein 重算
        length = 10 + room_width  / 4
        height = 10 + room_height / 4
        x = -20, y = -20                          ← 覆盖整个新房间
   ↓
⑥ 新房间每帧 Draw_0:
      image_alpha += fadespeed        (fadespeed = -0.08)
      draw_sprite_ext(spr_pixel_white, image_index,
                      x, y, length, height, 0, image_blend, image_alpha)
      if image_alpha <= 0 -> instance_destroy()
```

**实测参数**：
| 参数 | 值 | 来源 |
|---|---|---|
| 黑幕精灵 | `spr_pixel_white` | `obj_fadeout`/`obj_fadein`/`obj_persistentfadein` 的 `spr` |
| `image_blend` | `0`（**纯黑**） | `Create_0` 首两条 |
| `fadespeed` | `+0.08`（淡出）/ `-0.08`（淡入） | `obj_fadeout_Create_0` / `obj_persistentfadein_Create_0` |
| 移动端（`os_type==1`） | `fadespeed = 0.14` | `obj_fadeout_Create_0` 有 os_type 分支 |
| 覆盖范围 | `x=y=-20`，`scale = 10 + room_wh/4` | 即**每帧按新房间尺寸自适应** |
| 淡出→换房 | 15 帧 = **0.500 s** | `alarm[2] = 15` @ 30 fps |
| 亮起 | 12.5 帧 = **0.417 s** | `1 / 0.08` |
| **合计** | **≈ 0.92 s** | 两者相加 |

★ **过渡是"全黑 + 自适应尺寸"，不是"滑动/擦除"**。
`obj_fadechain`（带 `hspeed/vspeed/bdir` 的那个位移动画对象）**存在但只被创建 1 次**——由 `obj_skychain_Step_0`（"天空链"特效）创建，**与房间切换无关**（`inst43c.txt` 全量普查实证）。

### 1.4 对象创建普查（① 换房操作是最高频的视觉操作）

ch1 全量 **1306 个 `instance_create*` 调用点**（`inst43c.txt`，解析器经三重锚点校验）：

| 被创建对象 | 次数 | 说明 |
|---|---|---|
| `obj_dialoguer` | 450 | 对话框 |
| `obj_writer` | 70 | 打字机 |
| `obj_dbulletcontroller` | 46 | 弹幕控制器 |
| **`obj_fadeout`** | **45** | ★ 黑幕淡出 |
| **`obj_shake`** | **45** | 镜头震动 |
| **`obj_persistentfadein`** | **37** | ★ 持久黑幕淡入（= 换房过渡本体） |
| `obj_soliddark` / `obj_darkener` / `obj_npc_room` | 24 / 21 / 16 | — |

★ **`obj_fadeout` 45 次 + `obj_persistentfadein` 37 次**。而且**全部 16 个 `obj_door*`（A~F/X/W，含 `_musfade` 变体）都是同一种写法**：

```
obj_door*_Other_19  -> instance_create(obj_fadeout)          ← 淡出
obj_door*_Alarm_2   -> instance_create(obj_persistentfadein) ← 持久淡入
                       + room_goto*()
```
⇒ **换房机制在编译期就是"模板化"的，16 个门逐字同构**。

其他定向创建点（各 1~2 处）：`obj_panner` ← `scr_pan`；`obj_marker` ← `scr_marker` / `scr_dark_marker`；
`obj_backgrounder_sprite` ← `obj_town_event_Create_0` / `obj_thrashafter_follow_Create_0`。
⇒ **`obj_backgrounder_*`（背景对象）是"按需创建"的，不是每房间常驻**。

### 1.3 ch2+ 的差异（不影响机制）

ch2 的 `Alarm_2` 比 ch1 多 4 条指令：多了 `event_user(7)`；`Other_19` 多 `event_user(8)`。
⇒ **同一套机制，每章挂自己的钩子**。

---

## 2. 背景移动机制（= 相机）

### 2.1 事实：没有视差

ch1 全部 **1014 个层**：

```
几何非零的层 = 16        （XOffset/YOffset 有值）
HSpeed/VSpeed 非零的层 = 0  ← ★ 零视差
EffectEnabled = True  的层 = 1014
EffectType 非 null 的层 = 0   ← ★ 无 GMS2 层特效
```

那 16 个非零层**全是 `Compatibility_Background_*`**（GMS1→GMS2 迁移产物），只有静态偏移，例如：

| 房间 | 层 | X | Y | 背景精灵 |
|---|---|---|---|---|
| `room_beach` | `Compatibility_Background_0_bg_beach` | 17 | 0 | `spr_leaves_shaking` |
| `room_dark_chase1` | `Compatibility_Background_1_bg_darkstone_2` | 0 | 560 | `bg_darkstone_2` |
| `room_castle_front` | `Compatibility_Background_0_bg_darkcastle_front` | 190 | 0 | `bg_darkcastle_front` |
| `room_forest_castlefront` | `Compatibility_Background_0_bg_cardcastle_outside` | 0 | 300 | `bg_cardcastle_outside` |

⇒ **不存在"远景层慢速、近景层快速"这种视差**。所有东西（背景、瓦片、实例）都在**同一个房间坐标系**里，相机一移全都跟着移。

### 2.2 相机尺寸（决定"镜头有多远"）

每房间 `view[0]` 的实测分布（ch1，147 间）：

| view 尺寸 | port 尺寸 | 房间数 | 含义 |
|---|---|---|---|
| **640×480** | 640×480 | **100** | 1:1 —— 暗世界 / 大场面 |
| **320×240** | 640×320 | **33** | **2× 放大** —— 现实世界（小镇/家/学校） |
| 320×240 | 640×480 | 7 | 2× 放大（另一种 port） |
| 1024×768 | 1024×768 | 6 | 未使用占位 |
| 640×768 | 1024×768 | 1 | 特殊 |

窗口基准：`GeneralInfo.DefaultWindowWidth = 640` / `DefaultWindowHeight = 480`；`Options.Scale = 1`。

★ **现实世界是"放大 2 倍"的**：320×240 的视野塞进 640×480 窗口 ⇒ 人物看起来大、能看到的范围小 ⇒ 这正是小镇场景"靠得很近"的观感来源。

`view[0].obj`（GMS2 内置跟随目标）：**115 间为空 / 32 间 = `obj_mainchara`**。
★ 但**这只是声明**——真正的每帧跟随是代码做的（见 2.3），所以**不能用 `view.ObjectId` 判断某个房间有没有相机跟随**。

### 2.3 每帧相机更新（`obj_mainchara` 的 End Step，319 指令全解）

**现实世界分支**（`global.darkzone == 1` 时走另一支）：

```
wd = x - floor(view_wview[0] / 2 - initwd / 2)
ht = y - floor(view_hview[0] / 2 - initht / 2)
view_xview[0] = wd
view_yview[0] = ht
# —— 钳制到房间内（四个方向各一次） ——
if view_xview[0] <  0                       : view_xview[0] = 0
if view_xview[0] >  room_width  - view_wview[0] : view_xview[0] = room_width  - view_wview[0]
if view_yview[0] <  0                       : view_yview[0] = 0
if view_yview[0] >  room_height - view_hview[0] : view_yview[0] = room_height - view_hview[0]
```

**暗世界分支**（`global.darkzone == 1`）：

```
view_object[0] = -4                         ← 关掉 GMS2 内置跟随
wd = x - floor(view_wview[0] / 2) + 11      ← ★ 横向 +11 px 偏心
ht = y - floor(view_hview[0] / 2) + 17      ← ★ 纵向 +17 px 偏心
view_xview[0] = wd ; view_yview[0] = ht
（同样的四向钳制）
if bg == 1 : event_user(0)
```

★ 两个要点：
1. **相机是硬跟随，没有插值/缓动**（一帧到位）；"顺滑感"来自 `view_hspeed/vspeed` 极少被用。
2. **暗世界把主角故意偏到画面中心偏左/偏上**（+11/+17），因为队伍成员跟在身后。

### 2.4 相机平移的"第二个入口"：脚本化平移

| 对象/脚本 | 作用 | 代码要点 |
|---|---|---|
| `obj_panner` | **定时平移**（`obj_panner` **只被 `scr_pan` 创建 1 次**） | `view_xview[0] += panx` / `view_yview[0] += pany`；`pantimer++`；`pantimer >= panmax` ⇒ `instance_destroy()`。`Create` 里 `panx=pany=0, panmax=1, pantimer=0`，且 `if global.plot == 11 { pantimer = -1 }` |
| `scr_pan_to_obj` | **过场平移**（6 处调用：`scr_endcombat`、`obj_susieandlancer_event`、`obj_scarelancerevent`、`obj_darkcastle_event`、`obj_lancerchaseevent`） | 118 指令；内部经 `scr_pan` 造 panner |
| `obj_shake` | **镜头震动**（全 ch1 被创建 **45 次**，由 `scr_damage` / 各事件触发） | `instance_exists(147)` 在 `obj_mainchara_Step_2` 里被检查 |
| `shakex` / `shakey` / `shakeamt` / `shaketimer` … | 震动变量族 | 变量出现次数：`shakex` **213**、`hurtshake` **106**、`shakeamt` **41**（`var43.txt`） |

### 2.5 相机操作的**唯一收口**

```
游戏代码里的 GMS1 风格变量
       ↓   __view_get(prop, index)   [476 次调用引用 / 123 个调用者]
       ↓   __view_set(prop, index, value)  [61 次 / 21 个调用者]
  __view_set_internal(prop, index, value)   ← 唯一一个调 camera_* 的地方
       ↓
  camera_set_view_pos / _size / _angle / _border / _speed / _target
  view_set_camera / _visible / _xport / _yport / _wport / _hport / _surface_id
```

`__view_set_internal` 是一个 **prop 0~16 的分派器**（224 指令，反汇编直读）：

| prop | 转成 | GMS1 对应 |
|---|---|---|
| 0 | `camera_set_view_pos(cam, val, cam.view_y)` | `view_xview` |
| 1 | `camera_set_view_pos(cam, cam.view_x, val)` | `view_yview` |
| 2 | `camera_set_view_size(cam, val, cam.view_h)` | `view_wview` |
| 3 | `camera_set_view_size(cam, cam.view_w, val)` | `view_hview` |
| 4 | `camera_set_view_angle(cam, val)` | `view_angle` |
| 5 / 6 | `camera_set_view_border(...)` 分 x / y | `view_hborder` / `view_vborder` |
| 7 / 8 | `camera_set_view_speed(...)` 分 x / y | `view_hspeed` / `view_vspeed` |
| 9 | `camera_set_view_target(cam, val)` | `view_object` |
| 10 | `view_set_visible(index, val)` | `view_visible` |
| 11~14 | `view_set_xport/_yport/_wport/_hport` | `view_xport` 等 |
| 15 | `view_set_camera(index, val)` | `view_camera` |
| 16 | `view_set_surface_id` | `view_surface_id` |

**相机对象从哪来**：`camera_create()` **只被 `gml_GlobalScript___init_d3d` 调用 1 次**（41 指令）。
⇒ **整章全局只有一台相机**，所有房间共用它，切房时只改参数不重建。

### 2.6 深度（顺带取证，直接决定"谁压在谁上面"）

`scr_depth()`（21 指令，**69 处调用**）：

```
depth = 100000 - (y * 10 + sprite_height * 10)
```

⇒ **深度完全由 y 决定，越靠下越靠前**；`y*10` 说明纵向 0.1 px 的精度分级。这是原作"2.5D 俯视"观感的核心公式。

---

## 3. 对桌面端的映射建议（不改产品代码，仅给口径）

| 原作机制 | 桌面端等价做法 | 备注 |
|---|---|---|
| 相机 = 320×240（现实世界） | 桌面场景用**同样的"视口"概念**：屏幕是一个窗口，房间是比它大的画布 | 桌面天然就是"一个视口"，无需照搬分辨率 |
| 相机硬跟随主角 + 四向钳制 | 若桌宠会走出屏幕，用同一套钳制公式 | 4 条 if，零依赖 |
| 无视差层 | **不要做视差**。背景整张按 1:1 铺在房间坐标上 | 现 `bg/` 的 236 张图可直接用 |
| 门不可见、画在背景里 | 传送口（暗之泉）**可以**可见——原作里 `obj_darkfountain` 的 `Visible = True` | 与 `obj_door*` 相反，别搞混 |
| 换房 = 黑幕 0.42s 出 + 0.42s 入 | 用同一个 `spr_pixel_white` 铺满 + alpha 动画，**时长 0.42s/段** | 30fps 口径 |
| 换房时停 BGM | 原项目 `sound_manager` 只有 3 音效、无 BGM ⇒ 只需淡出音效 | 见详版 §37 |
| 落点由目标房间的 marker 决定 | 在场景 JSON 里给每个房间加 `entries: {doorA: (x,y), ...}` | 可直接从 `inst42.json` 的 `obj_marker*` 坐标批量生成 |
| `depth = 100000 - (y + spr_h) * 10` | 桌宠/道具的 z 序按 y 排序 | 已有"建楼"线用 z 序，此处是原作的等价口径 |

---

## 4. 取证方法与判据（可复现）

### 4.1 工具（`_tools/`，18 个）

| 文件 | 作用 |
|---|---|
| `types43.csx` | 反射出 UTMT 真实类型名（**嵌套类型必须带外类名**） |
| `view43.csx` | 房间 view 实值 + 每层几何/效果 + 层背景精灵 |
| `func43.csx` | 关键字函数普查 + code 名单 + 部分 code 指令流 |
| `func43b.csx` | 全量函数普查（`func43_all.txt`）+ 调用者索引 v1 + code 导出 |
| `func43c.csx` | 调用者索引 **v2**（用 `ValueFunction` 全名，v1 的裸名会全 0） |
| `func43d.csx` | 精确导出 **`gml_GlobalScript_*` 真实函数体**（★ 关键修正，见 4.3-2） |
| `func43e.csx` | 定向导出（`fadechain` / `backgrounder` / `panner` / `marker`） |
| `var43.csx` | 变量名 / 字符串 / 内建 三份全量普查 |
| `objmap43.csx` | 对象索引 ↔ 名字映射（解码 `instance_create` 的整型参数） |
| `inst43.csx` | 对象创建普查 **v1**（错的，留档作反例） |
| `inst43b.csx` | 对象创建普查 **v2**（错在"变量不占槽"，留档作反例） |
| `inst43c.csx` | 对象创建普查 **v3**（★ 正确版，三重锚点校验） |
| `gi43.csx` | `GeneralInfo` / `Options`（**帧率 30**、窗口 640×480） |
| `run43.py` | 统一跑法（`load` + `-s`，关 stdin，跑前后 sha256 比对） |
| `an43.py` / `an43b.py` | 把 `*_codes.txt` 渲染成可读指令流 |
| `an43c.py` | 在长函数里按关键词定位（打印命中行 ±1 上下文） |
| `mx43.py` | 汇总/交叉核对脚本（对象索引跨章一致性、view 分布、层视差） |

### 4.2 判据

- **`data.win` 只读**：每次运行打印 `data.win 未改动: True`（跑前后 sha256）
- **函数调用次数**：`Data.Code` 全量遍历 `Instructions[*].ValueFunction` 计数
- **调用者索引**：按 code 去重后记录引用者（`callers43.txt`）
- **跨章验证**：`obj_persistentfadein` 在 ch1=145 / ch2=195，两章 `Alarm_2` 的 `instance_create` 常量分别命中 ⇒ **机制一致、索引不同**

### 4.3 本轮踩到的 5 个坑（每一个都会产出"假结论"）

1. **★ 类型名必须带外类**：`UndertaleRoomView` **不存在**，真名是 `UndertaleRoom+View`。
   CSX 里写 `typeof(UndertaleRoomView)` ⇒ `CS0246` ⇒ **整个脚本挂掉、零输出**。
   修法：先跑 `types43.csx` 反射真实全名。
2. **★★ 脚本有"两个 code"**：
   ```
   gml_Script_<name>        ins=0     ← 空桩（调用方引用的就是这个名字）
   gml_GlobalScript_<name>  ins=N     ← ★ 真实函数体在这里
   ```
   我第一版按 `gml_Script_` 前缀导出，**全部拿到 ins=0**。修法：导出用 `gml_GlobalScript_` 前缀。
   ⇒ **"函数被调用 N 次"与"函数体在哪"是两个不同的 code 名**，必须分开处理。
3. **★ 调用者索引不能用裸名**：`TARGETS` 写 `scr_depth` ⇒ 0 命中；写 `gml_Script_scr_depth` ⇒ 69 命中。
   ⇒ **别把"0 引用"当成"没人用"**。（与第 42 轮"误报清单"同型）
4. **★★ GMS2 字节码的实参【从右往左】压栈**。
   `instance_create(x, y, obj)` 的压栈顺序是 **`obj, y, x`**。
   判据（三个独立 A/B 锚点，全部命中）：
   | 站点 | 期望 | 实测 |
   |---|---|---|
   | `obj_doorA_musfade_Alarm_2` | `obj_persistentfadein`(145) | ✓ 145 |
   | `obj_skychain_Step_0` | `obj_fadechain`(250) | ✓ 250 |
   | `obj_getsusieevent_Create_0` 第 3 处（源码变量名就叫 `door`） | `obj_doorA`(97) | ✓ 97 |
   ⇒ 取对象参数应该用 **`pend[Count - 参数个数]`**。
5. **★★ 数"压栈槽位"必须把变量 peek 也算进去**（本轮最坑的一个，连着错两版）：
   - v1 取 `pend[Count-1]` ⇒ 拿到的是 `x` ⇒ 门全被解成 `idx=0`；
   - v2 改成 `pend[Count-3]`，但 `pend` **只收集了整数常量**，**变量 push（`PushVar/PushLoc/PushBltn/PushGlb`）没占槽** ⇒
     `obj_skychain_Step_0` 里 `push 2; push 1; push 250; push y; push x` 被压缩成 `[2,1,250]` ⇒ 解出对象 index = **2**（错）。
   - v3 修法：**所有 `Kind.StartsWith("Push")` 的指令各占一槽**（值取不到就记 `"?"`）。
   ⇒ **判据：先设计三个"已知真值"的锚点，解析器过了锚点才允许用它的输出。**
   ⇒ 教训写进记忆：**"提取成功"≠"提取正确"；没有锚点校验的解析器不得用于出结论。**

---

## 5. 遗留 / 待拍板

1. **落点表尚未批量导出**：`obj_markerA..F/X/W/r..w` 的**实例坐标**只在第 42 轮 `inst42.json` 里按房间登记过（过滤键含 `marker`），但**未按"门↔标记"配对整理成 `entries` 表**。建议下轮做（零新成本，纯重组已有 JSON）。
2. **`initwd` / `initht` 的赋值来源未定位**：两者在 ch1 各出现 **2 次**（`inst43b.txt` 变量复核实测），
   且都在 `obj_mainchara_Step_2` 内部（现实世界相机公式里）。
   ★ **本条是我第一版的错误结论——当时写"只出现 0 次（`var43_all.txt` 无）"，实际是我"没去搜"就当成"不存在"。**
   不影响机制结论（去掉它只差一个常量偏移）。
3. **`global.darkzone` / `global.plot` 的取值域未枚举**：`plot` 至少出现 11 / 245 两个值。属剧情状态机，本轮不展开。
4. **是否要把本机制落成产品代码**（相机/过渡/深度）—— **等用户拍板**。本轮**只读取证，`src/` 零改动**。
5. **ch2~ch5 未做全量 `view43` 普查**（只做了 `objmap43` + 一个 `func43` 抽样）。理由是机制在引擎侧、五章共用；若后续要"五章都跑对"，建议补跑。

---

## 6. 与第 42 轮的衔接

| 第 42 轮给了 | 第 43 轮补上 |
|---|---|
| 门 = 沿 `Data.Rooms` 下标位移 | **门触发后到新房间之间的 0.92 秒里发生了什么** |
| 门全部 `visible:False` | **为什么不可见**：黑幕对象 `obj_persistentfadein` 才是视觉主体 |
| 五章 782 条有向边 / 455 无向 | **边怎么走**：相机硬跟随 + 四向钳制；无视差 |
| `_room_order.json` / `_room_graph.json` | **新增可用事实**：每房间 `view` 尺寸 + 房间宽高 ⇒ 可直接算"镜头行程" |
| `dump_rooms.csx` 缺 5 项 | **再补 3 项**：`View` 族字段、`Layer.EffectType`（实测恒 null）、`GeneralInfo.GMS2FPS` ⇒ 缺项降为 **4 项**（均为美术资源侧） |
