# 人味改造 · 世界观按需召回 + 平级口径 + 关系演进

**轮次**：第二十轮（2026-09-19，接第十九轮"世界观节与禁语兜底"）
**回归**：G2 全量 **PASS=1135 / FAIL=0 / 24 套件**（基线已用合并模式重建）
**套件**：`persona_chat` **93 → 129 PASS**（新增 J/K/L 三组 36 条锁）

---

## 1. 本轮要解决的问题（用户原话）

> 「没必要每次让他说话的时候都读取完整的世界观啊，可以按照语言的关联性去单个读取
>   或是一定范围内读取，就和咱那个记忆系统一样，或是说这个世界观本就是他自己自带的初始记忆，
>   还有，别把我的身份变成"主人"，我和他平级，他也不是为了什么而来的，他只是来了仅此而已」

> 「朋友关系是**逐渐**的，**开始并不是朋友**……他只是一个**意外**来到我桌面上的人，
>   而后期和我们相处的关系是**一步一步搭起来**的。你可以考虑加入一个**信任度**，
>   这个信任度**不可见**，只是作为你们之间关系好坏的一个评估标准。
>   也就是说，开始他是害怕我们对他的举动，到**不愿意继续和我们说什么**，再逐渐成为朋友」

拆成三条：

| | 诉求 | 落点 |
|---|---|---|
| **C1** | 世界观别每轮全读 | 按需召回（新文件 + 新模块） |
| **C2** | 平级、不是"主人"、无使命 | persona + **代码侧 5 处** |
| **D** | 关系要一步步长出来（信任度不可见） | 新模块 + 四档演进 |

---

## 2. C1：世界观从"每轮全读"改为"想起才读"

### 2.1 改动

| | 改前 | 改后 |
|---|---|---|
| 存放 | `ralsei_persona.md` 的「我知道的世界」一节 | 新文件 `assets/ralsei_worldview.md`（11 个语义块） |
| 体积 | 4768 B / ~600 tok（占全文件 **36%**） | 常驻为零；命中才追加 ≤2 块（≤700 B） |
| persona 常驻 | 13140 B / ~4380 tok | **9269 B / ~3100 tok**（省 32%） |
| 召回 | 无 | `modules/worldview_recall.py`，与 `memory_system.recall_text` **同口径** |

### 2.2 为什么"省 token"是次要收益

**主要收益是保 KV 前缀缓存。** Ollama 只复用"从头逐字相同"的那一段，
而世界观原是 system 中部的**固定长文本** —— 它一旦因任何原因发生位移，
后面**所有**内容（口癖 / 我怎么说 / 语气示范 / 上下文）的缓存全部作废。
抽掉它，恒定骨架变成一段**更短也更稳**的前缀，命中率反而更高。

### 2.3 召回质量（实测）

| 输入 | 召回 |
|---|---|
| 黑暗喷泉是啥 | 世界怎么运作 ✅ |
| 庆典那天发生了什么 | 庆典那天 ✅ |
| 你还记得 Dess 吗 | 那件事的结局 ✅ |
| 你们去过哪些地方 | 走过的地方 ✅ |
| **我最近好累啊** | **（空）** ✅ |
| **今天吃什么** | **（空）** ✅ |

后两条是**负控制**，也是最要紧的一条：用户要的是"按关联性读取"，
不是"什么都想起来"。若只测"能召回"，把阈值调到 0（全命中）也能通过 ——
那就成了"每轮还是在倒设定"，等于没改。

### 2.4 两条硬约束

- **`MAX_BLOCKS=2` 是硬顶，不是默认值。** 实测抓到：原先 `limit=99` 能召回 6 块
  （`limit` 参数绕过了 `MAX_BLOCKS`）。已改为 `min(limit, MAX_BLOCKS)`。
  persona 里"一次别倒太多"是写死的口径，召回是它的**唯一执行点**，留个口子等于口径失效。
- **无命中返回空串**，不拿某块硬兜底 —— 硬兜底会让他在无关话题上突然讲设定（违反"别把话题硬拐"）。

---

## 3. C2：平级口径

### 3.1 persona 侧

- 全部 **22 处"主人" → "你"**，只保留 **1 处**（禁令本身："我不用管他叫'主人'"）。
- 「我现在在哪」整节重写，去掉使命叙事（"他把我带到这台电脑上 / 让我陪着他 /
  这是我现在的日常"），改为：

  > 我是一个人待在这儿的，谁也没有非让我来不可的理由 —— 我就是来了，仅此而已。
  > ……他和我是平级的……我不会一直围着他转，我有我自己的事、自己的心情。

### 3.2 代码侧（**改 persona 不改这些等于白改**）

"主人"在代码里还有 5 处会**真的发进模型请求**：

| 位置 | 改动 |
|---|---|
| `_build_ai_context` 冷落提示 ×2 | `主人已经很久没跟你说话了` → `你有很久没跟对方说话了` |
| `_build_ai_context` 名字提示 | `主人的名字是…` → `对方的名字是…` |
| `_PERSONA_FALLBACK` | persona 读失败时的兜底，同步改成平级口径 |
| `memory_system.recall_text` 的 `who` | `'主人'` → `'你'` |
| `start_autonomous_speech` 两条 prompt | `主人主动来找你` → `对方主动来找你` |

**`_role_marker_re()` 里的「主人」刻意保留** —— 那是"认得旧残留角色标记、防漏截一整条
自问自答"的截断闸，与"不许拿它当称呼"不矛盾，两条锁分工明确（A23 vs J6）。

---

## 4. D：关系演进（本轮最实质的一条）

### 4.1 设计

新模块 `modules/relationship.py`：

- **四档**：`distrust`（害怕戒备）→ `guarded`（不愿多说）→ `warming`（慢慢熟）→ `friend`
  —— **逐字对齐用户原话顺序**，锁 L2 钉死。
- **起始 `TRUST_INITIAL = 0.12`** → 落在 `distrust` 档。开局**不是朋友**。
- **上界 `TRUST_MAX = 0.95`**（留余量，成为朋友后仍需维持）。
- **单轮涨幅硬顶 `GAIN_CAP_PER_TURN = 0.05`** + **边际递减**（`delta *= 1 - trust`）
  —— 这是"一步一步"的技术落点：从 0.1 涨 0.02 是明显的，从 0.9 涨 0.02 几乎推不动。
- **久未互动回落**：>3 天开始，每天 −0.01。信任度是"关系**好坏**评估"，必须能往回走。

### 4.2 信任度**不可见** —— 做法与理由

**数值绝不进提示词。** `build_brief()` 只输出两样东西：

1. **这个阶段是什么态度**的行为指令（不含数值、不含档位名）；
2. **一句显式禁令**：不许把它当话题说、不许提"信任/关系/阶段"、不许打分说百分比。

理由：本项目反复实测过"**任何进 system 的量，模型都会找机会念出来**"。
把 `trust=0.42` 放进去，4B 一定会说"我现在信任你 42%"。
锁 **L3（无数字）/ L4（无档位名）/ L5（有禁令）** 三条一起钉。

### 4.3 实测抓到的 bug（值得记）

连打 400 次 `harsh`，信任度跌到 **0.0**，穿破下界。

第一反应是把下界设成 `TRUST_INITIAL` —— **这是错的**：那样 `harsh` 在开局就
完全失效（因为一开局就在下界），信任度只剩好的一端，退化成进度条。

最终定为 **`[0, TRUST_MAX]`**：`0` = 戒备到极点，仍归"害怕戒备"档。
并加 **L8b 反向控制**确保 `harsh` 不是死事件。

### 4.4 persona 里必须删掉静态关系句

C2 先写的"但我们是一起待着的关系"**太热** —— 它等于**一开局就已经是朋友**，
会把动态演进顶掉。已删除，并改为显式声明：

> 我们俩是什么关系，不是定死的……具体现在是哪一步、该用什么态度对他，
> 我另有一句话告诉你，以那句为准。

锁 **L13（负控制：无静态热关系句）/ L14（正向：声明以那句为准）**。

---

## 5. 锁的连带修改（世界观搬家的必然结果）

A14–A22 原断言在 `PERSONA` 里找世界观锚，正文搬走后**全红**。
视图改为 **`PERSONA + _WV_TEXT` 并集**：这组锁防的是"**存不存在**"，
位置由 K1/K2 专门管。

A7 / A12 / A15 / G4 / G8 / G9 的措辞锚同步更新，
**每条都保留了鉴别力**（把措辞改回旧的必然变红，不是恒真）。

---

## 6. 新增锁一览（J/K/L 三组 36 条）

| 组 | 管什么 | 条数 |
|---|---|---|
| **J** | 平级口径的**接线**（代码侧 5 处 + 兜底人设 + 截断闸保留） | 6 |
| **K** | 世界观按需召回（搬走了 / 该想起想起 / 闲聊不召回 / 上限 / 接线 / 初始化环） | 10 |
| **L** | 关系演进（起始戒备 / 不可见 / 路径有序 / 边界 / 回落 / 分类 / 接线 / 无静态热关系句） | 17 |

---

## 7. 遗留

1. **真机 A/B 行为探针未做**（用户 2026-09-19 明确"还没办法实测"）：
   关系演进与世界观召回的**真机行为级**未验 —— 目前锁到契约级 + 单元级。
   可用的探针已备好：`verify_persona_worldview.py`（12 题真机探针，上轮 PASS=12）。
2. **配对真机脚本**可再补一个"关系演进"探针：跑 N 轮好/坏互动，看语气是否真的变化。
3. 编码存量告警（`.bak` 与 `code-quality-audit/` 部分旧文件）未清理，等用户点头。
