# 场景系统 · 扩展接口设计方案

> 日期：2026-09-22 ｜ 状态：**方案（只出设计，不动产品代码）**
> 用户诉求（原话）：「我后期会给他添加场景系统，也就是**把原作的世界搬到桌面上**，
> 你要**留好拓展接口**哦（**桌面也会被我当成一个场景**，你自己看一下原作的场景切换这类的，
> 为后期添加做准备）」
>
> 依据：`场景系统架构地图_2026-09-22.md` + Deltarune 原作机制调研（`code.deltawiki` 代码实证）
> 基线：G2 `PASS=1169 FAIL=0`（本方案**未改任何产品代码**）

---

## 0. 设计纲领（先看这一页）

### 0.1 用户的两句话 → 两条硬约束

| 用户原话 | 我读出的硬约束 |
|---|---|
| 「把原作的世界**搬到桌面上**」 | 场景不是"宠物窗口里贴张背景图"。**桌面才是画布**，场景是**洒在桌面上的**一层东西。 |
| 「**桌面也会被我当成一个场景**」 | **「桌面」必须是场景表里的一个普通场景**（`scene_id = "desktop"`），走**完全同一条**切换管线。**不许给它开后门**，也不许让它成为"没有场景"的特殊态。 |
| 「留好**拓展接口**」 | 这轮**只做接口与骨架**，不实现美术内容。判定标准 = **以后加一个新场景，不用改 `main.py`，只加一个数据文件**。 |

### 0.2 六条设计原则

1. **数据驱动**：场景 = **一份数据**（`assets/scenes/*.json`），不是一段代码。
   新增场景**零改核心**。
2. **复用既有预留接口**：`customization_system.py` 已埋 `background`/`home_location`
   + 两张清单 + 4 处空注释 —— **先接管它们，不另起炉灶**（架构地图 §6）。
3. **桌面是平等的一等场景**，`scene_id="desktop"`，与其他场景同构。
4. **只管"氛围 + 物件 + 上报"，不管"自主行为"**：
   场景事件**必须走 `ai_driver.note_event()`**（架构地图 §9.3 —— 唯一合法通道），
   **不许场景自己播特殊动画**（用户第八轮明令）。
5. **不侵入物理**：地面判定继续走 `floor_manager.desktop_floor`；
   场景物件"可站立"是**可选能力**（P4），默认关闭。
6. **AI 只吃一句场景描述**，不塞长文本（保 persona 体积 K3 锁 + 保 KV 缓存）。

### 0.3 从原作抄什么、不抄什么（调研结论落地）

| 原作机制（代码实证） | 本项目如何落地 |
|---|---|
| `scr_roomname(id)` 的 **ID→显示名解耦**（55/61 都叫 Castle Town） | ✅ 抄。`display_name` 与 `scene_id` 分离 |
| `scr_get_room_by_id` 的**章号进万位** | ✅ 抄。`scene_id` 用 `ch1.field_start` 分层命名 |
| `obj_musicer` + **`snd_is_playing` 防重播守卫** | ✅ 抄。场景 BGM 只在"该换"时换 |
| `global.plot` **一个标量驱动"场景长什么样"** | ✅ 抄。`progress` 字段做条件分支 |
| `global.flag[]` + **位掩码** | ✅ 抄（简化版）。`flags` 存场景记忆 |
| **`marker` 命名落点**（优先于像素坐标） | ✅ **重点抄**。`spawn` 用锚点名，适配任意分辨率 |
| `scr_depth()` 脚底排序 `100000-(y*10+h*10)` | ✅ 抄。物件与角色前后遮挡 |
| `obj_dw_transition` 剪影走门 + **drone 音高 0.1→1.0** | ✅ 抄（转场签名，辨识度来源） |
| `scr_become_dark/light()` 双向换算（木剑↔铅笔） | ✅ 抄。同一物件在桌面/暗世界两种形态 |
| **640×480 锁定画布** | ❌ **不抄**。桌面是任意分辨率 |
| **一屏一房间**的房间粒度 | ❌ **不抄**。改为"区域 + 命名锚点" |
| **`scr_destroyoutside` 销毁出界实例** | ❌ **改造**。改为"出界挂起 / 入界恢复" |
| **摄像机跟随主角** | ❌ **不抄**。桌面场景摄像机静止 |
| 30 FPS 上限 | ❌ 不抄（保留逐帧动画低换帧率即可） |

---

## 1. 场景是什么：三级模型

```
Scene（场景）
 ├─ id            唯一标识      "desktop" | "ch1.castle_town" | "ch1.field_start"
 ├─ display_name  显示名        "桌面" | "城堡镇" | "希望与梦想之原"
 ├─ meta          元信息        章节 / 区域 / 氛围 / 代表色
 ├─ ambience      氛围层        背景图 / 色调 / 光照 / 天气 / 粒子
 ├─ bgm           音乐          {track, volume, pitch, fade_in}
 │   └─ variants[]  按 progress/flags 分支（抄 obj_musicer_town 模式）
 ├─ objects[]     可交互物件    见 §1.2
 └─ messages      进/出场台词    enter / exit（可带条件）
```

### 1.1 三级粒度（对应原作的"章 / 区域 / 房间"）

| 级别 | 本项目 | 例子 | 桌面映射 |
|---|---|---|---|
| 章 | `chapter` | `ch1` ~ `ch5` + `hometown` | — |
| 区域 | `area` | `castle_town` / `field` / `cyber_city` | — |
| **场景** | `scene` | `ch1.field_start` | **桌面 = `desktop`（同级、同构）** |

> **原作的「房间」在这个项目里对应「场景」**；但**不再"一屏一场景"** ——
> 一个场景可以横跨整块屏，靠 `anchor`（命名锚点）定位内容。

### 1.2 物件模型（复用既有 `desktop_elements` schema）

架构地图 §11 已确认：`desktop_interaction.desktop_elements` 每项含
`x/y/width/height/weight/material/is_fragile/temperature/texture/rect` —— **现成可用**。

场景物件的 schema **在它之上加场景特有字段**：

```json
{
  "id": "savepoint_01",
  "kind": "savepoint",              // savepoint | door | readable | chest | decoration
  "sprite": "spr_savepoint.png",
  "anchor": "town_center",          // ★ 命名锚点（优先，分辨率无关）
  "offset": [0, 0],                 // 相对锚点的微调
  "rect": [0, 0, 32, 48],           // 命中框（相对自身左上角）
  "depth_mode": "foot",             // foot（脚底排序）| fixed
  "solid": false,                   // 是否可站立（P4 能力）
  "interact": {
    "trigger": "click",             // click | walk_into | hover
    "once": false,                  // 抄原作的 once 默认语义（这里按需显式）
    "requires": {"progress": 0},    // 条件：progress / flags
    "note_event": "我凑近了城堡镇的存档点",
    "say_pool": ["..."]             // 可选内置台词（pool=None 则沉默）
  },
  "decoration": false               // true = 纯装饰，无逻辑（抄 obj_marker 模式）
}
```

> **`decoration: true` 的物件零逻辑** —— 抄原作 `obj_marker`：
> 场景美术不需要每个都写类，统一成"marker + sprite"。

### 1.3 锚点表（`marker` 机制的本项目落地）

```json
// assets/scenes/_anchors.json —— 全局锚点，场景之间共享
{
  "desktop": {
    "left":        {"x_ratio": 0.04, "y": "floor"},
    "center":      {"x_ratio": 0.50, "y": "floor"},
    "right":       {"x_ratio": 0.96, "y": "floor"},
    "taskbar_top": {"x_ratio": 0.50, "y": "floor_top"}
  },
  "ch1.castle_town": {
    "entrance":  {"x_ratio": 0.10, "y": "floor"},
    "square":    {"x_ratio": 0.50, "y": "floor"},
    "fountain":  {"x_ratio": 0.82, "y": "floor"}
  }
}
```

- **`x_ratio`（0~1 比例）+ `y: "floor"`（贴地）** ⇒ **分辨率无关**。
- 这是对原作 `marker` 的忠实搬运，也是"桌面任意分辨率"的唯一可行解。

---

## 2. 目录结构（新建，不动现有文件）

```
ralsei_pet/
├── assets/
│   └── scenes/                     ← ★ 新增：场景数据（纯数据，无代码）
│       ├── _anchors.json           全局锚点表
│       ├── _index.json             场景清单（id → 文件 + 显示名 + 分类）
│       ├── desktop.json            ★ 桌面本身 = 一个普通场景
│       ├── ch1/
│       │   ├── castle_town.json
│       │   ├── field_start.json
│       │   └── scarlet_forest.json
│       ├── ch2/  ch3/  ch4/  ch5/
│       └── hometown/
│           ├── school.json
│           └── library.json
├── modules/
│   └── scene_system.py             ← ★ 新增：场景运行时（加载/切换/查询）
│       └── scene_controller.py     ← ★ 新增：控制器（复用转发壳模式）
└── assets/scenes/backgrounds/      ← 背景图素材（待补，见 §7）
```

> **关键**：新增一个场景 = **在 `scenes/` 下加一个 `.json` + 在 `_index.json` 加一行**。
> **不碰 `main.py`** —— 这是本方案的验收标准。

---

## 3. 核心接口（签名即契约）

### 3.1 `modules/scene_system.py`（运行时，无 Qt 依赖）

> **纪律**：`scene_system.py` **不许 import Qt、不许 import 项目内模块**
> （初始化环铁律，同 `event_speech.py`）。宿主能力全部由传入参数提供。

```python
# ========== 数据层 ==========
def load_index(assets_dir=None) -> dict:
    """读 _index.json。返回 {scene_id: {file, display_name, chapter, area}}。
    读不到 → 返回 {}（静默降级，不抛）。"""

def load_scene(scene_id, assets_dir=None) -> dict | None:
    """读单个场景定义。失败返回 None（调用方决定回落）。"""

def load_anchors(assets_dir=None) -> dict:
    """读全局锚点表。"""

# ========== 查询层（纯函数，可单测）==========
def resolve_anchor(scene, anchors, anchor_name, screen_rect) -> tuple[int,int]:
    """★ 命名锚点 → 屏幕像素坐标。
    输入 screen_rect=(x,y,w,h)（虚拟屏或当前屏），输出 (px, py)。
    锚点不存在 → 回落到场景的 'default' 锚点 → 再回落屏幕中心。"""

def scenes_for(chapter=None, area=None) -> list[str]:
    """按章/区域筛场景 id。"""

def pick_variant(variants, progress=None, flags=None, chapter=None) -> dict | None:
    """★ 抄 obj_musicer_town：从有序 variants 里挑第一条条件满足的。
    variants = [{"when": {"progress_min": 290}, "track": "rain.ogg"}, ...]
    条件支持：progress_min / progress_max / progress_eq / flag_is / flag_min / chapter_eq
    """

def visible_objects(scene, progress=None, flags=None) -> list[dict]:
    """按条件筛可见物件（requires 不满足的物件不出现）。"""

def depth_of(y, sprite_height, base=100000, k=10) -> int:
    """★ 抄 scr_depth()：depth = base - (y*k + sprite_height*k)。
    depth 越小越靠前（画在上层）。"""

# ========== 状态层（纯内存，宿主负责持久化）==========
class SceneState:
    """场景运行时状态。**实例由宿主持有**（不放在 controller 里，见 §5 铁律）。"""
    def __init__(self):
        self.current_scene   = None     # str | None（None 只在启动瞬间）
        self.previous_scene  = None
        self.progress        = 0        # 全局剧情进度（抄 global.plot）
        self.flags           = {}       # 场景记忆（简化版 global.flag）
        self.visited         = set()    # 访问过的场景 id（抄 once 语义）
        self.spawn_anchor    = None     # 本次入场落点锚点

    def mark_visited(self, scene_id): ...
    def is_visited(self, scene_id) -> bool: ...
    def set_flag(self, key, value=True): ...
    def get_flag(self, key, default=None): ...
    def to_dict(self) -> dict: ...      # 交给 data_store 持久化
    @classmethod
    def from_dict(cls, d): ...
```

### 3.2 `modules/scene_controller.py`（控制器，复用转发壳）

```python
class SceneController:
    """场景系统控制器。

    纪律（照抄 games_controller.py）：
      * 不许 import 任何项目内模块（避免初始化环）
      * __init__ 只存 self.p，不碰宿主其它属性
      * 实现控制器侧 __getattr__ 回落给宿主（照抄 games_controller.py:97）
    """
    def __init__(self, ralsei_pet):
        self.p = ralsei_pet

    # ---------- 查询（供 UI / AI 链路用）----------
    def current_scene(self) -> dict | None: ...
    def scene_description(self) -> str:
        """★ 给 AI 用的一句话场景描述。
        返回如 "城堡镇" / "桌面上" / ""（无场景时返回空串，调用方自己跳过）。"""

    # ---------- 切换（唯一入口）----------
    def switch_to(self, scene_id, *, spawn=None, transition=None, reason="user"):
        """★ 场景切换唯一入口。
        spawn: 落点锚点名（None = 用场景默认）
        transition: 'none'|'fade'|'door'（默认读配置）
        reason: 'user' | 'story' | 'auto'

        实现要点（必须遵守）：
          1. 幂等：scene_id == 当前 → 直接 return False
          2. 先 __exit__ 旧场景（停 BGM / 隐藏物件 / 记 previous）
          3. 写 state（current_scene / spawn_anchor / visited）
          4. 解析锚点 → 把宠物移到落点（复用 _clamp_pos_to_desktop）
          5. 播放转场（若有）
          6. __enter__ 新场景（起 BGM / 显物件 / 报 note_event）
          7. 持久化（宿主 save）
        """

    def back(self): """回上一场景（previous_scene）。"""

    # ---------- 物件 ----------
    def objects_at(self, screen_pos) -> list[dict]:
        """★ 某屏幕坐标上有哪些场景物件（命中检测）。
        宿主 mousePressEvent 在判 sprite_label 之前调它（架构地图 §11 P2）。"""
    def on_object_interact(self, obj, *, source="click"):
        """★ 物件交互唯一出口。
        只做三件事：记 flag / 报 ai_driver.note_event / （可选）speak_event。
        ★★ 绝不自己 play_animation（用户第八轮明令）。"""

    # ---------- 配置 ----------
    def list_scenes(self) -> list[dict]: ...
    def enabled(self) -> bool: ...
```

### 3.3 宿主需要新增的状态（**必须预声明**，`main.py:806` 后）

```python
# ========== 场景 (scene) 状态字段 ==========
# ⚠️ 铁律：控制器会用到的字段必须在此预声明，否则控制器 __setattr__ 判据
#    （"宿主已拥有该名字"）不满足 → 状态劈成两份（见 main.py:770-806 血泪注释）。
self._scene_state     = None    # SceneState 实例（由 SceneController 创建后回填）
self.current_scene    = None    # 当前场景 id（str），与 _scene_state.current_scene 同步
self.scene_objects    = []      # 当前场景可见物件（缓存）
self._scene_bg_win    = None    # 背景窗口句柄（若采用独立背景窗口方案）
self._scene_transition= None    # 转场进行中标记
```

---

## 4. 桌面 = 一个场景（用户明确要求）

### 4.1 `scenes/desktop.json`（骨架示例）

```json
{
  "id": "desktop",
  "display_name": "桌面",
  "chapter": null,
  "area": "desktop",
  "builtin": true,
  "meta": {
    "mood": "日常",
    "palette": ["#1e1e2e", "#3b3b52"],
    "note": "桌面本身就是一个场景。不加背景图，保持透明——桌面自己的样子就是它的美术。"
  },
  "ambience": {
    "background": null,
    "tint": null,
    "lighting": "ambient",
    "weather": "inherit",
    "particles": []
  },
  "bgm": {
    "track": null,
    "variants": [
      {"when": {"progress_min": 1}, "track": "an_another_day.ogg"},
      {"when": {}, "track": null}
    ]
  },
  "anchors": ["left", "center", "right", "taskbar_top"],
  "objects": [
    {
      "id": "desktop_portal_01",
      "kind": "door",
      "sprite": "spr_door_dark.png",
      "anchor": "right",
      "decoration": false,
      "interact": {
        "trigger": "click",
        "once": false,
        "note_event": "我凑近了那扇通往那边的门",
        "action": {"switch_scene": "ch1.castle_town"}
      }
    }
  ],
  "messages": {
    "enter": ["又回到桌面了。"],
    "exit":  ["那……我先过去一下。"]
  }
}
```

### 4.2 为什么"桌面必须是普通场景"（三条理由）

1. **用户明说**：「桌面也会被我当成一个场景」。
2. **工程上**：若给桌面开后门（"没有场景时等于桌面"），
   就会有 **两套代码路径**（有场景 / 无场景）→ 必然出现"切回桌面时漏掉某个恢复动作"
   这类 bug。**统一走 `switch_to("desktop")` 只有一条路径。**
3. **可扩展**：以后"桌面"想加东西（比如桌面上的传送门、桌面专属 BGM），
   **改 `desktop.json` 即可，不加代码**。

### 4.3 `background: null` 的语义

桌面的 `ambience.background = null` **不是"没实现"，是"刻意的"**：
桌面美术就是 Windows 桌面本身，宠物窗口保持透明穿透。
这就是 §0.3 里"背景渲染方案"对桌面场景的正确答案。

---

## 5. ★ 接线点清单（施工时的精确坐标）

> 每条都已核验行号（架构地图 §11）。**本方案不动它们，只登记位置。**

| # | 动作 | 文件:行号 | 备注 |
|---|---|---|---|
| 1 | `SceneState` 等 5 个字段**预声明** | `main.py:806` 后 | ⚠️ 漏了 → 状态劈两份 |
| 2 | `_CONTROLLER_ATTRS` 加 `'scene'` | `main.py:512` | 元组追加 |
| 3 | `self.scene = SceneController(self)` | `main.py:761` 后、`764` 前 | 此时 customization/floor/sprite 已就绪 |
| 4 | **AI「载体状态」分片加场景** | **`main.py:6902`** | 加 `elif self.current_scene and self.current_scene != 'desktop':` |
| 5 | 鼠标命中前插物件判定 | `main.py:5068` 前 | 先判物件，再判 `sprite_label` |
| 6 | 右键菜单加"场景"子菜单 | `main.py:5679` | `show_interaction_menu` |
| 7 | `customization_system` 接管 | `customization_system.py:400` | 把空注释换成真调用 |
| 8 | 清单换 Deltarune 命名 | `customization_system.py:308,336` | 或改为读 `scenes/_index.json` |
| 9 | 物件上报走 `note_event` | `ai_driver.py:285` | **唯一合法通道** |
| 10 | 可选：场景物件作为 floor | `floor_manager.py:195` | P4，默认不做 |
| 11 | 启动时读场景状态 | `__init__` / `data_store` | 与现有一致（E 盘优先） |

### 5.1 第 4 条（AI 注入）的推荐写法

```python
# main.py:6897 附近 —— 在"载体状态"分支里插一条
try:
    if getattr(self, 'is_falling', False):
        parts.append("你正从高处往下掉")
    elif getattr(self, 'is_moving', False):
        parts.append("你正在走动")
    # ★ 新增：场景优先于"在窗口上/在桌面上"
    elif self.scene is not None and self.scene.current_scene() \
         and self.scene.current_scene()['id'] != 'desktop':
        parts.append(f"你在{self.scene.current_scene()['display_name']}")
    elif getattr(self, 'current_window', None) or getattr(self, 'window_level', 0):
        parts.append("你站在一个打开的窗口上")
    else:
        parts.append("你站在桌面上")
except Exception as e:
    _log.debug("AI 上下文：载体状态分片获取失败（已忽略）: %s", e)
```

> **注意**：`desktop` 场景走 `else` 分支（还是"你站在桌面上"）——
> **因为对 AI 来说"桌面场景"和"没有场景"应该是同一句话**，这样
> **不改变现有行为**，也保住了既有回归锁（`s7_event_speech` / `persona_chat`）。
> 这正是"桌面是普通场景"的好处：**默认态不产生行为差异**。

---

## 6. 切换管线（`switch_to` 的七步，抄原作 `obj_dw_transition`）

```
switch_to(scene_id)
 │
 ├─ 0. 幂等检查：同场景 → return False
 │
 ├─ 1. 退场（__exit__）
 │     · 停 BGM（抄 mus_volume(cur, 0, 30) 的淡出）
 │     · 隐藏旧物件
 │     · 记 previous_scene
 │
 ├─ 2. 写状态
 │     · state.current_scene = scene_id
 │     · state.spawn_anchor  = spawn
 │     · state.mark_visited(scene_id)
 │
 ├─ 3. 定位（★ 命名锚点，不写死坐标）
 │     · pos = resolve_anchor(scene, anchors, spawn, screen_rect)
 │     · 复用宿主 _clamp_pos_to_desktop() 夹紧
 │
 ├─ 4. 转场（transition）
 │     · 'none'  → 直接切
 │     · 'fade'  → 黑场淡入淡出（抄 merge_color(c_black,c_white,...)）
 │     · 'door'  → 剪影走门 + drone 音高 0.1→1.0（原作签名）
 │
 ├─ 5. 进场（__enter__）
 │     · 起 BGM（★ pick_variant + "在放就不换"守卫）
 │     · 显物件（visible_objects 按条件）
 │     · 报 note_event（"我来到了城堡镇"）→ 由 AI 决定说不说话
 │
 ├─ 6. 持久化（宿主 save → data_store）
 │
 └─ 7. return True
```

### 6.1 BGM 的"在放就不换"守卫（★ 抄原作的灵魂）

```python
def _ensure_bgm(self, scene):
    """抄 obj_musicer_town：条件树 + snd_is_playing 守卫。
    这是"穿场景时音乐不断"的核心机制。"""
    want = pick_variant(scene['bgm'].get('variants', []),
                        progress=..., flags=..., chapter=...)
    track = want['track'] if want else scene['bgm'].get('track')
    if not track:
        return
    if self._bgm_track == track and self._bgm_playing:
        return                          # ★ 在放同一首 → 什么都不做
    self._bgm_track = track
    self.p.sound_manager.play_bgm(track, loop=True,
                                  fade_in=want.get('fade_in', 0))
```

### 6.2 条件字段（`when`）支持集（抄 `global.plot` 的最小可用集）

| key | 语义 | 对应原作 |
|---|---|---|
| `progress_min` / `progress_max` | 剧情进度区间 | `global.plot >= 290` |
| `progress_eq` | 精确值 | `global.plot == 95` |
| `flag_is` / `flag_min` | 场景记忆 | `scr_flag_get(1454) >= 100` |
| `chapter_eq` | 章节 | `global.chapter == 4` |
| `visited` | 是否访问过 | （新加，用于"第一次来"的演出） |

---

## 7. ⚠️ 素材缺口（必须用户决策的前置）

**架构地图 §6 已确认：`deltarune_ralsei/` 1112 张 PNG 全是 Ralsei 角色精灵，
没有任何背景/房间/物件素材；`assets/sprites/` 和 `assets/faces/` 都是空目录。**

**三条出路（我推荐 A+B 组合，但需要用户点头）：**

| 方案 | 做法 | 优点 | 代价 |
|---|---|---|---|
| **A. 程序化生成（推荐起步）** | 用纯色/渐变/几何图块，按场景 `palette` 现场画背景 | **零素材依赖，立刻可跑**；且"抽象色块"反而贴桌面，不抢视线 | 不是原作美术 |
| **B. 用户提供 / 截图裁切** | 用户给原作截图，裁成背景条 | 最还原 | 版权风险 + 需手工裁 |
| **C. 重绘像素素材** | 照着原作风格画 | 最正统 | 工作量最大 |

> **本方案的设计对三者都兼容**：`ambience.background` 可以是
> `null`（透明，如桌面）/ `"procedural:purple_grass"`（程序化）/ `"file:xxx.png"`（图片）。
> **不预设，用户后期定。**

---

## 8. 施工分期（本轮只做 P0，其余留接口）

| 期 | 内容 | 改动文件 | 风险评估 |
|---|---|---|---|
| **P0（接口骨架）** | `scene_system.py`（纯数据+纯函数）+ `scenes/desktop.json` + `_index.json` + `_anchors.json` + 状态预声明 + `SceneController` 空壳 | 3 新增 + `main.py` 3 处 | **低**（不切场景就没有行为变化） |
| P1（切换链路） | `switch_to` / `back` / `__enter__` / `__exit__` / BGM 守卫 / 转场 | `scene_controller.py` | 中 |
| P2（物件交互） | 命中检测 + `on_object_interact` + `note_event` 上报 | `main.py:5068` 前 | 中（碰鼠标分发，需回归锁） |
| P3（渲染） | 背景窗口 / 程序化背景 | 新建 | 中高 |
| P4（可选） | 场景物件作为 floor（可站立） | `floor_manager.py:195` | 高（碰物理，本方案默认不做） |

### 8.1 P0 的验收标准（本方案的"可交付"定义）

1. **加一个新场景 = 加一个 JSON + 改一行 `_index.json`，零改 `main.py`。**
2. 桌面场景 `desktop.json` 存在且**行为与现状完全一致**（G2 1169 PASS 不许动）。
3. `resolve_anchor` / `pick_variant` / `depth_of` 三个纯函数有**正/负控制成对**的回归锁。
4. **`scene_system.py` 不 import Qt、不 import 项目内模块**（有断言锁）。

### 8.2 建议的回归锁（新套件 `scene_system`）

| 锁 | 内容 | 类型 |
|---|---|---|
| S1 | `load_index` 能读真实 `_index.json`，且 `desktop` 在其中 | 冒烟 |
| S2 | `resolve_anchor` 对已知锚点返回正确比例坐标 | 正控制 |
| S3 | `resolve_anchor` 对**不存在**的锚点回落到默认（不抛） | 负控制 |
| S4 | `pick_variant` 条件分支：`progress_min` 命中/不命中各一条 | 正+负 |
| S5 | `depth_of` 单调性：y 越大 depth 越小（越靠前） | 正控制 |
| S6 | `scene_system.py` 源码不含 `import PyQt5` / `from PyQt5` / 项目内模块名 | 结构锁 |
| S7 | `visible_objects` 的 `requires` 不满足 → 物件不出现 | 负控制 |
| S8 | `desktop.json` 的 `background` 为 `null`（桌面透明口径不被改坏） | 禁回归 |

> ⚠️ 按项目铁律：**别在锁的 detail 里写会随场景数增长的数字**（"共 N 个场景"必漂）。
> 断行为不断赋值；正/负控制成对。

---

## 9. 与现有系统的边界（绝不做的事）

| 系统 | 场景系统的边界 |
|---|---|
| **物理 / 建楼** | **不侵入**。地面继续走 `floor_manager.desktop_floor`。场景物件"可站立"是 P4 可选能力，默认关。 |
| **动画** | **不自己播特殊动画**。物件事件只走 `ai_driver.note_event()`，由模型决定。 |
| **AI 对话** | 只往「载体状态」分片加**一句**场景名（`main.py:6902`）。**不塞长文本**（保 K3 锁 + KV 缓存）。 |
| **persona** | **不改**。persona 的"我现在在哪"是静态单源，场景是动态态 —— 两者在 §5.1 的注入点合并，**不写回 persona 文件**。 |
| **关系 / 情绪 / 记忆** | 不改。场景可**读**它们做条件（如"去过的场景"），不写。 |
| **floor_manager** | 不改（P4 才考虑加注入点）。 |
| **状态归属** | 场景状态**必须预声明在宿主**，不能放 controller（§3.3 铁律）。 |

---

## 10. 遗留待用户裁定

1. **素材来源**（§7）—— A/B/C 三选一或组合。**这是开工前唯一阻塞项。**
2. **场景粒度**：一个场景 = 一整块屏？还是 = 一个"区域"（可跨多屏）？
   我倾向**一块屏一个场景**（对应"桌面也算一个场景"），但用户可能有别的想法。
3. **是否要 BGM**：现在的 `sound_manager` 只有 3 个音效，没有 BGM 能力。
   场景 BGM 要么扩展 `sound_manager`，要么先不做（`bgm.track = null`）。
4. **原作版权**：把原作美术搬上桌面的尺度 —— 这是用户的决定。

---

## 附：本方案的事实依据

- `场景系统架构地图_2026-09-22.md`（本项目现状，行号已逐条核验）
- Deltarune 原作机制调研（`code.deltarune.wiki` GML 代码实证）：
  `scr_roomname` / `scr_get_room_by_id` / `scr_musicer` / `scr_flag_get*` /
  `scr_depth` / `obj_dw_transition` / `obj_musicer_town` / `scr_become_light` /
  `scr_makecaterpillar` / Kristal 社区的 `World Objects` 抽象
- 项目既有铁律：`.workbuddy/memory/MEMORY.md` §0 / §4 / §6 / §7
