# 第51轮报告 — 移动系统真机体检 · 托盘常驻 · E 盘写入故障（2026-09-27）

> 本轮口径（用户原话，逐字）：
> 「他现在光有那个移动的动画，没有实际移动，这样吧，你自己到时候也整体查一下他的移动系统这类的，
> **用屏幕自动化来看，而不是后台日志**，对了，**测完了记得关掉**，还有，**以后预留一个后台运行的图标在托盘里，方便我关**」
> 以及前段两次纠偏：「我让你测试是指实测，实际上的操控 ralsei 去各个场景（**也就是我也能看到的那种**）而不是你在后台自己跑」。

---

## 一、结论先行

| # | 事项 | 结论 |
|---|---|---|
| 1 | 移动系统体检 | **做完了**，修复前实测：移动只占 21.2%、**单段位移中位数仅 49.5px（1.3 个身位）**、最长静息 33.5s、"原地播动画"仅 0.8% |
| 2 | "光有动画没移动"的真因 | **不是"动画在播但位移为 0"**，而是**单次移动太短 + 静止太长** ⇒ 肉眼分辨不出位置变化 |
| 3 | 已修 | 移动距离/频率/休息时长（15 项参数，含逐令牌复检） |
| 4 | 修复后实测 | 移动占比 **21.2% → 39.2%**、单段位移 **49.5 → 146.1px**、最长静息 **33.5 → 19.1s** |
| 5 | 托盘常驻图标 | **已实现**并取证（图标常驻 + 右键菜单「显示/隐藏/退出」） |
| 6 | 测试进程 | **已全部关闭**（每次采集脚本自动 kill，事后复核残留 = 0） |
| 7 | ★ 附带重大发现 | **`E:\RalseiMemory` 上的"既有文件"在文件系统层不可写** ⇒ 配置与记忆**从 2026-09-19 起全部存不下来** |

---

## 二、方法与纪律（回应"用屏幕自动化，不是后台日志"）

观测通道**全部落在"看得见"的层面**，不读产品的日志/内部状态：

| 手段 | 用途 |
|---|---|
| `EnumWindows` + `GetWindowRect` | 每 0.25s 取宠物窗口的**真实屏幕矩形** |
| `GetWindowTextW` 判标题 `'Ralsei Pet'` | 锁定唯一目标窗口（见 3.1 的教训） |
| `PIL.ImageGrab.grab(bbox)` + 像素差分 | 位置不变时判"画面是否在动" |
| `GetFileAttributesW` / `os.open` 矩阵 | 第五节的文件系统取证 |

**产生的可见证据**（`code-quality-audit/第51轮-真机巡行与交互/_evidence/`）：
`motion_audit51.png`（位移曲线 + 动画活跃度）、`motion_audit51_petwin_ready.png`（被抓窗口的原始截图，用于自证"这确实是 Ralsei"）、`motion_audit51.json`（原始 884 个采样）。

**两条硬纪律**：
* 起子进程必须 `env['PYTHONPATH'] = ''` —— 宿主注入的 shim 会劫持 `os.remove` 并同步阻塞 0.6~6.8s，**会伪造出"主线程卡顿"**（第51轮前段已实锤）。
* 采集脚本末尾必须 `kill_tree`，并在事后**独立复核**残留进程。

---

## 三、移动系统体检

### 3.1 判据自证：先证明"我抓的是谁"

第一版探针按"该 pid 下第一个 16~1200px 的可见窗口"取目标，结果抓到 **138×94** 的窗口，而 Ralsei 实际是 **38×80** —— 白/绿/粉像素计数全为 0，**根本不是 Ralsei**，那份 180s 数据全部作废。

修正后的判据：
1. 只认窗口标题 `'Ralsei Pet'`（实测特征：类 `Qt5152QWindowToolSaveBits`、exstyle 含 `LAYERED|TOOLWINDOW`）；
2. **必须连续 3 次**通过"白/绿/粉像素 ≥ 50"的回证才允许开始采集；
3. 采集开始**立刻把窗口原样存一张 PNG** 供肉眼核对。

> 教训（对应项目记忆 §4）：**探针不保真 = 报假问题**。窗口标题在启动 ~4.5s 时已是 `'Ralsei Pet'`，但那时表层还是全透明的（sprite 尚未绘制），据此采到的"位移/动画"毫无意义。

### 3.2 修复前（240s，就绪自证通过）

| 指标 | 值 | 读法 |
|---|---|---|
| `T_moving_pct` | **21.2%** | 五分之一的时长在移动 |
| `T_still_silent_s` | **167.8s（70%）** | 位置不动 **且画面一格未变** |
| `T_still_anim_s` | **1.8s（0.8%）** | ← 真正"原地播动画"只占 0.8% |
| `walk_px_median` | **49.5px** | Ralsei 自身 38px 宽 ⇒ **一次只挪 1.3 个身位** |
| `longest_rest_s` | **33.5s** | 连续两次长休息叠加 |
| `total_travel_px` | 6326px | 240s 累计 |

位移曲线（`motion_audit51.png` 上半）：t=0~20s 从右下角走到左上角（一大段），**之后 220 秒几乎平躺**；下半图的像素差分**几乎全程贴地**，只有 3 个尖峰。

### 3.3 根因

**不是"动画与位移不同步"**（那项只占 0.8%）。真因是三条叠加：

1. **单次目标距离过小**：`generate_new_move_target` 里 `move_distance`（sad/tired 档仅 `randint(30,100)`，shy 档 `randint(50,150)`）⇒ 一次只挪 1~4 个身位，**在 2560px 宽的屏上肉眼分辨不出**。
2. **休息过长**：`max_idle_duration` shy 档 `uniform(8,20)`、sad/tired 档 `uniform(10,25)`，且"不走"时会**再排一轮** ⇒ 实测最长静息 33.5s。
3. **静止期画面完全冻结**：`IDLE_LOOP_MIN_SECONDS = 180.0` 让 idle 的 5 帧循环**要静止满 3 分钟才播** ⇒ 那 33.5s 里连一帧都不动，"像卡住了"。

> ⚠️ 第 3 条**是用户明确要求**（代码注释原文：用户要求"待机动画要在原地不动3分钟以上才会播放哦，而不是停止就播"）⇒ **本轮未改动**，见第七节待裁定。

### 3.4 修复内容与验证

只改**第 1、2 条**对应的数值，保留各情绪的相对排序（兴奋最活跃 / 疲惫最安静）：

| 参数 | 修改前 | 修改后 |
|---|---|---|
| 移动距离 excited/energetic | 100~300 | 180~420 |
| 移动距离 shy/peaceful | 50~150 | **150~340** |
| 移动距离 curious | 80~250 | 160~380 |
| 移动距离 sad/tired | 30~100 | **120~280**（仍为最短档） |
| 移动距离 其他 | 80~250 | 150~360 |
| 移动概率 | 0.7 / 0.3 / 0.6 / 0.2 / 0.4 | 0.85 / **0.5** / 0.7 / **0.35** / 0.55 |
| 休息时长 | 3~8 / 8~20 / 5~12 / 10~25 / 6~15 | 2~5 / **4~10** / 3~7 / **5~12** / 4~9 |
| 首次移动的起始概率 | 0.6 / 0.2 / 0.3 | 0.75 / 0.45 / 0.5 |

**修复后同口径实测（240s）**：

| 指标 | 修复前 | 修复后 | 变化 |
|---|---|---|---|
| `T_moving_pct` | 21.2% | **39.2%** | ↑85% |
| `walk_px_median` | 49.5px | **146.1px** | **↑195%（≈3.8 个身位）** |
| `longest_rest_s` | 33.5s | **19.1s** | ↓43% |
| `total_travel_px` | 6326px | **10823px** | ↑71% |
| `T_still_silent_s` | 167.8s | 130.2s | ↓22% |

**逐项复检**：`_tools/recheck51.py` → **PASS=15 / FAIL=0**，含一条**回退守卫**：`IDLE_LOOP_MIN_SECONDS = 180.0` 必须保持原样（已核）。

> 复检过程中判据自身出过一次误报：`⑤b 旧值已消失` 用全文 `in` 判定，把 `generate_new_move_target` 里合法的"60% 横向移动"(`< 0.6`) 与别处的 `< 0.3` 一并当成"残留"。已把判定范围**收窄到被改的两个函数体内**。⇒ 项目教训：判据过宽与过窄**都要防**。

---

## 四、托盘常驻图标（用户要求 ③）

`_setup_tray` 的第51轮修复：**图标原来建好却从没 `show()`**（`tray.show()` 只在 `_hide_ralsei()` 里调用）⇒ 没隐藏过的用户整个会话都看不到托盘图标。现在：

* 图标**常驻显示**，`setToolTip("Ralsei 桌宠（右键可退出）")`；
* 右键菜单：**显示 Ralsei / 隐藏 Ralsei / —— / 退出 Ralsei**；
* 「退出」走 `cleanup_on_exit() → tray.hide() → QApplication.quit()`（原来直接 `quit`，跳过清理会留下单实例互斥残留，导致"明明关了却起不来"）；
* 「显示 Ralsei」**不再顺手 `tray.hide()`**（否则把唯一的退出入口一起抹掉）；
* 首次启动弹一次气泡提示（落 `ui.tray_hint_shown`）。

**取证**：`_evidence/motion_audit51_petwin_ready.png` 意外抓到了气泡文字「图标可以退」——正是这句提示，说明**气泡确实会弹**。

> ⚠️ 但**"只弹一次"没生效** —— 根因见第五节（写盘失败，标记落不了盘）。

---

## 五、★ 重大附带发现：E 盘上的既有文件在文件系统层不可写

### 5.1 现象

真机日志（产品自己的 ERROR，非我构造）：

```
[ERROR] config_manager — 保存配置文件失败: [WinError 5] 拒绝访问。:
        'E:\RalseiMemory\config.json.tmp' -> 'E:\RalseiMemory\config.json'
[ERROR] config_manager — set() 写盘失败，已回滚内存配置: ui.tray_hint_shown
[WARNING] memory_system — 保存记忆失败: [WinError 5] ... memory.json.tmp -> memory.json   ×3
[日志] 文件日志初始化失败，仅使用控制台: [Errno 13] Permission denied: 'E:\RalseiMemory\logs\ralsei_pet.log'
```

`E:\RalseiMemory\config.json` 的 mtime 停在 **2026-09-19 10:32** —— 也就是说**这一周多以来所有配置写入全部失败**（用户改的任何设置、Ralsei 的记忆都存不下来）。

### 5.2 取证矩阵（正/负对照齐备）

| 操作 | 结果 |
|---|---|
| 在 `E:\RalseiMemory\` **新建**文件 / 子目录 | **OK** |
| 在新文件上写 / 删 | **OK** |
| `open(config.json,'r')` 读 | **OK** |
| `open(config.json,'r+')` / `'a'` / `'w'` / `'rb+'` | **PermissionError** |
| `os.rename(老文件, …)` | **WinError 5** |
| `os.remove(老文件)` | **WinError 5** |
| `os.replace(tmp, 老文件)` | **WinError 5**（与产品错误一字不差） |
| `GetFileAttributesW` → READONLY 位 | **0（不是只读属性）** |
| `os.access(..., os.W_OK)` | **True** ← **假判据**（Windows 上不做真实检查） |
| 同卷 `E:\Download\_tmp` 的老文件 | **同样失败**（310/324 不可写） |

**判读**：这不是权限、不是属性、不是进程占用，而是**该 exFAT 卷上的"既有文件"整体不可写/不可改名/不可删**，**新建文件完全正常**。同卷另一目录同样如此 ⇒ 与 vault 无关，是**卷级问题**。

> 这也一次性解释了用户级记忆里那条悬案：`E:\RalseiMemory\memory.json.tmp → memory.json` 的 WinError 5 —— **同一个病灶**。

### 5.3 影响面（重要）

* 用户改的**任何设置重启后丢失**（`config.json` 写不进去）；
* **Ralsei 的记忆写不进去** ⇒ 他不可能"记得"新东西；
* 运行时配置里 `api.model` 是 **`ralsei:v3`**，而仓库模板是 `ralsei:v4` —— **因为写盘失败，用户改不回 v4**；
* 日志文件初始化失败 ⇒ 只能看控制台。

### 5.4 建议（需用户执行，我不会擅自动盘）

1. **先试 `chkdsk E: /f`**（管理员命令行；修复 exFAT 卷元数据，低风险、可逆，但可能耗时）；
2. 若无效：**把 `E:\RalseiMemory` 整个复制到 C 盘 → 验证可写 → 再考虑格式化 E 盘**；
3. 或**把 vault 迁到 C 盘**（需改 `_resolve_config_path`，属契约变更，等用户拍板）。

⚠️ 在修好之前，**产品的自动保存都不可靠**，这不是代码能绕过去的。

---

## 六、回归与提交

* **全量回归**：`code-quality-audit/regress/run_all.py` → **`PASS=2376 / FAIL=0`，47 套件全部 `IDENTICAL`**。
  * 与移动直接相关的套件均绿：`walk_round47`（PASS=37 / FAIL=0）、`round34_movement`（PASS=36 / FAIL=0）。
  * 说明：本轮的移动参数上调**没有**破坏既有行为锁；`scene_walk.py` 的穿模/尖刺修复仍在 `walk_round47` 覆盖内。
* 复检：`_tools/recheck51.py` → **PASS=15 / FAIL=0**（含回退守卫 `IDLE_LOOP_MIN_SECONDS = 180.0`）。
* 工作区：已还原被测试误改的 `ralsei_pet/config.json`（纯缩进格式变化，已备份到 `E:\Download\_tmp\config_json_repo_backup51.json`）。
* 本轮改动清单：`ralsei_pet/src/main.py`（托盘常驻 + `timer_dt` 计时口径 + 移动参数 3 处）、`ralsei_pet/modules/scene_walk.py`（穿模/尖刺）、`code-quality-audit/regress/verify_walk47.py`、`run_all.py`（desc）、`baseline.json`、第51轮审计目录与报告。
* **测试进程**：每轮采集脚本末尾自动 `kill_tree`，事后独立复核 **残留 = 0**；E 盘测试文件亦已清理。

---

## 七、待用户裁定

1. **`IDLE_LOOP_MIN_SECONDS = 180.0`**（静止 3 分钟才播待机动画）——这是您明确要求的，本轮**没动**。但实测修复后仍有 **54% 的时长画面完全冻结**（`T_still_silent_s = 130.2s`）。**是否放宽到 20~30 秒？**（不改的话，"静止时像卡住"的观感会一直在。）
2. **E 盘**：是否按 5.4 处理？先跑 `chkdsk`，还是直接迁 vault 到 C 盘？
3. 是否要我把"配置/记忆写盘失败"做成**可见提示**（现在只写日志，屏幕上完全看不出来）？
4. 前段主任务仍在队列中：**用户可见的"桌面 → 各 room"行走入口**（`plan_walk`/`follow_route` 目前在 `main.py` 里零调用，所以场景巡行只能"后台跑"，这正是您两次纠偏指出的问题）。
