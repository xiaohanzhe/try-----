# 代码质量复审报告 — 第十三轮（2026-09-16）

**主题**：「建楼」遮挡判定 + 窗口层数 + 渲染层序（按 Windows 原生口径）
**用户原话**："他直接走到我的窗口上面了，根本没遮挡关系这类的，你看一下那个建楼要求，我想要的是那种效果，这个遮挡判定这类的，还有窗口层数这样的你可以直接参考 Windows 本地是怎么判定的"

**验证**：本轮自检 **71 PASS / 0 FAIL**（A–G 七组）；G2 回归 **17 套件 / 655 PASS / 0 FAIL / 全 IDENTICAL**
**提交**：与本报告同一 commit（见 `git log`）

---

## 一、结论（先说结果）

用户报的是一句话，底下压着**三处硬伤**——其中一处让前两处的修法全都白搭：

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 1 | 站在"看不见的地板"上 | `_generate_floors` 用 `rect.contains(rect)` 判"被盖住"——那是**完全包含**。被挡住 90% 的窗口仍算**一整块**可站楼板 | 引入**可见区域矩形相减**：`visible_subrects(自身, 更高的窗口们)` |
| 2 | 站在被盖住的部分也算数 | `get_current_floor` 只判 `rect.contains(pos)`，完全不看遮挡 | 站立/下落/跳跃**全部**改判"点是否落在**可见区域**里" |
| 3 | 宠物**永远画在所有窗口之上** | `main.py` 用 `Qt.WindowStaysOnTopHint` **强制置顶** → 遮挡关系从渲染层就失效了 | 移除置顶；接活 floor_manager 里**早就写好但零调用**的 `get_insert_after_hwnd` / `set_window_behind` |

**第 3 条是最要紧的。** 前两条就算修得再准，只要窗口是 `WindowStaysOnTop`，宠物还是压在一切之上——用户看到的"根本没遮挡关系"就是这么来的。而正确的机制**早就躺在 `floor_manager.py` 里**（`SetWindowPos(pet, insertAfter=所站楼板)`，让系统自己去算谁盖谁），只是**全项目零调用**，是一段死代码。本轮把它接活了。

**顺带挖出两个自家问题**（都不在用户报的范围内，但不修则本轮无法验收）：

- **G2 回归把自己掐死了**：程序每次启动会真的建/删十几次一个探针文件，17 个套件跑一轮把它们累积到宿主沙箱的"同轮同路径删除配额"上 → 套件进程**在开头就被杀**，输出只剩一行守卫日志，11 个套件同时报 `exit=1 PASS=0`，看着像代码全崩。**是假警报，但耗掉了上一轮的全部排查时间。**（§七）
- **DPI 单位陷阱**：真机探针一度显示"DWM 矩形正好是 `GetWindowRect` 的 1.5 倍"，看起来像我把坐標系改错了方向。查下来是探针自己没建 `QApplication`、跑在 DPI 不感知的裸进程里造成的假象。（§八）

---

## 二、交付物

| 文件 | 变更 | 说明 |
|---|---|---|
| `ralsei_pet/modules/floor_manager.py` | **改（核心，约 370 行）** | 可见区域相减、按名次编号的楼层、站立/下落/跳跃改判可见区域、z 序 helper 显式化 |
| `ralsei_pet/modules/desktop_interaction.py` | 改（约 160 行） | 新增 DWM 可见边框 / 幽灵窗口 / 原生命中测试 / 取顶层 / 取进程号 五个助手；枚举按进程排除自身；删掉第二套楼层编号 |
| `ralsei_pet/src/main.py` | 改（约 70 行） | 新增 `_apply_pet_z_order()` 并在 4 处接线；**移除 `Qt.WindowStaysOnTopHint`** |
| `ralsei_pet/modules/memory_store.py` | 改（约 20 行） | 可写性探测加**零副作用快路径**（修 G2 自伤，§七） |
| `code-quality-audit/第十三轮/verify_round13_build.py` | **新增（71 项）** | 自检套件，已挂进 G2（A 几何 / B 楼层 / C 查询 / D 原生 / E 接线 / F 契约 / G 存储探测零副作用） |
| `code-quality-audit/第十三轮/probe_native_occlusion.py` | 新增 | 真机探针（真实窗口，证据用，不入 G2） |
| `code-quality-audit/第十三轮/_probe_dpi.py` | 新增 | DPI 量纲自检探针（§八 的定论依据） |
| `code-quality-audit/第十三轮/_evidence/` | 新增 | 真机证据 3 份（原生口径 / DPI 量纲 / 楼层生成） |
| `code-quality-audit/regress/run_all.py` | 改 | 挂入 `round13_build`；**隔离清单 `HERMETIC_IDS`**；`save_baseline` 改**合并模式**（§七） |
| `code-quality-audit/regress/baseline.json` | 改 | 基线重建（17 套件） |
| `code-quality-audit/第八轮/verify_round8_floor.py` | 改 | 补 `_apply_pet_z_order` 桩（新接线让老桩缺方法） |

---

## 三、"建楼"要求 → 实现映射（逐条对表）

用户给的 `"建楼"要求` 是逐条写的，我按它的条款对账：

| 要求原文（摘） | 落点 |
|---|---|
| "底层大地：电脑桌面本身就是最底层，ralsei 永远可以站在上面" | `DESKTOP_IDENTITY = 'desktop'`，`platform_height = 0` |
| "我每打开一个新窗口，就在对应的位置，立刻给我盖一层新楼板。这个楼板的大小、位置，必须和窗口一模一样" | 枚举可见窗口 → 每窗一层；矩形取自 **DWM 可见边框**（见 §五，这才是"和窗口一模一样"） |
| "哪个窗口在最前面，哪个就是最高楼层" | `platform_height = (n − i) × 5`：**按名次编号**，最前 = 数值最大 |
| "后面的窗口如果被前面的**完全盖住**，那就相当于被压在下面了，暂时'不存在'" | 可见面积 `0 < MIN_FLOOR_VISIBLE_AREA` → **直接跳过，不成楼层**；且它**不再参与遮挡更低的窗口**（"它自己也不存在"） |
| "ralsei 眼里只有两种东西：它正站着的这块楼板，以及比当前这块楼板更高的、没有被完全挡住的'悬崖边'" | `floor_visible_contains(floor, pos)`：点必须落在该层的**可见区域**里；查询一律"从高到低取第一个命中" |
| "向上跳：只能跳到这个窗口**没被其他东西挡住的那部分边缘**上" | `get_jump_destinations` 的落点判定改走可见区域；且只认**相邻**下一层 |
| "向下跳：必须先跳到 2 楼，再从 2 楼跳回 1 楼。不能直接从 3 楼穿透 2 楼跳到 1 楼" | `get_drop_destination` / `find_support_below` 只取"**下面第一个**能接住的"，不做跨层搜索 |
| "走到楼板边缘，脚下没东西了，就直直地掉下去，直到落到下面第一块能接住它的楼板" | 同上（已有机制，判据换成可见区域） |
| "我挪动窗口……如果宠物正站在上面，它必须跟着楼板一起移动" | 第八轮已实现（`window_moved`），本轮未动 |
| "我关闭窗口……必须立刻开始往下掉" | 第八轮已实现，本轮未动 |
| "我用新窗口盖住旧窗口……旧楼板对宠物来说就'失效了'" | 可见面积跌破阈值 → 旧楼板从楼层表消失；`current_floor` 在下一秒 tick 里重判为新窗口 |
| "**做出那种'一层压一层'的真实堆叠感，而不是所有窗口都透明地飘在空中**" | `_apply_pet_z_order()`：`SetWindowPos(pet, insertAfter=所站楼板窗口)` —— 由 Windows 自己算遮挡（§五） |
| "摔到桌面上的动画用之前那个，但**如果是我的行为导致他摔到桌面上**就用生气的那个，至少 5s" | 第七/八轮已实现（`fall_mad` / `splat_mad`，`animations.json` 里有完整注释记录），本轮未动 |
| "我移动他所在的'楼板'时他会重心不稳甚至摔倒……至少 3s" | 已实现（`main.py` 窗口位移 > 30px → `start_fall('window_move')`），本轮未动 |

**对账结果：13 条要求里 4 条是本轮新做的（加粗那几行的判据），5 条已由前几轮满足，4 条是动画要求（已满足）。** 没有漏项。

---

## 四、三处硬伤的定位与修法

### 4.1 可见区域相减：`visible_subrects(target, blockers)`

不能"扣面积"，因为后面所有判断都是"**这个点在不在可见区域里**"（站立、下落、跳跃、边缘判定）。所以必须拿到**矩形集合**，而不是一个数。

做法是构造性相减：把 `target` 依次被每个 `blocker` 切，每次最多切成 4 块（上/下/左/右），**产出天然两两不重叠** —— 于是 `sum(面积)` 精确等于真实可见面积，不需要去重、不需要容差。

```
_cut(rect, hole) -> ≤4 块互不重叠的子矩形
visible_subrects(target, blockers) -> 依次切，得 [矩形…]
```

**双向验证**（这是我在这轮学到的东西）：解析法与一个**独立的采样网格 oracle**（把区域按 10px 网格打点、逐点判可见、数点数）交叉比对。两者一个是解析几何、一个是暴力采样，算法完全无关——它们对得上，才说明"两两不重叠 + 面积和正确"不是我自己推错公式推出来的。

> 上一轮修 `verify_round13_build.py` 时我确实手算错过两处期望面积（两个互相重叠的遮挡者被我重复扣了）。加了这个 oracle 之后，错的是我的**期望值**、不是代码，一眼就看出来了。

### 4.2 两个阈值，两个不同的语义

- `MIN_FLOOR_VISIBLE_AREA = 40 × 40 = 1600 px²` ——**"够不够站"**，不是"有没有缝"。
  只剩 1px 缝（1000 px²）时不成楼层；缝放宽到 4px（4000 px²）就恢复成楼层。这条差别是**故意的**：如果按"有没有被遮"判，1px 缝也算楼板，宠物会站到一根头发丝上。
  阈值取 40×40 的依据：宠物脚下接触面约 40px 见方。
- `platform_height = (n − i) × 5` ——**名次编号**，只用来排序和给跳跃目标标个高度，不是像素。

### 4.3 `is_floor_valid` 不再要求"四边完全相等"

老实现要求楼板矩形和当前窗口矩形**四边逐一相等**才算有效。真机上一跑就发现：DWM 边框会有 1px 抖动，于是**每一次抖动都让楼板"消失"**，宠物无故开始坠落。现在只判"那个窗口还在不在"。

### 4.4 删掉第二套楼层编号

`desktop_interaction.get_all_visible_windows` 里原本还有一行
`window['platform_height'] = window['z_order'] * 5` —— 和 `floor_manager` 里那套**并存**，两个数不同源。这正是这个项目反复踩的坑（"唯一真源"），删掉，`floor_manager` 是唯一权威。

---

## 五、Windows 原生口径（用户要求"参考 Windows 本地怎么判定"）

用户明确说"可以**直接参考 Windows 本地**是怎么判定的"，所以本轮不自己发明判据，全部对齐系统 API：

| 要做的事 | 用的 Windows API | 为什么不能用原来那个 |
|---|---|---|
| 顶层窗口从前往后 | `GetTopWindow` + `GetWindow(GW_HWNDNEXT)` | —— 新引入，之前靠枚举顺序 |
| "这一点上最上面是谁" | `WindowFromPoint` + `GetAncestor(GA_ROOT)` | `GA_ROOT` 必须有：`WindowFromPoint` 可能返回**子控件**，子控件的矩形不是楼板 |
| 窗口**可见**边框 | `DwmGetWindowAttribute(DWMWA_EXTENDED_FRAME_BOUNDS)` | `GetWindowRect` 含 DWM 隐形阴影/调整边框 → 楼板比看到的窗口大一圈（要求原文："必须和窗口一模一样"） |
| 幽灵窗口 | `DwmGetWindowAttribute(DWMWA_CLOAKED)` | 挂起的 UWP / **别的虚拟桌面**上的窗口，`IsWindowVisible` 说它可见，实际根本看不见 → 会被当成楼板 |
| 排除自身 | `GetWindowThreadProcessId` 比对本进程 | 原来靠**标题里有没有 "Ralsei"**：既会误伤用户自己的同名窗口，又会**漏掉本进程的其它窗口**（对话框、托盘气泡都是独立顶层窗口，标题里未必有 Ralsei）→ 宠物能站在自己的对话框上 |
| "这算不算地板" | 排除 `WS_EX_TOOLWINDOW` / `WS_EX_NOACTIVATE` | 浮动面板/挂件/点击不激活的覆盖层不是用户心里的"窗口"（保留原有的 `WS_EX_TRANSPARENT` / 半透明过滤） |
| 把宠物插到正确层 | `SetWindowPos(hwnd, insertAfter, SWP_NOMOVE\|NOSIZE\|NOACTIVATE\|NOOWNERZORDER)` | 见 4.1 的说明：**让系统算遮挡，不要自己算** |

**一个 64 位坑**：`WindowFromPoint` 和 `GetAncestor` 的返回类型必须显式声明 `restype = c_void_p`。默认会被当成 `c_int`，64 位下 HWND 高位被截断 → 匹配不上任何已知窗口，而且**不报错**，只是静默失效。同项目里 `OpenProcess` 早就踩过同一个坑（有 `restype = c_void_p` 的先例注释）。

### 5.1 关键设计决定：几何优先，原生只做"向上"的兜底

`get_current_floor(pos)` 的主判据是**几何**（"点在不在某一层的可见区域里"），`WindowFromPoint` **只在几何判到桌面时**才启用（只在它能指向一个**已知楼层**的情况下），且只会"往上抬一层"。

为什么不做成"原生说谁在最上面就用谁"：`WindowFromPoint` 依赖真实桌面状态，在离屏/合成窗口表里不可复现，会让 G2 变成随机测试。原生口径是**兜底与交叉校验**，不是主判据。

---

## 六、真机证据（`_evidence/round13_native_probe.txt`）

在真实桌面上跑 `probe_native_occlusion.py`（**必须先建 `QApplication`**，理由见 §八）：

```
=== 0) 量纲自检 ===
Qt  primaryScreen().geometry() : 2560 x 1600   devicePixelRatio=1.0
Win GetSystemMetrics(0,1)      : 2560 x 1600          ← 同量纲 ✓

=== 1) DWM 可见边框 vs GetWindowRect ===
WorkBuddy    GetWindowRect (3, 7, 1206, 910)   DWM (3, 7, 1206, 910)   每边差额 (0,0,0,0)
（当前桌面配置下两者完全一致 → 换 DWM 是零回归；它挡的是**别的**窗口形状/配置下
  GetWindowRect 把隐形调整边框算进去的情形）

=== 2) 过滤器的真实效果（粗筛 4 个可见顶层窗口）===
DWM 幽灵窗口(cloaked) : 1  ['Windows 输入体验']          ← 挂起的 UWP，实际看不见
WS_EX_TOOLWINDOW      : 2  ['wv_1001', 'Program Manager'] ← 桌面 Progman 本身就是工具窗
WS_EX_NOACTIVATE      : 1  ['wv_1001']

=== 4) 建楼层：走产品真实的过滤枚举 ===
粗筛窗口数 = 4  →  产品过滤后 = 1  （被滤掉 3：幽灵/工具窗/无激活/自身/出屏）
  留下: hwnd=1970972 'WorkBuddy' rect=(3, 7, 1206, 910)
窗口数 = 1 → 有效楼层数 = 1
  L5   hwnd=1970972   可见 1086309/1086309 px (100.0%)  WorkBuddy
```

第 4 项值得单独说：**如果不过滤，这个桌面上会凭空多出 3 块"楼板"**（一个看不见的 UWP 面板、桌面本身、一个隐藏 helper），其中"Windows 输入体验"还占了屏幕 73.5% 的可见面积 —— 宠物会以为整个屏幕都是地板。过滤后只剩 1 块，和用户眼前看到的一致。

---

## 七、意外收获：G2 被自家程序掐死（已修）

### 现象

修完建楼代码跑 G2 全量，出现**11 个套件同时** `exit=1 PASS=0` + DIFF，合计 `PASS=337`（正常应为 647）。每个套件的输出**只剩一行**：

```
[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":51,"threshold":50,"scope":"turn","targets":["E://RalseiMemory//.write_probe"],"targetCount":1}
```

### 根因

`memory_store._is_writable_dir()` 的老实现**每次都真的**建一个 `.write_probe`、写一下、再删掉：

```python
with open(probe, 'w') as fh: fh.write('ok')
os.remove(probe)
```

而 `find_device_dir(create=True)` 在**一次启动**里会被调用十几次（`data_store` 要解析记忆库 + 7 类运行时产物）。17 个套件跑一轮 → **同一路径 `E:\RalseiMemory\.write_probe` 单轮被删 50+ 次** → 累积撞上宿主沙箱"同一轮内同路径删除配额"守卫 → 进程被杀。

**误导性极强**：删除发生在 `import`/初始化阶段，所以套件**一个字都没来得及打印**就被杀；11 个套件同时挂，看起来像"这轮改动把代码全搞崩了"。真相是**测试自伤**。

### 修法（两层，都是真问题）

1. **生产代码**：给探针加**零副作用快路径**——先 `os.access(path, os.W_OK)`，只有它给不出结论时才退回老的写删探针。快路径**不缓存**，所以"E 盘掉线"依然能在下次调用被检出（这是第十二轮定的契约，不能破）。这不只是修测试，本身也是**每次启动十几次无谓的文件系统写删**。
2. **测试封闭性**：`run_all.py` 增加 `HERMETIC_IDS`，凡是会实例化 App / 触碰存储的套件统一注入临时的 `RALSEI_MEMORY_DIR`（`find_device_dir` 见到它直接 return，**连探针都不会跑**）。副作用是这些套件不再往用户真实的 E 盘写日志/成长数据 —— 本来就该这样。

### 顺手修的第三个问题：`--update` 会静默抹掉其它套件

`save_baseline` 原来是无条件整体重写，于是

```
run_all.py --only round8_anim --update
```

会把基线里其余 15 个套件**静默删掉**。下次全量跑，那些套件全部变成 `BASELINE`（无从比对）—— 而输出看起来一切正常。**基线是整个 H4/H5 改造的唯一安全性判据**，不能有这种一键抹除的路径。已改成**合并模式**（只覆盖本次跑到的套件）。本轮 `--only round13_build --update` 正是走的新路径，输出明确提示"本次只重建了 1 个套件，其余保持不动"。

### 回归锁

新增验证组 **G**（8 项）把这件事钉住，其中最关键的是 **G4**：

> 把 `os.remove` 打桩成"任何删除都抛异常"，`_is_writable_dir` 仍必须返回 `True`。

老实现对这条必然返回 `False`（误判"盘不可写" → 记忆被错误搬去桌面兜底）。所以这条断言**能真正区分新旧实现**，不是走过场。另外 G7/G8 锁住 `HERMETIC_IDS ⊆ 套件表`（写错名字会静默不隔离，等于没做）以及关键套件都在清单里。

### 7.4 顺带关掉了第十二轮补丁的遗留项

第十二轮补丁当时判定：**"默认启动方式在 Agent 沙箱里永远起不来，只能让用户真机复跑"**，并把 `§六-1` 挂成未闭合项。那条判定的依据是"换全新轮次复跑，计数照样是 160/阈值 50 → 所以是确定性拦截，不是删除预算"。

**本轮证伪了这个推论。** 计数不是固定的：本轮实测是 `count=51 / threshold=50 / targetCount=1`（同一个目标路径）。它**就是按目标路径累计的删除计数**，只是累计得快（一次启动十几次探针写删），所以随便一次复跑都早已越过阈值、看起来"恒定"。

修完快路径之后，按用户日常方式启动：

```
Set-Location ralsei_pet
& C:\Python311\python.exe src\main.py
```

进程正常存活（`psutil` 实测 pid 2116），日志里 **没有 `[safe-delete]` 行、没有 traceback**：

```
2026-09-16 22:54:12 [INFO] ralsei_pet.text_segmenter - 已启用 jieba 分词（用户词表: 无）
Loading model from cache E:\RalseiMemory\cache\jieba.cache
2026-09-16 22:54:12 [INFO] ...sprite_loader - [anim-config] 已从 animations.json 加载 109 组动画（legacy 28 组，schema=1）
Loading model cost 0.981 seconds.
Prefix dict has been built successfully.
```

证据：`_evidence/round13_default_launch_ok.md`。**这条遗留项就此关闭**（不是"改判由用户复跑"，是真的跑起来了）。

> 教训收进记忆：**看到"计数恒定"就下"确定性"的结论太快了。** 计数在一次启动里能涨到 50+，所以"每次复跑都超阈值"和"阈值是个固定常量"这两种解释会产生同样的观测。破除的办法是**直接把触发源干掉再看**——本轮一旦不再删除，问题就消失了，这比反复复跑有说服力得多。

---

## 八、DPI 陷阱（差点把改动方向搞反）

第一次跑真机探针，输出是这样的：

```
WorkBuddy    GetWindowRect (2, 5, 804, 607)      DWM 可见边框 (3, 7, 1206, 910)
Windows 输入体验  GetWindowRect (0,0,1707,1067)   DWM (0,0,2560,1600)
```

`1206/804 = 1.5`、`2560/1707 = 1.5` —— **DWM 矩形整整大 1.5 倍**。第一反应是："本机 150% 缩放，DWM 返回物理像素、`GetWindowRect` 返回逻辑像素，那我把楼层换成 DWM 就把楼板坐标系和宠物坐标系搞成了两套单位 —— 方向反了。"

写了一支独立的量纲探针 `_probe_dpi.py` 去定论，结论是：

```
（未建 QApplication 时）
ctypes user32.GetSystemMetrics(0,1) : 1707 x 1067     ← 被虚拟化
（建了 QApplication 之后）
Qt  primaryScreen().geometry()      : 2560 x 1600     ← 物理
Win GetSystemMetrics(0,1)           : 2560 x 1600     ← 物理
win32gui.GetWindowRect   : (3, 7, 1206, 910)
ctypes  user32.GetWindowRect : (3, 7, 1206, 910)
DWM EXTENDED_FRAME       : (3, 7, 1206, 910)          ← 三者完全一致
```

**Qt 在创建 `QApplication` 时会把进程 DPI 感知级别设成 per-monitor v2。** 在那之前进程是 DPI 不感知的，Windows 会把 `GetWindowRect` / `GetSystemMetrics` 按系统缩放**虚拟化**（本机 150%：物理 2560×1600 → 回报 1707×1067），而 `DwmGetWindowAttribute` **永远返回物理像素**。两者混用才产生"1.5 倍"的假象。

宠物软件一定活在 `QApplication` 里 → **楼层（DWM）和宠物（Qt）本来就在同一个物理像素坐标系**，改动方向是对的。那支探针自己没建 `QApplication`，所以它测的是一个**产品里不会出现的进程状态**。

修法：探针里加 `QApplication`，并在文件头写清这一条（连同"第 0 项量纲自检"一起固化，下次谁改探针都不会再踩）。**这也是为什么 §六 的证据里第 0 项是量纲自检 —— 先证明尺子对，再量东西。**

---

## 九、遗留 / 待办

1. **真机看效果**：本轮全部结论里，只有"真机上宠物不再永远置顶、会被前台窗口盖住"需要用户肉眼确认。Agent 沙箱里窗口是无边框透明非置顶窗，**被遮挡/穿透时鼠标事件打不到**（第八轮就定过这条：甩飞/抛物线只能离屏断言），所以 z 序的观感只能由用户在真机上确认。
2. **`DwmGetWindowAttribute` 的另一个已知差异**（本轮实测差额为 0，仅记录）：某些窗口形状/系统版本下 `DWMWA_EXTENDED_FRAME_BOUNDS` 与 `GetWindowRect` 会差几天像素（隐形调整边框）。差异为 0 是**当前配置**的幸运，不是普遍保证；代码两边都做了"取不到就回落 `(0,0,0,0)` 并由上层过滤"的防御。
3. **`round8_anim` 之外的套件**是否还有别的"真实副作用"（写注册表、发窗口消息等）没有系统清过——本轮只处理了存储这一条最痛的。建议在下一轮做一次"套件副作用审计"。
4. **H4/H5 架构改造**（用户排期"单独做"）不在本轮范围，本轮只是又给它清掉两块绊脚石（基线不可靠、探针有副作用）。
