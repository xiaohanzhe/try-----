第 37 轮 收尾核验证据
生成时间：2026-09-23（第 37 轮收尾）
生成方式：全部由 Python 直接跑出，逐条可复现。原始输出未经手工修饰。

================================================================================
一、E3 判据升级：恒真判据 → 真判据（+ 两个负控制）
================================================================================
背景：第 36 轮套件 verify_scene_route_original.py 的 E3 是一条
      `check('E3 bg 占位文件当前尚不存在（等待用户导出，属预期）', True, ...)`
      —— **恒真占位**。恒真判据比不写还危险：它看着像在守，其实什么都没守。
      当时素材确实还没导出，所以只能这么写；第 37 轮素材已反编译就位，必须升级。

升级方式：code-quality-audit/第37轮-原作素材反编译/patch_e3.py
运行结果：
    EOL = LF
    patched: #  E. 素材占位口径（用户选 B：自导出）不许伪装成"没素材"
    patched: # bg 文件确实还不存在（用户尚未导出）—— 这是**预期状态**，不是缺陷
    patched: check('E1 每个作品内场景都声明了 bg 占位路径',
    patched: print('=== E. 背景素材占位口径 ===')
    patches applied = 4
    自检：contains E3b/E3c/_bg_health_pairs/素材已反编译就位 = True
          no longer has 尚不存在 = True

升背后的 E 段实跑输出（单套件）：
    === E. 背景素材口径 ===
    [ OK ] E1 每个作品内场景都声明了 bg 路径
    [ OK ] E2 bg 路径统一走 bg/ 子目录（约定文件名）
    [ OK ] E3 每个场景声明的 bg 都指向真实存在的 PNG（第37轮素材已反编译就位）
    [ OK ] E3b 负控制：判据抓得住"bg 文件不存在"（有鉴别力）
    [ OK ] E3c 负控制：判据抓得住"文件存在但不是 PNG"
    [ OK ] E4 bg 走 scene_dir 路径口径（不是仓库根）
    第三十六轮场景路线回归锁：PASS=52 FAIL=0
（升级前 PASS=50；+2 来自 E3b/E3c 两个负控制。E3 本身由恒真变为真判据。）

判据内核（正负控制共用同一个函数，保证"抓得住的"和"守着的"是同一条判据）：
    def _bg_health_pairs(pairs):  # -> (missing, not_png)
        for sid, fp in pairs:
            if not isfile(fp): missing.append(sid); continue
            if open(fp,'rb').read(8) != b'\x89PNG\r\n\x1a\n': bad.append(sid)
    E3  = 真实扫描 87 个场景 → 断言 missing 与 not_png 都为空，且 len(scan)>0
    E3b = 喂一个不存在的路径 → 必须抓到（负控制）
    E3c = 喂 _index.json（存在但非 PNG）→ 必须抓到（负控制）

配套改动：
  · run_all.py 该套件的注释块与 desc 文案同步更新（原写"占位口径/文件暂缺属预期"）。
  · 基线用**合并模式**重建：`run_all.py --only scene_route_original --update`
    ⇒ baseline.json 里该套件 pass: 50 → 52、sha256 同步（git diff 仅 2 增 2 删）。

================================================================================
二、G2 全量回归：结论与两条 DIFF 的定性
================================================================================
全量结果（32 套件）：
    合计：PASS=1502 FAIL=0  套件=32
其中：
  · PASS 1502 = 第 36 轮的 1500 + 本轮新增的 E3b/E3c 两条。
  · FAIL 恒为 0。
  · 30 套 IDENTICAL；2 套初判 DIFF（round17_build_fall / round18_climb）。

两条 DIFF 的**逐字节定性**（读了 _out/*.diff.txt 全文，不靠猜）：
    --- round17_build_fall (baseline)
    +++ round17_build_fall (now)
    @@ -1,3 +1,5 @@
    +[日志] 文件日志初始化失败，仅使用控制台: [Errno 13] Permission denied: 'E://RalseiMemory//logs//ralsei_pet.log'
    +
     === A G2：坠落判据换成"层高比较"（关窗掉到**下方窗口**也要掉） ===
    [PASS] A1 ...

即：**整份 diff 只多了一行环境提示 + 一个空行**，PASS 计数（18 / 53）与 FAIL 计数（0 / 0）
一字未变，没有任何断言变化。round18_climb 同型（320 B 的 diff，同一行）。

⇒ 判定：**不是回归，是沙箱环境噪声**。处置 = 把它归一化掉，**不去改基线**
   （改基线等于把环境问题固化成预期值，下次环境正常反而会假 DIFF）。

归一化补丁（run_all.py normalize()）：
    _ENV_NOISE = re.compile(r'^\[日志\]\s*文件日志初始化失败')
    ... if _ENV_NOISE.match(ln): continue   # 与既有 _QT_NOISE 同一条处理路径
复跑验证：
    round17_build_fall 0 18 0 IDENTICAL
    round18_climb      0 53 0 IDENTICAL
    合计：PASS=71 FAIL=0 套件=2
⇒ 两条 DIFF 消失，证明前述定性成立。

================================================================================
三、顺手补上的一个真缺口：3 套件一直**没有基线**
================================================================================
排查 DIFF 时发现 baseline.json 只有 **29** 条，而套件有 **32** 套 ⇒ 这三套一直显示
BASELINE（= 只跑内部断言，不做字节级漂移比对）：
    round34_store_config / round34b_move_beat / round34c_privacy_wire
（都是第 34 轮续三新增的套件；基线当时没落，此后每轮都"看起来跑了"，其实少了半道闸。）

处置：用合并模式登记（先确认其输出在归一化后不含环境噪声）：
    run_all.py --only round34_store_config --only round34b_move_beat \
               --only round34c_privacy_wire --update
    合计：PASS=40 FAIL=0 套件=3
结果：baseline.json 套件数 **29 → 32**，32 套件全覆盖。
（登记前逐份查过 _out/*.txt：round34_store_config 含环境噪声行 ⇒ 依赖上面那条归一化；
  round34b / round34c 本就干净。）

================================================================================
四、写入探针矩阵：沙箱策略的精确签名（用于把"环境问题"和"产品缺陷"分开）
================================================================================
症状：G2 里宠物日志栈报 `PermissionError: [Errno 13] 'E:\RalseiMemory\logs\ralsei_pet.log'`。

逐项探测（Python，非 PowerShell；每项都真跑）：
    %TEMP%                             NEW-OK  DEL-OK
    E:\Download\_tmp                   NEW-OK  DEL-OK
    E:\RalseiMemory                    NEW-OK  DEL-OK
    E:\RalseiMemory\logs               NEW-OK  DEL-OK

    覆写 E:\RalseiMemory\config.json            OVERWRITE-FAIL PermissionError
    覆写 E:\Download\_tmp\dump_rooms.csx        OVERWRITE-FAIL PermissionError
    覆写 E:\Download\_tmp\rerun_ch1_rooms.txt   OVERWRITE-OK   ← 本会话新建的文件

文件属性核对（排除 ACL / 只读位这个更简单的解释）：
    E:\RalseiMemory\logs\ralsei_pet.log
      mode = 0o100666    readonly = False    size = 34856 B
      mtime = 2026-09-23 20:58:10
      os.rename(...) → PermissionError [WinError 5] 拒绝访问

⇒ 签名：**新建处处成功、删除成功；覆写「本会话之前就存在的文件」一律被拒**
   —— 与文件自身权限无关（0666 且非只读），是**沙箱策略**。
   ★ 判据方向：先怀疑核验手段（沙箱），再怀疑被测物（产品）。
   ★ 结论：`ralsei_pet.log` 写不进去是**跑 G2 的这个受限 shell** 造成的；
     真机直接 `python src\main.py` 时磁盘可写，不出现。

================================================================================
五、推翻一个"遗留项"：ch1 rooms_map.json 并不需要重跑
================================================================================
第 36/37 轮记录的遗留项原文：
    「ch1 rooms_map.json 需重跑（bg_sprite 全被解析成类型名 UndertaleSprite）」

实测复核（只读，未改动任何既有文件）：
    work copy = E:\Download\_tmp\drw\chapter1_windows\data.win
    BEFORE  rooms=147  bg_sprite_layers=40  bad(UndertaleSprite)=0
    BEFORE  sample names = ['bg_alphysalley','bg_alphysclass','bg_cardcastle_outside',
                            'bg_cc_elevator','bg_cc_throneroom','bg_chess_statue_small',
                            'bg_darkcastle_front','bg_darkstone_1','bg_darkstone_2',
                            'bg_darktown','bg_darkwest','bg_diner', ...]

即 **bad = 0，40 个背景层全是真实精灵名**。再对上第 37 轮的批处理日志：
    CHAPTER chapter1_windows
      orig size=14658588 sha=e16ff3ea7c838350
      [dump]  rc=0 48.0s
      [rooms] rc=0 2.6s
      orig sha unchanged: True
      rooms_map.json 191665 bytes
      rooms in map: 147

⇒ batch_all.py 那一轮**已经把 ch1 用修正版 dump_rooms.csx 重导出过一次**
  （该 csx 内含 ResolveName 修复，覆盖 Sprite / BackgroundDefinition /
   SpriteDefinition / ObjectDefinition / Texture 三层回退）。
⇒ **遗留项不成立**，无需重跑。
  旁证：第 37 轮的背景映射里 ch1 有 3 个场景命中 `room.bg_layer`
  （ch1.kris_room.kris_s_room / ch1.castle_town.castle_town /
    ch1.card_castle.card_castle_throne）——若 ch1 的 bg_sprite 还是
  `"UndertaleSprite"` 这个类型名，素材池里绝不可能有这个 PNG，命中数为 0。
  「推翻依据本身也是证据」，故一并归档。

================================================================================
六、记忆文件压缩（系统强制项）
================================================================================
    MEMORY.md（速查本，会被自动注入）
        before: 12270 字符  → 会被截断（注入上限约 12000 字符）
        after : 11114 字符  余量 886 字符（约 7.4%）
        结构自检：## 0 .. ## 11 全在；glued（标题被粘到上一行）= []；
                  BOM = False；替换字符 U+FFFD = 0；指针 40 个
   参考-契约与历轮（详版）.md（不注入，可任意长）
        before: 236598 B      →  after: 263117 B

压缩手法（逐字改写被证明无效，只省 138 字符 ⇒ 必须**结构性删除**）：
  · 历轮索引：17 行散表 → 6 行紧凑块（信息不减，行数减半）
  · 第 26~31 轮 6 行 → 1 行；第 34 轮的 4 行 → 2 行
  · 待裁定清单去重（"动态结巴率"原本在 #3 与 #10 各写一遍 → 合并进 #10，
    并同步把 §3 误报清单里的「待裁定 #8」改成「#6」）

★ 压缩途中发现并修掉一个**真缺陷：悬空指针**
    速查本与详版本文件第 2555 行都在引用 `§19`，而**详版从来没有 §19 这一节**
    （§14/§15/§17/§18/§20/§21/§22 都在，中间断开）。
    按"先补详版再压速查"的原则，在详版补写 §19（第 30 轮：产品链路首字实测 + 口径纠正，
    提交 aaa89f8 / 8b4d50c），并把该节自己引用的悬空指针点清。
    复验：速查本 40 个指针 **missing = []**。
    教训：压缩前必须核验"指针指向的章节真的存在"，否则压缩 = 静默丢信息。

★ 同轮修掉 skill `agent-memory-compaction` 里一条**自相矛盾的判据**：
    「何时用」段写"判据：bytes 是否 ≤ 16384 B"（旧口径，字节），
    而「陷阱清单」段写"上限是字符数、约 12000 字符"——两处冲突且前者会误判安全。
    统一改成字符口径，并补一句"1 中文字 = 1 字符 = 3 字节，拿字节看会觉得离上限还远"。

================================================================================
七、本轮删除/新增的文件清单
================================================================================
删除（我早前留下的写入探针垃圾，内容分别是 "AAAA" / "y" / "a"）：
    zz_g1.txt（已 git rm）、.workbuddy/memory/zz_probe.txt、.workbuddy/zz_probe.txt
新增（仓库内）：
    ralsei_pet/assets/scenes/bg/*.png                      87 张场景背景
    code-quality-audit/第37轮-原作素材反编译/              报告脚本与产物
    code-quality-audit/第37轮-原作素材反编译/_evidence/     证据（含本文件）
新增（仓库外临时件，落在 E:\Download\_tmp\，用后即删）：
    rerun_ch1_rooms.py / rerun_ch1_rooms.txt（ch1 遗留项复核）
