=== 第40轮 · 令牌回验（父版本 7b9d10a → 现版）=== 
父版本（压缩前）：js_len=14159  bytes=24338  lines=117
现版本（压缩后）：js_len=9904  bytes=16220  lines=73
候选令牌（正则 §x.y / 7~40 位 hex / ascii 标识符 / 3+ 位数字）：266 个

在「现版速查本 ∪ 详版」中缺失：0 个
missing = []

【逐条裁决】（压缩**当次**回验时 HEAD 仍为父版本，报红 2 个）
  · 216  —— **有意删除**。父版本第 5 行「24,216 字节」是**已被推翻的错误判据**
           （第40轮代码实证：真上限 = 10,000 字符），留着就是留错。
  · §11.6 —— **误报（判据太窄）**。父版本写 `§11.6`（指父版本自身 §11 第 6 条），
             详版对应内容写作 `### 11.6 persona 的 markdown 禁令又赚了一次`（**不带 §**）。
  ⇒ 把上述两条裁决写进详版 §41.9 后重跑，**missing = 0**（裁决文字本身即含这两串字面量）。

★★ 方法论：拿"现版比现版"是**恒真判据**（本条脚本初版即踩 —— HEAD 已推进到新版，
   对比退化为自己比自己）。必须先 `git cat-file blob <父提交>:<路径>` 切回压缩前版本再比。
