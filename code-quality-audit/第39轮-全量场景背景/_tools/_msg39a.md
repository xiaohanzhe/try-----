第39轮：全量场景真背景落地（157 个场景配原作 PNG）+ 近似档零扩大

用户口径（睡前提的）：「我设置了几个等待发送的任务你就做吧」+ 第 34 轮「测完了没问题后按计划自己推进」。
本轮只做**不需要口径拍板**的那一半 —— 凡原作房间自带背景精灵的，用原作 PNG 原样落盘；
拿不到真背景的一律留空，第 37 轮那套"区域代表素材"近似**适用范围一点没扩大**。

## 数据与素材
- 新增 148 张原作 PNG（原样复制，零缩放/裁切），bg/ 88 → 236（os.listdir 真实计数），0 出错
- 场景字段接线 235 条目：bg + bg_source + bg_asset（原作精灵名，可溯源）
  其中 87 锚点补来源标注、148 个区域分片场景首次配上 bg
- 修正 87 处过期注释（why_bg_is_a_placeholder 说"文件尚不存在"，而第 37 轮早已生成）
- git diff = +705 / -383，**精确等于设计值** ⇒ 文本级逐行替换只在目标行动手

## 量化（全部照进证据）
- 五章 1,251 房间（147/278/246/328/252）；有背景层 185 / 有瓦片层 689
- 1,013 场景：真背景 157（15.5%）/ 近似 856（area_table 700 + chapter_tiles 156）/ 未配上 0

## 判据工程化
- 判据唯一实现 _tools/bg_common.py::classify()（与第 37 轮 pick_asset 逐字同口径）
- 溯源事实表蒸馏入库（_evidence/原作房间背景溯源.json）⇒ 套件**不依赖 E:\Download 临时区**
  —— 临时文件一清，套件不是报红而是静默失去鉴别力（恒真判据的另一种形态）
- 自检：溯源表重建 classify vs 原始 dump，1,013 场景全部一致

## 关键发现
- 真背景的忠实上限就是 15.5%；856 个近似场景里 634 个所属房间有瓦片层
  ⇒ 更忠实的路径是"按原作瓦片数据还原房间画面"（需用户点头）
- P1 前置：dump_rooms.csx 没导出背景层几何（x/y/xscale/yscale/tiled/层序）
  ⇒ 实测 6 个房间素材宽 < 房间宽一半（平铺重复绘制）⇒ P1 前必须扩 csx + 重跑五章 UTMT
- 选层复核：20 个房间有多个背景层；取到的不是最大层的只有 1 个；
  2 处待复核（torielclass 取到日语变体 / dw_castle_restaurant 取到柜台层）—— 不擅自改

## 判据侧 3 次失真 + 1 次我的错误推断
- 溯源表自检抓 844 条（蒸馏尺寸表漏了全量 bg_* 候选）
- 新套件 B1/B3 报红 78 条（判据越权管了第 37 轮口径的近似锚点）
- B2 负控制变恒真（受害对象挑了锚点，而锚点分支不看真/近似差异）
- H1 报红 148 条（夹具喂裸 _index.json 条目，违反 load_index 契约）
- 我推断"素材是半分辨率靠 xscale 放大"被数据推翻（87 个 320×240 里 84 个房间本身也是 320×240）

## 回归
- 新套件 verify_bg_round39.py 30 项（8 负控制 + 3 正控制）已进 G2
- 全量 G2：33 套件 / PASS=1547 / FAIL=0 / 全 IDENTICAL（1505 → 1517 → 1547）
- 记忆复检 40/40；逐令牌回验 0 丢失；.pyc 漂移 0

## 待用户裁定
- Q1 856 个场景的画面：保持留空 / 按原作瓦片还原 / 沿用近似
- Q2 ch1.kris_room 的 bg 是 640×480 放大件（原作 320×240、其余 4 章同名场景都是原样），是否换回
- Q3 2 处选层是否定向覆盖；Q4 78 个近似锚点是否与 Q1 一起升级
