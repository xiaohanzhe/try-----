# ⚠️ 本目录产物可信度说明

## 不可信（请勿当作权威数据）

| 文件 | 状态 | 原因 |
|---|---|---|
| `ROOM.json` | ❌ **不可信** | 147 行中 118 行解析失败；读出的"名字"是二进制噪声 |
| `SPRT.json` | ❌ **不可信** | 388 行中 709 行解析失败 |
| `OBJT.json` | ❌ **不可信** | 0 行成功 / 353 行失败 |
| `datawin_index.json` | ⚠️ **仅 chunk 表可信** | chunk 偏移链（22 块）已验证正确；rooms/sprites/objects 为空 |

**根因**：GEN8 的**指针表基址不是「count + ptrs 之后」** —— 中间存在对齐填充或额外 payload，
导致按 `base = ptr_table 尾` 计算出的每项地址全部落在 chunk 的二进制区里。

## 可信的

| 文件 | 状态 | 说明 |
|---|---|---|
| `parse_datawin.py` | ✅ | 单遍 `bytes.find` 扫描，**22 个 chunk 偏移链已交叉验证正确** |
| `parse_sub.py` | ⚠️ 骨架可用 | 「一次一张表 + 上界校验 + 体量断言」的框架是对的，**只是基址算法要换** |

### 已验证正确的 chunk 链（chapter1_windows/data.win, 14 658 588 B）
```
@8         GEN8   784
@800       OPTN   88
@896       LANG   24
@928       EXTN   8
@7008      AGRP   24
@7040      SPRT   147640
@154688    BGND   9608
@164304    PATH   8
@164320    SCPT   8312
@173888    SHDR   8
@173904    FONT   421496
@595408    TMLN   8
@595424    OBJT   145784
@741232    ACRV   8
@741248    SEQN   8
@741296    ROOM   1866216
@2607520   DAFL   8
@2607536   EMBI   24
@2607568   TPAG   78344
@2685920   TGIN   4792
@2690720   CODE   1657304
@4348032   VARI   103640
@4451680   FUNC   34184
@4485904   STRG   523752
@5009664   TXTR   3272968
@8282640   AUDO   6375940
```

### 建议的重做路线（下次接手时）
**不猜偏移**，改走文本侧：
1. 全文件扫「连续可打印 UTF-8 且长度在 2~64 之间的短串」→ 得到候选名字池。
2. 用 `chapter1_windows/lang/lang_en.json` 的**键名前缀**（对象名，如 `DEVICE_CONTACT`）
   与候选池交叉，反推这些名字在 `STRG` 块中的真实区段。
3. 由 `STRG` 的真实区段反推 `STRG` 的指针表基址 ⇒ 得到 GEN8 的**对齐规则**。
4. 用这个对齐规则回头修 `SPRT`/`OBJT`/`ROOM`。

### 已有的替代数据源（已确认可用，无需解析 data.win）
- `chapter1_windows/lang/lang_en.json`（568 660 B / **6242 条**）：
  键名 = `对象名_slash_事件_gml_行号_序号`，值 = 原文对白。
  **等于是按 GML 源码逐行索引的对白库** —— 对「一切根据原作」这个口径，
  **文本侧已经够用了**，精灵/房间表可以后补。
