
=== A 上楼：走进更高楼层 → **起跳**而不是被静默提升（G3 本体） ===
[PASS] A1 走上更高楼板 → 真的起跳（is_jumping=True）
[PASS] A2 **没有静默提升**：current_floor 仍是桌面（缺口 G3：改前这里会被直接赋值成 201 那层）
[PASS] A3 跳跃目标 = 那块窗口的**楼层**（不是裸窗口/桌面）
[PASS] A4 落点落在目标层的**可见区域**内
[PASS] A5 落点与宠物**预留了水平距离**（用户："别看着和垂直起跳一样"）
[PASS] A6 本拍**没有**触发任何坠落（起跳与坠落互斥）

=== B 跨度高：跨度 > 一层 → 用攀爬素材（用户："跨度低跳、跨度高爬"） ===
[PASS] B0 前置：合成几何给出 ph=10 的目标层（跨度 10 > 5 → 该用"爬"）
[PASS] B1 跨度 10 → 起跳第一帧就用**攀爬素材** climb_right（不是 jump_ready）
[PASS] B2 覆盖标记被记住（handle_jump 每帧据此保持攀爬素材，而不是被 jump 顶掉）
[PASS] B3 跨度低（≤一层）→ 仍用 jump 家族（不能把所有跳跃都换成爬）
[PASS] B4 跨度高（>一层）→ 判为 climb（上、下两个方向都算）
[PASS] B5 爬向选型：横向为主 → left/right；无横向位移 → front（没有朝后的素材）

=== C 下楼：走进更低楼层 → **跳**而不是重力掉落（用户："下楼别用掉落，也用跳"） ===
[PASS] C0 前置：宠物站位命中 ph=5 的更低楼层、出发层是 ph=10
[PASS] C1 走进更低楼层 → **起跳**（is_jumping=True）
[PASS] C2 **没有**走重力掉落（用户口径：主动下楼不用掉落）
[PASS] C3 目标是那块更低的楼板（ph 5），且落点在它的可见区域里
[PASS] C4 下落方向被正确识别为"往下"（jump_target_z 更低）
[PASS] C5 下楼也预留水平距离

=== D 被动成因不被误伤：关窗（要求⑩）/ 新窗口盖住旧窗口（要求⑪） ===
[PASS] D1 关窗（旧楼板失效，is_moving=False）→ 仍是 start_falling(reason="floor_removed")，闸门不接管
[PASS] D2 关窗（旧楼板失效，is_moving=True）→ 仍是 start_falling(reason="floor_removed")，闸门不接管
[PASS] D3 被更高的新窗口盖住（要求⑪，宠物没走路）→ 不跳不掉，直接站新板

=== E 闸门兜底：起跳不可行时，上楼**不提升**、下楼**服从重力** ===
[PASS] E1 上楼衔接不可行 → **保持原楼层**、不起跳（不能退回"静默提升"）
[PASS] E2 下楼衔接不可行 → 走重力掉落（不带"用户行为"起因）

=== F 摔扁门槛：只在"层数比较高"时触发（用户第十八轮口径） ===
[PASS] F1 落差一层（5）+ 高速 → **不**摔扁（"层数比较高"不成立）
[PASS] F2 落差两层（10）+ 高速 → 摔扁（门槛 = 10）
[PASS] F3 落点**不是桌面**（25 → 10，落差两层）→ 也摔扁（不只"掉到桌面"才算）
[PASS] F4 速度不够（≤150）→ 不摔扁（沿用改前口径，不引入新手感变量）
[PASS] F5 落差为 0（同一层"落地"）→ 不摔扁
[PASS] F6 缺 `_fall_from_height` 时按 0 处理（防御：不因缺属性抛异常）
[PASS] F7 落差**夹到 ≥0**（落点比起点还高时不能交出负的"落差"）

=== G 素材与登记（用户指定的三组攀爬素材） ===
[PASS] G 动画组 climb_right 已登记且首帧 = spr_ralsei_climb_1_0.png（5 帧）
[PASS] G 动画组 climb_left 已登记且首帧 = spr_ralsei_climb_left_0.png（5 帧）
[PASS] G 动画组 climb_front 已登记且首帧 = spr_ralsei_climb_0_degrees_0.png（6 帧）
[PASS] G4 三组用到的素材**全部在磁盘上**（含 5 张镜像生成的 climb_left）
[PASS] G5 climb_left 的帧与 climb_right 的帧**不是同一批文件**（镜像必须真落盘，不能靠 alias_of 顶替）
[PASS] G6 素材在库 → 返回对应爬向的素材名
[PASS] G7 素材**不在库**（旧素材包）→ 返回 None，调用方回落 jump 家族（不切灰块）
[PASS] G8 未知方向 → 退到 front（不抛 KeyError）
[PASS] G9 完全**没有** sprite_loader 属性 → 也不抛异常（返回 None 回落 jump）
[PASS] G10 climb_left 每帧都是 climb_right 对应帧的**严格水平镜像**（逐像素采样；"原样复制"会被这条抓住）
[PASS] G10b 自检：该判据对"未镜像的同一张图"必须报不符（否则它是恒真的假绿）

=== H 源码级：闸门与"跳还是爬"各只有一处 ===
[PASS] X0 源码视图自检：func_code 剥字符串字面量 / func_lit 保留；两者都剥注释
[PASS] H1 `check_window_movement` 里真的调了层高闸门 `_start_climb_transition`
[PASS] H2 闸门的入口条件用了既有属性 `is_moving`（不为闸门新增状态）
[PASS] H3 闸门对"上楼够不着"的处理是**保持原楼层**（return 而不是赋值提升）
[PASS] H4 "跳还是爬"只在 start_jump 里选一次（两条入口共用，不各写一份）
[PASS] H5 `handle_jump` 尊重 `_jump_anim_override`（否则每帧被 jump 顶掉 = 白切）
[PASS] H6 摔扁判定收敛成唯一入口 `_should_splat_on_landing`（不再有裸的 fall_speed > 150）
[PASS] H7 起跳楼层与目标楼层都能在 `start_jump` 里看到（单点选型的前提）
[PASS] H8 `_start_climb_transition` 只走楼层跳跃链路（不碰 start_falling）
[PASS] H9 第十二轮起的"生气动画时长"契约没被本轮动过

=== I 设计前提：为什么"跨层拆成逐层小跳"在这套几何下不可行（勿回退的依据） ===
[PASS] I1 站在前层可见区域里的位置，**同一位置的下层没有可见区域**（逐层小跳的第一步将无处落脚）
[PASS] I2 两层名次确实不同（本断言的前提：前层更高）

============================================================
第十八轮（批次 B）：PASS=53 FAIL=0
============================================================

EXIT=0
