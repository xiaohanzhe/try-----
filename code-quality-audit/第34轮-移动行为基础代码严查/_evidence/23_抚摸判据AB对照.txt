========================================================================
探针 23：抚摸判据 A/B 对照
========================================================================
git = D:\Program Files\Git\cmd\git.EXE

HEAD = 2e49fd8e629a6ea540a3713086213501aaa06555 (rc=0)
HEAD~1 = 55c53fea4e168d2a22a62d9e8c3962aa9a385348 (rc=0)
父版本 main.py 取到 375919 字符

工作区 main.py 取到 376234 字符

[PASS] 新旧源码不同，A/B 有鉴别力

--- 旧判据关键行 ---
    current_dir = 'horizontal' if abs(move_dx) > abs(move_dy) else 'vertical'
    prev_dir = 'horizontal' if abs(prev_dx) > abs(prev_dy) else 'vertical'
    if current_dir == prev_dir:
    if current_dir == 'horizontal':
--- 新判据关键行 ---
    if abs(move_dx) >= 1 and abs(prev_dx) >= 1:
    if abs(move_dy) >= 1 and abs(prev_dy) >= 1:

用例                               旧dc      新dc      阈值2        判定
------------------------------------------------------------------------------
T1 纯水平往返                         17       17       期望触发       旧✓/新✓
T2 水平往返+纵抖±4                     11       54       期望触发       旧✓/新✓
T3 垂直往返+横抖±4                     14       38       期望触发       旧✓/新✓
T4 自然弧线(15,12)                   2        2        期望触发       旧✓/新✓
T5 自然弧线+抖动                       2        12       期望触发       旧✓/新✓
T6 斜向45°来回                       5        11       期望触发       旧✓/新✓
T7 原地噪声（不该触发）                    0        0        期望不触发      旧✗/新✗
T8 单向横扫（不该触发）                    0        0        期望不触发      旧✗/新✗

------------------------------------------------------------------------------
  旧判据符合预期: 8/8
  新判据符合预期: 8/8
  修复生效（旧不触发→新触发）: （无）
  修复引入退化           : （无）

[FAIL] 修复未生效 —— 新判据与旧判据行为一致，C 缺陷不成立或被误诊
