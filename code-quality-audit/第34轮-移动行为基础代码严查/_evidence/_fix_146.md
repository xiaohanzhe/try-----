### 14.6 提交与推送（铁律 0 全套）

- commit `8f95f5e`：25 files changed，**+2760 / -100**；
- 提交信息**逐字节核验 IDENTICAL（45/45 行）**、无 BOM（首 3 字节 `[231,172,172]` = 「第」）；
- push 走 `extraheader`（非交互 GCM 取凭据）+ 本地代理 7897；
- **三方一致**：本地 HEAD == `origin/main` == `ls-remote` == `8f95f5e387161b8...`；
- 工作区剩余未提交 **0**；`E:\Download\_tmp\` 临时件已清。

> ⚠️ **本条踩坑记录**：我最初用内联 `python -c` 写这段（含反引号），
> Bash 把反引号内容当**命令替换**执行 → 报告落盘成 `commit ****`、`HEAD ==  ==` 残句。
> 这正是本文件 §1 已记录的坑，**又一次复发** ⇒ **含反引号的文本一律用 Write 写文件，再拼接**。
