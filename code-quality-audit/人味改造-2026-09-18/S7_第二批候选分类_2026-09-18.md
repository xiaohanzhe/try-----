# S7 第二批候选分类（决策清单）

> ## 〇、执行结果与口径更正（2026-09-19）
>
> **已完成**：§二 的 2 条（`rest_start` / `eat_start`）+ §五 的文案修复。
> G2 全量 **PASS=1050 FAIL=0 / 24 套件 / 全 IDENTICAL**；
> `s7_event_speech` 套件新增 C10~C12、D24~D25 共 7 条断言（117 → 124，全过）。
>
> **口径更正**：本文档 §一 与 §三 有两处把风险**说重了**，核对源码后修正如下 ——
>
> 1. §一 第二行 + §三 说「迁环境自动触发 → 绕过「自主说话」600 秒节流 → Ralsei 明显变话多」。
>    **不成立。** 说话**次数**由触发源决定，跟「走不走 AI」无关：§三 表里的累/饿播报，
>    本来就由 `energy_hunger.check_status_changes()` 的
>    `if energy_tier != self._prev_energy_tier:`（档位跨越才弹一次，原注释写着
>    "避免每个 tick 重复刷屏"）管着。迁不迁它，**弹几次一模一样**，
>    变的只是"措辞由固定句改成 AI 现编"。
> 2. §一 末段说「`speak_event` 没有时间维度的节流 —— 它只防"叠加"，不防"太频繁"」。
>    **不成立。** `main.py:5200` 有 `EVENT_SPEAK_MIN_INTERVAL = 2.0`，
>    `_event_ai_ready()` 尾部就是这道频率闸（回归锁 B10 / D18 一直在钉它）。
>
> **结论**：§三 那 11 条**不需要"先立一条契约"才能动**，也不存在"会让 Ralsei 变话多"的风险。
> 它们只剩一个很小的偏好问题 ——「累/饿/定时问候这些自言自语，你要固定台词，还是 AI 现编」。
> 这条**不阻塞任何事**，可以随时做、也可以一直不做。
>
> 下面的 §一、§三 原始判断**原样保留**（留痕，不改写历史），但以本节为准。

---

**日期**：2026-09-18
**依据**：`_evidence/event_lines.txt`（本轮重跑，`scan_event_lines.py`）——
`main.py` **131 → 112**、全项目 **191 → 172**（即 batch 1 已迁 19 处）。
**源码核对**：`main.py` 的 `speak_event` / `_event_ai_ready` / `_event_say`、`event_speech.py` 的 `EVENT_TIERS`。

---

## 一、为什么这一批不能"照着搬"

batch 1 的对象是**鼠标体感交互**（戳/捏/拍/摸/喂/双击），特征是：

* 用户**显式交互**触发；
* 台词是**纯情绪表达**，不含信息；
* 同一句会被反复听到（"连着摸三次都是「嘿嘿~ 好舒服呀！」"）→ 复读感最强。

剩下 112 处里，**大部分不满足这三条**。硬搬会有两类可感知的回归：

| 误迁类型 | 后果 |
|---|---|
| 迁了**功能性回执**（"已切换到下一张幻灯片！"、"表中包含了以下人名：…"） | **信息丢失 / 变慢**：这些是确定性操作的结果播报，交模型只会更差（还可能编造路径、编造文件名） |
| 迁了**环境自动触发**（累/饿播报、定时问候） | **绕过节流契约**：见 §三，Ralsei 会明显变"话多" |

而且 `speak_event` 的 `_event_ai_ready()` 虽然有一整套"让路"条件（主人正在打字 / `_ai_inflight` /
`_streaming` / 前台是「……」占位 / 上一个事件还在等 AI），**但它没有时间维度的节流** ——
它只防"叠加"，不防"太频繁"。

---

## 二、该迁（**建议本批就做**，2 条，与 batch 1 同性质）—— ✅ 已于 2026-09-19 执行
| # | 位置 | 台词 | 为什么该迁 |
|---|---|---|---|
| 1 | `energy_hunger.py:160` `rest()` 正常进入休息 | 「我要休息一下啦... 呼...」 | 用户点了"休息"按钮 → **显式交互**；台词是纯情绪（"呼..."），**不含信息**；每次点都同一句 |
| 2 | `energy_hunger.py:179` `eat()` 正常进入进食 | 「哇！有好吃的！我开动啦！」 | 同上（用户点了"喂食"） |

**建议事件名**：`rest_start` / `eat_start`（写进 `EVENT_TIERS` = `TIER_AI` + `EVENT_DIRECTIVES`
旁白，例如「（主人让你去休息，你乖乖躺下了。）」/「（主人喂了你东西，你开心地吃了起来。）」）。

**改造方式**：`self.parent.speak_event("rest_start", pool=["我要休息一下啦... 呼..."], face="normal")`
—— `speak_event` 内部已经做了 `add_dialogue` + `show_dialogue` 成对处理（`_event_say`），
所以**两行普通调用要合并成一次 `speak_event`**，别保留原来的 `add_dialogue` + `show_dialogue`。

---

## 三、**需要先拍板**的设计问题：环境自动触发的话，走哪条路？

下面这些是**环境/时间/物理触发**（不是用户交互），batch 1 里一个都没有，所以这一批会第一次遇到：

| # | 位置 | 台词 | 触发源 |
|---|---|---|---|
| 3-6 | `energy_hunger.py:102/119/128/132` `check_status_changes` | 「呜... 我真的好累好累... 几乎走不动了...」「我有点累了... 能不能休息一下？」「肚子好饿好饿... 我快饿死了...」「嗯... 我有点饿了... 有没有什么吃的？」 | **env**（精力/饥饿档位跨越） |
| 7-10 | `pet_ai.py:393/399/405/411` `check_special_events` | 「早上好！今天看起来是个美好的一天！」「中午了呢，你有没有好好吃饭呀？」「晚上好！今天过得怎么样呀？」「已经这么晚了，要注意休息哦！」 | **env**（时间） |
| 11 | `main.py:3737` `wake_up` | `random.choice(wake_up_messages)` | **physics** |
| 12 | `main.py:3642/3659/3711` `handle_fall` 恢复语 | 「呼...我没事了...谢谢你的关心...」 | **physics** |
| 13 | `main.py:3949` `react_to_desktop_element` | `reaction['dialogue']` | **env** |

**问题**：项目有一条既有契约（勿回退契约 #13 那一族）——

> **自主说话的唯一入口是 `start_autonomous_speech()`**，它的节流常量是
> `AUTONOMOUS_SPEECH_MIN_INTERVAL = 600.0`（秒）。

上面这些"没人操作、Ralsei 自己开口"的台词，**语义上就是"自主说话"**。

* 若走 **`speak_event`** → 绕过 600 秒节流 → 累/饿 + 定时问候都可能连着说 → **Ralsei 明显变话多**；
* 若走 **`start_autonomous_speech()`** → 继承节流与既有判断，但那条路径的语义是
  "自己想说话"（有话题锚、有记忆召回），把"饿了的生理播报"塞进去**语义不对**；
* 若**保持罐头** → 零风险，但放弃了这部分人味。

**我的建议**：这一批**先只做 §二 的 2 条**，把 3-13 留下来，
在"自主说话节流"这条契约上**单独定一条规则**再做（例如"档位播报走 `speak_event` 但自带
N 秒最小间隔"，或"生理播报走 `speak_event`、社交问候走 `start_autonomous_speech()`"）。
—— 在那条规则定下来之前迁移，等于**悄悄改掉了"Ralsei 多久能自己开口一次"这个用户可直接感知的观感**，
而这是"分批迁移"最不该发生的事。

---

## 四、明确**不迁**（而且要写下来，防止下一轮有人顺手扫过去）

### 4.1 功能性回执（交模型会丢信息 / 会编造）

* `main._execute_api_action` 全部 11 条：搜索 / PPT 播放切换停止 / 浏览器窗口计数 / 桌面文件计数
  —— 都是**确定性操作的结果播报**，含文件名、数量、页数。
* `main.handle_file_operation` / `create_person_name_table` 全部 14 条：含**生成的路径与文件名**，
  交模型有编造风险。
* `main.play_rock_paper_scissors` / `play_guess_number` / `end_*` 全部：**游戏规则与比分**，
  规则类文本必须逐字准确（"请输入 1 到 100 之间的数字"）。
* `main.save_config`「配置已保存！」、`_execute_api_action`「我更喜欢自己走来走去呢！」
  —— 系统回执 / 模式说明。
* `main.start_mouse_drag`「我来帮你拖动鼠标吧！」/ `stop_mouse_drag`「拖动完成啦！」
  —— 是"开始/结束"的**状态确认**，用户靠它知道操作生效了。
* `energy_hunger.rest()` 精力已满的「我现在精神得很，一点都不累哦！」
  和 `eat()` 已吃饱的「呜…我已经吃得饱饱的啦，再吃就要撑破了！」
  —— **这两句是"拒绝"的原因说明**，不是情绪表达。迁了 AI 就**失去了"为什么没反应"的唯一提示**。

### 4.2 短促物理反应（batch 1 已经定过口径，沿用）

`start_fall` / `trigger_splat` / `handle_fall` 的"啊！摔扁了..."、`fling`、`splat_poked`、`ear_ruffle`
—— 0 延迟要求，物理状态机输出，`TIER_INSTANT`。

---

## 五、顺带发现：一条**纯文案**问题（零成本，与 S7 无关）—— ✅ 已于 2026-09-19 执行

`main.py:3981` `react_to_desktop_element`：
> 「**需要我帮你打开浏览器或搜索什么吗？**」

这是 persona 第 40–41 行**明令禁说**的助手腔（"有什么可以帮你的吗"的近亲），
但它**是写死的字符串**——persona 管不到写死台词，所以这句话会一直说下去。

**建议**：直接改文案（换成 Ralsei 的口吻，例如"那个东西看起来有点意思……不过我还是不随便碰啦"），
**不需要接 AI**。这条可以随时做，不阻塞任何东西。

---

## 六、本批的回归要求（做的时候照这个走）

1. `s7_event_speech` 套件加 **C 组迁移完整性**断言：新登记的每个 kind 都必须在
   `EVENT_TIERS` 与 `EVENT_DIRECTIVES` **同时**存在（只登记一边 → 行为会静默退化成罐头）。
2. 加 **D 组行为级**断言：`rest_start` / `eat_start` 在"AI 可用"时走 AI、
   在"AI 关闭"时**必须沉默或走罐头**（守既有契约：AI 关闭不得回落内置台词）。
3. `energy_hunger.py` 的调用点断言：**不得**同时保留 `add_dialogue` + `show_dialogue`
   （否则一次事件两个气泡）。
4. 全量 G2（当前 24 套件 / 1043 PASS）必须全绿，`--update` 重建基线后复跑确认 IDENTICAL。

---

## 七、本次落地清单（2026-09-19）

| 文件 | 改动 |
|---|---|
| `ralsei_pet/modules/event_speech.py` | `EVENT_TIERS` 登记 `rest_start` / `eat_start`（均 `TIER_AI`）+ `EVENT_DIRECTIVES` 两条旁白 |
| `ralsei_pet/modules/energy_hunger.py` | `rest()` / `eat()` 的"开始"台词改走 `self.parent.speak_event(...)`（原 `add_dialogue` + `show_dialogue` 两行**删除**，避免一次事件两个气泡）；**"拒绝原因说明"两句保持罐头不动** |
| `ralsei_pet/src/main.py` | `react_to_desktop_element` 里写死的助手腔「需要我帮你打开浏览器或搜索什么吗？」→ 「咦，是浏览器呀……我平时不太敢乱碰里面的东西呢。」（`face` 由 `helpful` 改 `surprised`） |
| `code-quality-audit/人味改造-2026-09-18/verify_s7_event_speech.py` | 新增 C10/C11/C12（迁移完整性、双侧登记、拒绝语保持罐头）+ D24/D24b/D24c/D25（行为级：AI 可用走 AI / AI 关闭说罐头原句且不发请求） |
| `code-quality-audit/regress/baseline.json` | 重建（`s7_event_speech` 断言 117 → 124） |

**验证**：`s7_event_speech` **124 PASS / 0 FAIL**；全量 G2 **PASS=1050 FAIL=0 / 24 套件 / 全 IDENTICAL**
（日志：`_evidence/s7_event_speech_after_batch2.txt`、`_evidence/g2_full_compare_batch2.txt`）。

**没动的**：§三 那 11 条环境/时间触发台词（口径更正见 §〇 —— 它们不阻塞、不会让 Ralsei 变话多，属可选润色）；
`main.py:4000~4010` 的 `help_messages`（PPT/Excel/Word…）—— 那些是**功能发现性提示**（真的在告诉用户能干什么），
不是 §五 那种**零信息的助手填充句**，不在本次范围。

