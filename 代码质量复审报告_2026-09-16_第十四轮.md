# 代码质量复审报告 — 第十四轮（2026-09-16）

**主题**：「建楼」的**上下动**（跨楼层移动）＋ **落到某一层楼的效果**
**用户原话**：*"继续完成上下动和落到某一层楼的效果，要求就是我之前说的那个建楼的要求，然后你可以分批次完成"*
**提交**：批次一 `41ffecd`（上下动 / 接线）｜批次二 `9960e89`（落地 / 生气动画）｜G2 **18 套件 / PASS=697 / FAIL=0 / 全 IDENTICAL**

---

## 一、结论（先说结果）

用户说的"上下动"和"落到某一层楼"，**代码里其实都写过 —— 但产品一个都没调用**。
第十三轮把 `floor_manager` 的判据全换成了"可见区域"，套件 C 段也确实在测那些函数，
可真正决定"跳不跳、跳去哪"的 `main.py::check_nearby_windows` 压根没碰，
用的还是**裸窗口矩形 + 10~30px 贴边**那套老启发式。于是两条建楼要求真机上**没生效**：

| # | 症状（真机） | 根因 | 修法 |
|---|---|---|---|
| 1 | 能跳到窗口**被挡住的那部分** | `get_jump_destinations` 改对了可见区域，但**零调用**（死代码）；产品路径只看裸窗口矩形 | `check_nearby_windows` 改走楼层判定，落点必须落在该层**可见区域** |
| 2 | 从 3 楼"穿透"2 楼回桌面 | 站在窗口上时旧逻辑把目标**直接声明成桌面**，`handle_jump` 的穿透检查把它打断成"取消跳跃 + 自由落体"（硬着陆） | 向下**显式**取相邻下一层（`adjacent_lower_floor`），不穿透 |
| 3 | 走路站上窗口后按"桌面"判 | `current_floor` 有更新、`current_window` 没更新 —— **两套真源**，跳跃判定读的是后者 | `current_window` 降级为**派生缓存**，统一由 `_sync_window_cache_from_floor` 刷新 |
| 4 | 关窗（用户行为）掉下来用的是普通动画 | `start_falling()` 没有"起因"参数，选不出生气的那组 | 加 `reason='floor_removed'` → `fall_mad` ≥5s（要求第36行） |
| 5 | 跳跃落地那一瞬层级是错的 | `handle_jump` 完成帧只写老缓存、**不写 `current_floor`**，z 序要等 1 秒节拍才纠正 | 落地当场结算 `current_floor` + 同步缓存 + 立刻重排 z 序 |

**一句话**：这一轮不是加功能，是把上一轮**已经写对但没接线**的判定接进产品，
顺手把"两套真源"收成一套。

---

## 二、交付物

| 文件 | 动作 | 说明 |
|---|---|---|
| `ralsei_pet/src/main.py` | 改 | 新增 6 个方法（跳跃规划 / 缓存同步）；`check_nearby_windows` 改走楼层；`start_jump` 支持 `target_floor`/`target_pos`；落地三处收口 |
| `ralsei_pet/modules/floor_manager.py` | 改 | 新增 `adjacent_lower_floor`、`nearest_visible_point` |
| `code-quality-audit/第十四轮/verify_round14_move.py` | **新增（41 项）** | 自检套件（A 接线 / B 上跳可见性 / C 下跳逐层 / D 单真源 / E 新契约 / F 源码不变量 / G 落地） |
| `code-quality-audit/第十四轮/_evidence/` | 新增 | 证据 3 份：改前 G2（17 套件/655）、改后 G2（18 套件/697）、套件输出 |
| `code-quality-audit/第八轮/verify_round8_floor.py` | 改 | PetStub 补 `_sync_window_cache_from_floor` 与 `start_falling(reason=)`；**加严** W6（新增 W6b：关窗必须带起因）→ 16 项变 17 项 |
| `code-quality-audit/regress/run_all.py` | 改 | 挂入 `round14_move`；加入隔离清单 `HERMETIC_IDS` |
| `code-quality-audit/regress/baseline.json` | 改 | 基线重建（18 套件） |

---

## 三、"建楼"要求 → 逐条对表

原文 13 条里，**与"动"有关的这几条**是本轮的重点（★ = 本轮才真正闭环）：

| 要求原文（摘） | 落点 | 状态 |
|---|---|---|
| ★"向上跳：只能跳到这个窗口**没被其他东西挡住**的那部分边缘上" | `_floor_entry_plan` 算出落点后过 `nearest_visible_point` 吸附进可见区域；被遮部分按不了 | 本轮闭环 |
| ★"向下跳：必须先跳到 2 楼，再从 2 楼跳回 1 楼，不能穿透" | `adjacent_lower_floor` 显式取相邻下一层；落点在该层可见区域内 | 本轮闭环 |
| ★"走到楼板边缘，脚下没东西了，就直直掉下去，落到下面第一块能接住的楼板" | `get_drop_destination` 按可见区域（第十三轮已改）＋ 本轮落地当场结算 | 本轮闭环 |
| ★"我关闭窗口……必须立刻开始往下掉" | `check_window_movement` 每 1 秒节拍（第八轮已改）＋ 本轮**带起因** → 生气动画 | 本轮闭环 |
| ★"如果是我的行为导致他摔到桌面上的就用生气的那个，至少 5s" | `start_falling(reason='floor_removed')` → `fall_mad`，`max_fall_duration=5.0` | 本轮闭环 |
| "它只能往旁边跳（同一层楼板的其他位置），或者垂直向上/向下跳" | 侧向 = 走路（不受跳跃逻辑干扰）；上下 = 楼层规划 | 满足 |
| "它眼里只有……比当前这块楼板更高的、没有被完全挡住的悬崖边" | 候选来自 `get_jump_destinations`（只给更高的层 + 相邻下层） | 满足 |
| "楼层关系：哪个窗口在最前面，哪个就是最高楼层" | `platform_height = (n − i) × 5`（第十三轮） | 满足 |
| "后面的窗口如果被前面的完全盖住……暂时'不存在'" | 可见面积不足阈值 → 不成楼层（第十三轮） | 满足 |
| "我挪动窗口……宠物必须跟着楼板一起移动" | `_follow_floor_move`（第八轮） | 满足 |
| "做出那种'一层压一层'的真实堆叠感" | `_apply_pet_z_order`（第十三轮）＋ 本轮落地/跳跃**当场**重排 | 满足 |
| "移动他所在的楼板……重心不稳甚至摔倒，至少 3s" | `start_fall("window_move")` → `fall_mad` = 3.0s | 满足 |
| "摔到桌面上的动画用之前那个" | 非用户行为 → `fall`；低速落地 → `idle`/`land` | 满足 |

---

## 四、为什么"上一轮改对了却没生效"（本轮最值得记的一节）

这是本项目**第三次**踩同一个坑：

| 轮次 | 写好的东西 | 症状 |
|---|---|---|
| 第八轮 | `get_insert_after_hwnd` / `set_window_behind`（正确的 z 序机制） | 零调用 → 宠物用 `WindowStaysOnTopHint` 永久置顶 |
| 第十三轮 | 同上，被接活 | —— |
| 第十三轮 | `get_jump_destinations` / `find_support_below` / `is_on_floor_edge`（正确的可见区域判定） | **零调用** → 跳到被挡住的部分 |
| 第十四轮 | `adjacent_lower_floor` / `nearest_visible_point`（新增） | 本轮**同时**接了线，并用套件 A2 行为级锁住 |

**为什么第十三轮的套件没抓到**：C 段测的是 `floor_manager` 的函数本身（设计上没错，
那是对的函数），E 段只断言 `_apply_pet_z_order` / `get_insert_after_hwnd` 被接活 ——
**跳跃链路没有"被调用"的断言**。换句话说：套件覆盖了"函数对不对"，漏了"产品用不用"。

**本轮的锁**（`verify_round14_move.py` A2/A2b/A2c）：
- 用一个**故意不提供 `desktop_interaction`** 的 stub 驱动真实的 `check_nearby_windows`
  —— 一旦代码偷偷回落旧逻辑（要枚举裸窗口表），立刻 `AttributeError`；
- 断言产品真的起跳了、且目标楼层是那块窗口的楼层；
- 归因教训写进套件注释与本节。

---

## 五、设计取舍（为什么这么做）

### 5.1 楼层可用时**不回落**旧逻辑
`check_nearby_windows` 里新路径是**早返回**：

```
fm = self._floors_for_jump()
if fm is not None:
    plan = self._nearest_floor_jump(fm, cur_floor, ralsei_rect)
    if plan is not None: self._start_floor_jump(*plan)
    return          # ← 故意不 continue 到下面的老启发式
```

理由：老启发式**就是**"跳到看不见的地板"的根源。留着它当兜底，等于留着这个 bug。
楼层不可用（无桌面窗口 / 单测桩）时才走旧路 —— 保证 G2 不漂移，也保证沙箱/离屏可用。

### 5.2 保留"贴边才跳"的触感，只换判据
没有改成"只要横向覆盖就跳" —— 那会让宠物一路狂跳。**触发几何照旧**（宠物得贴到
某块楼板边上，`NEAR=60px` 带内），换掉的是：几何来源改成楼层的权威矩形（DWM 可见
边框），落点必须过可见性门。这样"手感不变、判据变对"。

### 5.3 向下跳**显式**取相邻层
没有从 `get_jump_destinations` 的结果里"顺便拿"相邻下层。因为那里会按"宠物 x 是否
落在该层横向范围内"过滤，而"走到楼板边缘、半个身子探出去"**恰好**就是 x 已出范围的
情形 —— 会被漏掉。要求里明写了"必须先跳到 2 楼"，这条不能靠副作用满足。

### 5.4 `current_window` 降级为派生缓存
不删（三十多处还在读它），但**只允许**由 `_sync_window_cache_from_floor(floor)` 写。
三个入口（走路换楼板 / 跳跃落地 / 重力落地）全部收口到这一处；原来重力落地里
那份手写的裸 dict 构造删掉（`verify_round14_move.py` G4 锁住"不再出现裸构造"）。

---

## 六、落地（批次二）细节

### 6.1 跳跃落地当场结算
`handle_jump` 的完成帧原来只写 `current_window`。现在：

```
landed_floor = self.jump_target_floor
if landed_floor is not None:
    self.current_floor = landed_floor
    self.current_platform_z = landed_floor['platform_height']
self._sync_window_cache_from_floor(landed_floor)
self.show(); self.move(x, y)
self.spatial_pos["z"] = self.jump_target_z
self._apply_pet_z_order()         # ← 落地那一瞬就归位，不等 1 秒节拍
self.play_animation_once("land", restore_to="idle")
```

### 6.2 低速落到**窗口楼层**要有落地交代
原来低速落地直接 `change_animation("idle")` —— 看着像"平移"过去而不是"落"上去。
现在：高速仍 `splat`（摔扁），低速且目标层是窗口 → `play_animation_once("land")`
播一次再站稳（与跳跃落地同一口径，一次性播放，符合第八轮"起身类只播一次"的约束）。
桌面那侧不动（用户已认可的观感）。

### 6.3 坠落动画按起因分流
```
start_falling(fall_velocity=0, is_thrown=False, reason=None)
  reason == 'floor_removed'  → fall_mad，max_fall_duration = 5.0
  fall_velocity > 100 / thrown → fall，3.0
  其他                        → fall，2.0
```
`reason` 只由两个"用户抽走楼板"的入口传（关窗 / 窗口被移走没跟上）。
"自己走到边缘掉下去""跳跃被判穿透"都不带起因 —— 不算用户行为，不误用生气动画
（套件 G2 专门反证这一条）。

---

## 七、验证

### 7.1 自检套件（41 项，全过）

| 分组 | 内容 |
|---|---|
| A 接线 | 行为级证明产品真走了楼层判定；stub 故意不给 `desktop_interaction` |
| B 向上跳可见性 | 落点必在目标层可见区域；被遮 → 吸附回可见区；顶部被完全盖住 → 不产生该跳跃 |
| C 向下跳逐层 | 3楼→2楼（不是桌面）；相邻下层是桌面时目标=桌面；落点在楼板下方 |
| D 单真源 | `current_window` 与 `current_floor` 同步；两个换楼板入口都接了同步器；楼层表空→回落 |
| E 新契约 | `adjacent_lower_floor` 3→2→1（桌面返回 None）；`nearest_visible_point` 吸附语义 |
| F 源码不变量 | `start_jump` 签名；`check_nearby_windows` 走 `_nearest_floor_jump`；落点不再反推 |
| G 落地 | 起因→动画（行为级）；落地结算 / z 序 / land 动画；驱动真实 `handle_jump` 完成帧 |

### 7.2 G2 全量

```
改前（18 套件挂入之前）：PASS=655 FAIL=0  套件=17
改后：                    PASS=697 FAIL=0  套件=18   全部 IDENTICAL
```

### 7.3 老 stub 又一次炸了（教训第二次重现）
`verify_round8_floor.py` 的 `PetStub` 缺 `_sync_window_cache_from_floor` → `AttributeError`；
补上后 `start_falling` 又因签名多了 `reason` → `TypeError`。
**这与第十三轮"第八轮套件补 `_apply_pet_z_order` 桩"是同一个教训**：生产代码的
方法表面一变，所有 stub 都要跟。本轮除了补，还把 W6 **加严**：新增 W6b 断言
"关窗必须带 `reason='floor_removed'`"，把新要求锁进回归（16 项 → 17 项，属有意变更）。

### 7.4 证据编码
本轮 `_evidence/` 3 份文件经第十三轮新增的 `check_evidence_encoding.py` 校验：
**确定性损坏 0 / 告警 0**，全部合法 UTF-8、无 BOM。

---

## 八、遗留 / 待办

1. **真机看效果**（只能用户确认）：上下动（走到楼板边缘能逐层往下；贴着楼板边能跳上
   更高的可见悬崖边；不会跳到被挡住的部分）＋ 落地（落到窗口楼层有落地动作；关窗
   掉下来用生气动画 ≥5s）。沙箱里窗口是离屏非置顶窗，被遮挡/穿透时鼠标事件打不到。
2. **旧逻辑仍留在 `check_nearby_windows` 尾部**：楼层不可用时的兜底路径。它包含
   "站在窗口底边直接跳桌面"那条穿透老路 —— 只在"一个桌面窗口都没有"时才会走到，
   可接受；但**彻底删掉**需要先确认没有别的依赖。建议下一轮做"裸窗口矩形路径清除"。
3. **`is_on_floor_edge` / `find_support_below` 仍是零调用**（第十三轮遗留）。
   目前"走到边缘掉下去"由 `get_current_floor` 重判 + 重力掉落覆盖，功能上不缺；
   要么接进"边缘判定"，要么删掉 —— 不能让新死代码继续积。
4. **H4/H5 架构改造**（用户排期"单独做"）仍不在本轮范围。本轮又清掉一块绊脚石：
   `current_window` 从"第二真源"降级为派生缓存，`main.py` 少了一份重复实现。
5. **历史证据编码 70 个文件**（第五/六/八/九轮 + `架构改造-H4H5/`）：第十三轮报告
   §十第 5 条仍在等用户点头（BOM 可机械剥，29 个真乱码不该猜着改）。
